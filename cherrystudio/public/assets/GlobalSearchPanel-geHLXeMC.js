import { c as __toESM } from "./rolldown-runtime-D8OvLAZx.js";
import { t as loggerService } from "./LoggerService-ChVOAPl8.js";
import "./error-CskkREu_.js";
import "./PreferenceService-CvpJqJd7.js";
import "./Model-CThI07dL.js";
import "./aiGeneration-OaAly3Fj.js";
import "./platform-fGkkNTU9.js";
import "./dataApiDevtools-Bm4JBknU.js";
import { t as require_dayjs_min } from "./dayjs.min-BBb2vAs7.js";
import "./resolver-Bn-i1elC.js";
import "./i18next-CqNcSVOM.js";
import { t as require_react } from "./react-C1DTAr29.js";
import "./Trans-pez8bY7I.js";
import { t as useTranslation } from "./useTranslation-DRFkwCLq.js";
import "./react-dom-D5lMhlFn.js";
import { n as usePreference } from "./usePreference-DRNUEk4I.js";
import { n as UiDataSlot } from "./utils-Bnoyl1me.js";
import { t as require_jsx_runtime } from "./jsx-runtime-Cc0-uZGc.js";
import "./shim-2TzZzGeK.js";
import { f as DropdownMenuTrigger, o as DropdownMenuRadioGroup, r as DropdownMenuContent, s as DropdownMenuRadioItem, t as DropdownMenu } from "./dropdown-menu-CVGezg_T.js";
import { t as Button } from "./button-BBhIgYp8.js";
import "./dropzone-ipDj8md_.js";
import "./react-error-boundary-CnvE_Ln4.js";
import "./es2015-wfS3L7va.js";
import { t as Input } from "./input-DWk3xPNN.js";
import "./EmojiPicker-CTF843Lc.js";
import { n as defaultRangeExtractor } from "./esm-Dxo6iURQ.js";
import "./esm-BG45amY4.js";
import "./dist-DAWIImZF.js";
import "./w3c-keyname-DKIRohbk.js";
import "./diff-KPuWBc9d.js";
import "./with-selector-DlsRhNV6.js";
import "./chunk-BO2N2NFS-fKJ-DR8B.js";
import "./extend-BDo4rIBl.js";
import "./marked.esm-5kn1DqGn.js";
import "./dist-Bsd0pttl.js";
import "./dist-oTn6Mzbo.js";
import "./sortable.esm-TnTcc6bt.js";
import { n as KbdGroup, t as Kbd } from "./kbd-Bk40TDX7.js";
import { t as SegmentedControl } from "./segmented-control-BPwDi61u.js";
import "./useCodeStyle-hWHmowmT.js";
import "./useTheme-mM6gKAcD.js";
import "./shiki-g8Tr0-KC.js";
import "./command-CyHQabGE.js";
import "./command-B9oiyMDG.js";
import "./useCloseBeforeAction-DvYaYpLC.js";
import "./ipc-DpcwPFwy.js";
import "./ErrorBoundary-CrY8yFbh.js";
import { i as useTabs } from "./tab-CVOgL8bf.js";
import "./useWindowInitData-BzKs7Qq9.js";
import "./mainWindowNavigation-Dpzd_Ttr.js";
import { n as cn } from "./style-BQVh98fR.js";
import "./CacheService-IyXh62g9.js";
import { n as usePersistCache } from "./useCache-lF6Sw_ck.js";
import "./file-OKCzlHoD.js";
import "./file-CkrjUGO_.js";
import "./model-BOGgSmTN.js";
import "./aiSdk-C-csCQz7.js";
import "./message-CtGMR9KJ.js";
import { t as dataApiService } from "./DataApiService-Csr1w5Kb.js";
import { a as useInvalidateCache, c as useQuery, r as useInfiniteFlatItems } from "./useDataApi-H7ZhyZ_J.js";
import "./useReorder-Cmki1Ln4.js";
import { t as toast } from "./toast-D2efAzAF.js";
import { t as ArrowRight } from "./arrow-right-QZWv3bkd.js";
import { t as Bot } from "./bot-CmbvEihZ.js";
import { t as ChevronDown } from "./chevron-down-CZ6Nbooa.js";
import { t as Clock3 } from "./clock-3-DYbzXsQ9.js";
import { t as CornerDownLeft } from "./corner-down-left-DABdFZ4l.js";
import { t as ExternalLink } from "./external-link-BNegULD7.js";
import { t as FileSearch } from "./file-search-C7yEl-oU.js";
import { t as MessageSquare } from "./message-square-CpgItVh9.js";
import { t as MousePointerClick } from "./mouse-pointer-click-Ce6pSBmG.js";
import { t as Search } from "./search-CafLf998.js";
import { t as Sparkles } from "./sparkles-D4OSBloB.js";
import { t as X } from "./x-CpgfqVTG.js";
import "./popup-fJcKiA2S.js";
import "./mcp-BGt1L-d_.js";
import "./label-CSb71add.js";
import "./systemProviderId-B4QvwwvR.js";
import "./provider-0IejLFjL.js";
import "./naming-BVJSQooI.js";
import "./image-DyuN5EBc.js";
import { c as getDisplayGlobalSearchRecentEntries, d as getMessageSearchSources, i as buildGlobalSearchGroups, l as getGlobalSearchRecentEntryId, n as areGlobalSearchRecentEntriesEqual, p as sanitizeGlobalSearchRecentEntries, r as buildGlobalMessageSearchGroups, t as GLOBAL_SEARCH_MESSAGE_PREVIEW_LIMIT, u as getGlobalSearchTypes } from "./globalSearchGroups-SGTUbIHd.js";
import { n as buildAgentSessionTopicId } from "./agentSession-DEX9ehkG.js";
import "./useSession-CwZkOUrY.js";
import "./conversationEntry-CTSSFR4L.js";
import "./agentRuntimeCapabilities-C7UvdsI7.js";
import "./provider-bQl8RVRp.js";
import "./toolOutput-DFxKz6Jf.js";
import { n as EventEmitter, t as EVENT_NAMES } from "./EventService-CMzpRLnw.js";
import "./uiParts-ClY38h-2.js";
import { r as mapApiTopicToRendererTopic } from "./useTopic-DcX9oNzM.js";
import { t as EmojiIcon_default } from "./EmojiIcon-oUJ-cQSb.js";
import "./with-selector-DFVq3c2Y.js";
import "./filePreview-CGUDTTTP.js";
import "./filePath-SVkx1Fle.js";
import "./constants-BF9E3O0o.js";
import { s as toMessageListItem } from "./messageListItem-DqE378fB.js";
import "./useModel-kU1hKaYa.js";
import "./useProvider-DvntuFRN.js";
import "./capabilities-DNV_QUgI.js";
import "./model-BCwc3I-J.js";
import "./useAssistant-DKYAwKxt.js";
import "./useTopicStreamStatus-Be-jC8fI.js";
import "./ModelAvatar-Dlf3Jj6Z.js";
import "./useTimer-CtD1TRRj.js";
import "./cocoAgent-Dkf7ljK5.js";
import "./useAgent-9Zqveu03.js";
import "./tokenView-yvehtvx6.js";
import "./file-B-bjOfog.js";
import "./composerTokenPolicy-BR1gNZTA.js";
import "./BracesVariableIcon-zDcWRqre.js";
import "./FallbackFavicon-Cm9QCLm8.js";
import "./ImagePreviewService-B71uVdEp.js";
import "./__vite-browser-external-DlQ9XxoO.js";
import "./useSkills-Be92aMP5.js";
import "./group-Cqj37oRb.js";
import "./knowledge-8Me8AnnG.js";
import "./agent-e68nHjCJ.js";
import "./ToolBlockGroup-DmvbWNSS.js";
import "./MessagePartsContext-Be-3g0sl.js";
import "./ScrollOwnershipContext-Cz9Tjpos.js";
import "./iconify-C6gOqm1j.js";
import "./SvgIcon-DI_kUaAV.js";
import "./CodeViewer-DvhZTSjd.js";
import "./CopyIcon-XfJhVEu-.js";
import "./useTemporaryValue-CJVn11gD.js";
import "./ImageViewer-B8XAqqwe.js";
import "./useMcpServer-BA8oDtqe.js";
import "./Link-0PkeiIUY.js";
import "./MarqueeText-DgzQ8dVh.js";
import { t as toAgentSessionUIMessage } from "./useAgentSessionParts-CXWIKKtJ.js";
import { t as useConversationHistoryQuery } from "./useConversationHistoryQuery-C8JSfBsH.js";
import "./useWindowFrame-BNKTfWS3.js";
import "./EditDialogShared-jA4mvNoE.js";
import "./citations-B4d_KMap.js";
import "./citation-BDKmyu_p.js";
import "./find-Bek5EVkJ.js";
import { t as ResourceEditDialogHost } from "./edit-BgB-A0ar.js";
import "./ModelSelector-DGMGvnR1.js";
import "./resourceCatalog-BJgI2SG2.js";
import { i as DynamicVirtualList_default, n as GroupedVirtualList_default } from "./VirtualList-2tuBY6WI.js";
import "./contextSettings-CKgBWQdg.js";
import "./agent-IxO6TDCv.js";
import "./useKnowledgeBase-DfgLPgtw.js";
import "./providerSettings-DJmmyeTu.js";
import "./useMcpRuntimeStatus-Ctv5GtW6.js";
import "./CreateGroupDialog-pRoHlnCL.js";
import { t as useConversationNavigation } from "./useConversationNavigation-DfhrEROd.js";
import "./HtmlArtifactPopupContext-DpPKhD97.js";
import { a as MessageContent_default } from "./types-DmDsFD-i.js";
import "./composerTokens-Dtpcx_hI.js";
import "./useSmoothStream-DhnK2rGz.js";
import { t as MessageContentProvider } from "./MessageContentProvider-Dax83t0p.js";
import { t as TopicType } from "./topic-DTaas9k2.js";
import { n as sharedMessageToUIMessage, r as uiMessagesToPartsMap } from "./messageProjection-BWf8l1I6.js";
import { n as formatRelativeTime } from "./time-BzKBmfSx.js";
function emitResourceListReveal(payload) {
	const emit = () => {
		EventEmitter.emit(EVENT_NAMES.REVEAL_ACTIVE_RESOURCE_LIST, payload);
	};
	if (typeof window === "undefined") {
		emit();
		return;
	}
	if (window.requestAnimationFrame) {
		window.requestAnimationFrame(emit);
		return;
	}
	window.setTimeout(emit, 0);
}
var import_react = /* @__PURE__ */ __toESM(require_react());
function escapeRegex(text) {
	return text.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function splitKeywordsToTerms(keywords) {
	const input = (keywords || "").trim();
	if (input.length === 0) return [];
	const terms = [];
	const pattern = /"([^"]*)"?|'([^']*)'?|(\S+)/g;
	let match;
	while ((match = pattern.exec(input)) !== null) {
		const term = (match[1] ?? match[2] ?? match[3]).trim();
		if (term.length > 0) terms.push(term.toLowerCase());
	}
	return terms;
}
function containsCJK(text) {
	return /[\u4e00-\u9fff\u3400-\u4dbf\u3040-\u309f\u30a0-\u30ff\uac00-\ud7af]/.test(text);
}
function buildWholeWordPattern(escapedTerm) {
	if (containsCJK(escapedTerm)) return escapedTerm;
	return `(?<![\\p{L}\\p{N}])${escapedTerm}(?![\\p{L}\\p{N}])`;
}
function addRegexFlag(flags, flag) {
	return flags.includes(flag) ? flags : `${flags}${flag}`;
}
function buildKeywordPattern(term, matchMode) {
	const escaped = escapeRegex(term);
	return matchMode === "whole-word" ? buildWholeWordPattern(escaped) : escaped;
}
function buildKeywordRegex(term, options) {
	const flags = options.flags ?? "i";
	const normalizedFlags = options.matchMode === "whole-word" ? addRegexFlag(flags, "u") : flags;
	return new RegExp(buildKeywordPattern(term, options.matchMode), normalizedFlags);
}
function buildKeywordRegexes(terms, options) {
	return terms.filter((term) => term.length > 0).map((term) => buildKeywordRegex(term, options));
}
var import_jsx_runtime = /* @__PURE__ */ __toESM(require_jsx_runtime());
var PREVIEW_PAGE_SIZE = 50;
var PREVIEW_MESSAGE_ESTIMATED_HEIGHT = 240;
var PREVIEW_MESSAGE_OVERSCAN = 3;
var LOAD_OLDER_SCROLL_THRESHOLD = 200;
var PREVIEW_MATCH_MODE = "substring";
var HIGHLIGHT_MARK_SELECTOR = "mark[data-global-search-preview-highlight=\"true\"]";
var MESSAGE_BODY_SELECTOR = "[data-global-search-preview-message-body=\"true\"]";
var TEXT_NODE_PARENT_SKIP_SELECTOR = "script, style, svg, mark";
function getPreviewTopic(target) {
	if (target.sourceType === "topic") return {
		id: target.topicId,
		assistantId: target.assistantId ?? "",
		name: target.title,
		lastActivityAt: target.updatedAt ?? target.createdAt ?? "",
		createdAt: target.createdAt ?? "",
		updatedAt: target.updatedAt ?? target.createdAt ?? "",
		messages: []
	};
	return {
		id: buildAgentSessionTopicId(target.sessionId),
		type: TopicType.Session,
		assistantId: target.agentId ?? "",
		name: target.title,
		lastActivityAt: target.createdAt ?? "",
		createdAt: target.createdAt ?? "",
		updatedAt: target.createdAt ?? "",
		messages: []
	};
}
function getTargetMessageType(target) {
	return target.sourceType === "topic" ? "globalSearch.messageSearch.sources.topic" : "globalSearch.messageSearch.sources.session";
}
function getMessageRoleLabelKey$1(role) {
	switch (role) {
		case "assistant": return "globalSearch.messageSearch.roles.assistant";
		case "system": return "globalSearch.messageSearch.roles.system";
		case "tool": return "globalSearch.messageSearch.roles.tool";
		default: return "globalSearch.messageSearch.roles.user";
	}
}
function unwrapPreviewHighlights(root) {
	const marks = Array.from(root.querySelectorAll(HIGHLIGHT_MARK_SELECTOR));
	for (const mark of marks) {
		const parent = mark.parentNode;
		if (!parent) continue;
		mark.replaceWith(document.createTextNode(mark.textContent ?? ""));
		parent.normalize();
	}
}
function highlightTextNode(textNode, regex) {
	const text = textNode.nodeValue ?? "";
	regex.lastIndex = 0;
	if (!regex.test(text)) return;
	regex.lastIndex = 0;
	const fragment = document.createDocumentFragment();
	let cursor = 0;
	let match;
	while ((match = regex.exec(text)) !== null) {
		if (match.index > cursor) fragment.append(document.createTextNode(text.slice(cursor, match.index)));
		const mark = document.createElement("mark");
		mark.dataset.globalSearchPreviewHighlight = "true";
		mark.className = "rounded-[3px] [background-color:var(--highlight-accent)] px-0.5 [color:var(--highlight-foreground)]";
		mark.textContent = match[0];
		fragment.append(mark);
		cursor = match.index + match[0].length;
		if (match[0].length === 0) regex.lastIndex += 1;
	}
	if (cursor < text.length) fragment.append(document.createTextNode(text.slice(cursor)));
	textNode.replaceWith(fragment);
}
function highlightPreviewMatches(root, regex) {
	const walker = document.createTreeWalker(root, NodeFilter.SHOW_TEXT, { acceptNode(node$1) {
		const parent = node$1.parentElement;
		if (!parent || parent.closest(TEXT_NODE_PARENT_SKIP_SELECTOR)) return NodeFilter.FILTER_REJECT;
		if (!parent.closest(MESSAGE_BODY_SELECTOR)) return NodeFilter.FILTER_REJECT;
		if (!node$1.nodeValue?.trim()) return NodeFilter.FILTER_REJECT;
		return NodeFilter.FILTER_ACCEPT;
	} });
	const textNodes = [];
	let node = walker.nextNode();
	while (node) {
		textNodes.push(node);
		node = walker.nextNode();
	}
	for (const textNode of textNodes) highlightTextNode(textNode, regex);
}
function buildPreviewHighlightRegex(searchQuery) {
	const regexes = buildKeywordRegexes(splitKeywordsToTerms(searchQuery), {
		matchMode: PREVIEW_MATCH_MODE,
		flags: "gi"
	});
	if (regexes.length === 0) return void 0;
	return new RegExp(regexes.map((regex) => `(?:${regex.source})`).join("|"), regexes[0].flags);
}
function PreviewMessageRow({ message, highlightRegex, isActive, roleLabel, onOpen }) {
	const bodyRef = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		const body = bodyRef.current;
		if (!body) return;
		unwrapPreviewHighlights(body);
		if (highlightRegex) highlightPreviewMatches(body, highlightRegex);
	}, [highlightRegex, message]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "ui.preview-message-row",
		className: "pb-4",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			"data-ui": "ui.preview-message-row.button",
			id: `global-search-preview-message-${message.id}`,
			role: "button",
			tabIndex: 0,
			onClick: () => onOpen(message.id),
			onKeyDown: (event) => {
				if (event.key !== "Enter" && event.key !== " ") return;
				event.preventDefault();
				onOpen(message.id);
			},
			className: cn("-mx-3 w-[calc(100%+1.5rem)] cursor-pointer rounded-xl border border-transparent px-3 py-2 text-left transition-colors hover:bg-muted/35 focus-visible:bg-muted/35 focus-visible:outline-none", isActive && "border-border-selected bg-accent/55"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "mb-1 font-medium text-muted-foreground text-xs",
				children: roleLabel
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				ref: bodyRef,
				"data-global-search-preview-message-body": "true",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageContent_default, { message })
			})]
		})
	});
}
function estimatePreviewMessageSize() {
	return PREVIEW_MESSAGE_ESTIMATED_HEIGHT;
}
function GlobalSearchMessagePreviewPanel({ className, searchQuery, target, onClose, onOpenMessage }) {
	const { t } = useTranslation();
	const virtualListRef = (0, import_react.useRef)(null);
	const scrollContainerRef = (0, import_react.useRef)(null);
	const pendingOlderAnchorRef = (0, import_react.useRef)(null);
	const scrolledActiveMessageIdRef = (0, import_react.useRef)(null);
	const [activeMessageId, setActiveMessageId] = (0, import_react.useState)(target.messageId);
	const topicId = target.sourceType === "topic" ? target.topicId : "";
	const sessionId = target.sourceType === "session" ? target.sessionId : "";
	const { pages: topicPages, isLoading: isTopicLoading, isRefreshing: isTopicRefreshing, error: topicError, hasNext: hasNextTopicPage, loadNext: loadNextTopicPage } = useConversationHistoryQuery("/topics/:topicId/messages", {
		params: { topicId },
		query: {
			includeSiblings: false,
			nodeId: target.sourceType === "topic" ? target.messageId : void 0
		},
		limit: PREVIEW_PAGE_SIZE,
		enabled: target.sourceType === "topic"
	});
	const { pages: sessionPages, isLoading: isSessionLoading, isRefreshing: isSessionRefreshing, error: sessionError, hasNext: hasNextSessionPage, loadNext: loadNextSessionPage } = useConversationHistoryQuery("/agent-sessions/:sessionId/messages", {
		params: { sessionId },
		query: {
			messageId: target.sourceType === "session" ? target.messageId : void 0,
			deferToolOutputs: true
		},
		limit: PREVIEW_PAGE_SIZE,
		enabled: target.sourceType === "session"
	});
	const topicBranchItems = useInfiniteFlatItems(topicPages, { reversePages: true });
	const sessionRows = useInfiniteFlatItems(sessionPages, {
		reversePages: true,
		reverseItems: true
	});
	const messages = (0, import_react.useMemo)(() => {
		if (target.sourceType === "topic") return topicBranchItems.map((item) => sharedMessageToUIMessage(item.message));
		return sessionRows.map(toAgentSessionUIMessage);
	}, [
		sessionRows,
		target.sourceType,
		topicBranchItems
	]);
	const partsByMessageId = (0, import_react.useMemo)(() => uiMessagesToPartsMap(messages), [messages]);
	const previewTopic = (0, import_react.useMemo)(() => getPreviewTopic(target), [target]);
	const messageItems = (0, import_react.useMemo)(() => messages.map((message) => toMessageListItem(message, {
		assistantId: target.sourceType === "topic" ? target.assistantId : target.agentId,
		topicId: previewTopic.id
	})), [
		messages,
		previewTopic.id,
		target
	]);
	const isLoading = target.sourceType === "topic" ? isTopicLoading : isSessionLoading;
	const isLoadingMore = target.sourceType === "topic" ? isTopicRefreshing && messages.length > 0 : isSessionRefreshing && messages.length > 0;
	const error = target.sourceType === "topic" ? topicError : sessionError;
	const hasMoreOlder = target.sourceType === "topic" ? hasNextTopicPage : hasNextSessionPage;
	const loadOlder = target.sourceType === "topic" ? loadNextTopicPage : loadNextSessionPage;
	const highlightRegex = (0, import_react.useMemo)(() => buildPreviewHighlightRegex(searchQuery), [searchQuery]);
	const activeMessageIndex = (0, import_react.useMemo)(() => messageItems.findIndex((message) => message.id === activeMessageId), [activeMessageId, messageItems]);
	const extractPreviewRange = (0, import_react.useCallback)((range) => {
		const indexes = defaultRangeExtractor(range);
		if (activeMessageIndex < 0 || indexes.includes(activeMessageIndex)) return indexes;
		return [...indexes, activeMessageIndex].sort((left, right) => left - right);
	}, [activeMessageIndex]);
	const getMessageKey = (0, import_react.useCallback)((index) => messageItems[index].id, [messageItems]);
	const handleScroll = (0, import_react.useCallback)(() => {
		const container = scrollContainerRef.current;
		if (!container || !hasMoreOlder) return;
		if (pendingOlderAnchorRef.current !== null) return;
		if (container.scrollTop > LOAD_OLDER_SCROLL_THRESHOLD) return;
		pendingOlderAnchorRef.current = container.scrollHeight - container.scrollTop;
		loadOlder();
	}, [hasMoreOlder, loadOlder]);
	(0, import_react.useLayoutEffect)(() => {
		const container = scrollContainerRef.current;
		const previousDistanceFromBottom = pendingOlderAnchorRef.current;
		if (!container || previousDistanceFromBottom === null) return;
		container.scrollTop = container.scrollHeight - previousDistanceFromBottom;
		if (!isLoadingMore) pendingOlderAnchorRef.current = null;
	}, [isLoadingMore, messages]);
	(0, import_react.useEffect)(() => {
		setActiveMessageId(target.messageId);
	}, [target.messageId]);
	const openPreviewMessage = (0, import_react.useCallback)((messageId) => {
		setActiveMessageId(messageId);
		window.requestAnimationFrame(() => {
			onOpenMessage(messageId);
		});
	}, [onOpenMessage]);
	(0, import_react.useEffect)(() => {
		if (scrolledActiveMessageIdRef.current === activeMessageId) return;
		if (activeMessageIndex < 0) return;
		scrolledActiveMessageIdRef.current = activeMessageId;
		virtualListRef.current?.scrollToIndex(activeMessageIndex, { align: "center" });
		const frame = window.requestAnimationFrame(() => {
			const element = document.getElementById(`global-search-preview-message-${activeMessageId}`);
			const scrollTarget = element?.querySelector(`${MESSAGE_BODY_SELECTOR} ${HIGHLIGHT_MARK_SELECTOR}`) ?? element;
			if (typeof scrollTarget?.scrollIntoView !== "function") return;
			scrollTarget.scrollIntoView({ block: "center" });
		});
		return () => window.cancelAnimationFrame(frame);
	}, [activeMessageId, activeMessageIndex]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("aside", {
		"data-ui": "ui.global-search-message-preview",
		className: cn("relative flex min-h-0 flex-col bg-background", className),
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex h-14 shrink-0 items-center gap-3 border-border-subtle border-b px-5",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "flex size-8 shrink-0 items-center justify-center rounded-full bg-muted/50 text-muted-foreground",
						children: target.sourceType === "topic" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageSquare, { className: "size-4" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MousePointerClick, { className: "size-4" })
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "min-w-0 flex-1",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "truncate font-medium text-foreground text-sm",
							children: target.title || t("common.unnamed")
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "text-muted-foreground text-xs",
							children: t(getTargetMessageType(target))
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						"data-ui": "ui.global-search-message-preview.action.close",
						type: "button",
						"aria-label": t("common.close"),
						onClick: onClose,
						className: "flex size-8 shrink-0 items-center justify-center rounded-full text-muted-foreground transition-colors hover:bg-muted/60 hover:text-foreground",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { className: "size-4" })
					})
				]
			}),
			isLoading && messages.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "min-h-0 flex-1 px-6 pt-5 pb-20",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex h-full items-center justify-center text-muted-foreground text-sm",
					children: t("common.loading")
				})
			}) : error && messageItems.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "min-h-0 flex-1 px-6 pt-5 pb-20",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex h-full items-center justify-center text-muted-foreground text-sm",
					children: t("globalSearch.error")
				})
			}) : messageItems.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "min-h-0 flex-1 px-6 pt-5 pb-20",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex h-full items-center justify-center text-muted-foreground text-sm",
					children: t("common.no_results")
				})
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageContentProvider, {
				messages: messageItems,
				partsByMessageId,
				topic: previewTopic,
				renderConfig: {
					narrowMode: false,
					showMessageOutline: false
				},
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DynamicVirtualList_default, {
					ref: virtualListRef,
					list: messageItems,
					estimateSize: estimatePreviewMessageSize,
					getItemKey: getMessageKey,
					overscan: PREVIEW_MESSAGE_OVERSCAN,
					rangeExtractor: extractPreviewRange,
					scrollElementRef: scrollContainerRef,
					onScroll: handleScroll,
					className: "min-h-0 flex-1 overflow-y-auto px-6 pt-5 pb-20",
					header: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [error && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						"data-ui": "ui.global-search-message-preview.alert",
						role: "alert",
						className: "mb-4 rounded-lg border border-error-border bg-error-subtle px-3 py-2 text-error-subtle-foreground text-xs leading-4",
						children: t("globalSearch.error")
					}), isLoadingMore && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "py-2 text-center text-muted-foreground text-xs",
						children: t("common.loading")
					})] }),
					children: (message) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PreviewMessageRow, {
						message,
						highlightRegex,
						isActive: message.id === activeMessageId,
						roleLabel: t(getMessageRoleLabelKey$1(message.role)),
						onOpen: openPreviewMessage
					})
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "pointer-events-none absolute right-0 bottom-0 left-0 flex justify-center bg-gradient-to-t from-background via-background/90 to-transparent px-4 pt-8 pb-4",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
					type: "button",
					variant: "ghost",
					className: "pointer-events-auto h-8 gap-1.5 rounded-full border border-border-subtle bg-background/95 px-3 font-medium text-xs shadow-sm hover:bg-muted/70",
					onClick: () => openPreviewMessage(activeMessageId),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ExternalLink, { className: "size-3.5" }), t("common.open")]
				})
			})
		]
	});
}
var HighlightText = ({ text, keyword, caseSensitive = false, className }) => {
	const highlightedText = (0, import_react.useMemo)(() => {
		if (!keyword || !text) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			"data-ui": "ui.highlight-text",
			children: text
		});
		const escapedKeyword = keyword.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
		const flags = caseSensitive ? "g" : "gi";
		const regex = new RegExp(`(${escapedKeyword})`, flags);
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: text.split(regex).map((part, index) => {
			const isMatch = regex.test(part);
			regex.lastIndex = 0;
			if (isMatch) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("mark", {
				"data-ui": "ui.highlight-text",
				children: part
			}, index);
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				"data-ui": "ui.highlight-text",
				children: part
			}, index);
		}) });
	}, [
		text,
		keyword,
		caseSensitive
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		"data-ui": "ui.highlight-text",
		className,
		children: highlightedText
	});
};
var HighlightText_default = (0, import_react.memo)(HighlightText);
function getGlobalSearchFooterItemId(groupId, footer) {
	return `footer:${groupId}:${footer.kind}`;
}
const GLOBAL_MESSAGE_SEARCH_LOAD_MORE_ITEM_ID = "message-search:load-more";
function getGlobalSearchOptionDomId(itemId) {
	return `global-search-option-${encodeURIComponent(itemId)}`;
}
function useGlobalSearchKeyboard({ groups, hasMoreMessageResults, isMessageSearchMode, messageGroups, panelMode }) {
	const [activeItemId, setActiveItemId] = (0, import_react.useState)();
	const selectableItems = (0, import_react.useMemo)(() => {
		if (panelMode !== "search") return [];
		return groups.flatMap((group) => [...group.items.filter((item) => item.kind !== "message-parent"), ...group.footer ? [{
			kind: "footer",
			id: getGlobalSearchFooterItemId(group.id, group.footer),
			footer: group.footer
		}] : []]);
	}, [groups, panelMode]);
	const messageSelectableItems = (0, import_react.useMemo)(() => messageGroups.flatMap((group) => group.items), [messageGroups]);
	const messageKeyboardItems = (0, import_react.useMemo)(() => hasMoreMessageResults ? [...messageSelectableItems, {
		kind: "message-load-more",
		id: GLOBAL_MESSAGE_SEARCH_LOAD_MORE_ITEM_ID
	}] : messageSelectableItems, [hasMoreMessageResults, messageSelectableItems]);
	const keyboardItems = isMessageSearchMode ? messageKeyboardItems : selectableItems;
	(0, import_react.useEffect)(() => {
		if (keyboardItems.length === 0) {
			setActiveItemId(void 0);
			return;
		}
		setActiveItemId((current) => current && keyboardItems.some((item) => item.id === current) ? current : keyboardItems[0].id);
	}, [keyboardItems]);
	return {
		activeItemId,
		keyboardItems,
		messageSelectableItems,
		moveActiveItem: (0, import_react.useCallback)((direction) => {
			if (keyboardItems.length === 0) return;
			setActiveItemId(keyboardItems[(Math.max(0, keyboardItems.findIndex((item) => item.id === activeItemId)) + direction + keyboardItems.length) % keyboardItems.length].id);
		}, [activeItemId, keyboardItems]),
		selectableItems,
		setActiveItemId
	};
}
var RESULT_ICONS = {
	topic: MessageSquare,
	session: MousePointerClick,
	assistant: Sparkles,
	agent: Bot,
	"knowledge-base": FileSearch
};
var RECENT_ICONS = {
	route: Clock3,
	topic: MessageSquare,
	session: MousePointerClick
};
var GROUP_LABEL_KEYS = {
	recent: "globalSearch.groups.recent",
	topic: "globalSearch.groups.topic",
	session: "globalSearch.groups.session",
	message: "globalSearch.groups.message",
	assistant: "globalSearch.groups.assistant",
	agent: "globalSearch.groups.agent",
	"knowledge-base": "globalSearch.groups.knowledge-base"
};
var RESULT_TYPE_LABEL_KEYS = {
	topic: "globalSearch.resultTypes.topic",
	session: "globalSearch.resultTypes.session",
	assistant: "globalSearch.resultTypes.assistant",
	agent: "globalSearch.resultTypes.agent",
	"knowledge-base": "common.knowledge_base"
};
var MESSAGE_ROLE_LABEL_KEYS = {
	assistant: "globalSearch.messageSearch.roles.assistant",
	system: "globalSearch.messageSearch.roles.system",
	user: "globalSearch.messageSearch.roles.user"
};
function getGroupLabelKey(groupId) {
	return GROUP_LABEL_KEYS[groupId];
}
function getResultTypeLabelKey(type) {
	return RESULT_TYPE_LABEL_KEYS[type];
}
function getMessageRoleLabelKey(role) {
	return MESSAGE_ROLE_LABEL_KEYS[role];
}
function getMessageActorLabel(result, t, userName) {
	if (!result.result.role || result.result.role === "user") return userName?.trim() || t(getMessageRoleLabelKey("user"));
	return t(getMessageRoleLabelKey(result.result.role));
}
function getResultSubtitle(result, t) {
	if (result.type === "topic" || result.type === "session") return result.subtitle;
	return result.subtitle ?? t(getResultTypeLabelKey(result.type));
}
function GlobalSearchGroupHeader({ group }) {
	const { t } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "ui.global-search-group-header.presentation",
		role: "presentation",
		className: "flex h-7 items-center gap-1.5 px-5 pt-1 font-medium text-muted-foreground text-sm",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t(getGroupLabelKey(group.id)) }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "·" }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: group.total ?? group.items.length })
		]
	});
}
function GlobalSearchGroupFooter({ active, domId, footer, onMouseEnter, onOpen }) {
	const { t } = useTranslation();
	const label = footer.kind === "expand-results" ? t("globalSearch.showMore", { count: footer.remainingCount }) : t("globalSearch.messageSearch.viewMore");
	const Icon = footer.kind === "expand-results" ? ChevronDown : ArrowRight;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "ui.global-search-group-footer",
		className: "h-9 pt-1",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
			"data-ui": "ui.global-search-group-footer.action.open",
			type: "button",
			id: domId,
			role: "option",
			"aria-selected": active,
			onMouseEnter,
			onClick: onOpen,
			className: cn("mx-5 flex h-8 w-[calc(100%-2.5rem)] items-center gap-1 rounded-lg py-0 pr-3 pl-8 text-left font-medium text-xs transition-colors", active ? "bg-muted/60 text-accent-foreground" : "text-muted-foreground hover:bg-muted/40 hover:text-foreground"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: label }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, { className: "size-4" })]
		})
	});
}
function GlobalSearchRow({ item, active, language, query, onMouseEnter, onOpen }) {
	const { t } = useTranslation();
	const isRecent = item.kind === "recent";
	const title = isRecent ? item.recent.title : item.result.title;
	const subtitle = isRecent ? void 0 : getResultSubtitle(item.result, t);
	const Icon = isRecent ? RECENT_ICONS[item.recent.kind] : RESULT_ICONS[item.result.type];
	const emoji = !isRecent && [
		"assistant",
		"agent",
		"knowledge-base"
	].includes(item.result.type) ? item.result.emoji : void 0;
	const displayTimestamp = isRecent ? void 0 : item.result.lastActivityAt ?? item.result.updatedAt;
	const timestampLabel = displayTimestamp ? formatRelativeTime(displayTimestamp, language) : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
		"data-ui": "ui.global-search-row.option",
		type: "button",
		id: getGlobalSearchOptionDomId(item.id),
		role: "option",
		"aria-selected": active,
		onMouseEnter,
		onClick: onOpen,
		className: cn("mx-5 flex h-[48px] w-[calc(100%-2.5rem)] items-center gap-2.5 rounded-[12px] px-3 text-left transition-colors", active ? "bg-muted/60 text-accent-foreground" : "hover:bg-muted/40"),
		children: [
			emoji ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmojiIcon_default, {
				emoji,
				size: 32,
				fontSize: 15,
				className: "mr-0 bg-muted/50"
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "flex size-8 shrink-0 items-center justify-center rounded-full bg-muted/50 text-muted-foreground",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, { className: "size-4" })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
				className: "min-w-0 flex-1",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "block truncate font-medium text-foreground text-sm leading-5",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HighlightText_default, {
						text: title || t("common.unnamed"),
						keyword: query
					})
				}), subtitle && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "block truncate text-muted-foreground text-xs leading-4",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HighlightText_default, {
						text: subtitle,
						keyword: query
					})
				})]
			}),
			timestampLabel && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "ml-2 shrink-0 text-muted-foreground text-xs leading-4",
				title: displayTimestamp,
				children: timestampLabel
			})
		]
	});
}
function GlobalMessageSearchGroupHeader({ group, inset = "default" }) {
	const { t } = useTranslation();
	const Icon = group.sourceType === "topic" ? MessageSquare : MousePointerClick;
	const sourceLabelKey = group.sourceType === "topic" ? "globalSearch.messageSearch.sources.topic" : "globalSearch.messageSearch.sources.session";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "ui.global-message-search-group.presentation",
		role: "presentation",
		className: cn("flex h-8 items-center gap-2 text-sm", inset === "nested" ? "px-8" : "px-5"),
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "flex size-6 shrink-0 items-center justify-center rounded-full bg-muted/50",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, { className: "size-3.5" })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "min-w-0 flex-1 truncate font-semibold text-foreground",
				children: group.title || t("common.unnamed")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
				className: "ml-2 flex h-5 shrink-0 items-center gap-1 rounded-[6px] bg-muted/40 px-1.5 font-medium text-muted-foreground text-xs",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t(sourceLabelKey) }),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "·" }),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: group.total })
				]
			})
		]
	});
}
function GlobalMessageSearchRow({ active, item, inset = "default", language, query, userName, onMouseEnter, onOpen, onJump }) {
	const { t } = useTranslation();
	const [isJumpActionVisible, setIsJumpActionVisible] = (0, import_react.useState)(false);
	if (item.kind === "more") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "ui.global-message-search-row",
		className: "h-9 pt-1",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
			"data-ui": "ui.global-message-search-row.action.open",
			type: "button",
			id: getGlobalSearchOptionDomId(item.id),
			role: "option",
			"aria-selected": active,
			onMouseEnter,
			onClick: onOpen,
			className: cn("flex h-8 items-center gap-1 rounded-lg py-0 pr-3 pl-8 text-left font-medium text-xs transition-colors", inset === "nested" ? "mx-8 w-[calc(100%-4rem)]" : "mx-5 w-[calc(100%-2.5rem)]", active ? "bg-muted/60 text-accent-foreground" : "text-muted-foreground hover:bg-muted/40 hover:text-foreground"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("globalSearch.messageSearch.more", { count: item.remainingCount }) }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown, { className: "size-4" })]
		})
	});
	const updatedAtLabel = formatRelativeTime(item.result.createdAt, language);
	const actorLabel = getMessageActorLabel(item, t, userName);
	const jumpLabel = t("globalSearch.messageSearch.jumpToMessage");
	const handleJumpClick = (event) => {
		event.preventDefault();
		event.stopPropagation();
		onJump();
	};
	const handleMouseEnter = () => {
		setIsJumpActionVisible(true);
		onMouseEnter();
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "ui.global-message-search-row.option",
		id: getGlobalSearchOptionDomId(item.id),
		role: "option",
		"aria-selected": active,
		onMouseEnter: handleMouseEnter,
		onMouseLeave: () => setIsJumpActionVisible(false),
		onClick: onOpen,
		className: cn("group flex h-11 cursor-pointer items-center gap-2 rounded-[10px] pr-3 pl-8 text-left transition-colors", inset === "nested" ? "mx-8 w-[calc(100%-4rem)]" : "mx-5 w-[calc(100%-2.5rem)]", active ? "bg-muted/60 text-accent-foreground" : "hover:bg-muted/40"),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
			className: "min-w-0 flex-1 truncate text-foreground text-sm leading-5",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "font-medium text-muted-foreground",
					children: actorLabel
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "text-muted-foreground",
					children: ": "
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(HighlightText_default, {
					text: item.result.snippet,
					keyword: query
				})
			]
		}), updatedAtLabel && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
			className: "relative ml-2 flex h-7 min-w-19 shrink-0 items-center justify-end",
			title: item.result.createdAt,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "whitespace-nowrap text-muted-foreground text-xs leading-4 transition-opacity group-focus-within:opacity-0 group-hover:opacity-0",
				children: updatedAtLabel
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				"aria-label": jumpLabel,
				"aria-hidden": !isJumpActionVisible,
				tabIndex: isJumpActionVisible ? 0 : -1,
				title: jumpLabel,
				onClick: handleJumpClick,
				className: "pointer-events-none absolute right-0 flex size-7 items-center justify-center rounded-[7px] text-muted-foreground opacity-0 transition-[background-color,color,opacity] hover:bg-accent hover:text-foreground group-focus-within:pointer-events-auto group-focus-within:opacity-100 group-hover:pointer-events-auto group-hover:opacity-100",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrowRight, { className: "size-4" })
			})]
		})]
	});
}
function GlobalSearchRecentHint({ label, offset }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "ui.global-search-recent-hint",
		className: "pointer-events-none absolute right-5 left-5 text-muted-foreground text-sm",
		style: { top: offset },
		children: label
	});
}
function GlobalSearchState({ label }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "ui.global-search-state",
		className: "flex h-full items-center justify-center text-muted-foreground text-sm",
		children: label
	});
}
var import_dayjs_min = /* @__PURE__ */ __toESM(require_dayjs_min());
function createContentSearchState(baseKey) {
	return {
		baseKey,
		items: [],
		requestedCursors: {},
		nextCursors: {}
	};
}
function getUpdatedAtFromForTimeFilter(filter) {
	if (filter === "any") return void 0;
	switch (filter) {
		case "today": return (0, import_dayjs_min.default)().startOf("day").toISOString();
		case "week": return (0, import_dayjs_min.default)().subtract(7, "day").toISOString();
		case "month": return (0, import_dayjs_min.default)().subtract(1, "month").toISOString();
		case "quarter": return (0, import_dayjs_min.default)().subtract(3, "month").toISOString();
	}
}
function toContentSearchSource(source) {
	return source === "topic" ? "topic-message" : "session-message";
}
function getContentSearchSources({ shouldSearchSessionMessages, shouldSearchTopicMessages }) {
	return [...shouldSearchTopicMessages ? [toContentSearchSource("topic")] : [], ...shouldSearchSessionMessages ? [toContentSearchSource("session")] : []];
}
function getContentSearchStateKey({ deferredQuery, messageSearchLimit, sources, updatedAtFrom }) {
	return JSON.stringify({
		q: deferredQuery,
		sources,
		createdAtFrom: updatedAtFrom,
		limitPerSource: messageSearchLimit
	});
}
function mapContentSearchGroup(group) {
	if (group.sourceType === "topic-message") return group.items.map((item) => ({
		...item,
		sourceType: "topic"
	}));
	return group.items.map((item) => ({
		...item,
		sourceType: "session"
	}));
}
function getMessageSearchResultId(result) {
	return result.sourceType === "topic" ? `topic:${result.topicId}:${result.messageId}` : `session:${result.sessionId}:${result.messageId}`;
}
function getContentSearchNextCursors(groups) {
	return Object.fromEntries(groups.flatMap((group) => group.nextCursor ? [[group.sourceType, group.nextCursor]] : []));
}
function areContentSearchCursorsEqual(a, b) {
	const aEntries = Object.entries(a);
	const bEntries = Object.entries(b);
	return aEntries.length === bEntries.length && aEntries.every(([source, cursor]) => b[source] === cursor);
}
function areMessageSearchItemsEqual(a, b) {
	return a.length === b.length && a.every((item, index) => {
		const next = b[index];
		if (!next) return false;
		if (item.sourceType !== next.sourceType || item.messageId !== next.messageId || item.role !== next.role || item.snippet !== next.snippet || item.createdAt !== next.createdAt) return false;
		if (item.sourceType === "topic") return next.sourceType === "topic" && item.topicId === next.topicId && item.topicName === next.topicName && item.topicAssistantId === next.topicAssistantId && item.topicCreatedAt === next.topicCreatedAt && item.topicUpdatedAt === next.topicUpdatedAt;
		return next.sourceType === "session" && item.sessionId === next.sessionId && item.sessionName === next.sessionName && item.agentId === next.agentId && item.agentName === next.agentName;
	});
}
function useGlobalSearchPanelData({ deferredQuery, expandedMessageParentIds, expandedSearchGroupIds, filter, messageSourceFilter, panelMode, recentItems, timeFilter }) {
	const hasQuery = deferredQuery.length > 0;
	const isMessageSearchMode = panelMode === "message-search";
	const shouldShowGlobalMessagePreview = panelMode === "search" && filter === "all";
	const searchTypes = (0, import_react.useMemo)(() => getGlobalSearchTypes(filter), [filter]);
	const messageSearchSources = (0, import_react.useMemo)(() => getMessageSearchSources(messageSourceFilter), [messageSourceFilter]);
	const shouldSearchTopicMessages = shouldShowGlobalMessagePreview || isMessageSearchMode && messageSearchSources.includes("topic");
	const shouldSearchSessionMessages = shouldShowGlobalMessagePreview || isMessageSearchMode && messageSearchSources.includes("session");
	const updatedAtFrom = (0, import_react.useMemo)(() => getUpdatedAtFromForTimeFilter(timeFilter), [timeFilter]);
	const messageSearchLimit = shouldShowGlobalMessagePreview ? 5 : 50;
	const contentSearchSources = (0, import_react.useMemo)(() => getContentSearchSources({
		shouldSearchSessionMessages,
		shouldSearchTopicMessages
	}), [shouldSearchSessionMessages, shouldSearchTopicMessages]);
	const contentSearchStateKey = (0, import_react.useMemo)(() => getContentSearchStateKey({
		deferredQuery,
		messageSearchLimit,
		sources: contentSearchSources,
		updatedAtFrom
	}), [
		contentSearchSources,
		deferredQuery,
		messageSearchLimit,
		updatedAtFrom
	]);
	const [contentSearchState, setContentSearchState] = (0, import_react.useState)(() => createContentSearchState(contentSearchStateKey));
	const activeContentSearchState = contentSearchState.baseKey === contentSearchStateKey ? contentSearchState : createContentSearchState(contentSearchStateKey);
	const requestedContentSearchCursors = activeContentSearchState.requestedCursors;
	const requestedContentSearchSources = (0, import_react.useMemo)(() => {
		const cursorSources = contentSearchSources.filter((source) => requestedContentSearchCursors[source]);
		return cursorSources.length > 0 ? cursorSources : contentSearchSources;
	}, [contentSearchSources, requestedContentSearchCursors]);
	const contentSearchQuery = (0, import_react.useMemo)(() => ({
		q: deferredQuery,
		sources: requestedContentSearchSources,
		limitPerSource: messageSearchLimit,
		...Object.keys(requestedContentSearchCursors).length > 0 ? { cursors: requestedContentSearchCursors } : {},
		...updatedAtFrom ? { createdAtFrom: updatedAtFrom } : {}
	}), [
		deferredQuery,
		messageSearchLimit,
		requestedContentSearchCursors,
		requestedContentSearchSources,
		updatedAtFrom
	]);
	const searchQuery = (0, import_react.useMemo)(() => ({
		q: deferredQuery,
		types: searchTypes,
		...updatedAtFrom ? { updatedAtFrom } : {}
	}), [
		deferredQuery,
		searchTypes,
		updatedAtFrom
	]);
	(0, import_react.useEffect)(() => {
		setContentSearchState((state) => state.baseKey === contentSearchStateKey ? state : createContentSearchState(contentSearchStateKey));
	}, [contentSearchStateKey]);
	const { data: contentSearchData, isLoading: isContentSearchLoading, isRefreshing: isContentSearchRefreshing, error: contentSearchError } = useQuery("/search/contents", {
		enabled: hasQuery && contentSearchSources.length > 0,
		query: contentSearchQuery,
		swrOptions: { keepPreviousData: false }
	});
	(0, import_react.useEffect)(() => {
		if (!contentSearchData || contentSearchData.query !== deferredQuery) return;
		setContentSearchState((state) => {
			const current = state.baseKey === contentSearchStateKey ? state : createContentSearchState(contentSearchStateKey);
			const itemsById = new Map(current.items.map((item) => [getMessageSearchResultId(item), item]));
			for (const group of contentSearchData.groups) for (const item of mapContentSearchGroup(group)) itemsById.set(getMessageSearchResultId(item), item);
			const nextItems = Array.from(itemsById.values());
			const nextCursors = getContentSearchNextCursors(contentSearchData.groups);
			if (areMessageSearchItemsEqual(current.items, nextItems) && areContentSearchCursorsEqual(current.nextCursors, nextCursors)) return current;
			return {
				...current,
				items: nextItems,
				nextCursors
			};
		});
	}, [
		contentSearchData,
		contentSearchStateKey,
		deferredQuery
	]);
	const hasMoreMessageResults = isMessageSearchMode && Object.keys(activeContentSearchState.nextCursors).length > 0;
	const isLoadingMoreMessageResults = isMessageSearchMode && Object.keys(activeContentSearchState.requestedCursors).length > 0 && isContentSearchRefreshing;
	const isMessageLoading = isMessageSearchMode && activeContentSearchState.items.length === 0 && isContentSearchLoading;
	const messageError = contentSearchError;
	const messageLoadMoreCount = Object.keys(activeContentSearchState.nextCursors).length * messageSearchLimit;
	const loadMoreMessageResults = (0, import_react.useCallback)(() => {
		setContentSearchState((state) => {
			const current = state.baseKey === contentSearchStateKey ? state : createContentSearchState(contentSearchStateKey);
			if (Object.keys(current.nextCursors).length === 0) return current;
			return {
				...current,
				requestedCursors: current.nextCursors
			};
		});
	}, [contentSearchStateKey]);
	const { data, isLoading, error } = useQuery("/search/entities", {
		enabled: hasQuery && panelMode === "search",
		query: searchQuery
	});
	const messageSearchItems = (0, import_react.useMemo)(() => [...activeContentSearchState.items].sort((a, b) => {
		const timeA = (0, import_dayjs_min.default)(a.createdAt).valueOf() || 0;
		const timeB = (0, import_dayjs_min.default)(b.createdAt).valueOf() || 0;
		if (timeA !== timeB) return timeB - timeA;
		if (a.sourceType !== b.sourceType) return a.sourceType === "topic" ? -1 : 1;
		return b.messageId.localeCompare(a.messageId);
	}), [activeContentSearchState.items]);
	const groups = (0, import_react.useMemo)(() => buildGlobalSearchGroups({
		expandedGroupIds: expandedSearchGroupIds,
		messageItems: shouldShowGlobalMessagePreview ? messageSearchItems : [],
		query: deferredQuery,
		filter,
		recentItems: recentItems ?? [],
		response: data
	}), [
		data,
		deferredQuery,
		expandedSearchGroupIds,
		filter,
		messageSearchItems,
		recentItems,
		shouldShowGlobalMessagePreview
	]);
	const messageGroups = (0, import_react.useMemo)(() => buildGlobalMessageSearchGroups({
		expandedParentIds: expandedMessageParentIds,
		items: messageSearchItems
	}), [expandedMessageParentIds, messageSearchItems]);
	const virtualGroups = (0, import_react.useMemo)(() => groups.map((group) => ({
		group,
		header: group,
		items: group.items,
		footer: group.footer
	})), [groups]);
	return {
		error,
		groups,
		hasQuery,
		hasMoreMessageResults,
		isLoading,
		isLoadingMoreMessageResults,
		isMessageLoading,
		isMessageSearchMode,
		loadMoreMessageResults,
		messageError,
		messageGroups,
		messageLoadMoreCount,
		messageVirtualGroups: (0, import_react.useMemo)(() => messageGroups.map((group) => ({
			group,
			header: group,
			items: group.items
		})), [messageGroups]),
		updatedAtFrom,
		virtualGroups
	};
}
var GLOBAL_SEARCH_LISTBOX_ID = "global-search-listbox";
var SEARCH_FILTERS = [
	"topic",
	"session",
	"assistant",
	"agent",
	"knowledge"
];
var MESSAGE_SOURCE_FILTER_BUTTONS = ["topic", "session"];
var SEARCH_SCOPE_CONTROL_CLASS_NAME = "h-7 shrink-0 border-border-subtle bg-muted/40 p-0.5 [&_[role=radio]]:h-6 [&_[role=radio]]:px-2 [&_[role=radio]]:text-xs [&_[role=radio]]:leading-none";
var logger = loggerService.withContext("GlobalSearchPanel");
var RECENT_ITEMS_REFRESH_THROTTLE_MS = 60 * 1e3;
var recentRefreshHistory = /* @__PURE__ */ new Map();
var FILTER_LABEL_KEYS = {
	all: "globalSearch.filters.all",
	topic: "globalSearch.filters.topic",
	session: "globalSearch.filters.session",
	assistant: "globalSearch.filters.assistant",
	agent: "globalSearch.filters.agent",
	knowledge: "globalSearch.filters.knowledge"
};
var MESSAGE_SOURCE_FILTER_LABEL_KEYS = {
	all: "globalSearch.messageSearch.sources.all",
	topic: "globalSearch.messageSearch.sources.topic",
	session: "globalSearch.messageSearch.sources.session"
};
var TIME_FILTERS = [
	"any",
	"today",
	"week",
	"month",
	"quarter"
];
var TIME_FILTER_LABEL_KEYS = {
	any: "globalSearch.timeFilters.any",
	today: "globalSearch.timeFilters.today",
	week: "globalSearch.timeFilters.week",
	month: "globalSearch.timeFilters.month",
	quarter: "globalSearch.timeFilters.quarter"
};
function getFilterLabelKey(filter) {
	return FILTER_LABEL_KEYS[filter];
}
function getTimeFilterLabelKey(filter) {
	return TIME_FILTER_LABEL_KEYS[filter];
}
function getTimeFilterAriaLabelKey(mode) {
	return mode === "message-search" ? "globalSearch.timeFilters.messageLabel" : "globalSearch.timeFilters.label";
}
function getMessageSourceFilterLabelKey(filter) {
	return MESSAGE_SOURCE_FILTER_LABEL_KEYS[filter];
}
function getAssistantTargetId(target) {
	return "assistantId" in target && typeof target.assistantId === "string" ? target.assistantId : void 0;
}
function getAgentTargetId(target) {
	return "agentId" in target && typeof target.agentId === "string" ? target.agentId : void 0;
}
function getTopicTargetId(target) {
	return "topicId" in target && typeof target.topicId === "string" ? target.topicId : void 0;
}
function getSessionTargetId(target) {
	return "sessionId" in target && typeof target.sessionId === "string" ? target.sessionId : void 0;
}
function getKnowledgeBaseTargetId(target) {
	return "knowledgeBaseId" in target && typeof target.knowledgeBaseId === "string" ? target.knowledgeBaseId : void 0;
}
function getMessageSearchResultJumpTarget(result) {
	if (result.sourceType === "topic") return {
		sourceType: "topic",
		topicId: result.topicId,
		messageId: result.messageId
	};
	return {
		sourceType: "session",
		sessionId: result.sessionId,
		messageId: result.messageId
	};
}
function getPreviewMessageJumpTarget(target, messageId) {
	if (target.sourceType === "topic") return {
		sourceType: "topic",
		topicId: target.topicId,
		messageId
	};
	return {
		sourceType: "session",
		sessionId: target.sessionId,
		messageId
	};
}
function getOpenItemLogContext(item) {
	switch (item.kind) {
		case "footer": return {
			itemKind: item.kind,
			footerKind: item.footer.kind
		};
		case "message": return {
			itemKind: item.kind,
			sourceType: item.result.sourceType,
			messageId: item.result.messageId
		};
		case "recent": return {
			itemKind: item.kind,
			recentKind: item.recent.kind
		};
		case "result": return {
			itemKind: item.kind,
			resultType: item.result.type
		};
	}
}
function logOpenFailure(error, context) {
	logger.error("Failed to open global search result", error, context);
}
function emitGlobalSearchSelection(eventName, payload, context) {
	Promise.resolve(EventEmitter.emit(eventName, payload)).catch((error) => {
		logger.error("Failed to emit global search selection event", error, context);
	});
}
function logMissingSelectionTarget(context) {
	logger.warn("Skipped global search selection event without target tab", context);
}
function getGroupedVirtualListRowIndex(groups, itemId, getItemId, getFooterId) {
	let rowIndex = 0;
	for (const entry of groups) {
		if (entry.header !== void 0) rowIndex += 1;
		for (const item of entry.items) {
			if (getItemId(item) === itemId) return rowIndex;
			rowIndex += 1;
		}
		if (entry.footer !== void 0) {
			if (getFooterId?.(entry.group, entry.footer) === itemId) return rowIndex;
			rowIndex += 1;
		}
	}
}
function TimeFilterDropdown({ timeFilter, panelMode, onSelect }) {
	const { t } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DropdownMenu, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropdownMenuTrigger, {
		asChild: true,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
			type: "button",
			variant: "ghost",
			className: "h-7 gap-1.5 rounded-[8px] px-2 font-medium text-muted-foreground text-xs hover:bg-muted/50 hover:text-foreground",
			"aria-label": `${t(getTimeFilterAriaLabelKey(panelMode))}: ${t(getTimeFilterLabelKey(timeFilter))}`,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Clock3, { className: "size-3.5" }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t(getTimeFilterLabelKey(timeFilter)) }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown, { className: "size-3.5" })
			]
		}) })
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropdownMenuContent, {
		align: "start",
		className: "z-[90] min-w-[132px] rounded-[10px] p-1",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropdownMenuRadioGroup, {
			value: timeFilter,
			onValueChange: (value) => onSelect(value),
			children: TIME_FILTERS.map((filterOption) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropdownMenuRadioItem, {
				value: filterOption,
				className: "h-8 rounded-[7px] font-medium text-xs data-[state=checked]:bg-accent data-[state=checked]:text-accent-foreground",
				children: t(getTimeFilterLabelKey(filterOption))
			}, filterOption))
		})
	})] });
}
function GlobalSearchPanel({ onClose }) {
	const { t, i18n } = useTranslation();
	const { openTab } = useTabs();
	const chatNav = useConversationNavigation("assistants");
	const agentNav = useConversationNavigation("agents");
	const invalidateCache = useInvalidateCache();
	const inputRef = (0, import_react.useRef)(null);
	const messageListRef = (0, import_react.useRef)(null);
	const searchListRef = (0, import_react.useRef)(null);
	const [query, setQuery] = (0, import_react.useState)("");
	const [panelMode, setPanelMode] = (0, import_react.useState)("search");
	const deferredQuery = (0, import_react.useDeferredValue)(query.trim());
	const [filter, setFilter] = (0, import_react.useState)("all");
	const [timeFilter, setTimeFilter] = (0, import_react.useState)("any");
	const [messageSourceFilter, setMessageSourceFilter] = (0, import_react.useState)("all");
	const [expandedSearchGroupIds, setExpandedSearchGroupIds] = (0, import_react.useState)(() => /* @__PURE__ */ new Set());
	const [expandedMessageParentIds, setExpandedMessageParentIds] = (0, import_react.useState)(() => /* @__PURE__ */ new Set());
	const [messagePreviewTarget, setMessagePreviewTarget] = (0, import_react.useState)(null);
	const [editDialogTarget, setEditDialogTarget] = (0, import_react.useState)(null);
	const [recentItems, setRecentItems] = usePersistCache("ui.global_search.recent_items");
	const sanitizedRecentItems = (0, import_react.useMemo)(() => sanitizeGlobalSearchRecentEntries(recentItems ?? []), [recentItems]);
	const [userName] = usePreference("app.user.name");
	const { error, groups, hasMoreMessageResults, hasQuery, isLoading, isLoadingMoreMessageResults, isMessageLoading, isMessageSearchMode, loadMoreMessageResults, messageError, messageGroups, messageLoadMoreCount, messageVirtualGroups, updatedAtFrom, virtualGroups } = useGlobalSearchPanelData({
		deferredQuery,
		expandedMessageParentIds,
		expandedSearchGroupIds,
		filter,
		messageSourceFilter,
		panelMode,
		recentItems: sanitizedRecentItems,
		timeFilter
	});
	const { activeItemId, keyboardItems, messageSelectableItems, moveActiveItem, selectableItems, setActiveItemId } = useGlobalSearchKeyboard({
		groups,
		hasMoreMessageResults,
		isMessageSearchMode,
		messageGroups,
		panelMode
	});
	const shouldShowRecentHint = !hasQuery && !isLoading && !error && selectableItems.length > 0 && selectableItems.length < 3;
	const messageVirtualGroupsWithLoadMore = (0, import_react.useMemo)(() => {
		if (!hasMoreMessageResults || messageVirtualGroups.length === 0) return messageVirtualGroups;
		const lastGroupIndex = messageVirtualGroups.length - 1;
		return messageVirtualGroups.map((entry, index) => index === lastGroupIndex ? {
			...entry,
			footer: true
		} : entry);
	}, [hasMoreMessageResults, messageVirtualGroups]);
	(0, import_react.useEffect)(() => {
		inputRef.current?.focus({ preventScroll: true });
	}, []);
	(0, import_react.useEffect)(() => {
		if (!recentItems || sanitizedRecentItems === recentItems) return;
		setRecentItems(sanitizedRecentItems);
	}, [
		recentItems,
		sanitizedRecentItems,
		setRecentItems
	]);
	(0, import_react.useEffect)(() => {
		const refreshable = getDisplayGlobalSearchRecentEntries(sanitizedRecentItems).flatMap((entry) => {
			if (entry.kind === "route") return [];
			return [entry];
		});
		const now = Date.now();
		const due = refreshable.filter((entry) => {
			if (entry.title.trim() === "") return true;
			return now - (recentRefreshHistory.get(getGlobalSearchRecentEntryId(entry)) ?? 0) > RECENT_ITEMS_REFRESH_THROTTLE_MS;
		});
		if (due.length === 0) return;
		let cancelled = false;
		Promise.all(due.map(async (entry) => {
			const refreshKey = getGlobalSearchRecentEntryId(entry);
			try {
				const name = (await dataApiService.get(entry.kind === "topic" ? `/topics/${entry.topicId}` : `/agent-sessions/${entry.sessionId}`))?.name?.trim();
				if (name) {
					recentRefreshHistory.set(refreshKey, Date.now());
					return {
						id: refreshKey,
						name
					};
				}
				recentRefreshHistory.set(refreshKey, Date.now());
				return null;
			} catch (error$1) {
				logger.warn("Failed to refresh recent title", {
					entryKind: entry.kind,
					id: refreshKey,
					error: error$1
				});
				return null;
			}
		})).then((results) => {
			if (cancelled) return;
			const updates = /* @__PURE__ */ new Map();
			for (const result of results) {
				if (!result) continue;
				updates.set(result.id, result.name);
			}
			if (updates.size === 0) return;
			setRecentItems((prev) => {
				const current = [...prev ?? []];
				const next = current.map((entry) => {
					if (entry.kind === "route") return entry;
					const updateName = updates.get(getGlobalSearchRecentEntryId(entry));
					return updateName && entry.title.trim() !== updateName ? {
						...entry,
						title: updateName
					} : entry;
				});
				return next.every((entry, index) => areGlobalSearchRecentEntriesEqual(entry, current[index])) ? current : next;
			});
		});
		return () => {
			cancelled = true;
		};
	}, []);
	(0, import_react.useEffect)(() => {
		setExpandedSearchGroupIds(/* @__PURE__ */ new Set());
		setExpandedMessageParentIds(/* @__PURE__ */ new Set());
		setMessagePreviewTarget(null);
	}, [
		deferredQuery,
		filter,
		updatedAtFrom
	]);
	const handleSearchScopeChange = (0, import_react.useCallback)((nextScope) => {
		setPanelMode(nextScope === "messages" ? "message-search" : "search");
		setMessagePreviewTarget(null);
	}, []);
	const openTopic = (0, import_react.useCallback)(async (topicId) => {
		const topic = mapApiTopicToRendererTopic(await dataApiService.get(`/topics/${topicId}`));
		const targetTabId = chatNav.openConversationTab(topic.id);
		if (!targetTabId) {
			logMissingSelectionTarget({
				eventName: EVENT_NAMES.GLOBAL_SEARCH_SELECT_TOPIC,
				topicId
			});
			onClose();
			return;
		}
		emitResourceListReveal({
			source: "assistants",
			tabId: targetTabId
		});
		window.requestAnimationFrame(() => {
			emitGlobalSearchSelection(EVENT_NAMES.GLOBAL_SEARCH_SELECT_TOPIC, {
				targetTabId,
				topic
			}, {
				eventName: EVENT_NAMES.GLOBAL_SEARCH_SELECT_TOPIC,
				targetTabId,
				topicId
			});
		});
		onClose();
	}, [onClose, chatNav]);
	const openSession = (0, import_react.useCallback)((sessionId) => {
		const targetTabId = agentNav.openConversationTab(sessionId);
		if (!targetTabId) {
			logMissingSelectionTarget({
				eventName: EVENT_NAMES.GLOBAL_SEARCH_SELECT_AGENT_SESSION,
				sessionId
			});
			onClose();
			return;
		}
		emitResourceListReveal({
			source: "agents",
			tabId: targetTabId
		});
		window.requestAnimationFrame(() => {
			emitGlobalSearchSelection(EVENT_NAMES.GLOBAL_SEARCH_SELECT_AGENT_SESSION, {
				sessionId,
				targetTabId
			}, {
				eventName: EVENT_NAMES.GLOBAL_SEARCH_SELECT_AGENT_SESSION,
				sessionId,
				targetTabId
			});
		});
		onClose();
	}, [onClose, agentNav]);
	const openTopicMessageById = (0, import_react.useCallback)(async (topicId, messageId) => {
		const messagePathEndpoint = `/topics/${topicId}/path`;
		const [apiTopic, messagePath] = await Promise.all([dataApiService.get(`/topics/${topicId}`), dataApiService.get(messagePathEndpoint, { query: { nodeId: messageId } })]);
		const activeNodeId = Array.isArray(messagePath) ? messagePath[messagePath.length - 1]?.id ?? messageId : messageId;
		const topic = {
			...mapApiTopicToRendererTopic(apiTopic),
			activeNodeId
		};
		await dataApiService.put(`/topics/${topicId}/active-node`, { body: { nodeId: activeNodeId } });
		await invalidateCache([`/topics/${topicId}/messages`, `/topics/${topicId}/tree`]);
		const targetTabId = chatNav.openConversationTab(topic.id);
		if (!targetTabId) {
			logMissingSelectionTarget({
				eventName: EVENT_NAMES.GLOBAL_SEARCH_SELECT_TOPIC_MESSAGE,
				messageId,
				topicId
			});
			onClose();
			return;
		}
		emitResourceListReveal({
			source: "assistants",
			tabId: targetTabId
		});
		window.requestAnimationFrame(() => {
			emitGlobalSearchSelection(EVENT_NAMES.GLOBAL_SEARCH_SELECT_TOPIC_MESSAGE, {
				messageId,
				targetTabId,
				topic
			}, {
				eventName: EVENT_NAMES.GLOBAL_SEARCH_SELECT_TOPIC_MESSAGE,
				messageId,
				targetTabId,
				topicId
			});
		});
		onClose();
	}, [
		invalidateCache,
		onClose,
		chatNav
	]);
	const openSessionMessageById = (0, import_react.useCallback)(async (sessionId, messageId) => {
		await invalidateCache([
			"/agent-sessions",
			`/agent-sessions/${sessionId}`,
			`/agent-sessions/${sessionId}/messages`
		]);
		const targetTabId = agentNav.openConversationTab(sessionId);
		if (!targetTabId) {
			logMissingSelectionTarget({
				eventName: EVENT_NAMES.GLOBAL_SEARCH_SELECT_AGENT_SESSION_MESSAGE,
				messageId,
				sessionId
			});
			onClose();
			return;
		}
		emitResourceListReveal({
			source: "agents",
			tabId: targetTabId
		});
		window.requestAnimationFrame(() => {
			emitGlobalSearchSelection(EVENT_NAMES.GLOBAL_SEARCH_SELECT_AGENT_SESSION_MESSAGE, {
				messageId,
				sessionId,
				targetTabId
			}, {
				eventName: EVENT_NAMES.GLOBAL_SEARCH_SELECT_AGENT_SESSION_MESSAGE,
				messageId,
				sessionId,
				targetTabId
			});
		});
		onClose();
	}, [
		invalidateCache,
		onClose,
		agentNav
	]);
	const openKnowledgeBase = (0, import_react.useCallback)((knowledgeBaseId) => {
		openTab("/app/knowledge");
		window.requestAnimationFrame(() => {
			emitGlobalSearchSelection(EVENT_NAMES.GLOBAL_SEARCH_SELECT_KNOWLEDGE_BASE, knowledgeBaseId, {
				eventName: EVENT_NAMES.GLOBAL_SEARCH_SELECT_KNOWLEDGE_BASE,
				knowledgeBaseId
			});
		});
		onClose();
	}, [onClose, openTab]);
	const jumpToMessage = (0, import_react.useCallback)((target) => {
		if (target.sourceType === "topic") {
			openTopicMessageById(target.topicId, target.messageId).catch((error$1) => {
				logOpenFailure(error$1, target);
				toast.error(t("globalSearch.open_failed"));
			});
			return;
		}
		openSessionMessageById(target.sessionId, target.messageId).catch((error$1) => {
			logOpenFailure(error$1, target);
			toast.error(t("globalSearch.open_failed"));
		});
	}, [
		openSessionMessageById,
		openTopicMessageById,
		t
	]);
	const openMessagePanelItem = (0, import_react.useCallback)((item) => {
		if (item.kind === "more") {
			setExpandedMessageParentIds((current) => {
				const next = new Set(current);
				next.add(item.parentId);
				return next;
			});
			return;
		}
		if (item.result.sourceType === "topic") {
			setMessagePreviewTarget({
				sourceType: "topic",
				topicId: item.result.topicId,
				title: item.result.topicName,
				messageId: item.result.messageId,
				assistantId: item.result.topicAssistantId,
				createdAt: item.result.topicCreatedAt,
				updatedAt: item.result.topicUpdatedAt
			});
			return;
		}
		setMessagePreviewTarget({
			sourceType: "session",
			sessionId: item.result.sessionId,
			title: item.result.sessionName,
			messageId: item.result.messageId,
			agentId: item.result.agentId,
			createdAt: item.result.createdAt
		});
	}, []);
	const jumpMessagePanelItem = (0, import_react.useCallback)((item) => {
		if (item.kind === "more") {
			openMessagePanelItem(item);
			return;
		}
		jumpToMessage(getMessageSearchResultJumpTarget(item.result));
	}, [jumpToMessage, openMessagePanelItem]);
	const openMessagePreviewMessage = (0, import_react.useCallback)((messageId) => {
		if (!messagePreviewTarget) return;
		jumpToMessage(getPreviewMessageJumpTarget(messagePreviewTarget, messageId));
	}, [jumpToMessage, messagePreviewTarget]);
	const openGlobalSearchFooter = (0, import_react.useCallback)((footer) => {
		if (footer.kind === "expand-results") {
			setExpandedSearchGroupIds((current) => {
				const next = new Set(current);
				next.add(footer.groupId);
				return next;
			});
			return;
		}
		setMessageSourceFilter("all");
		setMessagePreviewTarget(null);
		setPanelMode("message-search");
	}, []);
	const handleLoadMoreMessageResults = (0, import_react.useCallback)(() => {
		if (isLoadingMoreMessageResults) return;
		loadMoreMessageResults();
	}, [isLoadingMoreMessageResults, loadMoreMessageResults]);
	const openPanelItem = (0, import_react.useCallback)(async (item) => {
		try {
			if (item.kind === "footer") {
				openGlobalSearchFooter(item.footer);
				return;
			}
			if (item.kind === "message") {
				setMessageSourceFilter("all");
				setPanelMode("message-search");
				openMessagePanelItem(item);
				return;
			}
			if (item.kind === "recent") switch (item.recent.kind) {
				case "route":
					openTab(item.recent.url, {
						title: item.recent.title,
						icon: item.recent.icon
					});
					onClose();
					return;
				case "topic":
					await openTopic(item.recent.topicId);
					return;
				case "session":
					openSession(item.recent.sessionId);
					return;
			}
			const result = item.result;
			switch (result.type) {
				case "assistant": {
					const assistantId = getAssistantTargetId(result.target);
					if (!assistantId) return;
					setEditDialogTarget({
						kind: "assistant",
						id: assistantId
					});
					return;
				}
				case "agent": {
					const agentId = getAgentTargetId(result.target);
					if (!agentId) return;
					setEditDialogTarget({
						kind: "agent",
						id: agentId
					});
					return;
				}
				case "topic": {
					const topicId = getTopicTargetId(result.target);
					if (!topicId) return;
					await openTopic(topicId);
					return;
				}
				case "session": {
					const sessionId = getSessionTargetId(result.target);
					if (!sessionId) return;
					openSession(sessionId);
					return;
				}
				case "knowledge-base": {
					const knowledgeBaseId = getKnowledgeBaseTargetId(result.target);
					if (!knowledgeBaseId) return;
					openKnowledgeBase(knowledgeBaseId);
					return;
				}
				default: return;
			}
		} catch (error$1) {
			logOpenFailure(error$1, getOpenItemLogContext(item));
			toast.error(t("globalSearch.open_failed"));
		}
	}, [
		onClose,
		openGlobalSearchFooter,
		openKnowledgeBase,
		openMessagePanelItem,
		openSession,
		openTab,
		openTopic,
		t
	]);
	const handleInputKeyDown = (0, import_react.useCallback)((event) => {
		if (event.nativeEvent.isComposing || event.keyCode === 229) return;
		if (event.key === "Escape") {
			event.preventDefault();
			onClose();
			return;
		}
		if (event.key === "ArrowDown") {
			event.preventDefault();
			moveActiveItem(1);
			return;
		}
		if (event.key === "ArrowUp") {
			event.preventDefault();
			moveActiveItem(-1);
			return;
		}
		if (event.key === "Enter") {
			const item = keyboardItems.find((candidate) => candidate.id === activeItemId);
			if (!item) return;
			event.preventDefault();
			if (isMessageSearchMode) {
				if (item.kind === "message-load-more") {
					handleLoadMoreMessageResults();
					return;
				}
				openMessagePanelItem(item);
				return;
			}
			openPanelItem(item);
		}
	}, [
		activeItemId,
		handleLoadMoreMessageResults,
		isMessageSearchMode,
		keyboardItems,
		moveActiveItem,
		onClose,
		openMessagePanelItem,
		openPanelItem
	]);
	const handleFilterSelect = (0, import_react.useCallback)((nextFilter) => {
		setFilter((current) => current === nextFilter ? "all" : nextFilter);
	}, []);
	const handleTimeFilterSelect = (0, import_react.useCallback)((nextFilter) => {
		setTimeFilter(nextFilter);
		setMessagePreviewTarget(null);
	}, []);
	const handleMessageSourceFilterSelect = (0, import_react.useCallback)((nextFilter) => {
		setMessageSourceFilter((current) => current === nextFilter ? "all" : nextFilter);
		setExpandedMessageParentIds(/* @__PURE__ */ new Set());
		setMessagePreviewTarget(null);
	}, []);
	const showEmptyState = !isLoading && !error && selectableItems.length === 0;
	const showMessageEmptyState = !isMessageLoading && !messageError && (hasQuery ? messageSelectableItems.length === 0 : true);
	const showSearchControls = hasQuery || isMessageSearchMode;
	const isSearchListboxVisible = !isMessageSearchMode && !(isLoading && hasQuery) && !error && !showEmptyState;
	const isMessageListboxVisible = isMessageSearchMode && !messagePreviewTarget && !(isMessageLoading && hasQuery) && !messageError && !showMessageEmptyState;
	const isListboxVisible = isSearchListboxVisible || isMessageListboxVisible;
	const activeMessageRowIndex = (0, import_react.useMemo)(() => {
		if (!isMessageListboxVisible || !activeItemId) return void 0;
		return getGroupedVirtualListRowIndex(messageVirtualGroupsWithLoadMore, activeItemId, (item) => item.id, () => GLOBAL_MESSAGE_SEARCH_LOAD_MORE_ITEM_ID);
	}, [
		activeItemId,
		isMessageListboxVisible,
		messageVirtualGroupsWithLoadMore
	]);
	const activeSearchRowIndex = (0, import_react.useMemo)(() => {
		if (!isSearchListboxVisible || !activeItemId) return void 0;
		return getGroupedVirtualListRowIndex(virtualGroups, activeItemId, (item) => item.id, (group, footer) => getGlobalSearchFooterItemId(group.id, footer));
	}, [
		activeItemId,
		isSearchListboxVisible,
		virtualGroups
	]);
	(0, import_react.useEffect)(() => {
		if (activeMessageRowIndex === void 0) return;
		messageListRef.current?.scrollToIndex(activeMessageRowIndex, { align: "auto" });
	}, [activeMessageRowIndex]);
	(0, import_react.useEffect)(() => {
		if (activeSearchRowIndex === void 0) return;
		searchListRef.current?.scrollToIndex(activeSearchRowIndex, { align: "auto" });
	}, [activeSearchRowIndex]);
	const messageResultsContent = isMessageLoading && hasQuery ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchState, { label: t("common.loading") }) : messageError ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchState, { label: t("globalSearch.error") }) : showMessageEmptyState ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchState, { label: hasQuery ? t("common.no_results") : t("globalSearch.messageSearch.hint") }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "ui.global-search-panel",
		className: "flex h-full min-h-0 flex-col",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "min-h-0 flex-1",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GroupedVirtualList_default, {
				ref: messageListRef,
				role: "listbox",
				scrollerProps: { id: GLOBAL_SEARCH_LISTBOX_ID },
				groups: messageVirtualGroupsWithLoadMore,
				estimateGroupHeaderSize: () => 32,
				estimateItemSize: (item) => {
					if (item.kind === "more") return 36;
					return 44;
				},
				estimateGroupFooterSize: () => 48,
				className: "pt-2 pb-2",
				renderGroupHeader: (group) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalMessageSearchGroupHeader, { group }),
				renderGroupFooter: () => {
					const active = activeItemId === GLOBAL_MESSAGE_SEARCH_LOAD_MORE_ITEM_ID;
					return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "h-12 px-5 pt-2",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							type: "button",
							id: getGlobalSearchOptionDomId(GLOBAL_MESSAGE_SEARCH_LOAD_MORE_ITEM_ID),
							role: "option",
							"aria-selected": active,
							"aria-disabled": isLoadingMoreMessageResults,
							variant: "ghost",
							disabled: isLoadingMoreMessageResults,
							onMouseEnter: () => setActiveItemId(GLOBAL_MESSAGE_SEARCH_LOAD_MORE_ITEM_ID),
							onClick: handleLoadMoreMessageResults,
							className: cn("h-8 w-full rounded-[8px] font-medium text-xs hover:bg-muted/50 hover:text-foreground", active ? "bg-muted/60 text-accent-foreground" : "text-muted-foreground"),
							children: isLoadingMoreMessageResults ? t("common.loading") : t("globalSearch.showMore", { count: messageLoadMoreCount })
						})
					});
				},
				renderItem: (item) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalMessageSearchRow, {
					item,
					active: item.id === activeItemId,
					language: i18n.language,
					query: deferredQuery,
					userName,
					onMouseEnter: () => setActiveItemId(item.id),
					onOpen: () => openMessagePanelItem(item),
					onJump: () => jumpMessagePanelItem(item)
				})
			})
		})
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "ui.global-search-panel",
		className: "flex h-full min-h-0 flex-col bg-background",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "shrink-0 px-5 pt-4 pb-2",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "relative",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Search, { className: "-translate-y-1/2 pointer-events-none absolute top-1/2 left-4 size-5 text-muted-foreground" }),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
							ref: inputRef,
							value: query,
							onChange: (event) => {
								const nextQuery = event.target.value.trimStart();
								setQuery(nextQuery);
								setPanelMode((current) => !nextQuery ? "search" : current);
								setMessagePreviewTarget(null);
							},
							onKeyDown: handleInputKeyDown,
							placeholder: t("globalSearch.placeholder"),
							"aria-label": t("globalSearch.placeholder"),
							role: "combobox",
							"aria-expanded": isListboxVisible,
							"aria-controls": isListboxVisible ? GLOBAL_SEARCH_LISTBOX_ID : void 0,
							"aria-activedescendant": isListboxVisible && activeItemId ? getGlobalSearchOptionDomId(activeItemId) : void 0,
							spellCheck: false,
							className: "h-11 rounded-[22px] border-border-subtle bg-muted/20 pr-12 pl-12 text-[15px] shadow-none placeholder:text-muted-foreground"
						}),
						query && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
							type: "button",
							"aria-label": t("globalSearch.clear"),
							onClick: () => {
								setQuery("");
								setPanelMode("search");
								setMessagePreviewTarget(null);
								inputRef.current?.focus({ preventScroll: true });
							},
							className: "-translate-y-1/2 absolute top-1/2 right-3 flex size-7 items-center justify-center rounded-full text-muted-foreground transition-colors hover:bg-accent hover:text-foreground",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { className: "size-4" })
						})
					]
				}), showSearchControls && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "mt-3 flex h-7 items-center gap-2",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SegmentedControl, {
						size: "sm",
						"aria-label": t("globalSearch.filters.label"),
						value: isMessageSearchMode ? "messages" : "all",
						onValueChange: handleSearchScopeChange,
						className: SEARCH_SCOPE_CONTROL_CLASS_NAME,
						options: [{
							value: "all",
							label: t("globalSearch.filters.all")
						}, {
							value: "messages",
							label: t("globalSearch.messageSearch.entry")
						}]
					}), isMessageSearchMode ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [MESSAGE_SOURCE_FILTER_BUTTONS.map((filterOption) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						variant: "ghost",
						"aria-label": `${t("globalSearch.messageSearch.sourceLabel")}: ${t(getMessageSourceFilterLabelKey(filterOption))}`,
						"aria-pressed": messageSourceFilter === filterOption,
						onClick: () => handleMessageSourceFilterSelect(filterOption),
						className: cn("h-7 rounded-[8px] px-2 font-medium text-muted-foreground text-xs hover:bg-muted/50 hover:text-foreground", messageSourceFilter === filterOption && "bg-muted text-foreground hover:bg-muted"),
						children: t(getMessageSourceFilterLabelKey(filterOption))
					}, filterOption)), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TimeFilterDropdown, {
						timeFilter,
						panelMode,
						onSelect: handleTimeFilterSelect
					})] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [SEARCH_FILTERS.map((filterOption) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						variant: "ghost",
						"aria-label": `${t("globalSearch.filters.label")}: ${t(getFilterLabelKey(filterOption))}`,
						"aria-pressed": filter === filterOption,
						onClick: () => handleFilterSelect(filterOption),
						className: cn("h-7 rounded-[8px] px-2 font-medium text-muted-foreground text-xs hover:bg-muted/50 hover:text-foreground", filter === filterOption && "bg-muted text-foreground hover:bg-muted"),
						children: t(getFilterLabelKey(filterOption))
					}, filterOption)), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TimeFilterDropdown, {
						timeFilter,
						panelMode,
						onSelect: handleTimeFilterSelect
					})] })]
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: cn("min-h-0 flex-1", showSearchControls && "border-border-subtle border-t"),
				children: isMessageSearchMode ? messagePreviewTarget ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchMessagePreviewPanel, {
					className: "h-full min-w-0",
					searchQuery: deferredQuery,
					target: messagePreviewTarget,
					onClose: () => setMessagePreviewTarget(null),
					onOpenMessage: openMessagePreviewMessage
				}) : messageResultsContent : isLoading && hasQuery ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchState, { label: t("common.loading") }) : error ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchState, { label: t("globalSearch.error") }) : showEmptyState ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchState, { label: hasQuery ? t("common.no_results") : t("globalSearch.recent_hint") }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "relative h-full",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(GroupedVirtualList_default, {
						ref: searchListRef,
						role: "listbox",
						scrollerProps: { id: GLOBAL_SEARCH_LISTBOX_ID },
						groups: virtualGroups,
						estimateGroupHeaderSize: () => 28,
						estimateItemSize: (item) => {
							if (item.kind === "message-parent") return 32;
							if (item.kind === "message") return 44;
							return 52;
						},
						estimateGroupFooterSize: () => 36,
						className: "pt-1 pb-2",
						renderGroupHeader: (group) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchGroupHeader, { group }),
						renderGroupFooter: (footer, group) => {
							const footerId = getGlobalSearchFooterItemId(group.id, footer);
							return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchGroupFooter, {
								footer,
								active: footerId === activeItemId,
								domId: getGlobalSearchOptionDomId(footerId),
								onMouseEnter: () => setActiveItemId(footerId),
								onOpen: () => openGlobalSearchFooter(footer)
							});
						},
						renderItem: (item) => item.kind === "message-parent" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalMessageSearchGroupHeader, {
							group: item.group,
							inset: "nested"
						}) : item.kind === "message" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalMessageSearchRow, {
							item,
							active: item.id === activeItemId,
							inset: "nested",
							language: i18n.language,
							query: deferredQuery,
							userName,
							onMouseEnter: () => setActiveItemId(item.id),
							onOpen: () => void openPanelItem(item),
							onJump: () => jumpMessagePanelItem(item)
						}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchRow, {
							item,
							active: item.id === activeItemId,
							language: i18n.language,
							query: deferredQuery,
							onMouseEnter: () => setActiveItemId(item.id),
							onOpen: () => void openPanelItem(item)
						})
					}), shouldShowRecentHint && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalSearchRecentHint, {
						label: t("globalSearch.recent_hint"),
						offset: 32 + selectableItems.length * 52 + 8
					})]
				})
			}),
			showSearchControls && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex h-10 shrink-0 items-center gap-4 border-border-subtle border-t bg-background/95 px-5 text-muted-foreground text-xs",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(KbdGroup, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Kbd, {
						className: "bg-muted text-muted-foreground shadow-none",
						children: "↑↓"
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("globalSearch.keyboard.select") })] }),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(KbdGroup, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Kbd, {
						className: "bg-muted text-muted-foreground shadow-none",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CornerDownLeft, { className: "size-3" })
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("common.open") })] }),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(KbdGroup, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Kbd, {
						className: "bg-muted text-muted-foreground shadow-none",
						children: "ESC"
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("common.close") })] })
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceEditDialogHost, {
				target: editDialogTarget,
				onOpenChange: (open) => {
					if (!open) setEditDialogTarget(null);
				}
			})
		]
	});
}
const testOnlyClearRefreshHistory = () => {
	recentRefreshHistory.clear();
};
export { GlobalSearchPanel, testOnlyClearRefreshHistory };
