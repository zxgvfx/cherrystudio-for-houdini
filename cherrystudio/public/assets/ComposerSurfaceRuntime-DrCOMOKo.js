import { c as __toESM } from "./rolldown-runtime-D8OvLAZx.js";
import { t as loggerService } from "./LoggerService-ChVOAPl8.js";
import "./error-CskkREu_.js";
import "./PreferenceService-CvpJqJd7.js";
import "./Model-CThI07dL.js";
import "./aiGeneration-OaAly3Fj.js";
import "./platform-fGkkNTU9.js";
import "./dataApiDevtools-Bm4JBknU.js";
import "./dayjs.min-BBb2vAs7.js";
import "./resolver-Bn-i1elC.js";
import "./i18next-CqNcSVOM.js";
import { t as require_react } from "./react-C1DTAr29.js";
import { t as useTranslation } from "./useTranslation-DRFkwCLq.js";
import "./react-dom-D5lMhlFn.js";
import { n as usePreference } from "./usePreference-DRNUEk4I.js";
import { t as cn } from "./utils-Bnoyl1me.js";
import { t as require_jsx_runtime } from "./jsx-runtime-Cc0-uZGc.js";
import "./shim-2TzZzGeK.js";
import { t as Button } from "./button-BBhIgYp8.js";
import "./dropzone-ipDj8md_.js";
import { n as Tooltip } from "./tooltip-a5SkzYdn.js";
import "./es2015-wfS3L7va.js";
import "./EmojiPicker-CTF843Lc.js";
import "./esm-BG45amY4.js";
import "./dist-DAWIImZF.js";
import "./w3c-keyname-DKIRohbk.js";
import "./diff-KPuWBc9d.js";
import "./with-selector-DlsRhNV6.js";
import "./chunk-BO2N2NFS-fKJ-DR8B.js";
import "./extend-BDo4rIBl.js";
import "./marked.esm-5kn1DqGn.js";
import "./dist-Bsd0pttl.js";
import "./sortable.esm-TnTcc6bt.js";
import "./useTheme-mM6gKAcD.js";
import "./command-CyHQabGE.js";
import "./useCloseBeforeAction-DvYaYpLC.js";
import { n as ipcApi } from "./ipc-DpcwPFwy.js";
import "./mainWindowNavigation-Dpzd_Ttr.js";
import "./CacheService-IyXh62g9.js";
import "./useCache-lF6Sw_ck.js";
import { t as AbsoluteFilePathSchema } from "./file-OKCzlHoD.js";
import { d as createFilePathHandle, t as fileUrlToPath } from "./file-CkrjUGO_.js";
import "./model-BOGgSmTN.js";
import "./aiSdk-C-csCQz7.js";
import "./message-CtGMR9KJ.js";
import "./DataApiService-Csr1w5Kb.js";
import "./useDataApi-H7ZhyZ_J.js";
import { t as toast } from "./toast-D2efAzAF.js";
import { t as Check } from "./check-C3qUONPw.js";
import { t as CirclePause } from "./circle-pause-CYpcd_Zx.js";
import { t as LocateFixed } from "./locate-fixed-CTa55vOm.js";
import { t as Maximize2 } from "./maximize-2-B2iaKESx.js";
import { t as Minimize2 } from "./minimize-2-baHAflSg.js";
import { t as Pencil } from "./pencil-DNqUonRT.js";
import { t as X } from "./x-CpgfqVTG.js";
import "./popup-fJcKiA2S.js";
import "./mcp-BGt1L-d_.js";
import "./label-CSb71add.js";
import "./systemProviderId-B4QvwwvR.js";
import "./provider-0IejLFjL.js";
import "./naming-BVJSQooI.js";
import "./image-DyuN5EBc.js";
import "./agentRuntimeCapabilities-C7UvdsI7.js";
import "./provider-bQl8RVRp.js";
import "./toolOutput-DFxKz6Jf.js";
import "./EventService-CMzpRLnw.js";
import "./uiParts-ClY38h-2.js";
import "./with-selector-DFVq3c2Y.js";
import "./constants-BF9E3O0o.js";
import "./useModel-kU1hKaYa.js";
import "./useProvider-DvntuFRN.js";
import "./capabilities-DNV_QUgI.js";
import "./model-BCwc3I-J.js";
import "./useAssistant-DKYAwKxt.js";
import "./ModelAvatar-Dlf3Jj6Z.js";
import { t as useTimer } from "./useTimer-CtD1TRRj.js";
import "./cocoAgent-Dkf7ljK5.js";
import "./useAgent-9Zqveu03.js";
import { o as isComposerFileTokenPathLike } from "./composerFileTokenSource-CsEu7TtB.js";
import { n as FileComposerToken, r as createComposerLinkToken } from "./tokenView-yvehtvx6.js";
import { a as isSupportedFile, c as removeFileExtension, f as isPastedTextFileMetadata, i as getFileExtension, t as filterSupportedFiles, u as COMPOSER_FILE_KIND } from "./file-B-bjOfog.js";
import { r as isComposerInputTokenKind } from "./composerTokenPolicy-BR1gNZTA.js";
import { U as mergeAttributes, n as Extension, o as Node3 } from "./dist-DEHNmG6a.js";
import { t as EditorContent } from "./dist-DWgqr56K.js";
import "./BracesVariableIcon-zDcWRqre.js";
import "./FallbackFavicon-Cm9QCLm8.js";
import "./ImagePreviewService-B71uVdEp.js";
import "./__vite-browser-external-DlQ9XxoO.js";
import { _ as createComposerTokenMarkerInlineContent, a as serializeComposerDocument, c as createPromptVariableInlineContent, d as getSelectedPromptVariableToken, f as selectPromptVariableToken, g as createComposerTokenContent, h as createComposerPlainTextContent, l as createPromptVariableMarkerRule, m as updateSelectedPromptVariableToken, n as createComposerDraftContent, p as tokenizePromptVariablesInEditor, s as createPromptVariableContent, t as COMPOSER_INPUT_MAX_LENGTH, u as getNextPromptVariableIndex, v as COMPOSER_TOKEN_NODE_NAME, y as ComposerTokenNode } from "./composerDraft-CEGp3JAk.js";
import "./useSkills-Be92aMP5.js";
import "./group-Cqj37oRb.js";
import "./knowledge-8Me8AnnG.js";
import "./SvgIcon-DI_kUaAV.js";
import "./useMcpServer-BA8oDtqe.js";
import { n as toComposerAttachments, t as toComposerAttachment } from "./composerAttachment-BB5cm1a7.js";
import "./EditDialogShared-jA4mvNoE.js";
import "./citations-B4d_KMap.js";
import "./citation-BDKmyu_p.js";
import "./find-Bek5EVkJ.js";
import "./edit-BgB-A0ar.js";
import "./ModelSelector-DGMGvnR1.js";
import "./resourceCatalog-BJgI2SG2.js";
import "./VirtualList-2tuBY6WI.js";
import "./contextSettings-CKgBWQdg.js";
import "./agent-IxO6TDCv.js";
import "./useKnowledgeBase-DfgLPgtw.js";
import "./providerSettings-DJmmyeTu.js";
import "./useMcpRuntimeStatus-Ctv5GtW6.js";
import "./CreateGroupDialog-pRoHlnCL.js";
import "./NotesService-DV3jgPSg.js";
import "./ActionIconButton-Cesj5p65.js";
import { n as NarrowLayout_default } from "./dist-6KqCbk7Z.js";
import { a as undo, i as redo, n as Placeholder, r as history, t as useRichTextEditorKernel } from "./useRichTextEditorKernel-BSsXglr7.js";
import { A as getComposerPositionAtTextOffset, C as createComposerSuggestionExtension, D as getComposerCursorTextOffset, E as createComposerSuggestionQuickPanelItem, F as useQuickPanel, I as QuickPanelView, M as hasComposerQuickPanelTriggerBoundary, N as SendMessageButton_default, O as getComposerInputLeafText, S as COMPOSER_SUPPRESS_SUGGESTION_META, T as ROOT_QUICK_PANEL_ALLOWED_PREFIXES, a as ComposerToolMenu, b as hasUnifiedQuickPanelRootContent, c as useComposerPinnedTools, j as getComposerSuggestionTriggerContext, k as getComposerInputText, n as getComposerEditorMinHeight, t as getCompactComposerEditorMinHeight, x as ComposerPanelSymbol, y as createUnifiedQuickPanelOpenOptions } from "./composerSizing-diBV7-2E.js";
import { a as readComposerClipboardFragmentFromDataTransfer, n as createComposerRichClipboardContentFromDraft, o as readComposerClipboardFragmentFromSessionCache, s as writeComposerClipboardData, t as createComposerAttachmentFromComposerClipboardToken } from "./composerClipboard-DBhZr-TC.js";
import { i as useActiveComposerOverride, n as shouldHandleInputHistoryNavigation } from "./inputHistoryNavigation-B145IdKJ.js";
import "./NotesTreeService-jyDHDKaA.js";
import "./useNotesSettings-D0_QFpMn.js";
import "./api-BXzzMAT7.js";
import "./webSearchProviderMeta-DoUpW3GP.js";
import { t as createComposerFolderToken } from "./folderToken-BkDtosgK.js";
import { t as useDrag } from "./useDrag-CfucRTDs.js";
import { r as getTextFromDropEvent, t as getFilesFromDropEvent } from "./input-ByVKymku.js";
import { t as useResizeDrag } from "./useResizeDrag-J5F6ZuzI.js";
var import_react = /* @__PURE__ */ __toESM(require_react());
var SKILL_TOKEN_MARKER_PATTERN = /(^|\s)\/([^/\s]+)\/(?=$|\s)/g;
var KNOWLEDGE_BASE_TOKEN_MARKER_PATTERN = /(^|\s)#([^#\r\n]+)#/g;
function createSkillMarkerRule(resolveSkillMarker) {
	return {
		id: "skill",
		pattern: SKILL_TOKEN_MARKER_PATTERN,
		resolve: (match) => {
			const prefix = match[1] ?? "";
			const marker = match[2];
			const index = match.index ?? 0;
			if (!marker) return null;
			const token = resolveSkillMarker(marker);
			if (!token) return null;
			const markerStart = index + prefix.length;
			return {
				from: markerStart,
				to: markerStart + marker.length + 2,
				token
			};
		}
	};
}
function createKnowledgeBaseMarkerRule(resolveKnowledgeBaseMarker) {
	return {
		id: "knowledge",
		pattern: KNOWLEDGE_BASE_TOKEN_MARKER_PATTERN,
		resolve: (match) => {
			const prefix = match[1] ?? "";
			const marker = match[2]?.trim();
			const index = match.index ?? 0;
			if (!marker) return null;
			const token = resolveKnowledgeBaseMarker(marker);
			if (!token) return null;
			const markerStart = index + prefix.length;
			return {
				from: markerStart,
				to: markerStart + marker.length + 2,
				token
			};
		}
	};
}
function createComposerPlainTextPasteContent(text) {
	return createComposerPlainTextContent(text);
}
function getPrivateTokenMarker(token, prefix) {
	return token.id.startsWith(prefix) ? token.id.slice(prefix.length) : token.label;
}
function resolvePrivateClipboardToken(token, options) {
	if (token.kind === "skill") {
		const resolvedToken = options.resolveSkillMarker?.(getPrivateTokenMarker(token, "skill:"));
		return resolvedToken ? { token: resolvedToken } : null;
	}
	if (token.kind === "knowledge") {
		const resolvedToken = options.resolveKnowledgeBaseMarker?.(getPrivateTokenMarker(token, "knowledge:"));
		return resolvedToken ? { token: resolvedToken } : null;
	}
	if (token.kind === "file") {
		const file = createComposerAttachmentFromComposerClipboardToken(token);
		if (!file) return null;
		return {
			token: {
				id: token.id,
				kind: "file",
				label: token.label,
				payload: file
			},
			file
		};
	}
	return { token: {
		id: token.id,
		kind: token.kind,
		label: token.label,
		...token.description && { description: token.description },
		...token.promptText && { promptText: token.promptText }
	} };
}
function getComposerClipboardPasteOverride(fragment, options) {
	if (!fragment?.segments.length) return null;
	const content = [];
	const files = [];
	for (const segment of fragment.segments) {
		if (segment.type === "text") {
			content.push(...createComposerPlainTextContent(segment.text));
			continue;
		}
		const resolved = resolvePrivateClipboardToken(segment.token, options);
		if (!resolved) {
			content.push(...createComposerPlainTextContent(segment.fallbackText));
			continue;
		}
		content.push(createComposerTokenContent(resolved.token));
		if (resolved.file) files.push(resolved.file);
	}
	return content.length ? {
		content,
		files
	} : null;
}
function createPlainTextPasteMarkerRules(options) {
	const rules = [createPromptVariableMarkerRule({ startIndex: options.promptVariableStartIndex ?? 0 })];
	if (options.resolveKnowledgeBaseMarker) rules.push(createKnowledgeBaseMarkerRule(options.resolveKnowledgeBaseMarker));
	if (options.resolveSkillMarker) rules.push(createSkillMarkerRule(options.resolveSkillMarker));
	return rules;
}
function createComposerLinkPasteContent(text) {
	const url = text.trim();
	const token = createComposerLinkToken(url);
	if (!token) return null;
	const start = text.indexOf(url);
	return [
		...createComposerPlainTextContent(text.slice(0, start)),
		createComposerTokenContent(token),
		...createComposerPlainTextContent(text.slice(start + url.length))
	];
}
function getComposerPlainTextPasteOverride(text, options) {
	if (!text) return null;
	if (!options.inlineLongText && text.length > 1500) return null;
	const linkContent = createComposerLinkPasteContent(text);
	if (linkContent) return linkContent;
	const markedTextContent = createComposerTokenMarkerInlineContent(text, createPlainTextPasteMarkerRules(options));
	if (markedTextContent.hasToken) return markedTextContent.content;
	return createComposerPlainTextPasteContent(text);
}
const ComposerDocument = Node3.create({
	name: "doc",
	topNode: true,
	content: "block+"
});
const ComposerParagraph = Node3.create({
	name: "paragraph",
	group: "block",
	content: "inline*",
	parseHTML() {
		return [{ tag: "p" }];
	},
	renderHTML({ HTMLAttributes }) {
		return [
			"p",
			mergeAttributes(HTMLAttributes),
			0
		];
	}
});
const ComposerText = Node3.create({
	name: "text",
	group: "inline"
});
const ComposerHardBreak = Node3.create({
	name: "hardBreak",
	inline: true,
	group: "inline",
	selectable: false,
	linebreakReplacement: true,
	parseHTML() {
		return [{ tag: "br" }];
	},
	renderHTML({ HTMLAttributes }) {
		return ["br", mergeAttributes(HTMLAttributes)];
	},
	renderText() {
		return "\n";
	},
	addCommands() {
		return { setHardBreak: () => ({ chain }) => {
			return chain().insertContent({ type: this.name }).scrollIntoView().run();
		} };
	},
	addKeyboardShortcuts() {
		return {
			Enter: () => this.editor.commands.setHardBreak(),
			NumpadEnter: () => this.editor.commands.setHardBreak(),
			"Mod-Enter": () => this.editor.commands.setHardBreak(),
			"Shift-Enter": () => this.editor.commands.setHardBreak()
		};
	}
});
const ComposerUndoRedo = Extension.create({
	name: "composerUndoRedo",
	addOptions() {
		return {
			depth: 100,
			newGroupDelay: 500
		};
	},
	addCommands() {
		return {
			undo: () => ({ state, dispatch }) => {
				return undo(state, dispatch);
			},
			redo: () => ({ state, dispatch }) => {
				return redo(state, dispatch);
			}
		};
	},
	addKeyboardShortcuts() {
		return {
			"Mod-z": () => this.editor.commands.undo(),
			"Shift-Mod-z": () => this.editor.commands.redo(),
			"Mod-y": () => this.editor.commands.redo()
		};
	},
	addProseMirrorPlugins() {
		return [history(this.options)];
	}
});
function createComposerEditorPreset({ placeholder, enableUndoRedo = true, renderToken, suggestionSources = [] } = {}) {
	return [
		ComposerDocument,
		ComposerParagraph,
		ComposerText,
		ComposerHardBreak,
		Placeholder.configure({
			placeholder,
			showOnlyWhenEditable: true,
			showOnlyCurrent: true,
			includeChildren: false
		}),
		ComposerTokenNode.configure({ renderToken }),
		...suggestionSources.length > 0 ? [createComposerSuggestionExtension(suggestionSources)] : [],
		...enableUndoRedo ? [ComposerUndoRedo] : []
	];
}
var logger$1 = loggerService.withContext("pasteHandling");
var lastFocusedComponent = "inputbar";
var handlers = {};
var isInitialized = false;
const handlePaste = async (event, supportExts, setFiles, setText, text, resizeTextArea, t) => {
	try {
		const clipboardText = event.clipboardData?.getData("text");
		if (clipboardText) {
			if (clipboardText.length > 1500) {
				if (!supportExts.includes(".txt")) return false;
				event.preventDefault();
				const tempFilePath = await window.api.file.createTempFile("pasted_text.txt");
				await window.api.file.write(tempFilePath, clipboardText);
				const selectedFile = await window.api.file.get(tempFilePath);
				if (selectedFile) {
					const pastedTextFile = {
						...selectedFile,
						origin_name: t?.("chat.input.pasted_text_file_name") ?? selectedFile.origin_name,
						composerFileKind: COMPOSER_FILE_KIND.PASTED_TEXT
					};
					setFiles((prevFiles) => [...prevFiles, toComposerAttachment(pastedTextFile)]);
					if (setText && text) setText(text);
					if (resizeTextArea) setTimeout(() => resizeTextArea(), 50);
				}
				return true;
			}
			return false;
		}
		if (event.clipboardData?.files && event.clipboardData.files.length > 0) {
			event.preventDefault();
			const extensionSet = new Set(supportExts);
			try {
				for (const file of event.clipboardData.files) {
					const filePath = window.api.file.getPathForFile(file);
					if (!filePath) {
						const isImage = file.type.startsWith("image/");
						if (supportExts.includes(getFileExtension(file.name))) {
							const tempFilePath = await window.api.file.createTempFile(file.name);
							const arrayBuffer = await file.arrayBuffer();
							const uint8Array = new Uint8Array(arrayBuffer);
							await window.api.file.write(tempFilePath, uint8Array);
							const selectedFile = await window.api.file.get(tempFilePath);
							if (selectedFile) {
								setFiles((prevFiles) => [...prevFiles, toComposerAttachment(isImage ? {
									...selectedFile,
									origin_name: removeFileExtension(file.name)
								} : selectedFile)]);
								if (isImage) break;
							}
						} else if (t) toast.info(t("chat.input.file_not_supported"));
						continue;
					}
					if (await isSupportedFile(filePath, extensionSet)) {
						const selectedFile = await window.api.file.get(filePath);
						if (selectedFile) setFiles((prevFiles) => [...prevFiles, toComposerAttachment(selectedFile)]);
					} else if (t) toast.info(t("chat.input.file_not_supported"));
				}
			} catch (error) {
				logger$1.error("onPaste:", error);
				if (t) toast.error(t("chat.input.file_error"));
			}
			return true;
		}
		return false;
	} catch (error) {
		logger$1.error("handlePaste error:", error);
		return false;
	}
};
const setLastFocusedComponent = (component) => {
	lastFocusedComponent = component;
};
const getLastFocusedComponent = () => {
	return lastFocusedComponent;
};
const init = () => {
	if (isInitialized) return;
	document.addEventListener("paste", async (event) => {
		await handleGlobalPaste(event);
	});
	isInitialized = true;
	logger$1.verbose("Global paste handler initialized");
};
const registerHandler = (component, handler) => {
	if (!component) return () => void 0;
	if (!handlers[component] || handlers[component] !== handler) handlers[component] = handler;
	return () => {
		if (handlers[component] === handler) delete handlers[component];
	};
};
const unregisterHandler = (component, handler) => {
	if (!component || !handlers[component]) return;
	if (handler && handlers[component] !== handler) return;
	delete handlers[component];
};
var handleGlobalPaste = async (event) => {
	const activeElement = document.activeElement;
	if (activeElement && (activeElement.tagName === "INPUT" || activeElement.tagName === "TEXTAREA" || activeElement.getAttribute("contenteditable") === "true")) return false;
	if (lastFocusedComponent && handlers[lastFocusedComponent]) {
		const handler = handlers[lastFocusedComponent];
		if (handler) return await handler(event);
	}
	if (handlers.inputbar) {
		const handler = handlers.inputbar;
		if (handler) return await handler(event);
	}
	return false;
};
var pasteHandling_default = {
	handlePaste,
	setLastFocusedComponent,
	getLastFocusedComponent,
	init,
	registerHandler,
	unregisterHandler
};
var logger = loggerService.withContext("useFileDragDrop");
function stripWrappingQuotes(value) {
	if (value.startsWith("\"") && value.endsWith("\"") || value.startsWith("'") && value.endsWith("'")) return value.slice(1, -1);
	return value;
}
function getSingleDroppedPathFromText(text) {
	const lines = text.split(/\r\n?|\n/).map((line) => stripWrappingQuotes(line.trim())).filter(Boolean);
	if (lines.length !== 1) return null;
	const value = lines[0];
	let path = value;
	if (value.toLowerCase().startsWith("file://")) try {
		path = fileUrlToPath(value);
	} catch {
		path = value;
	}
	return isComposerFileTokenPathLike(path) ? path : null;
}
async function getDroppedPathKind(path) {
	try {
		return (await ipcApi.request("file.get_metadata", createFilePathHandle(AbsoluteFilePathSchema.parse(path))))?.kind ?? "file";
	} catch {
		return null;
	}
}
async function splitDroppedFilesByKind(files) {
	const items = await Promise.all(files.map(async (file) => ({
		file,
		kind: await getDroppedPathKind(file.path)
	})));
	return {
		directories: items.filter((item) => item.kind === "directory").map((item) => item.file),
		files: items.filter((item) => item.kind !== "directory").map((item) => item.file)
	};
}
function useFileDragDrop(options) {
	const dragState = useDrag((0, import_react.useCallback)(async (event) => {
		if (!options.enabled) return;
		const droppedFiles = await getFilesFromDropEvent(event).catch((err) => {
			logger.error("handleDrop:", err);
			return null;
		});
		if (droppedFiles && droppedFiles.length > 0) {
			const { directories, files } = await splitDroppedFilesByKind(droppedFiles);
			directories.forEach((directory) => options.onFolderPathDropped?.(directory.path));
			const supportedFiles = await filterSupportedFiles(files, options.supportedExts);
			if (supportedFiles.length > 0) options.setFiles((prevFiles) => [...prevFiles, ...toComposerAttachments(supportedFiles)]);
			if (files.length > 0 && supportedFiles.length !== files.length) toast.info(options.t("chat.input.file_not_supported_count", { count: files.length - supportedFiles.length }));
			return;
		}
		const droppedText = await getTextFromDropEvent(event);
		if (!droppedText) return;
		const droppedPath = getSingleDroppedPathFromText(droppedText);
		if (droppedPath) {
			const pathKind = await getDroppedPathKind(droppedPath);
			if (pathKind === "directory") {
				options.onFolderPathDropped?.(droppedPath);
				return;
			}
			if (pathKind === "file") try {
				const selectedFile = await window.api.file.get(droppedPath);
				if (!selectedFile) {
					options.onTextDropped?.(droppedText);
					return;
				}
				if (!await isSupportedFile(droppedPath, new Set(options.supportedExts))) {
					toast.info(options.t("chat.input.file_not_supported"));
					return;
				}
				options.setFiles((prevFiles) => [...prevFiles, ...toComposerAttachments([selectedFile])]);
				return;
			} catch (error) {
				logger.error("handleDrop path:", error);
				toast.error(options.t("chat.input.file_error"));
				return;
			}
		}
		options.onTextDropped?.(droppedText);
	}, [options]));
	return {
		isDragging: options.enabled ? dragState.isDragging : false,
		setIsDragging: dragState.setIsDragging,
		handleDragOver: options.enabled ? dragState.handleDragOver : void 0,
		handleDragEnter: options.enabled ? dragState.handleDragEnter : void 0,
		handleDragLeave: options.enabled ? dragState.handleDragLeave : void 0,
		handleDrop: options.enabled ? dragState.handleDrop : void 0
	};
}
function usePasteHandler(text, setText, options) {
	return { handlePaste: (0, import_react.useCallback)(async (event) => {
		return await pasteHandling_default.handlePaste(event, options.supportedExts, options.setFiles, setText, text, options.onResize ?? (() => {}), options.t);
	}, [
		text,
		setText,
		options
	]) };
}
var COMPOSER_OVERFLOW_TOLERANCE_PX = 1;
function useCompactComposerPresentation({ enabled, frameRef, isComposing }) {
	const [requestedRevision, setRequestedRevision] = (0, import_react.useState)(0);
	const [measurement, setMeasurement] = (0, import_react.useState)({
		presentation: "compact",
		revision: -1
	});
	const measurementScheduledRef = (0, import_react.useRef)(false);
	const mountedRef = (0, import_react.useRef)(true);
	const wasEnabledRef = (0, import_react.useRef)(enabled);
	const requestMeasurement = (0, import_react.useCallback)(() => {
		if (!enabled || isComposing() || measurementScheduledRef.current) return;
		measurementScheduledRef.current = true;
		queueMicrotask(() => {
			measurementScheduledRef.current = false;
			if (mountedRef.current) setRequestedRevision((revision) => revision + 1);
		});
	}, [enabled, isComposing]);
	(0, import_react.useEffect)(() => {
		mountedRef.current = true;
		return () => {
			mountedRef.current = false;
		};
	}, []);
	(0, import_react.useLayoutEffect)(() => {
		if (!enabled) {
			wasEnabledRef.current = false;
			return;
		}
		if (!wasEnabledRef.current) {
			wasEnabledRef.current = true;
			requestMeasurement();
			return;
		}
		if (measurement.revision === requestedRevision || isComposing()) return;
		const frame = frameRef.current;
		const editorElement = frame?.querySelector(".composer-tiptap");
		const compactRowElement = frame?.closest("[data-composer-compact-row]");
		if (!editorElement || !compactRowElement) return;
		const hasExplicitHardBreak = editorElement.querySelector(":scope > p > br:not(.ProseMirror-trailingBreak)") !== null;
		const hasEditorOverflow = editorElement.scrollHeight > editorElement.clientHeight + COMPOSER_OVERFLOW_TOLERANCE_PX;
		const hasCompactRowOverflow = compactRowElement.scrollWidth > compactRowElement.clientWidth + COMPOSER_OVERFLOW_TOLERANCE_PX;
		setMeasurement({
			presentation: hasExplicitHardBreak || hasEditorOverflow || hasCompactRowOverflow ? "regular" : "compact",
			revision: requestedRevision
		});
	}, [
		enabled,
		frameRef,
		isComposing,
		measurement.revision,
		requestMeasurement,
		requestedRevision
	]);
	(0, import_react.useEffect)(() => {
		if (!enabled) return;
		const frame = frameRef.current;
		const inputbarElement = frame?.closest("[data-composer-inputbar]");
		if (!frame || !inputbarElement) return;
		const mutationObserver = new MutationObserver(requestMeasurement);
		mutationObserver.observe(frame, {
			characterData: true,
			childList: true,
			subtree: true
		});
		let lastInputbarWidth = inputbarElement.getBoundingClientRect().width;
		const resizeObserver = typeof ResizeObserver === "undefined" ? null : new ResizeObserver((entries) => {
			const nextInputbarWidth = entries[0]?.contentRect.width ?? inputbarElement.getBoundingClientRect().width;
			if (nextInputbarWidth === lastInputbarWidth) return;
			lastInputbarWidth = nextInputbarWidth;
			requestMeasurement();
		});
		resizeObserver?.observe(inputbarElement);
		return () => {
			mutationObserver.disconnect();
			resizeObserver?.disconnect();
		};
	}, [
		enabled,
		frameRef,
		requestMeasurement
	]);
	const measurementPending = measurement.revision !== requestedRevision;
	return {
		isCompact: enabled && (measurementPending || measurement.presentation === "compact"),
		requestMeasurement
	};
}
const COMPOSER_EDITOR_COLLAPSED_MAX_HEIGHT = "max(220px, 40vh)";
const COMPOSER_EDITOR_EXPANDED_MAX_HEIGHT = "max(220px, 50vh)";
const COMPOSER_EDITOR_COLLAPSED_MAX_HEIGHT_CLASS = "max-h-[max(220px,40vh)]!";
const COMPOSER_EDITOR_EXPANDED_MAX_HEIGHT_CLASS = "max-h-[max(220px,50vh)]!";
var COMPOSER_EDITOR_HEIGHT_TRANSITION_MS = 260;
var COMPOSER_EDITOR_RESIZE_KEYBOARD_STEP = 16;
var COMPOSER_EDITOR_ELEMENT_STYLE = [
	"max-height: var(--composer-editor-max-height) !important",
	"overflow-y: var(--composer-editor-overflow-y)",
	"height: var(--composer-editor-height)"
].join("; ");
function getViewportRelativeHeightPx(minHeight, viewportRatio) {
	return Math.max(minHeight, Math.round(window.innerHeight * viewportRatio));
}
function getExpandedEditorFrameHeightPx(editorMinHeight) {
	return Math.max(editorMinHeight, getViewportRelativeHeightPx(220, .5));
}
function clampComposerEditorHeightPx(height, minHeight, maxHeight) {
	return Math.min(maxHeight, Math.max(minHeight, Math.round(height)));
}
function getCollapsedEditorFrameHeightPx(frame, editorMinHeight) {
	const editorElement = frame.querySelector(".composer-tiptap");
	let contentHeight = frame.scrollHeight || editorMinHeight;
	const maxCollapsedHeight = getViewportRelativeHeightPx(220, .4);
	if (editorElement) {
		const previousHeight = editorElement.style.height;
		const previousMaxHeight = editorElement.style.maxHeight;
		try {
			editorElement.style.height = "auto";
			editorElement.style.maxHeight = "none";
			contentHeight = editorElement.scrollHeight || contentHeight;
		} finally {
			editorElement.style.height = previousHeight;
			editorElement.style.maxHeight = previousMaxHeight;
		}
	}
	return Math.max(editorMinHeight, Math.min(contentHeight, maxCollapsedHeight));
}
function getComposerEditorContentStyle(fontSize, isExpanded, manualEditorFrameHeight, compact = false) {
	const minHeight = compact ? getCompactComposerEditorMinHeight(fontSize) : getComposerEditorMinHeight(fontSize);
	const hasCustomHeight = isExpanded || manualEditorFrameHeight !== null;
	const isFixedHeight = compact || hasCustomHeight;
	const maxHeight = compact ? `${minHeight}px` : isExpanded ? COMPOSER_EDITOR_EXPANDED_MAX_HEIGHT : manualEditorFrameHeight !== null ? `${manualEditorFrameHeight}px` : COMPOSER_EDITOR_COLLAPSED_MAX_HEIGHT;
	return {
		height: compact ? minHeight : hasCustomHeight ? "100%" : void 0,
		minHeight,
		"--composer-editor-padding": compact ? "3px 0" : "6px 44px 0 15px",
		"--composer-editor-min-height": `${minHeight}px`,
		"--composer-editor-font-size": `${fontSize}px`,
		"--composer-editor-line-height": "1.4",
		"--composer-editor-max-height": maxHeight,
		"--composer-editor-overflow-y": compact ? "hidden" : "auto",
		"--composer-editor-height": isFixedHeight ? "100%" : "auto"
	};
}
function useComposerEditorFrameSizing({ fontSize, isExpanded, onExpandedChange, focusEditor, setTimeoutTimer }) {
	const minHeight = getComposerEditorMinHeight(fontSize);
	const compactMinHeight = getCompactComposerEditorMinHeight(fontSize);
	const maxHeight = getExpandedEditorFrameHeightPx(minHeight);
	const frameRef = (0, import_react.useRef)(null);
	const animationFrameRef = (0, import_react.useRef)(null);
	const pendingExpandedRef = (0, import_react.useRef)(null);
	const resizeDragRef = (0, import_react.useRef)({
		startClientY: 0,
		startHeight: 0,
		collapseExpanded: false
	});
	const [animatedHeight, setAnimatedHeight] = (0, import_react.useState)(null);
	const [manualHeight, setManualHeight] = (0, import_react.useState)(null);
	const hasCustomHeight = isExpanded || manualHeight !== null;
	const clearAnimationFrame = (0, import_react.useCallback)(() => {
		if (animationFrameRef.current === null) return;
		window.cancelAnimationFrame(animationFrameRef.current);
		animationFrameRef.current = null;
	}, []);
	const clearAnimatedHeightAfterTransition = (0, import_react.useCallback)(() => {
		setTimeoutTimer("composerEditorFrameHeightTransition", () => {
			setAnimatedHeight(null);
			pendingExpandedRef.current = null;
		}, COMPOSER_EDITOR_HEIGHT_TRANSITION_MS + 80);
	}, [setTimeoutTimer]);
	const getCurrentHeight = (0, import_react.useCallback)(() => {
		const measuredHeight = frameRef.current?.offsetHeight;
		if (measuredHeight) return measuredHeight;
		if (isExpanded) return maxHeight;
		return manualHeight ?? minHeight;
	}, [
		isExpanded,
		manualHeight,
		maxHeight,
		minHeight
	]);
	const setClampedManualHeight = (0, import_react.useCallback)((height) => {
		clearAnimationFrame();
		pendingExpandedRef.current = null;
		setAnimatedHeight(null);
		setManualHeight(clampComposerEditorHeightPx(height, minHeight, maxHeight));
	}, [
		clearAnimationFrame,
		maxHeight,
		minHeight
	]);
	const { isResizing, startResizing } = useResizeDrag({
		onMove: (0, import_react.useCallback)((moveEvent) => {
			const dragState = resizeDragRef.current;
			if (dragState.collapseExpanded) {
				dragState.collapseExpanded = false;
				onExpandedChange(false);
			}
			setClampedManualHeight(dragState.startHeight + dragState.startClientY - moveEvent.clientY);
		}, [onExpandedChange, setClampedManualHeight]),
		cursor: "row-resize"
	});
	const startResize = (0, import_react.useCallback)((event) => {
		resizeDragRef.current = {
			startClientY: event.clientY,
			startHeight: getCurrentHeight(),
			collapseExpanded: isExpanded
		};
		startResizing(event);
	}, [
		getCurrentHeight,
		isExpanded,
		startResizing
	]);
	const handleResizeKeyDown = (0, import_react.useCallback)((event) => {
		const currentHeight = getCurrentHeight();
		let nextHeight = null;
		switch (event.key) {
			case "ArrowUp":
				nextHeight = currentHeight + COMPOSER_EDITOR_RESIZE_KEYBOARD_STEP;
				break;
			case "ArrowDown":
				nextHeight = currentHeight - COMPOSER_EDITOR_RESIZE_KEYBOARD_STEP;
				break;
			case "Home":
				nextHeight = minHeight;
				break;
			case "End":
				nextHeight = maxHeight;
				break;
		}
		if (nextHeight === null) return;
		event.preventDefault();
		if (isExpanded) onExpandedChange(false);
		setClampedManualHeight(nextHeight);
	}, [
		getCurrentHeight,
		isExpanded,
		maxHeight,
		minHeight,
		onExpandedChange,
		setClampedManualHeight
	]);
	const toggleExpanded = (0, import_react.useCallback)((nextState) => {
		const target = typeof nextState === "boolean" ? nextState : !isExpanded;
		const frame = frameRef.current;
		if (frame) {
			clearAnimationFrame();
			setAnimatedHeight(`${frame.offsetHeight || minHeight}px`);
			pendingExpandedRef.current = target;
		}
		if (!target) setManualHeight(null);
		onExpandedChange(target);
		focusEditor();
	}, [
		clearAnimationFrame,
		focusEditor,
		isExpanded,
		minHeight,
		onExpandedChange
	]);
	(0, import_react.useEffect)(() => {
		const frame = frameRef.current;
		if (!frame || pendingExpandedRef.current !== isExpanded) return;
		const targetHeight = isExpanded ? getExpandedEditorFrameHeightPx(minHeight) : getCollapsedEditorFrameHeightPx(frame, minHeight);
		clearAnimationFrame();
		animationFrameRef.current = window.requestAnimationFrame(() => {
			setAnimatedHeight(`${targetHeight}px`);
			animationFrameRef.current = null;
		});
		clearAnimatedHeightAfterTransition();
	}, [
		clearAnimatedHeightAfterTransition,
		clearAnimationFrame,
		isExpanded,
		minHeight
	]);
	(0, import_react.useEffect)(() => clearAnimationFrame, [clearAnimationFrame]);
	const handleTransitionEnd = (0, import_react.useCallback)((event) => {
		if (event.propertyName && event.propertyName !== "height") return;
		setAnimatedHeight(null);
		pendingExpandedRef.current = null;
	}, []);
	const restoreDefaultHeight = (0, import_react.useCallback)(() => {
		const frame = frameRef.current;
		clearAnimationFrame();
		pendingExpandedRef.current = null;
		if (!frame) {
			setManualHeight(null);
			onExpandedChange(false);
			focusEditor();
			return;
		}
		const startHeight = frame.offsetHeight || getCurrentHeight();
		const targetHeight = getCollapsedEditorFrameHeightPx(frame, minHeight);
		setAnimatedHeight(`${startHeight}px`);
		animationFrameRef.current = window.requestAnimationFrame(() => {
			setManualHeight(null);
			onExpandedChange(false);
			setAnimatedHeight(`${targetHeight}px`);
			animationFrameRef.current = null;
		});
		clearAnimatedHeightAfterTransition();
		focusEditor();
	}, [
		clearAnimatedHeightAfterTransition,
		clearAnimationFrame,
		focusEditor,
		getCurrentHeight,
		minHeight,
		onExpandedChange
	]);
	const resolvedFrameHeight = animatedHeight ?? (isExpanded ? "max(220px, 50vh)" : manualHeight !== null ? `${manualHeight}px` : void 0);
	const frameStyle = (0, import_react.useMemo)(() => ({
		height: resolvedFrameHeight,
		minHeight,
		overflow: "hidden",
		transitionDuration: isResizing ? "0ms" : `${COMPOSER_EDITOR_HEIGHT_TRANSITION_MS}ms`
	}), [
		isResizing,
		minHeight,
		resolvedFrameHeight
	]);
	const editorContentStyle = (0, import_react.useMemo)(() => getComposerEditorContentStyle(fontSize, isExpanded, manualHeight), [
		fontSize,
		isExpanded,
		manualHeight
	]);
	return {
		frameRef,
		frameStyle,
		compactFrameStyle: (0, import_react.useMemo)(() => ({
			height: compactMinHeight,
			minHeight: compactMinHeight,
			overflow: "hidden",
			transitionDuration: "0ms"
		}), [compactMinHeight]),
		editorContentStyle,
		compactEditorContentStyle: (0, import_react.useMemo)(() => getComposerEditorContentStyle(fontSize, false, null, true), [fontSize]),
		editorElementStyle: COMPOSER_EDITOR_ELEMENT_STYLE,
		minHeight,
		maxHeight,
		resizeHandleValue: isExpanded ? maxHeight : manualHeight ?? minHeight,
		hasCustomHeight,
		isResizing,
		startResize,
		handleResizeKeyDown,
		handleTransitionEnd,
		toggleExpanded,
		restoreDefaultHeight
	};
}
var import_jsx_runtime = /* @__PURE__ */ __toESM(require_jsx_runtime());
var COMPOSER_SIDE_PADDING_PX = 24;
var ROOT_QUICK_PANEL_TRIGGER_SOURCES = [{
	char: ComposerPanelSymbol.Root,
	pluginKey: "composer-root-suggestion"
}, {
	char: "、",
	pluginKey: "composer-root-ideographic-comma-suggestion"
}];
var EMPTY_SUGGESTION_SOURCES = [];
function getQuickPanelItemText(value) {
	return typeof value === "string" || typeof value === "number" ? String(value) : "";
}
function getQuickPanelItemsSignature(items) {
	return (items ?? []).map((item, index) => [
		item.id ?? index,
		getQuickPanelItemText(item.label),
		getQuickPanelItemText(item.description),
		item.filterText ?? "",
		item.disabled ? "1" : "0",
		item.hidden ? "1" : "0",
		item.isSelected ? "1" : "0",
		item.isMenu ? "1" : "0",
		item.alwaysVisible ? "1" : "0"
	].join("")).join("");
}
function removeComposerTokens(editor, shouldRemove) {
	const ranges = [];
	editor.state.doc.descendants((node, position) => {
		if (node.type.name !== "composerToken") return;
		const token = serializeComposerDocument({
			type: "doc",
			content: [{
				type: "paragraph",
				content: [{
					type: COMPOSER_TOKEN_NODE_NAME,
					attrs: node.attrs
				}]
			}]
		}).tokens[0];
		if (token && shouldRemove(token)) ranges.push({
			from: position,
			to: position + node.nodeSize
		});
	});
	if (!ranges.length) return;
	const transaction = editor.state.tr;
	for (const range of ranges.reverse()) transaction.delete(range.from, range.to);
	editor.view.dispatch(transaction);
}
function addMissingToken(editor, token, existingTokens) {
	if (existingTokens.some((existing) => existing.id === token.id)) return;
	insertComposerTokenAtCursor(editor, token);
}
function hasComposerTokenBeforeSelection(editor) {
	const selection = editor.state.selection;
	if (selection.node?.type?.name === "composerToken") return true;
	if (!selection.empty) return false;
	return selection.$from.nodeBefore?.type.name === COMPOSER_TOKEN_NODE_NAME;
}
function insertComposerTokenAtCursor(editor, token, options = {}) {
	const chain = editor.chain().focus().insertComposerToken(token);
	if (options.insertSeparator === false) {
		chain.run();
		return;
	}
	chain.insertContent(" ").run();
}
function isComposerSendKeyPressed(event, shortcut) {
	switch (shortcut) {
		case "Enter": return !event.shiftKey && !event.ctrlKey && !event.metaKey && !event.altKey;
		case "Ctrl+Enter": return event.ctrlKey && !event.shiftKey && !event.metaKey && !event.altKey;
		case "Command+Enter": return event.metaKey && !event.shiftKey && !event.ctrlKey && !event.altKey;
		case "Alt+Enter": return event.altKey && !event.shiftKey && !event.ctrlKey && !event.metaKey;
		case "Shift+Enter": return event.shiftKey && !event.ctrlKey && !event.metaKey && !event.altKey;
	}
}
function deleteComposerTextRange(editor, range) {
	const fromOffset = Math.max(0, Math.min(range.from, range.to));
	const toOffset = Math.max(fromOffset, range.to);
	if (fromOffset === toOffset) return;
	const from = getComposerPositionAtTextOffset(editor, fromOffset);
	const to = getComposerPositionAtTextOffset(editor, toOffset);
	if (to <= from) return;
	editor.chain().focus().deleteRange({
		from,
		to
	}).run();
}
function createComposerInputAdapter(editor) {
	return {
		getText: () => getComposerInputText(editor),
		getCursorOffset: () => getComposerCursorTextOffset(editor),
		insertText: (insertedText) => {
			editor.chain().focus().insertContent(createPromptVariableInlineContent(insertedText, { startIndex: getNextPromptVariableIndex(editor) })).run();
		},
		insertToken: (token) => {
			insertComposerTokenAtCursor(editor, token);
		},
		deleteTriggerRange: (range) => {
			deleteComposerTextRange(editor, range);
		},
		focus: () => {
			editor.commands.focus();
		}
	};
}
function getComposerUnifiedPanelSearchText(inputAdapter, queryAnchor, triggerInfo) {
	if (!inputAdapter || queryAnchor === void 0) return "";
	const text = inputAdapter.getText();
	const cursorOffset = inputAdapter.getCursorOffset?.() ?? text.length;
	if (cursorOffset <= queryAnchor) return "";
	const rawSearchText = text.slice(queryAnchor, cursorOffset);
	const triggerSymbol = triggerInfo?.type === "input" ? triggerInfo.originalText?.slice(0, 1) : void 0;
	if (triggerSymbol && rawSearchText.startsWith(triggerSymbol)) return rawSearchText.slice(triggerSymbol.length);
	return rawSearchText;
}
var getTokenIds = (tokens) => new Set(tokens.map((token) => token.id));
var getTrackedTokenSignature = (tokens) => tokens.filter((token) => isComposerInputTokenKind(token.kind)).map((token) => JSON.stringify([
	token.kind,
	token.id,
	token.label,
	token.icon,
	token.description,
	token.index,
	token.textOffset,
	token.promptText,
	token.payload
])).join("\n");
function shouldDelegateLongTextPasteToFileHandler(text, supportedExts) {
	return Boolean(text && text.length > 1500 && supportedExts.includes(".txt"));
}
function insertComposerPastedContent(editor, content) {
	editor.chain().focus().setMeta(COMPOSER_SUPPRESS_SUGGESTION_META, true).insertContent(content).run();
}
function exceedsComposerInputMaxLength(currentText, nextText, replacedText = "") {
	return currentText.length - replacedText.length + nextText.length > COMPOSER_INPUT_MAX_LENGTH;
}
function getComposerInputTextWithinLimit(currentText, nextText, replacedText = "") {
	const remainingLength = COMPOSER_INPUT_MAX_LENGTH - (currentText.length - replacedText.length);
	if (remainingLength <= 0) return "";
	return nextText.slice(0, remainingLength);
}
function getComposerReplacementText(view, from, to) {
	if (!view || from >= to) return "";
	return view.state.doc.textBetween(from, to, "\n", getComposerInputLeafText);
}
function getComposerSelectedText(editor) {
	const { from, to } = editor.state.selection;
	if (from >= to) return "";
	return editor.state.doc.textBetween(from, to, "\n", getComposerInputLeafText);
}
function readComposerPayloadObject(payload) {
	return typeof payload === "object" && payload !== null && !Array.isArray(payload) ? payload : null;
}
function readComposerPayloadPath(payload) {
	const path = readComposerPayloadObject(payload)?.path;
	return typeof path === "string" ? path : void 0;
}
function mergeLiveFileTokenPayload(token, liveTokenById) {
	if (token.kind !== "file" || readComposerPayloadPath(token.payload)) return token;
	const liveToken = liveTokenById.get(token.id);
	if (liveToken?.kind !== "file") return token;
	const livePath = readComposerPayloadPath(liveToken.payload);
	if (!livePath) return token;
	return {
		...token,
		payload: {
			...readComposerPayloadObject(liveToken.payload),
			...readComposerPayloadObject(token.payload),
			path: livePath
		}
	};
}
function createComposerDraftFromSelectedContent(view) {
	const { selection } = view.state;
	if (selection.empty) return null;
	const content = selection.content().content.toJSON();
	if (!Array.isArray(content) || content.length === 0) return null;
	return serializeComposerDocument({
		type: "doc",
		content
	});
}
function handleComposerCopy(view, event, liveTokenById) {
	if (!event.clipboardData) return false;
	const draft = createComposerDraftFromSelectedContent(view);
	const richContent = draft ? createComposerRichClipboardContentFromDraft({
		...draft,
		tokens: draft.tokens.map((token) => mergeLiveFileTokenPayload(token, liveTokenById))
	}) : null;
	if (!richContent) return false;
	event.preventDefault();
	event.clipboardData.clearData();
	writeComposerClipboardData(event.clipboardData, richContent);
	return true;
}
function mergeComposerClipboardFiles(prev, files) {
	if (!files.length) return prev;
	const existing = new Set(prev.map((file) => `${file.fileTokenSourceId}:${file.path}`));
	const next = [...prev];
	let changed = false;
	for (const file of files) {
		const key = `${file.fileTokenSourceId}:${file.path}`;
		if (existing.has(key)) continue;
		existing.add(key);
		next.push(file);
		changed = true;
	}
	return changed ? next : prev;
}
function getComposerSelectionState(view, key, isInputHistoryActive) {
	const { doc, selection } = view.state;
	const lastSelectablePosition = Math.max(1, doc.content.size - 1);
	const isCursorAtEnd = selection.empty && selection.from === lastSelectablePosition;
	let isCursorAtHistoryBoundary = false;
	if (isCursorAtEnd) if (key === "ArrowUp" && isInputHistoryActive) isCursorAtHistoryBoundary = true;
	else {
		const topLevelBlockIndex = selection.$head.index(0);
		const isAtDocumentEdgeBlock = key === "ArrowUp" ? topLevelBlockIndex === 0 : topLevelBlockIndex === doc.childCount - 1;
		const direction = key === "ArrowUp" ? "up" : "down";
		isCursorAtHistoryBoundary = isAtDocumentEdgeBlock && view.endOfTextblock(direction);
	}
	return {
		isAllSelected: !selection.empty && selection.from <= 1 && selection.to >= lastSelectablePosition,
		isCursorAtHistoryBoundary
	};
}
var COMPOSER_EDITING_BORDER_HIGHLIGHT_MS = 900;
var COMPOSER_EDITING_BORDER_HIGHLIGHT_TIMER_KEY = "composerEditingBorderHighlight";
function ComposerSurfaceRuntime({ text, onTextChange, tokens, draftTokens, managedTokenKinds, onTokensChange, placeholder, sendMessageShortcut: _sendMessageShortcut, sendDisabled, sendBlockedReason, isLoading, onSendDraft, onPause, supportedExts, setFiles, filesCount, isExpanded, onExpandedChange, quickPanelEnabled, enableDragDrop, enableSpellCheck, editable = true, fontSize, narrowMode, railGutterPx, onFocus, onActionsChange, isInputHistoryActive = false, onInputHistoryNavigate, editingState, getToolLaunchers, toolLaunchersVersion, resolveSkillMarker, resolveKnowledgeBaseMarker, suggestionSources = EMPTY_SUGGESTION_SOURCES, resourceProvider, queueContent, rootPanelLeadingItems, rootPanelAdditionalItems, onRootPanelOpen, onToolLauncherSelect, renderLeftControls, renderBelowControls, topContent, leadingContent, compactWhenSingleLine = false, renderCompactControls, sendAccessory, deferQuickPanel = false, initialTextSelection, deferredIntent }) {
	const [editorReady, setEditorReady] = (0, import_react.useState)(!deferQuickPanel);
	const quickPanelReady = !deferQuickPanel || editorReady;
	const [preferredSendMessageShortcut] = usePreference("chat.input.send_message_shortcut");
	const sendMessageShortcut = _sendMessageShortcut ?? preferredSendMessageShortcut;
	const { t } = useTranslation();
	const quickPanel = useQuickPanel();
	const composerOverridden = useActiveComposerOverride() !== null;
	const closeQuickPanel = quickPanel.close;
	const isQuickPanelVisible = quickPanel.isVisible;
	const pinnedLauncherIds = useComposerPinnedTools();
	const pinnedLauncherIdSet = (0, import_react.useMemo)(() => new Set(pinnedLauncherIds), [pinnedLauncherIds]);
	const quickPanelRef = (0, import_react.useRef)(quickPanel);
	const { setTimeoutTimer } = useTimer();
	const [isEditingBorderHighlighted, setEditingBorderHighlighted] = (0, import_react.useState)(false);
	const editorRef = (0, import_react.useRef)(null);
	const textRef = (0, import_react.useRef)(text);
	const pendingLocalTextEchoRef = (0, import_react.useRef)(null);
	const inputListenersRef = (0, import_react.useRef)(/* @__PURE__ */ new Set());
	const isSyncingTokensRef = (0, import_react.useRef)(false);
	const trackedTokenSignatureRef = (0, import_react.useRef)("");
	const tokenByIdRef = (0, import_react.useRef)(/* @__PURE__ */ new Map());
	const sendDisabledRef = (0, import_react.useRef)(sendDisabled);
	const sendBlockedReasonRef = (0, import_react.useRef)(sendBlockedReason);
	const sendMessageShortcutRef = (0, import_react.useRef)(sendMessageShortcut);
	const setFilesRef = (0, import_react.useRef)(setFiles);
	const onSendDraftRef = (0, import_react.useRef)(onSendDraft);
	const isInputHistoryActiveRef = (0, import_react.useRef)(isInputHistoryActive);
	const onInputHistoryNavigateRef = (0, import_react.useRef)(onInputHistoryNavigate);
	const promptVariableEditRef = (0, import_react.useRef)(null);
	const promptVariableCompositionRef = (0, import_react.useRef)(null);
	const promptVariableSkipTextInputRef = (0, import_react.useRef)(null);
	const isExpandedRef = (0, import_react.useRef)(isExpanded);
	const pointerDownVersionRef = (0, import_react.useRef)(0);
	const filesCountRef = (0, import_react.useRef)(filesCount);
	const managedTokenKindSet = (0, import_react.useMemo)(() => new Set(managedTokenKinds), [managedTokenKinds]);
	const editingHighlightKey = editingState?.highlightKey;
	(0, import_react.useLayoutEffect)(() => {
		quickPanelRef.current = quickPanel;
		isExpandedRef.current = isExpanded;
		filesCountRef.current = filesCount;
		sendDisabledRef.current = sendDisabled;
		sendBlockedReasonRef.current = sendBlockedReason;
		sendMessageShortcutRef.current = sendMessageShortcut;
		setFilesRef.current = setFiles;
		onSendDraftRef.current = onSendDraft;
		isInputHistoryActiveRef.current = isInputHistoryActive;
		onInputHistoryNavigateRef.current = onInputHistoryNavigate;
	}, [
		filesCount,
		isExpanded,
		isInputHistoryActive,
		onInputHistoryNavigate,
		onSendDraft,
		quickPanel,
		sendBlockedReason,
		sendDisabled,
		sendMessageShortcut,
		setFiles
	]);
	(0, import_react.useLayoutEffect)(() => {
		if (composerOverridden && isQuickPanelVisible) closeQuickPanel("composer_override");
	}, [
		closeQuickPanel,
		composerOverridden,
		isQuickPanelVisible
	]);
	(0, import_react.useEffect)(() => {
		textRef.current = text;
	}, [text]);
	(0, import_react.useEffect)(() => {
		tokenByIdRef.current = new Map(tokens.map((token) => [token.id, token]));
	}, [tokens]);
	(0, import_react.useEffect)(() => {
		const handlePointerDown = () => {
			pointerDownVersionRef.current += 1;
		};
		document.addEventListener("pointerdown", handlePointerDown, true);
		return () => document.removeEventListener("pointerdown", handlePointerDown, true);
	}, []);
	(0, import_react.useEffect)(() => {
		if (editingHighlightKey === void 0) {
			setEditingBorderHighlighted(false);
			return;
		}
		setEditingBorderHighlighted(true);
		return setTimeoutTimer(COMPOSER_EDITING_BORDER_HIGHLIGHT_TIMER_KEY, () => setEditingBorderHighlighted(false), COMPOSER_EDITING_BORDER_HIGHLIGHT_MS);
	}, [editingHighlightKey, setTimeoutTimer]);
	const showBlockedSendReason = (0, import_react.useCallback)(() => {
		if (sendBlockedReasonRef.current) toast.error(sendBlockedReasonRef.current);
	}, []);
	const applyComposerText = (0, import_react.useCallback)((nextText) => {
		const limitedText = nextText.slice(0, COMPOSER_INPUT_MAX_LENGTH);
		const editor$1 = editorRef.current;
		if (limitedText === (editor$1 && !editor$1.isDestroyed ? serializeComposerDocument(editor$1).text : textRef.current)) return;
		textRef.current = limitedText;
		pendingLocalTextEchoRef.current = limitedText;
		onTextChange(limitedText);
		editor$1?.commands.setContent(createPromptVariableContent(limitedText), { emitUpdate: false });
	}, [onTextChange]);
	const { handlePaste: handlePaste$1 } = usePasteHandler(text, (0, import_react.useCallback)((value) => {
		applyComposerText(typeof value === "function" ? value(textRef.current) : value);
	}, [applyComposerText]), (0, import_react.useMemo)(() => ({
		supportedExts,
		setFiles,
		onResize: void 0,
		t
	}), [
		supportedExts,
		setFiles,
		t
	]));
	const { handleDragEnter, handleDragLeave, handleDragOver, handleDrop, isDragging } = useFileDragDrop({
		supportedExts,
		setFiles,
		onFolderPathDropped: (path) => {
			const editor$1 = editorRef.current;
			if (!editor$1 || editor$1.isDestroyed) return;
			insertComposerTokenAtCursor(editor$1, createComposerFolderToken(path));
		},
		onTextDropped: (droppedText) => {
			const editor$1 = editorRef.current;
			if (!editor$1) return;
			editor$1.chain().focus().insertContent(createPromptVariableInlineContent(droppedText, { startIndex: getNextPromptVariableIndex(editor$1) })).run();
		},
		enabled: enableDragDrop,
		t
	});
	const focusEditor = (0, import_react.useCallback)((position) => {
		editorRef.current?.commands.focus(position);
	}, []);
	const isEditorComposing = (0, import_react.useCallback)(() => editorRef.current?.view.composing ?? false, []);
	const { compactEditorContentStyle, compactFrameStyle, editorContentStyle, editorElementStyle, frameRef, frameStyle, handleResizeKeyDown, handleTransitionEnd, hasCustomHeight, isResizing: isEditorResizing, maxHeight: editorMaxHeight, minHeight: editorMinHeight, resizeHandleValue, restoreDefaultHeight, startResize: startEditorResize, toggleExpanded: toggleEditorExpanded } = useComposerEditorFrameSizing({
		fontSize,
		isExpanded,
		onExpandedChange,
		focusEditor,
		setTimeoutTimer
	});
	const { isCompact, requestMeasurement: requestCompactMeasurement } = useCompactComposerPresentation({
		enabled: compactWhenSingleLine && !hasCustomHeight,
		frameRef,
		isComposing: isEditorComposing
	});
	const createEditorFocusRestoreSnapshot = (0, import_react.useCallback)(() => ({
		activeElement: document.activeElement,
		pointerDownVersion: pointerDownVersionRef.current
	}), []);
	const shouldRestoreEditorFocus = (0, import_react.useCallback)((snapshot) => {
		if (pointerDownVersionRef.current !== snapshot.pointerDownVersion || !document.hasFocus()) return false;
		const activeElement = document.activeElement;
		return activeElement === snapshot.activeElement || activeElement === document.body || !!activeElement && !!frameRef.current?.contains(activeElement);
	}, [frameRef]);
	const compactMeasurementInputsRef = (0, import_react.useRef)({
		draftTokens,
		fontSize,
		renderCompactControls,
		sendAccessory,
		text,
		tokens
	});
	(0, import_react.useEffect)(() => {
		const previousInputs = compactMeasurementInputsRef.current;
		const inputsChanged = previousInputs.draftTokens !== draftTokens || previousInputs.fontSize !== fontSize || previousInputs.renderCompactControls !== renderCompactControls || previousInputs.sendAccessory !== sendAccessory || previousInputs.text !== text || previousInputs.tokens !== tokens;
		compactMeasurementInputsRef.current = {
			draftTokens,
			fontSize,
			renderCompactControls,
			sendAccessory,
			text,
			tokens
		};
		if (inputsChanged) requestCompactMeasurement();
	}, [
		draftTokens,
		fontSize,
		renderCompactControls,
		requestCompactMeasurement,
		sendAccessory,
		text,
		tokens
	]);
	const toggleEditorExpandedRef = (0, import_react.useRef)(toggleEditorExpanded);
	(0, import_react.useLayoutEffect)(() => {
		toggleEditorExpandedRef.current = toggleEditorExpanded;
	}, [toggleEditorExpanded]);
	const handleTextChangeFromTool = (0, import_react.useCallback)((updater) => {
		const currentText = editorRef.current ? serializeComposerDocument(editorRef.current).text : textRef.current;
		applyComposerText(typeof updater === "function" ? updater(currentText) : updater);
	}, [applyComposerText]);
	const removeToken = (0, import_react.useCallback)((tokenId) => {
		const editor$1 = editorRef.current;
		if (!editor$1 || editor$1.isDestroyed) return;
		removeComposerTokens(editor$1, (token) => token.id === tokenId);
		editor$1.commands.focus();
	}, []);
	const handleShowPastedTextFileInInput = (0, import_react.useCallback)(async (token, nodeViewProps) => {
		const file = isPastedTextFileMetadata(token.payload) ? token.payload : void 0;
		const editor$1 = editorRef.current;
		if (!file || !editor$1 || editor$1.isDestroyed) return;
		try {
			const fileText = await window.api.fs.readText(file.path);
			const currentText = serializeComposerDocument(editor$1).text;
			const textToInsert = getComposerInputTextWithinLimit(currentText, fileText);
			const position = typeof nodeViewProps.getPos === "function" ? nodeViewProps.getPos() : void 0;
			const content = textToInsert ? createPromptVariableInlineContent(textToInsert, { startIndex: getNextPromptVariableIndex(editor$1) }) : [];
			if (typeof position === "number") {
				const chain = editor$1.chain().focus().deleteRange({
					from: position,
					to: position + nodeViewProps.node.nodeSize
				});
				if (content.length > 0) chain.insertContent(content);
				chain.run();
			} else {
				removeComposerTokens(editor$1, (candidate) => candidate.id === token.id);
				if (content.length > 0) editor$1.chain().focus().insertContent(content).run();
				else editor$1.commands.focus();
			}
			setFiles((prev) => prev.filter((candidate) => candidate.fileTokenSourceId !== file.fileTokenSourceId || candidate.path !== file.path));
		} catch {
			toast.error(t("chat.input.file_error"));
		}
	}, [setFiles, t]);
	const insertToken = (0, import_react.useCallback)((token) => {
		const editor$1 = editorRef.current;
		if (!editor$1 || editor$1.isDestroyed) return;
		insertComposerTokenAtCursor(editor$1, token);
	}, []);
	const getDraft = (0, import_react.useCallback)(() => {
		const editor$1 = editorRef.current;
		if (!editor$1 || editor$1.isDestroyed) return {
			text: textRef.current,
			tokens: []
		};
		return serializeComposerDocument(editor$1);
	}, []);
	const replaceDraft = (0, import_react.useCallback)((draft) => {
		const editor$1 = editorRef.current;
		if (!editor$1 || editor$1.isDestroyed) return;
		textRef.current = draft.text;
		pendingLocalTextEchoRef.current = null;
		editor$1.commands.setContent(createComposerDraftContent(draft), { emitUpdate: false });
		trackedTokenSignatureRef.current = getTrackedTokenSignature(draft.tokens);
	}, []);
	(0, import_react.useEffect)(() => {
		onActionsChange?.({
			focus: focusEditor,
			onTextChange: handleTextChangeFromTool,
			replaceDraft,
			toggleExpanded: toggleEditorExpanded,
			removeToken,
			insertToken,
			getDraft
		});
	}, [
		focusEditor,
		getDraft,
		handleTextChangeFromTool,
		insertToken,
		onActionsChange,
		removeToken,
		replaceDraft,
		toggleEditorExpanded
	]);
	const rootPanelOpenRefreshRequestedRef = (0, import_react.useRef)(false);
	const unifiedResourceRequestRef = (0, import_react.useRef)(0);
	const unifiedPanelListRefreshKeyRef = (0, import_react.useRef)(void 0);
	const [unifiedResourceItems, setUnifiedResourceItems] = (0, import_react.useState)([]);
	const lastActiveRootSuggestionSessionGenByPluginKeyRef = (0, import_react.useRef)({});
	const activeRootSuggestionSessionRef = (0, import_react.useRef)(null);
	const pendingRootSuggestionExitRef = (0, import_react.useRef)(null);
	const rootSuggestionStateRef = (0, import_react.useRef)({
		getToolLaunchers,
		onRootPanelOpen,
		onToolLauncherSelect,
		quickPanel,
		resourceProvider,
		rootPanelLeadingItems,
		rootPanelAdditionalItems,
		pinnedLauncherIdSet,
		unifiedResourceItems
	});
	(0, import_react.useLayoutEffect)(() => {
		rootSuggestionStateRef.current = {
			getToolLaunchers,
			onRootPanelOpen,
			onToolLauncherSelect,
			quickPanel,
			resourceProvider,
			rootPanelLeadingItems,
			rootPanelAdditionalItems,
			pinnedLauncherIdSet,
			unifiedResourceItems
		};
	}, [
		getToolLaunchers,
		onRootPanelOpen,
		onToolLauncherSelect,
		pinnedLauncherIdSet,
		quickPanel,
		resourceProvider,
		rootPanelAdditionalItems,
		rootPanelLeadingItems,
		unifiedResourceItems
	]);
	const createUnifiedPanelOptions = (0, import_react.useCallback)(({ initialSearchText, inputAdapter: inputAdapter$1, queryAnchor, resourceItems, triggerInfo, includePinnedLaunchers = false }) => {
		const { getToolLaunchers: getToolLaunchers$1, onToolLauncherSelect: onToolLauncherSelect$1, pinnedLauncherIdSet: pinnedLauncherIdSet$1, quickPanel: quickPanel$1, rootPanelAdditionalItems: rootPanelAdditionalItems$1, rootPanelLeadingItems: rootPanelLeadingItems$1 } = rootSuggestionStateRef.current;
		const launchers = getToolLaunchers$1?.() ?? [];
		const isButtonRoot = (triggerInfo?.type ?? "button") === "button";
		return createUnifiedQuickPanelOpenOptions(launchers, {
			onToolLauncherSelect: onToolLauncherSelect$1,
			inputAdapter: inputAdapter$1,
			quickPanel: quickPanel$1,
			title: t("settings.quickPanel.title"),
			leadingItems: rootPanelLeadingItems$1,
			additionalItems: rootPanelAdditionalItems$1,
			resourceItems,
			queryAnchor,
			triggerInfo,
			initialSearchText,
			excludedLauncherIds: isButtonRoot && !includePinnedLaunchers ? pinnedLauncherIdSet$1 : void 0
		});
	}, [t]);
	const loadUnifiedResourceItems = (0, import_react.useCallback)(async ({ inputAdapter: inputAdapter$1, queryAnchor, searchText, triggerInfo }) => {
		const trimmedQuery = searchText.trim();
		const requestId = ++unifiedResourceRequestRef.current;
		const { quickPanel: quickPanel$1, resourceProvider: resourceProvider$1 } = rootSuggestionStateRef.current;
		const panelGeneration = quickPanel$1.getPanelGeneration();
		if (!resourceProvider$1 || trimmedQuery.length === 0) {
			setUnifiedResourceItems([]);
			return;
		}
		let resourceItems;
		try {
			resourceItems = await resourceProvider$1(trimmedQuery, {
				inputAdapter: inputAdapter$1,
				quickPanel: quickPanel$1,
				queryAnchor,
				searchText: trimmedQuery,
				triggerInfo
			});
		} catch {
			if (requestId !== unifiedResourceRequestRef.current) return;
			if (quickPanel$1.getPanelGeneration() !== panelGeneration || !quickPanel$1.isVisible || quickPanel$1.symbol !== ComposerPanelSymbol.Root) return;
			setUnifiedResourceItems([]);
			quickPanel$1.updateList(createUnifiedPanelOptions({
				inputAdapter: inputAdapter$1,
				queryAnchor,
				resourceItems: [],
				triggerInfo
			}).list);
			return;
		}
		if (requestId !== unifiedResourceRequestRef.current) return;
		if (quickPanel$1.getPanelGeneration() !== panelGeneration || !quickPanel$1.isVisible || quickPanel$1.symbol !== ComposerPanelSymbol.Root) return;
		setUnifiedResourceItems(resourceItems);
		quickPanel$1.updateList(createUnifiedPanelOptions({
			inputAdapter: inputAdapter$1,
			queryAnchor,
			resourceItems,
			triggerInfo
		}).list);
	}, [createUnifiedPanelOptions]);
	const openUnifiedComposerPanel = (0, import_react.useCallback)(({ initialSearchText, inputAdapter: inputAdapter$1, queryAnchor, requestRootPanelOpen = true, triggerInfo }) => {
		const { onRootPanelOpen: onRootPanelOpen$1, quickPanel: quickPanel$1 } = rootSuggestionStateRef.current;
		if (requestRootPanelOpen) onRootPanelOpen$1?.();
		setUnifiedResourceItems([]);
		quickPanel$1.open(createUnifiedPanelOptions({
			initialSearchText,
			inputAdapter: inputAdapter$1,
			queryAnchor,
			resourceItems: [],
			triggerInfo
		}));
	}, [createUnifiedPanelOptions]);
	const openUnifiedComposerLauncherSubmenu = (0, import_react.useCallback)(({ inputAdapter: inputAdapter$1, launcherId, queryAnchor, searchText, triggerInfo }) => {
		const { quickPanel: quickPanel$1 } = rootSuggestionStateRef.current;
		const rootPanelOptions = createUnifiedPanelOptions({
			initialSearchText: searchText,
			inputAdapter: inputAdapter$1,
			queryAnchor,
			resourceItems: [],
			triggerInfo,
			includePinnedLaunchers: true
		});
		const launcherItem = rootPanelOptions.list.find((item) => item.id === launcherId);
		if (!launcherItem?.isMenu || launcherItem.disabled) return false;
		launcherItem.action?.({
			action: "click",
			context: {
				...quickPanel$1,
				triggerInfo: rootPanelOptions.triggerInfo
			},
			item: launcherItem,
			parentPanel: rootPanelOptions,
			queryAnchor,
			searchText,
			inputAdapter: inputAdapter$1
		});
		return true;
	}, [createUnifiedPanelOptions]);
	const rootSuggestionSources = (0, import_react.useMemo)(() => ROOT_QUICK_PANEL_TRIGGER_SOURCES.map(({ char, pluginKey }) => ({
		pluginKey,
		char,
		title: t("settings.quickPanel.title"),
		renderMode: "headless",
		allowedPrefixes: ROOT_QUICK_PANEL_ALLOWED_PREFIXES,
		items: () => [],
		onActiveChange: ({ editor: editor$1, query, range, text: text$1 }) => {
			const { onRootPanelOpen: onRootPanelOpen$1, quickPanel: quickPanel$1 } = rootSuggestionStateRef.current;
			const { cursorOffset, queryAnchor, textBeforeTrigger, triggerText } = getComposerSuggestionTriggerContext(editor$1, {
				range,
				query,
				text: text$1,
				triggerChar: char
			});
			if (!hasComposerQuickPanelTriggerBoundary(textBeforeTrigger) || cursorOffset !== queryAnchor + triggerText.length) {
				const activeRootSuggestionSession = activeRootSuggestionSessionRef.current;
				if (!activeRootSuggestionSession || activeRootSuggestionSession.pluginKey === pluginKey) {
					activeRootSuggestionSessionRef.current = null;
					pendingRootSuggestionExitRef.current = null;
					rootPanelOpenRefreshRequestedRef.current = false;
					if (quickPanel$1.isVisible && quickPanel$1.symbol === ComposerPanelSymbol.Root) quickPanel$1.close("input_prefix_invalid");
				}
				return;
			}
			const lastActiveGenForThisPluginKey = (lastActiveRootSuggestionSessionGenByPluginKeyRef.current[pluginKey] ?? 0) + 1;
			const pendingRootSuggestionExit = pendingRootSuggestionExitRef.current;
			const isRestartingExitedRootSource = pendingRootSuggestionExit != null && pendingRootSuggestionExit.pluginKey === pluginKey && pendingRootSuggestionExit.generation === lastActiveGenForThisPluginKey - 1;
			if (isRestartingExitedRootSource) pendingRootSuggestionExitRef.current = null;
			lastActiveRootSuggestionSessionGenByPluginKeyRef.current[pluginKey] = lastActiveGenForThisPluginKey;
			activeRootSuggestionSessionRef.current = {
				pluginKey,
				generation: lastActiveGenForThisPluginKey
			};
			const triggerInfo = {
				type: "input",
				position: queryAnchor,
				originalText: triggerText
			};
			if (!rootPanelOpenRefreshRequestedRef.current || isRestartingExitedRootSource) {
				rootPanelOpenRefreshRequestedRef.current = true;
				onRootPanelOpen$1?.();
			}
			openUnifiedComposerPanel({
				inputAdapter: createComposerInputAdapter(editor$1),
				queryAnchor,
				requestRootPanelOpen: false,
				triggerInfo
			});
		},
		onKeyDown: ({ event }) => {
			return rootSuggestionStateRef.current.quickPanel.dispatchKeyDown(event) ?? false;
		},
		onExit: () => {
			const exitingRootSuggestionSession = {
				pluginKey,
				generation: lastActiveRootSuggestionSessionGenByPluginKeyRef.current[pluginKey] ?? 0
			};
			pendingRootSuggestionExitRef.current = exitingRootSuggestionSession;
			window.setTimeout(() => {
				const activeRootSuggestionSession = activeRootSuggestionSessionRef.current;
				if (!(!activeRootSuggestionSession || activeRootSuggestionSession.pluginKey === exitingRootSuggestionSession.pluginKey && activeRootSuggestionSession.generation === exitingRootSuggestionSession.generation)) {
					if (pendingRootSuggestionExitRef.current?.pluginKey === exitingRootSuggestionSession.pluginKey && pendingRootSuggestionExitRef.current.generation === exitingRootSuggestionSession.generation) pendingRootSuggestionExitRef.current = null;
					return;
				}
				activeRootSuggestionSessionRef.current = null;
				rootPanelOpenRefreshRequestedRef.current = false;
				if (pendingRootSuggestionExitRef.current?.pluginKey === exitingRootSuggestionSession.pluginKey && pendingRootSuggestionExitRef.current.generation === exitingRootSuggestionSession.generation) pendingRootSuggestionExitRef.current = null;
				const { quickPanel: quickPanel$1 } = rootSuggestionStateRef.current;
				if (quickPanel$1.isVisible && quickPanel$1.symbol === ComposerPanelSymbol.Root) quickPanel$1.close();
			}, 0);
		}
	})), [openUnifiedComposerPanel, t]);
	const suggestionPanelStateRef = (0, import_react.useRef)({ quickPanel });
	(0, import_react.useLayoutEffect)(() => {
		suggestionPanelStateRef.current = { quickPanel };
	}, [quickPanel]);
	const quickPanelSuggestionSources = (0, import_react.useMemo)(() => suggestionSources.map((source) => ({
		...source,
		renderMode: "headless",
		onActiveChange: (options) => {
			source.onActiveChange?.(options);
			const { quickPanel: quickPanel$1 } = suggestionPanelStateRef.current;
			const { cursorOffset, queryAnchor, textBeforeTrigger, triggerText } = getComposerSuggestionTriggerContext(options.editor, {
				range: options.range,
				query: options.query,
				text: options.text,
				triggerChar: source.char
			});
			if (!hasComposerQuickPanelTriggerBoundary(textBeforeTrigger) || cursorOffset !== queryAnchor + triggerText.length) {
				if (quickPanel$1.isVisible && quickPanel$1.symbol === source.char) quickPanel$1.close("input_prefix_invalid");
				return;
			}
			quickPanel$1.open({
				title: typeof source.title === "string" ? source.title : void 0,
				list: options.items.map((item) => createComposerSuggestionQuickPanelItem(item, {
					editor: options.editor,
					query: options.query,
					range: options.range
				})),
				symbol: source.char,
				pageSize: source.pageSize,
				multiple: source.multiple,
				queryAnchor,
				triggerInfo: {
					type: "input",
					position: queryAnchor,
					originalText: triggerText
				},
				trackInputQuery: true,
				manageListExternally: true
			});
		},
		onKeyDown: (props) => {
			if (suggestionPanelStateRef.current.quickPanel.dispatchKeyDown(props.event)) return true;
			return source.onKeyDown?.(props) ?? false;
		},
		onExit: (options) => {
			source.onExit?.(options);
			window.setTimeout(() => {
				const { quickPanel: quickPanel$1 } = suggestionPanelStateRef.current;
				if (quickPanel$1.isVisible && quickPanel$1.symbol === source.char) quickPanel$1.close();
			}, 0);
		}
	})), [suggestionSources]);
	const activeSuggestionSources = (0, import_react.useMemo)(() => quickPanelEnabled ? [...rootSuggestionSources, ...quickPanelSuggestionSources] : [], [
		quickPanelEnabled,
		rootSuggestionSources,
		quickPanelSuggestionSources
	]);
	const renderComposerToken = (0, import_react.useCallback)((token, { selected, nodeViewProps }) => {
		const currentToken = tokenByIdRef.current.get(token.id) ?? token;
		if (currentToken.kind !== "file") return void 0;
		const fileToken = currentToken;
		const pastedTextToken = isPastedTextFileMetadata(fileToken.payload) ? fileToken : void 0;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FileComposerToken, {
			token: fileToken,
			selected,
			imageIconPreview: true,
			onRemove: () => removeToken(fileToken.id),
			removeLabel: t("common.delete"),
			tooltipActions: pastedTextToken ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				type: "button",
				variant: "link",
				size: "sm",
				className: "h-auto min-h-0 w-fit justify-start gap-0 border-0 p-0 text-left font-medium text-link text-xs leading-4 shadow-none hover:text-link focus-visible:border-0 focus-visible:text-link focus-visible:underline focus-visible:ring-0 focus-visible:ring-offset-0",
				onMouseDown: (event) => {
					event.preventDefault();
					event.stopPropagation();
				},
				onClick: (event) => {
					event.preventDefault();
					event.stopPropagation();
					handleShowPastedTextFileInInput(pastedTextToken, nodeViewProps);
				},
				children: t("chat.input.paste_text_file")
			}) : void 0
		});
	}, [
		handleShowPastedTextFileInInput,
		removeToken,
		t
	]);
	const editorExtensions = (0, import_react.useMemo)(() => createComposerEditorPreset({
		placeholder,
		renderToken: renderComposerToken,
		suggestionSources: activeSuggestionSources
	}), [
		activeSuggestionSources,
		placeholder,
		renderComposerToken
	]);
	const memoizedEditorProps = (0, import_react.useMemo)(() => ({
		attributes: {
			class: cn("composer-tiptap after:hidden! box-border block w-full overflow-auto whitespace-pre-wrap break-words rounded-none text-foreground outline-none transition-none! [&::-webkit-scrollbar]:w-[3px]", hasCustomHeight ? COMPOSER_EDITOR_EXPANDED_MAX_HEIGHT_CLASS : COMPOSER_EDITOR_COLLAPSED_MAX_HEIGHT_CLASS, hasCustomHeight && "h-full"),
			style: editorElementStyle
		},
		handleKeyDown: (view, event) => {
			const isEnterKey = event.key === "Enter" || event.key === "NumpadEnter";
			const isImeComposing = event.isComposing || view?.composing || event.keyCode === 229;
			const isEnterPressed = isEnterKey && !isImeComposing;
			const isShiftEnterPressed = isEnterPressed && event.shiftKey && !event.ctrlKey && !event.metaKey && !event.altKey;
			const qp = quickPanelRef.current;
			if (isEnterKey && isImeComposing && !event.isComposing) return false;
			if ([
				"ArrowUp",
				"ArrowDown",
				"PageUp",
				"PageDown",
				"Tab",
				"Enter",
				"NumpadEnter",
				"Escape"
			].includes(event.key)) {
				if (qp.dispatchKeyDown(event)) return true;
				if (qp.isVisible && isShiftEnterPressed) return false;
				if (qp.isVisible && isEnterPressed) {
					event.preventDefault();
					event.stopPropagation();
					return true;
				}
			}
			if ((event.key === "ArrowUp" || event.key === "ArrowDown") && !event.ctrlKey && !event.metaKey && !event.altKey && !event.shiftKey && shouldHandleInputHistoryNavigation({
				...getComposerSelectionState(view, event.key, isInputHistoryActiveRef.current),
				isComposing: event.isComposing,
				isQuickPanelVisible: qp.isVisible,
				key: event.key,
				text: textRef.current
			})) {
				const direction = event.key === "ArrowUp" ? "up" : "down";
				if (onInputHistoryNavigateRef.current?.(direction) ?? false) {
					event.preventDefault();
					event.stopPropagation();
					return true;
				}
			}
			if (event.key === "Escape" && isExpandedRef.current) {
				event.stopPropagation();
				toggleEditorExpandedRef.current(false);
				return true;
			}
			if (event.key === "Tab" && !event.isComposing && !quickPanelRef.current.isVisible) {
				const targetToken = editorRef.current ? selectPromptVariableToken(editorRef.current, event.shiftKey ? -1 : 1) : null;
				if (targetToken) {
					event.preventDefault();
					event.stopPropagation();
					promptVariableEditRef.current = {
						tokenId: targetToken.id,
						started: false
					};
					return true;
				}
			}
			if (isEnterPressed && isComposerSendKeyPressed(event, sendMessageShortcutRef.current)) {
				event.preventDefault();
				if (event.repeat) return true;
				if (!sendDisabledRef.current && editorRef.current) {
					const draft = serializeComposerDocument(editorRef.current);
					const focusRestoreSnapshot = createEditorFocusRestoreSnapshot();
					Promise.resolve(onSendDraftRef.current(draft)).finally(() => {
						if (shouldRestoreEditorFocus(focusRestoreSnapshot)) focusEditor();
					});
				} else showBlockedSendReason();
				return true;
			}
			if (isEnterPressed && view) {
				const { from, to } = view.state.selection;
				const replacedText = getComposerReplacementText(view, from, to);
				if (exceedsComposerInputMaxLength(textRef.current, "\n", replacedText)) {
					event.preventDefault();
					return true;
				}
			}
			if (event.key === "Backspace" && textRef.current.trim().length === 0 && filesCountRef.current > 0 && (!editorRef.current || !hasComposerTokenBeforeSelection(editorRef.current))) {
				setFilesRef.current((prev) => prev.slice(0, -1));
				event.preventDefault();
				return true;
			}
			return false;
		},
		handleTextInput: (view, from, to, insertedText) => {
			const editor$1 = editorRef.current;
			if (!editor$1 || editor$1.isDestroyed) return false;
			const selectedPromptVariable = getSelectedPromptVariableToken(editor$1);
			if (!selectedPromptVariable) {
				const replacedText = getComposerReplacementText(view, from, to);
				if (!exceedsComposerInputMaxLength(textRef.current, insertedText, replacedText)) return false;
				const limitedInsertedText = getComposerInputTextWithinLimit(textRef.current, insertedText, replacedText);
				if (limitedInsertedText && view) view.dispatch(view.state.tr.insertText(limitedInsertedText, from, to));
				return true;
			}
			const composingToken = promptVariableCompositionRef.current;
			if (editor$1.view.composing || composingToken?.tokenId === selectedPromptVariable.token.id) {
				if (composingToken) composingToken.text = insertedText || composingToken.text;
				return true;
			}
			const skippedTextInput = promptVariableSkipTextInputRef.current;
			if (skippedTextInput?.tokenId === selectedPromptVariable.token.id && skippedTextInput.text === insertedText) {
				promptVariableSkipTextInputRef.current = null;
				return true;
			}
			const editState = promptVariableEditRef.current;
			const nextPromptText = `${editState?.tokenId === selectedPromptVariable.token.id && editState.started ? selectedPromptVariable.token.promptText ?? "" : ""}${insertedText}`;
			const limitedPromptText = getComposerInputTextWithinLimit(textRef.current, nextPromptText, selectedPromptVariable.token.promptText ?? "");
			if (!limitedPromptText) return true;
			updateSelectedPromptVariableToken(editor$1, limitedPromptText);
			promptVariableEditRef.current = {
				tokenId: selectedPromptVariable.token.id,
				started: true
			};
			return true;
		},
		handleDOMEvents: {
			copy: (view, event) => handleComposerCopy(view, event, tokenByIdRef.current),
			cut: (view, event) => {
				if (!handleComposerCopy(view, event, tokenByIdRef.current)) return false;
				const editor$1 = editorRef.current;
				if (editor$1 && !editor$1.isDestroyed && editor$1.isEditable) editor$1.chain().focus().deleteSelection().run();
				return true;
			},
			compositionstart: () => {
				const editor$1 = editorRef.current;
				if (!editor$1 || editor$1.isDestroyed) return false;
				const selectedPromptVariable = getSelectedPromptVariableToken(editor$1);
				if (!selectedPromptVariable) return false;
				promptVariableCompositionRef.current = {
					tokenId: selectedPromptVariable.token.id,
					text: ""
				};
				promptVariableEditRef.current = {
					tokenId: selectedPromptVariable.token.id,
					started: false
				};
				return false;
			},
			compositionupdate: (_view, event) => {
				const editor$1 = editorRef.current;
				const composingToken = promptVariableCompositionRef.current;
				if (!editor$1 || editor$1.isDestroyed || !composingToken) return false;
				if (getSelectedPromptVariableToken(editor$1)?.token.id !== composingToken.tokenId) return false;
				composingToken.text = ("data" in event && typeof event.data === "string" ? event.data : "") || composingToken.text;
				return true;
			},
			compositionend: (_view, event) => {
				const editor$1 = editorRef.current;
				const composingToken = promptVariableCompositionRef.current;
				promptVariableCompositionRef.current = null;
				if (!editor$1 || editor$1.isDestroyed || !composingToken) return false;
				const selectedPromptVariable = getSelectedPromptVariableToken(editor$1);
				if (selectedPromptVariable?.token.id !== composingToken.tokenId) return false;
				const nextValue = ("data" in event && typeof event.data === "string" ? event.data : "") || composingToken.text;
				if (!nextValue) return true;
				const limitedNextValue = getComposerInputTextWithinLimit(textRef.current, nextValue, selectedPromptVariable.token.promptText ?? "");
				if (!limitedNextValue) return true;
				updateSelectedPromptVariableToken(editor$1, limitedNextValue);
				promptVariableEditRef.current = {
					tokenId: selectedPromptVariable.token.id,
					started: true
				};
				promptVariableSkipTextInputRef.current = {
					tokenId: selectedPromptVariable.token.id,
					text: limitedNextValue
				};
				return true;
			}
		}
	}), [
		createEditorFocusRestoreSnapshot,
		editorElementStyle,
		focusEditor,
		hasCustomHeight,
		shouldRestoreEditorFocus,
		showBlockedSendReason
	]);
	const memoizedHandlePaste = (0, import_react.useCallback)((view, event) => {
		const pastedText = event.clipboardData?.getData("text/plain") || event.clipboardData?.getData("text") || "";
		const pastedHtml = event.clipboardData?.getData("text/html") || "";
		const editor$1 = view.dom.editor;
		const selectedPromptVariable = editor$1 ? getSelectedPromptVariableToken(editor$1) : null;
		if (editor$1 && selectedPromptVariable && pastedText) {
			event.preventDefault();
			const limitedPastedText = getComposerInputTextWithinLimit(textRef.current, pastedText, selectedPromptVariable.token.promptText ?? "");
			if (!limitedPastedText) return true;
			updateSelectedPromptVariableToken(editor$1, limitedPastedText);
			promptVariableEditRef.current = {
				tokenId: selectedPromptVariable.token.id,
				started: true
			};
			return true;
		}
		const shouldDelegateLongTextPaste = shouldDelegateLongTextPasteToFileHandler(pastedText, supportedExts);
		if (shouldDelegateLongTextPaste) {
			event.preventDefault();
			handlePaste$1(event);
			return true;
		}
		let textToInsert = pastedText;
		if (editor$1 && pastedText) {
			const selectedText = getComposerSelectedText(editor$1);
			textToInsert = getComposerInputTextWithinLimit(textRef.current, pastedText, selectedText);
			if (!textToInsert) {
				event.preventDefault();
				return true;
			}
		}
		if (editor$1 && textToInsert === pastedText) {
			const pasteOptions = {
				promptVariableStartIndex: getNextPromptVariableIndex(editor$1),
				resolveSkillMarker,
				resolveKnowledgeBaseMarker
			};
			const clipboardPasteOverride = getComposerClipboardPasteOverride(readComposerClipboardFragmentFromDataTransfer(event.clipboardData), pasteOptions) ?? getComposerClipboardPasteOverride(readComposerClipboardFragmentFromSessionCache(pastedText), pasteOptions);
			if (clipboardPasteOverride !== null) {
				event.preventDefault();
				insertComposerPastedContent(editor$1, clipboardPasteOverride.content);
				if (clipboardPasteOverride.files.length > 0) setFilesRef.current((prev) => mergeComposerClipboardFiles(prev, clipboardPasteOverride.files));
				return true;
			}
		}
		const plainTextOverride = getComposerPlainTextPasteOverride(textToInsert, {
			inlineLongText: !shouldDelegateLongTextPaste,
			promptVariableStartIndex: editor$1 ? getNextPromptVariableIndex(editor$1) : 0,
			resolveSkillMarker,
			resolveKnowledgeBaseMarker
		});
		if (plainTextOverride !== null && editor$1) {
			event.preventDefault();
			insertComposerPastedContent(editor$1, plainTextOverride);
			return true;
		}
		if (!pastedText && pastedHtml.includes("data-composer-token")) {
			event.preventDefault();
			return true;
		}
		if (!pastedText && hasClipboardFiles(event.clipboardData)) {
			event.preventDefault();
			handlePaste$1(event);
			return true;
		}
		handlePaste$1(event);
		return false;
	}, [
		handlePaste$1,
		resolveSkillMarker,
		resolveKnowledgeBaseMarker,
		supportedExts
	]);
	const editor = useRichTextEditorKernel({
		extensions: editorExtensions,
		content: createComposerDraftContent({
			text,
			tokens: draftTokens ?? []
		}),
		editable,
		immediatelyRender: false,
		enableSpellCheck,
		editorProps: memoizedEditorProps,
		handlePaste: memoizedHandlePaste,
		onUpdate: ({ editor: updatedEditor }) => {
			requestCompactMeasurement();
			if (tokenizePromptVariablesInEditor(updatedEditor)) return;
			const draft = serializeComposerDocument(updatedEditor);
			const nextText = draft.text;
			textRef.current = nextText;
			pendingLocalTextEchoRef.current = nextText;
			onTextChange(nextText);
			const inputEventCause = isSyncingTokensRef.current ? "state-sync" : "user-input";
			inputListenersRef.current.forEach((listener) => listener({
				isComposing: updatedEditor.view.composing,
				cause: inputEventCause
			}));
			const nextTrackedTokenSignature = getTrackedTokenSignature(draft.tokens);
			if (!isSyncingTokensRef.current) {
				if (nextTrackedTokenSignature !== trackedTokenSignatureRef.current) {
					trackedTokenSignatureRef.current = nextTrackedTokenSignature;
					onTokensChange(draft.tokens);
				}
			} else trackedTokenSignatureRef.current = nextTrackedTokenSignature;
		},
		onCreate: ({ editor: createdEditor }) => {
			window.requestAnimationFrame(() => {
				(0, import_react.startTransition)(() => setEditorReady(true));
			});
			const focusRestoreSnapshot = createEditorFocusRestoreSnapshot();
			setTimeoutTimer("composerSurfaceFocus", () => {
				if (!createdEditor || createdEditor.isDestroyed || !shouldRestoreEditorFocus(focusRestoreSnapshot)) return;
				if (initialTextSelection) {
					createdEditor.chain().focus().setTextSelection({
						from: getComposerPositionAtTextOffset(createdEditor, initialTextSelection.start),
						to: getComposerPositionAtTextOffset(createdEditor, initialTextSelection.end)
					}).run();
					return;
				}
				createdEditor.commands.focus("end");
			}, 0);
		}
	});
	(0, import_react.useEffect)(() => {
		editorRef.current = editor;
	}, [editor]);
	(0, import_react.useEffect)(() => {
		if (!editor || editor.isDestroyed) return;
		if (serializeComposerDocument(editor).text === text) {
			pendingLocalTextEchoRef.current = null;
			return;
		}
		if (pendingLocalTextEchoRef.current === text) {
			pendingLocalTextEchoRef.current = null;
			return;
		}
		pendingLocalTextEchoRef.current = null;
		editor.commands.setContent(createComposerDraftContent({
			text,
			tokens: draftTokens ?? []
		}), { emitUpdate: false });
	}, [
		draftTokens,
		editor,
		text
	]);
	(0, import_react.useEffect)(() => {
		if (editingHighlightKey === void 0) return;
		focusEditor();
	}, [editingHighlightKey, focusEditor]);
	(0, import_react.useEffect)(() => {
		if (!editor || editor.isDestroyed) return;
		const draft = serializeComposerDocument(editor);
		const desiredTokenIds = getTokenIds(tokens);
		isSyncingTokensRef.current = true;
		try {
			for (const token of tokens) addMissingToken(editor, token, draft.tokens);
			removeComposerTokens(editor, (token) => managedTokenKindSet.has(token.kind) && !desiredTokenIds.has(token.id));
		} finally {
			isSyncingTokensRef.current = false;
		}
		trackedTokenSignatureRef.current = getTrackedTokenSignature(serializeComposerDocument(editor).tokens);
	}, [
		editor,
		managedTokenKindSet,
		tokens
	]);
	const inputAdapter = (0, import_react.useMemo)(() => {
		if (!editor) return void 0;
		return {
			getText: () => getComposerInputText(editor),
			getCursorOffset: () => getComposerCursorTextOffset(editor),
			insertText: (insertedText) => {
				editor.chain().focus().insertContent(createPromptVariableInlineContent(insertedText, { startIndex: getNextPromptVariableIndex(editor) })).run();
			},
			insertToken: (token) => {
				insertComposerTokenAtCursor(editor, token);
			},
			deleteTriggerRange: (range) => {
				deleteComposerTextRange(editor, range);
			},
			focus: () => {
				editor.commands.focus();
			},
			subscribeInput: (listener) => {
				inputListenersRef.current.add(listener);
				return () => {
					inputListenersRef.current.delete(listener);
				};
			}
		};
	}, [editor]);
	const isRootQuickPanelVisible = quickPanelEnabled && quickPanel.isVisible && quickPanel.symbol === ComposerPanelSymbol.Root;
	const rootQuickPanelQueryAnchor = quickPanel.queryAnchor;
	const rootQuickPanelTriggerInfo = quickPanel.triggerInfo;
	const rootQuickPanelInitialSearchText = quickPanel.initialSearchText;
	(0, import_react.useEffect)(() => {
		if (!isRootQuickPanelVisible) {
			unifiedPanelListRefreshKeyRef.current = void 0;
			return;
		}
		const currentQuickPanel = quickPanelRef.current;
		const nextList = createUnifiedPanelOptions({
			inputAdapter,
			resourceItems: unifiedResourceItems,
			initialSearchText: rootQuickPanelInitialSearchText,
			queryAnchor: rootQuickPanelQueryAnchor,
			triggerInfo: rootQuickPanelTriggerInfo
		}).list;
		const nextListSignature = `${toolLaunchersVersion}${getQuickPanelItemsSignature(nextList)}`;
		const previous = unifiedPanelListRefreshKeyRef.current;
		if (previous?.signature === nextListSignature && previous.leadingItems === rootPanelLeadingItems && previous.additionalItems === rootPanelAdditionalItems) return;
		unifiedPanelListRefreshKeyRef.current = {
			signature: nextListSignature,
			leadingItems: rootPanelLeadingItems,
			additionalItems: rootPanelAdditionalItems
		};
		currentQuickPanel.updateList(nextList);
	}, [
		createUnifiedPanelOptions,
		inputAdapter,
		isRootQuickPanelVisible,
		rootQuickPanelInitialSearchText,
		rootQuickPanelQueryAnchor,
		rootQuickPanelTriggerInfo,
		rootPanelAdditionalItems,
		rootPanelLeadingItems,
		pinnedLauncherIdSet,
		toolLaunchersVersion,
		unifiedResourceItems
	]);
	(0, import_react.useEffect)(() => {
		if (!isRootQuickPanelVisible || !inputAdapter) return;
		if (!resourceProvider) {
			unifiedResourceRequestRef.current += 1;
			setUnifiedResourceItems((items) => items.length === 0 ? items : []);
			return;
		}
		const syncResourceItems = () => {
			loadUnifiedResourceItems({
				inputAdapter,
				queryAnchor: rootQuickPanelQueryAnchor,
				searchText: getComposerUnifiedPanelSearchText(inputAdapter, rootQuickPanelQueryAnchor, rootQuickPanelTriggerInfo),
				triggerInfo: rootQuickPanelTriggerInfo
			});
		};
		syncResourceItems();
		return inputAdapter.subscribeInput?.((event) => {
			if (event?.isComposing) return;
			syncResourceItems();
		});
	}, [
		inputAdapter,
		isRootQuickPanelVisible,
		loadUnifiedResourceItems,
		resourceProvider,
		rootQuickPanelQueryAnchor,
		rootQuickPanelTriggerInfo
	]);
	(0, import_react.useEffect)(() => {
		pasteHandling_default.init();
		return pasteHandling_default.registerHandler("inputbar", handlePaste$1);
	}, [handlePaste$1]);
	const sendDraft = (0, import_react.useCallback)(() => {
		if (!editor) return;
		if (sendDisabled) {
			showBlockedSendReason();
			return;
		}
		const draft = serializeComposerDocument(editor);
		const focusRestoreSnapshot = createEditorFocusRestoreSnapshot();
		Promise.resolve(onSendDraft(draft)).finally(() => {
			if (shouldRestoreEditorFocus(focusRestoreSnapshot)) focusEditor();
		});
	}, [
		createEditorFocusRestoreSnapshot,
		editor,
		focusEditor,
		onSendDraft,
		sendDisabled,
		shouldRestoreEditorFocus,
		showBlockedSendReason
	]);
	const onSaveEditing = editingState?.onSave;
	const saveEditingDraft = (0, import_react.useCallback)(() => {
		if (!editor || !onSaveEditing) return;
		if (sendDisabled) {
			showBlockedSendReason();
			return;
		}
		onSaveEditing(serializeComposerDocument(editor));
	}, [
		editor,
		onSaveEditing,
		sendDisabled,
		showBlockedSendReason
	]);
	const handleExpandControlClick = (0, import_react.useCallback)(() => {
		if (hasCustomHeight) {
			restoreDefaultHeight();
			return;
		}
		toggleEditorExpanded();
	}, [
		hasCustomHeight,
		restoreDefaultHeight,
		toggleEditorExpanded
	]);
	const unifiedPanelAvailable = (0, import_react.useMemo)(() => {
		if (!quickPanelReady || !quickPanelEnabled) return false;
		return hasUnifiedQuickPanelRootContent(getToolLaunchers?.() ?? [], {
			leadingItems: rootPanelLeadingItems,
			additionalItems: rootPanelAdditionalItems
		});
	}, [
		getToolLaunchers,
		quickPanelReady,
		quickPanelEnabled,
		rootPanelAdditionalItems,
		rootPanelLeadingItems,
		toolLaunchersVersion
	]);
	const unifiedPanelControl = (0, import_react.useMemo)(() => ({
		available: unifiedPanelAvailable,
		open: (options) => {
			if (!unifiedPanelAvailable) return;
			const { quickPanel: quickPanel$1 } = rootSuggestionStateRef.current;
			const requestedSearchText = options?.searchText ?? "";
			const isButtonPanelVisible = quickPanel$1.isVisible && quickPanel$1.triggerInfo?.type === "button";
			const expectedPanelSymbol = (options?.launcherId ? getToolLaunchers?.().find((launcher) => launcher.id === options.launcherId) : void 0)?.panelSymbol ?? options?.launcherId;
			const isSameLauncherPanel = Boolean(options?.launcherId && quickPanel$1.symbol === expectedPanelSymbol);
			const isSameRootPanel = quickPanel$1.symbol === ComposerPanelSymbol.Root && (quickPanel$1.initialSearchText ?? "") === requestedSearchText;
			if (isButtonPanelVisible && (isSameLauncherPanel || isSameRootPanel)) {
				quickPanel$1.close("toggle");
				inputAdapter?.focus();
				return;
			}
			const queryAnchor = inputAdapter?.getCursorOffset?.() ?? textRef.current.length;
			const triggerInfo = {
				type: "button",
				position: queryAnchor
			};
			if (!(options?.launcherId ? openUnifiedComposerLauncherSubmenu({
				inputAdapter,
				launcherId: options.launcherId,
				queryAnchor,
				searchText: options.searchText,
				triggerInfo
			}) : false)) openUnifiedComposerPanel({
				initialSearchText: options?.searchText,
				inputAdapter,
				queryAnchor,
				triggerInfo
			});
			inputAdapter?.focus();
		}
	}), [
		getToolLaunchers,
		inputAdapter,
		openUnifiedComposerLauncherSubmenu,
		openUnifiedComposerPanel,
		unifiedPanelAvailable
	]);
	const unifiedPanelOpen = unifiedPanelControl.open;
	(0, import_react.useEffect)(() => {
		if (!deferredIntent || !editor) return;
		const { transfer, openPanel, insertToken: pendingToken } = deferredIntent;
		delete deferredIntent.transfer;
		delete deferredIntent.openPanel;
		delete deferredIntent.insertToken;
		if (!transfer && !openPanel && !pendingToken) return;
		return setTimeoutTimer("composerDeferredIntent", () => {
			if (editor.isDestroyed) return;
			if (pendingToken) {
				editor.commands.setTextSelection({
					from: getComposerPositionAtTextOffset(editor, pendingToken.selection.start),
					to: getComposerPositionAtTextOffset(editor, pendingToken.selection.end)
				});
				insertComposerTokenAtCursor(editor, pendingToken.token);
			}
			if (transfer?.kind === "paste") editor.view.dom.dispatchEvent(new ClipboardEvent("paste", {
				clipboardData: transfer.data,
				bubbles: true,
				cancelable: true
			}));
			else if (transfer?.kind === "drop") editor.view.dom.dispatchEvent(new DragEvent("drop", {
				dataTransfer: transfer.data,
				bubbles: true,
				cancelable: true
			}));
			if (openPanel) unifiedPanelOpen(openPanel);
		}, 0);
	}, [
		deferredIntent,
		editor,
		setTimeoutTimer,
		unifiedPanelOpen
	]);
	const quickPanelElement = quickPanelReady && quickPanelEnabled ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(QuickPanelView, { inputAdapter }) : null;
	const showPauseButton = isLoading && sendDisabled;
	const leftControls = renderLeftControls?.(inputAdapter, unifiedPanelControl);
	const belowControls = renderBelowControls?.(inputAdapter, unifiedPanelControl);
	const sendAccessoryElement = typeof sendAccessory === "function" ? sendAccessory(inputAdapter, unifiedPanelControl) : sendAccessory;
	const compactControls = renderCompactControls?.(inputAdapter, unifiedPanelControl);
	const ExpandIcon = hasCustomHeight ? Minimize2 : Maximize2;
	const sendAction = showPauseButton ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
		content: t("chat.input.pause"),
		placement: "top",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
			"data-ui": "chat.composer.action.pause",
			type: "button",
			className: "flex size-7.5 items-center justify-center rounded-full text-error hover:bg-accent",
			"aria-label": t("chat.input.pause"),
			onClick: () => void onPause(),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CirclePause, { size: 20 })
		})
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SendMessageButton_default, {
		sendMessage: sendDraft,
		disabled: sendDisabled,
		onDisabledClick: showBlockedSendReason
	});
	const editingModeHeader = editingState ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.composer-surface.status",
		role: "status",
		"aria-live": "polite",
		"aria-label": t("chat.input.editing_message"),
		"data-composer-editing-header": "",
		className: "flex h-9 shrink-0 items-center justify-between border-border-subtle border-b bg-transparent px-3 text-muted-foreground text-xs",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex min-w-0 items-center gap-1.5",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Pencil, {
				"aria-hidden": "true",
				"data-composer-editing-icon": "",
				className: "size-3.5 shrink-0"
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "min-w-0 truncate font-medium",
				children: t("chat.input.editing")
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex shrink-0 items-center gap-1",
			children: [
				editingState.onLocate ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("chat.input.locate_editing_message"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						onClick: editingState.onLocate,
						variant: "ghost",
						size: "icon-sm",
						className: "shrink-0 rounded-full text-muted-foreground! hover:bg-accent hover:text-foreground!",
						"aria-label": t("chat.input.locate_editing_message"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LocateFixed, { size: 14 })
					})
				}) : null,
				onSaveEditing ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("common.save"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						onClick: saveEditingDraft,
						variant: "ghost",
						size: "icon-sm",
						className: "shrink-0 rounded-full text-muted-foreground! hover:bg-accent hover:text-foreground!",
						"aria-label": t("common.save"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Check, { size: 14 })
					})
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("chat.input.cancel_editing"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						onClick: editingState.onCancel,
						variant: "ghost",
						size: "icon-sm",
						className: "shrink-0 rounded-full text-muted-foreground! hover:bg-accent hover:text-foreground!",
						"aria-label": t("chat.input.cancel_editing"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { size: 14 })
					})
				})
			]
		})]
	}) : null;
	const inputbarElement = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.composer",
		id: "inputbar",
		"data-composer-inputbar": "",
		"data-composer-presentation": isCompact ? "compact" : "regular",
		className: cn("inputbar-container relative rounded-[20px] border-[0.5px] border-border bg-card shadow-sm transition-all duration-200 ease-in-out", isCompact || editingState ? "pt-0" : "pt-2", belowControls ? "mb-0.5" : "mb-3", isEditingBorderHighlighted && !isDragging && "border-primary ring-2 ring-primary/20", isDragging && "border-2 border-success border-dashed before:pointer-events-none before:absolute before:inset-0 before:z-5 before:rounded-[18px] before:bg-success/[0.03] before:content-['']", isExpanded && "expanded"),
		children: [
			!isCompact ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-ui": "chat.composer-surface.separator",
				"data-composer-resize-handle": "",
				"data-resizing": isEditorResizing || void 0,
				role: "separator",
				"aria-orientation": "horizontal",
				"aria-valuemin": editorMinHeight,
				"aria-valuemax": editorMaxHeight,
				"aria-valuenow": resizeHandleValue,
				"aria-label": t("chat.input.resize_height"),
				tabIndex: 0,
				onMouseDown: startEditorResize,
				onKeyDown: handleResizeKeyDown,
				className: "group/composer-resize-handle absolute top-0 right-4 left-4 z-3 h-2 cursor-row-resize [-webkit-app-region:no-drag] focus-visible:bg-primary/40 focus-visible:outline-none",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "absolute top-0 right-0 left-0 h-0.5 rounded-full bg-primary/20 opacity-0 transition-opacity group-hover/composer-resize-handle:opacity-100 group-focus/composer-resize-handle:opacity-100 group-data-[resizing=true]/composer-resize-handle:bg-primary/35 group-data-[resizing=true]/composer-resize-handle:opacity-100" })
			}), !editingState ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				"data-composer-expand-corner": "",
				className: "group/expand-corner absolute top-px right-px z-4 size-8",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					"aria-hidden": "true",
					"data-composer-expand-corner-line": "",
					className: "pointer-events-none absolute top-1 right-1 size-3 origin-top-right scale-100 rounded-tr-[16px] border-foreground/60 border-t-[1.5px] border-r-[1.5px] opacity-70 transition-[opacity,scale] duration-200 ease-out group-focus-within/expand-corner:scale-50 group-focus-within/expand-corner:opacity-0 group-hover/expand-corner:scale-50 group-hover/expand-corner:opacity-0"
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					type: "button",
					onClick: handleExpandControlClick,
					variant: "ghost",
					size: "icon-sm",
					className: "-translate-y-2.5 [&_svg]:!size-3 pointer-events-none absolute top-1 right-1 size-5.5 translate-x-2.5 rotate-[-8deg] scale-80 rounded-full bg-transparent text-muted-foreground opacity-0 shadow-none transition-[opacity,translate,scale,rotate,color,background-color] duration-300 ease-out hover:bg-accent hover:text-foreground focus-visible:pointer-events-auto focus-visible:translate-x-0 focus-visible:translate-y-0 focus-visible:rotate-0 focus-visible:scale-100 focus-visible:bg-accent focus-visible:text-foreground focus-visible:opacity-100 group-focus-within/expand-corner:pointer-events-auto group-focus-within/expand-corner:translate-x-0 group-focus-within/expand-corner:translate-y-0 group-focus-within/expand-corner:rotate-0 group-focus-within/expand-corner:scale-100 group-focus-within/expand-corner:bg-accent/80 group-focus-within/expand-corner:text-foreground group-focus-within/expand-corner:opacity-100 group-hover/expand-corner:pointer-events-auto group-hover/expand-corner:translate-x-0 group-hover/expand-corner:translate-y-0 group-hover/expand-corner:rotate-0 group-hover/expand-corner:scale-100 group-hover/expand-corner:bg-accent/80 group-hover/expand-corner:text-foreground group-hover/expand-corner:opacity-100",
					"aria-pressed": hasCustomHeight,
					"aria-label": hasCustomHeight ? t("chat.input.restore") : t("chat.input.expand"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ExpandIcon, { className: "transition-[scale] duration-300 ease-out group-focus-within/expand-corner:scale-110 group-hover/expand-corner:scale-110" })
				})]
			}) : null] }) : null,
			editingModeHeader,
			topContent,
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				"data-composer-compact-row": isCompact ? "" : void 0,
				className: isCompact ? "grid grid-cols-[auto_minmax(0,1fr)_auto] items-center gap-1 px-2 py-1" : leadingContent ? "flex items-start" : "contents",
				children: [
					isCompact ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComposerToolMenu, {
						inputAdapter,
						unifiedPanelControl
					}) : null,
					leadingContent ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "shrink-0 pt-1.5 pl-3.5",
						children: leadingContent
					}) : null,
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						"data-ui": "chat.composer-surface.composer-input part:composer-input",
						ref: frameRef,
						"data-composer-editor-frame": "",
						className: cn("min-w-0 flex-1 overflow-hidden transition-[height] ease-out", editingState && "mt-2"),
						onTransitionEnd: handleTransitionEnd,
						style: isCompact ? compactFrameStyle : frameStyle,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EditorContent, {
							editor,
							style: isCompact ? compactEditorContentStyle : editorContentStyle,
							onFocus: () => {
								onFocus?.();
								pasteHandling_default.setLastFocusedComponent("inputbar");
							}
						})
					}),
					isCompact ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						"data-ui": "chat.composer-surface.composer-actions part:composer-actions",
						className: "flex shrink-0 flex-row items-center gap-1.5",
						children: [
							compactControls,
							sendAccessoryElement,
							sendAction
						]
					}) : null
				]
			}),
			!isCompact ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				"data-ui": "chat.composer-surface.composer-actions part:composer-actions",
				"data-composer-toolbar": "",
				className: "relative z-2 flex h-10 shrink-0 flex-row justify-between gap-4 px-2 py-1.25",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex min-w-0 flex-1 items-center overflow-hidden",
					children: leftControls
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex flex-row items-center gap-1.5",
					children: [sendAccessoryElement, sendAction]
				})]
			}) : null
		]
	});
	const inputbarStack = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.composer-surface",
		className: "relative",
		children: [quickPanelElement, inputbarElement]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NarrowLayout_default, {
		narrowMode,
		withSidePadding: true,
		className: "pointer-events-auto",
		style: {
			width: "100%",
			...railGutterPx != null ? {
				paddingLeft: COMPOSER_SIDE_PADDING_PX + railGutterPx,
				paddingRight: COMPOSER_SIDE_PADDING_PX + railGutterPx
			} : {}
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "w-full",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "inputbar relative z-2 flex flex-col pt-0",
				onDragEnter: handleDragEnter,
				onDragLeave: handleDragLeave,
				onDragOver: handleDragOver,
				onDrop: handleDrop,
				children: belowControls ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "mb-6 rounded-[20px] bg-muted/45 pb-1.5 dark:bg-muted/25",
					children: [
						queueContent,
						inputbarStack,
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "min-w-0 overflow-hidden px-2 pt-0.5",
							children: belowControls
						})
					]
				}) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [queueContent, inputbarStack] })
			})
		})
	});
}
function hasClipboardFiles(data) {
	if (!data) return false;
	if (data.files?.length > 0) return true;
	return Array.from(data.items ?? []).some((item) => item.kind === "file");
}
export { ComposerSurfaceRuntime as default };
