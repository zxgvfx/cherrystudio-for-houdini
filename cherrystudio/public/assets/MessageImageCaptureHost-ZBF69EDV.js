import { s as __toESM } from "./chunk-DiqNceaa.js";
import { n as IpcChannel, t as loggerService } from "./LoggerService-oVV4iwe6.js";
import { C as string, M as datetime, S as strictObject, a as array, o as boolean } from "./schemas-CV_EtlSZ.js";
import { H as number, i as formatErrorMessageWithPrefix } from "./error-DgGhUgCT.js";
import { n as getDefaultValue } from "./PreferenceService-Ba0ofBX2.js";
import { i as useSharedCacheSelector, n as usePersistCache, o as cacheService, t as useCache } from "./useCache-SsOQx-L2.js";
import { c as DynamicVirtualList_default } from "./providerSettings-BcvJgtqn.js";
import { t as useSWRImmutable } from "./immutable-J0MUNQkZ.js";
import { c as getNamingTextContent, n as fetchMessagesSummary } from "./aiGeneration-CtrH0Bds.js";
import { t as require_dayjs_min } from "./dayjs.min-CNu3tPBh.js";
import { n as t$1, t as instance } from "./i18next-DBAWv9hQ.js";
import { t as require_react } from "./react-BgPOU4At.js";
import { t as useTranslation } from "./useTranslation-DnuRkr5k.js";
import { t as require_react_dom } from "./react-dom-CKbeLgrG.js";
import { n as usePreference, t as useMultiplePreferences } from "./usePreference-78czMD_R.js";
import { a as mergeUiProps, i as UiDataSlot, r as cn$1 } from "./dist-BGahvVh8.js";
import { t as require_jsx_runtime } from "./jsx-runtime-DCB_IiL2.js";
import { n as AvatarFallback, r as AvatarImage, t as Avatar } from "./avatar-BMK8th-L.js";
import { c as SelectValue, i as SelectItem, n as SelectContent, s as SelectTrigger, t as Select } from "./select-BPh2DI-f.js";
import { t as Alert } from "./alert-BwuKGcCL.js";
import { t as Button } from "./button-Db6_VSWw.js";
import { n as Tooltip } from "./tooltip-ZuayyV11.js";
import { g as invariant, h as setTarget, m as animateVisualElement, n as motion, v as useIsomorphicLayoutEffect$1, y as useConstant } from "./react-DY3ULJV9.js";
import { t as AnimatePresence } from "./AnimatePresence-o8FLtg_-.js";
import { B as knowledgeBaseToComposerToken, G as ComposerContextProvider, J as QuickPanelProvider, K as useActiveComposerOverride, L as composerKnowledgeBaseTokenId, M as useComposerToolLauncherController, N as useComposerToolLauncherVersion, V as getComposerToolbarManifestsForScope, W as useOverflowIconOnly, Y as useReducedMotion, a as COMPOSER_INPUT_MAX_LENGTH, c as serializeComposerDocument, i as getNextInputHistoryIndex, l as trimComposerDraftBoundaryBlankLines, o as createComposerUserMessageParts, q as useOptionalQuickPanel, s as excludeComposerDraftTokens, t as HorizontalScrollContainer_default, u as COMPOSER_TOKEN_NODE_NAME, x as COMPOSER_SEND_ACCESSORY_BUTTON_CLASS, z as getComposerTokenIds } from "./HorizontalScrollContainer-BW0UgWea.js";
import { n as Switch } from "./switch-DEX3m-Zc.js";
import { a as DialogFooter, o as DialogHeader, r as DialogContent, s as DialogTitle, t as Dialog } from "./dialog-9s9VTSCz.js";
import { t as ConfirmDialog } from "./confirm-dialog-B5-JWpzh.js";
import { t as Checkbox } from "./checkbox-D7bjMZU-.js";
import { n as RadioGroupItem, t as RadioGroup } from "./radio-group-BLEiejOL.js";
import { t as Input } from "./input-dvr72LyA.js";
import { i as PopoverTrigger, n as PopoverAnchor, r as PopoverContent, t as Popover } from "./popover-n5iWPIgn.js";
import { t as EmptyState$1 } from "./empty-state-C4JfZPek.js";
import { a as RowFlex } from "./flex-BmIgOnUt.js";
import { t as scrollbar_default } from "./scrollbar-u8WAeDey.js";
import { t as PageSidePanel } from "./page-side-panel-CF7JqFV7.js";
import { t as SearchInput } from "./search-input-Dk6EmZ7J.js";
import { t as SelectDropdown } from "./select-dropdown-DkQrXXq5.js";
import { t as code_editor_default } from "./code-editor-BAVAnwGZ.js";
import { t as Label } from "./label-CUymrXZH.js";
import { i as extractTextFromNode, r as createSlugger } from "./markdown-CUccgRk8.js";
import { k as visit, r as remarkParse, t as unified } from "./lib-BGOUa2xQ.js";
import { A as markdownToPlainText, N as removeTrailingDoubleSpaces } from "./citations-B-nR2KVB.js";
import { A as ScrollOwnershipProvider, B as SelectionContextMenu_default, C as isValidAgentToolsType, D as getCachedMessageUiState, F as isAskUserQuestionToolName, G as useMessageListMeta, H as useMessageListActions, I as parseAskUserQuestionToolInput, J as useMessageListUiSelectors, K as useMessageListSelection, L as WebCitationCard, M as APPROVAL_REQUESTED, N as buildToolResponseFromPart, P as AgentToolsType, R as useCitationPreviewSession, S as UnknownToolRenderer, T as ToolArgsTable, U as useMessageListData, V as MessageListProvider, W as useMessageListEditingId, Y as useMessageRenderConfig, a as MessageContent_default, f as hasPartParentToolCallId, i as defaultMessageRenderConfig, j as useScrollRuntimeNavigation, k as updateCachedMessageUiState, m as ToolDisclosure, n as defaultMessageMenuConfig, o as getToolGroupIcon, q as useMessageListUi, r as defaultMessageMenuExportOptions, s as getToolGroupSemanticTitle, u as projectCompletedMessageParts, w as renderTool, z as KnowledgeCitationCard } from "./types-Dy5m-gs4.js";
import { t as ReorderableList } from "./reorderable-list-CM96dcTS.js";
import { i as FieldError } from "./field-v-3EhHEn.js";
import { n as HoverCardContent, r as HoverCardTrigger, t as HoverCard } from "./hover-card-mOA75M9A.js";
import { t as SegmentedControl } from "./segmented-control-DMdgjBZR.js";
import { t as Skeleton } from "./skeleton-CmSX8uoi.js";
import { t as Slider } from "./slider-_Xxahv5d.js";
import { a as INITIAL_FIND_BAR_STATE, c as SaveToKnowledgePopup_default, i as FindBar, l as ObsidianExportPopup_default, n as findTextMatches, r as supportsCustomHighlights, s as scrollIntoView, t as findRangesInScope } from "./contentSearch-D349wOZP.js";
import { n as useCodeStyle } from "./useCodeStyle-ZK5esKOR.js";
import { n as useTheme } from "./useTheme-CbMe73se.js";
import { x as useCommandHandler } from "./command-DJV4tTI9.js";
import { a as CommandTooltip, n as CommandContextMenu, r as CommandPopupMenu } from "./command-lPk0rc3k.js";
import { n as ipcApi } from "./ipc-BDTAufGC.js";
import { t as ErrorBoundaryCustomized } from "./ErrorBoundary-BQINHMNg.js";
import { c as useIsActiveTab } from "./tab-CbOwJVjs.js";
import { n as cn, t as classNames } from "./style-qqUWb85F.js";
import { n as useAvatar, t as UserPopup_default } from "./UserPopup-CoT72u_G.js";
import { t as AbsoluteFilePathSchema } from "./file-KsLXrn8b.js";
import { _ as videoExts, c as createFileEntryHandle, d as archiveExts, f as audioExts, g as textExts, i as toSafeFileUrl, l as createFilePathHandle, m as imageExts, p as documentExts, r as toFileUrl } from "./file-DzPAqnYr.js";
import { a as isUniqueModelId, i as createUniqueModelId, o as parseUniqueModelId } from "./model-CfoN7z8F.js";
import { a as MessageStatsSchema, i as MessageSnapshotSchema, n as MessageDataSchema, o as MessageStatusSchema, t as ContentMessageRoleSchema } from "./message-Bm3cYvp1.js";
import { t as dataApiService } from "./DataApiService-DP44jQXR.js";
import { c as useQuery, i as useInfiniteQuery, r as useInfiniteFlatItems } from "./useDataApi-DhDD9bgI.js";
import { t as toast } from "./toast-DKTI9lZN.js";
import { t as ArrowDown } from "./arrow-down-CDJR_LRA.js";
import { t as ArrowRight } from "./arrow-right-10DRsfJt.js";
import { t as ArrowUp } from "./arrow-up-C4RPKFe7.js";
import { t as AtSign } from "./at-sign-Cm5Ar7rp.js";
import { t as Bot } from "./bot-Gan43VZU.js";
import { t as BrushCleaning } from "./brush-cleaning-CI4SI0VB.js";
import { t as Check } from "./check-DGAvfDpR.js";
import { t as ChevronDown } from "./chevron-down-BVtofl6V.js";
import { t as ChevronLeft } from "./chevron-left-CAuKBMJN.js";
import { t as ChevronRight } from "./chevron-right-CvFIUMTW.js";
import { t as ChevronsDown } from "./chevrons-down-CBHzUyUc.js";
import { t as ChevronsUp } from "./chevrons-up-DFXoBF5t.js";
import { t as CirclePause } from "./circle-pause-OCZpDuvF.js";
import { t as Columns2 } from "./columns-2-DJJTKlZR.js";
import { t as Copy } from "./copy-DLNIVOlq.js";
import { t as Database } from "./database-BE71Q_Id.js";
import { t as Ellipsis } from "./ellipsis-Db1MXgzr.js";
import { t as ExternalLink } from "./external-link-CFdmQa9Q.js";
import { t as FilePenLine } from "./file-pen-line-AcBcBtBM.js";
import { t as FileText } from "./file-text-C2NTQkH7.js";
import { t as FolderInput } from "./folder-input-l4VG7R4t.js";
import { t as Folder } from "./folder-CNcoOMX-.js";
import { t as Gauge } from "./gauge-CFak-sSG.js";
import { t as Grid2x2 } from "./grid-2x2-BYXRoG-Z.js";
import { t as GripVertical } from "./grip-vertical-Dlsr9QCa.js";
import { t as History } from "./history-BBBeCIJj.js";
import { t as Image } from "./image-BueQcb3X.js";
import { t as Languages } from "./languages-BOYrlzfC.js";
import { t as ListChecks } from "./list-checks-BJMDksSn.js";
import { t as List } from "./list-DvMZwghn.js";
import { t as LoaderCircle } from "./loader-circle-CyzyZXyF.js";
import { t as Maximize2 } from "./maximize-2-qIo3C-a1.js";
import { t as Menu } from "./menu-BIhzz-9c.js";
import { t as MessageSquareText } from "./message-square-text-DipwE5ph.js";
import { t as MessageSquare } from "./message-square-BaVSRIpg.js";
import { t as Minimize2 } from "./minimize-2-B2cBjSDU.js";
import { t as MousePointerClick } from "./mouse-pointer-click-CvyegIRT.js";
import { t as MoveRight } from "./move-right-CbrdaUgi.js";
import { t as NotebookPen } from "./notebook-pen-QFP_idMC.js";
import { t as PanelLeft } from "./panel-left-DiQcdOo7.js";
import { t as Pause } from "./pause-CnkjU-kX.js";
import { t as PenLine } from "./pen-line-ChJ7Ihtl.js";
import { t as Pencil } from "./pencil-D9tpl364.js";
import { t as PinOff } from "./pin-off-olwlRswQ.js";
import { t as Pin } from "./pin-BrGtHzQa.js";
import { t as Play } from "./play-DCcouX13.js";
import { t as RotateCcw } from "./rotate-ccw-BS5J7gIn.js";
import { t as Rows3 } from "./rows-3-Tapx1FMo.js";
import { t as Save } from "./save-Cp4EyjDF.js";
import { t as Settings2 } from "./settings-2-BTluMuWp.js";
import { t as Settings } from "./settings-DM9xfvj6.js";
import { t as Sparkle } from "./sparkle-cGJlkBhz.js";
import { t as Sparkles } from "./sparkles-CUMLbh34.js";
import { t as Split } from "./split-B-pwHlNM.js";
import { t as ThumbsUp } from "./thumbs-up-BkEbUNru.js";
import { t as Trash2 } from "./trash-2-_PaXFSXZ.js";
import { t as Upload } from "./upload-Djm0HR6v.js";
import { t as X } from "./x-DelRxIMm.js";
import { t as Zap } from "./zap-Bw6CmTiN.js";
import { n as createPopup, t as popup } from "./popup-BqV2ZD7D.js";
import { t as useIcon } from "./use-icon-DpFtqCDZ.js";
import { G as isEmoji, K as removeLeadingEmoji, V as firstLetter, c as deriveThinkingOptions, d as isAudioModel, f as isAudioModels, m as isVideoModels, p as isVideoModel, u as getModelLogoRef } from "./model-J88ZZ_lQ.js";
import { i as captureScrollableAsDataUrl, n as captureScrollable, r as captureScrollableAsBlob } from "./image-C_pRJZT2.js";
import { a as isToolUIPart } from "./dist-Zc546Plu.js";
import { d as useUpdateSession, i as useAssistantTopicsSource, r as useAgentSessionsSource } from "./resourceViewSources-C_1OsQvz.js";
import { n as EventEmitter, t as EVENT_NAMES } from "./EventService-WIK9sU2f.js";
import { a as withCherryMeta } from "./uiParts-B-0xzcKJ.js";
import { i as startTopicRenaming, l as useTopicMutations, n as getTopicMessages, r as mapApiTopicToRendererTopic, t as finishTopicRenaming } from "./useTopic-ThwH_aKa.js";
import { t as EmojiIcon_default } from "./EmojiIcon-Cws6jG0n.js";
import { t as useNavigate } from "./useNavigate-DEeZmEwY.js";
import { o as useProviderDisplayName } from "./useProvider-DEPjZhOC.js";
import { c as useMessageParts, r as PartsProvider, t as MessagePartsScopeProvider, u as usePartsMap } from "./MessagePartsContext-DyxIfSwn.js";
import { n as usePins } from "./ModelSelector-DG0XcmYM.js";
import { n as classifyErrorByAI, r as ConfirmActionPopup_default, t as showErrorDetailPopup } from "./ErrorDetailModal-D2XPz4-l.js";
import { t as Scrollbar_default } from "./Scrollbar-CRo8NoWr.js";
import { t as useTimer } from "./useTimer-B4RsTlvl.js";
import { t as classifyTurn } from "./transport-0repP7FI.js";
import { t as CopyIcon_default } from "./CopyIcon-oeHf1okg.js";
import { t as LoadingIcon_default } from "./LoadingIcon-CmUpDics.js";
import { a as hasTranslationParts, c as getStreamBlockedMessage, i as hasTextParts, l as Chat, n as getTextFromParts, r as getTranslationFromParts, s as ipcChatTransport, t as canEditAssistantMessageParts, u as useChat } from "./partsHelpers-BYW3sR0L.js";
import { a as createMessageExportView, c as getMessageListItemModelName, i as useTopicStreamStatus, l as isMessageListItemProcessing, n as useTopicDbRefreshOnAwaitingApproval, o as getDirectAssistantModelsByUserId, s as getMessageListItemModel, u as shareDirectAssistantModelsByUserId } from "./useTopicStreamStatus-C4CQlzhh.js";
import { i as useAssistants } from "./useAssistant-BNTbY6l-.js";
import { t as useHotkeys } from "./react-hotkeys-hook.esm-CQVE5KWH.js";
import { t as ModelAvatar_default } from "./ModelAvatar-B9O4au8z.js";
import { t as useTemporaryValue } from "./useTemporaryValue-BuEAjgKO.js";
import { t as RefreshIcon_default } from "./RefreshIcon-BarZjwm0.js";
import { n as useWindowFrame } from "./useWindowFrame-fMmvacK8.js";
import { n as OpenInNewWindowIcon } from "./WindowIcons-OebsN3ly.js";
import { t as NavbarIcon_default } from "./NavbarIcon-1BlIzHCM.js";
import { t as safeOpen } from "./safeOpen-C84okA1J.js";
import { i as useAgents } from "./PermissionModeOption-4HnqlfoN.js";
import { t as getAgentAvatarFromConfiguration } from "./agent-xClAvhQf.js";
import { t as useConversationNavigation } from "./useConversationNavigation-BVBNOjGL.js";
import { d as FILE_TYPE, l as removeSpecialCharactersForFileName, s as parseFileTypes } from "./file-DfGDXADI.js";
import { i as formatQuoteTokenPromptText, t as ComposerToken } from "./tokenView-CYgh6TvB.js";
import { r as isComposerInputTokenKind } from "./composerTokenPolicy-DjR5UGdr.js";
import { n as getComposerTextFromParts } from "./composerTokens-CbjE6w3w.js";
import { n as buildAgentSessionTopicId } from "./agentSession-DA38qDeM.js";
import { t as createDurationFormatter } from "./time-DHr4iSFS.js";
import { D as useResourceListItemAccessors, E as useResourceListActions, S as ResourceList, a as buildResourceListItemDropAnchor$1, c as getResourceTimeBucket$1, g as DEFAULT_ASSISTANT_EMOJI, h as renderAssistantEntityIcon, l as sortRankedResourceItems$1, o as compareResourceOrderKey$1, s as compareResourceRecency$1, x as ConversationResourceMenu } from "./base-D37xdY33.js";
import { i as AgentSelector, r as AssistantSelector } from "./selectors-Djbljrx_.js";
import { t as DeleteIcon_default } from "./DeleteIcon-DNqpuzDC.js";
import { t as EditIcon_default } from "./EditIcon-5uYqh3lm.js";
import { t as useNotesSettings } from "./useNotesSettings-PaJa4HH7.js";
import { S as messagesToPlainText, a as exportMarkdownToSiyuan, b as topicToPlainText, c as exportMessageToNotes, f as exportTopicAsMarkdown, g as messageToMarkdown, h as getMessageTitle, l as exportMessageToNotion, m as exportTopicToNotion, n as exportMarkdownContentAsFile, o as exportMarkdownToYuque, p as exportTopicToNotes, r as exportMarkdownToJoplin, s as exportMessageAsMarkdown, v as messagesToMarkdown, x as messageToPlainText, y as topicToMarkdown } from "./ExportService-CGqRJr_-.js";
import { t as ResourceCatalogView } from "./catalog-TLPWXCAN.js";
import { n as useMessagePlatformActions, t as useMessageListRenderConfig } from "./useMessageListRenderConfig-DIvsn7eO.js";
import { i as createComposerRichClipboardContentFromParts, r as createComposerRichClipboardContentFromPartGroups } from "./composerClipboard-CLOfQP5i.js";
import { t as useResizeDrag } from "./useResizeDrag-X1hRhP5b.js";
import { m as ActionIconButton_default, p as NarrowLayout_default } from "./dist-BFxYwiI3.js";
import { t as MessageErrorBoundary_default } from "./MessageErrorBoundary-BKmkdMtU.js";
import { t as Selector_default } from "./Selector-BBGwchH-.js";
function stopAnimation(visualElement) {
	visualElement.values.forEach((value) => value.stop());
}
function setVariants(visualElement, variantLabels) {
	[...variantLabels].reverse().forEach((key) => {
		const variant = visualElement.getVariant(key);
		variant && setTarget(visualElement, variant);
		if (visualElement.variantChildren) visualElement.variantChildren.forEach((child) => {
			setVariants(child, variantLabels);
		});
	});
}
function setValues(visualElement, definition) {
	if (Array.isArray(definition)) return setVariants(visualElement, definition);
	else if (typeof definition === "string") return setVariants(visualElement, [definition]);
	else setTarget(visualElement, definition);
}
function animationControls() {
	let hasMounted = false;
	const subscribers = /* @__PURE__ */ new Set();
	const controls = {
		subscribe(visualElement) {
			subscribers.add(visualElement);
			return () => void subscribers.delete(visualElement);
		},
		start(definition, transitionOverride) {
			invariant(hasMounted, "controls.start() should only be called after a component has mounted. Consider calling within a useEffect hook.");
			const animations = [];
			subscribers.forEach((visualElement) => {
				animations.push(animateVisualElement(visualElement, definition, { transitionOverride }));
			});
			return Promise.all(animations);
		},
		set(definition) {
			invariant(hasMounted, "controls.set() should only be called after a component has mounted. Consider calling within a useEffect hook.");
			return subscribers.forEach((visualElement) => {
				setValues(visualElement, definition);
			});
		},
		stop() {
			subscribers.forEach((visualElement) => {
				stopAnimation(visualElement);
			});
		},
		mount() {
			hasMounted = true;
			return () => {
				hasMounted = false;
				controls.stop();
			};
		}
	};
	return controls;
}
function useAnimationControls() {
	const controls = useConstant(animationControls);
	useIsomorphicLayoutEffect$1(controls.mount, []);
	return controls;
}
function actionsToCommandMenuExtraItems(actions, runAction) {
	const items = [];
	let previousGroup;
	for (const action of actions) {
		if (!action.availability.visible) continue;
		if (items.length > 0 && action.group !== previousGroup) items.push({ type: "separator" });
		previousGroup = action.group;
		if (action.children.length > 0) {
			items.push({
				type: "submenu",
				id: action.id,
				label: action.label,
				icon: action.icon,
				description: action.availability.reason,
				enabled: action.availability.enabled,
				children: actionsToCommandMenuExtraItems(action.children, runAction)
			});
			continue;
		}
		items.push({
			type: "item",
			id: action.id,
			label: action.label,
			icon: action.icon,
			description: action.availability.reason,
			enabled: action.availability.enabled,
			destructive: action.danger,
			shortcutLabel: action.shortcut,
			onSelect: () => runAction(action)
		});
	}
	return items;
}
var import_react = /* @__PURE__ */ __toESM(require_react());
var import_jsx_runtime = /* @__PURE__ */ __toESM(require_jsx_runtime());
var EMPTY_ACTIONS = [];
var EMPTY_EXTRA_ITEMS = [];
function ResourceListActionContextMenu({ actions, getActions, item, children, onAction }) {
	const listActions = useResourceListActions();
	const { getItemId } = useResourceListItemAccessors();
	const runAction = (0, import_react.useCallback)(async (action) => {
		if (!action.availability.enabled) return;
		const confirm = action.confirm;
		if (confirm) {
			await ConfirmActionPopup_default.show({
				title: confirm.title,
				content: confirm.description ?? confirm.content,
				okText: confirm.confirmText,
				cancelText: confirm.cancelText,
				danger: confirm.destructive,
				action: () => onAction(action)
			});
			return;
		}
		await onAction(action);
	}, [onAction]);
	const extraItems = (0, import_react.useMemo)(() => actionsToCommandMenuExtraItems(actions ?? EMPTY_ACTIONS, runAction), [actions, runAction]);
	const getExtraItems = (0, import_react.useCallback)(() => getActions ? actionsToCommandMenuExtraItems(getActions(), runAction) : extraItems, [
		extraItems,
		getActions,
		runAction
	]);
	const markActiveItem = (0, import_react.useCallback)(() => listActions.openContextMenu(getItemId(item)), [
		listActions,
		getItemId,
		item
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandContextMenu, {
		location: "webcontents.context",
		extraItems: getActions ? EMPTY_EXTRA_ITEMS : extraItems,
		getExtraItems: getActions ? getExtraItems : void 0,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: "contents",
			onContextMenu: markActiveItem,
			children
		})
	});
}
var ENTITY_RAIL_PINNED_SECTION_ID = "resource-entity-rail:section:pinned";
var ENTITY_RAIL_DEFAULT_SECTION_ID = "resource-entity-rail:section:default";
var ENTITY_RAIL_PINNED_GROUP_ID = "resource-entity-rail:group:pinned";
var ENTITY_RAIL_DEFAULT_GROUP_ID = "resource-entity-rail:group:default";
var ENTITY_RAIL_GROUP_SECTION_PREFIX = "resource-entity-rail:section:";
var ENTITY_RAIL_GROUP_GROUP_PREFIX = "resource-entity-rail:group:";
var ENTITY_RAIL_UNGROUPED_KEY = JSON.stringify(["ungrouped"]);
function getEntityRailGroupBucketKey(groupId) {
	return groupId ? JSON.stringify(["group", groupId]) : ENTITY_RAIL_UNGROUPED_KEY;
}
function getEntityRailGroupRank(item) {
	if (item.pinned) return 0;
	return item.groupId ? 2 : 1;
}
function sortEntityRailItemsForGroupGrouping(items) {
	return items.map((item, index) => ({
		item,
		index,
		rank: getEntityRailGroupRank(item)
	})).sort((a$1, b$1) => {
		const rankDifference = a$1.rank - b$1.rank;
		if (rankDifference !== 0) return rankDifference;
		if (a$1.rank !== 2) return a$1.index - b$1.index;
		return compareResourceOrderKey$1(a$1.item.groupOrderKey, b$1.item.groupOrderKey) || a$1.index - b$1.index;
	}).map(({ item }) => item);
}
var ENTITY_RAIL_LEADING_SLOT_CLASS = "text-foreground group-hover:text-inherit group-focus-visible:text-inherit group-data-[selected=true]:text-inherit";
var ENTITY_RAIL_TITLE_CLASS = "font-medium text-foreground group-hover:text-inherit group-focus-visible:text-inherit group-data-[selected=true]:text-inherit";
function getEntityRailTrailingActionPaddingClassName(actionCount) {
	if (actionCount >= 3) return "group-focus-within:pr-16 group-hover:pr-16 group-has-[[data-resource-list-item-actions][data-active=true]]:pr-16";
	if (actionCount === 2) return "group-focus-within:pr-12 group-hover:pr-12 group-has-[[data-resource-list-item-actions][data-active=true]]:pr-12";
	if (actionCount === 1) return "group-focus-within:pr-7 group-hover:pr-7 group-has-[[data-resource-list-item-actions][data-active=true]]:pr-7";
	return "";
}
function ResourceEntityRail({ addIcon, addLabel, ariaLabel, defaultGroupLabel, groupByGroup = false, emptyFallback, getContextMenuActions, headerActions, historyRecordsActive = false, listRef, onAdd, onOpenHistoryRecords, resourceMenuItems, onContextMenuAction, onReorder, reorderEnabled: reorderEnabledProp = true, onSelect, onSelectedClick, selectedClickId, selectedId, status = "idle", variant, items }) {
	const { t: t$2 } = useTranslation();
	const hasReorderHandler = !!onReorder;
	const reorderEnabled = hasReorderHandler && reorderEnabledProp;
	const fallbackListRef = (0, import_react.useRef)(null);
	const effectiveListRef = listRef ?? fallbackListRef;
	const hasActiveResourceMenuItem = resourceMenuItems?.some((item) => item.active) ?? false;
	const effectiveSelectedId = hasActiveResourceMenuItem || historyRecordsActive ? null : selectedId;
	const effectiveSelectedClickId = hasActiveResourceMenuItem ? null : selectedClickId ?? selectedId;
	const handleItemClick = (0, import_react.useCallback)((item) => {
		if (effectiveSelectedClickId === item.id && onSelectedClick) {
			onSelectedClick(item);
			return;
		}
		onSelect(item);
	}, [
		effectiveSelectedClickId,
		onSelect,
		onSelectedClick
	]);
	const handleSelectItemById = (0, import_react.useCallback)((id) => {
		const item = items.find((entry) => entry.id === id);
		if (item) handleItemClick(item);
	}, [handleItemClick, items]);
	const runContextMenuAction = (0, import_react.useCallback)(async (item, action) => {
		if (!action.availability.enabled || !onContextMenuAction) return;
		const confirm = action.confirm;
		if (confirm) {
			await ConfirmActionPopup_default.show({
				title: confirm.title,
				content: confirm.description ?? confirm.content,
				okText: confirm.confirmText,
				cancelText: confirm.cancelText,
				danger: confirm.destructive,
				action: () => onContextMenuAction(item, action)
			});
			return;
		}
		await onContextMenuAction(item, action);
	}, [onContextMenuAction]);
	const renderItem = (0, import_react.useCallback)((item) => {
		const actions = getContextMenuActions?.(item) ?? [];
		const hasVisibleMenuActions = !!onContextMenuAction && actions.some((action) => action.availability.visible);
		const hasTrailingAction = Boolean(item.trailingAction);
		const trailingActionPaddingClassName = getEntityRailTrailingActionPaddingClassName((hasTrailingAction ? 1 : 0) + (hasVisibleMenuActions ? 1 : 0));
		const extraItems = hasVisibleMenuActions ? actionsToCommandMenuExtraItems(actions, (action) => runContextMenuAction(item, action)) : [];
		const row = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ResourceList.Item, {
			item,
			"data-testid": "resource-entity-rail-row",
			tooltip: item.tooltip,
			children: [
				item.icon && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceList.ItemLeadingSlot, {
					className: ENTITY_RAIL_LEADING_SLOT_CLASS,
					children: item.icon
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceList.ItemTitle, {
					className: cn(ENTITY_RAIL_TITLE_CLASS, "transition-[padding]", trailingActionPaddingClassName),
					title: item.tooltip ? void 0 : item.name,
					children: item.name
				}),
				(hasTrailingAction || hasVisibleMenuActions) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ResourceList.ItemActions, {
					onClick: (event) => event.stopPropagation(),
					children: [hasVisibleMenuActions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						title: t$2("common.more"),
						delay: 500,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandPopupMenu, {
							location: "webcontents.context",
							extraItems,
							align: "end",
							side: "bottom",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceList.GroupHeaderActionButton, {
								type: "button",
								"aria-label": t$2("common.more"),
								onClick: (event) => event.stopPropagation(),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Ellipsis, { className: "block" })
							})
						})
					}), item.trailingAction]
				})
			]
		});
		if (!actions.length || !onContextMenuAction) return row;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceListActionContextMenu, {
			item,
			actions,
			onAction: (action) => onContextMenuAction(item, action),
			children: row
		}, item.id);
	}, [
		getContextMenuActions,
		onContextMenuAction,
		runContextMenuAction,
		t$2
	]);
	const empty = (0, import_react.useMemo)(() => emptyFallback ?? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.resource-entity-rail",
		className: "min-h-0 flex-1"
	}), [emptyFallback]);
	const providerItems = (0, import_react.useMemo)(() => groupByGroup ? sortEntityRailItemsForGroupGrouping(items) : items, [groupByGroup, items]);
	const sectionBy = (0, import_react.useMemo)(() => (item) => {
		if (item.pinned) return {
			id: ENTITY_RAIL_PINNED_SECTION_ID,
			label: t$2("selector.common.pinned_title")
		};
		if (groupByGroup) {
			const groupBucketKey = getEntityRailGroupBucketKey(item.groupId);
			return item.groupId && item.groupName ? {
				id: `${ENTITY_RAIL_GROUP_SECTION_PREFIX}${groupBucketKey}`,
				label: item.groupName
			} : {
				id: `${ENTITY_RAIL_GROUP_SECTION_PREFIX}${groupBucketKey}`,
				label: t$2("assistants.groups.ungrouped")
			};
		}
		return {
			id: ENTITY_RAIL_DEFAULT_SECTION_ID,
			label: defaultGroupLabel ?? ""
		};
	}, [
		defaultGroupLabel,
		groupByGroup,
		t$2
	]);
	const groupBy = (0, import_react.useMemo)(() => (item) => {
		if (item.pinned) return {
			id: ENTITY_RAIL_PINNED_GROUP_ID,
			label: ""
		};
		if (groupByGroup) return {
			id: `${ENTITY_RAIL_GROUP_GROUP_PREFIX}${getEntityRailGroupBucketKey(item.groupId)}`,
			label: ""
		};
		return {
			id: ENTITY_RAIL_DEFAULT_GROUP_ID,
			label: ""
		};
	}, [groupByGroup]);
	const Provider = ResourceList.Provider;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Provider, {
		variant,
		items: providerItems,
		selectedId: effectiveSelectedId,
		onSelectItem: handleSelectItemById,
		status,
		groupBy,
		sectionBy,
		defaultGroupVisibleCount: Number.POSITIVE_INFINITY,
		dragCapabilities: {
			groups: false,
			items: reorderEnabled,
			itemSameGroup: reorderEnabled,
			itemCrossGroup: false
		},
		canDragItem: ({ item }) => reorderEnabled && item.reorderable !== false && !item.pinned,
		canDropItem: ({ activeItem, sourceGroupId, targetGroupId }) => reorderEnabled && activeItem.reorderable !== false && !activeItem.pinned && targetGroupId !== ENTITY_RAIL_PINNED_GROUP_ID && sourceGroupId === targetGroupId,
		onReorder: reorderEnabled ? onReorder : void 0,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ResourceList.Frame, {
			className: "h-full min-h-0",
			"data-testid": `${variant}-entity-rail`,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ResourceList.Header, {
				className: "gap-1",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceList.HeaderItem, {
					type: "button",
					icon: addIcon,
					label: addLabel,
					"aria-label": addLabel,
					onClick: () => void onAdd(),
					actions: headerActions || onOpenHistoryRecords ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [headerActions, onOpenHistoryRecords && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						title: t$2("history.records.shortTitle"),
						delay: 500,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceList.HeaderActionButton, {
							type: "button",
							"aria-label": t$2("history.records.shortTitle"),
							"aria-current": historyRecordsActive ? "page" : void 0,
							className: cn(historyRecordsActive && "bg-muted text-foreground!"),
							onClick: () => onOpenHistoryRecords(),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(History, { className: "block" })
						})
					})] }) : void 0
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConversationResourceMenu, { items: resourceMenuItems })]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceList.Body, {
				listRef: effectiveListRef,
				draggable: hasReorderHandler,
				ariaLabel,
				virtualClassName: "pt-1 pb-3",
				errorFallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceList.ErrorState, { message: t$2("error.boundary.default.message") }),
				emptyFallback: empty,
				renderItem
			})]
		})
	});
}
var RESOURCE_TIME_BUCKET_RANK = {
	today: 1,
	yesterday: 2,
	"this-week": 3,
	earlier: 4
};
function sortResourceItemsByPinnedTime(items, now) {
	return sortRankedResourceItems$1(items, {
		getRank: (item) => item.pinned === true ? 0 : RESOURCE_TIME_BUCKET_RANK[getResourceTimeBucket$1(item.updatedAt, now)],
		isPinned: (item) => item.pinned === true,
		compareWithinGroup: compareResourceRecency$1((item) => item.updatedAt)
	});
}
function useResourceEntityRail({ entities, resources, getResourceParentId, activeEntityId, isLoading, isError, sortResourcesForEntity, onPickResource, onCreateResource, reorder, refetchEntities, onReorderError }) {
	const [optimisticOrderIds, setOptimisticOrderIds] = (0, import_react.useState)(null);
	const entityIdsWithResources = (0, import_react.useMemo)(() => new Set(resources.map(getResourceParentId).filter((id) => !!id)), [getResourceParentId, resources]);
	(0, import_react.useEffect)(() => {
		setOptimisticOrderIds(null);
	}, [(0, import_react.useMemo)(() => entities.map((entity) => `${entity.id}:${entity.orderKey ?? ""}`).join("|"), [entities])]);
	const items = (0, import_react.useMemo)(() => {
		const ordered = [...entities.filter((entity) => entityIdsWithResources.has(entity.id))].sort((a$1, b$1) => compareResourceOrderKey$1(a$1.orderKey, b$1.orderKey));
		let base = ordered;
		if (optimisticOrderIds) {
			const byId = new Map(ordered.map((entity) => [entity.id, entity]));
			const optimistic = optimisticOrderIds.flatMap((id) => {
				const entity = byId.get(id);
				return entity ? [entity] : [];
			});
			const optimisticIds = new Set(optimisticOrderIds);
			base = [...optimistic, ...ordered.filter((entity) => !optimisticIds.has(entity.id))];
		}
		const pinned = base.filter((entity) => entity.pinned);
		if (pinned.length === 0) return base;
		return [...pinned, ...base.filter((entity) => !entity.pinned)];
	}, [
		entities,
		entityIdsWithResources,
		optimisticOrderIds
	]);
	return {
		items,
		listStatus: isError ? "error" : isLoading && items.length === 0 ? "loading" : "idle",
		selectedId: activeEntityId && entityIdsWithResources.has(activeEntityId) ? activeEntityId : null,
		handleSelect: (0, import_react.useCallback)((item) => {
			const first = sortResourcesForEntity(resources.filter((resource) => getResourceParentId(resource) === item.id))[0];
			if (first) {
				onPickResource(first);
				return;
			}
			onCreateResource(item.id);
		}, [
			getResourceParentId,
			onCreateResource,
			onPickResource,
			resources,
			sortResourcesForEntity
		]),
		handleReorder: (0, import_react.useCallback)(async (payload) => {
			if (payload.type !== "item") return;
			const activeId = payload.activeId;
			const nextIds = items.map((item) => item.id);
			const activeIndex = nextIds.indexOf(activeId);
			const overIndex = nextIds.indexOf(payload.overId);
			if (activeIndex < 0 || overIndex < 0) return;
			if (items[activeIndex]?.reorderable === false || items[overIndex]?.reorderable === false) return;
			nextIds.splice(activeIndex, 1);
			const adjustedOverIndex = nextIds.indexOf(payload.overId);
			nextIds.splice(payload.position === "before" ? adjustedOverIndex : adjustedOverIndex + 1, 0, activeId);
			setOptimisticOrderIds(nextIds);
			try {
				await reorder(activeId, buildResourceListItemDropAnchor$1(payload));
			} catch (error) {
				setOptimisticOrderIds(null);
				onReorderError(error);
				await refetchEntities().catch(() => void 0);
				return;
			}
			await refetchEntities().catch(() => void 0);
		}, [
			items,
			onReorderError,
			refetchEntities,
			reorder
		])
	};
}
function SidebarCollapseIcon(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
		"data-ui": "icons.sidebar-collapse-icon",
		viewBox: "0 0 24 24",
		fill: "none",
		"aria-hidden": "true",
		...mergeUiProps(props, "icons.sidebar-collapse-icon"),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", {
			x: "3.5",
			y: "4",
			width: "17",
			height: "16",
			rx: "4",
			stroke: "currentColor",
			strokeWidth: "1.7"
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "M8.2 8v8",
			stroke: "currentColor",
			strokeLinecap: "round",
			strokeWidth: "1.7"
		})]
	});
}
function SidebarExpandIcon(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
		"data-ui": "icons.sidebar-expand-icon",
		viewBox: "0 0 24 24",
		fill: "none",
		"aria-hidden": "true",
		...mergeUiProps(props, "icons.sidebar-expand-icon"),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", {
			x: "3.5",
			y: "4",
			width: "17",
			height: "16",
			rx: "4",
			stroke: "currentColor",
			strokeWidth: "1.7"
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "M9.5 5v14",
			stroke: "currentColor",
			strokeLinecap: "round",
			strokeWidth: "1.7"
		})]
	});
}
function RightSidebarCollapseIcon(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
		"data-ui": "icons.right-sidebar-collapse-icon",
		viewBox: "0 0 24 24",
		fill: "none",
		"aria-hidden": "true",
		...mergeUiProps(props, "icons.right-sidebar-collapse-icon"),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", {
			x: "3.5",
			y: "4",
			width: "17",
			height: "16",
			rx: "4",
			stroke: "currentColor",
			strokeWidth: "1.7"
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "M15.8 8v8",
			stroke: "currentColor",
			strokeLinecap: "round",
			strokeWidth: "1.7"
		})]
	});
}
function ConversationSidebarToggleButton({ onSidebarToggle, sidebarOpen, tooltipDelay = 800, tooltipPlacement, ...buttonProps }) {
	const [preferredShowSidebar, setShowSidebar] = usePreference("topic.tab.show");
	const showSidebar = sidebarOpen ?? preferredShowSidebar;
	const label = showSidebar ? t$1("navbar.hide_sidebar") : t$1("navbar.show_sidebar");
	const ToggleIcon = showSidebar ? SidebarCollapseIcon : SidebarExpandIcon;
	const toggleShowSidebar = () => {
		if (onSidebarToggle) {
			onSidebarToggle();
			return;
		}
		setShowSidebar(!showSidebar);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandTooltip, {
		command: "app.sidebar.toggle",
		label,
		placement: tooltipPlacement,
		delay: tooltipDelay,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavbarIcon_default, {
			tone: "conversation",
			"aria-label": label,
			"aria-pressed": showSidebar,
			onClick: toggleShowSidebar,
			...buttonProps,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToggleIcon, {})
		})
	});
}
function isGlobalSearchSelectionForTab(payload, currentTabId) {
	return !!currentTabId && !!payload?.targetTabId && payload.targetTabId === currentTabId;
}
var getAgentSessionStreamStatusCacheKey = (sessionId) => `topic.stream.statuses.${buildAgentSessionTopicId(sessionId)}`;
var SESSION_ID_SEPARATOR = "\0";
var EMPTY_AGENT_SESSION_STREAM_STATUSES = /* @__PURE__ */ new Map();
function toAgentSessionStreamState(entry) {
	if (!entry) return void 0;
	return {
		isPending: classifyTurn(entry.status).isTurnActive,
		status: entry.status
	};
}
function areAgentSessionStreamStatusesEqual(a$1, b$1) {
	if (a$1 === b$1) return true;
	if (a$1.size !== b$1.size) return false;
	for (const [sessionId, state] of a$1) {
		const other = b$1.get(sessionId);
		if (!other || other.status !== state.status || other.isPending !== state.isPending) return false;
	}
	return true;
}
function useAgentSessionStreamStatuses(sessionIds) {
	const sessionIdsKey = (0, import_react.useMemo)(() => Array.from(new Set(sessionIds)).sort().join(SESSION_ID_SEPARATOR), [sessionIds]);
	const uniqueSessionIds = (0, import_react.useMemo)(() => sessionIdsKey ? sessionIdsKey.split(SESSION_ID_SEPARATOR) : [], [sessionIdsKey]);
	const selector = (0, import_react.useCallback)((values) => {
		const entries = [];
		uniqueSessionIds.forEach((sessionId, index) => {
			const state = toAgentSessionStreamState(values[index]);
			if (state) entries.push([sessionId, state]);
		});
		if (entries.length === 0) return EMPTY_AGENT_SESSION_STREAM_STATUSES;
		return new Map(entries);
	}, [uniqueSessionIds]);
	return useSharedCacheSelector(uniqueSessionIds.map(getAgentSessionStreamStatusCacheKey), selector, areAgentSessionStreamStatusesEqual);
}
var DEFAULT_AVAILABILITY = {
	visible: true,
	enabled: true
};
function normalizeAvailability(input) {
	if (input === false) return {
		visible: false,
		enabled: false
	};
	if (input === true || input === void 0) return DEFAULT_AVAILABILITY;
	const visible = input.visible ?? true;
	return {
		visible,
		enabled: visible ? input.enabled ?? true : false,
		...input.reason && { reason: input.reason }
	};
}
function combineAvailability(first, second) {
	const visible = first.visible && second.visible;
	return {
		visible,
		enabled: visible && first.enabled && second.enabled,
		...first.reason || second.reason ? { reason: first.reason ?? second.reason } : {}
	};
}
function matchesSurface(action, surface) {
	if (!surface || !action.surface) return true;
	return Array.isArray(action.surface) ? action.surface.includes(surface) : action.surface === surface;
}
function resolveNode(node, context) {
	return typeof node === "function" ? node(context) : node;
}
function resolveText(text, context) {
	return typeof text === "function" ? text(context) : text;
}
function resolveConfirm(confirm, context) {
	if (!confirm) return void 0;
	const resolved = typeof confirm === "function" ? confirm(context) : confirm;
	if (!resolved) return void 0;
	return {
		title: resolveNode(resolved.title, context),
		...resolved.description && { description: resolveNode(resolved.description, context) },
		...resolved.content && { content: resolveNode(resolved.content, context) },
		...resolved.confirmText && { confirmText: resolveText(resolved.confirmText, context) },
		...resolved.cancelText && { cancelText: resolveText(resolved.cancelText, context) },
		...resolved.destructive !== void 0 && { destructive: resolved.destructive }
	};
}
function sortResolvedActions(actions) {
	return actions.sort((first, second) => {
		const firstOrder = first.order ?? 0;
		const secondOrder = second.order ?? 0;
		if (firstOrder !== secondOrder) return firstOrder - secondOrder;
		return first.id.localeCompare(second.id);
	});
}
var ActionRegistry = class {
	actions = /* @__PURE__ */ new Map();
	commands = /* @__PURE__ */ new Map();
	registerAction(descriptor) {
		this.actions.set(descriptor.id, descriptor);
		return () => {
			if (this.actions.get(descriptor.id) === descriptor) this.actions.delete(descriptor.id);
		};
	}
	registerCommand(descriptor) {
		this.commands.set(descriptor.id, descriptor);
		return () => {
			if (this.commands.get(descriptor.id) === descriptor) this.commands.delete(descriptor.id);
		};
	}
	unregister(id) {
		this.actions.delete(id);
		this.commands.delete(id);
	}
	listActions() {
		return Array.from(this.actions.values());
	}
	listCommands() {
		return Array.from(this.commands.values());
	}
	resolve(context, surface) {
		return sortResolvedActions(this.listActions().map((action) => this.resolveAction(action, context, surface)).filter((action) => !!action));
	}
	async execute(actionId, context) {
		const action = this.findAction(actionId, context);
		if (!action) return false;
		const resolvedAction = this.resolveAction(action, context);
		if (!resolvedAction?.availability.visible || !resolvedAction.availability.enabled) return false;
		const commandId = resolvedAction.commandId;
		if (!commandId) return false;
		const command = this.commands.get(commandId);
		if (!command) return false;
		await command.run(context);
		return true;
	}
	clear() {
		this.actions.clear();
		this.commands.clear();
	}
	findAction(actionId, context) {
		for (const action of this.actions.values()) {
			const found = this.findActionInTree(action, actionId, context);
			if (found) return found;
		}
	}
	findActionInTree(action, actionId, context) {
		if (action.id === actionId) return action;
		const children = typeof action.children === "function" ? action.children(context) : action.children ?? [];
		for (const child of children) {
			const found = this.findActionInTree(child, actionId, context);
			if (found) return found;
		}
	}
	resolveAction(action, context, surface) {
		if (!matchesSurface(action, surface)) return void 0;
		const commandAvailability = action.commandId ? normalizeAvailability(this.commands.get(action.commandId)?.availability?.(context)) : DEFAULT_AVAILABILITY;
		const availability = combineAvailability(normalizeAvailability(action.availability?.(context)), commandAvailability);
		if (!availability.visible) return void 0;
		const children = sortResolvedActions((typeof action.children === "function" ? action.children(context) : action.children ?? []).map((child) => this.resolveAction(child, context, surface)).filter((child) => !!child));
		const confirm = resolveConfirm(action.confirm, context);
		return {
			id: action.id,
			...action.commandId && { commandId: action.commandId },
			label: resolveNode(action.label, context),
			...action.icon && { icon: resolveNode(action.icon, context) },
			...action.group && { group: action.group },
			...action.order !== void 0 && { order: action.order },
			...action.surface && { surface: action.surface },
			danger: action.danger ?? false,
			...action.shortcut && { shortcut: action.shortcut },
			...confirm && { confirm },
			availability,
			children
		};
	}
};
function createActionRegistry() {
	return new ActionRegistry();
}
var sessionActionRegistry = createActionRegistry();
var hasExportOption$1 = ({ exportMenuOptions, onExportImage, onExportJoplin, onExportMarkdown, onExportMarkdownReason, onExportNotion, onExportObsidian, onExportSiyuan, onExportWord, onExportYuque }) => exportMenuOptions?.image && !!onExportImage || exportMenuOptions?.markdown && !!onExportMarkdown || exportMenuOptions?.markdown_reason && !!onExportMarkdownReason || exportMenuOptions?.docx && !!onExportWord || exportMenuOptions?.notion && !!onExportNotion || exportMenuOptions?.yuque && !!onExportYuque || exportMenuOptions?.obsidian && !!onExportObsidian || exportMenuOptions?.joplin && !!onExportJoplin || exportMenuOptions?.siyuan && !!onExportSiyuan;
var hasCopyOption = ({ exportMenuOptions, onCopyImage, onCopyMarkdown, onCopyPlainText }) => !!onCopyMarkdown || !!exportMenuOptions?.image && !!onCopyImage || !!exportMenuOptions?.plain_text && !!onCopyPlainText;
sessionActionRegistry.registerCommand({
	id: "session.auto-rename",
	availability: ({ isRenaming, onAutoRename }) => ({
		visible: !!onAutoRename,
		enabled: !!onAutoRename && !isRenaming
	}),
	run: ({ onAutoRename }) => onAutoRename?.()
});
sessionActionRegistry.registerCommand({
	id: "session.rename",
	availability: ({ isRenaming }) => ({ enabled: !isRenaming }),
	run: ({ sessionName, startEdit }) => startEdit(sessionName)
});
sessionActionRegistry.registerCommand({
	id: "session.toggle-pin",
	availability: ({ onTogglePin }) => ({
		visible: !!onTogglePin,
		enabled: !!onTogglePin
	}),
	run: ({ onTogglePin }) => onTogglePin?.()
});
sessionActionRegistry.registerCommand({
	id: "session.open-in-new-tab",
	availability: ({ isActiveInCurrentTab, onOpenInNewTab }) => ({
		visible: !!onOpenInNewTab && !isActiveInCurrentTab,
		enabled: !!onOpenInNewTab && !isActiveInCurrentTab
	}),
	run: ({ onOpenInNewTab }) => onOpenInNewTab?.()
});
sessionActionRegistry.registerCommand({
	id: "session.open-in-new-window",
	availability: ({ onOpenInNewWindow }) => ({
		visible: !!onOpenInNewWindow,
		enabled: !!onOpenInNewWindow
	}),
	run: ({ onOpenInNewWindow }) => onOpenInNewWindow?.()
});
sessionActionRegistry.registerCommand({
	id: "session.save-notes",
	availability: ({ exportMenuOptions, onSaveToNotes }) => ({
		visible: !!exportMenuOptions?.notes && !!onSaveToNotes,
		enabled: !!exportMenuOptions?.notes && !!onSaveToNotes
	}),
	run: ({ onSaveToNotes }) => onSaveToNotes?.()
});
sessionActionRegistry.registerCommand({
	id: "session.save-knowledge",
	availability: ({ onSaveToKnowledge }) => ({
		visible: !!onSaveToKnowledge,
		enabled: !!onSaveToKnowledge
	}),
	run: ({ onSaveToKnowledge }) => onSaveToKnowledge?.()
});
sessionActionRegistry.registerCommand({
	id: "session.export.image",
	availability: ({ exportMenuOptions, onExportImage }) => ({
		visible: !!exportMenuOptions?.image && !!onExportImage,
		enabled: !!exportMenuOptions?.image && !!onExportImage
	}),
	run: ({ onExportImage }) => onExportImage?.()
});
sessionActionRegistry.registerCommand({
	id: "session.export.markdown",
	availability: ({ exportMenuOptions, onExportMarkdown }) => ({
		visible: !!exportMenuOptions?.markdown && !!onExportMarkdown,
		enabled: !!exportMenuOptions?.markdown && !!onExportMarkdown
	}),
	run: ({ onExportMarkdown }) => onExportMarkdown?.()
});
sessionActionRegistry.registerCommand({
	id: "session.export.markdown-reason",
	availability: ({ exportMenuOptions, onExportMarkdownReason }) => ({
		visible: !!exportMenuOptions?.markdown_reason && !!onExportMarkdownReason,
		enabled: !!exportMenuOptions?.markdown_reason && !!onExportMarkdownReason
	}),
	run: ({ onExportMarkdownReason }) => onExportMarkdownReason?.()
});
sessionActionRegistry.registerCommand({
	id: "session.export.word",
	availability: ({ exportMenuOptions, onExportWord }) => ({
		visible: !!exportMenuOptions?.docx && !!onExportWord,
		enabled: !!exportMenuOptions?.docx && !!onExportWord
	}),
	run: ({ onExportWord }) => onExportWord?.()
});
sessionActionRegistry.registerCommand({
	id: "session.export.notion",
	availability: ({ exportMenuOptions, onExportNotion }) => ({
		visible: !!exportMenuOptions?.notion && !!onExportNotion,
		enabled: !!exportMenuOptions?.notion && !!onExportNotion
	}),
	run: ({ onExportNotion }) => onExportNotion?.()
});
sessionActionRegistry.registerCommand({
	id: "session.export.yuque",
	availability: ({ exportMenuOptions, onExportYuque }) => ({
		visible: !!exportMenuOptions?.yuque && !!onExportYuque,
		enabled: !!exportMenuOptions?.yuque && !!onExportYuque
	}),
	run: ({ onExportYuque }) => onExportYuque?.()
});
sessionActionRegistry.registerCommand({
	id: "session.export.obsidian",
	availability: ({ exportMenuOptions, onExportObsidian }) => ({
		visible: !!exportMenuOptions?.obsidian && !!onExportObsidian,
		enabled: !!exportMenuOptions?.obsidian && !!onExportObsidian
	}),
	run: ({ onExportObsidian }) => onExportObsidian?.()
});
sessionActionRegistry.registerCommand({
	id: "session.export.joplin",
	availability: ({ exportMenuOptions, onExportJoplin }) => ({
		visible: !!exportMenuOptions?.joplin && !!onExportJoplin,
		enabled: !!exportMenuOptions?.joplin && !!onExportJoplin
	}),
	run: ({ onExportJoplin }) => onExportJoplin?.()
});
sessionActionRegistry.registerCommand({
	id: "session.export.siyuan",
	availability: ({ exportMenuOptions, onExportSiyuan }) => ({
		visible: !!exportMenuOptions?.siyuan && !!onExportSiyuan,
		enabled: !!exportMenuOptions?.siyuan && !!onExportSiyuan
	}),
	run: ({ onExportSiyuan }) => onExportSiyuan?.()
});
sessionActionRegistry.registerCommand({
	id: "session.copy.image",
	availability: ({ exportMenuOptions, onCopyImage }) => ({
		visible: !!exportMenuOptions?.image && !!onCopyImage,
		enabled: !!exportMenuOptions?.image && !!onCopyImage
	}),
	run: ({ onCopyImage }) => onCopyImage?.()
});
sessionActionRegistry.registerCommand({
	id: "session.copy.markdown",
	availability: ({ onCopyMarkdown }) => ({
		visible: !!onCopyMarkdown,
		enabled: !!onCopyMarkdown
	}),
	run: ({ onCopyMarkdown }) => onCopyMarkdown?.()
});
sessionActionRegistry.registerCommand({
	id: "session.copy.plain-text",
	availability: ({ exportMenuOptions, onCopyPlainText }) => ({
		visible: !!exportMenuOptions?.plain_text && !!onCopyPlainText,
		enabled: !!exportMenuOptions?.plain_text && !!onCopyPlainText
	}),
	run: ({ onCopyPlainText }) => onCopyPlainText?.()
});
sessionActionRegistry.registerCommand({
	id: "session.position-left",
	availability: ({ onSetPanePosition, panePosition }) => ({
		visible: !!onSetPanePosition && !!panePosition,
		enabled: !!onSetPanePosition && panePosition !== "left"
	}),
	run: ({ onSetPanePosition }) => onSetPanePosition?.("left")
});
sessionActionRegistry.registerCommand({
	id: "session.position-right",
	availability: ({ onSetPanePosition, panePosition }) => ({
		visible: !!onSetPanePosition && !!panePosition,
		enabled: !!onSetPanePosition && panePosition !== "right"
	}),
	run: ({ onSetPanePosition }) => onSetPanePosition?.("right")
});
sessionActionRegistry.registerCommand({
	id: "session.delete",
	run: ({ onDelete }) => onDelete()
});
sessionActionRegistry.registerAction({
	id: "session.auto-rename",
	commandId: "session.auto-rename",
	label: ({ t: t$2 }) => t$2("agent.session.auto_rename"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Sparkles, { size: 14 }),
	order: 10,
	surface: "menu"
});
sessionActionRegistry.registerAction({
	id: "session.rename",
	commandId: "session.rename",
	label: ({ t: t$2 }) => t$2("agent.session.edit.title"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EditIcon_default, { size: 14 }),
	order: 20,
	surface: "menu"
});
sessionActionRegistry.registerAction({
	id: "session.toggle-pin",
	commandId: "session.toggle-pin",
	label: ({ pinned, t: t$2 }) => pinned ? t$2("agent.session.unpin.title") : t$2("agent.session.pin.title"),
	icon: ({ pinned }) => pinned ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PinOff, { size: 14 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Pin, { size: 14 }),
	order: 30,
	surface: "menu"
});
sessionActionRegistry.registerAction({
	id: "session.open-in-new-tab",
	commandId: "session.open-in-new-tab",
	label: ({ t: t$2 }) => t$2("common.open_in_new_tab"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ExternalLink, { size: 14 }),
	order: 35,
	surface: "menu"
});
sessionActionRegistry.registerAction({
	id: "session.open-in-new-window",
	commandId: "session.open-in-new-window",
	label: ({ t: t$2 }) => t$2("tab.open_in_new_window"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OpenInNewWindowIcon, { size: 14 }),
	order: 37,
	surface: "menu"
});
sessionActionRegistry.registerAction({
	id: "session.position",
	label: ({ t: t$2 }) => t$2("settings.agent.position.label"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PanelLeft, { size: 14 }),
	order: 36,
	surface: "menu",
	availability: ({ onSetPanePosition, panePosition }) => ({ visible: !!onSetPanePosition && !!panePosition }),
	children: [{
		id: "session.position-left",
		commandId: "session.position-left",
		label: ({ t: t$2 }) => t$2("settings.agent.position.left"),
		order: 10,
		surface: "menu"
	}, {
		id: "session.position-right",
		commandId: "session.position-right",
		label: ({ t: t$2 }) => t$2("settings.agent.position.right"),
		order: 20,
		surface: "menu"
	}]
});
sessionActionRegistry.registerAction({
	id: "session.save-notes",
	commandId: "session.save-notes",
	label: ({ t: t$2 }) => t$2("notes.save"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NotebookPen, { size: 14 }),
	group: "share",
	order: 50,
	surface: "menu"
});
sessionActionRegistry.registerAction({
	id: "session.save-knowledge",
	commandId: "session.save-knowledge",
	label: ({ t: t$2 }) => t$2("chat.save.topic.knowledge.menu_title"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Database, { size: 14 }),
	group: "share",
	order: 60,
	surface: "menu"
});
sessionActionRegistry.registerAction({
	id: "session.export",
	label: ({ t: t$2 }) => t$2("chat.topics.export.title"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Upload, { size: 14 }),
	group: "share",
	order: 70,
	surface: "menu",
	availability: (context) => ({ visible: !!hasExportOption$1(context) }),
	children: [
		{
			id: "session.export.image",
			commandId: "session.export.image",
			label: ({ t: t$2 }) => t$2("chat.topics.export.image"),
			order: 10,
			surface: "menu"
		},
		{
			id: "session.export.markdown",
			commandId: "session.export.markdown",
			label: ({ t: t$2 }) => t$2("chat.topics.export.md.label"),
			order: 20,
			surface: "menu"
		},
		{
			id: "session.export.markdown-reason",
			commandId: "session.export.markdown-reason",
			label: ({ t: t$2 }) => t$2("chat.topics.export.md.reason"),
			order: 30,
			surface: "menu"
		},
		{
			id: "session.export.word",
			commandId: "session.export.word",
			label: ({ t: t$2 }) => t$2("chat.topics.export.word"),
			order: 40,
			surface: "menu"
		},
		{
			id: "session.export.notion",
			commandId: "session.export.notion",
			label: ({ t: t$2 }) => t$2("chat.topics.export.notion"),
			order: 50,
			surface: "menu"
		},
		{
			id: "session.export.yuque",
			commandId: "session.export.yuque",
			label: ({ t: t$2 }) => t$2("chat.topics.export.yuque"),
			order: 60,
			surface: "menu"
		},
		{
			id: "session.export.obsidian",
			commandId: "session.export.obsidian",
			label: ({ t: t$2 }) => t$2("chat.topics.export.obsidian"),
			order: 70,
			surface: "menu"
		},
		{
			id: "session.export.joplin",
			commandId: "session.export.joplin",
			label: ({ t: t$2 }) => t$2("chat.topics.export.joplin"),
			order: 80,
			surface: "menu"
		},
		{
			id: "session.export.siyuan",
			commandId: "session.export.siyuan",
			label: ({ t: t$2 }) => t$2("chat.topics.export.siyuan"),
			order: 90,
			surface: "menu"
		}
	]
});
sessionActionRegistry.registerAction({
	id: "session.copy",
	label: ({ t: t$2 }) => t$2("chat.topics.copy.title"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Copy, { size: 14 }),
	group: "share",
	order: 80,
	surface: "menu",
	availability: (context) => ({ visible: hasCopyOption(context) }),
	children: [
		{
			id: "session.copy.image",
			commandId: "session.copy.image",
			label: ({ t: t$2 }) => t$2("chat.topics.copy.image"),
			icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Image, { size: 14 }),
			order: 10,
			surface: "menu"
		},
		{
			id: "session.copy.markdown",
			commandId: "session.copy.markdown",
			label: ({ t: t$2 }) => t$2("chat.topics.copy.md"),
			icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FileText, { size: 14 }),
			order: 20,
			surface: "menu"
		},
		{
			id: "session.copy.plain-text",
			commandId: "session.copy.plain-text",
			label: ({ t: t$2 }) => t$2("chat.topics.copy.plain_text"),
			icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FileText, { size: 14 }),
			order: 30,
			surface: "menu"
		}
	]
});
sessionActionRegistry.registerAction({
	id: "session.delete",
	commandId: "session.delete",
	label: ({ t: t$2 }) => t$2("common.delete"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeleteIcon_default, {
		size: 14,
		className: "lucide-custom"
	}),
	group: "danger",
	order: 90,
	surface: "menu",
	danger: true,
	availability: ({ pinned }) => ({ visible: !pinned }),
	confirm: ({ t: t$2 }) => ({
		title: t$2("agent.session.delete.title"),
		description: t$2("agent.session.delete.content"),
		confirmText: t$2("common.delete"),
		cancelText: t$2("common.cancel"),
		destructive: true
	})
});
function resolveSessionMenuActions(context) {
	return sessionActionRegistry.resolve(context, "menu");
}
async function executeSessionMenuAction(action, context) {
	return sessionActionRegistry.execute(action.id, context);
}
function createSessionActionContext(context) {
	return context;
}
function getSessionMenuActions(actionContext) {
	return resolveSessionMenuActions(actionContext);
}
async function runSessionMenuAction(action, actionContext) {
	await executeSessionMenuAction(action, actionContext);
}
function useSessionMenuPreset({ getActionContext }) {
	const getActionContextWithOverride = (0, import_react.useCallback)((item, contextOverride) => ({
		...getActionContext(item),
		...contextOverride
	}), [getActionContext]);
	const getActions = (0, import_react.useCallback)((item, contextOverride) => getSessionMenuActions(getActionContextWithOverride(item, contextOverride)), [getActionContextWithOverride]);
	const onAction = (0, import_react.useCallback)(async (item, action, contextOverride) => {
		await runSessionMenuAction(action, getActionContextWithOverride(item, contextOverride));
	}, [getActionContextWithOverride]);
	return (0, import_react.useMemo)(() => ({
		getActions,
		onAction
	}), [getActions, onAction]);
}
function useSessionMenuActions(actionContext) {
	return {
		getActions: (0, import_react.useCallback)(() => getSessionMenuActions(actionContext), [actionContext]),
		handleMenuAction: (0, import_react.useCallback)(async (action) => {
			await runSessionMenuAction(action, actionContext);
		}, [actionContext])
	};
}
var import_dayjs_min$5 = /* @__PURE__ */ __toESM(require_dayjs_min());
function getResourceTimeBucket(timestamp, now) {
	if (timestamp === void 0) return "earlier";
	const item = (0, import_dayjs_min$5.default)(timestamp);
	const current = now === void 0 ? (0, import_dayjs_min$5.default)() : (0, import_dayjs_min$5.default)(now);
	if (!item.isValid() || !current.isValid()) return "earlier";
	const itemStart = item.startOf("day");
	const todayStart = current.startOf("day");
	if (itemStart.isSame(todayStart)) return "today";
	const yesterdayStart = todayStart.subtract(1, "day");
	if (itemStart.isSame(yesterdayStart)) return "yesterday";
	const weekStart = todayStart.startOf("week");
	if (itemStart.isSame(weekStart) || itemStart.isAfter(weekStart) && itemStart.isBefore(yesterdayStart)) return "this-week";
	return "earlier";
}
function composeResourceListGroupResolvers(...resolvers) {
	return (item) => {
		for (const resolver of resolvers) {
			const group = resolver(item);
			if (group) return group;
		}
		return null;
	};
}
function createPinnedGroupResolver({ group, isPinned }) {
	return (item) => isPinned(item) ? group : null;
}
function createTimeGroupResolver({ getTimestamp, labels, now }) {
	return (item) => {
		const bucket = getResourceTimeBucket(getTimestamp(item), now);
		return {
			id: `time:${bucket}`,
			label: labels[bucket]
		};
	};
}
function sortRankedResourceItems(items, { getRank, isPinned, compareWithinGroup }) {
	return items.map((item, index) => ({
		item,
		index,
		rank: getRank(item),
		pinned: isPinned(item)
	})).sort((a$1, b$1) => {
		if (a$1.rank !== b$1.rank) return a$1.rank - b$1.rank;
		if (a$1.pinned || b$1.pinned) return a$1.index - b$1.index;
		const withinDelta = compareWithinGroup(a$1.item, b$1.item);
		if (withinDelta !== 0) return withinDelta;
		return a$1.index - b$1.index;
	}).map(({ item }) => item);
}
function compareResourceRecency(getUpdatedAt) {
	return (a$1, b$1) => {
		const aMs = Date.parse(getUpdatedAt(a$1));
		const bMs = Date.parse(getUpdatedAt(b$1));
		if (Number.isFinite(aMs) && Number.isFinite(bMs)) return bMs - aMs;
		return 0;
	};
}
function compareResourceOrderKey(a$1, b$1) {
	if (a$1 && b$1) {
		if (a$1 < b$1) return -1;
		if (a$1 > b$1) return 1;
	}
	return 0;
}
function buildResourceListItemDropAnchor(payload) {
	if (payload.overType === "item") return payload.position === "before" ? { before: payload.overId } : { after: payload.overId };
	return { position: "last" };
}
function buildResourceListGroupDropAnchor(payload, overId) {
	return payload.sourceIndex < payload.targetIndex ? { after: overId } : { before: overId };
}
function moveResourceListStringGroupAfterDrop(ids, activeId, overId, payload) {
	const activeIndex = ids.indexOf(activeId);
	const overIndex = ids.indexOf(overId);
	if (activeIndex < 0 || overIndex < 0 || activeIndex === overIndex) return [...ids];
	const next = ids.filter((id) => id !== activeId);
	const adjustedOverIndex = next.indexOf(overId);
	const insertIndex = payload.sourceIndex < payload.targetIndex ? adjustedOverIndex + 1 : adjustedOverIndex;
	next.splice(insertIndex, 0, activeId);
	return next;
}
function withResourceListGroupIdPrefix(prefix, resolver) {
	return (item) => {
		const group = resolver(item);
		if (!group) return null;
		return {
			...group,
			id: `${prefix}${group.id}`
		};
	};
}
var SESSION_TIME_BUCKET_RANK = {
	today: 1,
	yesterday: 2,
	"this-week": 3,
	earlier: 4
};
const SESSION_PINNED_GROUP_ID = "session:pinned";
const SESSION_PINNED_SECTION_ID = "session:section:pinned";
const SESSION_AGENT_SECTION_ID = "session:section:agent";
const SESSION_WORKDIR_SECTION_ID = "session:section:workdir";
const SESSION_NO_PROJECT_GROUP_ID = "session:no-project";
const SESSION_NO_PROJECT_SECTION_ID = "session:section:no-project";
const SESSION_UNKNOWN_AGENT_GROUP_ID = "session:agent:unknown";
const SESSION_NO_WORKDIR_GROUP_ID = "session:workdir:none";
var SESSION_AGENT_GROUP_ID_PREFIX = "session:agent:";
var SESSION_WORKSPACE_GROUP_ID_PREFIX = "session:workspace:";
var SESSION_WORKDIR_GROUP_ID_PREFIX = "session:workdir:";
var NO_PROJECT_GROUP_RANK = Number.MAX_SAFE_INTEGER;
var UNKNOWN_GROUP_RANK = Number.MAX_SAFE_INTEGER - 1;
function withSessionGroupIdPrefix(resolver) {
	return withResourceListGroupIdPrefix("session:", resolver);
}
function getSessionAgentGroupId(agentId) {
	return `${SESSION_AGENT_GROUP_ID_PREFIX}${agentId}`;
}
function getAgentIdFromSessionGroupId(groupId) {
	if (groupId === "session:agent:unknown" || !groupId.startsWith(SESSION_AGENT_GROUP_ID_PREFIX)) return void 0;
	return groupId.slice(14);
}
function getWorkdirPathFromSessionGroupId(groupId) {
	if (groupId === "session:workdir:none" || !groupId.startsWith(SESSION_WORKDIR_GROUP_ID_PREFIX)) return void 0;
	return decodeURIComponent(groupId.slice(16));
}
function getWorkspaceSessionGroupId(workspaceId) {
	return `${SESSION_WORKSPACE_GROUP_ID_PREFIX}${encodeURIComponent(workspaceId)}`;
}
function getFallbackWorkdirSessionGroupId(path) {
	return `${SESSION_WORKDIR_GROUP_ID_PREFIX}${encodeURIComponent(path)}`;
}
function normalizeSessionWorkdirPath(path) {
	const trimmed = path?.trim();
	if (!trimmed) return null;
	return trimmed.replace(/[\\/]+$/, "") || trimmed;
}
function isSystemWorkspaceSession(session) {
	return session.workspace?.type === "system";
}
function getPrimarySessionWorkdir(session) {
	if (isSystemWorkspaceSession(session)) return null;
	return normalizeSessionWorkdirPath(session.workspace?.path);
}
function getPathSegments(path) {
	return path.split(/[\\/]+/).filter(Boolean);
}
function getSessionWorkdirFallbackLabel(path) {
	return getPathSegments(path).at(-1) ?? path;
}
function getKnownSessionWorkspaceGroupId(session, groupIdByWorkspaceId, groupIdByPath) {
	if (session.workspaceId) {
		const groupId = groupIdByWorkspaceId.get(session.workspaceId);
		if (groupId) return groupId;
	}
	const path = getPrimarySessionWorkdir(session);
	return path ? groupIdByPath.get(path) : void 0;
}
function getSessionWorkdirGroupId(session, display) {
	const knownGroupId = display ? getKnownSessionWorkspaceGroupId(session, display.groupIdByWorkspaceId, display.groupIdByPath) : void 0;
	if (knownGroupId) return knownGroupId;
	const path = getPrimarySessionWorkdir(session);
	return path ? getFallbackWorkdirSessionGroupId(path) : SESSION_NO_WORKDIR_GROUP_ID;
}
function getUniqueSessionFallbackWorkdirPaths(sessions, groupIdByWorkspaceId, groupIdByPath) {
	return Array.from(new Set(sessions.filter((session) => !getKnownSessionWorkspaceGroupId(session, groupIdByWorkspaceId, groupIdByPath)).map(getPrimarySessionWorkdir).filter((path) => typeof path === "string")));
}
function createFallbackWorkdirLabelEntries(paths) {
	const basenameCounts = /* @__PURE__ */ new Map();
	for (const path of paths) {
		const basename = getSessionWorkdirFallbackLabel(path);
		basenameCounts.set(basename, (basenameCounts.get(basename) ?? 0) + 1);
	}
	return paths.map((path) => {
		const segments = getPathSegments(path);
		const basename = segments.at(-1) ?? path;
		if ((basenameCounts.get(basename) ?? 0) <= 1) return [path, basename];
		const parent = segments.at(-2);
		return [path, parent ? `${parent}/${basename}` : path];
	});
}
function createSessionWorkdirDisplayMaps(sessions, workspaces = []) {
	const groupIdByPath = /* @__PURE__ */ new Map();
	const groupIdByWorkspaceId = /* @__PURE__ */ new Map();
	const labelByGroupId = /* @__PURE__ */ new Map();
	const pathByGroupId = /* @__PURE__ */ new Map();
	const rankByGroupId = /* @__PURE__ */ new Map();
	const workspaceIdByGroupId = /* @__PURE__ */ new Map();
	const referencedWorkspaceIds = new Set(sessions.map((session) => session.workspaceId).filter((workspaceId) => typeof workspaceId === "string" && workspaceId.length > 0));
	const referencedWorkspacePaths = new Set(sessions.map(getPrimarySessionWorkdir).filter((path) => typeof path === "string"));
	for (const workspace of workspaces) {
		if (workspace.type === "system") continue;
		const path = normalizeSessionWorkdirPath(workspace.path);
		if (!path || groupIdByWorkspaceId.has(workspace.id)) continue;
		if (!referencedWorkspaceIds.has(workspace.id) && !referencedWorkspacePaths.has(path)) continue;
		const groupId = getWorkspaceSessionGroupId(workspace.id);
		groupIdByWorkspaceId.set(workspace.id, groupId);
		if (!groupIdByPath.has(path)) groupIdByPath.set(path, groupId);
		labelByGroupId.set(groupId, workspace.name.trim() || getSessionWorkdirFallbackLabel(path));
		pathByGroupId.set(groupId, path);
		workspaceIdByGroupId.set(groupId, workspace.id);
		rankByGroupId.set(groupId, rankByGroupId.size);
	}
	for (const [path, label] of createFallbackWorkdirLabelEntries(getUniqueSessionFallbackWorkdirPaths(sessions, groupIdByWorkspaceId, groupIdByPath))) {
		const groupId = getFallbackWorkdirSessionGroupId(path);
		if (labelByGroupId.has(groupId)) continue;
		groupIdByPath.set(path, groupId);
		labelByGroupId.set(groupId, label);
		pathByGroupId.set(groupId, path);
		rankByGroupId.set(groupId, rankByGroupId.size);
	}
	return {
		groupIdByPath,
		groupIdByWorkspaceId,
		labelByGroupId,
		pathByGroupId,
		rankByGroupId,
		workspaceIdByGroupId
	};
}
function createSessionDisplayGroupResolver({ agentById, labels, mode, now, pinnedAsSection = false, workdirDisplay }) {
	const pinnedGroupLabel = mode === "time" || !pinnedAsSection ? labels.pinned : "";
	if (mode === "time") return withSessionGroupIdPrefix(composeResourceListGroupResolvers(createPinnedGroupResolver({
		isPinned: (session) => session.pinned === true,
		group: {
			id: "pinned",
			label: pinnedGroupLabel
		}
	}), createTimeGroupResolver({
		getTimestamp: (session) => session.updatedAt,
		labels: labels.time,
		now
	})));
	if (mode === "agent") return composeResourceListGroupResolvers(createPinnedGroupResolver({
		isPinned: (session) => session.pinned === true,
		group: {
			id: SESSION_PINNED_GROUP_ID,
			label: pinnedGroupLabel
		}
	}), (session) => {
		const agentId = session.agentId;
		if (!agentId) return {
			id: SESSION_UNKNOWN_AGENT_GROUP_ID,
			label: labels.agent.unknown
		};
		const agent = agentById?.get(agentId);
		return agent ? {
			id: getSessionAgentGroupId(agent.id),
			label: agent.name
		} : {
			id: SESSION_UNKNOWN_AGENT_GROUP_ID,
			label: labels.agent.unknown
		};
	});
	return composeResourceListGroupResolvers(createPinnedGroupResolver({
		isPinned: (session) => session.pinned === true,
		group: {
			id: SESSION_PINNED_GROUP_ID,
			label: pinnedGroupLabel
		}
	}), (session) => {
		if (isSystemWorkspaceSession(session)) return {
			id: SESSION_NO_PROJECT_GROUP_ID,
			label: ""
		};
		const groupId = getSessionWorkdirGroupId(session, workdirDisplay);
		if (groupId === "session:workdir:none") return {
			id: SESSION_NO_WORKDIR_GROUP_ID,
			label: labels.workdir.none
		};
		const path = getPrimarySessionWorkdir(session);
		return {
			id: groupId,
			label: workdirDisplay?.labelByGroupId.get(groupId) ?? (path ? getSessionWorkdirFallbackLabel(path) : groupId)
		};
	});
}
function getWorkdirGroupRank(session, workdirDisplay) {
	if (isSystemWorkspaceSession(session)) return NO_PROJECT_GROUP_RANK;
	const groupId = getSessionWorkdirGroupId(session, workdirDisplay);
	if (groupId === "session:workdir:none") return UNKNOWN_GROUP_RANK;
	return workdirDisplay?.rankByGroupId.get(groupId) ?? UNKNOWN_GROUP_RANK;
}
function getAgentGroupRank(session, agentRankById) {
	if (!session.agentId) return UNKNOWN_GROUP_RANK;
	return agentRankById?.get(session.agentId) ?? UNKNOWN_GROUP_RANK;
}
function sortSessionsForDisplayGroups(sessions, options) {
	const isPinned = (session) => session.pinned === true;
	if (options.mode === "time") return sortRankedResourceItems(sessions, {
		getRank: (session) => session.pinned === true ? 0 : SESSION_TIME_BUCKET_RANK[getResourceTimeBucket(session.updatedAt, options.now)],
		isPinned,
		compareWithinGroup: compareResourceRecency((session) => session.updatedAt)
	});
	return sortRankedResourceItems(sessions, {
		getRank: (session) => {
			if (session.pinned === true) return 0;
			let displayRank;
			if (options.mode === "workdir" && isSystemWorkspaceSession(session)) displayRank = NO_PROJECT_GROUP_RANK;
			else if (options.mode === "agent") displayRank = getAgentGroupRank(session, options.agentRankById);
			else displayRank = getWorkdirGroupRank(session, options.workdirDisplay);
			return displayRank >= UNKNOWN_GROUP_RANK ? displayRank : displayRank + 1;
		},
		isPinned,
		compareWithinGroup: (a$1, b$1) => compareResourceOrderKey(a$1.orderKey, b$1.orderKey)
	});
}
function normalizeSessionDropPayload(payload) {
	return payload;
}
function buildSessionDropAnchor(payload) {
	return buildResourceListItemDropAnchor(payload);
}
function buildSessionWorkdirGroupDropAnchor(payload, overWorkspaceId) {
	return buildResourceListGroupDropAnchor(payload, overWorkspaceId);
}
function buildSessionAgentGroupDropAnchor(payload, overAgentId) {
	return buildResourceListGroupDropAnchor(payload, overAgentId);
}
function canDropSessionItemInDisplayGroup({ mode, sourceGroupId, targetGroupId }) {
	return mode !== "time" && sourceGroupId === targetGroupId && targetGroupId !== "session:pinned";
}
function applyOptimisticSessionDisplayMove(sessions, payload) {
	const activeIndex = sessions.findIndex((session) => session.id === payload.activeId);
	if (activeIndex < 0) return [...sessions];
	const next = sessions.filter((session) => session.id !== payload.activeId);
	let insertIndex = next.length;
	if (payload.overType === "item") {
		const overIndex = next.findIndex((session) => session.id === payload.overId);
		if (overIndex >= 0) insertIndex = payload.position === "before" ? overIndex : overIndex + 1;
	}
	next.splice(insertIndex, 0, sessions[activeIndex]);
	return next;
}
function moveSessionWorkdirGroupAfterDrop(workspaces, activeWorkspaceId, overWorkspaceId, payload) {
	const activeIndex = workspaces.findIndex((workspace) => workspace.id === activeWorkspaceId);
	const overIndex = workspaces.findIndex((workspace) => workspace.id === overWorkspaceId);
	if (activeIndex < 0 || overIndex < 0 || activeIndex === overIndex) return [...workspaces];
	const next = workspaces.filter((workspace) => workspace.id !== activeWorkspaceId);
	const adjustedOverIndex = next.findIndex((workspace) => workspace.id === overWorkspaceId);
	const insertIndex = payload.sourceIndex < payload.targetIndex ? adjustedOverIndex + 1 : adjustedOverIndex;
	next.splice(insertIndex, 0, workspaces[activeIndex]);
	return next;
}
function moveSessionAgentGroupAfterDrop(agentIds, activeAgentId, overAgentId, payload) {
	return moveResourceListStringGroupAfterDrop(agentIds, activeAgentId, overAgentId, payload);
}
var EditNameDialog = ({ initialName, inputLabel, onOpenChange, onSubmit, open, placeholder, submitLabel, title }) => {
	const { t: t$2 } = useTranslation();
	const inputId = (0, import_react.useId)();
	const [name, setName] = (0, import_react.useState)(initialName);
	const [hasAttemptedSubmit, setHasAttemptedSubmit] = (0, import_react.useState)(false);
	const [isSubmitting, setIsSubmitting] = (0, import_react.useState)(false);
	(0, import_react.useEffect)(() => {
		if (!open) {
			setName("");
			setHasAttemptedSubmit(false);
			setIsSubmitting(false);
			return;
		}
		setName(initialName);
		setHasAttemptedSubmit(false);
		setIsSubmitting(false);
	}, [initialName, open]);
	const submitName = async () => {
		const trimmedName = name.trim();
		setHasAttemptedSubmit(true);
		if (!trimmedName) return;
		if (trimmedName === initialName.trim()) {
			onOpenChange(false);
			return;
		}
		setIsSubmitting(true);
		try {
			await onSubmit(trimmedName);
			onOpenChange(false);
		} finally {
			setIsSubmitting(false);
		}
	};
	const handleSubmit = (event) => {
		event.preventDefault();
		submitName();
	};
	const handleKeyDown = (event) => {
		if (event.key !== "Enter") return;
		event.preventDefault();
		submitName();
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog, {
		open,
		onOpenChange,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogContent, {
			closeOnOverlayClick: false,
			className: "max-w-md gap-0 overflow-hidden rounded-2xl border-border-subtle p-0",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogHeader, {
				className: "gap-0.5 border-border-subtle border-b px-4 py-3 text-left",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogTitle, {
					className: "leading-4",
					children: title
				})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("form", {
				"data-ui": "ui.edit-name-dialog.action.submit",
				className: "flex flex-col",
				onSubmit: handleSubmit,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "space-y-1 px-4 py-3",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
							htmlFor: inputId,
							className: "text-muted-foreground leading-4",
							children: inputLabel ?? t$2("common.name")
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
							id: inputId,
							"aria-invalid": hasAttemptedSubmit && !name.trim(),
							autoFocus: true,
							className: "h-8 rounded-lg px-2.5 leading-4 placeholder:text-muted-foreground",
							placeholder,
							value: name,
							onChange: (event) => {
								setName(event.target.value);
								setHasAttemptedSubmit(false);
							},
							onKeyDown: handleKeyDown
						}),
						hasAttemptedSubmit && !name.trim() ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldError, {
							className: "leading-4",
							children: t$2("common.required_field")
						}) : null
					]
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogFooter, {
					className: "gap-2 border-border-subtle border-t px-4 py-3 sm:justify-end",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						variant: "outline",
						className: "h-8 rounded-lg px-3",
						onClick: () => onOpenChange(false),
						children: t$2("common.cancel")
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "submit",
						loading: isSubmitting,
						className: "h-8 rounded-lg px-3",
						children: submitLabel ?? t$2("common.save")
					})]
				})]
			})]
		})
	});
};
var EditNameDialog_default = EditNameDialog;
function ActionConfirmDialog({ confirm, contentClassName, overlayClassName, open, onConfirm, onOpenChange }) {
	if (!confirm) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfirmDialog, {
		open,
		onOpenChange,
		contentClassName,
		overlayClassName,
		title: confirm.title,
		description: confirm.description,
		content: confirm.content,
		confirmText: confirm.confirmText,
		cancelText: confirm.cancelText,
		destructive: confirm.destructive,
		onConfirm
	});
}
var import_dayjs_min$4 = /* @__PURE__ */ __toESM(require_dayjs_min());
var historyTableClassName = "min-w-[760px] rounded-none border-0 bg-card shadow-none";
const historyTableGridClassName = "grid min-w-[760px] grid-cols-[44px_minmax(180px,1fr)_minmax(280px,2.5fr)_100px_84px]";
var historyHeaderClassName = "sticky top-0 z-10 border-border-subtle border-b bg-card text-muted-foreground text-sm leading-5";
var historyHeaderCellClassName = "flex h-8 min-w-0 items-center px-3 py-1.5 font-semibold";
const historyBodyRowClassName = "group border-border-subtle border-b bg-card text-muted-foreground text-sm leading-5";
const historyBodyCellClassName = "flex min-w-0 items-center px-3 py-1.5 transition-colors group-hover:bg-muted group-data-[state=selected]:bg-muted";
const historyFixedActionCellClassName = "sticky right-0 z-2 justify-center bg-card px-2 [border-left:0.5px_solid_var(--border-subtle)]";
function HistoryVirtualTable({ emptyContent, estimateSize, header, items, onFixedActionShadowChange, renderRow }) {
	const scrollerRef = (0, import_react.useRef)(null);
	const updateFixedActionShadow = (0, import_react.useCallback)(() => {
		const scroller = scrollerRef.current;
		if (!scroller) {
			onFixedActionShadowChange(false);
			return;
		}
		const maxScrollLeft = scroller.scrollWidth - scroller.clientWidth;
		onFixedActionShadowChange(maxScrollLeft > 1 && scroller.scrollLeft < maxScrollLeft - 1);
	}, [onFixedActionShadowChange]);
	(0, import_react.useEffect)(() => {
		updateFixedActionShadow();
		const scroller = scrollerRef.current;
		if (!scroller || typeof ResizeObserver === "undefined") return;
		const resizeObserver = new ResizeObserver(updateFixedActionShadow);
		resizeObserver.observe(scroller);
		if (scroller.firstElementChild) resizeObserver.observe(scroller.firstElementChild);
		return () => resizeObserver.disconnect();
	}, [items.length, updateFixedActionShadow]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "history.virtual-table",
		className: "min-h-0 flex-1 px-3 pt-3 pb-2",
		role: "table",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: cn("flex h-full min-h-0 flex-col overflow-hidden", historyTableClassName),
			children: items.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DynamicVirtualList_default, {
				autoHideScrollbar: true,
				className: "min-h-0 flex-1",
				estimateSize,
				header,
				list: items,
				onScroll: updateFixedActionShadow,
				overscan: 8,
				role: "rowgroup",
				scrollElementRef: scrollerRef,
				scrollerStyle: { overflowX: "auto" },
				children: renderRow
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				ref: scrollerRef,
				className: "min-h-0 flex-1 overflow-auto",
				onScroll: updateFixedActionShadow,
				children: [header, emptyContent]
			})
		})
	});
}
const HistoryTableHeader = ({ actionsLabel, selectAllLabel, selectionDisabled = false, selectedState, showFixedActionShadow, sourceLabel, timeLabel, titleLabel, onToggleAll }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
	"data-ui": "history.table-header.row",
	className: cn(historyTableGridClassName, historyHeaderClassName),
	role: "row",
	children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "history.table-header.columnheader",
			className: cn(historyHeaderCellClassName, "justify-center px-2"),
			role: "columnheader",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Checkbox, {
				size: "sm",
				checked: selectedState,
				disabled: selectionDisabled,
				"aria-label": selectAllLabel,
				onCheckedChange: (checked) => onToggleAll(Boolean(checked)),
				onClick: (event) => event.stopPropagation()
			})
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "history.table-header.columnheader",
			className: historyHeaderCellClassName,
			role: "columnheader",
			children: sourceLabel
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "history.table-header.columnheader",
			className: historyHeaderCellClassName,
			role: "columnheader",
			children: titleLabel
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "history.table-header.columnheader",
			className: historyHeaderCellClassName,
			role: "columnheader",
			children: timeLabel
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "history.table-header.columnheader",
			className: cn(historyHeaderCellClassName, historyFixedActionCellClassName, showFixedActionShadow && "[--history-fixed-action-shadow:color-mix(in_oklch,var(--foreground)_33.3333%,transparent)] [box-shadow:-8px_0_12px_-12px_var(--history-fixed-action-shadow)]"),
			role: "columnheader",
			children: actionsLabel
		})
	]
});
const HistorySelectionCell = ({ checked, disabled = false, label, onCheckedChange }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "history.selection-cell",
	className: cn(historyBodyCellClassName, "justify-center px-2"),
	role: "cell",
	children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Checkbox, {
		size: "sm",
		checked,
		disabled,
		"aria-label": label,
		onCheckedChange: (nextChecked) => onCheckedChange(Boolean(nextChecked)),
		onClick: (event) => event.stopPropagation()
	})
});
const HistoryTitleButton = ({ title, onOpen }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
	"data-ui": "history.title-button",
	role: "button",
	tabIndex: 0,
	className: "-mx-1 block w-full min-w-0 max-w-full cursor-pointer truncate rounded-sm px-1 py-0 text-left font-medium text-muted-foreground transition-colors hover:text-foreground focus-visible:text-foreground focus-visible:underline focus-visible:outline-none",
	title,
	onClick: (event) => {
		event.stopPropagation();
		onOpen?.();
	},
	onKeyDown: (event) => {
		if (event.key !== "Enter" && event.key !== " ") return;
		event.preventDefault();
		event.stopPropagation();
		onOpen?.();
	},
	children: title
});
function HistoryActionContextMenu({ actions, children, className, onAction }) {
	const runAction = (0, import_react.useCallback)(async (action) => {
		if (!action.availability.enabled) return;
		const confirm = action.confirm;
		if (confirm) {
			await ConfirmActionPopup_default.show({
				title: confirm.title,
				content: confirm.description ?? confirm.content,
				okText: confirm.confirmText,
				cancelText: confirm.cancelText,
				danger: confirm.destructive,
				action: () => onAction(action)
			});
			return;
		}
		await onAction(action);
	}, [onAction]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandContextMenu, {
		location: "webcontents.context",
		extraItems: (0, import_react.useMemo)(() => {
			const toItems = (list) => {
				const items = [];
				let previousGroup;
				for (const action of list) {
					if (!action.availability.visible) continue;
					if (items.length > 0 && action.group !== previousGroup) items.push({ type: "separator" });
					previousGroup = action.group;
					const label = String(action.label);
					if (action.children.length > 0) items.push({
						type: "submenu",
						id: action.id,
						label,
						icon: action.icon,
						enabled: action.availability.enabled,
						children: toItems(action.children)
					});
					else items.push({
						type: "item",
						id: action.id,
						label,
						icon: action.icon,
						enabled: action.availability.enabled,
						destructive: action.danger,
						shortcutLabel: action.shortcut,
						onSelect: () => runAction(action)
					});
				}
				return items;
			};
			return toItems(actions);
		}, [actions, runAction]),
		contentClassName: className,
		children
	});
}
function HistoryActionsCell({ actions, deleteLabel, isPinned, pinLabel, unpinLabel, onAction, onTogglePin }) {
	const [pendingDeleteAction, setPendingDeleteAction] = (0, import_react.useState)();
	const deleteAction = (0, import_react.useMemo)(() => actions.find(isDeleteAction), [actions]);
	const handleAction = (0, import_react.useCallback)((action) => {
		window.requestAnimationFrame(() => {
			onAction(action);
		});
	}, [onAction]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RowFlex, {
		className: "items-center justify-center gap-1",
		onClick: (event) => event.stopPropagation(),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PinActionButton, {
			isPinned,
			pinLabel,
			unpinLabel,
			onClick: onTogglePin
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeleteActionButton, {
			action: deleteAction,
			label: deleteLabel,
			onClick: (action) => {
				if (action.confirm) {
					setPendingDeleteAction(action);
					return;
				}
				handleAction(action);
			}
		})]
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionConfirmDialog, {
		open: !!pendingDeleteAction,
		confirm: pendingDeleteAction?.confirm,
		contentClassName: "z-50",
		overlayClassName: "z-40",
		onOpenChange: (open) => {
			if (!open) setPendingDeleteAction(void 0);
		},
		onConfirm: async () => {
			if (!pendingDeleteAction) return;
			handleAction(pendingDeleteAction);
			setPendingDeleteAction(void 0);
		}
	})] });
}
function isDeleteAction(action) {
	return action.id.endsWith(".delete") || action.commandId?.endsWith(".delete");
}
var DeleteActionButton = ({ action, label, onClick }) => {
	const disabled = !action?.availability.enabled;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		type: "button",
		"aria-label": label,
		className: "text-muted-foreground hover:bg-accent hover:text-foreground",
		"data-testid": "history-delete-button",
		disabled,
		size: "icon-sm",
		title: label,
		variant: "ghost",
		onClick: (event) => {
			event.stopPropagation();
			if (action) onClick(action);
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trash2, { className: "size-4" })
	});
};
var PinActionButton = ({ isPinned, pinLabel, unpinLabel, onClick }) => {
	const label = isPinned ? unpinLabel : pinLabel;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		type: "button",
		"aria-label": label,
		className: "text-muted-foreground hover:bg-accent hover:text-foreground",
		"data-testid": "history-pin-button",
		size: "icon-sm",
		title: label,
		variant: "ghost",
		onClick: (event) => {
			event.stopPropagation();
			onClick?.();
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Pin, {
			size: 14,
			className: cn(isPinned && "-rotate-45")
		})
	});
};
const HistoryRecordRow = ({ actions, avatar, deleteLabel, isPinned, isSelected, minHeight, pinLabel, selectLabel, showFixedActionShadow, sourceLabel, timeLabel, title, unpinLabel, onAction, onOpen, onSelectedChange, onTogglePin }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
	"data-ui": "history.record-row",
	className: cn(historyTableGridClassName, historyBodyRowClassName),
	style: { minHeight },
	"data-state": isSelected ? "selected" : void 0,
	role: "row",
	children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistorySelectionCell, {
			checked: isSelected,
			disabled: isPinned,
			label: selectLabel,
			onCheckedChange: onSelectedChange
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "history.record-row.cell",
			className: historyBodyCellClassName,
			role: "cell",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RowFlex, {
				className: "min-w-0 items-center gap-2",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "flex size-6 shrink-0 items-center justify-center text-foreground text-sm leading-none",
					children: avatar
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "truncate text-foreground text-xs",
					children: sourceLabel
				})]
			})
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "history.record-row.cell",
			className: historyBodyCellClassName,
			role: "cell",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RowFlex, {
				className: "min-w-0 flex-1 items-center",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					"data-ui": "history.record-row.history-record-rename-field",
					className: "min-w-0 flex-1",
					"data-testid": "history-record-rename-field",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RowFlex, {
						className: "min-w-0 flex-1 items-center gap-1.5",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryTitleButton, {
							title,
							onOpen
						})
					})
				})
			})
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "history.record-row.cell",
			className: historyBodyCellClassName,
			role: "cell",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "text-muted-foreground text-xs tabular-nums",
				children: timeLabel
			})
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "history.record-row.cell",
			className: cn(historyBodyCellClassName, historyFixedActionCellClassName, showFixedActionShadow && "[--history-fixed-action-shadow:color-mix(in_oklch,var(--foreground)_33.3333%,transparent)] [box-shadow:-8px_0_12px_-12px_var(--history-fixed-action-shadow)]"),
			role: "cell",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryActionsCell, {
				actions,
				deleteLabel,
				isPinned,
				pinLabel,
				unpinLabel,
				onAction,
				onTogglePin
			})
		})
	]
});
function formatHistoryTime(value, t$2) {
	const date = (0, import_dayjs_min$4.default)(value);
	const now = (0, import_dayjs_min$4.default)();
	if (!date.isValid()) return t$2("history.records.table.emptyValue");
	if (date.isSame(now, "day")) return date.format("HH:mm");
	if (date.isSame(now.subtract(1, "day"), "day")) return t$2("common.yesterday");
	if (date.isSame(now, "year")) return date.format("MM/DD");
	return date.format("YYYY/MM/DD");
}
function HistoryRecordList({ descriptor, items, isLoading, isSelected, selectAllState, selectionDisabled, onToggleSelection, onToggleSelectAll }) {
	const { t: t$2 } = useTranslation();
	const list = (0, import_react.useMemo)(() => Array.from(items), [items]);
	const [renameTarget, setRenameTarget] = (0, import_react.useState)(null);
	const [showFixedActionShadow, setShowFixedActionShadow] = (0, import_react.useState)(false);
	const { getId, getName, getRowActions, getSelectLabel, getSourceLabel, getUpdatedAt, isPinned, onOpen, onRename, onTogglePin, renderAvatar, renderRowMenu, rowHeight, strings: { deleteLabel, pinLabel, unpinLabel } } = descriptor;
	const openRename = (0, import_react.useCallback)((id, name) => setRenameTarget({
		id,
		name
	}), []);
	const handleRenameSubmit = (0, import_react.useCallback)((name) => {
		if (!renameTarget) return;
		onRename(renameTarget.id, name);
	}, [onRename, renameTarget]);
	const handleRenameOpenChange = (0, import_react.useCallback)((open) => {
		if (!open) setRenameTarget(null);
	}, []);
	const emptyTitle = isLoading ? descriptor.strings.loadingTitle : descriptor.strings.emptyTitle;
	const emptyDescription = isLoading ? descriptor.strings.loadingDescription : descriptor.strings.emptyDescription;
	const emptyContent = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "history.record-list",
		className: "flex min-h-[320px] items-center justify-center px-5 py-8",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmptyState$1, {
			compact: true,
			icon: MessageSquareText,
			title: emptyTitle,
			description: emptyDescription
		})
	});
	const header = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryTableHeader, {
		actionsLabel: t$2("history.records.table.actions"),
		selectAllLabel: t$2("common.select_all"),
		selectedState: selectAllState,
		selectionDisabled,
		sourceLabel: descriptor.strings.sourceLabel,
		showFixedActionShadow,
		timeLabel: t$2("history.records.table.time"),
		titleLabel: descriptor.strings.titleColumnLabel,
		onToggleAll: onToggleSelectAll
	});
	const renderRow = (0, import_react.useCallback)((item) => {
		const id = getId(item);
		const rowActions = getRowActions(item, openRename);
		const pinned = isPinned(id);
		return renderRowMenu(item, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryRecordRow, {
			actions: rowActions.actions,
			avatar: renderAvatar(item),
			deleteLabel,
			isPinned: pinned,
			isSelected: !pinned && isSelected(id),
			minHeight: rowHeight,
			pinLabel,
			selectLabel: getSelectLabel(item),
			showFixedActionShadow,
			sourceLabel: getSourceLabel(item),
			timeLabel: formatHistoryTime(getUpdatedAt(item), t$2),
			title: getName(item),
			unpinLabel,
			onAction: rowActions.onAction,
			onOpen: () => onOpen(item),
			onSelectedChange: (checked) => onToggleSelection(id, checked),
			onTogglePin: async () => {
				if (await onTogglePin(item) !== false) onToggleSelection(id, false);
			}
		}), rowActions);
	}, [
		deleteLabel,
		getId,
		getName,
		getRowActions,
		getSelectLabel,
		getSourceLabel,
		getUpdatedAt,
		isPinned,
		isSelected,
		onOpen,
		onTogglePin,
		onToggleSelection,
		openRename,
		pinLabel,
		renderAvatar,
		renderRowMenu,
		rowHeight,
		showFixedActionShadow,
		t$2,
		unpinLabel
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "history.record-list",
		className: "flex min-h-0 flex-1 flex-col overflow-hidden bg-card",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryVirtualTable, {
			emptyContent,
			estimateSize: () => descriptor.rowHeight,
			header,
			items: list,
			onFixedActionShadowChange: setShowFixedActionShadow,
			renderRow
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EditNameDialog_default, {
			open: !!renameTarget,
			title: descriptor.strings.renameDialogTitle,
			initialName: renameTarget?.name ?? "",
			onSubmit: handleRenameSubmit,
			onOpenChange: handleRenameOpenChange
		})]
	});
}
var UNLINKED_ASSISTANT_SOURCE_ID = "__unlinked_assistant__";
var UNKNOWN_AGENT_SOURCE_ID = "__unknown_agent__";
function getTopicSourceId(topic, assistantById) {
	if (!topic.assistantId) return UNLINKED_ASSISTANT_SOURCE_ID;
	if (assistantById && !assistantById.has(topic.assistantId)) return UNLINKED_ASSISTANT_SOURCE_ID;
	return topic.assistantId;
}
function getSessionAgentSourceId(session, agentById) {
	if (!session.agentId) return UNKNOWN_AGENT_SOURCE_ID;
	if (agentById && !agentById.has(session.agentId)) return UNKNOWN_AGENT_SOURCE_ID;
	return session.agentId;
}
function getAgentHistoryStatus(streamStatus) {
	if (streamStatus?.isPending === true) return "running";
	if (streamStatus?.status === "error") return "failed";
	return "completed";
}
function findAdjacentHistoryRecordAfterBulkDelete(items, deletedIds, activeId, getId) {
	const deletedIdSet = new Set(deletedIds);
	const activeIndex = items.findIndex((item) => getId(item) === activeId);
	if (activeIndex < 0) return void 0;
	for (let index = activeIndex + 1; index < items.length; index += 1) if (!deletedIdSet.has(getId(items[index]))) return items[index];
	for (let index = activeIndex - 1; index >= 0; index -= 1) if (!deletedIdSet.has(getId(items[index]))) return items[index];
}
function buildAgentStatusItems(t$2) {
	return [
		{
			id: "all",
			label: t$2("common.all")
		},
		{
			id: "running",
			label: t$2("history.records.status.running"),
			dotClassName: "text-warning"
		},
		{
			id: "completed",
			label: t$2("history.records.status.completed"),
			dotClassName: "text-success"
		},
		{
			id: "failed",
			label: t$2("history.records.status.failed"),
			dotClassName: "text-destructive"
		}
	];
}
function buildAssistantSources(topics, assistantById, assistantRankById, unlinkedAssistantLabel, t$2) {
	const hasUnlinkedAssistant = topics.some((topic) => getTopicSourceId(topic, assistantById) === UNLINKED_ASSISTANT_SOURCE_ID);
	return [
		{
			id: "all",
			label: t$2("common.all")
		},
		...Array.from(assistantById.values()).sort((left, right) => getAssistantSourceRank(left.id, assistantRankById) - getAssistantSourceRank(right.id, assistantRankById)).map((assistant) => ({
			id: assistant.id,
			label: assistant.name,
			icon: assistant.emoji ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				"data-ui": "history.build-assistant-sources",
				className: "text-sm leading-none",
				children: assistant.emoji
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bot, { size: 15 })
		})),
		...hasUnlinkedAssistant ? [{
			id: UNLINKED_ASSISTANT_SOURCE_ID,
			label: unlinkedAssistantLabel,
			icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bot, { size: 15 })
		}] : []
	];
}
function buildAgentSources(sessions, agentById, agentRankById, unknownAgentLabel, t$2) {
	const hasUnknownAgent = sessions.some((session) => getSessionAgentSourceId(session, agentById) === UNKNOWN_AGENT_SOURCE_ID);
	return [
		{
			id: "all",
			label: t$2("common.all")
		},
		...Array.from(agentById.values()).sort((left, right) => getAgentSourceRank(left.id, agentRankById) - getAgentSourceRank(right.id, agentRankById)).map((agent) => {
			return {
				id: agent.id,
				label: agent.name,
				icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmojiIcon_default, {
					emoji: getAgentAvatarFromConfiguration(agent.configuration),
					size: 18,
					fontSize: 11,
					className: "mr-0 text-foreground"
				})
			};
		}),
		...hasUnknownAgent ? [{
			id: UNKNOWN_AGENT_SOURCE_ID,
			label: unknownAgentLabel,
			icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bot, { size: 15 })
		}] : []
	];
}
function getAssistantSourceRank(sourceId, assistantRankById) {
	const assistantRank = assistantRankById.get(sourceId);
	if (assistantRank !== void 0) return assistantRank;
	return Number.MAX_SAFE_INTEGER;
}
function getAgentSourceRank(sourceId, agentRankById) {
	const agentRank = agentRankById.get(sourceId);
	if (agentRank !== void 0) return agentRank;
	return Number.MAX_SAFE_INTEGER;
}
var HistoryTopBar = ({ mode, toolbarLeading, searchText, searchPlaceholder, onSearchTextChange, selectedSourceId, onSourceSelect, renderSourceFilter, statusOptions, statusLabel, statusPlaceholder, selectedStatus, onStatusSelect, selectedCount, bulkDeleteCount, bulkMoveTargets = [], onBulkDelete, onBulkMove }) => {
	const { t: t$2 } = useTranslation();
	const [deleteDialogOpen, setDeleteDialogOpen] = (0, import_react.useState)(false);
	const [moveDialogOpen, setMoveDialogOpen] = (0, import_react.useState)(false);
	const [moveTargetId, setMoveTargetId] = (0, import_react.useState)("");
	const moveTargets = (0, import_react.useMemo)(() => Array.from(bulkMoveTargets), [bulkMoveTargets]);
	const selectedMoveTarget = (0, import_react.useMemo)(() => moveTargets.find((target) => target.id === moveTargetId), [moveTargetId, moveTargets]);
	const canBulkDelete = bulkDeleteCount > 0 && !!onBulkDelete;
	const canBulkMove = mode === "assistant" && selectedCount > 0 && moveTargets.length > 0 && !!onBulkMove;
	const deleteTitle = mode === "assistant" ? t$2("history.records.bulkDeleteTopics.title") : t$2("history.records.bulkDeleteSessions.title");
	const deleteDescription = mode === "assistant" ? t$2("history.records.bulkDeleteTopics.description", { count: bulkDeleteCount }) : t$2("history.records.bulkDeleteSessions.description", { count: bulkDeleteCount });
	(0, import_react.useEffect)(() => {
		if (!moveDialogOpen) return;
		if (moveTargets.length === 0) {
			setMoveTargetId("");
			return;
		}
		if (!moveTargets.some((target) => target.id === moveTargetId)) setMoveTargetId(moveTargets[0].id);
	}, [
		moveDialogOpen,
		moveTargetId,
		moveTargets
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			"data-ui": "history.top-bar",
			className: "flex h-11 shrink-0 items-center gap-2 bg-card px-2",
			children: [
				toolbarLeading ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex shrink-0 items-center",
					children: toolbarLeading
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "w-[220px] max-w-[38vw] [&_[data-slot=input-group-control]]:h-8 [&_[data-slot=input-group]]:h-8",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchInput, {
						value: searchText,
						placeholder: searchPlaceholder,
						"aria-label": searchPlaceholder,
						onChange: (event) => onSearchTextChange(event.target.value),
						onClear: () => onSearchTextChange(""),
						clearLabel: t$2("history.records.clearSearch")
					})
				}),
				renderSourceFilter(selectedSourceId === "all" ? null : selectedSourceId, (id) => onSourceSelect(id ?? "all")),
				statusOptions && selectedStatus && onStatusSelect && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "group/status-select relative flex items-center",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Select, {
						value: selectedStatus === "all" ? "" : selectedStatus,
						onValueChange: (value) => onStatusSelect(value),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectTrigger, {
							"aria-label": statusLabel,
							className: cn("h-8 w-[132px] text-xs", selectedStatus !== "all" && "[&_svg]:transition-opacity group-focus-within/status-select:[&_svg]:opacity-0 group-hover/status-select:[&_svg]:opacity-0"),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectValue, { placeholder: statusPlaceholder ?? statusLabel })
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectContent, { children: statusOptions.filter((option) => option.id !== "all").map((option) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
							value: option.id,
							className: "text-xs",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
								className: "flex items-center gap-2",
								children: [option.dotClassName && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: cn("size-2 rounded-full bg-current", option.dotClassName) }), option.label]
							})
						}, option.id)) })]
					}), selectedStatus !== "all" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						variant: "ghost",
						"aria-label": t$2("common.clear"),
						onClick: (event) => {
							event.stopPropagation();
							onStatusSelect("all");
						},
						className: "-translate-y-1/2 pointer-events-none absolute top-1/2 right-2 flex size-5 min-h-0 shrink-0 items-center justify-center rounded-full bg-transparent p-0 text-muted-foreground opacity-0 shadow-none transition-[background-color,color,opacity] hover:bg-muted hover:text-foreground focus-visible:pointer-events-auto focus-visible:opacity-100 group-focus-within/status-select:pointer-events-auto group-focus-within/status-select:opacity-100 group-hover/status-select:pointer-events-auto group-hover/status-select:opacity-100",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { size: 12 })
					}) : null]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "min-w-0 flex-1" }),
				mode === "assistant" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
					type: "button",
					variant: "outline",
					className: "h-8 gap-1.5 rounded-md px-2.5 text-xs shadow-none",
					disabled: !canBulkMove,
					onClick: () => {
						setMoveTargetId((current) => current || moveTargets[0]?.id || "");
						setMoveDialogOpen(true);
					},
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FolderInput, { className: "size-3.5" }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [t$2("history.records.bulkMove"), selectedCount > 0 ? ` (${selectedCount})` : ""] })]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
					type: "button",
					variant: "outline",
					className: "h-8 gap-1.5 rounded-md px-2.5 text-destructive text-xs shadow-none hover:text-destructive",
					disabled: !canBulkDelete,
					onClick: () => setDeleteDialogOpen(true),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trash2, { className: "size-3.5" }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [t$2("history.records.bulkDelete"), bulkDeleteCount > 0 ? ` (${bulkDeleteCount})` : ""] })]
				})
			]
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfirmDialog, {
			open: deleteDialogOpen,
			onOpenChange: setDeleteDialogOpen,
			title: deleteTitle,
			description: deleteDescription,
			confirmText: t$2("common.delete"),
			cancelText: t$2("common.cancel"),
			destructive: true,
			onConfirm: async () => {
				await onBulkDelete?.();
				setDeleteDialogOpen(false);
			}
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfirmDialog, {
			open: moveDialogOpen,
			onOpenChange: setMoveDialogOpen,
			title: t$2("history.records.bulkMoveTopics.title"),
			description: t$2("history.records.bulkMoveTopics.description", { count: selectedCount }),
			content: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "space-y-2",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "font-medium text-muted-foreground text-xs leading-4",
					children: t$2("history.records.bulkMoveTopics.target")
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectDropdown, {
					items: moveTargets,
					selectedId: moveTargetId,
					onSelect: setMoveTargetId,
					placeholder: t$2("history.records.bulkMoveTopics.placeholder"),
					emptyText: t$2("history.records.bulkMoveTopics.empty"),
					triggerClassName: "h-8 rounded-md border-border-subtle bg-card text-xs shadow-none",
					renderSelected: (target) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryBulkMoveTargetLabel, { target }),
					renderItem: (target) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryBulkMoveTargetLabel, { target })
				})]
			}),
			confirmText: t$2("history.records.bulkMoveTopics.confirm"),
			cancelText: t$2("common.cancel"),
			onConfirm: async () => {
				if (!selectedMoveTarget) return;
				await onBulkMove?.(selectedMoveTarget.id);
				setMoveDialogOpen(false);
			}
		})
	] });
};
var HistoryBulkMoveTargetLabel = ({ target }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
	"data-ui": "history.bulk-move-target",
	className: "flex min-w-0 items-center gap-2",
	children: [target.icon && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		className: "flex size-4 shrink-0 items-center justify-center",
		children: target.icon
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		className: "truncate",
		children: target.label
	})]
});
var HistoryTopBar_default = HistoryTopBar;
function HistoryRecordsContent({ descriptor, controller, isLoading, toolbarLeading }) {
	const { t: t$2 } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
		"data-ui": "history.records-content",
		className: "flex min-h-0 flex-1 flex-col overflow-hidden bg-card pb-3 text-card-foreground",
		"aria-label": t$2("history.records.shortTitle"),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryTopBar_default, {
			mode: descriptor.mode,
			toolbarLeading,
			searchText: controller.searchText,
			searchPlaceholder: descriptor.strings.searchPlaceholder,
			onSearchTextChange: controller.setSearchText,
			selectedSourceId: controller.selectedSourceId,
			onSourceSelect: controller.setSelectedSourceId,
			renderSourceFilter: descriptor.renderSourceFilter,
			statusOptions: descriptor.statusOptions,
			statusLabel: t$2("history.records.filter.statusLabel"),
			statusPlaceholder: t$2("history.records.filter.statusPlaceholder"),
			selectedStatus: controller.selectedStatus,
			onStatusSelect: controller.setSelectedStatus,
			selectedCount: controller.selectedCount,
			bulkDeleteCount: controller.bulkDeleteCount,
			bulkMoveTargets: descriptor.bulkMoveTargets,
			onBulkDelete: controller.handleBulkDelete,
			onBulkMove: descriptor.onBulkMove ? controller.handleBulkMove : void 0
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryRecordList, {
			descriptor,
			items: controller.visibleItems,
			isLoading,
			isSelected: controller.isSelected,
			selectAllState: controller.selectAllState,
			selectionDisabled: controller.selectionDisabled,
			onToggleSelection: controller.toggleSelection,
			onToggleSelectAll: controller.toggleSelectAll
		})]
	});
}
var SourceFilterTrigger = ({ ref, label, icon, hasValue, className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
	"data-ui": "history.source-filter-trigger",
	ref,
	type: "button",
	className: cn("inline-flex h-8 w-fit min-w-[128px] max-w-[220px] items-center justify-between gap-2 whitespace-nowrap", "rounded-md border border-border bg-transparent px-3 font-normal text-foreground text-xs outline-none transition-colors", "hover:bg-accent/40 focus-visible:bg-accent/40", "data-[state=open]:bg-accent/40", className),
	...mergeUiProps(props, "history.source-filter-trigger"),
	children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
		className: "flex min-w-0 items-center gap-1.5",
		children: [icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: "flex size-4 shrink-0 items-center justify-center",
			children: icon
		}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: cn("min-w-0 truncate", !hasValue && "text-muted-foreground"),
			children: label
		})]
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown, { className: cn("size-4 shrink-0 text-muted-foreground", hasValue && "transition-opacity group-focus-within/source-select:opacity-0 group-hover/source-select:opacity-0") })]
});
SourceFilterTrigger.displayName = "SourceFilterTrigger";
const HistorySourceFilterField = ({ label, icon, hasValue, clearLabel, onClear, selector }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
	"data-ui": "history.source-filter-field",
	className: "group/source-select relative flex shrink-0 items-center",
	children: [selector(/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SourceFilterTrigger, {
		label,
		icon,
		hasValue
	})), hasValue ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		type: "button",
		variant: "ghost",
		"aria-label": clearLabel,
		onClick: (event) => {
			event.stopPropagation();
			onClear();
		},
		className: "-translate-y-1/2 pointer-events-none absolute top-1/2 right-2 flex size-5 min-h-0 shrink-0 items-center justify-center rounded-full bg-transparent p-0 text-muted-foreground opacity-0 shadow-none transition-[background-color,color,opacity] hover:bg-muted hover:text-foreground focus-visible:pointer-events-auto focus-visible:opacity-100 group-focus-within/source-select:pointer-events-auto group-focus-within/source-select:opacity-100 group-hover/source-select:pointer-events-auto group-hover/source-select:opacity-100",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { size: 12 })
	}) : null]
});
function useHistoryRecordsController({ descriptor, timeSorted, sourceSorted, activeRecordId }) {
	const { getId, isPinned, getSourceId, statusOf, matchesSearch, sources, onBulkDelete, onActiveRecordChange, onBulkMove } = descriptor;
	const [searchText, setSearchText] = (0, import_react.useState)("");
	const [selectedSourceId, setSelectedSourceId] = (0, import_react.useState)("all");
	const [selectedStatus, setSelectedStatus] = (0, import_react.useState)("all");
	const [selectedIds, setSelectedIds] = (0, import_react.useState)([]);
	const visibleItems = (0, import_react.useMemo)(() => {
		const base = selectedSourceId === "all" ? timeSorted : sourceSorted;
		const afterStatus = statusOf && selectedStatus !== "all" ? base.filter((item) => statusOf(item) === selectedStatus) : base;
		const afterSource = selectedSourceId === "all" ? afterStatus : afterStatus.filter((item) => getSourceId(item) === selectedSourceId);
		const keywords = searchText.trim().toLowerCase();
		if (!keywords) return afterSource;
		return afterSource.filter((item) => matchesSearch(item, keywords));
	}, [
		getSourceId,
		matchesSearch,
		searchText,
		selectedSourceId,
		selectedStatus,
		sourceSorted,
		statusOf,
		timeSorted
	]);
	(0, import_react.useEffect)(() => {
		if (selectedSourceId === "all") return;
		if (sources.some((source) => source.id === selectedSourceId)) return;
		setSelectedSourceId("all");
	}, [selectedSourceId, sources]);
	(0, import_react.useEffect)(() => {
		const visibleSelectableIds = new Set(visibleItems.filter((item) => !isPinned(getId(item))).map((item) => getId(item)));
		setSelectedIds((ids) => {
			const next = ids.filter((id) => visibleSelectableIds.has(id));
			return next.length === ids.length ? ids : next;
		});
	}, [
		getId,
		isPinned,
		visibleItems
	]);
	const selectableIds = (0, import_react.useMemo)(() => visibleItems.filter((item) => !isPinned(getId(item))).map((item) => getId(item)), [
		getId,
		isPinned,
		visibleItems
	]);
	const selectedIdSet = (0, import_react.useMemo)(() => new Set(selectedIds), [selectedIds]);
	const selectedDeletableIds = (0, import_react.useMemo)(() => selectedIds.filter((id) => !isPinned(id)), [isPinned, selectedIds]);
	const selectedSelectableCount = (0, import_react.useMemo)(() => selectableIds.filter((id) => selectedIdSet.has(id)).length, [selectableIds, selectedIdSet]);
	const selectAllState = selectableIds.length > 0 && selectedSelectableCount === selectableIds.length ? true : selectedSelectableCount > 0 ? "indeterminate" : false;
	const isSelected = (0, import_react.useCallback)((id) => selectedIdSet.has(id), [selectedIdSet]);
	const toggleSelection = (0, import_react.useCallback)((id, checked) => {
		if (checked && isPinned(id)) return;
		setSelectedIds((ids) => checked ? ids.includes(id) ? ids : [...ids, id] : ids.filter((current) => current !== id));
	}, [isPinned]);
	const toggleSelectAll = (0, import_react.useCallback)((checked) => setSelectedIds(checked ? selectableIds : []), [selectableIds]);
	const handleBulkDelete = (0, import_react.useCallback)(async () => {
		const ids = selectedDeletableIds;
		if (ids.length === 0) return;
		const deletedIds = await onBulkDelete(ids);
		if (!deletedIds) return;
		const deletedIdSet = new Set(deletedIds);
		setSelectedIds((current) => current.filter((id) => !deletedIdSet.has(id)));
		if (activeRecordId && deletedIds.includes(activeRecordId)) onActiveRecordChange(findAdjacentHistoryRecordAfterBulkDelete(timeSorted, deletedIds, activeRecordId, getId) ?? null);
	}, [
		activeRecordId,
		getId,
		onActiveRecordChange,
		onBulkDelete,
		selectedDeletableIds,
		timeSorted
	]);
	const handleBulkMove = (0, import_react.useCallback)(async (targetId) => {
		const ids = selectedIds;
		if (ids.length === 0 || !onBulkMove) return;
		const movedIds = await onBulkMove(targetId, ids);
		if (!movedIds) return;
		const movedIdSet = new Set(movedIds);
		setSelectedIds((current) => current.filter((id) => !movedIdSet.has(id)));
	}, [onBulkMove, selectedIds]);
	return {
		searchText,
		setSearchText,
		selectedSourceId,
		setSelectedSourceId,
		selectedStatus,
		setSelectedStatus,
		visibleItems,
		selectedIds,
		selectedCount: selectedIds.length,
		bulkDeleteCount: selectedDeletableIds.length,
		selectAllState,
		selectionDisabled: selectableIds.length === 0,
		isSelected,
		toggleSelection,
		toggleSelectAll,
		handleBulkDelete,
		handleBulkMove
	};
}
var AgentHistoryRecords = ({ activeRecordId, onClose, onRecordSelect, toolbarLeading }) => {
	const { t: t$2 } = useTranslation();
	const [groupNow] = (0, import_react.useState)(() => /* @__PURE__ */ new Date());
	const conversationNav = useConversationNavigation("agents");
	const { sessions, pinIdBySessionId, isLoadingAll: isSessionsLoading, deleteSession, deleteSessions, togglePin } = useAgentSessionsSource();
	const { agents } = useAgents();
	const { updateSession } = useUpdateSession();
	const isSessionPinned = (0, import_react.useCallback)((sessionId) => pinIdBySessionId.has(sessionId), [pinIdBySessionId]);
	const sessionItems = (0, import_react.useMemo)(() => sessions.map((session) => ({
		...session,
		pinned: isSessionPinned(session.id)
	})), [isSessionPinned, sessions]);
	const agentById = (0, import_react.useMemo)(() => new Map(agents.map((agent) => [agent.id, agent])), [agents]);
	const agentRankById = (0, import_react.useMemo)(() => new Map(agents.map((agent, index) => [agent.id, index])), [agents]);
	const timeSortedSessions = (0, import_react.useMemo)(() => sortSessionsForDisplayGroups(sessionItems, {
		mode: "time",
		now: groupNow
	}), [groupNow, sessionItems]);
	const agentSortedSessions = (0, import_react.useMemo)(() => sortSessionsForDisplayGroups(sessionItems, {
		agentRankById,
		mode: "agent",
		now: groupNow
	}), [
		agentRankById,
		groupNow,
		sessionItems
	]);
	const streamStatusBySessionId = useAgentSessionStreamStatuses((0, import_react.useMemo)(() => sessionItems.map((session) => session.id), [sessionItems]));
	const unknownAgentLabel = t$2("agent.session.group.unknown_agent");
	const statusItems = (0, import_react.useMemo)(() => buildAgentStatusItems(t$2), [t$2]);
	const agentSources = (0, import_react.useMemo)(() => buildAgentSources(sessionItems, agentById, agentRankById, unknownAgentLabel, t$2), [
		agentById,
		agentRankById,
		sessionItems,
		t$2,
		unknownAgentLabel
	]);
	const additionalAgentSourceItems = (0, import_react.useMemo)(() => agentSources.filter((source) => source.id !== "all" && !agentById.has(source.id)).map((source) => ({
		id: source.id,
		name: source.label,
		editDisabled: true,
		pinDisabled: true
	})), [agentById, agentSources]);
	const handleSessionSelect = (0, import_react.useCallback)((session) => {
		const title = session.name || t$2("common.unnamed");
		if (conversationNav.openConversationTab(session.id, title, { forceNew: true })) return;
		onRecordSelect?.(session.id);
		onClose();
	}, [
		conversationNav,
		onClose,
		onRecordSelect,
		t$2
	]);
	const handleDeleteSession = (0, import_react.useCallback)(async (id) => {
		if (isSessionPinned(id)) return;
		if (await deleteSession(id) && activeRecordId === id) {
			const nextSession = findAdjacentHistoryRecordAfterBulkDelete(timeSortedSessions, [id], id, (session) => session.id);
			onRecordSelect?.(nextSession?.id ?? null);
		}
	}, [
		activeRecordId,
		deleteSession,
		isSessionPinned,
		onRecordSelect,
		timeSortedSessions
	]);
	const handleBulkDeleteSessions = (0, import_react.useCallback)(async (ids) => {
		const result = await deleteSessions(ids);
		return result ? result.deletedIds : void 0;
	}, [deleteSessions]);
	const handleRenameSession = (0, import_react.useCallback)(async (id, name) => {
		const session = sessions.find((candidate) => candidate.id === id);
		const trimmedName = name.trim();
		if (!session || !trimmedName || trimmedName === session.name) return;
		if (await updateSession({
			id,
			name: trimmedName,
			isNameManuallyEdited: true
		}, { showSuccessToast: false })) toast.success(t$2("common.saved"));
	}, [
		sessions,
		t$2,
		updateSession
	]);
	const handleToggleSessionPin = (0, import_react.useCallback)((sessionId) => togglePin(sessionId), [togglePin]);
	const sessionMenuPreset = useSessionMenuPreset({ getActionContext: (0, import_react.useCallback)((session) => createSessionActionContext({
		isActiveInCurrentTab: false,
		onDelete: () => {
			handleDeleteSession(session.id);
		},
		onTogglePin: () => {
			handleToggleSessionPin(session.id);
		},
		pinned: isSessionPinned(session.id),
		sessionName: session.name ?? session.id,
		startEdit: () => void 0,
		t: t$2
	}), [
		handleDeleteSession,
		handleToggleSessionPin,
		isSessionPinned,
		t$2
	]) });
	const descriptor = {
		mode: "agent",
		getId: (0, import_react.useCallback)((session) => session.id, []),
		isPinned: isSessionPinned,
		getSourceId: (0, import_react.useCallback)((session) => getSessionAgentSourceId(session, agentById), [agentById]),
		statusOf: (0, import_react.useCallback)((session) => getAgentHistoryStatus(streamStatusBySessionId.get(session.id)), [streamStatusBySessionId]),
		matchesSearch: (0, import_react.useCallback)((session, keywords) => {
			const agent = session.agentId ? agentById.get(session.agentId) : void 0;
			return [
				session.name,
				session.description,
				agent?.name
			].some((value) => value?.toLowerCase().includes(keywords));
		}, [agentById]),
		onBulkDelete: handleBulkDeleteSessions,
		onActiveRecordChange: (0, import_react.useCallback)((session) => onRecordSelect?.(session?.id ?? null), [onRecordSelect]),
		...(0, import_react.useMemo)(() => ({
			getName: (session) => session.name || t$2("common.unnamed"),
			getUpdatedAt: (session) => session.updatedAt,
			getSourceLabel: (session) => (session.agentId ? agentById.get(session.agentId)?.name : void 0) ?? unknownAgentLabel,
			renderAvatar: (session) => {
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmojiIcon_default, {
					emoji: getAgentAvatarFromConfiguration((session.agentId ? agentById.get(session.agentId) : void 0)?.configuration),
					size: 20,
					fontSize: 12,
					className: "mr-0 text-foreground"
				});
			},
			rowHeight: 32,
			getSelectLabel: (session) => `${t$2("common.select")} ${session.name || t$2("common.unnamed")}`,
			getRowActions: (session, openRename) => {
				const contextOverride = { startEdit: () => openRename(session.id, session.name ?? "") };
				return {
					actions: sessionMenuPreset.getActions(session, contextOverride),
					onAction: (action) => sessionMenuPreset.onAction(session, action, contextOverride)
				};
			},
			onOpen: handleSessionSelect,
			onTogglePin: (session) => handleToggleSessionPin(session.id),
			renderRowMenu: (_session, row, rowActions) => rowActions.actions.length ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryActionContextMenu, {
				actions: rowActions.actions,
				className: "z-50",
				onAction: rowActions.onAction,
				children: row
			}) : row
		}), [
			agentById,
			handleSessionSelect,
			handleToggleSessionPin,
			sessionMenuPreset,
			t$2,
			unknownAgentLabel
		]),
		sources: agentSources,
		renderSourceFilter: (selectedId, onSelect) => {
			const source = selectedId ? agentSources.find((candidate) => candidate.id === selectedId) : void 0;
			const agent = selectedId ? agentById.get(selectedId) : void 0;
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistorySourceFilterField, {
				label: selectedId ? source?.label || agent?.name || t$2("common.unnamed") : t$2("history.records.filter.selectAgent"),
				hasValue: !!selectedId,
				clearLabel: t$2("common.clear"),
				onClear: () => onSelect(null),
				icon: selectedId ? source?.icon ? source.icon : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmojiIcon_default, {
					emoji: getAgentAvatarFromConfiguration(agent?.configuration),
					size: 16,
					fontSize: 10,
					className: "mr-0 text-foreground"
				}) : void 0,
				selector: (trigger) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AgentSelector, {
					value: selectedId,
					onChange: onSelect,
					trigger,
					additionalItems: additionalAgentSourceItems
				})
			});
		},
		statusOptions: statusItems,
		onRename: handleRenameSession,
		strings: {
			sourceLabel: t$2("common.agent"),
			searchPlaceholder: t$2("history.records.searchSession"),
			titleColumnLabel: t$2("history.records.table.session"),
			emptyTitle: t$2("history.records.empty.sessionsTitle"),
			emptyDescription: t$2("history.records.empty.sessionsDescription"),
			loadingTitle: t$2("history.records.loading.sessionsTitle"),
			loadingDescription: t$2("history.records.loading.sessionsDescription"),
			pinLabel: t$2("selector.common.pin"),
			unpinLabel: t$2("selector.common.unpin"),
			deleteLabel: t$2("common.delete"),
			renameDialogTitle: t$2("agent.session.edit.title")
		}
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryRecordsContent, {
		descriptor,
		controller: useHistoryRecordsController({
			descriptor,
			timeSorted: timeSortedSessions,
			sourceSorted: agentSortedSessions,
			activeRecordId
		}),
		isLoading: isSessionsLoading,
		toolbarLeading
	});
};
var AgentHistoryRecords_default = AgentHistoryRecords;
var topicActionRegistry = createActionRegistry();
var MOVE_TO_ASSISTANT_ACTION_PREFIX = "topic.move-to-assistant.";
function buildMoveToAssistantActionId(assistantId) {
	return `${MOVE_TO_ASSISTANT_ACTION_PREFIX}${encodeURIComponent(assistantId)}`;
}
function getMoveToAssistantTargetId(actionId) {
	if (!actionId.startsWith(MOVE_TO_ASSISTANT_ACTION_PREFIX)) return void 0;
	try {
		return decodeURIComponent(actionId.slice(24));
	} catch {
		return;
	}
}
function renderMoveAssistantTargetIcon(icon) {
	if (!icon) return void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		"data-ui": "chat.render-move-assistant-target",
		className: "flex size-4 items-center justify-center [&>*]:m-0 [&>*]:max-h-full [&>*]:max-w-full",
		children: icon
	});
}
var hasExportOption = ({ exportMenuOptions }) => exportMenuOptions.image || exportMenuOptions.markdown || exportMenuOptions.markdown_reason || exportMenuOptions.docx || exportMenuOptions.notion || exportMenuOptions.yuque || exportMenuOptions.obsidian || exportMenuOptions.joplin || exportMenuOptions.siyuan;
topicActionRegistry.registerCommand({
	id: "topic.auto-rename",
	run: ({ onAutoRename, topic }) => onAutoRename(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.rename",
	run: ({ onStartRename, topic }) => onStartRename(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.pin",
	run: ({ onPinTopic, topic }) => onPinTopic(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.open-in-new-tab",
	availability: ({ isActiveInCurrentTab, onOpenInNewTab }) => ({
		visible: !!onOpenInNewTab && !isActiveInCurrentTab,
		enabled: !!onOpenInNewTab && !isActiveInCurrentTab
	}),
	run: ({ onOpenInNewTab, topic }) => onOpenInNewTab?.(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.open-in-new-window",
	availability: ({ onOpenInNewWindow }) => ({
		visible: !!onOpenInNewWindow,
		enabled: !!onOpenInNewWindow
	}),
	run: ({ onOpenInNewWindow, topic }) => onOpenInNewWindow?.(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.position-left",
	availability: ({ onSetPanePosition, panePosition }) => ({
		visible: !!onSetPanePosition && !!panePosition,
		enabled: !!onSetPanePosition && panePosition !== "left"
	}),
	run: ({ onSetPanePosition }) => onSetPanePosition?.("left")
});
topicActionRegistry.registerCommand({
	id: "topic.position-right",
	availability: ({ onSetPanePosition, panePosition }) => ({
		visible: !!onSetPanePosition && !!panePosition,
		enabled: !!onSetPanePosition && panePosition !== "right"
	}),
	run: ({ onSetPanePosition }) => onSetPanePosition?.("right")
});
topicActionRegistry.registerCommand({
	id: "topic.clear-messages",
	run: ({ onClearMessages, topic }) => onClearMessages(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.save-notes",
	availability: ({ exportMenuOptions }) => ({
		visible: exportMenuOptions.notes,
		enabled: exportMenuOptions.notes
	}),
	run: ({ onSaveToNotes, topic }) => onSaveToNotes(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.save-knowledge",
	run: ({ onSaveToKnowledge, topic }) => onSaveToKnowledge(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.export.image",
	run: ({ onExportImage, topic }) => onExportImage(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.export.markdown",
	run: ({ onExportMarkdown, topic }) => onExportMarkdown(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.export.markdown-reason",
	run: ({ onExportMarkdownReason, topic }) => onExportMarkdownReason(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.export.word",
	run: ({ onExportWord, topic }) => onExportWord(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.export.notion",
	run: ({ onExportNotion, topic }) => onExportNotion(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.export.yuque",
	run: ({ onExportYuque, topic }) => onExportYuque(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.export.obsidian",
	run: ({ onExportObsidian, topic }) => onExportObsidian(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.export.joplin",
	run: ({ onExportJoplin, topic }) => onExportJoplin(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.export.siyuan",
	run: ({ onExportSiyuan, topic }) => onExportSiyuan(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.copy.image",
	availability: ({ exportMenuOptions }) => ({
		visible: exportMenuOptions.image,
		enabled: exportMenuOptions.image
	}),
	run: ({ onCopyImage, topic }) => onCopyImage(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.copy.markdown",
	run: ({ onCopyMarkdown, topic }) => onCopyMarkdown(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.copy.plain-text",
	availability: ({ exportMenuOptions }) => ({
		visible: exportMenuOptions.plain_text,
		enabled: exportMenuOptions.plain_text
	}),
	run: ({ onCopyPlainText, topic }) => onCopyPlainText(topic)
});
topicActionRegistry.registerCommand({
	id: "topic.delete",
	run: ({ onDelete, topic }) => onDelete(topic)
});
topicActionRegistry.registerAction({
	id: "topic.auto-rename",
	commandId: "topic.auto-rename",
	label: ({ t: t$2 }) => t$2("chat.topics.auto_rename"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Sparkles, { size: 14 }),
	order: 10,
	surface: "menu",
	availability: ({ isRenaming }) => ({ enabled: !isRenaming })
});
topicActionRegistry.registerAction({
	id: "topic.rename",
	commandId: "topic.rename",
	label: ({ t: t$2 }) => t$2("chat.topics.edit.title"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PenLine, { size: 14 }),
	order: 20,
	surface: "menu",
	availability: ({ isRenaming }) => ({ enabled: !isRenaming })
});
topicActionRegistry.registerAction({
	id: "topic.pin",
	commandId: "topic.pin",
	label: ({ t: t$2, topic }) => topic.pinned ? t$2("chat.topics.unpin") : t$2("chat.topics.pin"),
	icon: ({ topic }) => topic.pinned ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PinOff, { size: 14 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Pin, { size: 14 }),
	order: 30,
	surface: "menu"
});
topicActionRegistry.registerAction({
	id: "topic.move-to-assistant",
	label: ({ t: t$2 }) => t$2("chat.topics.move_to"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MoveRight, { size: 14 }),
	order: 34,
	surface: "menu",
	availability: ({ assistantMoveTargets, onMoveToAssistant }) => ({
		visible: !!onMoveToAssistant && assistantMoveTargets.length > 0,
		enabled: !!onMoveToAssistant && assistantMoveTargets.length > 0
	}),
	children: ({ assistantMoveTargets }) => assistantMoveTargets.map((target, index) => ({
		id: buildMoveToAssistantActionId(target.id),
		label: target.name,
		icon: renderMoveAssistantTargetIcon(target.icon),
		order: index,
		surface: "menu"
	}))
});
topicActionRegistry.registerAction({
	id: "topic.open-in-new-tab",
	commandId: "topic.open-in-new-tab",
	label: ({ t: t$2 }) => t$2("common.open_in_new_tab"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ExternalLink, { size: 14 }),
	order: 35,
	surface: "menu"
});
topicActionRegistry.registerAction({
	id: "topic.open-in-new-window",
	commandId: "topic.open-in-new-window",
	label: ({ t: t$2 }) => t$2("tab.open_in_new_window"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OpenInNewWindowIcon, { size: 14 }),
	order: 37,
	surface: "menu"
});
topicActionRegistry.registerAction({
	id: "topic.position",
	label: ({ t: t$2 }) => t$2("settings.topic.position.label"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PanelLeft, { size: 14 }),
	order: 38,
	surface: "menu",
	availability: ({ onSetPanePosition, panePosition }) => ({ visible: !!onSetPanePosition && !!panePosition }),
	children: [{
		id: "topic.position-left",
		commandId: "topic.position-left",
		label: ({ t: t$2 }) => t$2("settings.topic.position.left"),
		order: 10,
		surface: "menu"
	}, {
		id: "topic.position-right",
		commandId: "topic.position-right",
		label: ({ t: t$2 }) => t$2("settings.topic.position.right"),
		order: 20,
		surface: "menu"
	}]
});
topicActionRegistry.registerAction({
	id: "topic.clear-messages",
	commandId: "topic.clear-messages",
	label: ({ t: t$2 }) => t$2("chat.topics.clear.title"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BrushCleaning, { size: 14 }),
	order: 40,
	surface: "menu"
});
topicActionRegistry.registerAction({
	id: "topic.save-notes",
	commandId: "topic.save-notes",
	label: ({ t: t$2 }) => t$2("notes.save"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NotebookPen, { size: 14 }),
	group: "share",
	order: 50,
	surface: "menu"
});
topicActionRegistry.registerAction({
	id: "topic.save-knowledge",
	commandId: "topic.save-knowledge",
	label: ({ t: t$2 }) => t$2("chat.save.topic.knowledge.menu_title"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Database, { size: 14 }),
	group: "share",
	order: 60,
	surface: "menu"
});
topicActionRegistry.registerAction({
	id: "topic.export",
	label: ({ t: t$2 }) => t$2("chat.topics.export.title"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Upload, { size: 14 }),
	group: "share",
	order: 70,
	surface: "menu",
	availability: (context) => ({ visible: hasExportOption(context) }),
	children: [
		{
			id: "topic.export.image",
			commandId: "topic.export.image",
			label: ({ t: t$2 }) => t$2("chat.topics.export.image"),
			order: 10,
			surface: "menu",
			availability: ({ exportMenuOptions }) => ({ visible: exportMenuOptions.image })
		},
		{
			id: "topic.export.markdown",
			commandId: "topic.export.markdown",
			label: ({ t: t$2 }) => t$2("chat.topics.export.md.label"),
			order: 20,
			surface: "menu",
			availability: ({ exportMenuOptions }) => ({ visible: exportMenuOptions.markdown })
		},
		{
			id: "topic.export.markdown-reason",
			commandId: "topic.export.markdown-reason",
			label: ({ t: t$2 }) => t$2("chat.topics.export.md.reason"),
			order: 30,
			surface: "menu",
			availability: ({ exportMenuOptions }) => ({ visible: exportMenuOptions.markdown_reason })
		},
		{
			id: "topic.export.word",
			commandId: "topic.export.word",
			label: ({ t: t$2 }) => t$2("chat.topics.export.word"),
			order: 40,
			surface: "menu",
			availability: ({ exportMenuOptions }) => ({ visible: exportMenuOptions.docx })
		},
		{
			id: "topic.export.notion",
			commandId: "topic.export.notion",
			label: ({ t: t$2 }) => t$2("chat.topics.export.notion"),
			order: 50,
			surface: "menu",
			availability: ({ exportMenuOptions }) => ({ visible: exportMenuOptions.notion })
		},
		{
			id: "topic.export.yuque",
			commandId: "topic.export.yuque",
			label: ({ t: t$2 }) => t$2("chat.topics.export.yuque"),
			order: 60,
			surface: "menu",
			availability: ({ exportMenuOptions }) => ({ visible: exportMenuOptions.yuque })
		},
		{
			id: "topic.export.obsidian",
			commandId: "topic.export.obsidian",
			label: ({ t: t$2 }) => t$2("chat.topics.export.obsidian"),
			order: 70,
			surface: "menu",
			availability: ({ exportMenuOptions }) => ({ visible: exportMenuOptions.obsidian })
		},
		{
			id: "topic.export.joplin",
			commandId: "topic.export.joplin",
			label: ({ t: t$2 }) => t$2("chat.topics.export.joplin"),
			order: 80,
			surface: "menu",
			availability: ({ exportMenuOptions }) => ({ visible: exportMenuOptions.joplin })
		},
		{
			id: "topic.export.siyuan",
			commandId: "topic.export.siyuan",
			label: ({ t: t$2 }) => t$2("chat.topics.export.siyuan"),
			order: 90,
			surface: "menu",
			availability: ({ exportMenuOptions }) => ({ visible: exportMenuOptions.siyuan })
		}
	]
});
topicActionRegistry.registerAction({
	id: "topic.copy",
	label: ({ t: t$2 }) => t$2("chat.topics.copy.title"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Copy, { size: 14 }),
	group: "share",
	order: 80,
	surface: "menu",
	children: [
		{
			id: "topic.copy.image",
			commandId: "topic.copy.image",
			label: ({ t: t$2 }) => t$2("chat.topics.copy.image"),
			icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Image, { size: 14 }),
			order: 10,
			surface: "menu"
		},
		{
			id: "topic.copy.markdown",
			commandId: "topic.copy.markdown",
			label: ({ t: t$2 }) => t$2("chat.topics.copy.md"),
			icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FileText, { size: 14 }),
			order: 20,
			surface: "menu"
		},
		{
			id: "topic.copy.plain-text",
			commandId: "topic.copy.plain-text",
			label: ({ t: t$2 }) => t$2("chat.topics.copy.plain_text"),
			icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FileText, { size: 14 }),
			order: 30,
			surface: "menu"
		}
	]
});
topicActionRegistry.registerAction({
	id: "topic.delete",
	commandId: "topic.delete",
	label: ({ t: t$2 }) => t$2("common.delete"),
	icon: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trash2, { size: 14 }),
	group: "danger",
	order: 90,
	surface: "menu",
	danger: true,
	availability: ({ topic }) => ({ visible: !topic.pinned }),
	confirm: ({ t: t$2 }) => ({
		title: t$2("chat.topics.manage.delete.confirm.title"),
		description: t$2("chat.topics.manage.delete.confirm.content", { count: 1 }),
		confirmText: t$2("common.delete"),
		cancelText: t$2("common.cancel"),
		destructive: true
	})
});
function resolveTopicMenuActions(context) {
	return topicActionRegistry.resolve(context, "menu");
}
async function executeTopicMenuAction(action, context) {
	const targetAssistantId = getMoveToAssistantTargetId(action.id);
	if (targetAssistantId) {
		if (!context.onMoveToAssistant) return false;
		if (context.topic.assistantId === targetAssistantId) return false;
		if (!context.assistantMoveTargets.some((target) => target.id === targetAssistantId)) return false;
		await context.onMoveToAssistant(context.topic, targetAssistantId);
		return true;
	}
	return topicActionRegistry.execute(action.id, context);
}
const copyTopicAsMarkdown = async (topic) => {
	const markdown = await topicToMarkdown(topic);
	await navigator.clipboard.writeText(markdown);
	toast.success(instance.t("message.copy.success"));
};
const copyTopicAsPlainText = async (topic) => {
	const plainText = await topicToPlainText(topic);
	await navigator.clipboard.writeText(plainText);
	toast.success(instance.t("message.copy.success"));
};
function createTopicActionContext({ exportMenuOptions, isActiveInCurrentTab, isRenaming, notesPath, assistantMoveTargets = [], onAutoRename, onClearMessages, onCopyImage, onDelete, onExportImage, onMoveToAssistant, onOpenInNewTab, onOpenInNewWindow, onPinTopic, onSetPanePosition, onStartRename, panePosition, t: t$2, topic, topicsLength }) {
	return {
		exportMenuOptions,
		isActiveInCurrentTab,
		isRenaming,
		onAutoRename,
		onClearMessages,
		onCopyImage: onCopyImage ?? ((topic$1) => void EventEmitter.emit(EVENT_NAMES.COPY_TOPIC_IMAGE, topic$1)),
		onCopyMarkdown: copyTopicAsMarkdown,
		onCopyPlainText: copyTopicAsPlainText,
		onDelete,
		onExportImage: onExportImage ?? ((topic$1) => void EventEmitter.emit(EVENT_NAMES.EXPORT_TOPIC_IMAGE, topic$1)),
		onExportJoplin: async (topic$1) => {
			const topicMessages = await getTopicMessages(topic$1.id);
			exportMarkdownToJoplin(topic$1.name, topicMessages);
		},
		onExportMarkdown: exportTopicAsMarkdown,
		onExportMarkdownReason: (topic$1) => exportTopicAsMarkdown(topic$1, true),
		onExportNotion: (topic$1) => {
			exportTopicToNotion(topic$1);
		},
		onExportObsidian: (topic$1) => {
			ObsidianExportPopup_default.show({
				title: topic$1.name,
				topic: topic$1,
				processingMethod: "3"
			});
		},
		onExportSiyuan: async (topic$1) => {
			const markdown = await topicToMarkdown(topic$1);
			exportMarkdownToSiyuan(topic$1.name, markdown);
		},
		onExportWord: async (topic$1) => {
			const markdown = await topicToMarkdown(topic$1);
			ipcApi.request("export.word.from_markdown", {
				markdown,
				fileName: removeSpecialCharactersForFileName(topic$1.name)
			});
		},
		onExportYuque: async (topic$1) => {
			const markdown = await topicToMarkdown(topic$1);
			exportMarkdownToYuque(topic$1.name, markdown);
		},
		assistantMoveTargets: assistantMoveTargets.filter((target) => target.id !== topic.assistantId),
		onMoveToAssistant,
		onOpenInNewTab,
		onOpenInNewWindow,
		onPinTopic,
		onSetPanePosition,
		onSaveToKnowledge: async (topic$1) => {
			try {
				const result = await SaveToKnowledgePopup_default.showForTopic(topic$1);
				if (result?.success) toast.success(t$2("chat.save.topic.knowledge.success", { count: result.savedCount }));
			} catch {
				toast.error(t$2("chat.save.topic.knowledge.error.save_failed"));
			}
		},
		onSaveToNotes: (topic$1) => exportTopicToNotes(topic$1, notesPath),
		onStartRename,
		panePosition,
		t: t$2,
		topic,
		topicsLength
	};
}
function getTopicMenuActions(actionContext) {
	return resolveTopicMenuActions(actionContext);
}
async function runTopicMenuAction(action, actionContext) {
	await executeTopicMenuAction(action, actionContext);
}
function useTopicMenuPreset({ getActionContext }) {
	const getActionContextWithOverride = (0, import_react.useCallback)((item, contextOverride) => ({
		...getActionContext(item),
		...contextOverride
	}), [getActionContext]);
	const getActions = (0, import_react.useCallback)((item, contextOverride) => getTopicMenuActions(getActionContextWithOverride(item, contextOverride)), [getActionContextWithOverride]);
	const onAction = (0, import_react.useCallback)(async (item, action, contextOverride) => {
		await runTopicMenuAction(action, getActionContextWithOverride(item, contextOverride));
	}, [getActionContextWithOverride]);
	return (0, import_react.useMemo)(() => ({
		getActions,
		onAction
	}), [getActions, onAction]);
}
function useTopicMenuActions(options) {
	const { exportMenuOptions, isActiveInCurrentTab, isRenaming, notesPath, assistantMoveTargets, onAutoRename, onClearMessages, onCopyImage, onDelete, onExportImage, onMoveToAssistant, onOpenInNewTab, onOpenInNewWindow, onPinTopic, onSetPanePosition, onStartRename, panePosition, t: t$2, topic, topicsLength } = options;
	const actionContext = (0, import_react.useMemo)(() => createTopicActionContext({
		exportMenuOptions,
		isActiveInCurrentTab,
		isRenaming,
		notesPath,
		assistantMoveTargets,
		onAutoRename,
		onClearMessages,
		onCopyImage,
		onDelete,
		onExportImage,
		onMoveToAssistant,
		onOpenInNewTab,
		onOpenInNewWindow,
		onPinTopic,
		onSetPanePosition,
		onStartRename,
		panePosition,
		t: t$2,
		topic,
		topicsLength
	}), [
		exportMenuOptions,
		isActiveInCurrentTab,
		isRenaming,
		notesPath,
		assistantMoveTargets,
		onAutoRename,
		onClearMessages,
		onCopyImage,
		onDelete,
		onExportImage,
		onMoveToAssistant,
		onOpenInNewTab,
		onOpenInNewWindow,
		onPinTopic,
		onSetPanePosition,
		onStartRename,
		panePosition,
		t$2,
		topic,
		topicsLength
	]);
	return {
		actionContext,
		getMenuActions: (0, import_react.useCallback)(() => getTopicMenuActions(actionContext), [actionContext]),
		handleMenuAction: (0, import_react.useCallback)(async (action) => {
			await runTopicMenuAction(action, actionContext);
		}, [actionContext])
	};
}
var TOPIC_TIME_BUCKET_RANK = {
	today: 1,
	yesterday: 2,
	"this-week": 3,
	earlier: 4
};
const TOPIC_PINNED_GROUP_ID = "topic:pinned";
const TOPIC_PINNED_SECTION_ID = "topic:section:pinned";
const TOPIC_ASSISTANT_SECTION_ID = "topic:section:assistant";
const TOPIC_UNLINKED_ASSISTANT_GROUP_ID = "topic:assistant:unknown";
var TOPIC_ASSISTANT_GROUP_ID_PREFIX = "topic:assistant:";
var TOPIC_UNLINKED_ASSISTANT_RANK = Number.MAX_SAFE_INTEGER;
function applyOptimisticTopicDisplayMove(topics, payload, targetAssistantId, groupBy) {
	const activeIndex = topics.findIndex((topic) => topic.id === payload.activeId);
	if (activeIndex < 0) return [...topics];
	const activeTopic = topics[activeIndex];
	const movedTopic = (activeTopic.assistantId ?? null) === targetAssistantId ? activeTopic : {
		...activeTopic,
		assistantId: targetAssistantId ?? void 0
	};
	const next = topics.filter((topic) => topic.id !== payload.activeId);
	let insertIndex = next.length;
	if (payload.overType === "item") {
		const overIndex = next.findIndex((topic) => topic.id === payload.overId);
		if (overIndex >= 0) insertIndex = payload.position === "before" ? overIndex : overIndex + 1;
	} else {
		const lastTargetTopicIndex = next.findLastIndex((topic) => groupBy(topic)?.id === payload.targetGroupId);
		if (lastTargetTopicIndex >= 0) insertIndex = lastTargetTopicIndex + 1;
	}
	next.splice(insertIndex, 0, movedTopic);
	return next;
}
function buildTopicDropAnchor(payload) {
	return buildResourceListItemDropAnchor(payload);
}
function buildAssistantGroupDropAnchor(payload, overAssistantId) {
	return buildResourceListGroupDropAnchor(payload, overAssistantId);
}
function moveAssistantGroupAfterDrop(assistantIds, activeAssistantId, overAssistantId, payload) {
	return moveResourceListStringGroupAfterDrop(assistantIds, activeAssistantId, overAssistantId, payload);
}
function normalizeTopicDropPayload(payload) {
	return payload;
}
function getTopicTimeBucket(updatedAt, now) {
	return getResourceTimeBucket(updatedAt, now);
}
function withTopicGroupIdPrefix(resolver) {
	return withResourceListGroupIdPrefix("topic:", resolver);
}
function getAssistantIdFromTopicGroupId(groupId) {
	if (groupId === "topic:assistant:unknown" || !groupId.startsWith(TOPIC_ASSISTANT_GROUP_ID_PREFIX)) return;
	return groupId.slice(16);
}
function getTopicAssistantGroupId(assistantId) {
	return `${TOPIC_ASSISTANT_GROUP_ID_PREFIX}${assistantId}`;
}
function getTopicAssistantDisplayGroupId(topic) {
	return topic.assistantId ? getTopicAssistantGroupId(topic.assistantId) : TOPIC_UNLINKED_ASSISTANT_GROUP_ID;
}
function createTopicDisplayGroupResolver({ assistantById, labels, mode, now, pinnedAsSection = false }) {
	const pinnedResolver = createPinnedGroupResolver({
		isPinned: (topic) => topic.pinned === true,
		group: {
			id: "pinned",
			label: mode === "time" || !pinnedAsSection ? labels.pinned : ""
		}
	});
	if (mode === "time") return withTopicGroupIdPrefix(composeResourceListGroupResolvers(pinnedResolver, createTimeGroupResolver({
		getTimestamp: (topic) => topic.updatedAt,
		labels: labels.time,
		now
	})));
	return withTopicGroupIdPrefix(composeResourceListGroupResolvers(pinnedResolver, (topic) => {
		const assistantId = topic.assistantId;
		if (!assistantId) return {
			id: "assistant:unknown",
			label: labels.assistant.unlinked
		};
		const assistant = assistantById?.get(assistantId);
		if (assistant) return {
			id: `assistant:${assistant.id}`,
			label: assistant.name
		};
		return {
			id: "assistant:unknown",
			label: labels.assistant.unlinked
		};
	}));
}
function getAssistantGroupRank(topic, assistantRankById) {
	if (topic.pinned === true) return 0;
	const assistantRank = topic.assistantId ? assistantRankById?.get(topic.assistantId) : void 0;
	if (assistantRank !== void 0) return assistantRank + 1;
	return TOPIC_UNLINKED_ASSISTANT_RANK;
}
function readOptionalOrderKey(item) {
	return "orderKey" in item && typeof item.orderKey === "string" ? item.orderKey : void 0;
}
function sortTopicsForDisplayGroups(topics, options) {
	const isPinned = (topic) => topic.pinned === true;
	if (options.mode === "assistant") return sortRankedResourceItems(topics, {
		getRank: (topic) => getAssistantGroupRank(topic, options.assistantRankById),
		isPinned,
		compareWithinGroup: (a$1, b$1) => compareResourceOrderKey(readOptionalOrderKey(a$1), readOptionalOrderKey(b$1))
	});
	return sortRankedResourceItems(topics, {
		getRank: (topic) => topic.pinned === true ? 0 : TOPIC_TIME_BUCKET_RANK[getTopicTimeBucket(topic.updatedAt, options.now)],
		isPinned,
		compareWithinGroup: compareResourceRecency((topic) => topic.updatedAt)
	});
}
var logger$12 = loggerService.withContext("AssistantHistoryRecords");
var AssistantHistoryRecords = ({ activeRecordId, onClose, onRecordSelect, toolbarLeading }) => {
	const { t: t$2 } = useTranslation();
	const [groupNow] = (0, import_react.useState)(() => /* @__PURE__ */ new Date());
	const conversationNav = useConversationNavigation("assistants");
	const { topics: rawTopics, isLoadingAll: isTopicsLoading } = useAssistantTopicsSource();
	const { assistants } = useAssistants();
	const [assistantIconType] = usePreference("assistant.icon_type");
	const [defaultModelId] = usePreference("chat.default_model_id");
	const [renamingTopics] = useCache("topic.renaming");
	const { notesPath } = useNotesSettings();
	const { updateTopic: patchTopic, deleteTopic: deleteTopicById, deleteTopics, batchUpdateTopics } = useTopicMutations();
	const [exportMenuOptions] = useMultiplePreferences({
		docx: "data.export.menus.docx",
		image: "data.export.menus.image",
		joplin: "data.export.menus.joplin",
		markdown: "data.export.menus.markdown",
		markdown_reason: "data.export.menus.markdown_reason",
		notes: "data.export.menus.notes",
		notion: "data.export.menus.notion",
		obsidian: "data.export.menus.obsidian",
		plain_text: "data.export.menus.plain_text",
		siyuan: "data.export.menus.siyuan",
		yuque: "data.export.menus.yuque"
	});
	const { pinnedIds: topicPinnedIds, togglePin: toggleTopicPin } = usePins("topic");
	const topicPinnedIdSet = (0, import_react.useMemo)(() => new Set(topicPinnedIds), [topicPinnedIds]);
	const isTopicPinned = (0, import_react.useCallback)((topicId) => topicPinnedIdSet.has(topicId), [topicPinnedIdSet]);
	const renamingTopicIdSet = (0, import_react.useMemo)(() => new Set(Array.isArray(renamingTopics) ? renamingTopics : []), [renamingTopics]);
	const isTopicRenaming = (0, import_react.useCallback)((topicId) => renamingTopicIdSet.has(topicId), [renamingTopicIdSet]);
	const topics = (0, import_react.useMemo)(() => rawTopics.map((topic) => ({
		...topic,
		assistantId: topic.assistantId,
		pinned: isTopicPinned(topic.id)
	})), [isTopicPinned, rawTopics]);
	const assistantById = (0, import_react.useMemo)(() => new Map(assistants.map((assistant) => [assistant.id, assistant])), [assistants]);
	const assistantRankById = (0, import_react.useMemo)(() => new Map(assistants.map((assistant, index) => [assistant.id, index])), [assistants]);
	const unlinkedAssistantLabel = t$2("history.records.filter.unlinkedAssistant");
	const timeSortedTopics = (0, import_react.useMemo)(() => sortTopicsForDisplayGroups(topics, {
		mode: "time",
		now: groupNow
	}), [groupNow, topics]);
	const assistantSortedTopics = (0, import_react.useMemo)(() => sortTopicsForDisplayGroups(topics, {
		assistantRankById,
		mode: "assistant",
		now: groupNow
	}), [
		assistantRankById,
		groupNow,
		topics
	]);
	const rendererTopicById = (0, import_react.useMemo)(() => new Map(topics.map((topic) => [topic.id, {
		...mapApiTopicToRendererTopic(topic),
		pinned: isTopicPinned(topic.id)
	}])), [isTopicPinned, topics]);
	const getRendererTopic = (0, import_react.useCallback)((topic) => rendererTopicById.get(topic.id) ?? {
		...mapApiTopicToRendererTopic(topic),
		pinned: isTopicPinned(topic.id)
	}, [isTopicPinned, rendererTopicById]);
	const assistantSources = (0, import_react.useMemo)(() => buildAssistantSources(topics, assistantById, assistantRankById, unlinkedAssistantLabel, t$2), [
		assistantById,
		assistantRankById,
		t$2,
		topics,
		unlinkedAssistantLabel
	]);
	const additionalAssistantSourceItems = (0, import_react.useMemo)(() => assistantSources.filter((source) => source.id !== "all" && !assistantById.has(source.id)).map((source) => ({
		id: source.id,
		name: source.label,
		editDisabled: true,
		pinDisabled: true
	})), [assistantById, assistantSources]);
	const bulkMoveTargets = (0, import_react.useMemo)(() => assistants.map((assistant) => ({
		id: assistant.id,
		label: assistant.name || t$2("common.unnamed"),
		icon: assistant.emoji ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			"data-ui": "history.assistant-history-records",
			className: "text-sm leading-none",
			children: assistant.emoji
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bot, { size: 14 })
	})), [assistants, t$2]);
	const handleTopicSelect = (0, import_react.useCallback)((topic) => {
		const title = topic.name || t$2("chat.default.topic.name");
		if (conversationNav.openConversationTab(topic.id, title, { forceNew: true })) return;
		onRecordSelect?.(rendererTopicById.get(topic.id) ?? mapApiTopicToRendererTopic(topic));
		onClose();
	}, [
		conversationNav,
		onClose,
		onRecordSelect,
		rendererTopicById,
		t$2
	]);
	const updateTopic = (0, import_react.useCallback)((topic) => patchTopic(topic.id, {
		name: topic.name,
		isNameManuallyEdited: topic.isNameManuallyEdited
	}), [patchTopic]);
	const handlePinTopic = (0, import_react.useCallback)(async (topic) => {
		try {
			await toggleTopicPin(topic.id);
			return true;
		} catch (err) {
			logger$12.error("Failed to toggle topic pin from history records", {
				topicId: topic.id,
				err
			});
			return false;
		}
	}, [toggleTopicPin]);
	const handleDeleteTopicFromMenu = (0, import_react.useCallback)(async (topic) => {
		if (topic.pinned) return;
		try {
			await deleteTopicById(topic.id);
		} catch (err) {
			logger$12.error("Failed to delete topic from history records", {
				topicId: topic.id,
				err
			});
			const message = err instanceof Error ? err.message : t$2("chat.topics.manage.delete.error");
			toast.error(message);
			return;
		}
		if (topic.id === activeRecordId) {
			const nextTopic = findAdjacentHistoryRecordAfterBulkDelete(timeSortedTopics, [topic.id], topic.id, (candidate) => candidate.id);
			onRecordSelect?.(nextTopic ? getRendererTopic(nextTopic) : null);
		}
	}, [
		activeRecordId,
		deleteTopicById,
		getRendererTopic,
		onRecordSelect,
		t$2,
		timeSortedTopics
	]);
	const handleBulkDeleteTopics = (0, import_react.useCallback)(async (ids) => {
		try {
			return (await deleteTopics(ids)).deletedIds;
		} catch (err) {
			logger$12.error("Failed to bulk delete topics from history records", {
				ids,
				err
			});
			const message = err instanceof Error ? err.message : t$2("chat.topics.manage.delete.error");
			toast.error(message);
			return;
		}
	}, [deleteTopics, t$2]);
	const handleBulkMoveTopics = (0, import_react.useCallback)(async (targetAssistantId, ids) => {
		try {
			const results = await batchUpdateTopics(ids.map((id) => ({
				id,
				dto: { assistantId: targetAssistantId }
			})));
			const movedIds = ids.filter((_$1, index) => results[index]?.status === "fulfilled");
			const failedResults = results.filter((result) => result.status === "rejected");
			if (failedResults.length === 0) {
				toast.success(t$2("history.records.bulkMoveTopics.success", { count: ids.length }));
				return movedIds;
			}
			logger$12.error("Failed to bulk move topics from history records", {
				ids,
				targetAssistantId,
				failedResults
			});
			if (movedIds.length > 0) {
				toast.warning(t$2("history.records.bulkMoveTopics.partialSuccess", {
					failed: failedResults.length,
					moved: movedIds.length,
					total: ids.length
				}));
				return movedIds;
			}
			const firstReason = failedResults[0]?.reason;
			const message = firstReason instanceof Error ? firstReason.message : t$2("history.records.bulkMoveTopics.error");
			toast.error(message);
			return movedIds;
		} catch (err) {
			logger$12.error("Failed to bulk move topics from history records", {
				ids,
				targetAssistantId,
				err
			});
			const message = err instanceof Error ? err.message : t$2("history.records.bulkMoveTopics.error");
			toast.error(message);
			return;
		}
	}, [batchUpdateTopics, t$2]);
	const handleClearMessages = (0, import_react.useCallback)((topic) => {
		EventEmitter.emit(EVENT_NAMES.CLEAR_MESSAGES, topic);
	}, []);
	const handleAutoRename = (0, import_react.useCallback)(async (topic) => {
		const messages = await getTopicMessages(topic.id);
		if (messages.length < 2) return;
		startTopicRenaming(topic.id);
		try {
			const { text: summaryText, error: summaryError } = await fetchMessagesSummary({ messages });
			if (summaryText) updateTopic({
				...topic,
				name: summaryText,
				isNameManuallyEdited: false
			});
			else if (summaryError) toast.error(`${t$2("message.error.fetchTopicName")}: ${summaryError}`);
		} finally {
			finishTopicRenaming(topic.id);
		}
	}, [t$2, updateTopic]);
	const handleRenameTopic = (0, import_react.useCallback)(async (topicId, name) => {
		const topic = rendererTopicById.get(topicId);
		const trimmedName = name.trim();
		if (!topic || !trimmedName || trimmedName === topic.name) return;
		try {
			await updateTopic({
				...topic,
				name: trimmedName,
				isNameManuallyEdited: true
			});
			toast.success(t$2("common.saved"));
		} catch (err) {
			logger$12.error("Failed to rename topic from history records", {
				topicId,
				err
			});
			const message = err instanceof Error ? err.message : t$2("common.save_failed");
			toast.error(message);
		}
	}, [
		rendererTopicById,
		t$2,
		updateTopic
	]);
	const topicMenuPreset = useTopicMenuPreset({ getActionContext: (0, import_react.useCallback)((apiTopic) => {
		const topic = getRendererTopic(apiTopic);
		return createTopicActionContext({
			exportMenuOptions,
			isActiveInCurrentTab: false,
			isRenaming: isTopicRenaming(topic.id),
			onAutoRename: handleAutoRename,
			onClearMessages: handleClearMessages,
			onDelete: handleDeleteTopicFromMenu,
			onPinTopic: async (topic$1) => {
				await handlePinTopic(topic$1);
			},
			onStartRename: () => void 0,
			notesPath,
			t: t$2,
			topic,
			topicsLength: topics.length
		});
	}, [
		exportMenuOptions,
		getRendererTopic,
		handleAutoRename,
		handleClearMessages,
		handleDeleteTopicFromMenu,
		handlePinTopic,
		isTopicRenaming,
		notesPath,
		t$2,
		topics.length
	]) });
	const descriptor = {
		mode: "assistant",
		getId: (0, import_react.useCallback)((topic) => topic.id, []),
		isPinned: isTopicPinned,
		getSourceId: (0, import_react.useCallback)((topic) => getTopicSourceId(topic, assistantById), [assistantById]),
		matchesSearch: (0, import_react.useCallback)((topic, keywords) => (topic.name || t$2("chat.default.topic.name")).toLowerCase().includes(keywords), [t$2]),
		onBulkDelete: handleBulkDeleteTopics,
		onActiveRecordChange: (0, import_react.useCallback)((topic) => onRecordSelect?.(topic ? getRendererTopic(topic) : null), [getRendererTopic, onRecordSelect]),
		...(0, import_react.useMemo)(() => ({
			getName: (topic) => topic.name || t$2("chat.default.topic.name"),
			getUpdatedAt: (topic) => topic.updatedAt,
			getSourceLabel: (topic) => (topic.assistantId ? assistantById.get(topic.assistantId)?.name : void 0) ?? unlinkedAssistantLabel,
			renderAvatar: (topic) => {
				const assistant = topic.assistantId ? assistantById.get(topic.assistantId) : void 0;
				return renderAssistantEntityIcon(assistantIconType, {
					emoji: assistant?.emoji ?? "😀",
					modelId: assistant?.modelId ?? defaultModelId,
					modelName: assistant?.modelName
				}, defaultModelId) ?? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bot, { size: 14 });
			},
			rowHeight: 32,
			getSelectLabel: (topic) => `${t$2("common.select")} ${topic.name || t$2("chat.default.topic.name")}`,
			getRowActions: (topic, openRename) => {
				const contextOverride = { onStartRename: () => openRename(topic.id, topic.name ?? "") };
				return {
					actions: topicMenuPreset.getActions(topic, contextOverride),
					onAction: (action) => topicMenuPreset.onAction(topic, action, contextOverride)
				};
			},
			onOpen: handleTopicSelect,
			onTogglePin: handlePinTopic,
			renderRowMenu: (_topic, row, rowActions) => rowActions.actions.length ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryActionContextMenu, {
				actions: rowActions.actions,
				className: "z-50",
				onAction: rowActions.onAction,
				children: row
			}) : row
		}), [
			assistantById,
			assistantIconType,
			defaultModelId,
			handlePinTopic,
			handleTopicSelect,
			t$2,
			topicMenuPreset,
			unlinkedAssistantLabel
		]),
		sources: assistantSources,
		renderSourceFilter: (selectedId, onSelect) => {
			const source = selectedId ? assistantSources.find((candidate) => candidate.id === selectedId) : void 0;
			const assistant = selectedId ? assistantById.get(selectedId) : void 0;
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistorySourceFilterField, {
				label: selectedId ? source?.label || assistant?.name || t$2("common.unnamed") : t$2("history.records.filter.selectAssistant"),
				hasValue: !!selectedId,
				clearLabel: t$2("common.clear"),
				onClear: () => onSelect(null),
				icon: selectedId ? source?.icon ? source.icon : assistant?.emoji ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					"aria-hidden": true,
					children: assistant.emoji
				}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bot, { size: 14 }) : void 0,
				selector: (trigger) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AssistantSelector, {
					multi: false,
					value: selectedId,
					onChange: onSelect,
					trigger,
					additionalItems: additionalAssistantSourceItems
				})
			});
		},
		bulkMoveTargets,
		onBulkMove: handleBulkMoveTopics,
		onRename: handleRenameTopic,
		strings: {
			sourceLabel: t$2("common.assistant"),
			searchPlaceholder: t$2("history.records.searchTopic"),
			titleColumnLabel: t$2("history.records.table.conversation"),
			emptyTitle: t$2("history.records.empty.title"),
			emptyDescription: t$2("history.records.empty.description"),
			loadingTitle: t$2("history.records.loading.title"),
			loadingDescription: t$2("history.records.loading.description"),
			pinLabel: t$2("chat.topics.pin"),
			unpinLabel: t$2("chat.topics.unpin"),
			deleteLabel: t$2("common.delete"),
			renameDialogTitle: t$2("chat.topics.edit.title")
		}
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HistoryRecordsContent, {
		descriptor,
		controller: useHistoryRecordsController({
			descriptor,
			timeSorted: timeSortedTopics,
			sourceSorted: assistantSortedTopics,
			activeRecordId
		}),
		isLoading: isTopicsLoading,
		toolbarLeading
	});
};
var AssistantHistoryRecords_default = AssistantHistoryRecords;
var HistoryRecordsView = (props) => {
	if (!props.open) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "history.records-view.history-records-view",
		className: "flex min-h-0 flex-1 bg-card [-webkit-app-region:none]",
		"data-testid": "history-records-view",
		children: props.mode === "assistant" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AssistantHistoryRecords_default, {
			activeRecordId: props.activeRecordId,
			onClose: props.onClose,
			onRecordSelect: props.onRecordSelect,
			toolbarLeading: props.toolbarLeading
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AgentHistoryRecords_default, {
			activeRecordId: props.activeRecordId,
			onClose: props.onClose,
			onRecordSelect: props.onRecordSelect,
			toolbarLeading: props.toolbarLeading
		})
	});
};
var HistoryRecordsView_default = HistoryRecordsView;
function ConversationResourceView({ className, kind, onOpenAssistantChat, toolbarLeading }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceCatalogView, {
		className: cn("bg-background", className),
		onOpenAssistantChat,
		resourceType: kind,
		toolbarLeading
	});
}
var RIGHT_PANE_OPEN_OVERRIDE_CACHE_KEY = {
	chat: "ui.chat.right_pane_open_override",
	agent: "ui.agent.right_pane_open_override"
};
function useClassicLayoutRightPaneOpen(surface, { enabled, defaultOpen }) {
	const [storedOverride, setStoredOverride] = usePersistCache(RIGHT_PANE_OPEN_OVERRIDE_CACHE_KEY[surface]);
	return [enabled && (storedOverride ?? defaultOpen), (0, import_react.useCallback)((open, options) => {
		if (enabled || options?.force) setStoredOverride(open);
	}, [enabled, setStoredOverride])];
}
function useConversationCenterSurface({ conversationKey, disabled = false, resourceDefinitions }) {
	const [active, setActive] = (0, import_react.useState)(null);
	const activeResourceExists = active?.type === "resource" ? resourceDefinitions.some((definition) => definition.kind === active.kind) : active?.type === "history";
	const activeSurface = !disabled && active?.conversationKey === conversationKey && activeResourceExists ? active : null;
	const activeResourceKind = activeSurface?.type === "resource" ? activeSurface.kind : null;
	const historyActive = activeSurface?.type === "history";
	const closeSurface = (0, import_react.useCallback)(() => {
		setActive(null);
	}, []);
	const toggleResource = (0, import_react.useCallback)((kind) => {
		if (disabled) {
			setActive(null);
			return;
		}
		setActive((current) => current?.conversationKey === conversationKey && current.type === "resource" && current.kind === kind ? null : {
			conversationKey,
			kind,
			type: "resource"
		});
	}, [conversationKey, disabled]);
	const toggleHistory = (0, import_react.useCallback)(() => {
		if (disabled) {
			setActive(null);
			return;
		}
		setActive((current) => current?.conversationKey === conversationKey && current.type === "history" ? null : {
			conversationKey,
			type: "history"
		});
	}, [conversationKey, disabled]);
	(0, import_react.useEffect)(() => {
		if (!active) return;
		if (!disabled && active.conversationKey === conversationKey && (active.type === "history" || resourceDefinitions.some((definition) => definition.kind === active.kind))) return;
		setActive(null);
	}, [
		active,
		conversationKey,
		disabled,
		resourceDefinitions
	]);
	return {
		activeResourceKind,
		closeSurface,
		historyActive,
		resourceMenuItems: (0, import_react.useMemo)(() => {
			if (disabled || resourceDefinitions.length === 0) return void 0;
			return resourceDefinitions.map((definition) => ({
				active: activeResourceKind === definition.kind,
				icon: definition.icon,
				id: definition.id,
				label: definition.label,
				onSelect: () => toggleResource(definition.kind)
			}));
		}, [
			activeResourceKind,
			disabled,
			resourceDefinitions,
			toggleResource
		]),
		toggleHistory
	};
}
function useConversationShellPaneState({ isMessageOnlyView, persistedPaneOpen, setPersistedPaneOpen, onManualPaneOpen }) {
	const isWindowFrame = useWindowFrame().mode === "window";
	const [detachedPaneOpen, setDetachedPaneOpen] = (0, import_react.useState)(false);
	const [autoCollapsedPane, setAutoCollapsedPane] = (0, import_react.useState)(false);
	const [paneManualToggle, setPaneManualToggle] = (0, import_react.useState)();
	const shellPaneOpen = !isMessageOnlyView && (isWindowFrame ? detachedPaneOpen : persistedPaneOpen) && !autoCollapsedPane;
	const setShellPaneOpen = (0, import_react.useCallback)((open) => {
		setAutoCollapsedPane(false);
		if (isWindowFrame) {
			setDetachedPaneOpen(open);
			return;
		}
		setPersistedPaneOpen(open);
	}, [isWindowFrame, setPersistedPaneOpen]);
	const setShellPaneOpenManually = (0, import_react.useCallback)((open) => {
		setPaneManualToggle((previous) => ({
			seq: (previous?.seq ?? 0) + 1,
			open
		}));
		setShellPaneOpen(open);
	}, [setShellPaneOpen]);
	return {
		isWindowFrame,
		shellPaneOpen,
		paneManualToggle,
		setShellPaneOpen,
		setShellPaneOpenManually,
		toggleShellPane: (0, import_react.useCallback)(() => {
			if (isMessageOnlyView) return;
			const nextOpen = !shellPaneOpen;
			setShellPaneOpenManually(nextOpen);
			if (nextOpen) onManualPaneOpen?.();
		}, [
			isMessageOnlyView,
			onManualPaneOpen,
			setShellPaneOpenManually,
			shellPaneOpen
		]),
		handlePaneAutoCollapseChange: (0, import_react.useCallback)((collapsed) => {
			setAutoCollapsedPane(collapsed);
		}, [])
	};
}
var UNTOUCHED_SINCE_CREATION_TOLERANCE_MS = 1;
function isUntouchedSinceCreation(item) {
	if (item.createdAt === void 0 || item.updatedAt === void 0) return false;
	const createdAtMs = Date.parse(item.createdAt);
	const updatedAtMs = Date.parse(item.updatedAt);
	if (!Number.isFinite(createdAtMs) || !Number.isFinite(updatedAtMs)) return item.updatedAt === item.createdAt;
	return Math.abs(updatedAtMs - createdAtMs) <= UNTOUCHED_SINCE_CREATION_TOLERANCE_MS;
}
function pickNeighbourAfterRemoval(orderedList, id) {
	if (orderedList.length <= 1) return void 0;
	const index = orderedList.findIndex((item) => item.id === id);
	if (index === -1) return void 0;
	return orderedList[index + 1 === orderedList.length ? index - 1 : index + 1];
}
function findLatestUpdated(items) {
	let latest;
	let latestUpdatedAtMs = Number.NEGATIVE_INFINITY;
	for (const item of items) {
		const parsedUpdatedAtMs = item.updatedAt ? Date.parse(item.updatedAt) : Number.NEGATIVE_INFINITY;
		const updatedAtMs = Number.isFinite(parsedUpdatedAtMs) ? parsedUpdatedAtMs : Number.NEGATIVE_INFINITY;
		if (!latest || updatedAtMs > latestUpdatedAtMs) {
			latest = item;
			latestUpdatedAtMs = updatedAtMs;
		}
	}
	return latest;
}
const SECOND_MIN_WINDOW_WIDTH = 520;
const MIN_WINDOW_HEIGHT = 600;
const CitationsPanelContent = ({ citations, actions }) => {
	const previewSession = useCitationPreviewSession();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(scrollbar_default, {
		className: "min-h-0 flex-1",
		children: citations.map((citation) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "border-border border-b-[0.5px] last:border-b-0",
			children: citation.type === "websearch" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "max-w-[min(400px,60vw)] px-3",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WebCitationCard, {
					citation,
					previewSession,
					actions
				})
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "max-w-150 px-3",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(KnowledgeCitationCard, {
					citation,
					actions
				})
			})
		}, `${citation.number}-${citation.url || citation.title}`))
	});
};
var CitationsPanel = ({ open, onClose, citations }) => {
	const { t: t$2 } = useTranslation();
	const openPath = (0, import_react.useCallback)((path) => window.api.file.openPath(path), []);
	const openExternalUrl = (0, import_react.useCallback)((url) => {
		window.open(url, "_blank", "noopener,noreferrer");
	}, []);
	const { copyText, notifyError } = useMessagePlatformActions();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageSidePanel, {
		open,
		onClose,
		header: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: "font-medium text-sm",
			children: t$2("message.citations")
		}),
		closeLabel: t$2("common.close"),
		bodyClassName: "flex min-h-0 flex-col space-y-0 overflow-hidden p-0 pb-2",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CitationsPanelContent, {
			citations,
			actions: {
				openPath,
				openExternalUrl,
				copyText,
				notifyError
			}
		})
	});
};
var CitationsPanel_default = CitationsPanel;
var ChatLayoutModeContext = (0, import_react.createContext)({
	forceWideLayout: false,
	setForceWideLayout: () => {},
	railGutterPx: 0,
	setRailGutterPx: () => {}
});
const ChatLayoutModeProvider = ({ children }) => {
	const [forceWideLayout, setForceWideLayout] = (0, import_react.useState)(false);
	const [railGutterPx, setRailGutterPx] = (0, import_react.useState)(0);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChatLayoutModeContext, {
		value: (0, import_react.useMemo)(() => ({
			forceWideLayout,
			setForceWideLayout,
			railGutterPx,
			setRailGutterPx
		}), [forceWideLayout, railGutterPx]),
		children
	});
};
const useChatLayoutMode = () => (0, import_react.use)(ChatLayoutModeContext);
const CHAT_SHELL_PANE_WIDTH = "var(--assistants-width)";
const CHAT_SHELL_TRANSITION = {
	duration: .3,
	ease: "easeInOut"
};
const RESOURCE_LIST_PANE_CACHE_KEY = "ui.chat.sidebar.width";
const ARTIFACT_RIGHT_PANE_CACHE_KEY = "ui.chat.artifact_pane.width";
var PANE_MIN = 255;
var CENTER_MIN = 360;
var PROPORTIONAL_TOTAL = PANE_MIN + 200;
function resolveDockedPaneWidth(available, resolvedWidth) {
	if (available <= 0) return 0;
	return Math.max(Math.min(resolvedWidth, available - CENTER_MIN), Math.min(PANE_MIN, available * PANE_MIN / PROPORTIONAL_TOTAL));
}
function buildDockedPaneWidthExpression(resolvedWidth) {
	return `max(min(${typeof resolvedWidth === "number" ? `${resolvedWidth}px` : resolvedWidth}, calc(100% - ${CENTER_MIN}px)), min(${PANE_MIN}px, calc(100% * ${PANE_MIN} / ${PROPORTIONAL_TOTAL})))`;
}
function getPaneSpaceCap(available) {
	return Math.max(PANE_MIN, available - CENTER_MIN);
}
function predictCenterWidth({ shellWidth, listWidth, paneOpen, paneWidth }) {
	const available = shellWidth - listWidth;
	return available - (paneOpen ? resolveDockedPaneWidth(available, paneWidth) : 0);
}
var RESTORE_HYSTERESIS = 4;
function evaluateAutoCollapse(predictedCenter, currentlyCollapsed) {
	if (predictedCenter < CENTER_MIN) return true;
	if (predictedCenter >= CENTER_MIN + RESTORE_HYSTERESIS) return false;
	return currentlyCollapsed;
}
const RIGHT_PANE_CLIP_COLLAPSED = "inset(0% 0% 0% 100%)";
const RIGHT_PANE_CLIP_REVEALED = "inset(0% 0% 0% 0%)";
function getRightPaneDockedClip(width) {
	return `inset(0% 0% 0% calc(100% - ${typeof width === "number" ? `${width}px` : width}))`;
}
function isClosedRightPanePhase(phase) {
	return phase === "closed";
}
function isFullWidthRightPanePhase(phase) {
	return phase === "maximizing" || phase === "maximized" || phase === "minimizing" || phase === "closing-maximized";
}
function getInitialPersistentRightPaneState(targetMode) {
	if (targetMode === "docked") return {
		phase: "docked",
		reservesDockedSpace: true
	};
	if (targetMode === "maximized") return {
		phase: "maximized",
		reservesDockedSpace: false
	};
	return {
		phase: "closed",
		reservesDockedSpace: false
	};
}
function getSettledRightPaneMode(phase) {
	if (phase === "closed" || phase === "docked" || phase === "maximized") return phase;
	return null;
}
function getPersistentRightPaneMotionState(targetMode) {
	return targetMode === "closed" ? {
		clipPath: RIGHT_PANE_CLIP_COLLAPSED,
		opacity: 0
	} : {
		clipPath: RIGHT_PANE_CLIP_REVEALED,
		opacity: 1
	};
}
function planPersistentRightPaneReconnect(currentPhase, targetMode) {
	return {
		completedMode: getSettledRightPaneMode(currentPhase) === targetMode ? void 0 : targetMode,
		motionState: getPersistentRightPaneMotionState(targetMode),
		settledState: getInitialPersistentRightPaneState(targetMode)
	};
}
function planPersistentRightPaneTransition(currentPhase, targetMode, { dockedClip, reduceMotion }) {
	if (targetMode === "closed" && isClosedRightPanePhase(currentPhase) || targetMode === "docked" && currentPhase === "docked" || targetMode === "maximized" && currentPhase === "maximized") return null;
	const transition = reduceMotion ? { duration: 0 } : CHAT_SHELL_TRANSITION;
	if (targetMode === "closed") {
		const closingFromMaximized = isFullWidthRightPanePhase(currentPhase);
		return {
			animateTo: {
				clipPath: RIGHT_PANE_CLIP_COLLAPSED,
				opacity: 0,
				transition
			},
			completedMode: "closed",
			deferUntilNextFrame: false,
			runningState: {
				phase: closingFromMaximized ? "closing-maximized" : "closing-docked",
				reservesDockedSpace: false
			},
			settledState: {
				phase: "closed",
				reservesDockedSpace: false
			}
		};
	}
	if (targetMode === "docked") {
		if (isFullWidthRightPanePhase(currentPhase) && !isClosedRightPanePhase(currentPhase)) return {
			animateTo: {
				clipPath: dockedClip,
				opacity: 1,
				transition
			},
			completedMode: "docked",
			deferUntilNextFrame: false,
			runningState: {
				phase: "minimizing",
				reservesDockedSpace: true
			},
			settledState: {
				phase: "docked",
				reservesDockedSpace: true
			}
		};
		return {
			animateTo: {
				clipPath: RIGHT_PANE_CLIP_REVEALED,
				opacity: 1,
				transition
			},
			completedMode: "docked",
			deferUntilNextFrame: false,
			runningState: {
				phase: "opening-docked",
				reservesDockedSpace: true
			},
			settledState: {
				phase: "docked",
				reservesDockedSpace: true
			}
		};
	}
	const resetBeforeReveal = !isFullWidthRightPanePhase(currentPhase) || isClosedRightPanePhase(currentPhase);
	const setBeforeStart = resetBeforeReveal ? {
		clipPath: isClosedRightPanePhase(currentPhase) ? RIGHT_PANE_CLIP_COLLAPSED : dockedClip,
		opacity: 1
	} : void 0;
	return {
		animateTo: {
			clipPath: RIGHT_PANE_CLIP_REVEALED,
			opacity: 1,
			transition
		},
		completedMode: "maximized",
		deferUntilNextFrame: resetBeforeReveal,
		runningState: {
			phase: "maximizing",
			reservesDockedSpace: !isClosedRightPanePhase(currentPhase)
		},
		setBeforeStart,
		settledState: {
			phase: "maximized",
			reservesDockedSpace: false
		}
	};
}
var SPLITTER_KEYBOARD_STEP = 16;
function getVerticalSplitterProps({ width, min, max, label, onResize, invert = false }) {
	const grow = invert ? "ArrowLeft" : "ArrowRight";
	const shrink = invert ? "ArrowRight" : "ArrowLeft";
	return {
		role: "separator",
		"aria-orientation": "vertical",
		"aria-valuemin": min,
		"aria-valuemax": max,
		"aria-valuenow": width,
		"aria-label": label,
		tabIndex: 0,
		onKeyDown: (event) => {
			switch (event.key) {
				case grow:
					event.preventDefault();
					onResize(width + SPLITTER_KEYBOARD_STEP);
					break;
				case shrink:
					event.preventDefault();
					onResize(width - SPLITTER_KEYBOARD_STEP);
					break;
				case "Home":
					event.preventDefault();
					onResize(min);
					break;
				case "End":
					event.preventDefault();
					onResize(max);
					break;
			}
		}
	};
}
function clampRightPaneWidth(width, minWidth, maxWidth) {
	return Math.min(maxWidth, Math.max(minWidth, Math.round(width)));
}
function useMainRegionWidth(paneRef) {
	const [width, setWidth] = (0, import_react.useState)(null);
	(0, import_react.useLayoutEffect)(() => {
		const pane = paneRef.current;
		if (!pane || typeof ResizeObserver === "undefined") return;
		const region = pane.offsetParent;
		if (!(region instanceof HTMLElement)) return;
		const update = () => {
			const next = region.getBoundingClientRect().width;
			setWidth(next > 0 ? next : null);
		};
		update();
		const observer = new ResizeObserver(update);
		observer.observe(region);
		return () => observer.disconnect();
	}, [paneRef]);
	return width;
}
function useRightPaneResize({ cacheKey, defaultWidth, minWidth, maxWidth, spaceCapRef, onDragClose }) {
	const [storedWidth, setStoredWidth] = usePersistCache(cacheKey);
	const paneRef = (0, import_react.useRef)(null);
	const paneRightRef = (0, import_react.useRef)(0);
	const pendingDragCloseRef = (0, import_react.useRef)(false);
	const onDragCloseRef = (0, import_react.useRef)(onDragClose);
	(0, import_react.useEffect)(() => {
		onDragCloseRef.current = onDragClose;
	}, [onDragClose]);
	const [liveWidth, setLiveWidth] = (0, import_react.useState)(null);
	const pendingWidthRef = (0, import_react.useRef)(null);
	const rafScheduledRef = (0, import_react.useRef)(false);
	const rafIdRef = (0, import_react.useRef)(null);
	const paneWidth = clampRightPaneWidth(liveWidth ?? storedWidth ?? defaultWidth, minWidth, maxWidth);
	const cancelPendingRaf = (0, import_react.useCallback)(() => {
		if (rafIdRef.current !== null) {
			cancelAnimationFrame(rafIdRef.current);
			rafIdRef.current = null;
		}
		rafScheduledRef.current = false;
	}, []);
	const handleMouseMove = (0, import_react.useCallback)((moveEvent, stop) => {
		const geometricWidth = paneRightRef.current - moveEvent.clientX;
		if (geometricWidth < minWidth - 80) {
			pendingWidthRef.current = null;
			pendingDragCloseRef.current = true;
			stop();
			return;
		}
		pendingWidthRef.current = clampRightPaneWidth(geometricWidth, minWidth, maxWidth);
		if (rafScheduledRef.current) return;
		rafScheduledRef.current = true;
		rafIdRef.current = requestAnimationFrame(() => {
			rafScheduledRef.current = false;
			rafIdRef.current = null;
			setLiveWidth(pendingWidthRef.current);
		});
	}, [maxWidth, minWidth]);
	const commitWidth = (0, import_react.useCallback)((requested) => {
		const cap = spaceCapRef?.current ?? null;
		if (cap === null) {
			setStoredWidth(clampRightPaneWidth(requested, minWidth, maxWidth));
			return;
		}
		const currentStored = clampRightPaneWidth(storedWidth ?? defaultWidth, minWidth, maxWidth);
		const currentEffective = clampRightPaneWidth(Math.min(currentStored, cap), minWidth, maxWidth);
		const nextEffective = clampRightPaneWidth(Math.min(requested, cap), minWidth, maxWidth);
		if (nextEffective !== currentEffective) setStoredWidth(nextEffective);
	}, [
		defaultWidth,
		maxWidth,
		minWidth,
		setStoredWidth,
		spaceCapRef,
		storedWidth
	]);
	const { isResizing, startResizing: startResizeDrag } = useResizeDrag({
		onMove: handleMouseMove,
		onEnd: (0, import_react.useCallback)(() => {
			cancelPendingRaf();
			if (pendingWidthRef.current !== null) {
				commitWidth(pendingWidthRef.current);
				pendingWidthRef.current = null;
			}
			setLiveWidth(null);
		}, [cancelPendingRaf, commitWidth])
	});
	(0, import_react.useEffect)(() => cancelPendingRaf, [cancelPendingRaf]);
	(0, import_react.useEffect)(() => {
		if (isResizing || !pendingDragCloseRef.current) return;
		pendingDragCloseRef.current = false;
		onDragCloseRef.current?.();
	}, [isResizing]);
	return {
		isResizing,
		paneRef,
		paneWidth,
		startResizing: (0, import_react.useCallback)((event) => {
			paneRightRef.current = paneRef.current?.getBoundingClientRect().right ?? event.clientX + paneWidth;
			startResizeDrag(event);
		}, [paneWidth, startResizeDrag]),
		setPaneWidth: (0, import_react.useCallback)((nextWidth) => commitWidth(nextWidth), [commitWidth])
	};
}
function RightPaneContents({ children, paneWidth, minWidth, maxWidth, resizeHandleVisible, startResizing, setPaneWidth }) {
	const { t: t$2 } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.right-pane-contents",
		className: "h-full min-h-0 group-data-[resizing=true]/right-pane:pointer-events-none",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, { children })
	}), resizeHandleVisible && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.right-pane-contents",
		"data-right-pane-resize-handle": true,
		onMouseDown: startResizing,
		...mergeUiProps(getVerticalSplitterProps({
			width: paneWidth,
			min: minWidth,
			max: maxWidth,
			label: t$2("common.resize_panel"),
			onResize: setPaneWidth,
			invert: true
		}), "chat.right-pane-contents"),
		className: "group/right-pane-resize-handle absolute top-0 bottom-0 left-0 z-30 w-2 cursor-col-resize focus-visible:bg-primary/40 focus-visible:outline-none",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "absolute top-0 left-0 h-full w-0.5 bg-primary/20 opacity-0 transition-opacity group-hover/right-pane-resize-handle:opacity-100 group-data-[resizing=true]/right-pane:bg-primary/35 group-data-[resizing=true]/right-pane:opacity-100" })
	})] });
}
function RightPaneHost({ children, open, width = CHAT_SHELL_PANE_WIDTH, className, style }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AnimatePresence, {
		initial: false,
		children: Boolean(open && children !== null && children !== void 0) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(motion.div, {
			"data-ui": "chat.right-pane-host.conversation-navigation part:conversation-navigation",
			initial: {
				width: 0,
				opacity: 0
			},
			animate: {
				width,
				opacity: 1
			},
			exit: {
				width: 0,
				opacity: 0
			},
			transition: CHAT_SHELL_TRANSITION,
			"data-right-pane": true,
			className: cn("h-full min-h-0 shrink-0 overflow-hidden", className),
			style,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, { children })
		}, "right-pane")
	});
}
function PersistentRightPaneHost({ children, open, maximized = false, width = CHAT_SHELL_PANE_WIDTH, className, style, resizable = false, minWidth = 255, defaultWidth, maxWidth = 720, cacheKey = ARTIFACT_RIGHT_PANE_CACHE_KEY, onLayoutAnimationComplete, onFullWidthPhaseChange, onResizingChange, onDragClose }) {
	const reduceMotion = useReducedMotion();
	const animationControls$1 = useAnimationControls();
	const resolvedDefaultWidth = defaultWidth ?? (typeof width === "number" ? width : 280);
	const spaceCapRef = (0, import_react.useRef)(null);
	const { isResizing, paneRef, paneWidth, startResizing, setPaneWidth } = useRightPaneResize({
		cacheKey,
		defaultWidth: resolvedDefaultWidth,
		minWidth,
		maxWidth,
		spaceCapRef,
		onDragClose
	});
	const mainRegionWidth = useMainRegionWidth(paneRef);
	(0, import_react.useLayoutEffect)(() => {
		spaceCapRef.current = mainRegionWidth === null ? null : getPaneSpaceCap(mainRegionWidth);
	}, [mainRegionWidth]);
	const resolvedWidth = resizable ? paneWidth : width;
	const dockedWidthExpression = buildDockedPaneWidthExpression(resolvedWidth);
	const effectiveWidth = mainRegionWidth === null || typeof resolvedWidth !== "number" ? paneWidth : Math.round(resolveDockedPaneWidth(mainRegionWidth, resolvedWidth));
	const splitterMinWidth = mainRegionWidth === null ? minWidth : Math.round(resolveDockedPaneWidth(mainRegionWidth, minWidth));
	const splitterMaxWidth = mainRegionWidth === null ? maxWidth : Math.round(resolveDockedPaneWidth(mainRegionWidth, maxWidth));
	const dockedClip = getRightPaneDockedClip(dockedWidthExpression);
	const targetMode = !open || !(children !== null && children !== void 0) ? "closed" : maximized ? "maximized" : "docked";
	const [visualState, setVisualStateState] = (0, import_react.useState)(() => getInitialPersistentRightPaneState(targetMode));
	const visualStateRef = (0, import_react.useRef)(visualState);
	const { phase, reservesDockedSpace } = visualState;
	const previousTargetModeRef = (0, import_react.useRef)(targetMode);
	const transitionTokenRef = (0, import_react.useRef)(0);
	const scheduledAnimationFrameRef = (0, import_react.useRef)(null);
	const effectsConnectedRef = (0, import_react.useRef)(false);
	const [initialAnimationState] = (0, import_react.useState)(() => ({
		clipPath: targetMode === "closed" ? RIGHT_PANE_CLIP_COLLAPSED : RIGHT_PANE_CLIP_REVEALED,
		opacity: targetMode === "closed" ? 0 : 1
	}));
	const onLayoutAnimationCompleteRef = (0, import_react.useRef)(onLayoutAnimationComplete);
	const setVisualState = (0, import_react.useCallback)((nextState) => {
		visualStateRef.current = nextState;
		setVisualStateState(nextState);
	}, []);
	const invalidateActiveTransition = (0, import_react.useCallback)(() => {
		transitionTokenRef.current += 1;
		if (scheduledAnimationFrameRef.current !== null) {
			cancelAnimationFrame(scheduledAnimationFrameRef.current);
			scheduledAnimationFrameRef.current = null;
		}
		animationControls$1.stop();
	}, [animationControls$1]);
	(0, import_react.useLayoutEffect)(() => {
		onLayoutAnimationCompleteRef.current = onLayoutAnimationComplete;
	}, [onLayoutAnimationComplete]);
	const reconcileAfterEffectsReconnect = (0, import_react.useEffectEvent)(() => {
		const plan = planPersistentRightPaneReconnect(visualStateRef.current.phase, targetMode);
		previousTargetModeRef.current = targetMode;
		if (!plan.completedMode) return;
		setVisualState(plan.settledState);
		onLayoutAnimationCompleteRef.current?.(plan.completedMode);
	});
	(0, import_react.useLayoutEffect)(() => {
		if (effectsConnectedRef.current) reconcileAfterEffectsReconnect();
		else effectsConnectedRef.current = true;
		return invalidateActiveTransition;
	}, [invalidateActiveTransition]);
	(0, import_react.useLayoutEffect)(() => {
		if (previousTargetModeRef.current === targetMode) return;
		previousTargetModeRef.current = targetMode;
		invalidateActiveTransition();
		const token = transitionTokenRef.current;
		const plan = planPersistentRightPaneTransition(visualStateRef.current.phase, targetMode, {
			dockedClip,
			reduceMotion: Boolean(reduceMotion)
		});
		if (!plan) return;
		const complete = () => {
			if (transitionTokenRef.current !== token) return;
			setVisualState(plan.settledState);
			onLayoutAnimationCompleteRef.current?.(plan.completedMode);
		};
		const start = (definition, onComplete, deferUntilNextFrame = false) => {
			const run = () => {
				scheduledAnimationFrameRef.current = null;
				if (transitionTokenRef.current !== token) return;
				animationControls$1.start(definition).then(onComplete);
			};
			if (deferUntilNextFrame && !reduceMotion && typeof requestAnimationFrame !== "undefined") scheduledAnimationFrameRef.current = requestAnimationFrame(run);
			else run();
		};
		if (targetMode === "closed") {
			const activeElement = typeof document === "undefined" ? null : document.activeElement;
			if (activeElement && typeof HTMLElement !== "undefined" && activeElement instanceof HTMLElement && paneRef.current?.contains(activeElement)) activeElement.blur();
		}
		if (plan.setBeforeStart) animationControls$1.set(plan.setBeforeStart);
		setVisualState(plan.runningState);
		start(plan.animateTo, complete, plan.deferUntilNextFrame);
	}, [
		animationControls$1,
		dockedClip,
		invalidateActiveTransition,
		paneRef,
		reduceMotion,
		setVisualState,
		targetMode
	]);
	(0, import_react.useLayoutEffect)(() => {
		const plan = planPersistentRightPaneReconnect(phase, targetMode);
		if (plan.completedMode) return;
		animationControls$1.set(plan.motionState);
	}, [
		animationControls$1,
		phase,
		targetMode
	]);
	const isDocked = phase === "docked" && targetMode === "docked";
	const fullWidthLayout = isFullWidthRightPanePhase(phase);
	const closed = isClosedRightPanePhase(phase);
	const interactionHidden = targetMode === "closed";
	const spacerTransition = isResizing || fullWidthLayout ? { duration: 0 } : CHAT_SHELL_TRANSITION;
	const onFullWidthPhaseChangeRef = (0, import_react.useRef)(onFullWidthPhaseChange);
	const onResizingChangeRef = (0, import_react.useRef)(onResizingChange);
	(0, import_react.useLayoutEffect)(() => {
		onFullWidthPhaseChangeRef.current = onFullWidthPhaseChange;
		onResizingChangeRef.current = onResizingChange;
	}, [onFullWidthPhaseChange, onResizingChange]);
	(0, import_react.useLayoutEffect)(() => {
		onFullWidthPhaseChangeRef.current?.(fullWidthLayout);
	}, [fullWidthLayout]);
	(0, import_react.useLayoutEffect)(() => {
		onResizingChangeRef.current?.(isResizing);
	}, [isResizing]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(motion.div, {
		"data-ui": "chat.persistent-right-pane-host",
		"aria-hidden": "true",
		"data-right-pane-spacer": true,
		animate: { width: reservesDockedSpace ? resolvedWidth : 0 },
		transition: spacerTransition,
		className: "h-full min-h-0 shrink-0",
		style: { maxWidth: dockedWidthExpression }
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(motion.div, {
		"data-ui": "chat.persistent-right-pane-host.conversation-inspector part:conversation-inspector",
		ref: paneRef,
		initial: initialAnimationState,
		animate: animationControls$1,
		inert: interactionHidden,
		"aria-hidden": interactionHidden || void 0,
		"data-right-pane": true,
		"data-right-pane-mode": targetMode,
		"data-right-pane-phase": phase,
		"data-resizing": isResizing || void 0,
		"data-shell-maximized-overlay": fullWidthLayout ? "" : void 0,
		className: cn("group/right-pane pointer-events-none absolute top-0 right-0 bottom-0 z-40 h-full min-h-0 overflow-hidden", className),
		style: {
			...style,
			width: fullWidthLayout ? "100%" : resolvedWidth,
			maxWidth: fullWidthLayout ? void 0 : dockedWidthExpression,
			visibility: closed ? "hidden" : void 0
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-shell-maximized-overlay-content": fullWidthLayout ? "" : void 0,
			className: cn("relative h-full min-h-0 overflow-hidden", !interactionHidden && "pointer-events-auto", fullWidthLayout && "bg-background", resizable && !fullWidthLayout && "[border-left:0.5px_solid_var(--border)]"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPaneContents, {
				paneWidth: effectiveWidth,
				minWidth: splitterMinWidth,
				maxWidth: splitterMaxWidth,
				resizeHandleVisible: resizable && isDocked,
				startResizing,
				setPaneWidth,
				children
			})
		})
	})] });
}
var RightPanelRenderContext = (0, import_react.createContext)(null);
var RightPanelStateContext = (0, import_react.createContext)(null);
var RightPanelActionsContext = (0, import_react.createContext)(null);
function resolveRightPanelEntries(capabilities, scope) {
	const entries = [];
	const panelIds = /* @__PURE__ */ new Set();
	for (const capability of capabilities) {
		const instance$1 = capability.resolve(scope);
		if (!instance$1) continue;
		if (panelIds.has(instance$1.id)) throw new Error(`Duplicate right-panel id: ${instance$1.id}`);
		panelIds.add(instance$1.id);
		entries.push({
			...instance$1,
			component: capability.component
		});
	}
	return entries;
}
function findEntry(entries, panelId) {
	return panelId ? entries.find((entry) => entry.id === panelId) : void 0;
}
function getDefaultEntry(entries) {
	return entries.find((entry) => entry.readiness === "ready") ?? entries.find((entry) => entry.readiness === "pending") ?? entries[0];
}
function updateMountedInstances(current, entries, activeEntry, presentationOpen) {
	const currentEntries = new Map(entries.map((entry) => [entry.id, entry]));
	const next = /* @__PURE__ */ new Map();
	let changed = false;
	for (const [panelId, instanceKey] of current) {
		const entry = currentEntries.get(panelId);
		if (entry && entry.instanceKey === instanceKey && entry.readiness !== "unavailable") next.set(panelId, instanceKey);
		else changed = true;
	}
	if (presentationOpen && activeEntry && next.get(activeEntry.id) !== activeEntry.instanceKey) {
		next.set(activeEntry.id, activeEntry.instanceKey);
		changed = true;
	}
	return changed ? next : current;
}
function RightPanelProvider({ capabilities, children, defaultOpen = false, defaultPanelId, onOpenChange, present = true, scope, userOpenIntentSeq = 0 }) {
	const entries = (0, import_react.useMemo)(() => resolveRightPanelEntries(capabilities, scope), [capabilities, scope]);
	const [open, setOpen] = (0, import_react.useState)(defaultOpen);
	const [maximized, setMaximized] = (0, import_react.useState)(false);
	const [requestedPanelId, setRequestedPanelId] = (0, import_react.useState)(defaultPanelId);
	const [layoutAnimationPending, setLayoutAnimationPending] = (0, import_react.useState)(false);
	const [fullWidthActive, setFullWidthActive] = (0, import_react.useState)(false);
	const [paneResizing, setPaneResizing] = (0, import_react.useState)(false);
	const [userOpenSeq, setUserOpenSeq] = (0, import_react.useState)(0);
	const [pdfLayoutPending, setPdfLayoutPending] = (0, import_react.useState)(false);
	const [pdfLayoutRefreshKey, setPdfLayoutRefreshKey] = (0, import_react.useState)(0);
	const [mountedInstances, setMountedInstances] = (0, import_react.useState)(() => /* @__PURE__ */ new Map());
	const openRef = (0, import_react.useRef)(open);
	const previousDefaultOpenRef = (0, import_react.useRef)(defaultOpen);
	const onOpenChangeRef = (0, import_react.useRef)(onOpenChange);
	const consumedUserOpenIntentRef = (0, import_react.useRef)(userOpenIntentSeq);
	(0, import_react.useEffect)(() => {
		onOpenChangeRef.current = onOpenChange;
	}, [onOpenChange]);
	(0, import_react.useEffect)(() => {
		openRef.current = open;
	}, [open]);
	(0, import_react.useEffect)(() => {
		if (previousDefaultOpenRef.current === defaultOpen) return;
		previousDefaultOpenRef.current = defaultOpen;
		const userIntent = consumedUserOpenIntentRef.current !== userOpenIntentSeq;
		consumedUserOpenIntentRef.current = userOpenIntentSeq;
		if (openRef.current === defaultOpen) return;
		openRef.current = defaultOpen;
		setOpen(defaultOpen);
		if (defaultOpen) {
			if (userIntent) setUserOpenSeq((seq) => seq + 1);
			setRequestedPanelId(defaultPanelId);
			setPdfLayoutPending(true);
		} else {
			setMaximized(false);
			setLayoutAnimationPending(false);
			setPdfLayoutPending(false);
		}
	}, [
		defaultOpen,
		defaultPanelId,
		userOpenIntentSeq
	]);
	const requestedEntry = findEntry(entries, requestedPanelId);
	const defaultEntry = getDefaultEntry(entries);
	const fallbackEntry = entries.find((entry) => entry.readiness === "ready");
	const activeEntry = requestedEntry?.readiness === "ready" ? requestedEntry : requestedEntry?.readiness === "pending" ? void 0 : fallbackEntry;
	const pendingEntry = requestedEntry?.readiness === "pending" ? requestedEntry : !activeEntry && defaultEntry?.readiness === "pending" ? defaultEntry : void 0;
	const reconciledEntry = activeEntry ?? pendingEntry;
	const presentationOpen = present && open && Boolean(activeEntry);
	const presentationMaximized = presentationOpen && maximized;
	(0, import_react.useLayoutEffect)(() => {
		if (!reconciledEntry || reconciledEntry.id === requestedPanelId) return;
		setRequestedPanelId(reconciledEntry.id);
	}, [reconciledEntry, requestedPanelId]);
	(0, import_react.useLayoutEffect)(() => {
		setMountedInstances((current) => updateMountedInstances(current, entries, activeEntry, presentationOpen));
	}, [
		activeEntry,
		entries,
		presentationOpen
	]);
	const isActive = (0, import_react.useCallback)((panelId) => presentationOpen && activeEntry?.id === panelId, [activeEntry?.id, presentationOpen]);
	const canOpen = (0, import_react.useCallback)((panelId) => findEntry(entries, panelId)?.readiness === "ready", [entries]);
	const requestOpen = (0, import_react.useCallback)((panelId, options) => {
		const wasOpen = openRef.current;
		openRef.current = true;
		setRequestedPanelId(panelId);
		setOpen(true);
		if (!wasOpen) {
			setPdfLayoutPending(true);
			if (options?.userInitiated) setUserOpenSeq((seq) => seq + 1);
		}
		onOpenChangeRef.current?.(true);
	}, []);
	const tryOpen = (0, import_react.useCallback)((panelId, options) => {
		if (!canOpen(panelId)) return false;
		requestOpen(panelId, options);
		return true;
	}, [canOpen, requestOpen]);
	const close = (0, import_react.useCallback)(() => {
		if (!openRef.current) return;
		openRef.current = false;
		setOpen(false);
		setMaximized(false);
		setPdfLayoutPending(false);
		onOpenChangeRef.current?.(false);
	}, []);
	const minimize = (0, import_react.useCallback)(() => {
		setLayoutAnimationPending(true);
		setPdfLayoutPending(false);
		setMaximized(false);
	}, []);
	const toggleMaximized = (0, import_react.useCallback)(() => {
		setLayoutAnimationPending(true);
		setPdfLayoutPending(false);
		setMaximized((current) => !current);
	}, []);
	const completeLayoutAnimation = (0, import_react.useCallback)((mode) => {
		setLayoutAnimationPending(false);
		if (mode === "closed") return;
		setPdfLayoutPending(false);
		setPdfLayoutRefreshKey((key) => key + 1);
	}, []);
	const reportFullWidthPhase = (0, import_react.useCallback)((active) => setFullWidthActive(active), []);
	const reportPaneResizing = (0, import_react.useCallback)((active) => setPaneResizing(active), []);
	const state = (0, import_react.useMemo)(() => ({
		activePanelId: activeEntry?.id,
		defaultPanelId: defaultEntry?.id,
		maximized,
		presentationOpen,
		presentationMaximized,
		presentationEnabled: present,
		layoutAnimationPending,
		fullWidthActive,
		paneResizing,
		userOpenSeq,
		pdfLayoutPending,
		pdfLayoutRefreshKey,
		isActive
	}), [
		activeEntry?.id,
		defaultEntry?.id,
		fullWidthActive,
		isActive,
		layoutAnimationPending,
		maximized,
		paneResizing,
		pdfLayoutPending,
		pdfLayoutRefreshKey,
		present,
		presentationMaximized,
		presentationOpen,
		userOpenSeq
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPanelActionsContext, {
		value: (0, import_react.useMemo)(() => ({
			canOpen,
			tryOpen,
			requestOpen,
			close,
			minimize,
			completeLayoutAnimation,
			toggleMaximized,
			reportFullWidthPhase,
			reportPaneResizing
		}), [
			canOpen,
			close,
			completeLayoutAnimation,
			minimize,
			reportFullWidthPhase,
			reportPaneResizing,
			requestOpen,
			toggleMaximized,
			tryOpen
		]),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPanelStateContext, {
			value: state,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPanelRenderContext, {
				value: (0, import_react.useMemo)(() => ({
					entries,
					mountedInstances,
					scope
				}), [
					entries,
					mountedInstances,
					scope
				]),
				children
			})
		})
	});
}
function useRightPanelState() {
	const state = (0, import_react.use)(RightPanelStateContext);
	if (!state) throw new Error("useRightPanelState must be used within <RightPanelProvider>");
	return state;
}
function useOptionalRightPanelState() {
	return (0, import_react.use)(RightPanelStateContext) ?? void 0;
}
function useRightPanelControllerActions() {
	const actions = (0, import_react.use)(RightPanelActionsContext);
	if (!actions) throw new Error("useRightPanelActions must be used within <RightPanelProvider>");
	return actions;
}
function useRightPanelActions() {
	return useRightPanelControllerActions();
}
function useOptionalRightPanelActions() {
	return (0, import_react.use)(RightPanelActionsContext) ?? void 0;
}
function RightPanelHeaderControls({ canMaximize = false }) {
	const state = useRightPanelState();
	const actions = useRightPanelControllerActions();
	const { t: t$2 } = useTranslation();
	const maximizeLabel = t$2(state.presentationMaximized ? "common.minimize" : "common.maximize");
	const MaximizeIcon = state.presentationMaximized ? Minimize2 : Maximize2;
	const closeLabel = t$2("common.close_sidebar");
	const maximizeButton = canMaximize || state.presentationMaximized ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
		content: maximizeLabel,
		delay: 800,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavbarIcon_default, {
			tone: "conversation",
			className: "[&_svg]:!size-3.5 shrink-0",
			"aria-label": maximizeLabel,
			"aria-pressed": state.presentationMaximized,
			onClick: actions.toggleMaximized,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MaximizeIcon, {})
		})
	}) : null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.right-panel-header-controls",
		className: "flex shrink-0 items-center gap-0.5 [-webkit-app-region:no-drag]",
		children: [maximizeButton, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
			content: closeLabel,
			delay: 800,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavbarIcon_default, {
				tone: "conversation",
				"aria-label": closeLabel,
				onClick: actions.close,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightSidebarCollapseIcon, {})
			})
		})]
	});
}
function RightPanelHeader({ canMaximize = false, title }) {
	const state = useRightPanelState();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.right-panel-header.shell-tab-list",
		"data-testid": "shell-tab-list",
		className: cn("flex h-(--navbar-height) shrink-0 items-center justify-between gap-2 border-border-subtle border-b px-2 [-webkit-app-region:no-drag]", state.presentationMaximized && "bg-card"),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "chat.right-panel-header.shell-tab-title",
			"data-testid": "shell-tab-title",
			className: "min-w-0 flex-1 select-none truncate px-1 font-medium text-foreground text-sm",
			children: title
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPanelHeaderControls, { canMaximize })]
	});
}
function RightPanelEntry({ active, entry, scope }) {
	const [renderFailed, setRenderFailed] = (0, import_react.useState)(false);
	const Panel = entry.component;
	const showFallbackHeader = renderFailed && entry.headerMode === "content";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.right-panel-entry",
		className: "flex h-full min-h-0 flex-col overflow-hidden",
		children: [showFallbackHeader ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPanelHeader, {
			canMaximize: entry.canMaximize,
			title: entry.title
		}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "min-h-0 flex-1 overflow-hidden",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, {
				onError: () => setRenderFailed(true),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Panel, {
					active,
					panelId: entry.id,
					scope
				})
			})
		})]
	});
}
function RightPanel() {
	const context = (0, import_react.use)(RightPanelRenderContext);
	if (!context) throw new Error("RightPanel must be used within <RightPanelProvider>");
	const state = useRightPanelState();
	const mountedEntries = context.entries.filter((entry) => context.mountedInstances.get(entry.id) === entry.instanceKey && entry.readiness !== "unavailable");
	const activeEntry = findEntry(context.entries, state.activePanelId);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.right-panel",
		className: "flex h-full flex-col gap-0 overflow-hidden text-card-foreground",
		children: [activeEntry?.headerMode === "content" ? null : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPanelHeader, {
			canMaximize: activeEntry?.canMaximize,
			title: activeEntry?.title
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "relative min-h-0 flex-1 overflow-hidden",
			children: mountedEntries.map((entry) => {
				const active = state.isActive(entry.id);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Activity, {
					mode: active ? "visible" : "hidden",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPanelEntry, {
						active,
						entry,
						scope: context.scope
					})
				}, `${entry.id}:${entry.instanceKey}`);
			})
		})]
	});
}
function RightPanelKeyboardShortcut() {
	const state = useRightPanelState();
	const actions = useRightPanelActions();
	const isActiveTab = useIsActiveTab();
	const targetPanelId = state.defaultPanelId;
	const enabled = state.presentationEnabled && isActiveTab && Boolean(targetPanelId && actions.canOpen(targetPanelId));
	useCommandHandler("topic.sidebar.toggle", (0, import_react.useCallback)(() => {
		if (state.presentationOpen) actions.close();
		else if (targetPanelId) actions.tryOpen(targetPanelId, { userInitiated: true });
	}, [
		actions,
		state.presentationOpen,
		targetPanelId
	]), { enabled });
	return null;
}
function RightPanelViewport({ children = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPanel, {}) }) {
	const state = useRightPanelState();
	const actions = useRightPanelControllerActions();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPanelKeyboardShortcut, {}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PersistentRightPaneHost, {
		open: state.presentationOpen,
		maximized: state.presentationMaximized,
		width: 280,
		resizable: true,
		minWidth: 255,
		defaultWidth: 280,
		maxWidth: 720,
		cacheKey: ARTIFACT_RIGHT_PANE_CACHE_KEY,
		onLayoutAnimationComplete: actions.completeLayoutAnimation,
		onFullWidthPhaseChange: actions.reportFullWidthPhase,
		onResizingChange: actions.reportPaneResizing,
		onDragClose: actions.close,
		children
	})] });
}
function RightPanelShortcut({ tab, label, icon, disabled = false, tooltip, className, onClick, ...buttonProps }) {
	const state = useRightPanelState();
	const actions = useRightPanelActions();
	const ready = actions.canOpen(tab);
	const active = state.isActive(tab);
	const tooltipContent = tooltip === false ? false : tooltip ?? label;
	const handleClick = (0, import_react.useCallback)((event) => {
		onClick?.(event);
		if (event.defaultPrevented) return;
		if (active) {
			actions.close();
			return;
		}
		actions.tryOpen(tab, { userInitiated: true });
	}, [
		actions,
		active,
		onClick,
		tab
	]);
	if (!ready || state.presentationMaximized) return null;
	const button = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavbarIcon_default, {
		...buttonProps,
		tone: "conversation",
		className: cn("[&_svg]:!size-3.5 shrink-0", className),
		active,
		disabled,
		"aria-label": label,
		"aria-pressed": active,
		"data-shell-tab-shortcut": tab,
		onClick: handleClick,
		children: icon
	});
	if (tooltipContent === false) return button;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
		content: tooltipContent,
		delay: 800,
		children: button
	});
}
const RESOURCE_PANE_TAB = "resources";
function ResourcePaneRightPanel({ scope }) {
	return scope.resourcePane?.node ?? null;
}
function createResourcePaneCapability({ instanceKey = RESOURCE_PANE_TAB } = {}) {
	return {
		component: ResourcePaneRightPanel,
		resolve: (scope) => ({
			id: RESOURCE_PANE_TAB,
			instanceKey,
			title: scope.resourcePane?.label ?? "",
			readiness: scope.resourcePane ? "ready" : "unavailable"
		})
	};
}
function ResourcePaneLocateOpener({ revealRequest }) {
	const actions = useRightPanelActions();
	const handledRequestIdRef = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!revealRequest?.clearFilters) return;
		if (!actions.canOpen("resources")) return;
		if (handledRequestIdRef.current === revealRequest.requestId) return;
		handledRequestIdRef.current = revealRequest.requestId;
		actions.tryOpen(RESOURCE_PANE_TAB);
	}, [actions, revealRequest]);
	return null;
}
function ResourcePaneCountButton({ label, count, className }) {
	const state = useRightPanelState();
	const actions = useRightPanelActions();
	const title = `${label} ${count}`;
	const active = state.isActive(RESOURCE_PANE_TAB);
	const handleClick = (0, import_react.useCallback)(() => {
		if (active) {
			actions.close();
			return;
		}
		actions.tryOpen(RESOURCE_PANE_TAB, { userInitiated: true });
	}, [actions, active]);
	if (!actions.canOpen("resources")) return null;
	if (state.presentationMaximized) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
		content: title,
		delay: 800,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
			type: "button",
			variant: "ghost",
			"aria-label": title,
			className: cn("group h-7 shrink-0 gap-1.5 rounded-full bg-card px-2.5 font-medium text-muted-foreground text-xs shadow-none", "hover:bg-accent hover:text-foreground", active && "bg-secondary text-secondary-foreground hover:bg-secondary-hover hover:text-secondary-foreground", "[&_svg]:!size-3.5 [-webkit-app-region:none]", className),
			"aria-pressed": active,
			onClick: handleClick,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(List, {}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: label }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: cn("text-muted-foreground group-hover:text-foreground", active && "text-secondary-foreground group-hover:text-secondary-foreground"),
					children: count
				})
			]
		})
	});
}
function EmptyState({ actions, className, compact = false, description, icon: Icon, iconClassName, iconSize, iconStrokeWidth, id, title, ...props }) {
	if (!actions && !iconClassName && !iconSize && !iconStrokeWidth) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmptyState$1, {
		className,
		compact,
		description,
		icon: Icon,
		title,
		...props
	});
	if (!Icon) return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.empty-state.chat-empty-state part:chat-empty-state",
		id,
		"data-slot": "chat-empty-state",
		className: cn$1("flex h-full w-full flex-col items-center justify-center gap-4", className),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmptyState$1, {
			compact,
			description,
			title,
			...props
		}), actions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "flex items-center gap-3",
			children: actions
		})]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.empty-state.chat-empty-state part:chat-empty-state",
		id,
		"data-slot": "chat-empty-state",
		className: cn$1("flex h-full w-full flex-col items-center justify-center gap-4 text-center", className),
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
				size: iconSize ?? (compact ? 40 : 56),
				strokeWidth: iconStrokeWidth,
				className: cn$1("text-muted-foreground", iconClassName)
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex flex-col items-center gap-2",
				children: [title && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("h3", {
					className: "m-0 font-medium text-base text-foreground",
					children: title
				}), description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
					className: "m-0 max-w-xs text-muted-foreground text-sm",
					children: description
				})]
			}),
			actions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex items-center gap-3",
				children: actions
			})
		]
	});
}
function LoadingState({ className, description, label, rows = 3, variant = "spinner", ...props }) {
	if (variant === "skeleton") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.loading-state.chat-loading-state part:chat-loading-state",
		"data-slot": "chat-loading-state",
		role: "status",
		"aria-live": "polite",
		className: cn$1("space-y-2", className),
		...mergeUiProps(props, "chat.loading-state.chat-loading-state part:chat-loading-state"),
		children: Array.from({ length: rows }).map((_$1, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: cn$1("h-4", index === rows - 1 ? "w-2/3" : "w-full") }, index))
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.loading-state.chat-loading-state part:chat-loading-state",
		"data-slot": "chat-loading-state",
		role: "status",
		"aria-live": "polite",
		className: cn$1("flex min-w-0 items-center gap-2 text-muted-foreground text-sm", className),
		...mergeUiProps(props, "chat.loading-state.chat-loading-state part:chat-loading-state"),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoaderCircle, { className: "size-4 shrink-0 animate-spin" }), (label || description) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
			className: "min-w-0",
			children: [label && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "block truncate text-foreground",
				children: label
			}), description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "block truncate text-muted-foreground text-xs",
				children: description
			})]
		})]
	});
}
function MessageListInitialLoading({ delayMs = 160 }) {
	const [visible, setVisible] = (0, import_react.useState)(() => delayMs <= 0);
	(0, import_react.useEffect)(() => {
		if (delayMs <= 0) {
			setVisible(true);
			return;
		}
		setVisible(false);
		const timer = window.setTimeout(() => setVisible(true), delayMs);
		return () => window.clearTimeout(timer);
	}, [delayMs]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.message-list-initial-loading",
		className: "flex h-full min-h-0 flex-1 flex-col px-6 py-8",
		"aria-busy": "true",
		children: visible && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			"aria-hidden": "true",
			"data-message-list-loading-skeleton": "",
			className: "mx-auto flex w-full max-w-[800px] flex-col gap-7",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex justify-end",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex w-[min(58%,32rem)] flex-col items-end gap-2",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "h-3 w-2/3 rounded-sm" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "h-3 w-full rounded-sm" })]
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex w-[min(76%,42rem)] items-start gap-3",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "size-7 shrink-0 rounded-full" }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex min-w-0 flex-1 flex-col gap-2 pt-1",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "h-3 w-11/12 rounded-sm" }),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "h-3 w-full rounded-sm" }),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "h-3 w-3/5 rounded-sm" })
						]
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex w-[min(68%,38rem)] items-start gap-3",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "size-7 shrink-0 rounded-full" }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex min-w-0 flex-1 flex-col gap-2 pt-1",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "h-3 w-full rounded-sm" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "h-3 w-4/5 rounded-sm" })]
					})]
				})
			]
		})
	});
}
var ChatBottomOverlayInsetContext = (0, import_react.createContext)(null);
function ChatBottomOverlayInsetProvider({ value, children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChatBottomOverlayInsetContext, {
		value,
		children
	});
}
function useChatBottomOverlayInset() {
	return (0, import_react.use)(ChatBottomOverlayInsetContext);
}
var COMPOSER_DOCK_MOTION_CLEAR_DELAY_MS = 280;
function useComposerDockMotionTransition(placement) {
	const previousPlacementRef = (0, import_react.useRef)(placement);
	const [transition, setTransition] = (0, import_react.useState)();
	(0, import_react.useLayoutEffect)(() => {
		const previousPlacement = previousPlacementRef.current;
		previousPlacementRef.current = placement;
		setTransition(previousPlacement === "home" && placement === "docked" ? "home-to-docked" : void 0);
	}, [placement]);
	(0, import_react.useEffect)(() => {
		if (!transition) return;
		const timer = window.setTimeout(() => {
			setTransition(void 0);
		}, COMPOSER_DOCK_MOTION_CLEAR_DELAY_MS);
		return () => window.clearTimeout(timer);
	}, [transition]);
	return transition;
}
function getComposerDockMotionAttributes(transition) {
	if (!transition) return void 0;
	return {
		className: "animation-chat-composer-dock-down",
		motion: transition
	};
}
var COMPOSER_MESSAGE_GAP_PX = 16;
var ZERO_COMPOSER_INLINE_INSETS = {
	left: 0,
	right: 0
};
function findComposerViewportInsetTarget(node) {
	const targetRoot = node.querySelector("[data-composer-active-layer]") ?? node;
	return targetRoot.querySelector("[data-composer-viewport-inset-target]") ?? targetRoot.querySelector("[data-composer-inputbar]");
}
function ComposerDockTransitionFrame({ placement, main, composer, mainVisible = placement === "docked", composerElevated = false, overlay }) {
	const rootRef = (0, import_react.useRef)(null);
	const composerRef = (0, import_react.useRef)(null);
	const [bottomOverlayInsets, setBottomOverlayInsets] = (0, import_react.useState)(null);
	const [composerInlineInsets, setComposerInlineInsets] = (0, import_react.useState)(ZERO_COMPOSER_INLINE_INSETS);
	const isDocked = placement === "docked";
	const hasComposer = Boolean(composer);
	const dockMotionAttributes = getComposerDockMotionAttributes(useComposerDockMotionTransition(placement));
	const setQuickPanelFill = useOptionalQuickPanel()?.setFillToAvailableHeight;
	(0, import_react.useLayoutEffect)(() => {
		if (!setQuickPanelFill) return;
		setQuickPanelFill(placement === "home");
		return () => setQuickPanelFill(false);
	}, [placement, setQuickPanelFill]);
	(0, import_react.useLayoutEffect)(() => {
		const node = composerRef.current;
		if (!node) return;
		let observer = null;
		let observedInsetTarget = null;
		const updateInset = () => {
			if (!isDocked || !hasComposer) {
				setBottomOverlayInsets(null);
				setComposerInlineInsets((current) => current.left === 0 && current.right === 0 ? current : ZERO_COMPOSER_INLINE_INSETS);
				return;
			}
			const insetTarget = findComposerViewportInsetTarget(node);
			if (observer && insetTarget !== observedInsetTarget) {
				if (observedInsetTarget) observer.unobserve(observedInsetTarget);
				if (insetTarget) observer.observe(insetTarget);
				observedInsetTarget = insetTarget;
			}
			const root = rootRef.current;
			if (!insetTarget || !root) {
				setBottomOverlayInsets(null);
				setComposerInlineInsets((current) => current.left === 0 && current.right === 0 ? current : ZERO_COMPOSER_INLINE_INSETS);
				return;
			}
			const insetTargetRect = insetTarget.getBoundingClientRect();
			if (insetTargetRect.width === 0 && insetTargetRect.height === 0) return;
			const composerRect = node.getBoundingClientRect();
			const rootRect = root.getBoundingClientRect();
			const scroller$1 = root.querySelector("[data-message-virtual-list-scroller]");
			const scrollerRect = scroller$1?.getBoundingClientRect();
			const scrollerClientWidth = scroller$1?.clientWidth ?? 0;
			const nextBottomOverlayInsets = {
				contentBottomPadding: Math.max(0, insetTargetRect.bottom - composerRect.top + COMPOSER_MESSAGE_GAP_PX),
				scrollerBottomMargin: Math.max(0, rootRect.bottom - insetTargetRect.bottom)
			};
			const nextComposerInlineInsets = {
				left: scrollerRect ? Math.max(0, scrollerRect.left - rootRect.left) : 0,
				right: scrollerRect ? Math.max(0, rootRect.right - scrollerRect.left - scrollerClientWidth) : 0
			};
			setBottomOverlayInsets((current) => current?.contentBottomPadding === nextBottomOverlayInsets.contentBottomPadding && current.scrollerBottomMargin === nextBottomOverlayInsets.scrollerBottomMargin ? current : nextBottomOverlayInsets);
			setComposerInlineInsets((current) => current.left === nextComposerInlineInsets.left && current.right === nextComposerInlineInsets.right ? current : nextComposerInlineInsets);
		};
		updateInset();
		if (typeof ResizeObserver === "undefined") return;
		observer = new ResizeObserver(updateInset);
		if (rootRef.current) observer.observe(rootRef.current);
		observer.observe(node);
		const scroller = rootRef.current?.querySelector("[data-message-virtual-list-scroller]");
		if (scroller) observer.observe(scroller);
		updateInset();
		const mutationObserver = typeof MutationObserver === "undefined" ? null : new MutationObserver(updateInset);
		mutationObserver?.observe(node, {
			attributes: true,
			attributeFilter: ["data-composer-active-layer", "data-composer-active-override"],
			subtree: true
		});
		return () => {
			mutationObserver?.disconnect();
			observer?.disconnect();
		};
	}, [hasComposer, isDocked]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.composer-dock-transition-frame",
		ref: rootRef,
		className: "relative flex h-full min-h-0 flex-1 flex-col overflow-hidden",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChatBottomOverlayInsetProvider, {
				value: bottomOverlayInsets,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: cn("flex h-full min-h-0 flex-1 flex-col overflow-hidden", !mainVisible && "pointer-events-none"),
					style: { opacity: mainVisible ? 1 : 0 },
					children: main
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-composer-dock-layer": "",
				style: isDocked ? {
					paddingInlineStart: composerInlineInsets.left,
					paddingInlineEnd: composerInlineInsets.right
				} : void 0,
				className: cn("pointer-events-none absolute inset-x-0 w-full", composerElevated ? "z-50" : "z-10", isDocked ? "bottom-0" : "top-0 bottom-0 flex items-center pb-[12vh] has-[.inputbar-container.expanded]:pb-0"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "w-full",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						ref: composerRef,
						"data-composer-dock-surface": "",
						"data-composer-dock-motion": dockMotionAttributes?.motion,
						className: cn("w-full", dockMotionAttributes?.className),
						children: composer
					})
				})
			}),
			overlay
		]
	});
}
function ConversationComposerStage({ placement, main, composer, overlay, composerElevated, mainVisible }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComposerDockTransitionFrame, {
		placement,
		main,
		composer,
		mainVisible: mainVisible ?? placement === "docked",
		overlay,
		composerElevated
	});
}
function ConversationStageCenter(props) {
	const presentationState = useOptionalRightPanelState();
	const mainVisible = props.mainVisible ?? props.placement === "docked";
	const paneMaximized = presentationState?.presentationMaximized ?? false;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.conversation-stage-center",
		className: "flex h-full min-h-0 flex-1 flex-col justify-between",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConversationComposerStage, {
			...props,
			mainVisible: mainVisible && !paneMaximized,
			composerElevated: props.composerElevated || paneMaximized
		})
	});
}
function ConversationCenterState({ state }) {
	if (state === "loading") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConversationStageCenter, {
		placement: "docked",
		main: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageListInitialLoading, {}),
		composer: null
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.conversation-center-state",
		className: "h-full min-h-0 flex-1"
	});
}
function ConversationGreeting({ avatar, title }) {
	const inset = useChatBottomOverlayInset();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.conversation-greeting",
		"data-testid": "conversation-greeting",
		className: "flex h-full w-full flex-col items-center justify-center gap-4 px-6 text-center",
		style: { paddingBottom: inset?.contentBottomPadding ?? 0 },
		children: [avatar && (isEmoji(avatar) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmojiIcon_default, {
			emoji: avatar,
			className: "mr-0",
			size: 48,
			fontSize: 28
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Avatar, {
			className: "size-12",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(AvatarImage, {
				className: "size-full object-cover",
				src: avatar
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AvatarFallback, {
				className: "text-2xl",
				children: "🤖"
			})]
		})), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", {
			className: "m-0 font-medium text-foreground text-lg",
			children: title
		})]
	});
}
function OverlayHost({ children, className, style }) {
	if (!children) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.overlay-host",
		className: cn("pointer-events-none absolute inset-0 z-1000", className),
		style,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "pointer-events-auto",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, { children })
		})
	});
}
function clampResourceListPaneWidth(width) {
	return Math.min(360, Math.max(200, Math.round(width)));
}
function useResourceListPaneResize({ onPaneCollapse } = {}) {
	const [storedWidth, setStoredWidth] = usePersistCache(RESOURCE_LIST_PANE_CACHE_KEY);
	const paneRef = (0, import_react.useRef)(null);
	const pendingPaneCollapseRef = (0, import_react.useRef)(false);
	const dragStateRef = (0, import_react.useRef)({
		paneLeft: 0,
		startClientX: 0
	});
	const paneWidth = clampResourceListPaneWidth(storedWidth ?? 240);
	(0, import_react.useLayoutEffect)(() => {
		document.documentElement.style.setProperty("--assistants-width", `${paneWidth}px`);
	}, [paneWidth]);
	const { isResizing, startResizing: startResizeDrag } = useResizeDrag({ onMove: (0, import_react.useCallback)((moveEvent, stop) => {
		const { paneLeft, startClientX } = dragStateRef.current;
		const nextWidth = moveEvent.clientX - paneLeft;
		const dragDelta = moveEvent.clientX - startClientX;
		if (nextWidth < 200 && dragDelta <= -200) {
			setStoredWidth(240);
			pendingPaneCollapseRef.current = true;
			stop();
			return;
		}
		setStoredWidth(clampResourceListPaneWidth(nextWidth));
	}, [setStoredWidth]) });
	(0, import_react.useEffect)(() => {
		if (isResizing || !pendingPaneCollapseRef.current) return;
		pendingPaneCollapseRef.current = false;
		onPaneCollapse?.();
	}, [isResizing, onPaneCollapse]);
	return {
		isResizing,
		paneRef,
		paneWidth,
		startResizing: (0, import_react.useCallback)((event) => {
			dragStateRef.current = {
				paneLeft: paneRef.current?.getBoundingClientRect().left ?? 0,
				startClientX: event.clientX
			};
			startResizeDrag(event);
		}, [startResizeDrag]),
		setPaneWidth: (0, import_react.useCallback)((nextWidth) => setStoredWidth(clampResourceListPaneWidth(nextWidth)), [setStoredWidth])
	};
}
function PageSidebar({ children, open, width, className, style, onPaneCollapse, onResizingChange }) {
	const { t: t$2 } = useTranslation();
	const { isResizing, paneRef, paneWidth, startResizing, setPaneWidth } = useResourceListPaneResize({ onPaneCollapse });
	const resolvedWidth = width ?? paneWidth;
	const onResizingChangeRef = (0, import_react.useRef)(onResizingChange);
	(0, import_react.useEffect)(() => {
		onResizingChangeRef.current = onResizingChange;
	}, [onResizingChange]);
	(0, import_react.useEffect)(() => {
		onResizingChangeRef.current?.(isResizing);
	}, [isResizing]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AnimatePresence, {
		initial: false,
		children: open && children && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(motion.div, {
			"data-ui": "chat.page-sidebar.conversation-navigation part:conversation-navigation",
			ref: paneRef,
			initial: {
				width: 0,
				opacity: 0
			},
			animate: {
				width: resolvedWidth || "var(--assistants-width)",
				opacity: 1
			},
			exit: {
				width: 0,
				opacity: 0
			},
			transition: isResizing ? { duration: 0 } : CHAT_SHELL_TRANSITION,
			"data-resource-list-pane": true,
			"data-resizing": isResizing || void 0,
			className: cn("group/resource-list-pane relative shrink-0 overflow-visible data-[resizing=true]:[&_.conversation-navigation-pane-content]:transition-none data-[resizing=true]:[&_.conversation-navigation-pane]:transition-none", className),
			style,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-resource-list-pane-content": true,
				className: "h-full min-h-0 overflow-hidden",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, { children })
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-resource-list-pane-resize-handle": true,
				onMouseDown: startResizing,
				...getVerticalSplitterProps({
					width: paneWidth,
					min: 200,
					max: 360,
					label: t$2("common.resize_panel"),
					onResize: setPaneWidth
				}),
				className: "group/resource-list-resize-handle absolute top-0 bottom-0 left-full z-10 w-2 cursor-col-resize focus-visible:bg-primary/40 focus-visible:outline-none",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "absolute top-0 left-0 h-full w-0.5 bg-primary/20 opacity-0 transition-opacity group-hover/resource-list-resize-handle:opacity-100 group-data-[resizing=true]/resource-list-pane:bg-primary/35 group-data-[resizing=true]/resource-list-pane:opacity-100" })
			})]
		}, "page-sidebar")
	});
}
var MANUAL_EXPAND_RELEASE_NARROWING = 8;
function clampPaneStoredWidth(width) {
	return Math.min(720, Math.max(255, Math.round(width)));
}
function useResourceListAutoCollapse({ leftPaneOpen, listResizing, onPaneAutoCollapseChange, paneManualToggle, rootRef }) {
	const rightPanelState = useOptionalRightPanelState();
	const [storedListWidth] = usePersistCache("ui.chat.sidebar.width");
	const [storedPaneWidth] = usePersistCache("ui.chat.artifact_pane.width");
	const dockedPaneOpen = Boolean(rightPanelState?.presentationOpen && !rightPanelState.presentationMaximized);
	const frozen = Boolean(rightPanelState?.fullWidthActive || rightPanelState?.presentationMaximized || rightPanelState?.layoutAnimationPending || rightPanelState?.paneResizing || listResizing);
	const userOpenSeq = rightPanelState?.userOpenSeq ?? 0;
	const shellWidthRef = (0, import_react.useRef)(0);
	const collapsedRef = (0, import_react.useRef)(false);
	const notifiedRef = (0, import_react.useRef)(false);
	const suppressedRef = (0, import_react.useRef)(false);
	const suppressStartShellRef = (0, import_react.useRef)(0);
	const pendingEvaluateRef = (0, import_react.useRef)(false);
	const consumedManualSeqRef = (0, import_react.useRef)(paneManualToggle?.seq ?? 0);
	const lastUserOpenSeqRef = (0, import_react.useRef)(userOpenSeq);
	const previousLeftPaneOpenRef = (0, import_react.useRef)(leftPaneOpen);
	const leftPaneOpenRef = (0, import_react.useRef)(leftPaneOpen);
	const frozenRef = (0, import_react.useRef)(frozen);
	const dockedPaneOpenRef = (0, import_react.useRef)(dockedPaneOpen);
	const widthsRef = (0, import_react.useRef)({
		listWidth: 0,
		paneWidth: 0
	});
	const onChangeRef = (0, import_react.useRef)(onPaneAutoCollapseChange);
	leftPaneOpenRef.current = leftPaneOpen;
	frozenRef.current = frozen;
	dockedPaneOpenRef.current = dockedPaneOpen;
	widthsRef.current = {
		listWidth: clampResourceListPaneWidth(storedListWidth ?? 0),
		paneWidth: clampPaneStoredWidth(storedPaneWidth ?? 0)
	};
	(0, import_react.useEffect)(() => {
		onChangeRef.current = onPaneAutoCollapseChange;
	}, [onPaneAutoCollapseChange]);
	const notify = (0, import_react.useCallback)((collapsed, force = false) => {
		if (!force && notifiedRef.current === collapsed) return;
		notifiedRef.current = collapsed;
		onChangeRef.current?.(collapsed);
	}, []);
	const evaluate = (0, import_react.useCallback)((forceNotify = false) => {
		if (suppressedRef.current) return;
		if (frozenRef.current) {
			pendingEvaluateRef.current = true;
			return;
		}
		const shellWidth = shellWidthRef.current;
		if (shellWidth <= 0) return;
		let next = evaluateAutoCollapse(predictCenterWidth({
			shellWidth,
			listWidth: widthsRef.current.listWidth,
			paneOpen: dockedPaneOpenRef.current,
			paneWidth: widthsRef.current.paneWidth
		}), collapsedRef.current);
		if (next && !collapsedRef.current && !leftPaneOpenRef.current) next = collapsedRef.current;
		if (next !== collapsedRef.current || forceNotify) {
			collapsedRef.current = next;
			notify(next, forceNotify);
		}
	}, [notify]);
	(0, import_react.useLayoutEffect)(() => {
		const signal = paneManualToggle;
		if (!signal || signal.seq === consumedManualSeqRef.current) return;
		consumedManualSeqRef.current = signal.seq;
		if (signal.open) {
			suppressedRef.current = true;
			suppressStartShellRef.current = shellWidthRef.current;
			if (collapsedRef.current) {
				collapsedRef.current = false;
				notify(false);
			}
		} else {
			suppressedRef.current = false;
			evaluate();
		}
	}, [
		evaluate,
		notify,
		paneManualToggle
	]);
	(0, import_react.useLayoutEffect)(() => {
		const wasOpen = previousLeftPaneOpenRef.current;
		previousLeftPaneOpenRef.current = leftPaneOpen;
		if (!wasOpen && leftPaneOpen) evaluate(true);
	}, [evaluate, leftPaneOpen]);
	(0, import_react.useLayoutEffect)(() => {
		evaluate();
	}, [dockedPaneOpen, evaluate]);
	(0, import_react.useLayoutEffect)(() => {
		if (userOpenSeq === lastUserOpenSeqRef.current) return;
		lastUserOpenSeqRef.current = userOpenSeq;
		suppressedRef.current = false;
		evaluate();
	}, [evaluate, userOpenSeq]);
	(0, import_react.useLayoutEffect)(() => {
		evaluate();
	}, [
		evaluate,
		storedListWidth,
		storedPaneWidth
	]);
	(0, import_react.useLayoutEffect)(() => {
		if (frozen || !pendingEvaluateRef.current) return;
		pendingEvaluateRef.current = false;
		evaluate();
	}, [evaluate, frozen]);
	(0, import_react.useEffect)(() => {
		const root = rootRef.current;
		if (!root || typeof ResizeObserver === "undefined") return;
		const observer = new ResizeObserver(([entry]) => {
			const nextShellWidth = entry.contentRect.width;
			if (nextShellWidth <= 0) return;
			shellWidthRef.current = nextShellWidth;
			if (suppressedRef.current) {
				if (suppressStartShellRef.current - nextShellWidth > MANUAL_EXPAND_RELEASE_NARROWING) {
					suppressedRef.current = false;
					evaluate();
				}
				return;
			}
			evaluate();
		});
		observer.observe(root);
		return () => observer.disconnect();
	}, [evaluate, rootRef]);
	(0, import_react.useEffect)(() => {
		return () => {
			if (collapsedRef.current) onChangeRef.current?.(false);
			collapsedRef.current = false;
			notifiedRef.current = false;
			suppressedRef.current = false;
			pendingEvaluateRef.current = false;
			shellWidthRef.current = 0;
		};
	}, []);
}
function ChatAppShell({ topBar, pane, paneOpen, panePosition = "left", main, centerContent, bottomComposer, sidePanel, centerOverlay, centerTopOverlay, rightPane, overlay, rootId, rootClassName, contentId, centerId, centerRef, centerClassName, onPaneCollapse, onPaneAutoCollapseChange, paneManualToggle }) {
	const hasCenterContent = centerContent !== void 0;
	const leftPaneOpen = Boolean(paneOpen && panePosition === "left");
	const rightPanelState = useOptionalRightPanelState();
	const centerTransition = rightPanelState?.layoutAnimationPending ? { duration: 0 } : CHAT_SHELL_TRANSITION;
	const rootRef = (0, import_react.useRef)(null);
	const [listResizing, setListResizing] = (0, import_react.useState)(false);
	useResourceListAutoCollapse({
		leftPaneOpen,
		listResizing,
		onPaneAutoCollapseChange,
		paneManualToggle,
		rootRef
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.app-shell",
		ref: rootRef,
		"data-chat-app-shell-root": true,
		id: rootId,
		className: cn("relative flex min-w-0 flex-1 flex-col overflow-hidden", rootClassName),
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				id: contentId,
				className: "flex min-w-0 flex-1 shrink flex-row overflow-hidden",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageSidebar, {
						open: leftPaneOpen,
						onPaneCollapse,
						onResizingChange: setListResizing,
						children: pane
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						"data-chat-app-shell-main-region": true,
						className: "relative flex min-w-0 flex-1 overflow-hidden",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "relative flex min-w-0 flex-1 flex-col",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(motion.div, {
								"data-ui": "chat.app-shell.conversation-main part:conversation-main",
								ref: centerRef,
								"data-chat-app-shell-center": true,
								id: centerId,
								layout: true,
								transition: centerTransition,
								className: cn("relative flex min-w-0 flex-1 flex-col overflow-hidden", centerClassName, rightPanelState?.presentationMaximized && "!transform-none !will-change-auto"),
								children: [
									topBar && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
										className: "relative z-10 shrink-0",
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, { children: topBar })
									}),
									hasCenterContent ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, { children: centerContent }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
										className: "flex min-h-0 flex-1 flex-col overflow-hidden",
										children: main
									}) }), bottomComposer && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, { children: bottomComposer })] }),
									centerOverlay && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, { children: centerOverlay })
								]
							}), centerTopOverlay && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OverlayHost, { children: centerTopOverlay })]
						}), rightPane]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightPaneHost, {
						open: Boolean(paneOpen && panePosition === "right"),
						children: pane
					})
				]
			}),
			sidePanel && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-chat-side-panel-host": true,
				className: "pointer-events-none absolute inset-0 z-80 *:pointer-events-auto",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, { children: sidePanel })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(OverlayHost, { children: overlay })
		]
	});
}
var import_react_dom$1 = require_react_dom();
var ConversationTopBarPortalContext = (0, import_react.createContext)(void 0);
function ConversationTopBarPortalProvider({ children }) {
	const { iconOnly, containerRef } = useOverflowIconOnly();
	const [target, setPortalTarget] = (0, import_react.useState)(null);
	const setTarget$1 = (0, import_react.useCallback)((nextTarget) => {
		containerRef(nextTarget);
		setPortalTarget(nextTarget);
	}, [containerRef]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConversationTopBarPortalContext, {
		value: (0, import_react.useMemo)(() => ({
			iconOnly,
			target,
			setTarget: setTarget$1
		}), [
			iconOnly,
			setTarget$1,
			target
		]),
		children
	});
}
function ConversationTopBarPortalHost({ children, className }) {
	const context = (0, import_react.use)(ConversationTopBarPortalContext);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.conversation-top-bar-portal",
		ref: context?.setTarget,
		"data-conversation-topbar-controls": true,
		className: cn("ml-2 flex min-w-0 flex-1 items-center gap-1.5 overflow-hidden [-webkit-app-region:no-drag] [&_button]:h-7 [&_button]:px-1.5", className),
		children
	});
}
function ConversationTopBarPortal({ children }) {
	const context = (0, import_react.use)(ConversationTopBarPortalContext);
	const composerOverridden = useActiveComposerOverride() !== null;
	if (!context) return children;
	if (!context.target || composerOverridden) return null;
	return (0, import_react_dom$1.createPortal)(children, context.target);
}
function useConversationTopBarPortalLayout() {
	const context = (0, import_react.use)(ConversationTopBarPortalContext);
	return {
		available: context !== void 0,
		iconOnly: context?.iconOnly ?? false
	};
}
function ConversationShell({ id, className, pane, paneOpen, panePosition, topBar, topRightTool, showTopRightToolWhenPaneOpen = false, center, sidePanel, centerOverlay, centerTopOverlay, overlay, rightPane, centerId, centerRef, centerClassName, onPaneCollapse, onPaneAutoCollapseChange, paneManualToggle }) {
	const { mode } = useWindowFrame();
	const isWindow = mode === "window";
	const resolvedTopBar = topRightTool ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConversationShellTopBar, {
		topRightTool,
		showTopRightToolWhenPaneOpen,
		children: topBar
	}) : topBar;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.view",
		id,
		className: cn("relative flex flex-1 overflow-hidden bg-background", isWindow ? "h-full" : "h-[calc(100vh-var(--navbar-height)-6px)] rounded-tl-[10px] rounded-bl-[10px]", className),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(QuickPanelProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConversationTopBarPortalProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChatAppShell, {
			pane,
			paneOpen,
			panePosition,
			topBar: resolvedTopBar,
			centerContent: center,
			sidePanel,
			centerOverlay,
			centerTopOverlay,
			rightPane,
			overlay,
			centerId,
			centerRef,
			centerClassName,
			onPaneCollapse,
			onPaneAutoCollapseChange,
			paneManualToggle
		}) }) })
	});
}
var ConversationShellTopBar = ({ topRightTool, showTopRightToolWhenPaneOpen, children }) => {
	const presentationState = useOptionalRightPanelState();
	const maximized = presentationState?.presentationMaximized ?? false;
	const open = presentationState?.presentationOpen ?? false;
	const shouldShowTopRightTool = Boolean(topRightTool) && !maximized && (!open || showTopRightToolWhenPaneOpen);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.conversation-shell-top-bar",
		"data-conversation-shell-topbar": true,
		className: "relative flex h-fit w-full min-w-0 items-center",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-conversation-shell-topbar-content": true,
				className: "min-w-0 flex-1",
				children
			}),
			shouldShowTopRightTool && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-conversation-shell-topbar-right": true,
				"data-navbar-right-occupant": true,
				className: "z-20 flex h-(--navbar-height) shrink-0 items-center gap-0.5 [-webkit-app-region:no-drag]",
				children: topRightTool
			}),
			shouldShowTopRightTool && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-conversation-shell-right-spacer": true,
				"aria-hidden": "true",
				className: "w-2 shrink-0"
			})
		]
	});
};
function ComposerCore({ fallback, className }) {
	const activeOverride = useActiveComposerOverride();
	const primaryLayerRef = (0, import_react.useRef)(null);
	const previousOverrideIdRef = (0, import_react.useRef)(null);
	const primaryFocusTargetRef = (0, import_react.useRef)(null);
	const primaryHadFocusRef = (0, import_react.useRef)(false);
	const activeOverrideId = activeOverride?.id ?? null;
	(0, import_react.useLayoutEffect)(() => {
		const previousOverrideId = previousOverrideIdRef.current;
		if (!previousOverrideId && activeOverrideId) {
			const activeElement = document.activeElement;
			const focusedPrimaryElement = activeElement instanceof HTMLElement && primaryLayerRef.current?.contains(activeElement) ? activeElement : null;
			if (focusedPrimaryElement || primaryHadFocusRef.current) {
				primaryFocusTargetRef.current = focusedPrimaryElement ?? primaryFocusTargetRef.current;
				focusedPrimaryElement?.blur();
			} else primaryFocusTargetRef.current = null;
			primaryHadFocusRef.current = false;
		} else if (previousOverrideId && !activeOverrideId) {
			const focusTarget = primaryFocusTargetRef.current;
			const activeElement = document.activeElement;
			const canRestoreFocus = !activeElement || activeElement === document.body || !activeElement.isConnected;
			if (focusTarget?.isConnected && canRestoreFocus) focusTarget.focus({ preventScroll: true });
			primaryFocusTargetRef.current = null;
		}
		previousOverrideIdRef.current = activeOverrideId;
	}, [activeOverrideId]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.composer-core",
		"data-composer-core": "",
		"data-composer-active-override": activeOverrideId ?? void 0,
		className: "relative w-full",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			ref: primaryLayerRef,
			"data-composer-primary-layer": "",
			"data-composer-active-layer": activeOverride ? void 0 : "",
			inert: Boolean(activeOverride),
			"aria-hidden": activeOverride ? true : void 0,
			className: activeOverride ? "pointer-events-none invisible absolute inset-x-0 bottom-0 w-full" : "relative w-full",
			onFocusCapture: (event) => {
				if (activeOverride) return;
				primaryFocusTargetRef.current = event.target;
				primaryHadFocusRef.current = true;
			},
			onBlurCapture: (event) => {
				if (activeOverride) return;
				if (!event.currentTarget.contains(event.relatedTarget)) primaryHadFocusRef.current = false;
			},
			children: fallback
		}), activeOverride ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-composer-override-layer": "",
			"data-composer-active-layer": "",
			className: "relative w-full",
			children: activeOverride.render({ className })
		}, activeOverride.id) : null]
	});
}
var emptyComposerContext = {};
function ConversationComposerSlot({ composerContext = emptyComposerContext, fallback }) {
	if (!fallback) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComposerContextProvider, {
		value: composerContext,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComposerCore, { fallback })
	});
}
function useExternalApps(options) {
	return useSWRImmutable(options?.enabled === false ? null : "external-apps/installed", async () => window.api.externalApps.detectInstalled());
}
function buildEditorUrl(app, filePath) {
	const encodedPath = filePath.split(/[/\\]/).map(encodeURIComponent).join("/");
	if (app.id === "zed") return `${app.protocol}file${encodedPath}`;
	return `${app.protocol}file/${encodedPath}?windowId=_blank`;
}
var baseProps = {
	xmlns: "http://www.w3.org/2000/svg",
	viewBox: "0 0 24 24",
	fill: "none",
	stroke: "currentColor",
	strokeWidth: 1.8,
	strokeLinecap: "round",
	strokeLinejoin: "round",
	"aria-hidden": true
};
function NewConversationIcon({ size = 24, className, ...props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
		"data-ui": "icons.new-conversation-icon",
		width: size,
		height: size,
		...mergeUiProps(baseProps, "icons.new-conversation-icon"),
		...mergeUiProps(props, "icons.new-conversation-icon"),
		className: cn("new-conversation-icon", className),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", {
			transform: "translate(12 12) scale(1.1) translate(-12 -12)",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M13 4H6a2 2 0 0 0-2 2v13l4-3h10a2 2 0 0 0 2-2v-3" }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M18 3.5v5" }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M15.5 6h5" })
			]
		})
	});
}
function withComposerFilePartMeta(part, attachment, fileEntryId) {
	return withCherryMeta(part, {
		...fileEntryId ? { fileEntryId } : {},
		fileTokenSourceId: attachment.fileTokenSourceId,
		...attachment.composerFileKind ? { composerFileKind: attachment.composerFileKind } : {}
	});
}
async function buildFilePartsForAttachments(attachments) {
	const paths = attachments.map((attachment) => {
		if (!attachment.path) throw new Error(`Cannot send attachment "${attachment.origin_name || attachment.name}": it has no file path`);
		return attachment.path;
	});
	return Promise.all(attachments.map(async (attachment, index) => {
		const entry = await window.api.file.createInternalEntry({
			source: "path",
			path: paths[index],
			cleanupPolicy: "delete_when_unreferenced"
		});
		const physicalPath = await window.api.file.getPhysicalPath({ id: entry.id });
		const metadata = await ipcApi.request("file.get_metadata", createFilePathHandle(physicalPath));
		if (metadata === null) throw new Error(`Failed to read metadata for freshly created file entry ${entry.id}`);
		return withComposerFilePartMeta({
			type: "file",
			mediaType: metadata.kind === "file" ? metadata.mime : "application/octet-stream",
			url: toFileUrl(physicalPath),
			filename: attachment.origin_name || attachment.name
		}, attachment, entry.id);
	}));
}
function DraftTokenChips({ item, hasText }) {
	const tokens = (item.draft?.tokens ?? []).filter((token) => isComposerInputTokenKind(token.kind));
	if (tokens.length === 0) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.draft-token-chips",
		className: cn("flex flex-wrap gap-1", hasText && "mt-1"),
		children: tokens.map((token) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComposerToken, { token: {
			id: token.id,
			kind: token.kind,
			label: token.label,
			description: token.description,
			promptText: token.promptText,
			payload: token.payload
		} }, token.id))
	});
}
function QueuedFollowupRow({ item, dragging, onSteer, onEdit, onRemove }) {
	const { t: t$2 } = useTranslation();
	const previewText = item.draft ? excludeComposerDraftTokens(item.draft, (token) => isComposerInputTokenKind(token.kind)).text.trim() : item.payload.text;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.queued-followup-row",
		className: "group flex items-center gap-1.5 rounded-[12px] bg-muted/40 px-2 py-1.5",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				"aria-hidden": true,
				"data-dragging": dragging ? "true" : "false",
				className: "flex shrink-0 cursor-grab items-center justify-center text-muted-foreground opacity-0 transition-opacity duration-150 group-hover:opacity-100 data-[dragging=true]:opacity-100",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GripVertical, { className: "size-4" })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "min-w-0 flex-1",
				children: [previewText ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "line-clamp-2 text-foreground text-sm",
					children: previewText
				}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DraftTokenChips, {
					item,
					hasText: Boolean(previewText)
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex shrink-0 items-center gap-0.5 text-muted-foreground opacity-0 transition-opacity group-hover:opacity-100",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						placement: "top",
						content: t$2("chat.input.followup_queue.steer"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							type: "button",
							variant: "ghost",
							size: "icon-sm",
							className: "size-7 shadow-none",
							"aria-label": t$2("chat.input.followup_queue.steer"),
							onClick: () => onSteer(item.id),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrowUp, { className: "size-4" })
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						placement: "top",
						content: t$2("chat.input.followup_queue.edit"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							type: "button",
							variant: "ghost",
							size: "icon-sm",
							className: "size-7 shadow-none",
							"aria-label": t$2("chat.input.followup_queue.edit"),
							onClick: () => onEdit(item.id),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Pencil, { className: "size-4" })
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						placement: "top",
						content: t$2("chat.input.followup_queue.remove"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							type: "button",
							variant: "ghost",
							size: "icon-sm",
							className: "size-7 shadow-none",
							"aria-label": t$2("chat.input.followup_queue.remove"),
							onClick: () => onRemove(item.id),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { className: "size-4" })
						})
					})
				]
			})
		]
	});
}
function QueuedFollowupsDock({ items, paused, onTogglePause, onSteer, onEdit, onRemove, onReorder }) {
	const { t: t$2 } = useTranslation();
	if (items.length === 0) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.queued-followups-dock",
		className: "mx-2 mb-1.5 rounded-[16px] border-[0.5px] border-border p-1.5 backdrop-blur",
		style: { backgroundColor: "color-mix(in srgb, var(--background) 88%, transparent)" },
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex items-center justify-between px-1.5 pb-1",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "text-muted-foreground text-xs",
				children: t$2("chat.input.followup_queue.title", { count: items.length })
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
				placement: "top",
				content: paused ? t$2("chat.input.followup_queue.resume") : t$2("chat.input.followup_queue.pause"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					type: "button",
					variant: "ghost",
					size: "icon-sm",
					className: "size-6 shadow-none",
					"aria-label": paused ? t$2("chat.input.followup_queue.resume") : t$2("chat.input.followup_queue.pause"),
					onClick: onTogglePause,
					children: paused ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Play, { className: "size-3.5" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Pause, { className: "size-3.5" })
				})
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "max-h-40 overflow-y-auto",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReorderableList, {
				items,
				getId: (item) => item.id,
				onReorder,
				direction: "vertical",
				gap: 4,
				renderItem: (item, _index, { dragging }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(QueuedFollowupRow, {
					item,
					dragging,
					onSteer,
					onEdit,
					onRemove
				})
			})
		})]
	});
}
var QUEUE_TTL = 1440 * 60 * 1e3;
var keyFor = (scopeKey) => `followup-queue.${scopeKey}`;
var pausedKeyFor = (scopeKey) => `followup-queue-paused.${scopeKey}`;
function loadQueue(scopeKey) {
	const cached = cacheService.getCasual(keyFor(scopeKey));
	return Array.isArray(cached) ? cached : [];
}
function loadPaused(scopeKey) {
	return cacheService.getCasual(pausedKeyFor(scopeKey)) === true;
}
function useFollowupQueue({ scopeKey, isFulfilled, markSeen, onDrain, onDrainFailed }) {
	const [items, setItems] = (0, import_react.useState)(() => loadQueue(scopeKey));
	const [paused, setPausedState] = (0, import_react.useState)(() => loadPaused(scopeKey));
	const scopeKeyRef = (0, import_react.useRef)(scopeKey);
	const itemsRef = (0, import_react.useRef)(items);
	itemsRef.current = items;
	const onDrainRef = (0, import_react.useRef)(onDrain);
	onDrainRef.current = onDrain;
	const onDrainFailedRef = (0, import_react.useRef)(onDrainFailed);
	onDrainFailedRef.current = onDrainFailed;
	const persist = (0, import_react.useCallback)((next) => {
		cacheService.setCasual(keyFor(scopeKeyRef.current), next, QUEUE_TTL);
	}, []);
	(0, import_react.useEffect)(() => {
		if (scopeKeyRef.current === scopeKey) return;
		scopeKeyRef.current = scopeKey;
		setItems(loadQueue(scopeKey));
		setPausedState(loadPaused(scopeKey));
	}, [scopeKey]);
	const setPaused = (0, import_react.useCallback)((nextPaused) => {
		cacheService.setCasual(pausedKeyFor(scopeKeyRef.current), nextPaused);
		setPausedState(nextPaused);
	}, []);
	const enqueue = (0, import_react.useCallback)((draft, payload) => {
		setItems((prev) => {
			const next = [...prev, {
				id: crypto.randomUUID(),
				draft,
				payload
			}];
			persist(next);
			return next;
		});
	}, [persist]);
	const removeId = (0, import_react.useCallback)((id) => {
		setItems((prev) => {
			const next = prev.filter((item) => item.id !== id);
			persist(next);
			return next;
		});
	}, [persist]);
	const reorder = (0, import_react.useCallback)((nextItems) => {
		setItems(nextItems);
		persist(nextItems);
	}, [persist]);
	(0, import_react.useEffect)(() => {
		if (!isFulfilled || paused) return;
		const head = itemsRef.current[0];
		if (!head) return;
		markSeen();
		const reportDrainFailure = () => onDrainFailedRef.current?.();
		onDrainRef.current(head.payload).then((sent) => {
			if (sent) removeId(head.id);
			else reportDrainFailure();
		}, reportDrainFailure);
	}, [
		isFulfilled,
		paused,
		markSeen,
		removeId
	]);
	return {
		items,
		enqueue,
		removeId,
		reorder,
		paused,
		setPaused
	};
}
function useInputHistory({ applyDraft }) {
	const [historyIndex, setHistoryIndex] = (0, import_react.useState)(-1);
	const draftBeforeHistoryRef = (0, import_react.useRef)(null);
	const navigationHistoryRef = (0, import_react.useRef)(null);
	const [history, setHistory] = usePersistCache("ui.composer.input_history");
	const applyHistoryIndex = (0, import_react.useCallback)((nextIndex) => {
		setHistoryIndex(nextIndex);
		if (nextIndex === -1) {
			applyDraft(draftBeforeHistoryRef.current ?? {
				text: "",
				tokens: []
			}, { source: "draft" });
			draftBeforeHistoryRef.current = null;
			navigationHistoryRef.current = null;
			return;
		}
		const historyItem = (navigationHistoryRef.current ?? history)[nextIndex];
		if (!historyItem) {
			applyDraft(draftBeforeHistoryRef.current ?? {
				text: "",
				tokens: []
			}, { source: "draft" });
			draftBeforeHistoryRef.current = null;
			navigationHistoryRef.current = null;
			setHistoryIndex(-1);
			return;
		}
		applyDraft({
			text: historyItem,
			tokens: []
		}, { source: "history" });
	}, [applyDraft, history]);
	const navigateHistory = (0, import_react.useCallback)((direction, currentDraft) => {
		const nextIndex = getNextInputHistoryIndex({
			currentIndex: historyIndex,
			direction,
			messagesLength: (navigationHistoryRef.current ?? history).length
		});
		if (nextIndex === historyIndex) return historyIndex !== -1;
		if (historyIndex === -1 && nextIndex !== -1) {
			draftBeforeHistoryRef.current = currentDraft;
			navigationHistoryRef.current = history;
		}
		applyHistoryIndex(nextIndex);
		return true;
	}, [
		applyHistoryIndex,
		history,
		historyIndex
	]);
	const resetHistoryIndex = (0, import_react.useCallback)(() => {
		setHistoryIndex(-1);
		draftBeforeHistoryRef.current = null;
		navigationHistoryRef.current = null;
	}, []);
	const takeDraftBeforeHistory = (0, import_react.useCallback)(() => {
		const draft = draftBeforeHistoryRef.current;
		setHistoryIndex(-1);
		draftBeforeHistoryRef.current = null;
		navigationHistoryRef.current = null;
		return draft;
	}, []);
	const saveHistory = (0, import_react.useCallback)((content) => {
		const normalizedContent = content.trim();
		if (!normalizedContent) return;
		setHistory((prev) => [normalizedContent, ...prev.filter((item) => item !== normalizedContent)].slice(0, 20));
	}, [setHistory]);
	return {
		isInputHistoryActive: historyIndex !== -1,
		navigateHistory,
		resetHistoryIndex,
		takeDraftBeforeHistory,
		saveHistory
	};
}
const emptyActions = {
	addNewTopic: () => void 0,
	focus: () => void 0,
	onTextChange: () => void 0,
	replaceDraft: () => void 0,
	toggleExpanded: () => void 0,
	removeToken: () => void 0,
	insertToken: () => void 0,
	getDraft: () => ({
		text: "",
		tokens: []
	})
};
function buildComposerQueuedPayload(draft, { files, fileTokenId, requireText = false, extra }) {
	const normalizedDraft = trimComposerDraftBoundaryBlankLines(draft);
	const hasText = normalizedDraft.text.trim().length > 0;
	const text = hasText ? normalizedDraft.text : "";
	const tokenIds = getComposerTokenIds(normalizedDraft.tokens);
	const attachedFiles = files.filter((file) => tokenIds.has(fileTokenId(file)));
	if (hasUnsyncedComposerAttachments(files, attachedFiles)) return null;
	if (requireText ? !hasText : !hasText && attachedFiles.length === 0) return null;
	const userMessageParts = createComposerUserMessageParts(normalizedDraft);
	return {
		text,
		attachments: attachedFiles.length ? attachedFiles : void 0,
		userMessageParts,
		...extra?.(tokenIds, attachedFiles)
	};
}
function getComposerHistoryText(userMessageParts) {
	return getComposerTextFromParts(userMessageParts, (token) => token.kind === "knowledge" ? "" : token.promptText ?? "");
}
function hasUnsyncedComposerAttachments(files, attachedFiles) {
	return attachedFiles.length !== files.length;
}
const createQuoteToken = (selectedText, label) => ({
	id: `quote:${Date.now()}:${Math.random().toString(36).slice(2)}`,
	kind: "quote",
	label,
	description: selectedText,
	promptText: formatQuoteTokenPromptText(selectedText)
});
function useComposerQuoteInsertion(actionsRef) {
	const { t: t$2 } = useTranslation();
	const insertQuote = (0, import_react.useEffectEvent)((selectedText) => {
		if (!selectedText) return;
		actionsRef.current.insertToken(createQuoteToken(selectedText, t$2("selection.action.builtin.quote")));
	});
	(0, import_react.useEffect)(() => {
		return window.electron?.ipcRenderer.on(IpcChannel.App_QuoteToMain, (_$1, selectedText) => {
			insertQuote(selectedText);
		});
	}, [insertQuote]);
}
var SLIDER_EFFORT_ORDER = [
	"default",
	"none",
	"auto",
	"minimal",
	"low",
	"medium",
	"high",
	"xhigh",
	"max"
];
var EFFORT_LABEL_KEYS = {
	default: "assistants.settings.reasoning_effort.default",
	none: "assistants.settings.reasoning_effort.off",
	minimal: "assistants.settings.reasoning_effort.minimal",
	low: "assistants.settings.reasoning_effort.low",
	medium: "assistants.settings.reasoning_effort.medium",
	high: "assistants.settings.reasoning_effort.high",
	xhigh: "assistants.settings.reasoning_effort.xhigh",
	max: "assistants.settings.reasoning_effort.max",
	auto: "assistants.settings.reasoning_effort.auto"
};
function resolveComposerReasoningEffort(model, effort) {
	return (deriveThinkingOptions(model) ?? []).includes(effort) ? effort : "default";
}
function ComposerSpeedControl({ model, reasoningEffort, fastMode, onReasoningEffortChange, onFastModeChange }) {
	const { t: t$2 } = useTranslation();
	const reasoningOptions = (0, import_react.useMemo)(() => {
		const declaredEfforts = new Set(deriveThinkingOptions(model) ?? []);
		return SLIDER_EFFORT_ORDER.filter((effort) => declaredEfforts.has(effort));
	}, [model]);
	const sliderEfforts = reasoningOptions.filter((effort) => effort !== "default");
	const supportsReasoning = reasoningOptions.length > 1;
	const showEffortSlider = sliderEfforts.filter((effort) => effort !== "none" && effort !== "auto").length > 1;
	const supportsFast = model.supportsFastMode === true;
	if (!supportsReasoning && !supportsFast) return null;
	const effectiveReasoningEffort = resolveComposerReasoningEffort(model, reasoningEffort);
	const selectedOption = supportsReasoning ? effectiveReasoningEffort : void 0;
	const defaultSliderEffort = model.reasoning?.defaultEffort;
	const sliderSelection = effectiveReasoningEffort === "default" && defaultSliderEffort !== void 0 && sliderEfforts.includes(defaultSliderEffort) ? defaultSliderEffort : effectiveReasoningEffort;
	const selectedIndex = sliderSelection === "default" ? -1 : sliderEfforts.indexOf(sliderSelection);
	const currentIndex = selectedIndex >= 0 ? selectedIndex : 0;
	const displayedEffort = showEffortSlider ? effectiveReasoningEffort : selectedOption;
	const effortLabel = displayedEffort ? t$2(EFFORT_LABEL_KEYS[displayedEffort]) : "";
	const triggerLabel = supportsReasoning ? effortLabel : fastMode ? t$2("agent.speed.fast") : t$2("agent.speed.label");
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverTrigger, {
		asChild: true,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
			type: "button",
			variant: "ghost",
			size: "sm",
			className: "h-8 gap-1 rounded-md px-2.5 text-muted-foreground text-xs hover:text-foreground",
			"aria-label": t$2("agent.speed.title"),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Gauge, {
					size: 14,
					className: "shrink-0"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: triggerLabel }),
				supportsReasoning && fastMode && supportsFast ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: ["· ", t$2("agent.speed.fast")] }) : null,
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown, {
					size: 13,
					className: "shrink-0 text-muted-foreground"
				})
			]
		}) })
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(PopoverContent, {
		side: "top",
		align: "end",
		sideOffset: 8,
		className: "w-56 overflow-hidden rounded-md border-frame-border p-1.5 text-xs shadow-xl",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex h-10 items-center px-2",
			children: [supportsReasoning ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex min-w-0 items-baseline gap-1 text-xs",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
					className: "shrink-0 text-muted-foreground",
					children: [t$2("agent.speed.effort"), ":"]
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					"data-ui": "chat.composer-speed-control.composer-effort-slider-label",
					"data-testid": "composer-effort-slider-label",
					"aria-live": "polite",
					className: "truncate font-medium text-foreground",
					children: effortLabel
				})]
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "text-muted-foreground",
				children: t$2("agent.speed.label")
			}), supportsFast ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				type: "button",
				variant: "ghost",
				size: "icon-sm",
				className: cn("ml-auto rounded-full", fastMode && "text-primary hover:text-primary"),
				"aria-label": t$2("agent.speed.fast"),
				"aria-pressed": fastMode,
				onClick: () => onFastModeChange(!fastMode),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Zap, {
					size: 14,
					fill: fastMode ? "currentColor" : "none"
				})
			}) : null]
		}), supportsReasoning && showEffortSlider ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "mx-2.5 mt-1 mb-2",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex items-center justify-between font-medium text-[11px]",
				"aria-hidden": "true",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "text-muted-foreground",
					children: t$2("agent.speed.faster")
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "text-primary",
					children: t$2("agent.speed.smarter")
				})]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "relative mt-1.5 h-8",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Slider, {
					value: [currentIndex],
					min: 0,
					max: sliderEfforts.length - 1,
					step: 1,
					size: "lg",
					getThumbAriaLabel: () => t$2("agent.speed.effort"),
					getThumbAriaValueText: () => effortLabel,
					className: cn("h-8", "[&_[data-slot=slider-track]]:h-2.5 [&_[data-slot=slider-track]]:bg-muted [&_[data-slot=slider-track]]:shadow-inner", "[&_[data-slot=slider-range]]:bg-primary", "[&_[data-slot=slider-thumb]]:z-20 [&_[data-slot=slider-thumb]]:size-5 [&_[data-slot=slider-thumb]]:rounded-full", "[&_[data-slot=slider-thumb]]:border-border [&_[data-slot=slider-thumb]]:bg-popover! [&_[data-slot=slider-thumb]]:shadow-sm", "[&_[data-slot=slider-thumb]:hover]:ring-0"),
					onValueChange: ([index]) => {
						const effort = sliderEfforts[index];
						if (effort) onReasoningEffortChange(effort);
					}
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "pointer-events-none absolute inset-x-3 top-1/2 z-10 h-0",
					children: sliderEfforts.map((effort, index) => index === currentIndex ? null : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						"data-ui": "chat.composer-speed-control.composer-effort-step part:composer-effort-step",
						"data-slot": "composer-effort-step",
						"data-index": index,
						className: "-translate-x-1/2 -translate-y-1/2 absolute size-1 rounded-full bg-background",
						style: { left: `${index / (sliderEfforts.length - 1) * 100}%` }
					}, effort))
				})]
			})]
		}) : supportsReasoning ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroup, {
			value: displayedEffort,
			"aria-label": t$2("agent.speed.effort"),
			className: "gap-0",
			onValueChange: (effort) => onReasoningEffortChange(effort),
			children: reasoningOptions.map((effort) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", {
				className: "flex h-8 cursor-pointer items-center gap-2 rounded-sm px-2 text-xs hover:bg-accent",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroupItem, {
					value: effort,
					size: "sm"
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t$2(EFFORT_LABEL_KEYS[effort]) })]
			}, effort))
		}) : null]
	})] });
}
var CUSTOMIZE_ROW_CLASS = "flex h-8 items-center gap-1.5 rounded-md px-1.5 hover:bg-accent/60";
var CUSTOMIZE_ROW_ICON_CLASS = "flex size-5 shrink-0 items-center justify-center text-muted-foreground [&_svg]:!size-[16px]";
var haveSameOrder = (left, right) => left.length === right.length && left.every((id, index) => id === right[index]);
var reconcileCustomizeOrder = (preferredOrder, pinnedIds, candidateIds, leadingCandidateIds) => {
	const availableIds = new Set([...pinnedIds, ...candidateIds]);
	const nextOrder = [];
	const seenIds = /* @__PURE__ */ new Set();
	for (const id of [
		...preferredOrder,
		...leadingCandidateIds,
		...pinnedIds,
		...candidateIds
	]) if (availableIds.has(id) && !seenIds.has(id)) {
		seenIds.add(id);
		nextOrder.push(id);
	}
	return nextOrder;
};
const ComposerToolbarShortcuts = ({ scope, pinnedIds, onPinnedIdsChange, onResetPinnedIds, isDefault, customTools, customizeOpen, onCustomizeOpenChange, isModelUnavailable, inputAdapter, unifiedPanelControl }) => {
	const { t: t$2 } = useTranslation();
	const { getLaunchers, dispatchLauncher } = useComposerToolLauncherController();
	const toolLaunchersVersion = useComposerToolLauncherVersion();
	const panelUnavailable = !unifiedPanelControl?.available;
	const toolbarManifests = (0, import_react.useMemo)(() => getComposerToolbarManifestsForScope(scope, t$2), [scope, t$2]);
	const toolbarManifestById = (0, import_react.useMemo)(() => new Map(toolbarManifests.map((manifest) => [manifest.id, manifest])), [toolbarManifests]);
	const toolbarLaunchers = (0, import_react.useMemo)(() => {
		return getLaunchers("popover");
	}, [getLaunchers, toolLaunchersVersion]);
	const lastResolvedIconById = (0, import_react.useRef)(/* @__PURE__ */ new Map());
	(0, import_react.useLayoutEffect)(() => {
		for (const launcher of toolbarLaunchers) if (toolbarManifestById.has(launcher.id)) lastResolvedIconById.current.set(launcher.id, launcher.icon);
	}, [toolbarLaunchers, toolbarManifestById]);
	const candidates = (0, import_react.useMemo)(() => {
		const manifestCandidates = toolbarManifests.map((manifest) => {
			const opensPanel = manifest.kind === "group" || manifest.kind === "panel";
			return {
				id: manifest.id,
				label: manifest.label,
				icon: lastResolvedIconById.current.get(manifest.id) ?? manifest.icon,
				active: false,
				disabled: true,
				haspopup: opensPanel ? "menu" : manifest.kind === "dialog" ? "dialog" : void 0,
				toggle: manifest.kind === "command",
				resolved: false,
				select: () => void 0
			};
		});
		const launcherCandidates = toolbarLaunchers.map((launcher) => {
			const manifest = toolbarManifestById.get(launcher.id);
			const kind = manifest?.kind ?? launcher.kind;
			const opensPanel = kind === "group" || kind === "panel";
			const label = manifest?.label ?? launcher.label;
			return {
				id: launcher.id,
				label,
				icon: launcher.icon,
				active: Boolean(launcher.active),
				disabled: Boolean(launcher.disabled) || opensPanel && panelUnavailable,
				disabledReason: launcher.disabledReason,
				tooltip: launcher.tooltip,
				haspopup: opensPanel ? "menu" : kind === "dialog" ? "dialog" : void 0,
				toggle: kind === "command",
				resolved: true,
				select: opensPanel ? () => unifiedPanelControl?.open({
					launcherId: launcher.id,
					searchText: typeof label === "string" ? label : void 0
				}) : () => dispatchLauncher(launcher, {
					source: "popover",
					inputAdapter
				})
			};
		});
		const customCandidates = (customTools ?? []).map((tool) => {
			const requiresPanel = tool.requiresPanel ?? true;
			return {
				id: tool.id,
				label: tool.label,
				icon: tool.icon,
				customizePlacement: tool.customizePlacement,
				active: false,
				disabled: Boolean(tool.disabled) || requiresPanel && panelUnavailable,
				haspopup: requiresPanel ? "menu" : void 0,
				toggle: false,
				resolved: true,
				availableWithoutModel: tool.availableWithoutModel,
				select: () => tool.onSelect({
					inputAdapter,
					unifiedPanelControl
				})
			};
		});
		const candidateById$1 = /* @__PURE__ */ new Map();
		const candidateOrder = [];
		for (const candidate of [
			...manifestCandidates,
			...customCandidates,
			...launcherCandidates
		]) {
			if (!candidateById$1.has(candidate.id)) candidateOrder.push(candidate.id);
			candidateById$1.set(candidate.id, candidate);
		}
		return candidateOrder.map((id) => candidateById$1.get(id));
	}, [
		customTools,
		dispatchLauncher,
		inputAdapter,
		panelUnavailable,
		toolbarLaunchers,
		toolbarManifestById,
		toolbarManifests,
		unifiedPanelControl
	]);
	const candidateById = (0, import_react.useMemo)(() => new Map(candidates.map((candidate) => [candidate.id, candidate])), [candidates]);
	const pinnedRows = (0, import_react.useMemo)(() => pinnedIds.map((id) => ({
		id,
		candidate: candidateById.get(id)
	})), [candidateById, pinnedIds]);
	const visiblePinnedRows = (0, import_react.useMemo)(() => pinnedRows.filter((row) => row.candidate), [pinnedRows]);
	const pinnedIdSet = (0, import_react.useMemo)(() => new Set(pinnedIds), [pinnedIds]);
	const candidateIds = (0, import_react.useMemo)(() => candidates.map((candidate) => candidate.id), [candidates]);
	const leadingCandidateIds = (0, import_react.useMemo)(() => candidates.filter((candidate) => candidate.customizePlacement === "leading").map((candidate) => candidate.id), [candidates]);
	const [customizeOrderState, setCustomizeOrderState] = (0, import_react.useState)(() => ({
		preferredOrder: [],
		syncedPinnedIds: pinnedIds,
		pendingPinnedIds: null
	}));
	if (!haveSameOrder(customizeOrderState.syncedPinnedIds, pinnedIds)) setCustomizeOrderState({
		preferredOrder: customizeOrderState.pendingPinnedIds !== null && haveSameOrder(customizeOrderState.pendingPinnedIds, pinnedIds) ? customizeOrderState.preferredOrder : [],
		syncedPinnedIds: pinnedIds,
		pendingPinnedIds: null
	});
	const customizeOrderIds = (0, import_react.useMemo)(() => reconcileCustomizeOrder(customizeOrderState.preferredOrder, pinnedIds, candidateIds, leadingCandidateIds), [
		candidateIds,
		customizeOrderState.preferredOrder,
		leadingCandidateIds,
		pinnedIds
	]);
	const customizeRows = (0, import_react.useMemo)(() => customizeOrderIds.map((id) => ({
		id,
		candidate: candidateById.get(id)
	})), [candidateById, customizeOrderIds]);
	const visibleCustomizeRows = (0, import_react.useMemo)(() => customizeRows.filter((row) => row.candidate), [customizeRows]);
	const customizeLabel = t$2("chat.input.toolbar.customize");
	const customizeTitleId = (0, import_react.useId)();
	const requestPinnedIdsChange = (nextPinnedIds) => {
		setCustomizeOrderState((current) => ({
			...current,
			pendingPinnedIds: nextPinnedIds
		}));
		onPinnedIdsChange(nextPinnedIds);
	};
	const togglePinned = (id, checked) => {
		const nextPinnedIdSet = new Set(pinnedIds);
		if (checked) nextPinnedIdSet.add(id);
		else nextPinnedIdSet.delete(id);
		requestPinnedIdsChange(customizeOrderIds.filter((candidateId) => nextPinnedIdSet.has(candidateId)));
	};
	const reorderCustomizeRows = (nextRows) => {
		const nextOrderIds = nextRows.map((row) => row.id);
		setCustomizeOrderState((current) => ({
			...current,
			preferredOrder: nextOrderIds
		}));
		const nextPinnedIds = nextOrderIds.filter((id) => pinnedIdSet.has(id));
		if (nextPinnedIds.length !== pinnedIds.length || nextPinnedIds.some((id, index) => id !== pinnedIds[index])) requestPinnedIdsChange(nextPinnedIds);
	};
	const resetPinnedIds = () => {
		setCustomizeOrderState({
			preferredOrder: [],
			syncedPinnedIds: pinnedIds,
			pendingPinnedIds: null
		});
		onResetPinnedIds();
	};
	const showModelRequiredToast = () => toast.error(t$2("code.model_required"));
	const dragAccessibility = (0, import_react.useMemo)(() => {
		const nameOf = (id) => {
			const label = candidateById.get(String(id))?.label;
			return typeof label === "string" ? label : String(id);
		};
		return {
			screenReaderInstructions: { draggable: t$2("chat.input.toolbar.drag.instructions") },
			announcements: {
				onDragStart: ({ active }) => t$2("chat.input.toolbar.drag.picked_up", { name: nameOf(active.id) }),
				onDragOver: ({ active, over }) => over ? t$2("chat.input.toolbar.drag.over", {
					name: nameOf(active.id),
					over: nameOf(over.id)
				}) : void 0,
				onDragEnd: ({ active }) => t$2("chat.input.toolbar.drag.dropped", { name: nameOf(active.id) }),
				onDragCancel: ({ active }) => t$2("chat.input.toolbar.drag.cancelled", { name: nameOf(active.id) })
			}
		};
	}, [candidateById, t$2]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, {
		open: customizeOpen,
		onOpenChange: onCustomizeOpenChange,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverAnchor, {
			asChild: true,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex min-h-8 shrink-0 items-center gap-1.5",
				children: visiblePinnedRows.map(({ candidate }) => {
					const shortcut = candidate;
					const blockedByMissingModel = isModelUnavailable && !shortcut.availableWithoutModel;
					return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						content: shortcut.disabled && shortcut.disabledReason ? shortcut.disabledReason : shortcut.tooltip ?? shortcut.label,
						placement: "top",
						isDisabled: blockedByMissingModel,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							type: "button",
							variant: "ghost",
							size: "icon-sm",
							className: cn(COMPOSER_SEND_ACCESSORY_BUTTON_CLASS, "disabled:pointer-events-none", !shortcut.resolved && "disabled:opacity-100", shortcut.active && "bg-accent"),
							"aria-label": typeof shortcut.label === "string" ? shortcut.label : void 0,
							"aria-haspopup": blockedByMissingModel ? void 0 : shortcut.haspopup,
							"aria-pressed": !blockedByMissingModel && shortcut.toggle && shortcut.resolved ? shortcut.active : void 0,
							disabled: !blockedByMissingModel && shortcut.disabled,
							"data-active": shortcut.active || void 0,
							onClick: blockedByMissingModel ? showModelRequiredToast : shortcut.select,
							children: shortcut.icon
						})
					}, shortcut.id);
				})
			}) })
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(PopoverContent, {
			side: "top",
			sideOffset: 8,
			align: "start",
			className: "w-64 p-1.5",
			"aria-labelledby": customizeTitleId,
			onFocusOutside: (event) => event.preventDefault(),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					id: customizeTitleId,
					className: "px-2 py-1 font-medium text-foreground text-sm",
					children: customizeLabel
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReorderableList, {
					items: customizeRows,
					visibleItems: visibleCustomizeRows,
					getId: (row) => row.id,
					onReorder: reorderCustomizeRows,
					direction: "vertical",
					gap: 1,
					dragHandle: true,
					accessibility: dragAccessibility,
					itemStyle: { cursor: "default" },
					renderItem: (row, _index, { dragging, dragHandleProps }) => {
						const candidate = row.candidate;
						if (!candidate) return null;
						const label = typeof candidate.label === "string" ? candidate.label : void 0;
						return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: CUSTOMIZE_ROW_CLASS,
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
									type: "button",
									ref: dragHandleProps?.ref,
									...dragHandleProps?.attributes,
									...dragHandleProps?.listeners,
									"data-dragging": dragging ? "true" : "false",
									"aria-label": t$2("chat.input.toolbar.drag_handle", { name: label ?? "" }),
									className: "flex size-5 shrink-0 cursor-grab touch-none items-center justify-center rounded-md text-muted-foreground outline-none transition-colors duration-150 hover:text-foreground focus-visible:bg-accent focus-visible:text-foreground active:cursor-grabbing data-[dragging=true]:cursor-grabbing data-[dragging=true]:text-foreground",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GripVertical, { className: "size-3.5" })
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: CUSTOMIZE_ROW_ICON_CLASS,
									children: candidate.icon
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: "min-w-0 flex-1 truncate text-sm",
									children: candidate.label
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Switch, {
									size: "xs",
									checked: pinnedIdSet.has(row.id),
									"data-tool-toggle-id": row.id,
									"aria-label": label,
									onCheckedChange: (checked) => togglePinned(row.id, checked)
								})
							]
						});
					}
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "mx-1.5 mt-1 border-border border-t pt-1",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						type: "button",
						variant: "ghost",
						size: "sm",
						className: "h-7 w-full justify-start px-1.5 text-muted-foreground text-sm hover:text-foreground",
						disabled: isDefault,
						onClick: resetPinnedIds,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RotateCcw, { className: "size-4" }), t$2("chat.input.toolbar.restore_default")]
					})
				})
			]
		})]
	});
};
var EMPTY_MODELS = [];
var ALL_FILE_EXTS = [
	...imageExts,
	...audioExts,
	...videoExts,
	...documentExts,
	...textExts,
	...archiveExts
];
var MEDIA_INPUT_PREDICATES = {
	audio: [isAudioModel, isAudioModels],
	video: [isVideoModel, isVideoModels]
};
function isMultiModelArgs(input) {
	return !!input && Array.isArray(input.models);
}
function useComposerFileCapabilities(input) {
	const isChatSurface = isMultiModelArgs(input);
	const { models, fallbackModel } = isChatSurface ? input : {
		models: EMPTY_MODELS,
		fallbackModel: input
	};
	return (0, import_react.useMemo)(() => {
		if (!isChatSurface) {
			const enabled = fallbackModel != null;
			return {
				canAddImageFile: enabled,
				canAddTextFile: enabled,
				supportedExts: enabled ? [...ALL_FILE_EXTS] : []
			};
		}
		const supports = ([single, plural]) => models.length > 0 ? plural(models) : fallbackModel ? single(fallbackModel) : false;
		const audio = supports(MEDIA_INPUT_PREDICATES.audio);
		const video = supports(MEDIA_INPUT_PREDICATES.video);
		return {
			canAddImageFile: true,
			canAddTextFile: true,
			supportedExts: [
				...imageExts,
				...audio ? audioExts : [],
				...video ? videoExts : [],
				...documentExts,
				...textExts
			]
		};
	}, [
		isChatSurface,
		models,
		fallbackModel
	]);
}
var KNOWLEDGE_BASE_IDS_KEY_SEPARATOR = "\0";
function useComposerKnowledgeBaseScope({ configuredKnowledgeBaseIds, allKnowledgeBases, isKnowledgeBasesLoading, scopeKey, selectedKnowledgeBases, setSelectedKnowledgeBases, remountsOnScopeChange }) {
	const selectedKnowledgeBasesScopeKeyRef = (0, import_react.useRef)(remountsOnScopeChange ? scopeKey : null);
	const configuredKnowledgeBaseIdsKey = (configuredKnowledgeBaseIds ?? []).join(KNOWLEDGE_BASE_IDS_KEY_SEPARATOR);
	const configuredKnowledgeBaseIdSet = (0, import_react.useMemo)(() => new Set(configuredKnowledgeBaseIdsKey ? configuredKnowledgeBaseIdsKey.split(KNOWLEDGE_BASE_IDS_KEY_SEPARATOR) : []), [configuredKnowledgeBaseIdsKey]);
	const availableKnowledgeBaseIdsKey = (0, import_react.useMemo)(() => allKnowledgeBases.map((base) => base.id).join(KNOWLEDGE_BASE_IDS_KEY_SEPARATOR), [allKnowledgeBases]);
	const availableKnowledgeBaseIdSet = (0, import_react.useMemo)(() => new Set(availableKnowledgeBaseIdsKey ? availableKnowledgeBaseIdsKey.split(KNOWLEDGE_BASE_IDS_KEY_SEPARATOR) : []), [availableKnowledgeBaseIdsKey]);
	const filterSelectableKnowledgeBases = (0, import_react.useCallback)((bases) => {
		if (configuredKnowledgeBaseIdSet.size === 0) return bases.filter((base) => isKnowledgeBasesLoading || availableKnowledgeBaseIdSet.has(base.id));
		return bases.filter((base) => configuredKnowledgeBaseIdSet.has(base.id) && (isKnowledgeBasesLoading || availableKnowledgeBaseIdSet.has(base.id)));
	}, [
		availableKnowledgeBaseIdSet,
		configuredKnowledgeBaseIdSet,
		isKnowledgeBasesLoading
	]);
	const selectableKnowledgeBases = (0, import_react.useMemo)(() => filterSelectableKnowledgeBases(allKnowledgeBases), [allKnowledgeBases, filterSelectableKnowledgeBases]);
	const knowledgeBaseMarkerMap = (0, import_react.useMemo)(() => {
		const map = /* @__PURE__ */ new Map();
		selectableKnowledgeBases.forEach((base) => {
			map.set(base.id, base);
			map.set(base.name, base);
			map.set(composerKnowledgeBaseTokenId(base), base);
		});
		return map;
	}, [selectableKnowledgeBases]);
	const resolveKnowledgeBaseMarker = (0, import_react.useCallback)((marker) => {
		const base = knowledgeBaseMarkerMap.get(marker);
		return base ? knowledgeBaseToComposerToken(base) : null;
	}, [knowledgeBaseMarkerMap]);
	const isSelectedKnowledgeBasesScopeCurrent = selectedKnowledgeBasesScopeKeyRef.current === scopeKey;
	const selectedKnowledgeBasesInScope = (0, import_react.useMemo)(() => isSelectedKnowledgeBasesScopeCurrent ? filterSelectableKnowledgeBases(selectedKnowledgeBases) : [], [
		filterSelectableKnowledgeBases,
		isSelectedKnowledgeBasesScopeCurrent,
		selectedKnowledgeBases
	]);
	const restoreKnowledgeBaseSelection = (0, import_react.useCallback)((baseIds) => {
		const wanted = new Set(baseIds);
		setSelectedKnowledgeBases(selectableKnowledgeBases.filter((base) => wanted.has(base.id)));
	}, [selectableKnowledgeBases, setSelectedKnowledgeBases]);
	(0, import_react.useEffect)(() => {
		const scopeChanged = selectedKnowledgeBasesScopeKeyRef.current !== scopeKey;
		selectedKnowledgeBasesScopeKeyRef.current = scopeKey;
		setSelectedKnowledgeBases((prev) => {
			const next = scopeChanged ? [] : filterSelectableKnowledgeBases(prev);
			if (next.length === prev.length && next.every((base, index) => base.id === prev[index]?.id)) return prev;
			return next;
		});
	}, [
		filterSelectableKnowledgeBases,
		scopeKey,
		setSelectedKnowledgeBases
	]);
	return {
		selectableKnowledgeBases,
		selectedKnowledgeBasesInScope,
		resolveKnowledgeBaseMarker,
		restoreKnowledgeBaseSelection
	};
}
function useComposerToolbarPinnedTools(prefKey) {
	const { t: t$2 } = useTranslation();
	const [pinnedIds, persistPinnedIds] = usePreference(prefKey);
	const [customizeOpen, setCustomizeOpen] = (0, import_react.useState)(false);
	const setPinnedIds = (0, import_react.useCallback)((next) => {
		persistPinnedIds(next).catch(() => {
			toast.error(t$2("common.error"));
		});
	}, [persistPinnedIds, t$2]);
	const defaultPinnedIds = (0, import_react.useMemo)(() => getDefaultValue(prefKey), [prefKey]);
	const isDefault = (0, import_react.useMemo)(() => pinnedIds.length === defaultPinnedIds.length && pinnedIds.every((id, i$1) => id === defaultPinnedIds[i$1]), [defaultPinnedIds, pinnedIds]);
	return {
		pinnedIds,
		setPinnedIds,
		resetPinnedIds: (0, import_react.useCallback)(() => setPinnedIds([...defaultPinnedIds]), [defaultPinnedIds, setPinnedIds]),
		isDefault,
		customizeOpen,
		setCustomizeOpen,
		customizePanelItem: (0, import_react.useMemo)(() => {
			return {
				id: "composer:customize-toolbar",
				label: t$2("chat.input.toolbar.customize"),
				icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Settings2, { size: 16 }),
				fixedToBottom: true,
				action: () => setCustomizeOpen(true)
			};
		}, [t$2])
	};
}
strictObject({
	cursor: string().optional(),
	messageId: string().min(1).optional(),
	limit: number().int().positive().max(200).optional(),
	deferToolOutputs: boolean().optional()
});
var AgentSessionMessageBaseSchema = strictObject({
	role: ContentMessageRoleSchema,
	data: MessageDataSchema,
	status: MessageStatusSchema,
	modelId: string().nullable(),
	messageSnapshot: MessageSnapshotSchema.nullable(),
	stats: MessageStatsSchema.nullable()
});
AgentSessionMessageBaseSchema.extend({
	id: string(),
	sessionId: string(),
	searchableText: string(),
	runtimeResumeToken: string().nullable(),
	createdAt: datetime(),
	updatedAt: datetime()
});
const CreateAgentSessionMessageSchema = AgentSessionMessageBaseSchema.pick({
	modelId: true,
	messageSnapshot: true
}).partial().extend({
	id: string().optional(),
	role: ContentMessageRoleSchema,
	data: MessageDataSchema,
	status: MessageStatusSchema.optional()
});
strictObject({
	sessionId: string(),
	runtimeResumeToken: string().optional(),
	messages: array(CreateAgentSessionMessageSchema)
});
AgentSessionMessageBaseSchema.pick({ data: true });
var logger$11 = loggerService.withContext("agentSessionExport");
function getAgentSessionExportTitle(session) {
	return session.name.trim() || instance.t("agent.session.new") || session.id;
}
function modelSnapshotToModel(snapshot) {
	if (!snapshot) return void 0;
	return {
		id: snapshot.id,
		name: snapshot.name,
		provider: snapshot.provider,
		group: snapshot.group ?? ""
	};
}
function agentSessionMessageToExportView(row, agentId, modelFallback) {
	let modelSnapshot = row.messageSnapshot?.model;
	if (!modelSnapshot && row.modelId && isUniqueModelId(row.modelId)) {
		const { providerId, modelId } = parseUniqueModelId(row.modelId);
		modelSnapshot = {
			id: modelId,
			name: modelId,
			provider: providerId
		};
	}
	if (!modelSnapshot && row.role === "assistant") modelSnapshot = modelFallback;
	return {
		id: row.id,
		role: row.role,
		assistantId: agentId ?? void 0,
		topicId: buildAgentSessionTopicId(row.sessionId),
		createdAt: row.createdAt,
		updatedAt: row.updatedAt,
		status: row.status,
		modelId: row.modelId ?? void 0,
		model: modelSnapshotToModel(modelSnapshot),
		messageSnapshot: row.messageSnapshot ?? void 0,
		stats: row.stats ?? void 0,
		parts: row.data.parts ?? []
	};
}
async function getAgentSessionMessagesForExport(session, options = {}) {
	const pages = [];
	let cursor;
	let collected = 0;
	do {
		const query = cursor ? {
			limit: 200,
			cursor
		} : { limit: 200 };
		const response = await dataApiService.get(`/agent-sessions/${session.id}/messages`, { query });
		pages.push(response.items.map((row) => agentSessionMessageToExportView(row, session.agentId, options.modelFallback)));
		collected += response.items.length;
		cursor = response.nextCursor;
	} while (cursor && (!options.maxMessages || collected < options.maxMessages));
	return pages.reverse().flatMap((page) => page.reverse());
}
async function agentSessionToMarkdown(session, exportReasoning, excludeCitations, options = {}) {
	const title = getAgentSessionExportTitle(session);
	const messages = await getAgentSessionMessagesForExport(session, options);
	if (messages.length === 0) return `# ${title}`;
	return `# ${title}\n\n${await messagesToMarkdown(messages, exportReasoning, excludeCitations)}`;
}
async function agentSessionToPlainText(session, options = {}) {
	const title = markdownToPlainText(getAgentSessionExportTitle(session)).trim();
	const messages = await getAgentSessionMessagesForExport(session, options);
	if (messages.length === 0) return title;
	return `${title}\n\n${messagesToPlainText(messages)}`;
}
async function copyAgentSessionAsMarkdown(session, options = {}) {
	try {
		const markdown = await agentSessionToMarkdown(session, void 0, void 0, options);
		await navigator.clipboard.writeText(markdown);
		toast.success(instance.t("message.copy.success"));
	} catch (error) {
		logger$11.error("Failed to copy agent session as markdown", error, { sessionId: session.id });
		toast.error(instance.t("common.copy_failed"));
	}
}
async function copyAgentSessionAsPlainText(session, options = {}) {
	try {
		const plainText = await agentSessionToPlainText(session, options);
		await navigator.clipboard.writeText(plainText);
		toast.success(instance.t("message.copy.success"));
	} catch (error) {
		logger$11.error("Failed to copy agent session as plain text", error, { sessionId: session.id });
		toast.error(instance.t("common.copy_failed"));
	}
}
async function exportAgentSessionAsMarkdown(session, exportReasoning, excludeCitations, options = {}) {
	try {
		const markdown = await agentSessionToMarkdown(session, exportReasoning, excludeCitations, options);
		await exportMarkdownContentAsFile(getAgentSessionExportTitle(session), markdown);
	} catch (error) {
		logger$11.error("Failed to export agent session as markdown", error, { sessionId: session.id });
		toast.error(instance.t("chat.topics.export.failed"));
	}
}
const REFERENCE_CONTEXT_MAX_TOTAL_CHARS = 8e3;
const REFERENCE_CONTEXT_MAX_MESSAGE_CHARS = 2e3;
var REFERENCE_CONTEXT_MAX_MESSAGES = 200;
function buildEntityReferencePromptText(options) {
	const maxTotal = options.maxTotalChars ?? 8e3;
	const maxPerMessage = options.maxMessageChars ?? 2e3;
	const usable = options.entries.filter((entry) => (entry.role === "user" || entry.role === "assistant") && entry.text.trim().length > 0);
	const kept = [];
	let used = 0;
	for (let index = usable.length - 1; index >= 0; index--) {
		const entry = usable[index];
		const text = entry.text.length > maxPerMessage ? `${entry.text.slice(0, maxPerMessage)}…` : entry.text;
		const block = `[${entry.role}]\n${text}`;
		if (kept.length > 0 && used + block.length > maxTotal) break;
		kept.unshift(block);
		used += block.length + 2;
	}
	const openTag = `<referenced-conversation type="${options.entityType}" name="${options.name.replace(/"/g, "'")}">`;
	const body = kept.length > 0 ? kept.join("\n\n") : "[empty]";
	return `${openTag}${kept.length < usable.length ? `\n[showing the ${kept.length} most recent of ${usable.length} messages]\n` : "\n"}${body}\n</referenced-conversation>`;
}
async function fetchEntityReferencePromptText(target, options = {}) {
	const messages = target.entityType === "topic" ? await getTopicMessages(target.id, { maxMessages: REFERENCE_CONTEXT_MAX_MESSAGES }) : await getAgentSessionMessagesForExport({
		id: target.id,
		agentId: target.agentId,
		name: target.name
	}, { maxMessages: REFERENCE_CONTEXT_MAX_MESSAGES });
	const maxTotalChars = Math.min(options.maxTotalChars ?? 8e3, REFERENCE_CONTEXT_MAX_TOTAL_CHARS);
	return buildEntityReferencePromptText({
		name: target.name,
		entityType: target.entityType,
		entries: messages.map((message) => ({
			role: message.role,
			text: getNamingTextContent(message)
		})),
		maxTotalChars,
		maxMessageChars: Math.min(maxTotalChars, REFERENCE_CONTEXT_MAX_MESSAGE_CHARS)
	});
}
var REFERENCE_RESULT_LIMIT = 50;
var REFERENCE_LIST_FETCH_LIMIT = 200;
var REFERENCE_MIN_ROOM_CHARS = 1e3;
var referenceTokenId = (entityType, id) => `reference:${entityType}:${id}`;
async function fetchReferenceHits(entityType, q$1) {
	if (!q$1) {
		if (entityType === "topic") return (await dataApiService.get("/topics", { query: { limit: REFERENCE_LIST_FETCH_LIMIT } })).items.toSorted((a$1, b$1) => b$1.updatedAt.localeCompare(a$1.updatedAt)).slice(0, REFERENCE_RESULT_LIMIT).map((topic) => ({
			id: topic.id,
			title: topic.name,
			agentId: null
		}));
		return (await dataApiService.get("/agent-sessions", { query: { limit: REFERENCE_LIST_FETCH_LIMIT } })).items.toSorted((a$1, b$1) => b$1.updatedAt.localeCompare(a$1.updatedAt)).slice(0, REFERENCE_RESULT_LIMIT).map((session) => ({
			id: session.id,
			title: session.name,
			agentId: session.agentId
		}));
	}
	const response = await dataApiService.get("/search/entities", { query: {
		q: q$1,
		types: [entityType],
		limitPerType: REFERENCE_RESULT_LIMIT
	} });
	const hits = [];
	for (const group of response.groups) if (group.type === "topic") for (const hit of group.items) hits.push({
		id: hit.id,
		title: hit.title,
		subtitle: hit.subtitle,
		agentId: null
	});
	else if (group.type === "session") for (const hit of group.items) hits.push({
		id: hit.id,
		title: hit.title,
		subtitle: hit.subtitle,
		agentId: hit.target.agentId
	});
	return hits;
}
function settlePendingReferenceToken(editor, tokenId, promptText) {
	if (editor.isDestroyed) return;
	const matches = [];
	editor.state.doc.descendants((node, position) => {
		if (node.type.name === "composerToken" && node.attrs.id === tokenId) matches.push({
			position,
			node
		});
	});
	const match = matches[0];
	if (!match) return;
	const transaction = editor.state.tr;
	if (promptText === null) transaction.delete(match.position, match.position + match.node.nodeSize);
	else transaction.setNodeMarkup(match.position, void 0, {
		...match.node.attrs,
		promptText
	});
	editor.view.dispatch(transaction);
}
function useEntityReferenceMentionItems({ entityType, excludeId }) {
	const { t: t$2 } = useTranslation();
	const stateRef = (0, import_react.useRef)({
		entityType,
		excludeId,
		t: t$2
	});
	stateRef.current = {
		entityType,
		excludeId,
		t: t$2
	};
	const [pendingCount, setPendingCount] = (0, import_react.useState)(0);
	return {
		getItems: (0, import_react.useCallback)(async ({ query, editor }) => {
			const { entityType: entityType$1, excludeId: excludeId$1, t: t$3 } = stateRef.current;
			const icon = entityType$1 === "topic" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageSquare, { size: 16 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MousePointerClick, { size: 16 });
			const untitledLabel = entityType$1 === "topic" ? t$3("chat.conversation.new") : t$3("agent.session.new");
			const hits = await fetchReferenceHits(entityType$1, query.trim());
			const insertedTokenIds = new Set(serializeComposerDocument(editor).tokens.map((token) => token.id));
			return hits.filter((hit) => hit.id !== excludeId$1).map((hit) => {
				const tokenId = referenceTokenId(entityType$1, hit.id);
				const title = hit.title.trim() || untitledLabel;
				return {
					id: tokenId,
					label: title,
					description: hit.subtitle,
					icon,
					filterText: `${title} ${hit.subtitle ?? ""}`,
					disabled: insertedTokenIds.has(tokenId),
					command: ({ editor: editor$1 }) => {
						const draft = serializeComposerDocument(editor$1);
						if (draft.tokens.some((token) => token.id === tokenId)) return;
						const remainingChars = COMPOSER_INPUT_MAX_LENGTH - draft.text.length;
						if (remainingChars < REFERENCE_MIN_ROOM_CHARS) {
							toast.error(t$3("chat.input.reference_panel.no_room"));
							return;
						}
						editor$1.chain().focus().insertComposerToken({
							id: tokenId,
							kind: "reference",
							label: title,
							description: hit.subtitle ? `${title} · ${hit.subtitle}` : title,
							promptText: "",
							payload: {
								entityType: entityType$1,
								id: hit.id,
								name: title
							}
						}).insertContent(" ").run();
						setPendingCount((count) => count + 1);
						(async () => {
							try {
								settlePendingReferenceToken(editor$1, tokenId, await fetchEntityReferencePromptText(entityType$1 === "topic" ? {
									entityType: entityType$1,
									id: hit.id,
									name: title
								} : {
									entityType: entityType$1,
									id: hit.id,
									name: title,
									agentId: hit.agentId
								}, { maxTotalChars: remainingChars }));
							} catch {
								settlePendingReferenceToken(editor$1, tokenId, null);
								toast.error(t$3("chat.input.reference_panel.load_failed"));
							} finally {
								setPendingCount((count) => count - 1);
							}
						})();
					}
				};
			});
		}, []),
		hasPendingReference: pendingCount > 0
	};
}
function useEntityReferenceMentionSource(options) {
	const { entityType } = options;
	const { t: t$2 } = useTranslation();
	const { getItems, hasPendingReference } = useEntityReferenceMentionItems(options);
	const getItemsWithEmptyState = (0, import_react.useCallback)(async (args) => {
		const items = await getItems(args);
		if (items.length > 0) return items;
		return [{
			id: "entity-reference:no-results",
			label: t$2(`chat.input.reference_panel.${entityType}.no_results.label`),
			description: t$2(`chat.input.reference_panel.${entityType}.no_results.description`),
			icon: entityType === "topic" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageSquare, { size: 16 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MousePointerClick, { size: 16 }),
			disabled: true,
			command: () => void 0
		}];
	}, [
		entityType,
		getItems,
		t$2
	]);
	return {
		sources: (0, import_react.useMemo)(() => [{
			pluginKey: "entity-reference-mention-suggestion",
			char: "@",
			title: t$2(`chat.input.reference_panel.${entityType}.title`),
			allowedPrefixes: [" ", "\n"],
			items: getItemsWithEmptyState
		}], [
			entityType,
			getItemsWithEmptyState,
			t$2
		]),
		hasPendingReference
	};
}
var useIsomorphicLayoutEffect = typeof window !== "undefined" ? import_react.useLayoutEffect : import_react.useEffect;
function useLatest(value) {
	const ref = (0, import_react.useRef)(value);
	useIsomorphicLayoutEffect(() => {
		ref.current = value;
	}, [value]);
	return ref;
}
function getMessageDeleteUnavailableText(reason, t$2) {
	if (reason === "root-unavailable") return t$2("message.delete.root_unavailable");
	if (reason === "message-unavailable") return t$2("message.delete.root_unavailable");
	if (reason === "first-turn") return t$2("message.delete.first_turn_not_supported");
}
var MultiSelectActionPopup = ({ selectedMessageIds, isMultiSelectMode, onSave, onCopy, onDelete, deleteDisabledReason, onClose }) => {
	const { t: t$2 } = useTranslation();
	if (!isMultiSelectMode) return null;
	const isActionDisabled = selectedMessageIds.length === 0;
	const deleteTooltip = getMessageDeleteUnavailableText(deleteDisabledReason, t$2) ?? t$2("common.delete");
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container$1, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ActionBar, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectionCount, { children: t$2("common.selectedMessages", { count: selectedMessageIds.length }) }),
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ActionButtons, { children: [
			onSave && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
				content: t$2("common.save"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					className: "rounded-full",
					variant: "ghost",
					disabled: isActionDisabled,
					onClick: onSave,
					size: "icon",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Save, { size: 16 })
				})
			}),
			onCopy && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
				content: t$2("common.copy"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					className: "rounded-full",
					variant: "ghost",
					disabled: isActionDisabled,
					onClick: onCopy,
					size: "icon",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyIcon_default, { size: 16 })
				})
			}),
			onDelete && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
				content: deleteTooltip,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					className: "rounded-full",
					variant: "ghost",
					disabled: isActionDisabled || !!deleteDisabledReason,
					onClick: onDelete,
					size: "icon",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeleteIcon_default, {
						size: 16,
						className: "lucide-custom"
					})
				})
			})
		] }),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
			content: t$2("chat.navigation.close"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				className: "rounded-full",
				variant: "ghost",
				onClick: onClose,
				size: "icon",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { size: 16 })
			})
		})
	] }) });
};
var Container$1 = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.multi-select-action-popup",
	className: cn("fixed inset-x-0 bottom-0 z-300 flex items-center justify-center p-4", className),
	...mergeUiProps(props, "chat.multi-select-action-popup")
});
var ActionBar = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.action-bar",
	className: cn("flex items-center justify-between gap-4 rounded-[99px] border-[0.5px] border-border", "bg-background p-1 shadow-md", className),
	...mergeUiProps(props, "chat.action-bar")
});
var ActionButtons = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.action-buttons",
	className: cn("flex items-center gap-2", className),
	...mergeUiProps(props, "chat.action-buttons")
});
var SelectionCount = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.selection-count",
	className: cn("shrink-0 pl-2 text-[14px] text-muted-foreground", className),
	...mergeUiProps(props, "chat.selection-count")
});
var MultiSelectActionPopup_default = MultiSelectActionPopup;
var MessageOutline = ({ message, multiModelMessageStyle, onNavigateToElement }) => {
	const messageParts = useMessageParts(message.id);
	const headings = (0, import_react.useMemo)(() => {
		const textEntries = [];
		let idx = 0;
		for (const part of messageParts) {
			if (part.type === "text" && "text" in part) {
				const text = part.text.trim();
				if (text.length > 0) textEntries.push({
					id: `${message.id}-part-${idx}`,
					content: text
				});
			}
			idx++;
		}
		if (!textEntries.length) return [];
		const result = [];
		for (const entry of textEntries) {
			const tree = unified().use(remarkParse).parse(entry.content);
			const slugger = createSlugger();
			visit(tree, ["heading", "html"], (node) => {
				if (node.type === "heading") {
					const level = node.depth ?? 0;
					if (!level || level < 1 || level > 6) return;
					const text = extractTextFromNode(node);
					if (!text) return;
					const id = `heading-${entry.id}--` + slugger.slug(text || "");
					result.push({
						id,
						level,
						text
					});
				} else if (node.type === "html") {
					const match = node.value.match(/<h([1-6])[^>]*>(.*?)<\/h\1>/i);
					if (match) {
						const level = parseInt(match[1], 10);
						const text = match[2].replace(/<[^>]*>/g, "").trim();
						if (text) {
							const id = `heading-${entry.id}--${slugger.slug(text || "")}`;
							result.push({
								id,
								level,
								text
							});
						}
					}
				}
			});
		}
		return result;
	}, [message.id, messageParts]);
	const miniLevel = (0, import_react.useMemo)(() => {
		return headings.length ? Math.min(...headings.map((heading) => heading.level)) : 1;
	}, [headings]);
	const scrollToHeading = (id) => {
		const messageContentContainer = document.getElementById(`message-${message.id}`)?.querySelector(".message-content-container");
		if (messageContentContainer) {
			const headingElement = messageContentContainer.querySelector(`#${id}`);
			if (headingElement) if (multiModelMessageStyle === "horizontal") scrollIntoView(headingElement, {
				behavior: "smooth",
				block: "nearest",
				container: "nearest"
			});
			else onNavigateToElement(headingElement);
		}
	};
	if (multiModelMessageStyle === "grid" || !headings.length) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.message-outline",
		className: "pointer-events-none absolute inset-y-0 right-0 left-2 z-999 flex items-center",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(scrollbar_default, {
			className: "group pointer-events-auto inline-flex max-h-[70vh] max-w-1/2 flex-col gap-1 overflow-x-hidden overflow-y-hidden rounded-[10px] border border-transparent px-0 pt-2.5 pr-0 pb-2.5 pl-2.5 hover:overflow-y-auto hover:border-border-subtle hover:bg-popover hover:px-2.5 hover:shadow-[0_0_10px_0_rgba(128,128,128,0.2)]",
			children: headings.map((heading, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex h-6 shrink-0 cursor-pointer items-center gap-2 [&:hover_.outline-dot]:bg-secondary [&:hover_.outline-text]:text-muted-foreground",
				onClick: () => scrollToHeading(heading.id),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "mr-1 h-1 shrink-0 rounded-[2px] bg-muted outline-dot transition-colors duration-200 ease-out",
					style: { width: `${16 - heading.level * 2}px` }
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "hidden truncate whitespace-nowrap px-2 py-0.5 text-foreground-tertiary opacity-0 outline-text transition-opacity duration-200 ease-out group-hover:block group-hover:opacity-100",
					style: {
						fontSize: `${16 - heading.level}px`,
						paddingLeft: `${(heading.level - miniLevel) * 8}px`
					},
					children: heading.text
				})]
			}, index))
		})
	});
};
var MessageOutline_default = import_react.memo(MessageOutline);
const MessagesContainer = ({ className, $right, ...props }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.message-list",
		className: cn$1("relative flex h-full flex-col overflow-hidden", className),
		...mergeUiProps(props, "chat.message-list")
	});
};
var TICK_BASE_WIDTH = 6;
var TICK_PEAK_WIDTH = 20;
var TICK_WAVE_BONUS = 10;
var HOVER_FALLOFF_DISTANCE = 56;
var FOCUS_MAX_DISTANCE = 24;
var PREVIEW_EDGE_INSET = 56;
var PREVIEW_MAX_CHARS = 240;
var RAIL_MIN_HEIGHT_PX = 220;
var RAIL_MIN_EDGE_MARGIN_PX = 24;
var RAIL_TICK_PITCH_PX = 10;
var RAIL_FADE_PX = 44;
var RAIL_MIN_TURNS = 5;
var EMPTY_MESSAGE_PARTS$1 = [];
var tickTransitionClassName = "transition-[width,height,background-color] duration-150 ease-[cubic-bezier(0.25,1,0.5,1)] [will-change:width]";
var MessageAnchorLine = (0, import_react.memo)(function MessageAnchorLine$1({ messages, activeMessageId, railOpacity = 1, hasOlder = false, historyPartsByMessageId, liveMessageIds, scrollToMessageId }) {
	const { t: t$2 } = useTranslation();
	const liveMessageIdSet = (0, import_react.useMemo)(() => new Set(liveMessageIds), [liveMessageIds]);
	const wrapperRef = (0, import_react.useRef)(null);
	const railScrollRef = (0, import_react.useRef)(null);
	const latestClientYRef = (0, import_react.useRef)(null);
	const animationFrameRef = (0, import_react.useRef)(null);
	const [railHeight, setRailHeight] = (0, import_react.useState)(0);
	const [scrollTop, setScrollTop] = (0, import_react.useState)(0);
	const [mouseY, setMouseY] = (0, import_react.useState)(null);
	const visible = railOpacity > .02 && !(railHeight > 0 && railHeight < RAIL_MIN_HEIGHT_PX);
	const hitStripWidth = 8 + Math.round(railOpacity * 24);
	const turns = (0, import_react.useMemo)(() => {
		const result = [];
		let current = null;
		let assistantOnActiveBranch = false;
		for (const message of messages) {
			if (message.isContextBoundary) continue;
			if (message.role === "user") {
				current = {
					anchorId: message.id,
					userMessageId: message.id,
					memberIds: [message.id]
				};
				assistantOnActiveBranch = false;
				result.push(current);
				continue;
			}
			if (!current) {
				current = {
					anchorId: message.id,
					memberIds: []
				};
				assistantOnActiveBranch = false;
				result.push(current);
			}
			current.memberIds.push(message.id);
			if (message.role === "assistant" && !assistantOnActiveBranch) {
				if (message.isActiveBranch) {
					current.assistantMessageId = message.id;
					assistantOnActiveBranch = true;
				} else if (!current.assistantMessageId) current.assistantMessageId = message.id;
			}
		}
		return result;
	}, [messages]);
	const turnIndexByMessageId = (0, import_react.useMemo)(() => {
		const map = /* @__PURE__ */ new Map();
		turns.forEach((turn, index) => turn.memberIds.forEach((id) => map.set(id, index)));
		return map;
	}, [turns]);
	const activeTurnIndex = activeMessageId != null ? turnIndexByMessageId.get(activeMessageId) ?? turns.length - 1 : turns.length - 1;
	const alignToBottomRef = (0, import_react.useRef)(hasOlder);
	const geometry = (0, import_react.useMemo)(() => {
		const count = turns.length;
		const viewport = Math.max(0, railHeight - RAIL_MIN_EDGE_MARGIN_PX * 2);
		const content = count * RAIL_TICK_PITCH_PX;
		const free = Math.max(0, viewport - content);
		const padTop = alignToBottomRef.current ? free : free / 2;
		const padBottom = free - padTop;
		const centerOf = (index) => RAIL_MIN_EDGE_MARGIN_PX + padTop + index * RAIL_TICK_PITCH_PX + RAIL_TICK_PITCH_PX / 2 - scrollTop;
		return {
			padTop,
			padBottom,
			centerOf
		};
	}, [
		turns.length,
		railHeight,
		scrollTop
	]);
	const focusedIndex = (0, import_react.useMemo)(() => {
		if (mouseY === null || turns.length === 0 || railHeight === 0) return null;
		const raw = Math.round((mouseY - geometry.centerOf(0)) / RAIL_TICK_PITCH_PX);
		const index = Math.min(Math.max(raw, 0), turns.length - 1);
		return Math.abs(mouseY - geometry.centerOf(index)) <= FOCUS_MAX_DISTANCE ? index : null;
	}, [
		mouseY,
		turns.length,
		railHeight,
		geometry
	]);
	const flushMouseMove = (0, import_react.useCallback)(() => {
		animationFrameRef.current = null;
		const wrapper = wrapperRef.current;
		const clientY = latestClientYRef.current;
		if (!wrapper || clientY === null) return;
		setMouseY(clientY - wrapper.getBoundingClientRect().top);
	}, []);
	const handleMouseMove = (0, import_react.useCallback)((event) => {
		latestClientYRef.current = event.clientY;
		if (animationFrameRef.current !== null) return;
		animationFrameRef.current = requestAnimationFrame(flushMouseMove);
	}, [flushMouseMove]);
	const clearPointerState = (0, import_react.useCallback)(() => {
		latestClientYRef.current = null;
		if (animationFrameRef.current !== null) {
			cancelAnimationFrame(animationFrameRef.current);
			animationFrameRef.current = null;
		}
		setMouseY(null);
	}, []);
	const handleScroll = (event) => setScrollTop(event.currentTarget.scrollTop);
	(0, import_react.useEffect)(() => {
		if (!visible) clearPointerState();
	}, [clearPointerState, visible]);
	(0, import_react.useEffect)(() => () => {
		if (animationFrameRef.current !== null) cancelAnimationFrame(animationFrameRef.current);
	}, []);
	const hasRail = turns.length >= RAIL_MIN_TURNS;
	const scrollAnchorRef = (0, import_react.useRef)({
		firstId: null,
		count: 0,
		entered: false
	});
	(0, import_react.useLayoutEffect)(() => {
		const el = railScrollRef.current;
		const anchor = scrollAnchorRef.current;
		const firstId = turns[0]?.anchorId ?? null;
		if (el) {
			const maxScroll$1 = Math.max(0, el.scrollHeight - el.clientHeight);
			const added = turns.length - anchor.count;
			if (!anchor.entered) {
				el.scrollTop = maxScroll$1;
				anchor.entered = true;
			} else if (added > 0 && firstId !== anchor.firstId) el.scrollTop += added * RAIL_TICK_PITCH_PX;
			else if (added > 0 && maxScroll$1 - el.scrollTop <= added * RAIL_TICK_PITCH_PX + 1) el.scrollTop = maxScroll$1;
			setScrollTop(el.scrollTop);
		}
		anchor.firstId = firstId;
		anchor.count = turns.length;
	}, [turns, railHeight]);
	(0, import_react.useLayoutEffect)(() => {
		if (!hasRail) return;
		const wrapper = wrapperRef.current;
		if (!wrapper || typeof ResizeObserver === "undefined") return;
		const update = () => setRailHeight(wrapper.getBoundingClientRect().height);
		update();
		const observer = new ResizeObserver(update);
		observer.observe(wrapper);
		return () => observer.disconnect();
	}, [hasRail]);
	if (!hasRail) return null;
	const isHovering = mouseY !== null;
	const focusedTurn = focusedIndex !== null ? turns[focusedIndex] : null;
	const cardTop = focusedIndex !== null ? Math.min(Math.max(geometry.centerOf(focusedIndex), PREVIEW_EDGE_INSET), Math.max(railHeight - PREVIEW_EDGE_INSET, PREVIEW_EDGE_INSET)) : 0;
	const waveBonus = (index) => {
		if (mouseY === null) return 0;
		return TICK_WAVE_BONUS * Math.max(0, 1 - Math.abs(geometry.centerOf(index) - mouseY) / HOVER_FALLOFF_DISTANCE) ** 1.5;
	};
	const railViewport = Math.max(0, railHeight - RAIL_MIN_EDGE_MARGIN_PX * 2);
	const maxScroll = Math.max(0, turns.length * RAIL_TICK_PITCH_PX - railViewport);
	const fadeTop = scrollTop > 1 || hasOlder;
	const fadeBottom = scrollTop < maxScroll - 1;
	const railMask = fadeTop || fadeBottom ? `linear-gradient(to bottom, ${fadeTop ? "transparent" : "black"} 0%, black ${fadeTop ? RAIL_FADE_PX : 0}px, black calc(100% - ${fadeBottom ? RAIL_FADE_PX : 0}px), ${fadeBottom ? "transparent" : "black"} 100%)` : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.message-anchor-line",
		ref: wrapperRef,
		className: classNames("group absolute top-2.5 right-4 bottom-8 z-20 select-none", !visible && "pointer-events-none"),
		inert: !visible,
		style: { width: hitStripWidth },
		onMouseMove: handleMouseMove,
		onMouseLeave: clearPointerState,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			ref: railScrollRef,
			onScroll: handleScroll,
			className: classNames("absolute inset-x-0 flex flex-col items-end overflow-y-auto transition-opacity duration-150 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden"),
			style: {
				top: RAIL_MIN_EDGE_MARGIN_PX,
				bottom: RAIL_MIN_EDGE_MARGIN_PX,
				opacity: (visible ? railOpacity : 0) * (isHovering ? 1 : .7),
				maskImage: railMask,
				WebkitMaskImage: railMask
			},
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex w-full flex-col items-end",
				style: {
					paddingTop: geometry.padTop,
					paddingBottom: geometry.padBottom
				},
				children: turns.map((turn, index) => {
					const isActive = index === activeTurnIndex;
					const isFocused = index === focusedIndex;
					const width = isHovering ? isFocused ? TICK_PEAK_WIDTH : TICK_BASE_WIDTH + waveBonus(index) : TICK_BASE_WIDTH;
					const emphasized = focusedIndex !== null ? isFocused : isActive;
					return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						type: "button",
						"data-message-anchor-tick": true,
						"data-active": isActive,
						"aria-label": t$2("chat.navigation.anchor.jump_to_turn", { number: index + 1 }),
						"aria-current": isActive ? "true" : void 0,
						className: "flex w-full shrink-0 cursor-pointer items-center justify-end rounded-sm outline-none focus-visible:ring-1 focus-visible:ring-ring",
						style: { height: RAIL_TICK_PITCH_PX },
						onClick: () => scrollToMessageId?.(turn.anchorId),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: classNames("rounded-full", tickTransitionClassName, isFocused ? "h-0.5" : "h-[1.5px]", emphasized ? "bg-foreground" : "bg-border-strong"),
							style: { width }
						})
					}, turn.anchorId);
				})
			})
		}), focusedTurn && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageAnchorPreviewCard, {
			turn: focusedTurn,
			top: cardTop,
			historyPartsByMessageId,
			liveMessageIdSet
		})]
	});
});
var MessageAnchorPreviewCard = ({ turn, top, ...preview }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
	"data-ui": "chat.message-anchor-preview-card",
	className: "-translate-y-1/2 pointer-events-none absolute right-full z-30 flex w-max max-w-80 flex-col gap-1 rounded-xl border-[0.5px] border-border bg-popover p-3 text-popover-foreground shadow-lg transition-[top] duration-150 ease-[cubic-bezier(0.25,1,0.5,1)] empty:hidden",
	style: { top },
	children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(AnchorPreviewLine, {
		...preview,
		messageId: turn.userMessageId,
		className: "line-clamp-1 break-all font-medium text-foreground text-sm"
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AnchorPreviewLine, {
		...preview,
		messageId: turn.assistantMessageId,
		className: "line-clamp-2 break-all text-muted-foreground text-sm leading-5"
	})]
});
var AnchorPreviewLine = ({ messageId, historyPartsByMessageId, liveMessageIdSet, className }) => {
	if (!messageId) return null;
	if (liveMessageIdSet.has(messageId)) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LiveMessagePreview, {
		messageId,
		className
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessagePreview, {
		parts: historyPartsByMessageId[messageId] ?? EMPTY_MESSAGE_PARTS$1,
		className
	});
};
var LiveMessagePreview = ({ messageId, className }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessagePreview, {
		parts: useMessageParts(messageId),
		className
	});
};
var MessagePreview = (0, import_react.memo)(function MessagePreview$1({ parts, className }) {
	const preview = getTextPreview(parts);
	if (!preview) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.message-anchor-line",
		className,
		children: preview
	});
});
function getTextPreview(parts) {
	let preview = "";
	for (const part of parts) {
		if (part.type !== "text") continue;
		const text = part.text;
		if (!/\S/.test(text)) continue;
		if (preview.length > 0) {
			preview += "\n\n";
			if (preview.length >= PREVIEW_MAX_CHARS) return preview.slice(0, PREVIEW_MAX_CHARS);
		}
		preview += text.slice(0, PREVIEW_MAX_CHARS - preview.length);
		if (preview.length >= PREVIEW_MAX_CHARS) return preview;
	}
	return preview;
}
var MessageAnchorLine_default = MessageAnchorLine;
var logger$10 = loggerService.withContext("SiblingNavigator");
var SiblingNavigator = ({ messageId }) => {
	const { t: t$2 } = useTranslation();
	const actions = useMessageListActions();
	const siblings = useMessageListUi().getMessageSiblings?.(messageId);
	const handleSwitch = (0, import_react.useCallback)(async (direction) => {
		if (!siblings || !actions.setActiveBranch) return;
		const { group: group$1, activeIndex: activeIndex$1 } = siblings;
		const target = group$1[(activeIndex$1 + direction + group$1.length) % group$1.length];
		try {
			await actions.setActiveBranch(target.id);
		} catch (error) {
			logger$10.error("Failed to switch sibling branch", error);
			actions.notifyError?.(error instanceof Error ? error.message : String(error));
		}
	}, [actions, siblings]);
	if (!siblings) return null;
	const { group, activeIndex } = siblings;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.sibling-navigator",
		className: "inline-flex select-none items-center gap-0.5 text-[11px] text-muted-foreground leading-none",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				className: "flex size-4.5 items-center justify-center rounded transition-colors hover:bg-muted hover:text-foreground",
				onClick: () => void handleSwitch(-1),
				"aria-label": t$2("common.previous"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronLeft, { size: 12 })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
				className: "min-w-8 text-center font-mono tabular-nums",
				children: [
					activeIndex + 1,
					"/",
					group.length
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				className: "flex size-4.5 items-center justify-center rounded transition-colors hover:bg-muted hover:text-foreground",
				onClick: () => void handleSwitch(1),
				"aria-label": t$2("common.next"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRight, { size: 12 })
			})
		]
	});
};
var SiblingNavigator_default = SiblingNavigator;
const MESSAGE_AVATAR_CONTAINER_CLASS = "message-avatar flex size-[30px] shrink-0 items-center justify-center overflow-hidden rounded-full p-0";
const MESSAGE_AVATAR_INNER_CLASS = "size-full rounded-full p-0";
const MESSAGE_MODEL_AVATAR_ICON_CLASS = "size-full";
const MessageAvatarFrame = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.message-avatar-frame",
	className: cn$1(MESSAGE_AVATAR_CONTAINER_CLASS, className),
	...mergeUiProps(props, "chat.message-avatar-frame")
});
var MessageAvatar = ({ avatar = "", fallback, fallbackAvatarStyle, className, onClick, ...props }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageAvatarFrame, {
		className: cn$1(!!onClick && "cursor-pointer", className),
		onClick,
		...props,
		children: isEmoji(avatar) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmojiIcon_default, {
			emoji: avatar,
			className: "mr-0",
			size: 30,
			fontSize: 17
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Avatar, {
			className: MESSAGE_AVATAR_INNER_CLASS,
			style: !avatar ? fallbackAvatarStyle : void 0,
			children: [avatar && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AvatarImage, {
				className: "size-full object-cover p-0",
				src: avatar
			}), fallback !== void 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AvatarFallback, {
				className: "size-full rounded-full p-0",
				children: fallback
			})]
		})
	});
};
var MessageAvatar_default = MessageAvatar;
function intervalFromRuntimeSpan(span, rangeEnd) {
	const completedAt = span.completedAt ?? rangeEnd;
	if (completedAt <= span.startedAt) return void 0;
	return {
		id: span.id,
		lane: span.kind === "tool-execution" ? "tool" : "approval",
		label: span.toolName ?? (span.kind === "tool-execution" ? span.toolCallId : span.approvalId),
		startedAt: span.startedAt,
		completedAt
	};
}
function complementIntervals(startedAt, completedAt, measured) {
	const merged = [];
	for (const interval of [...measured].sort((left, right) => left.startedAt - right.startedAt)) {
		const clipped = {
			startedAt: Math.max(startedAt, interval.startedAt),
			completedAt: Math.min(completedAt, interval.completedAt)
		};
		if (clipped.completedAt <= clipped.startedAt) continue;
		const tail = merged.at(-1);
		if (tail && clipped.startedAt <= tail.completedAt) tail.completedAt = Math.max(tail.completedAt, clipped.completedAt);
		else merged.push(clipped);
	}
	const gaps = [];
	let cursor = startedAt;
	for (const interval of merged) {
		if (interval.startedAt > cursor) gaps.push({
			id: `other:${gaps.length}`,
			lane: "other",
			startedAt: cursor,
			completedAt: interval.startedAt
		});
		cursor = Math.max(cursor, interval.completedAt);
	}
	if (cursor < completedAt) gaps.push({
		id: `other:${gaps.length}`,
		lane: "other",
		startedAt: cursor,
		completedAt
	});
	return gaps;
}
function buildRuntimeViewModel(stats, records, now) {
	const runtime = stats.runtimeTiming;
	const rangeEnd = runtime.completedAt ?? now;
	const runtimeIntervals = runtime.spans.map((span) => intervalFromRuntimeSpan(span, rangeEnd)).filter((span) => span !== void 0);
	const modelIntervals = [];
	for (const record of records) {
		if (record.recordKind !== "invocation") continue;
		const completedAt = Date.parse(record.createdAt);
		const fullDurationMs = record.timeCompletionMs ?? void 0;
		const startedAt = Number.isFinite(completedAt) && fullDurationMs !== void 0 ? completedAt - fullDurationMs : void 0;
		const label = record.modelName ?? record.modelId ?? record.providerName ?? record.providerId ?? record.modality;
		if (startedAt !== void 0 && completedAt > startedAt) modelIntervals.push({
			id: record.id,
			lane: "model",
			label,
			startedAt,
			completedAt
		});
	}
	const measuredOutputTokens = stats.providerPerformance?.measuredOutputTokens;
	const generationDuration = stats.providerPerformance?.generationDurationMs;
	const modelTokensPerSecond = measuredOutputTokens !== void 0 && generationDuration !== void 0 && generationDuration > 0 ? measuredOutputTokens / (generationDuration / 1e3) : void 0;
	const totalDurationMs = runtime.completedAt !== void 0 ? Math.max(0, runtime.completedAt - runtime.startedAt) : void 0;
	const endToEndTokensPerSecond = totalDurationMs !== void 0 && totalDurationMs > 0 && stats.outputTokens !== void 0 ? stats.outputTokens / (totalDurationMs / 1e3) : void 0;
	const measuredIntervals = [...modelIntervals, ...runtimeIntervals];
	return {
		startedAt: runtime.startedAt,
		completedAt: rangeEnd,
		totalDurationMs,
		modelTokensPerSecond,
		endToEndTokensPerSecond,
		intervals: [...measuredIntervals, ...complementIntervals(runtime.startedAt, rangeEnd, measuredIntervals)]
	};
}
function buildLegacyViewModel(stats) {
	const completion = stats.timeCompletionMs;
	if (completion === void 0 || completion <= 0) return { intervals: [] };
	const firstToken = Math.min(stats.timeFirstTokenMs ?? 0, completion);
	const reasoning = Math.min(Math.max(stats.timeThinkingMs ?? 0, 0), completion);
	const waiting = Math.max(0, firstToken - Math.min(reasoning, firstToken));
	const outputTokens = stats.outputTokens;
	const generationDuration = completion - firstToken;
	const modelTokensPerSecond = outputTokens !== void 0 && outputTokens > 0 && generationDuration > 0 ? outputTokens / (generationDuration / 1e3) : void 0;
	const endToEndTokensPerSecond = outputTokens !== void 0 && outputTokens > 0 ? outputTokens / (completion / 1e3) : void 0;
	const intervals = [];
	if (waiting > 0) intervals.push({
		id: "legacy-waiting-first-token",
		lane: "other",
		label: "waiting-first-token",
		startedAt: 0,
		completedAt: waiting
	});
	if (reasoning > 0) intervals.push({
		id: "legacy-reasoning-time",
		lane: "model",
		label: "reasoning-time",
		startedAt: waiting,
		completedAt: Math.min(completion, waiting + reasoning)
	});
	const textStartedAt = Math.min(completion, waiting + reasoning);
	if (completion > textStartedAt) intervals.push({
		id: "legacy-text-generation",
		lane: "model",
		label: "text-generation",
		startedAt: textStartedAt,
		completedAt: completion
	});
	return {
		startedAt: 0,
		completedAt: completion,
		totalDurationMs: completion,
		modelTokensPerSecond,
		endToEndTokensPerSecond,
		intervals
	};
}
function buildMessagePerformanceViewModel(stats, records = [], now = Date.now()) {
	return stats.runtimeTiming ? buildRuntimeViewModel(stats, records, now) : buildLegacyViewModel(stats);
}
function getMessageModelTokensPerSecond(stats) {
	if (stats.runtimeTiming) {
		const outputTokens = stats.providerPerformance?.measuredOutputTokens;
		const durationMs = stats.providerPerformance?.generationDurationMs;
		return outputTokens !== void 0 && durationMs !== void 0 && durationMs > 0 ? outputTokens / (durationMs / 1e3) : void 0;
	}
	return buildLegacyViewModel(stats).modelTokensPerSecond;
}
function InspectableMetricBar({ id, title, summary, segments, formatValue, formatPercent, showAllDetails }) {
	const [activeSegmentId, setActiveSegmentId] = (0, import_react.useState)();
	const visibleSegments = segments.filter((segment) => segment.value > 0);
	const total = visibleSegments.reduce((sum, segment) => sum + segment.value, 0);
	const activeSegment = visibleSegments.find((segment) => segment.id === activeSegmentId);
	if (total <= 0) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
		"data-ui": "chat.inspectable-metric-bar",
		"data-testid": `metric-bar-${id}`,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-testid": `metric-detail-${id}`,
				className: "flex h-5 min-w-0 items-center justify-between gap-3 text-xs leading-5",
				children: activeSegment ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "truncate font-medium text-foreground",
					children: activeSegment.label
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
					className: "shrink-0 text-muted-foreground tabular-nums",
					children: [
						formatValue(activeSegment.value),
						" · ",
						formatPercent(activeSegment.value / total)
					]
				})] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "truncate text-muted-foreground",
					children: title
				}), summary ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "shrink-0 text-foreground tabular-nums",
					children: summary
				}) : null] })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex h-10 w-full items-stretch",
				"aria-hidden": "true",
				onPointerLeave: () => setActiveSegmentId(void 0),
				children: visibleSegments.map((segment, index) => {
					const isActive = activeSegmentId === segment.id;
					const isDimmed = activeSegmentId !== void 0 && !isActive;
					const ratio = segment.value / total;
					return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						"data-testid": `metric-segment-${id}-${segment.id}`,
						className: "relative h-10 min-w-px",
						style: { width: `${ratio * 100}%` },
						onPointerEnter: () => setActiveSegmentId(segment.id),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: cn$1("-translate-y-1/2 absolute inset-x-0 top-1/2 h-2 transition-opacity duration-150", index === 0 && "rounded-l-full", index === visibleSegments.length - 1 && "rounded-r-full", segment.colorClassName, isDimmed && "opacity-35") })
					}, segment.id);
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex flex-wrap gap-x-3 gap-y-0.5",
				children: visibleSegments.map((segment) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "inline-flex items-center gap-1.5 text-[11px] text-foreground-tertiary leading-4",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: cn$1("size-1.5 rounded-full", segment.colorClassName),
							"aria-hidden": "true"
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: segment.label }),
						showAllDetails ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
							className: "text-muted-foreground tabular-nums",
							children: [
								formatValue(segment.value),
								" · ",
								formatPercent(segment.value / total)
							]
						}) : null
					]
				}, segment.id))
			})
		]
	});
}
var COST_DISPLAY_FLOOR = 1e-4;
function formatCost(cost, currencyFormatter) {
	if (cost > 0 && cost < COST_DISPLAY_FLOOR) return `<${currencyFormatter.format(COST_DISPLAY_FLOOR)}`;
	return currencyFormatter.format(cost);
}
var PERFORMANCE_LANES = [
	{
		id: "model",
		colorClassName: "bg-blue-500"
	},
	{
		id: "tool",
		colorClassName: "bg-emerald-500"
	},
	{
		id: "approval",
		colorClassName: "bg-amber-500"
	},
	{
		id: "other",
		colorClassName: "bg-neutral-400"
	}
];
function PerformanceTimeline({ performance: performance$1, formatMilliseconds, formatPercent, laneLabel, legacySegmentLabel, showAllDetails }) {
	if (performance$1.startedAt === void 0 || performance$1.completedAt === void 0 || performance$1.completedAt <= performance$1.startedAt) return null;
	const total = performance$1.completedAt - performance$1.startedAt;
	const legacyIntervals = performance$1.intervals.filter((interval) => interval.id.startsWith("legacy-"));
	if (legacyIntervals.length > 0) {
		const colors = {
			"legacy-waiting-first-token": "bg-amber-500",
			"legacy-reasoning-time": "bg-fuchsia-500",
			"legacy-text-generation": "bg-blue-500"
		};
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InspectableMetricBar, {
			id: "request-duration",
			title: legacySegmentLabel("request-duration"),
			segments: legacyIntervals.map((interval) => ({
				id: interval.id.replace("legacy-", ""),
				label: legacySegmentLabel(interval.id),
				value: interval.completedAt - interval.startedAt,
				colorClassName: colors[interval.id] ?? "bg-neutral-400"
			})),
			formatValue: formatMilliseconds,
			formatPercent,
			showAllDetails
		});
	}
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		"data-ui": "chat.performance-timeline.message-performance-timeline",
		className: "space-y-1.5",
		"data-testid": "message-performance-timeline",
		children: PERFORMANCE_LANES.map((lane) => {
			const intervals = performance$1.intervals.filter((interval) => interval.lane === lane.id);
			return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "grid grid-cols-[4.5rem_1fr] items-center gap-2",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "truncate text-[11px] text-foreground-tertiary leading-4",
					children: laneLabel(lane.id)
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "relative h-3 overflow-hidden rounded-sm bg-background-subtle",
					children: intervals.map((interval) => {
						const left = Math.max(0, (interval.startedAt - performance$1.startedAt) / total * 100);
						const width = Math.max(.5, (interval.completedAt - interval.startedAt) / total * 100);
						const intervalLabel = interval.label ?? laneLabel(interval.lane);
						return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: cn$1("absolute inset-y-0 rounded-sm opacity-85", lane.colorClassName),
							style: {
								left: `${left}%`,
								width: `${Math.min(width, 100 - left)}%`
							},
							title: `${intervalLabel} · ${formatMilliseconds(interval.completedAt - interval.startedAt)}`
						}, interval.id);
					})
				})]
			}, lane.id);
		})
	});
}
var MessageTokenDetailsCard = ({ message, records = [], showAllDetails = false }) => {
	const { t: t$2, i18n } = useTranslation();
	const stats = message.stats;
	const model = getMessageListItemModel(message);
	const providerName = useProviderDisplayName(model?.provider);
	const locale = i18n.resolvedLanguage;
	const numberFormatter = (0, import_react.useMemo)(() => new Intl.NumberFormat(locale), [locale]);
	const percentageFormatter = (0, import_react.useMemo)(() => new Intl.NumberFormat(locale, {
		style: "percent",
		maximumFractionDigits: 1
	}), [locale]);
	const decimalFormatter = (0, import_react.useMemo)(() => new Intl.NumberFormat(locale, { maximumFractionDigits: 1 }), [locale]);
	const durationFormatter = (0, import_react.useMemo)(() => createDurationFormatter(locale), [locale]);
	const dateFormatter = (0, import_react.useMemo)(() => new Intl.DateTimeFormat(locale, {
		year: "numeric",
		month: "long",
		day: "numeric",
		hour: "2-digit",
		minute: "2-digit",
		second: "2-digit"
	}), [locale]);
	if (!stats) return null;
	const inputTokens = stats.inputTokens ?? 0;
	const outputTokens = stats.outputTokens ?? 0;
	const totalTokens = stats.totalTokens ?? inputTokens + outputTokens;
	const reasoningTokens = Math.min(Math.max(stats.outputTokenDetails?.reasoningTokens ?? 0, 0), outputTokens);
	const textOutputTokens = Math.max(outputTokens - reasoningTokens, 0);
	const inputBreakdownSegments = [
		{
			id: "uncached",
			label: t$2("chat.message.token_details.uncached"),
			value: stats.inputTokenDetails?.noCacheTokens ?? 0,
			colorClassName: "bg-neutral-400"
		},
		{
			id: "cache-read",
			label: t$2("chat.message.token_details.cache_read"),
			value: stats.inputTokenDetails?.cacheReadTokens ?? 0,
			colorClassName: "bg-teal-500"
		},
		{
			id: "cache-write",
			label: t$2("chat.message.token_details.cache_write"),
			value: stats.inputTokenDetails?.cacheWriteTokens ?? 0,
			colorClassName: "bg-amber-500"
		}
	];
	const performance$1 = buildMessagePerformanceViewModel(stats, records);
	const laneLabels = {
		model: t$2("chat.message.token_details.lane_model"),
		tool: t$2("chat.message.token_details.lane_tool"),
		approval: t$2("chat.message.token_details.lane_approval"),
		other: t$2("chat.message.token_details.lane_other")
	};
	const createdAt = Date.parse(message.createdAt);
	const createdAtLabel = Number.isFinite(createdAt) ? dateFormatter.format(new Date(createdAt)) : void 0;
	const formatTokens = (value) => t$2("chat.message.token_details.tokens", { value: numberFormatter.format(value) });
	const formatPercent = (value) => percentageFormatter.format(value);
	const formatMilliseconds = durationFormatter;
	const costLabel = stats.costs?.map((cost) => formatCost(cost.amount, new Intl.NumberFormat(locale, {
		style: "currency",
		currency: cost.currency,
		maximumFractionDigits: 4
	}))).join(" · ");
	const providerReportedRequestCount = stats.costs?.reduce((sum, cost) => sum + cost.providerReportedRequestCount, 0) ?? 0;
	const computedRequestCount = stats.costs?.reduce((sum, cost) => sum + cost.computedRequestCount, 0) ?? 0;
	const costSourceLabel = providerReportedRequestCount > 0 && computedRequestCount === 0 ? t$2("chat.message.token_details.cost_billed") : computedRequestCount > 0 && providerReportedRequestCount === 0 ? t$2("chat.message.token_details.cost_estimated") : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.message-token-details-card",
		className: "text-popover-foreground",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("header", {
			className: "flex min-w-0 items-start gap-2.5 p-3",
			children: [model ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelAvatar_default, {
				model,
				size: 32,
				className: "-outline-offset-1 shrink-0 outline outline-1 outline-border"
			}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "min-w-0 flex-1",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "truncate font-medium text-foreground text-sm leading-5",
						title: model?.name,
						children: model?.name ?? model?.id ?? message.modelId
					}),
					providerName ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "truncate text-muted-foreground text-xs leading-5",
						title: providerName,
						children: providerName
					}) : null,
					createdAtLabel ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("time", {
						dateTime: message.createdAt,
						className: "block truncate text-[11px] text-foreground-tertiary leading-4",
						title: createdAtLabel,
						children: createdAtLabel
					}) : null
				]
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "space-y-2 border-border-subtle border-t p-3",
			children: [
				costLabel ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					"data-ui": "chat.message-token-details-card.message-cost",
					"data-testid": "message-cost",
					className: "flex h-5 min-w-0 items-center justify-between gap-3 text-xs leading-5",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "truncate text-muted-foreground",
						children: t$2("chat.message.token_details.cost")
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
						className: "flex shrink-0 items-baseline gap-1.5",
						children: [costSourceLabel ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "text-[11px] text-foreground-tertiary leading-4",
							children: costSourceLabel
						}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "text-foreground tabular-nums",
							children: costLabel
						})]
					})]
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InspectableMetricBar, {
					id: "token-usage",
					title: t$2("chat.message.token_details.usage"),
					summary: formatTokens(totalTokens),
					segments: [
						{
							id: "input",
							label: t$2("chat.message.token_details.input"),
							value: inputTokens,
							colorClassName: "bg-blue-500"
						},
						{
							id: "output",
							label: t$2(reasoningTokens > 0 ? "chat.message.token_details.text_output" : "chat.message.token_details.output"),
							value: textOutputTokens,
							colorClassName: "bg-violet-500"
						},
						{
							id: "reasoning",
							label: t$2("chat.message.token_details.reasoning"),
							value: reasoningTokens,
							colorClassName: "bg-fuchsia-500"
						}
					],
					formatValue: formatTokens,
					formatPercent,
					showAllDetails
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InspectableMetricBar, {
					id: "input-breakdown",
					title: t$2("chat.message.token_details.input_breakdown"),
					segments: inputBreakdownSegments,
					formatValue: formatTokens,
					formatPercent,
					showAllDetails
				}),
				performance$1.modelTokensPerSecond !== void 0 || performance$1.endToEndTokensPerSecond !== void 0 || performance$1.totalDurationMs !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
					"data-ui": "chat.message-token-details-card.message-performance-summary",
					className: "space-y-1 border-border-subtle border-t pt-2",
					"data-testid": "message-performance-summary",
					children: [
						performance$1.modelTokensPerSecond !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "flex items-center justify-between gap-3 text-xs leading-5",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "text-muted-foreground",
								children: t$2("chat.message.token_details.model_throughput")
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "text-foreground tabular-nums",
								children: t$2("chat.message.token_details.tokens_per_second_value", { value: decimalFormatter.format(performance$1.modelTokensPerSecond) })
							})]
						}) : null,
						performance$1.endToEndTokensPerSecond !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "flex items-center justify-between gap-3 text-xs leading-5",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "text-muted-foreground",
								children: t$2("chat.message.token_details.end_to_end_throughput")
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "text-foreground tabular-nums",
								children: t$2("chat.message.token_details.tokens_per_second_value", { value: decimalFormatter.format(performance$1.endToEndTokensPerSecond) })
							})]
						}) : null,
						performance$1.totalDurationMs !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "flex items-center justify-between gap-3 text-xs leading-5",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "text-muted-foreground",
								children: t$2("chat.message.token_details.total_duration")
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "text-foreground tabular-nums",
								children: formatMilliseconds(performance$1.totalDurationMs)
							})]
						}) : null
					]
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PerformanceTimeline, {
					performance: performance$1,
					formatMilliseconds,
					formatPercent,
					laneLabel: (lane) => laneLabels[lane],
					legacySegmentLabel: (id) => {
						switch (id) {
							case "legacy-waiting-first-token": return t$2("chat.message.token_details.waiting_first_token");
							case "legacy-reasoning-time": return t$2("chat.message.token_details.reasoning_time");
							case "legacy-text-generation": return t$2("chat.message.token_details.text_generation");
							default: return t$2("chat.message.token_details.request_duration");
						}
					},
					showAllDetails
				})
			]
		})]
	});
};
var MessageTokenDetailsCard_default = MessageTokenDetailsCard;
function getTotalTokens(stats) {
	return stats.totalTokens ?? (stats.inputTokens ?? 0) + (stats.outputTokens ?? 0);
}
function UserMessageTokens({ label, onLocate }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		"data-ui": "chat.user-message-tokens",
		type: "button",
		className: "message-tokens cursor-pointer select-text text-right text-muted-foreground text-xs tabular-nums leading-5 transition-colors duration-150 hover:text-foreground focus-visible:text-foreground focus-visible:underline focus-visible:outline-none",
		onClick: onLocate,
		children: label
	});
}
function AssistantMessageTokens({ label, message, onLocate }) {
	const [showAllDetails, setShowAllDetails] = (0, import_react.useState)(false);
	const [isDetailsOpen, setIsDetailsOpen] = (0, import_react.useState)(false);
	const contentId = (0, import_react.useId)();
	const messageKind = useMessageListMeta().aiUsageMessageKind ?? "chat";
	const { pages, isRefreshing, hasNext, loadNext } = useInfiniteQuery("/ai-usage-records", {
		enabled: isDetailsOpen && message.stats?.runtimeTiming !== void 0,
		query: {
			messageKind,
			messageId: message.id,
			sortBy: "createdAt",
			sortOrder: "asc"
		},
		limit: 200
	});
	const records = useInfiniteFlatItems(pages);
	const requestedPageCountRef = (0, import_react.useRef)(1);
	(0, import_react.useEffect)(() => {
		if (!isDetailsOpen) {
			requestedPageCountRef.current = pages.length;
			return;
		}
		if (isRefreshing || !hasNext || requestedPageCountRef.current > pages.length) return;
		requestedPageCountRef.current = pages.length + 1;
		loadNext();
	}, [
		hasNext,
		isDetailsOpen,
		isRefreshing,
		loadNext,
		pages.length
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(HoverCard, {
		open: isDetailsOpen,
		onOpenChange: setIsDetailsOpen,
		openDelay: 200,
		closeDelay: 100,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(HoverCardTrigger, {
			asChild: true,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				"aria-describedby": showAllDetails ? contentId : void 0,
				className: "message-tokens cursor-pointer select-text text-right text-muted-foreground text-xs tabular-nums leading-5 transition-colors duration-150 hover:text-foreground focus-visible:text-foreground focus-visible:underline focus-visible:outline-none",
				onFocus: () => setShowAllDetails(true),
				onBlur: () => setShowAllDetails(false),
				onClick: onLocate,
				children: label
			}) })
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HoverCardContent, {
			id: contentId,
			side: "top",
			align: "end",
			sideOffset: 8,
			collisionPadding: 12,
			className: "w-[28rem] max-w-(--radix-hover-card-content-available-width) p-0",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageTokenDetailsCard_default, {
				message,
				records,
				showAllDetails
			})
		})]
	});
}
var MessageTokens = ({ message }) => {
	const { t: t$2, i18n } = useTranslation();
	const actions = useMessageListActions();
	const stats = message.stats;
	const compactFormatter = (0, import_react.useMemo)(() => new Intl.NumberFormat(i18n.resolvedLanguage, {
		notation: "compact",
		maximumFractionDigits: 1
	}), [i18n.resolvedLanguage]);
	const decimalFormatter = (0, import_react.useMemo)(() => new Intl.NumberFormat(i18n.resolvedLanguage, { maximumFractionDigits: 1 }), [i18n.resolvedLanguage]);
	if (!stats) return null;
	const totalTokens = getTotalTokens(stats);
	const tokenLabel = t$2("chat.message.token_details.tokens", { value: compactFormatter.format(totalTokens) });
	const locateMessage = () => actions.locateMessage?.(message.id, false);
	if (message.role === "user") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserMessageTokens, {
		label: tokenLabel,
		onLocate: locateMessage
	});
	if (message.role === "assistant") {
		const tokensPerSecond = getMessageModelTokensPerSecond(stats);
		const throughputLabel = tokensPerSecond === void 0 ? void 0 : t$2("chat.message.token_details.tokens_per_second_value", { value: decimalFormatter.format(tokensPerSecond) });
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AssistantMessageTokens, {
			label: throughputLabel ? `${tokenLabel} · ${throughputLabel}` : tokenLabel,
			message,
			onLocate: locateMessage
		});
	}
	return null;
};
var MessageTokens_default = MessageTokens;
var import_dayjs_min$3 = /* @__PURE__ */ __toESM(require_dayjs_min());
var MessageHeader = (0, import_react.memo)(({ model, message, isGroupContextMessage, showModelIdentity = false, actionsSlot, contentSlot, footerSlot }) => {
	const { theme } = useTheme();
	const actions = useMessageListActions();
	const meta = useMessageListMeta();
	const renderConfig = useMessageRenderConfig() ?? defaultMessageRenderConfig;
	const selection = useMessageListSelection();
	const userName = renderConfig.userName;
	const assistantProfile = meta.assistantProfile;
	const { t: t$2 } = useTranslation();
	const isBubbleStyle = renderConfig.messageStyle === "bubble";
	const userAvatar = meta.userProfile?.avatar ?? "";
	const isMultiSelectMode = selection?.isMultiSelectMode ?? false;
	const isSelected = (selection?.selectedMessageIds)?.includes(message.id);
	const displayModel = (0, import_react.useMemo)(() => getMessageListItemModel(message), [message]) ?? model;
	const displayModelName = displayModel?.name || displayModel?.id;
	const ModelIcon = useIcon((0, import_react.useMemo)(() => getModelLogoRef(displayModel), [displayModel]));
	const authorSnapshot = message.messageSnapshot;
	const authorName = authorSnapshot ? authorSnapshot.name : assistantProfile?.name;
	const authorAvatar = authorSnapshot ? authorSnapshot.emoji : assistantProfile?.avatar;
	const getUserName = (0, import_react.useCallback)(() => {
		if (message.role === "assistant") return authorName || displayModel?.name || displayModel?.id || "";
		return userName || t$2("common.you");
	}, [
		authorName,
		displayModel,
		message.role,
		t$2,
		userName
	]);
	const isAssistantMessage = message.role === "assistant";
	const hiddenContentHoverClass = isAssistantMessage ? "group-hover/header:opacity-100" : "group-hover/message:opacity-100";
	const hiddenActionsHoverClass = isAssistantMessage ? "group-hover/header:pointer-events-auto group-hover/header:opacity-100" : "group-hover/message:pointer-events-auto group-hover/message:opacity-100";
	const username = (0, import_react.useMemo)(() => removeLeadingEmoji(getUserName()), [getUserName]);
	const avatarName = (0, import_react.useMemo)(() => firstLetter(authorName ?? username ?? "").toUpperCase(), [authorName, username]);
	const openUserProfile = (0, import_react.useCallback)(() => {
		actions.openUserProfile?.();
	}, [actions]);
	const canOpenUserProfile = !!actions.openUserProfile;
	const hasBodySlot = !!contentSlot || !!footerSlot;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.message-header",
		className: `message-header group/header relative flex gap-2.5 ${hasBodySlot ? "mb-0 items-start" : "mb-2 items-center"}`,
		children: [
			isAssistantMessage ? authorAvatar ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageAvatar_default, {
				avatar: authorAvatar,
				fallback: avatarName
			}) : ModelIcon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageAvatarFrame, {
				className: "bg-background",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelIcon, {
					className: MESSAGE_MODEL_AVATAR_ICON_CLASS,
					"aria-hidden": "true"
				})
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageAvatar_default, {
				fallback: avatarName,
				fallbackAvatarStyle: {
					border: "none",
					filter: theme === "dark" ? "invert(0.05)" : void 0
				}
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageAvatar_default, {
				avatar: userAvatar,
				onClick: canOpenUserProfile ? openUserProfile : void 0
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: hasBodySlot ? "message-body-column flex min-h-0 min-w-0 flex-1 flex-col" : "flex min-w-0 flex-1",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex w-full min-w-0 items-center gap-1.5",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "truncate font-semibold text-sm leading-5",
								style: { color: isBubbleStyle && theme === "dark" ? "white" : "var(--foreground)" },
								children: username
							}),
							isAssistantMessage && showModelIdentity && displayModelName && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
								className: "flex min-w-0 shrink items-center gap-1 text-foreground-tertiary text-xs leading-5",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									"aria-hidden": "true",
									className: "shrink-0",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelAvatar_default, {
										className: "rounded-full",
										model: displayModel,
										size: 16
									})
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: "truncate",
									children: displayModelName
								})]
							}),
							isGroupContextMessage && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
								content: t$2("chat.message.useful.tip"),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Sparkle, {
									className: "shrink-0",
									fill: "var(--primary)",
									strokeWidth: 0,
									size: 16
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: `message-header-info-wrap flex shrink-0 items-center gap-1 text-[10px] text-foreground-tertiary leading-none opacity-0 transition-opacity duration-150 focus-within:opacity-100 ${hiddenContentHoverClass}`,
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: (0, import_dayjs_min$3.default)(message?.updatedAt ?? message.createdAt).format("MM/DD HH:mm") }), renderConfig.showEstimatedTokens && isBubbleStyle && !isAssistantMessage && message.stats !== void 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: ["|", /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageTokens_default, { message })] })]
							}),
							actionsSlot && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: `message-header-actions pointer-events-none ml-auto flex shrink-0 items-center gap-1 opacity-0 transition-opacity duration-150 focus-within:pointer-events-auto focus-within:opacity-100 ${hiddenActionsHoverClass}`,
								children: actionsSlot
							})
						]
					}),
					contentSlot && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "message-body-content mt-2 min-h-0 min-w-0 max-w-full flex-1",
						children: contentSlot
					}),
					footerSlot && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "message-footer-slot mt-auto min-w-0 shrink-0",
						children: footerSlot
					})
				]
			}),
			isMultiSelectMode && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Checkbox, {
				"data-message-select-checkbox": true,
				checked: isSelected,
				onCheckedChange: (checked) => actions.selectMessage?.(message.id, checked === true),
				className: "absolute top-0 right-0"
			})
		]
	});
});
MessageHeader.displayName = "MessageHeader";
var MessageHeader_default = MessageHeader;
const DEFAULT_MESSAGE_MENUBAR_BUTTON_IDS = [
	"copy",
	"user-edit",
	"assistant-regenerate",
	"assistant-mention-model",
	"translate",
	"useful",
	"notes",
	"delete",
	"more-menu"
];
const STREAMING_DISABLED_BUTTON_IDS = new Set([
	"user-edit",
	"delete",
	"assistant-regenerate"
]);
const MessageActionButton = ({ active, children, className, softHoverBg, type, ...props }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionIconButton_default, {
		type: type ?? "button",
		active,
		icon: children,
		className: classNames("flex size-6.5 items-center justify-center rounded-md border-0 bg-transparent p-1 text-muted-foreground! transition-all duration-150 ease-out", "[&_svg]:!size-[15px] [&_.icon-at]:text-[9px] [&_.icon]:text-current! [&_.iconfont]:text-[9px] [&_.iconfont]:text-current! [&_.lucide]:text-current!", "enabled:cursor-pointer enabled:hover:text-foreground!", "enabled:[&_.iconfont]:cursor-pointer enabled:[&_svg]:cursor-pointer", softHoverBg ? "enabled:hover:bg-muted" : "enabled:hover:bg-accent", "disabled:cursor-not-allowed disabled:opacity-40", active && "text-primary!", className),
		...props
	});
};
var isMessageMenuBarTranslationDivider = (item) => "type" in item && item.type === "divider";
var ConfirmActionButton = ({ children, destructive, title, confirmText, disabled, onConfirm, onOpenChange }) => {
	const { t: t$2 } = useTranslation();
	const [open, setOpen] = (0, import_react.useState)(false);
	const handleOpenChange = (nextOpen) => {
		if (nextOpen && disabled) return;
		setOpen(nextOpen);
		onOpenChange?.(nextOpen);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [children(() => handleOpenChange(true)), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfirmDialog, {
		open,
		onOpenChange: handleOpenChange,
		title,
		confirmText: confirmText ?? t$2("common.confirm"),
		cancelText: t$2("common.cancel"),
		destructive,
		onConfirm
	})] });
};
var ActionButtonWithConfirm = ({ action, executeAction, icon = action.icon, onConfirmOpen, softHoverBg, tooltip = action.label }) => {
	const disabled = !action.availability.enabled;
	const label = typeof action.label === "string" ? action.label : void 0;
	const button = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageActionButton, {
		className: "message-action-button",
		"aria-label": label,
		onClick: (e$1) => {
			e$1.stopPropagation();
			if (!action.confirm) executeAction(action);
		},
		disabled,
		softHoverBg,
		children: icon
	});
	const content = action.confirm ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfirmActionButton, {
		title: action.confirm.title,
		destructive: action.confirm.destructive,
		confirmText: action.confirm.confirmText,
		onConfirm: () => executeAction(action),
		onOpenChange: (open) => open && onConfirmOpen?.(),
		disabled,
		children: (open) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageActionButton, {
			className: "message-action-button",
			"aria-label": label,
			onClick: (e$1) => {
				e$1.stopPropagation();
				open();
			},
			disabled,
			softHoverBg,
			children: icon
		})
	}) : button;
	if (tooltip === false) return content;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
		content: tooltip,
		delay: 800,
		children: content
	});
};
var DeleteToolbarAction = ({ action, executeAction, softHoverBg }) => {
	const [showDeleteTooltip, setShowDeleteTooltip] = (0, import_react.useState)(false);
	const unavailableReason = action.availability.reason;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionButtonWithConfirm, {
		action,
		executeAction,
		icon: unavailableReason ? action.icon : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
			content: action.label,
			delay: 1e3,
			isOpen: showDeleteTooltip,
			onOpenChange: setShowDeleteTooltip,
			children: action.icon
		}),
		onConfirmOpen: () => setShowDeleteTooltip(false),
		softHoverBg,
		tooltip: unavailableReason ?? false
	});
};
var MessageActionMenuPopover = ({ actions, align = "end", children, onAction, onOpenChange }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandPopupMenu, {
		location: "webcontents.context",
		extraItems: (0, import_react.useMemo)(() => actionsToCommandMenuExtraItems(actions, (action) => {
			onAction(action);
		}), [actions, onAction]),
		align,
		side: "top",
		onOpenChange,
		deferActionsUntilClosed: true,
		contentClassName: "[-webkit-app-region:no-drag]",
		children
	});
};
var TranslateMenuPopover = ({ children, items, align = "end", onOpenChange }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandPopupMenu, {
		location: "webcontents.context",
		extraItems: (0, import_react.useMemo)(() => items.map((item) => isMessageMenuBarTranslationDivider(item) ? { type: "separator" } : {
			type: "item",
			id: item.key,
			label: item.label,
			enabled: item.enabled,
			onSelect: () => {
				item.onSelect();
			}
		}), [items]),
		align,
		side: "top",
		onOpenChange,
		contentClassName: "[-webkit-app-region:no-drag]",
		children
	});
};
var useMenuTooltipState = (onOpenChange) => {
	const [isMenuOpen, setIsMenuOpen] = (0, import_react.useState)(false);
	const [isTooltipOpen, setIsTooltipOpen] = (0, import_react.useState)(false);
	const isMenuOpenRef = (0, import_react.useRef)(false);
	const suppressTooltipRef = (0, import_react.useRef)(false);
	const handleMenuOpenChange = (open) => {
		isMenuOpenRef.current = open;
		suppressTooltipRef.current = !open;
		setIsMenuOpen(open);
		setIsTooltipOpen(false);
		onOpenChange?.(open);
	};
	const handleTooltipOpenChange = (open) => {
		setIsTooltipOpen(open && !isMenuOpenRef.current && !suppressTooltipRef.current);
	};
	const releaseTooltipSuppression = () => {
		suppressTooltipRef.current = false;
	};
	return {
		tooltipOpen: !isMenuOpen && isTooltipOpen,
		handleMenuOpenChange,
		handleTooltipOpenChange,
		tooltipTriggerProps: {
			onPointerEnter: releaseTooltipSuppression,
			onPointerLeave: releaseTooltipSuppression,
			onBlur: releaseTooltipSuppression
		}
	};
};
function renderDefaultToolbarAction({ action, executeAction, softHoverBg }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionButtonWithConfirm, {
		action,
		executeAction,
		softHoverBg
	});
}
function renderModelPickerToolbarAction({ action, actionContext, softHoverBg, onMenuOpenChange }) {
	const label = typeof action.label === "string" ? action.label : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
		content: action.label,
		delay: 800,
		children: actionContext.actions.renderRegenerateModelPicker?.({
			message: actionContext.message,
			messageParts: actionContext.messageParts,
			trigger: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageActionButton, {
				className: "message-action-button",
				"aria-label": label,
				softHoverBg,
				children: action.icon
			}),
			onOpenChange: onMenuOpenChange
		}) ?? null
	});
}
function renderTranslateToolbarAction({ action, actionContext, executeAction, softHoverBg, translationItems, onMenuOpenChange }) {
	if (actionContext.isTranslating) {
		const label = actionContext.t("translate.stop");
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
			content: label,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageActionButton, {
				className: "message-action-button",
				"aria-label": label,
				onClick: (e$1) => {
					e$1.stopPropagation();
					executeAction(action);
				},
				softHoverBg,
				children: action.icon
			})
		});
	}
	if (translationItems.length === 0) return null;
	const handleMenuOpenChange = (open) => {
		if (open) actionContext.actions.requestTranslationLanguages?.();
		onMenuOpenChange?.(open);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TranslateToolbarAction, {
		action,
		translationItems,
		softHoverBg,
		onMenuOpenChange: handleMenuOpenChange
	});
}
var TranslateToolbarAction = ({ action, translationItems, softHoverBg, onMenuOpenChange }) => {
	const { handleMenuOpenChange, handleTooltipOpenChange, tooltipOpen, tooltipTriggerProps } = useMenuTooltipState(onMenuOpenChange);
	const label = typeof action.label === "string" ? action.label : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
		content: action.label,
		delay: 1200,
		isOpen: tooltipOpen,
		onOpenChange: handleTooltipOpenChange,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TranslateMenuPopover, {
			items: translationItems,
			align: "center",
			onOpenChange: handleMenuOpenChange,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageActionButton, {
				className: "message-action-button",
				"aria-label": label,
				onClick: (e$1) => e$1.stopPropagation(),
				softHoverBg,
				...tooltipTriggerProps,
				children: action.icon
			})
		})
	});
};
function renderMoreMenuToolbarAction({ action, executeAction, menuActions, softHoverBg, onMenuOpenChange }) {
	if (menuActions.length === 0) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MoreMenuToolbarAction, {
		action,
		executeAction,
		menuActions,
		softHoverBg,
		onMenuOpenChange
	});
}
var MoreMenuToolbarAction = ({ action, executeAction, menuActions, softHoverBg, onMenuOpenChange }) => {
	const { handleMenuOpenChange, handleTooltipOpenChange, tooltipOpen, tooltipTriggerProps } = useMenuTooltipState(onMenuOpenChange);
	const label = typeof action.label === "string" ? action.label : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
		content: action.label,
		delay: 800,
		isOpen: tooltipOpen,
		onOpenChange: handleTooltipOpenChange,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageActionMenuPopover, {
			actions: menuActions,
			align: "end",
			onAction: executeAction,
			onOpenChange: handleMenuOpenChange,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageActionButton, {
				className: "message-action-button",
				"aria-label": label,
				onClick: (e$1) => e$1.stopPropagation(),
				softHoverBg,
				...tooltipTriggerProps,
				children: action.icon
			})
		})
	});
};
function renderDeleteToolbarAction({ action, executeAction, softHoverBg }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeleteToolbarAction, {
		action,
		executeAction,
		softHoverBg
	});
}
var import_dayjs_min$2 = /* @__PURE__ */ __toESM(require_dayjs_min());
var toolbarOrder = new Map(DEFAULT_MESSAGE_MENUBAR_BUTTON_IDS.map((id, index) => [id, index * 10]));
var toolbarRenderers = /* @__PURE__ */ new Map();
var messageMenuBarActionRegistry = createActionRegistry();
var logger$9 = loggerService.withContext("MessageMenuBarActions");
function toolbarAvailability(id, isVisible = () => true) {
	return (context) => {
		const visible = isVisible(context);
		return {
			visible,
			enabled: visible && !(context.isProcessing && STREAMING_DISABLED_BUTTON_IDS.has(id))
		};
	};
}
function notifyCommandError(id, context, error) {
	logger$9.error(`Message menu action failed: ${id}`, error);
	context.actions.notifyError?.(formatErrorMessageWithPrefix(error, context.t("message.error.unknown")));
}
function registerCommand(id, run) {
	messageMenuBarActionRegistry.registerCommand({
		id,
		run
	});
}
function registerAction(descriptor) {
	messageMenuBarActionRegistry.registerAction(descriptor);
}
function registerToolbarAction(descriptor) {
	const { renderToolbar, ...actionDescriptor } = descriptor;
	if (renderToolbar) toolbarRenderers.set(actionDescriptor.id, renderToolbar);
	registerAction({
		...actionDescriptor,
		order: toolbarOrder.get(actionDescriptor.id) ?? 0,
		surface: "toolbar"
	});
}
registerCommand("message.copy", async ({ actions, mainTextContent, messageParts, setCopied, t: t$2 }) => {
	const richContent = actions.copyRichContent ? createComposerRichClipboardContentFromParts(messageParts) : null;
	if (richContent) {
		const plainText = removeTrailingDoubleSpaces(richContent.plainText.trimStart());
		await actions.copyRichContent?.({
			...richContent,
			plainText
		}, { successMessage: t$2("message.copied") });
	} else await actions.copyText?.(removeTrailingDoubleSpaces(mainTextContent.trimStart()), { successMessage: t$2("message.copied") });
	setCopied(true);
});
registerCommand("message.edit", ({ message, startEditingMessage }) => {
	startEditingMessage?.(message.id);
});
registerCommand("message.regenerate", async ({ actions, message }) => {
	await actions.regenerateMessage?.(message.id);
});
registerCommand("message.delete", async ({ actions, message }) => {
	await actions.abortMessageTranslation?.(message.id);
	await actions.deleteMessage?.(message.id, { modelName: getMessageListItemModelName(message) || void 0 });
});
registerCommand("message.abortTranslation", async ({ actions, message }) => {
	await actions.abortMessageTranslation?.(message.id);
});
registerCommand("message.newBranch", async ({ actions, message, t: t$2 }) => {
	await actions.startMessageBranch?.(message.id);
	actions.notifySuccess?.(t$2("chat.message.new.branch.created"));
});
registerCommand("message.multiSelect", ({ actions }) => {
	actions.toggleMultiSelectMode?.(true);
});
registerCommand("message.saveFile", async ({ actions, mainTextContent, message }) => {
	const fileName = (0, import_dayjs_min$2.default)(message.createdAt).format("YYYYMMDDHHmm") + ".md";
	await actions.saveTextFile?.(fileName, mainTextContent);
});
registerCommand("message.saveKnowledge", async ({ actions, messageForExport }) => {
	await actions.saveToKnowledge?.(messageForExport);
});
registerCommand("message.exportNotes", async ({ actions, messageForExport }) => {
	await actions.exportToNotes?.(messageForExport);
});
registerCommand("message.copyPlainText", async ({ actions, messageForExport, t: t$2 }) => {
	await actions.copyText?.(messageToPlainText(messageForExport), { successMessage: t$2("message.copy.success") });
});
registerCommand("message.copyImage", async ({ actions, messageContainerRef }) => {
	await captureScrollableAsBlob(messageContainerRef, async (blob) => {
		if (blob) await actions.copyImage?.(blob);
	});
});
registerCommand("message.exportImage", async ({ actions, messageContainerRef, messageForExport, t: t$2 }) => {
	const imageData = await captureScrollableAsDataUrl(messageContainerRef);
	const title = await getMessageTitle(messageForExport);
	if (!title || !imageData || !actions.saveImage) {
		actions.notifyError?.(t$2("message.error.unknown"));
		return;
	}
	if (await actions.saveImage(title, imageData)) actions.notifySuccess?.(t$2("chat.topics.export.image_saved"));
	else actions.notifyError?.(t$2("message.error.unknown"));
});
registerCommand("message.exportMarkdown", async ({ actions, messageForExport }) => {
	await actions.exportMessageAsMarkdown?.(messageForExport);
});
registerCommand("message.exportMarkdownReason", async ({ actions, messageForExport }) => {
	await actions.exportMessageAsMarkdown?.(messageForExport, true);
});
registerCommand("message.exportWord", async ({ actions, messageForExport }) => {
	const markdown = await messageToMarkdown(messageForExport);
	const title = await getMessageTitle(messageForExport);
	await actions.exportToWord?.(markdown, title);
});
registerCommand("message.exportNotion", async ({ actions, messageForExport }) => {
	await actions.exportToNotion?.(messageForExport);
});
registerCommand("message.exportYuque", async ({ actions, messageForExport }) => {
	await actions.exportToYuque?.(messageForExport);
});
registerCommand("message.exportObsidian", async ({ actions, messageForExport }) => {
	await actions.exportToObsidian?.(messageForExport);
});
registerCommand("message.exportJoplin", async ({ actions, messageForExport }) => {
	await actions.exportToJoplin?.(messageForExport);
});
registerCommand("message.exportSiyuan", async ({ actions, messageForExport }) => {
	await actions.exportToSiyuan?.(messageForExport);
});
registerCommand("message.useful", ({ message, onSelectContext }) => {
	onSelectContext?.(message.id);
});
registerToolbarAction({
	id: "user-edit",
	commandId: "message.edit",
	label: ({ t: t$2 }) => t$2("common.edit"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EditIcon_default, { size: 15 }),
	availability: toolbarAvailability("user-edit", ({ actions, isTranslating, isUserMessage, startEditingMessage }) => !isTranslating && isUserMessage && !!actions.editMessage && !!startEditingMessage)
});
registerToolbarAction({
	id: "copy",
	commandId: "message.copy",
	label: ({ t: t$2 }) => t$2("common.copy"),
	icon: ({ copied }) => copied ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Check, {
		size: 15,
		color: "var(--primary)"
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyIcon_default, { size: 15 }),
	availability: toolbarAvailability("copy", ({ actions }) => !!actions.copyText)
});
registerToolbarAction({
	id: "assistant-regenerate",
	commandId: "message.regenerate",
	label: ({ t: t$2 }) => t$2("common.regenerate"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RefreshIcon_default, { size: 15 }),
	availability: toolbarAvailability("assistant-regenerate", ({ actions, isAssistantMessage }) => isAssistantMessage && !!actions.regenerateMessage)
});
registerToolbarAction({
	id: "assistant-mention-model",
	renderToolbar: renderModelPickerToolbarAction,
	label: ({ t: t$2 }) => t$2("message.mention.title"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AtSign, { size: 15 }),
	availability: toolbarAvailability("assistant-mention-model", ({ actions, isAssistantMessage }) => isAssistantMessage && !!actions.renderRegenerateModelPicker)
});
registerToolbarAction({
	id: "translate",
	renderToolbar: renderTranslateToolbarAction,
	commandId: "message.abortTranslation",
	label: ({ t: t$2 }) => t$2("chat.translate"),
	icon: ({ isTranslating }) => isTranslating ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CirclePause, { size: 15 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Languages, { size: 15 }),
	availability: (context) => {
		const canTranslate = !!context.actions.translateMessage && (context.translateLanguages.length > 0 || !!context.actions.requestTranslationLanguages);
		const canCopyTranslation = context.hasTranslationBlocks && !!context.actions.copyText;
		const canRemoveTranslation = context.hasTranslationBlocks && !!context.actions.removeMessageTranslation;
		const canAbortTranslation = context.isTranslating && !!context.actions.abortMessageTranslation;
		const visible = !context.isUserMessage && (canTranslate || canCopyTranslation || canRemoveTranslation || canAbortTranslation);
		return {
			visible,
			enabled: visible && (context.isTranslating ? canAbortTranslation : canTranslate || canCopyTranslation || canRemoveTranslation)
		};
	}
});
registerToolbarAction({
	id: "useful",
	commandId: "message.useful",
	label: ({ t: t$2 }) => t$2("chat.message.useful.label"),
	icon: ({ isSelectedForContext }) => isSelectedForContext ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ThumbsUp, {
		size: 17.5,
		fill: "var(--primary)",
		strokeWidth: 0
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ThumbsUp, { size: 15 }),
	availability: toolbarAvailability("useful", ({ actions, isAssistantMessage, isGrouped }) => isAssistantMessage && !!isGrouped && !!actions.setActiveBranch)
});
registerToolbarAction({
	id: "notes",
	commandId: "message.exportNotes",
	label: ({ t: t$2 }) => t$2("notes.save"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NotebookPen, { size: 15 }),
	availability: toolbarAvailability("notes", ({ actions, isAssistantMessage }) => isAssistantMessage && !!actions.exportToNotes)
});
registerToolbarAction({
	id: "delete",
	renderToolbar: renderDeleteToolbarAction,
	commandId: "message.delete",
	label: ({ t: t$2 }) => t$2("common.delete"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeleteIcon_default, { size: 15 }),
	confirm: ({ menuConfig, t: t$2 }) => menuConfig.confirmDeleteMessage ? {
		title: t$2("message.message.delete.content"),
		confirmText: t$2("common.delete"),
		destructive: true
	} : void 0,
	availability: ({ actions, isProcessing, message, t: t$2 }) => {
		const visible = !!actions.deleteMessage;
		const deleteAvailability = actions.getMessageDeleteAvailability?.(message.id) ?? { enabled: true };
		const reason = getMessageDeleteUnavailableText(deleteAvailability.enabled ? void 0 : deleteAvailability.reason, t$2);
		return {
			visible,
			enabled: visible && !isProcessing && deleteAvailability.enabled,
			reason
		};
	}
});
registerToolbarAction({
	id: "more-menu",
	renderToolbar: renderMoreMenuToolbarAction,
	label: ({ t: t$2 }) => t$2("chat.message.more"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Menu, { size: 19 }),
	availability: toolbarAvailability("more-menu", ({ isUserMessage }) => !isUserMessage)
});
registerAction({
	id: "edit",
	commandId: "message.edit",
	label: ({ t: t$2 }) => t$2("common.edit"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FilePenLine, { size: 15 }),
	group: "write",
	order: 10,
	surface: "menu",
	availability: ({ actions, isAssistantMessage, isEditable, isTranslating, isUserMessage, startEditingMessage }) => !isTranslating && isEditable && !!actions.editMessage && !!startEditingMessage && (isUserMessage || isAssistantMessage)
});
registerAction({
	id: "new-branch",
	commandId: "message.newBranch",
	label: ({ t: t$2 }) => t$2("chat.message.new.branch.label"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Split, { size: 15 }),
	group: "write",
	order: 20,
	surface: "menu",
	availability: ({ actions, isAssistantMessage, isLastMessage, t: t$2 }) => {
		if (!actions.startMessageBranch || !isAssistantMessage) return false;
		if (isLastMessage) return {
			visible: true,
			enabled: false,
			reason: t$2("chat.message.new.branch.disabled.latest")
		};
		return true;
	}
});
registerAction({
	id: "multi-select",
	commandId: "message.multiSelect",
	label: ({ t: t$2 }) => t$2("chat.multiple.select.label"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ListChecks, { size: 15 }),
	group: "write",
	order: 30,
	surface: "menu",
	availability: ({ actions, isProcessing, selection }) => ({
		visible: !!selection?.enabled && !!actions.toggleMultiSelectMode,
		enabled: !isProcessing
	})
});
registerAction({
	id: "save",
	label: ({ t: t$2 }) => t$2("chat.save.label"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Save, { size: 15 }),
	group: "save",
	order: 100,
	surface: "menu",
	children: [{
		id: "save.file",
		commandId: "message.saveFile",
		label: ({ t: t$2 }) => t$2("chat.save.file.title"),
		availability: ({ actions }) => !!actions.saveTextFile
	}, {
		id: "save.knowledge",
		commandId: "message.saveKnowledge",
		label: ({ t: t$2 }) => t$2("chat.save.knowledge.title"),
		availability: ({ actions }) => !!actions.saveToKnowledge
	}]
});
registerAction({
	id: "export",
	label: ({ t: t$2 }) => t$2("chat.topics.export.title"),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Upload, { size: 15 }),
	group: "export",
	order: 200,
	surface: "menu",
	children: [
		{
			id: "export.copy-plain-text",
			commandId: "message.copyPlainText",
			label: ({ t: t$2 }) => t$2("chat.topics.copy.plain_text"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.plain_text && !!actions.copyText
		},
		{
			id: "export.copy-image",
			commandId: "message.copyImage",
			label: ({ t: t$2 }) => t$2("chat.topics.copy.image"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.image && !!actions.copyImage
		},
		{
			id: "export.image",
			commandId: "message.exportImage",
			label: ({ t: t$2 }) => t$2("chat.topics.export.image"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.image && !!actions.saveImage
		},
		{
			id: "export.markdown",
			commandId: "message.exportMarkdown",
			label: ({ t: t$2 }) => t$2("chat.topics.export.md.label"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.markdown && !!actions.exportMessageAsMarkdown
		},
		{
			id: "export.markdown-reason",
			commandId: "message.exportMarkdownReason",
			label: ({ t: t$2 }) => t$2("chat.topics.export.md.reason"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.markdown_reason && !!actions.exportMessageAsMarkdown
		},
		{
			id: "export.word",
			commandId: "message.exportWord",
			label: ({ t: t$2 }) => t$2("chat.topics.export.word"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.docx && !!actions.exportToWord
		},
		{
			id: "export.notion",
			commandId: "message.exportNotion",
			label: ({ t: t$2 }) => t$2("chat.topics.export.notion"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.notion && !!actions.exportToNotion
		},
		{
			id: "export.yuque",
			commandId: "message.exportYuque",
			label: ({ t: t$2 }) => t$2("chat.topics.export.yuque"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.yuque && !!actions.exportToYuque
		},
		{
			id: "export.obsidian",
			commandId: "message.exportObsidian",
			label: ({ t: t$2 }) => t$2("chat.topics.export.obsidian"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.obsidian && !!actions.exportToObsidian
		},
		{
			id: "export.joplin",
			commandId: "message.exportJoplin",
			label: ({ t: t$2 }) => t$2("chat.topics.export.joplin"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.joplin && !!actions.exportToJoplin
		},
		{
			id: "export.siyuan",
			commandId: "message.exportSiyuan",
			label: ({ t: t$2 }) => t$2("chat.topics.export.siyuan"),
			availability: ({ actions, menuConfig }) => menuConfig.exportMenuOptions.siyuan && !!actions.exportToSiyuan
		}
	]
});
function resolveMessageMenuBarTranslationItems(context) {
	const { actions, getTranslationLanguageLabel, hasTranslationBlocks, mainTextContent, message, messageParts, t: t$2 } = context;
	const items = actions.translateMessage ? context.translateLanguages.map((language) => ({
		label: getTranslationLanguageLabel?.(language) ?? language.langCode,
		key: language.langCode,
		onSelect: async () => {
			try {
				await actions.translateMessage?.(message.id, language, mainTextContent);
			} catch (error) {
				notifyCommandError("message.translate", context, error);
			}
		}
	})) : [];
	if (items.length === 0 && actions.translateMessage && actions.requestTranslationLanguages) {
		const retryTranslationLanguages = actions.retryTranslationLanguages;
		if (context.translationLanguagesStatus === "error" && retryTranslationLanguages) items.push({
			key: "translate-retry",
			label: t$2("common.retry"),
			onSelect: () => retryTranslationLanguages()
		});
		else items.push({
			key: "translate-loading",
			label: t$2("common.loading"),
			enabled: false,
			onSelect: () => void 0
		});
	}
	if (!hasTranslationBlocks) return items;
	const trailingItems = [];
	if (actions.copyText) trailingItems.push({
		label: "📋 " + t$2("common.copy"),
		key: "translate-copy",
		onSelect: async () => {
			const translationContent = getTranslationFromParts(messageParts).map((item) => item.content || "").join("\n\n").trim();
			if (translationContent) try {
				await actions.copyText?.(translationContent, { successMessage: t$2("translate.copied") });
			} catch (error) {
				notifyCommandError("message.copyTranslation", context, error);
			}
			else actions.notifyWarning?.(t$2("translate.empty"));
		}
	});
	if (actions.removeMessageTranslation) trailingItems.push({
		label: "✖ " + t$2("translate.close"),
		key: "translate-close",
		onSelect: async () => {
			try {
				await actions.removeMessageTranslation?.(message.id);
				actions.notifySuccess?.(t$2("translate.closed"));
			} catch (error) {
				notifyCommandError("message.removeTranslation", context, error);
			}
		}
	});
	if (trailingItems.length === 0) return items;
	return [
		...items,
		...items.length > 0 ? [{
			type: "divider",
			key: "translate-divider"
		}] : [],
		...trailingItems
	];
}
function resolveMessageMenuBarToolbarActions(context) {
	return messageMenuBarActionRegistry.resolve(context, "toolbar").map((action) => {
		const renderToolbar = toolbarRenderers.get(action.id);
		return renderToolbar ? {
			...action,
			renderToolbar
		} : action;
	});
}
function resolveMessageMenuBarMenuActions(context) {
	return messageMenuBarActionRegistry.resolve(context, "menu").filter((action) => !!action.commandId || action.children.length > 0);
}
async function executeMessageMenuBarAction(actionId, context) {
	try {
		return await messageMenuBarActionRegistry.execute(actionId, context);
	} catch (error) {
		notifyCommandError(actionId, context, error);
		return false;
	}
}
function MessageMenuBarToolbarAction({ action, actionContext, executeAction, menuActions, onMenuOpenChange, softHoverBg, translationItems }) {
	return (action.renderToolbar ?? renderDefaultToolbarAction)({
		action,
		actionContext,
		executeAction,
		menuActions,
		onMenuOpenChange,
		softHoverBg,
		translationItems
	});
}
var MessageMenuBar = (props) => {
	const { message, isGrouped, isLastMessage, forceVisible = false, isAssistantMessage, isProcessing, messageContainerRef, onStartEditing, onMenuOpenChange, onSelectContext, variant = "footer" } = props;
	const { t: t$2 } = useTranslation();
	const actions = useMessageListActions();
	const selection = useMessageListSelection();
	const messageUi = useMessageListUi();
	const renderConfig = useMessageRenderConfig();
	const menuConfig = messageUi.menuConfig ?? defaultMessageMenuConfig;
	const [copied, setCopied] = useTemporaryValue(false, 2e3);
	const translateLanguages = (0, import_react.useMemo)(() => messageUi.translationLanguages ?? [], [messageUi.translationLanguages]);
	const isBubbleStyle = renderConfig.messageStyle === "bubble";
	const isUserMessage = message.role === "user";
	const messageParts = useMessageParts(message.id);
	const messageForExport = (0, import_react.useMemo)(() => createMessageExportView(message, messageParts), [message, messageParts]);
	const mainTextContent = (0, import_react.useMemo)(() => getComposerTextFromParts(messageParts), [messageParts]);
	const isTranslating = messageUi.isMessageTranslating?.(message.id) ?? false;
	const isEditable = isAssistantMessage ? canEditAssistantMessageParts(messageParts) : hasTextParts(messageParts);
	const hasTranslationBlocks = hasTranslationParts(messageParts);
	const isSelectedForContext = !!message.isActiveBranch;
	const softHoverBg = isBubbleStyle && !isLastMessage;
	const showMessageTokens = renderConfig.showEstimatedTokens && variant === "footer" && (!isBubbleStyle || isAssistantMessage);
	const isUserBubbleStyleMessage = variant === "footer" && isBubbleStyle && isUserMessage;
	const actionContext = (0, import_react.useMemo)(() => ({
		actions,
		message,
		messageParts,
		messageForExport,
		messageContainerRef,
		mainTextContent,
		selection,
		menuConfig,
		copied,
		setCopied,
		isAssistantMessage,
		isGrouped,
		isLastMessage,
		isProcessing,
		isTranslating,
		hasTranslationBlocks,
		isUserMessage,
		isSelectedForContext,
		isEditable,
		translateLanguages,
		translationLanguagesStatus: messageUi.translationLanguagesStatus,
		getTranslationLanguageLabel: messageUi.getTranslationLanguageLabel,
		startEditingMessage: onStartEditing,
		onSelectContext,
		t: t$2
	}), [
		actions,
		copied,
		hasTranslationBlocks,
		isAssistantMessage,
		isEditable,
		isGrouped,
		isLastMessage,
		isProcessing,
		isTranslating,
		isSelectedForContext,
		isUserMessage,
		mainTextContent,
		menuConfig,
		message,
		messageContainerRef,
		messageUi.getTranslationLanguageLabel,
		messageUi.translationLanguagesStatus,
		messageForExport,
		messageParts,
		onStartEditing,
		onSelectContext,
		selection,
		setCopied,
		t$2,
		translateLanguages
	]);
	const menuActions = (0, import_react.useMemo)(() => resolveMessageMenuBarMenuActions(actionContext), [actionContext]);
	const toolbarActions = (0, import_react.useMemo)(() => resolveMessageMenuBarToolbarActions(actionContext), [actionContext]);
	const translationItems = (0, import_react.useMemo)(() => resolveMessageMenuBarTranslationItems(actionContext), [actionContext]);
	const executeAction = (0, import_react.useCallback)(async (action) => {
		await executeMessageMenuBarAction(action.id, actionContext);
	}, [actionContext]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.message-menu-bar.message-actions part:message-actions",
		className: classNames("menubar flex flex-row items-center justify-end gap-1.5", isUserBubbleStyleMessage && "user-bubble-style mt-[5px]", (isLastMessage || forceVisible) && "show"),
		children: toolbarActions.map((action) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageMenuBarToolbarAction, {
			action,
			actionContext,
			executeAction,
			menuActions,
			onMenuOpenChange,
			softHoverBg,
			translationItems
		}, action.id))
	}), showMessageTokens && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageTokens_default, { message })] });
};
var MessageMenuBar_default = (0, import_react.memo)(MessageMenuBar);
var import_dayjs_min$1 = /* @__PURE__ */ __toESM(require_dayjs_min());
var USER_MESSAGE_FOOTER_ACTIONS_CLASS = "absolute inset-0 flex items-center gap-2 opacity-0 transition-opacity duration-150 focus-within:opacity-100 group-hover/message:opacity-100";
var MessageItemContent = ({ message, index, hideMenuBar = false, isGrouped, onSelectContext, isGroupContextMessage, isHorizontalMultiModelLayout = false, isLatestAssistantMessage = false, showModelIdentity = false, lockedMentionedModels, messageTail }) => {
	const { t: t$2 } = useTranslation();
	const actions = useMessageListActions();
	const renderConfig = useMessageRenderConfig() ?? defaultMessageRenderConfig;
	const selection = useMessageListSelection();
	const messageUi = useMessageListUiSelectors();
	const isMultiSelectMode = selection?.isMultiSelectMode ?? false;
	const isSelected = selection?.selectedMessageIds?.includes(message.id) ?? false;
	const model = getMessageListItemModel(message);
	const messageFont = renderConfig.messageFont;
	const fontSize = renderConfig.fontSize;
	const messageStyle = renderConfig.messageStyle;
	const messageContainerRef = (0, import_react.useRef)(null);
	const navigateWithScrollRuntime = useScrollRuntimeNavigation();
	const messageParts = useMessageParts(message.id);
	const [isMessageMenuOpen, setIsMessageMenuOpen] = (0, import_react.useState)(false);
	const editingMessageId = useMessageListEditingId();
	const { setTimeoutTimer } = useTimer();
	const canEditMessage = !!actions.editMessage;
	const isAssistantMessage = message.role === "assistant";
	const isTranslating = messageUi.isMessageTranslating?.(message.id) ?? false;
	const canStartEditing = canEditMessage && (!isAssistantMessage || canEditAssistantMessageParts(messageParts) && !isTranslating);
	const isEditing = editingMessageId === message.id;
	const handleStartEditing = (0, import_react.useCallback)((messageId) => {
		if (canStartEditing && messageId === message.id) actions.startEditing?.(message, messageParts, { lockedMentionedModels: lockedMentionedModels && lockedMentionedModels.length > 1 ? lockedMentionedModels : void 0 });
	}, [
		actions,
		canStartEditing,
		lockedMentionedModels,
		message,
		messageParts
	]);
	const isLastMessage = index === 0 || !!isGrouped;
	const activityState = messageUi.getMessageActivityState?.(message);
	const isProcessing = activityState?.isProcessing ?? false;
	const isStreamTarget = activityState?.isStreamTarget ?? false;
	const isApprovalAnchor = activityState?.isApprovalAnchor ?? false;
	const showMenuBar = !hideMenuBar && !isEditing && !isStreamTarget && !isApprovalAnchor;
	const isUserBubbleMessage = messageStyle === "bubble" && !isAssistantMessage && !isMultiSelectMode;
	const showAssistantFooterActions = showMenuBar && isAssistantMessage;
	const showUserFooterActions = showMenuBar && !isAssistantMessage && !isMultiSelectMode && !isUserBubbleMessage;
	const assistantFooterVisibilityClass = isLatestAssistantMessage || isMessageMenuOpen ? "opacity-100" : "opacity-0 transition-opacity duration-150 focus-within:opacity-100 group-hover/message:opacity-100";
	const messageHighlightHandler = (0, import_react.useCallback)((highlight = true) => {
		const messageContainer = messageContainerRef.current;
		if (messageContainer) {
			if (!navigateWithScrollRuntime(messageContainer, "center")) scrollIntoView(messageContainer, {
				behavior: "smooth",
				block: "center",
				container: "nearest"
			});
			if (highlight) setTimeoutTimer("messageHighlightHandler", () => {
				const classList = messageContainerRef.current?.classList;
				classList?.add("animation-locate-highlight");
				const handleAnimationEnd = () => {
					classList?.remove("animation-locate-highlight");
					messageContainerRef.current?.removeEventListener("animationend", handleAnimationEnd);
				};
				messageContainerRef.current?.addEventListener("animationend", handleAnimationEnd);
			}, 500);
		}
	}, [navigateWithScrollRuntime, setTimeoutTimer]);
	(0, import_react.useEffect)(() => {
		return actions.bindMessageRuntime?.(message.id, {
			locateMessage: messageHighlightHandler,
			startEditing: () => {
				handleStartEditing(message.id);
			}
		});
	}, [
		actions,
		handleStartEditing,
		message.id,
		messageHighlightHandler
	]);
	const handleStartNewContext = (0, import_react.useCallback)(() => {
		if (isMultiSelectMode || !actions.startNewContext) return;
		actions.startNewContext?.();
	}, [actions, isMultiSelectMode]);
	const canStartNewContext = !isMultiSelectMode && Boolean(actions.startNewContext);
	const handleStartNewContextKeyDown = (0, import_react.useCallback)((event) => {
		if (event.key !== "Enter" && event.key !== " ") return;
		event.preventDefault();
		handleStartNewContext();
	}, [handleStartNewContext]);
	const handleMessageSelectClick = (0, import_react.useCallback)((event) => {
		if (!isMultiSelectMode) return;
		if (event.target?.closest("[role=\"checkbox\"]")) return;
		event.preventDefault();
		event.stopPropagation();
		actions.selectMessage?.(message.id, !isSelected);
	}, [
		actions,
		isMultiSelectMode,
		isSelected,
		message.id
	]);
	if (message.isContextBoundary) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.message-item-content.button",
		"aria-disabled": !canStartNewContext,
		className: cn("clear-context-divider flex-1", canStartNewContext ? "cursor-pointer" : "cursor-default"),
		onClick: handleStartNewContext,
		onKeyDown: handleStartNewContextKeyDown,
		role: "button",
		tabIndex: canStartNewContext ? 0 : -1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "mx-5 my-4 flex items-center gap-2 text-foreground-tertiary text-sm",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("hr", { className: "flex-1 border-border border-dashed" }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t$2("chat.message.new.context") }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("hr", { className: "flex-1 border-border border-dashed" })
			]
		})
	});
	const plainMessageContent = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(scrollbar_default, {
		"data-ui": "part:message-content",
		className: "message-content-container mt-0 min-h-0 max-w-full overflow-y-auto pl-0",
		style: {
			fontFamily: messageFont === "serif" ? "var(--font-family-serif)" : "var(--font-family)",
			fontSize,
			overflowY: isHorizontalMultiModelLayout ? "auto" : "visible"
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageErrorBoundary_default, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageContent_default, { message }) })
	}), isAssistantMessage ? messageTail : void 0] });
	const userFooter = showUserFooterActions ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.message-item-content",
		className: "MessageFooter relative mt-1 flex min-h-6.5 max-w-full shrink-0 items-center text-foreground-tertiary text-xs leading-none",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: USER_MESSAGE_FOOTER_ACTIONS_CLASS,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageMenuBar_default, {
				message,
				isLastMessage,
				isAssistantMessage: false,
				isGrouped,
				isProcessing,
				messageContainerRef,
				onStartEditing: handleStartEditing,
				onSelectContext,
				variant: "header"
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SiblingNavigator_default, { messageId: message.id })]
		})
	}) : void 0;
	const assistantFooter = showAssistantFooterActions ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.message-item-content",
		className: cn("MessageFooter mt-1 flex min-h-6.5 shrink-0 items-center justify-between gap-1.5 text-xs leading-none", assistantFooterVisibilityClass),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(HorizontalScrollContainer_default, {
			classNames: { content: cn("flex-1 flex-row items-center justify-between") },
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageMenuBar_default, {
				message,
				isLastMessage: isLatestAssistantMessage,
				forceVisible: isMessageMenuOpen,
				isAssistantMessage,
				isGrouped,
				isProcessing,
				messageContainerRef,
				onStartEditing: handleStartEditing,
				onMenuOpenChange: setIsMessageMenuOpen,
				onSelectContext
			})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SiblingNavigator_default, { messageId: message.id })]
	}) : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.message-item-content",
		className: cn(classNames({
			"message group/message transform-[translateZ(0)] relative flex w-full flex-col rounded-[10px] pt-2.5 pb-0 transition-colors duration-300 will-change-transform [&:hover_.menubar]:opacity-100 [&_.menubar.show]:opacity-100 [&_.menubar]:opacity-0 [&_.menubar]:transition-opacity [&_.menubar]:duration-200": true,
			"message-assistant": isAssistantMessage,
			"message-user": !isAssistantMessage,
			"bg-muted px-3 pb-2 opacity-70 outline-offset-[-1px] [outline:1px_solid_var(--border)]": isEditing,
			"cursor-pointer": isMultiSelectMode
		})),
		"aria-disabled": isEditing ? true : void 0,
		ref: messageContainerRef,
		onClickCapture: handleMessageSelectClick,
		children: isUserBubbleMessage ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserBubbleMessage, {
			message,
			isLastMessage,
			isGrouped,
			isProcessing,
			messageContainerRef,
			onStartEditing: handleStartEditing,
			onSelectContext,
			messageFont,
			fontSize,
			isEditing
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageHeader_default, {
			message,
			model,
			isGroupContextMessage,
			showModelIdentity,
			contentSlot: plainMessageContent,
			footerSlot: userFooter ?? assistantFooter
		}, model ? createUniqueModelId(model.provider, model.id) : "")
	}, message.id);
};
var MessageItem = ({ message, messageParts, ...props }) => {
	const content = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageItemContent, {
		message,
		...props
	});
	if (!messageParts) return content;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessagePartsScopeProvider, {
		messageId: message.id,
		parts: messageParts,
		children: content
	});
};
var MessageFrame_default = (0, import_react.memo)(MessageItem);
var UserBubbleMessage = ({ message, isLastMessage, isGrouped, isProcessing, messageContainerRef, onStartEditing, onSelectContext, messageFont, fontSize, isEditing }) => {
	const actions = useMessageListActions();
	const avatar = useMessageListMeta().userProfile?.avatar ?? "";
	const canOpenUserProfile = !!actions.openUserProfile;
	const openUserProfile = (0, import_react.useCallback)(() => {
		actions.openUserProfile?.();
	}, [actions]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.user-bubble-message",
		className: "flex w-full flex-col items-end",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex max-w-full items-start justify-end gap-2.5 has-[.code-block]:w-full",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex min-w-0 flex-1 flex-col items-end",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(scrollbar_default, {
					"data-ui": "part:message-content",
					className: "message-content-container mt-0 max-w-full overflow-y-auto rounded-[10px] bg-muted px-4 py-2.5 has-[.code-block]:w-full [&_.block-wrapper:last-child>*:last-child]:mb-0! [&_.markdown>p:last-child]:mb-0!",
					style: {
						fontFamily: messageFont === "serif" ? "var(--font-family-serif)" : "var(--font-family)",
						fontSize,
						overflowY: "visible"
					},
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageErrorBoundary_default, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageContent_default, { message }) })
				})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageAvatar_default, {
				avatar,
				className: "mt-1.5",
				onClick: canOpenUserProfile ? openUserProfile : void 0
			})]
		}), !isEditing && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "MessageFooter relative mt-1 mr-[30px] flex min-h-6.5 w-[calc(100%-30px)] max-w-full items-center justify-end text-foreground-tertiary text-xs leading-none",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: cn(USER_MESSAGE_FOOTER_ACTIONS_CLASS, "justify-end"),
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "shrink-0",
						children: (0, import_dayjs_min$1.default)(message.updatedAt ?? message.createdAt).format("MM/DD HH:mm")
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageMenuBar_default, {
						message,
						isLastMessage,
						isAssistantMessage: false,
						isGrouped,
						isProcessing,
						messageContainerRef,
						onStartEditing,
						onSelectContext,
						variant: "header"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SiblingNavigator_default, { messageId: message.id })
				]
			})
		})]
	});
};
var WIDE_MULTI_MODEL_LAYOUTS = new Set(["horizontal", "grid"]);
function isAssistantMultiModelGroup(messages) {
	return messages.length > 1 && messages.every((message) => message.role === "assistant");
}
function getEffectiveMultiModelMessageStyle(messages, getMessageUiState, defaultStyle) {
	if (messages.length < 2) return "fold";
	return getMessageUiState(messages[0]?.id).multiModelMessageStyle || defaultStyle;
}
function shouldUseWideLayoutForMessageGroup(messages, getMessageUiState, defaultStyle, isMultiSelectMode) {
	if (isMultiSelectMode || !isAssistantMultiModelGroup(messages)) return false;
	return WIDE_MULTI_MODEL_LAYOUTS.has(getEffectiveMultiModelMessageStyle(messages, getMessageUiState, defaultStyle));
}
var MessageGroupModelList = ({ messages, selectMessageId, setSelectedMessage }) => {
	const renderLabel = (0, import_react.useCallback)((message) => {
		const isProcessing = isMessageListItemProcessing(message);
		const isSelected = message.id === selectMessageId;
		const model = getMessageListItemModel(message);
		const modelName = model?.name || model?.id;
		const avatar = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelAvatar_default, {
			className: isProcessing ? "animation-pulse" : "",
			model,
			size: 20
		});
		return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(SegmentedLabel, { children: [isSelected || !modelName ? avatar : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
			content: modelName,
			delay: 600,
			children: avatar
		}), isSelected && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelName, { children: modelName })] });
	}, [selectMessageId]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelsContainer, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SegmentedControl, {
		className: "[&_button]:h-6.5 [&_button]:gap-1 [&_button]:px-2",
		value: selectMessageId,
		onValueChange: (value) => {
			setSelectedMessage(messages.find((message) => message.id === value));
		},
		options: messages.map((message) => ({
			label: renderLabel(message),
			value: message.id
		})),
		size: "sm"
	}) }) });
};
var Container = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RowFlex, {
	className: ["ml-1 flex-1 items-center overflow-hidden", className].filter(Boolean).join(" "),
	...props
});
var ModelsContainer = ({ className, ...props }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(scrollbar_default, {
		className: ["flex flex-1 flex-row items-center justify-start overflow-x-auto px-1 [-ms-overflow-style:none] [scrollbar-width:none] [&::-webkit-scrollbar]:hidden", className].filter(Boolean).join(" "),
		...props
	});
};
var SegmentedLabel = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.segmented-label",
	className: ["flex items-center gap-[5px] py-0.5", className].filter(Boolean).join(" "),
	...mergeUiProps(props, "chat.segmented-label")
});
var ModelName = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
	"data-ui": "chat.model-name",
	className: ["font-medium text-xs", className].filter(Boolean).join(" "),
	...mergeUiProps(props, "chat.model-name")
});
var MessageGroupModelList_default = (0, import_react.memo)(MessageGroupModelList);
var MessageGroupSettings = () => {
	const actions = useMessageListActions();
	const gridPopoverTrigger = (useMessageRenderConfig() ?? defaultMessageRenderConfig).multiModelGridPopoverTrigger;
	const { t: t$2 } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverTrigger, {
		asChild: true,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Settings, {
			className: "ml-1.5 cursor-pointer",
			size: 16
		}) })
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContent, {
		className: "w-80 p-2",
		align: "end",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(SettingsRow, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "mr-2.5",
			children: t$2("settings.messages.grid_popover_trigger.label")
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Selector_default, {
			size: 14,
			value: gridPopoverTrigger || "hover",
			onChange: (value) => actions.updateRenderConfig?.({ multiModelGridPopoverTrigger: value }),
			options: [{
				label: t$2("settings.messages.grid_popover_trigger.hover"),
				value: "hover"
			}, {
				label: t$2("settings.messages.grid_popover_trigger.click"),
				value: "click"
			}]
		})] }) })
	})] });
};
var SettingsRow = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.settings-row",
	className: ["flex min-h-9 items-center justify-between gap-3", className].filter(Boolean).join(" "),
	...mergeUiProps(props, "chat.settings-row")
});
var MessageGroupSettings_default = MessageGroupSettings;
var logger$8 = loggerService.withContext("MessageGroupMenuBar");
var MessageGroupMenuBar = ({ multiModelMessageStyle, setMultiModelMessageStyle, messages, selectMessageId, setSelectedMessage }) => {
	const { t: t$2 } = useTranslation();
	const partsMap = usePartsMap();
	const actions = useMessageListActions();
	const groupParentId = messages[0]?.parentId;
	const deleteAvailability = groupParentId ? actions.getMessageDeleteAvailability?.(groupParentId) : void 0;
	const isDeleteDisabled = deleteAvailability?.enabled === false;
	const deleteDisabledReason = isDeleteDisabled ? getMessageDeleteUnavailableText(deleteAvailability.reason, t$2) : void 0;
	const handleDeleteGroup = async () => {
		if (!groupParentId || !actions.deleteMessageGroupWithConfirm || isDeleteDisabled) return;
		await actions.deleteMessageGroupWithConfirm(groupParentId);
	};
	const isFailedMessage = (m$1) => {
		if (m$1.role !== "assistant") return false;
		const isError = (m$1.status || "").toLowerCase() === "error";
		const parts = partsMap?.[m$1.id];
		const content = parts ? getTextFromParts(parts) : "";
		const noContent = !content || content.trim().length === 0;
		return isError || noContent;
	};
	const isTransmittingMessage = (m$1) => {
		if (m$1.role !== "assistant") return false;
		return m$1.status === "pending";
	};
	const hasFailedMessages = !!actions.regenerateMessage && messages.some((m$1) => isFailedMessage(m$1) && !isTransmittingMessage(m$1));
	const handleRetryAll = async () => {
		const candidates = messages.filter((m$1) => isFailedMessage(m$1) && !isTransmittingMessage(m$1));
		let failedCount = 0;
		let lastError;
		for (const msg of candidates) try {
			await actions.regenerateMessage?.(msg.id);
		} catch (e$1) {
			failedCount++;
			lastError = e$1;
			logger$8.warn("Failed to retry grouped message", e$1, { messageId: msg.id });
		}
		if (failedCount > 0) actions.notifyError?.(formatErrorMessageWithPrefix(lastError, t$2("message.group.retry_failed")));
	};
	const multiModelMessageStyleTextByLayout = {
		fold: t$2("message.message.multi_model_style.fold.label"),
		vertical: t$2("message.message.multi_model_style.vertical"),
		horizontal: t$2("message.message.multi_model_style.horizontal"),
		grid: t$2("message.message.multi_model_style.grid")
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(GroupMenuBar, {
		$layout: multiModelMessageStyle,
		className: "group-menu-bar",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RowFlex, {
			className: "min-w-0 flex-1 items-center gap-1 overflow-hidden",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayoutContainer, { children: [
					"fold",
					"vertical",
					"horizontal",
					"grid"
				].map((layout) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					delay: 500,
					content: t$2("message.message.multi_model_style.label") + ": " + multiModelMessageStyleTextByLayout[layout],
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayoutOption, {
						$active: multiModelMessageStyle === layout,
						onClick: () => setMultiModelMessageStyle(layout),
						children: layout === "fold" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Folder, { size: 14 }) : layout === "horizontal" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Columns2, { size: 14 }) : layout === "vertical" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Rows3, { size: 14 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Grid2x2, { size: 14 })
					})
				}, layout)) }),
				multiModelMessageStyle === "fold" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageGroupModelList_default, {
					messages,
					selectMessageId,
					setSelectedMessage
				}),
				multiModelMessageStyle === "grid" && actions.updateRenderConfig && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageGroupSettings_default, {})
			]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ActionContainer, { children: [hasFailedMessages && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
			content: t$2("message.group.retry_failed"),
			delay: 600,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				variant: "ghost",
				size: "sm",
				onClick: handleRetryAll,
				className: "size-7 min-w-7 p-0",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RotateCcw, { size: 14 })
			})
		}), actions.deleteMessageGroupWithConfirm && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
			content: deleteDisabledReason ?? t$2("common.delete"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				variant: "ghost",
				size: "sm",
				disabled: isDeleteDisabled,
				onClick: handleDeleteGroup,
				className: "size-7 min-w-7 p-0",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trash2, {
					size: 14,
					className: "text-error"
				})
			})
		})] })]
	});
};
var GroupMenuBar = ({ className, $layout, ...props }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.group-menu-bar",
		className: ["group-menu-bar mt-2 mb-4 flex h-9 flex-row items-center justify-between gap-1 overflow-hidden rounded-[10px] border-[0.5px] border-border px-1.5 py-5", className].filter(Boolean).join(" "),
		...mergeUiProps(props, "chat.group-menu-bar")
	});
};
var LayoutContainer = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.layout",
	className: ["flex shrink-0 flex-row items-center gap-0.5", className].filter(Boolean).join(" "),
	...mergeUiProps(props, "chat.layout")
});
var LayoutOption = ({ className, $active, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.layout-option",
	className: [
		"flex size-7 cursor-pointer items-center justify-center rounded-md text-muted-foreground hover:bg-accent hover:text-foreground",
		$active && "bg-muted text-foreground hover:bg-muted",
		className
	].filter(Boolean).join(" "),
	...mergeUiProps(props, "chat.layout-option")
});
var ActionContainer = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.action",
	className: ["flex shrink-0 items-center gap-1", className].filter(Boolean).join(" "),
	...mergeUiProps(props, "chat.action")
});
var MessageGroupMenuBar_default = (0, import_react.memo)(MessageGroupMenuBar);
var logger$7 = loggerService.withContext("MessageGroup");
var EMPTY_MESSAGE_PARTS = [];
function pickPreferredSelectedMessage(messages, getMessageUiState) {
	return messages.find((message) => message.isActiveBranch) ?? messages.find((message) => getMessageUiState(message.id).foldSelected) ?? messages.find(isMessageListItemProcessing);
}
var MessageGroup = ({ messages, partsByMessageId, captureMode = false, registerMessageElement, isLatestAssistantGroup = false, directAssistantModelsByUserId, onMultiModelMessageStyleChange, messageTail }) => {
	const messageLength = messages.length;
	const actions = useMessageListActions();
	const renderConfig = useMessageRenderConfig() ?? defaultMessageRenderConfig;
	const selection = useMessageListSelection();
	const messageUi = useMessageListUiSelectors();
	const multiModelMessageStyleSetting = renderConfig.multiModelMessageStyle;
	const gridPopoverTrigger = renderConfig.multiModelGridPopoverTrigger;
	const { setTimeoutTimer } = useTimer();
	const navigateWithScrollRuntime = useScrollRuntimeNavigation();
	const isMultiSelectMode = selection?.isMultiSelectMode ?? false;
	const getMessageUiState = (0, import_react.useCallback)((messageId) => messageUi.getMessageUiState?.(messageId) ?? {}, [messageUi]);
	const updateMessageUiState = (0, import_react.useCallback)((messageId, updates) => {
		actions.updateMessageUiState?.(messageId, updates);
	}, [actions]);
	const isMultiModelGroup = isAssistantMultiModelGroup(messages);
	const isGrouped = isMultiSelectMode ? false : isMultiModelGroup;
	const [_multiModelMessageStyle, setMultiModelMessageStyle] = (0, import_react.useState)(() => getEffectiveMultiModelMessageStyle(messages, getMessageUiState, multiModelMessageStyleSetting));
	const [selectedIndex, setSelectedIndex] = (0, import_react.useState)(messageLength - 1);
	const previousMessageIdsRef = (0, import_react.useRef)(messages.map((message) => message.id));
	const activeBranchSelectionQueueRef = (0, import_react.useRef)(Promise.resolve());
	const multiModelMessageStyle = (0, import_react.useMemo)(() => messageLength < 2 ? "fold" : _multiModelMessageStyle, [_multiModelMessageStyle, messageLength]);
	const isGrid = multiModelMessageStyle === "grid";
	const [selectedMessageId, setSelectedMessageIdState] = (0, import_react.useState)(() => {
		if (messages.length === 1) return messages[0]?.id;
		return pickPreferredSelectedMessage(messages, getMessageUiState)?.id ?? messages.at(-1)?.id ?? messages[0]?.id;
	});
	(0, import_react.useEffect)(() => {
		if (captureMode) return;
		const previousIds = previousMessageIdsRef.current;
		const previousIdSet = new Set(previousIds);
		const addedMessages = messages.filter((message) => !previousIdSet.has(message.id));
		previousMessageIdsRef.current = messages.map((message) => message.id);
		const hasSelected = messages.some((m$1) => m$1.id === selectedMessageId);
		const activeBranchMessage = messages.find((message) => message.isActiveBranch);
		let nextSelectedMessage;
		if (activeBranchMessage && activeBranchMessage.id !== selectedMessageId) nextSelectedMessage = activeBranchMessage;
		else if (!hasSelected) nextSelectedMessage = pickPreferredSelectedMessage(messages, getMessageUiState) ?? messages.at(-1) ?? messages[0];
		else if (addedMessages.length > 0) nextSelectedMessage = pickPreferredSelectedMessage(addedMessages, getMessageUiState) ?? addedMessages.at(-1);
		if (nextSelectedMessage && nextSelectedMessage.id !== selectedMessageId) {
			if (selectedMessageId) updateMessageUiState(selectedMessageId, { foldSelected: false });
			updateMessageUiState(nextSelectedMessage.id, { foldSelected: true });
			setSelectedMessageIdState(nextSelectedMessage.id);
			setSelectedIndex(messages.findIndex((message) => message.id === nextSelectedMessage.id));
		}
	}, [
		captureMode,
		getMessageUiState,
		messages,
		selectedMessageId,
		updateMessageUiState
	]);
	const setSelectedMessage = (0, import_react.useCallback)((message) => {
		if (selectedMessageId) updateMessageUiState(selectedMessageId, { foldSelected: false });
		updateMessageUiState(message.id, { foldSelected: true });
		setSelectedMessageIdState(message.id);
		if (message.role === "assistant" && message.id !== selectedMessageId) Promise.resolve(actions.setActiveBranch?.(message.id)).catch((error) => {
			logger$7.error("Failed to set active branch from message group", error, { messageId: message.id });
			actions.notifyError?.(error instanceof Error ? error.message : String(error));
		});
		setTimeoutTimer("setSelectedMessage", () => {
			const messageElement = document.getElementById(`message-${message.id}`);
			if (messageElement) {
				if (!navigateWithScrollRuntime(messageElement)) scrollIntoView(messageElement, {
					behavior: "smooth",
					block: "start",
					container: "nearest"
				});
			}
		}, 200);
	}, [
		actions,
		navigateWithScrollRuntime,
		selectedMessageId,
		setTimeoutTimer,
		updateMessageUiState
	]);
	(0, import_react.useEffect)(() => {
		if (captureMode || !isGrouped || messageLength <= 1) return;
		const handleFlowNavigate = (event) => {
			const { messageId } = event.detail;
			const targetIndex = messages.findIndex((msg) => msg.id === messageId);
			if (targetIndex !== -1 && targetIndex !== selectedIndex) {
				setSelectedIndex(targetIndex);
				const targetMessage = messages[targetIndex];
				if (targetMessage) setSelectedMessage(targetMessage);
			}
		};
		document.addEventListener("flow-navigate-to-message", handleFlowNavigate);
		return () => {
			document.removeEventListener("flow-navigate-to-message", handleFlowNavigate);
		};
	}, [
		messages,
		selectedIndex,
		isGrouped,
		messageLength,
		captureMode
	]);
	(0, import_react.useEffect)(() => {
		if (captureMode) return;
		return actions.bindMessageGroupRuntime?.(messages.map((message) => message.id), { locateMessage: (messageId) => {
			const message = messages.find((item) => item.id === messageId);
			if (!message) return;
			const element = document.getElementById(`message-${message.id}`);
			if (!element) return;
			if (window.getComputedStyle(element).display === "none") {
				setSelectedMessage(message);
				return;
			}
			if (!navigateWithScrollRuntime(element)) scrollIntoView(element, {
				behavior: "smooth",
				block: "start",
				container: "nearest"
			});
		} });
	}, [
		actions,
		captureMode,
		messages,
		navigateWithScrollRuntime,
		setSelectedMessage
	]);
	(0, import_react.useEffect)(() => {
		if (captureMode) return;
		messages.forEach((message) => {
			const element = document.getElementById(`message-${message.id}`);
			element && registerMessageElement?.(message.id, element);
		});
		return () => messages.forEach((message) => registerMessageElement?.(message.id, null));
	}, [
		captureMode,
		messages,
		registerMessageElement
	]);
	const onSelectContext = (0, import_react.useCallback)((msgId) => {
		const message = messages.find((msg) => msg.id === msgId);
		if (!message) {
			logger$7.error("the message to update doesn't exist in this group");
			return;
		}
		const setActiveBranch = actions.setActiveBranch;
		if (!setActiveBranch) return;
		activeBranchSelectionQueueRef.current = activeBranchSelectionQueueRef.current.then(() => setActiveBranch(message.id)).catch((error) => {
			logger$7.error("Failed to set active branch from context selection", error, { messageId: message.id });
			actions.notifyError?.(error instanceof Error ? error.message : String(error));
		});
	}, [actions, messages]);
	const groupContextMessageId = (0, import_react.useMemo)(() => {
		const activeBranchMessage = messages.find((message) => message.isActiveBranch);
		if (activeBranchMessage) return activeBranchMessage.id;
		if (messages.length > 0) return messages[0].id;
		logger$7.warn("Empty message group");
		return "";
	}, [messages]);
	const handleHorizontalGroupWheel = (0, import_react.useCallback)((event) => {
		if (event.target?.closest(".message-content-container")) return;
		const groupContainer = event.currentTarget;
		const hasInnerVerticalScroll = Array.from(groupContainer.querySelectorAll(".message-content-container")).some((contentContainer) => contentContainer.scrollHeight > contentContainer.clientHeight + 1);
		const hasHorizontalScroll = groupContainer.scrollWidth > groupContainer.clientWidth + 1;
		const horizontalDelta = Math.abs(event.deltaX) > 0 ? event.deltaX : event.shiftKey ? event.deltaY : 0;
		if (horizontalDelta !== 0 && hasHorizontalScroll) {
			event.preventDefault();
			event.stopPropagation();
			groupContainer.scrollLeft += horizontalDelta;
			return;
		}
		if (hasInnerVerticalScroll) {
			event.preventDefault();
			event.stopPropagation();
		}
	}, []);
	const renderMessage = (0, import_react.useCallback)((message, index) => {
		const isGridGroupMessage = isGrid && message.role === "assistant" && isGrouped;
		const messageProps = {
			isGrouped,
			isHorizontalMultiModelLayout: multiModelMessageStyle === "horizontal",
			isLatestAssistantMessage: isLatestAssistantGroup && message.role === "assistant",
			showModelIdentity: isMultiModelGroup && multiModelMessageStyle !== "fold",
			lockedMentionedModels: directAssistantModelsByUserId?.get(message.id),
			messageTail: messageTail?.messageId === message.id ? messageTail.content : void 0,
			message,
			messageParts: partsByMessageId ? partsByMessageId[message.id] ?? EMPTY_MESSAGE_PARTS : void 0,
			index
		};
		const messageContent = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageWrapper, {
			id: `message-${message.id}`,
			className: classNames([{
				[multiModelMessageStyle]: message.role === "assistant" && messages.length > 1,
				selected: message.id === selectedMessageId
			}]),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageFrame_default, {
				onSelectContext,
				isGroupContextMessage: isGrouped && message.id === groupContextMessageId,
				...messageProps
			})
		}, message.id);
		if (isGridGroupMessage) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GridMessagePopover, {
			trigger: gridPopoverTrigger,
			content: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageWrapper, {
				className: classNames(["in-popover", {
					[multiModelMessageStyle]: message.role === "assistant" && messages.length > 1,
					selected: message.id === selectedMessageId
				}]),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageFrame_default, {
					onSelectContext,
					...messageProps
				})
			}),
			triggerContent: messageContent
		}, message.id);
		return messageContent;
	}, [
		isGrid,
		isGrouped,
		isMultiModelGroup,
		isLatestAssistantGroup,
		multiModelMessageStyle,
		messages,
		directAssistantModelsByUserId,
		selectedMessageId,
		onSelectContext,
		groupContextMessageId,
		gridPopoverTrigger,
		partsByMessageId,
		messageTail
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(GroupContainer, {
		id: messages[0].parentId ? `message-group-${messages[0].parentId}` : void 0,
		className: classNames([multiModelMessageStyle, { "multi-select-mode": isMultiSelectMode }]),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(GridContainer, {
			$count: messageLength,
			className: classNames([multiModelMessageStyle, { "multi-select-mode": isMultiSelectMode }]),
			onWheelCapture: multiModelMessageStyle === "horizontal" ? handleHorizontalGroupWheel : void 0,
			children: messages.map(renderMessage)
		}), isGrouped && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageGroupMenuBar_default, {
			multiModelMessageStyle,
			setMultiModelMessageStyle: (style) => {
				setMultiModelMessageStyle(style);
				onMultiModelMessageStyleChange?.(style);
				messages.forEach((message) => {
					updateMessageUiState(message.id, { multiModelMessageStyle: style });
				});
			},
			messages,
			selectMessageId: selectedMessageId,
			setSelectedMessage
		})]
	});
};
var GroupContainer = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.message.group",
	className: classNames("[&.grid]:py-1 [&.grid_.group-menu-bar]:mx-0 [&.horizontal]:py-1 [&.horizontal_.group-menu-bar]:mx-0 [&.multi-select-mode]:px-2.5 [&.multi-select-mode]:py-[5px]", className),
	...mergeUiProps(props, "chat.message.group")
});
var GridContainer = ({ className, $count, style, ...props }) => {
	const isHorizontal = className?.includes("horizontal");
	const isGrid = className?.includes("grid");
	const isFoldOrVertical = className?.includes("fold") || className?.includes("vertical");
	const gridTemplateColumns = isHorizontal ? `repeat(${$count}, minmax(420px, 1fr))` : isGrid ? "repeat(2, minmax(0, 1fr))" : isFoldOrVertical ? "repeat(1, minmax(0, 1fr))" : void 0;
	const overflowStyle = isHorizontal ? {
		overflowX: "auto",
		overflowY: "hidden"
	} : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(scrollbar_default, {
		className: classNames("[&.multi-select-mode_.message-content-container]:overflow-y-hidden! grid w-full gap-4 overflow-y-visible [&.fold]:gap-2 [&.grid]:grid-rows-[auto] [&.horizontal]:overflow-x-auto [&.horizontal]:overflow-y-hidden [&.horizontal]:pb-1 [&.multi-select-mode]:gap-2.5 [&.multi-select-mode_.MessageFooter]:hidden [&.multi-select-mode_.grid]:h-auto [&.multi-select-mode_.message-content-container]:pointer-events-none [&.multi-select-mode_.message-content-container]:max-h-[200px] [&.multi-select-mode_.message]:rounded-[10px] [&.multi-select-mode_.message]:border-[0.5px] [&.multi-select-mode_.message]:border-border [&.multi-select-mode_.message]:p-2.5", className),
		style: {
			gridTemplateColumns,
			...overflowStyle,
			...style
		},
		...props
	});
};
var MessageWrapper = ({ className, $isInPopover, ...props }) => {
	const isHorizontal = className?.includes("horizontal");
	const isGridCard = className?.includes("grid") && !className?.includes("in-popover");
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.message",
		className: classNames([
			"[&.horizontal_.message-content-container]:overflow-y-auto! [&.fold.selected]:inline-block [&.fold]:hidden [&.grid]:block [&.grid]:h-[300px] [&.grid]:cursor-pointer [&.grid]:overflow-y-hidden [&.grid]:rounded-[10px] [&.grid]:border-[0.5px] [&.grid]:border-border [&.grid_.MessageFooter]:mt-0.5 [&.grid_.MessageFooter]:mb-0.5 [&.grid_.MessageFooter]:ml-0 [&.grid_.message-body-column]:h-full [&.grid_.message-body-column]:min-h-0 [&.grid_.message-body-content]:flex [&.grid_.message-body-content]:min-h-0 [&.grid_.message-body-content]:flex-1 [&.grid_.message-content-container]:pointer-events-none [&.grid_.message-content-container]:flex-1 [&.grid_.message-content-container]:overflow-hidden [&.grid_.message-content-container]:pl-0 [&.grid_.message-header]:h-full [&.grid_.message]:h-full [&.grid_.message]:pt-0 [&.horizontal]:overflow-y-visible [&.horizontal]:p-px [&.horizontal_.MessageFooter]:mt-0.5 [&.horizontal_.MessageFooter]:mb-0.5 [&.horizontal_.MessageFooter]:ml-0 [&.horizontal_.message-body-column]:h-full [&.horizontal_.message-body-column]:min-h-0 [&.horizontal_.message-body-content]:flex [&.horizontal_.message-body-content]:min-h-0 [&.horizontal_.message-body-content]:flex-1 [&.horizontal_.message-content-container]:max-h-[calc(100vh-350px)] [&.horizontal_.message-content-container]:flex-1 [&.horizontal_.message-content-container]:pl-0 [&.horizontal_.message-header]:h-full [&.horizontal_.message]:h-full [&.horizontal_.message]:rounded-[10px] [&.horizontal_.message]:border-[0.5px] [&.horizontal_.message]:border-border [&.horizontal_.message]:p-2.5 [&.in-popover]:h-auto [&.in-popover]:max-h-[50vh] [&.in-popover]:cursor-default [&.in-popover]:overflow-y-auto [&.in-popover]:border-none [&.in-popover_.MessageFooter]:ml-0 [&.in-popover_.message-content-container]:pointer-events-auto [&.in-popover_.message-content-container]:pl-0",
			{ "p-2.5": isGridCard },
			className
		]),
		...mergeUiProps(props, "chat.message"),
		style: isHorizontal ? {
			overflowY: "visible",
			...props.style
		} : props.style
	});
};
var GridMessagePopover = ({ content, triggerContent, trigger }) => {
	const [open, setOpen] = (0, import_react.useState)(false);
	const isHover = trigger === "hover";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, {
		open: isHover ? open : void 0,
		onOpenChange: isHover ? setOpen : void 0,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverTrigger, {
			asChild: true,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				onMouseEnter: isHover ? () => setOpen(true) : void 0,
				onMouseLeave: isHover ? () => setOpen(false) : void 0,
				children: triggerContent
			}) })
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContent, {
			onMouseEnter: isHover ? () => setOpen(true) : void 0,
			onMouseLeave: isHover ? () => setOpen(false) : void 0,
			className: "z-1000 max-h-[60vh] w-auto max-w-[60vw] overflow-y-auto p-4",
			side: "top",
			align: "center",
			children: content
		})]
	});
};
function messageArrayShallowEqual(a$1, b$1) {
	if (a$1 === b$1) return true;
	if (a$1.length !== b$1.length) return false;
	for (let i$1 = 0; i$1 < a$1.length; i$1++) if (a$1[i$1] !== b$1[i$1]) return false;
	return true;
}
function messagePartsShallowEqual(previous, next, messages) {
	if (previous === next) return true;
	return messages.every((message) => previous?.[message.id] === next?.[message.id]);
}
var MessageGroup_default = (0, import_react.memo)(MessageGroup, (prev, next) => {
	return prev.captureMode === next.captureMode && prev.isLatestAssistantGroup === next.isLatestAssistantGroup && prev.directAssistantModelsByUserId === next.directAssistantModelsByUserId && prev.messageTail === next.messageTail && messageArrayShallowEqual(prev.messages, next.messages) && messagePartsShallowEqual(prev.partsByMessageId, next.partsByMessageId, prev.messages);
});
function getMessageGroupKey(message) {
	return message.role === "assistant" && message.parentId ? `assistant${message.parentId}` : message.role + message.id;
}
function groupMessageListItems(messages) {
	const grouped = {};
	for (const message of messages) {
		const key = getMessageGroupKey(message);
		grouped[key] ??= [];
		grouped[key].push(message);
	}
	return grouped;
}
function getLatestAssistantGroupKey(messages) {
	const latestAssistantMessage = [...messages].reverse().find((message) => message.role === "assistant");
	return latestAssistantMessage ? getMessageGroupKey(latestAssistantMessage) : void 0;
}
var partMatchCache = /* @__PURE__ */ new WeakMap();
var searchTextCache = /* @__PURE__ */ new WeakMap();
function getPartSearchText(part, renderAsMarkdown) {
	const source = part.text ?? "";
	const cached = searchTextCache.get(part);
	if (cached?.source === source && cached.renderAsMarkdown === renderAsMarkdown) return cached.text;
	const text = renderAsMarkdown ? markdownToPlainText(source) : source;
	searchTextCache.set(part, {
		renderAsMarkdown,
		source,
		text
	});
	return text;
}
function getTopLevelPartEntries(parts) {
	return parts.flatMap((part, index) => hasPartParentToolCallId(part) ? [] : [{
		part,
		index
	}]);
}
function projectMessageSearchDocuments(messages, partsByMessageId, options) {
	return messages.flatMap((message) => {
		if (options.excludedMessageIds?.has(message.id)) return [];
		if (message.role !== "assistant" && !(options.includeUser && message.role === "user")) return [];
		if (message.role === "assistant" && message.status === "pending") return [];
		const entries = getTopLevelPartEntries(partsByMessageId[message.id] ?? []);
		return (message.role === "assistant" ? projectCompletedMessageParts(entries).resultEntries : entries).flatMap((entry) => {
			if (entry.part.type !== "text" || !entry.part.text) return [];
			return [{
				messageId: message.id,
				partId: `${message.id}-part-${entry.index}`,
				role: message.role,
				text: getPartSearchText(entry.part, message.role === "assistant" || options.renderUserTextAsMarkdown),
				sourcePart: entry.part
			}];
		});
	});
}
function getMatchCount(document$1, searchText, options) {
	const criteriaKey = `${searchText}\u0000${options.caseSensitive ? "1" : "0"}${options.wholeWord ? "1" : "0"}`;
	const cached = partMatchCache.get(document$1.sourcePart);
	if (cached?.criteriaKey === criteriaKey && cached.text === document$1.text) return cached.count;
	const count = findTextMatches(document$1.text, searchText, options).length;
	partMatchCache.set(document$1.sourcePart, {
		criteriaKey,
		text: document$1.text,
		count
	});
	return count;
}
function computeMessageSearchMatches(messages, partsByMessageId, searchText, options) {
	const trimmed = searchText.trim();
	if (!trimmed) return [];
	const groupedMessages = groupMessageListItems(messages);
	return Object.entries(groupedMessages).flatMap(([groupKey, groupMessages]) => {
		const documents = projectMessageSearchDocuments(groupMessages, partsByMessageId, options);
		if (isAssistantMultiModelGroup(groupMessages)) {
			const matchedDocument = documents.find((document$1) => getMatchCount(document$1, trimmed, options) > 0);
			return matchedDocument ? [{
				type: "message-group",
				key: `message-group:${groupKey}`,
				messageId: matchedDocument.messageId,
				role: "assistant"
			}] : [];
		}
		return documents.flatMap((document$1) => {
			const count = getMatchCount(document$1, trimmed, options);
			return Array.from({ length: count }, (_$1, occurrence) => ({
				type: "text",
				key: `${document$1.partId}:${occurrence}`,
				messageId: document$1.messageId,
				partId: document$1.partId,
				role: document$1.role,
				occurrence
			}));
		});
	});
}
var MESSAGE_PART_SELECTOR = "[data-message-part-id]";
var MESSAGE_SEARCH_EXCLUDED_ELEMENT_SELECTOR = "button,[role=\"button\"],[data-citation],[data-message-search-exclude],[aria-hidden=\"true\"],[data-streamdown=\"code-block-header\"],.code-block-header,.code-toolbar,script,style";
function getRangeElement(range) {
	const container = range.commonAncestorContainer;
	return container instanceof HTMLElement ? container : container.parentElement;
}
function createMessageSearchNodeFilter() {
	return { acceptNode(node) {
		const parent = node.parentElement;
		if (!parent || parent.closest(MESSAGE_SEARCH_EXCLUDED_ELEMENT_SELECTOR)) return NodeFilter.FILTER_REJECT;
		return NodeFilter.FILTER_ACCEPT;
	} };
}
function getMountedMessagePartElements(scope) {
	const elements = /* @__PURE__ */ new Map();
	for (const element of scope.querySelectorAll(MESSAGE_PART_SELECTOR)) {
		const partId = element.dataset.messagePartId;
		if (partId && !elements.has(partId)) elements.set(partId, element);
	}
	return elements;
}
function requestUserMessagePartExpansion(partElement) {
	const toggle = partElement.querySelector("[data-user-message-content-toggle]");
	if (!toggle || toggle.getAttribute("aria-expanded") !== "false") return false;
	toggle.click();
	return true;
}
function canScrollAxis(element, axis) {
	const style = window.getComputedStyle(element);
	const overflow = axis === "x" ? style.overflowX : style.overflowY;
	if (overflow === "visible" || overflow === "unset") return false;
	return axis === "x" ? element.scrollWidth > element.clientWidth + 1 : element.scrollHeight > element.clientHeight + 1;
}
function revealRangeInElement(range, element) {
	const rangeRect = range.getBoundingClientRect();
	const elementRect = element.getBoundingClientRect();
	if (canScrollAxis(element, "x") && (rangeRect.left < elementRect.left || rangeRect.right > elementRect.right)) element.scrollLeft += rangeRect.left + rangeRect.width / 2 - (elementRect.left + elementRect.width / 2);
	if (canScrollAxis(element, "y") && (rangeRect.top < elementRect.top || rangeRect.bottom > elementRect.bottom)) element.scrollTop += rangeRect.top + rangeRect.height / 2 - (elementRect.top + elementRect.height / 2);
}
function revealRangeInNestedScrollContainers(range, outerScroller) {
	const rangeElement = getRangeElement(range);
	if (!rangeElement) return;
	let ancestor = rangeElement.parentElement;
	while (ancestor && ancestor !== outerScroller) {
		revealRangeInElement(range, ancestor);
		ancestor = ancestor.parentElement;
	}
}
var EMPTY_MATCHES = [];
var MATCHES_HIGHLIGHT = "message-search-matches";
var CURRENT_HIGHLIGHT = "message-search-current";
var clearHighlights = () => {
	if (!supportsCustomHighlights()) return;
	CSS.highlights.delete(MATCHES_HIGHLIGHT);
	CSS.highlights.delete(CURRENT_HIGHLIGHT);
};
var clearCurrentHighlight = () => {
	if (!supportsCustomHighlights()) return;
	CSS.highlights.delete(CURRENT_HIGHLIGHT);
};
var isRangeConnected = (range) => range.commonAncestorContainer.isConnected;
var getCriteriaKey = (query, caseSensitive, wholeWord, includeUser) => `${query}\u0000${caseSensitive ? "1" : "0"}${wholeWord ? "1" : "0"}${includeUser ? "1" : "0"}`;
const MessageListSearch = ({ messages, partsByMessageId, renderUserTextAsMarkdown, excludedMessageIds, isStreaming, locateMessage, scrollToRange, getOuterScroller, scopeRef }) => {
	const isActiveTab = useIsActiveTab();
	const searchRef = (0, import_react.useRef)(null);
	const [searchState, setSearchState] = (0, import_react.useState)(() => ({ ...INITIAL_FIND_BAR_STATE }));
	const [cursor, setCursor] = (0, import_react.useState)(null);
	const [navigationSequence, setNavigationSequence] = (0, import_react.useState)(0);
	const pendingNavigationRef = (0, import_react.useRef)(null);
	const settledHighlightFrameRef = (0, import_react.useRef)(null);
	const { enabled, query, caseSensitive, wholeWord, includeUser } = searchState;
	const trimmedQuery = (0, import_react.useDeferredValue)(query).trim();
	const criteriaKey = getCriteriaKey(trimmedQuery, caseSensitive, wholeWord, includeUser);
	const searchSourceRef = (0, import_react.useRef)({
		messages,
		partsByMessageId
	});
	if (!isStreaming) searchSourceRef.current = {
		messages,
		partsByMessageId
	};
	const { messages: searchMessages, partsByMessageId: searchParts } = searchSourceRef.current;
	const matches = (0, import_react.useMemo)(() => enabled && trimmedQuery ? computeMessageSearchMatches(searchMessages, searchParts, trimmedQuery, {
		caseSensitive,
		wholeWord,
		includeUser,
		renderUserTextAsMarkdown,
		excludedMessageIds
	}) : EMPTY_MATCHES, [
		caseSensitive,
		enabled,
		excludedMessageIds,
		includeUser,
		searchMessages,
		searchParts,
		renderUserTextAsMarkdown,
		trimmedQuery,
		wholeWord
	]);
	const currentIndex = (0, import_react.useMemo)(() => cursor?.criteriaKey === criteriaKey ? matches.findIndex((match) => match.key === cursor.matchKey) : -1, [
		criteriaKey,
		cursor,
		matches
	]);
	const current = currentIndex >= 0 ? matches[currentIndex] : null;
	const currentRef = (0, import_react.useRef)(current);
	currentRef.current = current;
	const matchesByPartId = (0, import_react.useMemo)(() => {
		const byPartId = /* @__PURE__ */ new Map();
		for (const match of matches) {
			if (match.type !== "text") continue;
			const partMatches = byPartId.get(match.partId);
			if (partMatches) partMatches.push(match);
			else byPartId.set(match.partId, [match]);
		}
		return byPartId;
	}, [matches]);
	(0, import_react.useEffect)(() => {
		if (settledHighlightFrameRef.current !== null) {
			cancelAnimationFrame(settledHighlightFrameRef.current);
			settledHighlightFrameRef.current = null;
		}
		pendingNavigationRef.current = null;
		clearHighlights();
	}, [criteriaKey, enabled]);
	(0, import_react.useEffect)(() => {
		const pending = pendingNavigationRef.current;
		if (pending && !matches.some((match) => match.key === pending.match.key)) pendingNavigationRef.current = null;
	}, [matches]);
	const rebuildMountedHighlights = (0, import_react.useCallback)(() => {
		const scope = scopeRef.current;
		if (!enabled || !trimmedQuery || !scope) {
			clearHighlights();
			return;
		}
		if (isStreaming) return;
		const mountedParts = getMountedMessagePartElements(scope);
		const searchOptions = {
			caseSensitive,
			wholeWord
		};
		const filter = createMessageSearchNodeFilter();
		const nextRangesByPartId = /* @__PURE__ */ new Map();
		for (const [partId, partElement] of mountedParts) {
			const partMatches = matchesByPartId.get(partId);
			if (!partMatches) continue;
			nextRangesByPartId.set(partId, findRangesInScope(partElement, trimmedQuery, searchOptions, filter).slice(0, partMatches.length));
		}
		clearHighlights();
		if (supportsCustomHighlights()) {
			const mountedRanges = [...nextRangesByPartId.values()].flat();
			if (mountedRanges.length > 0) CSS.highlights.set(MATCHES_HIGHLIGHT, new Highlight(...mountedRanges));
		}
		const latestCurrent = currentRef.current;
		if (latestCurrent?.type !== "text" || !supportsCustomHighlights()) return;
		const currentRange = nextRangesByPartId.get(latestCurrent.partId)?.[latestCurrent.occurrence];
		if (currentRange) CSS.highlights.set(CURRENT_HIGHLIGHT, new Highlight(currentRange));
	}, [
		caseSensitive,
		enabled,
		isStreaming,
		matchesByPartId,
		scopeRef,
		trimmedQuery,
		wholeWord
	]);
	const syncPendingNavigation = (0, import_react.useCallback)(() => {
		const pending = pendingNavigationRef.current;
		const scope = scopeRef.current;
		if (!pending || pending.isWaitingForHighlight || pending.criteriaKey !== criteriaKey || !scope) return;
		const partElement = getMountedMessagePartElements(scope).get(pending.match.partId);
		if (!partElement) return;
		const partMatchCount = matchesByPartId.get(pending.match.partId)?.length ?? 0;
		const range = findRangesInScope(partElement, trimmedQuery, {
			caseSensitive,
			wholeWord
		}, createMessageSearchNodeFilter()).slice(0, partMatchCount)[pending.match.occurrence];
		if (!range) {
			if (pending.match.role === "user" && requestUserMessagePartExpansion(partElement)) return;
			pendingNavigationRef.current = null;
			clearCurrentHighlight();
			locateMessage(pending.match.messageId);
			return;
		}
		pending.isWaitingForHighlight = true;
		revealRangeInNestedScrollContainers(range, getOuterScroller() ?? scope);
		scrollToRange(range);
		settledHighlightFrameRef.current = requestAnimationFrame(() => {
			settledHighlightFrameRef.current = requestAnimationFrame(() => {
				settledHighlightFrameRef.current = null;
				const latestPending = pendingNavigationRef.current;
				if (latestPending?.criteriaKey !== pending.criteriaKey || latestPending.match.key !== pending.match.key) return;
				pendingNavigationRef.current = null;
				if (supportsCustomHighlights() && isRangeConnected(range)) CSS.highlights.set(CURRENT_HIGHLIGHT, new Highlight(range));
			});
		});
	}, [
		caseSensitive,
		criteriaKey,
		getOuterScroller,
		locateMessage,
		matchesByPartId,
		scopeRef,
		scrollToRange,
		trimmedQuery,
		wholeWord
	]);
	const rebuildMountedHighlightsRef = (0, import_react.useRef)(rebuildMountedHighlights);
	const syncPendingNavigationRef = (0, import_react.useRef)(syncPendingNavigation);
	rebuildMountedHighlightsRef.current = rebuildMountedHighlights;
	syncPendingNavigationRef.current = syncPendingNavigation;
	(0, import_react.useEffect)(() => {
		rebuildMountedHighlights();
	}, [rebuildMountedHighlights]);
	(0, import_react.useEffect)(() => {
		syncPendingNavigation();
	}, [navigationSequence, syncPendingNavigation]);
	(0, import_react.useEffect)(() => {
		if (!enabled) return;
		const scope = scopeRef.current;
		if (!scope) return;
		let frame = null;
		const observer = new MutationObserver(() => {
			if (frame !== null) return;
			if (isStreaming && !pendingNavigationRef.current) return;
			frame = requestAnimationFrame(() => {
				frame = null;
				if (pendingNavigationRef.current) syncPendingNavigationRef.current();
				else if (!isStreaming) rebuildMountedHighlightsRef.current();
			});
		});
		observer.observe(scope, {
			childList: true,
			subtree: true,
			characterData: true
		});
		return () => {
			observer.disconnect();
			if (frame !== null) cancelAnimationFrame(frame);
		};
	}, [
		enabled,
		isStreaming,
		scopeRef
	]);
	(0, import_react.useEffect)(() => () => {
		if (settledHighlightFrameRef.current !== null) cancelAnimationFrame(settledHighlightFrameRef.current);
		clearHighlights();
	}, []);
	useCommandHandler("chat.message.search", () => {
		const selectedText = window.getSelection()?.toString().trim();
		searchRef.current?.enable(selectedText || void 0);
	}, { enabled: isActiveTab });
	useHotkeys("esc", () => searchRef.current?.disable(), { enabled }, [enabled]);
	const navigateToMatch = (0, import_react.useCallback)((match) => {
		if (settledHighlightFrameRef.current !== null) {
			cancelAnimationFrame(settledHighlightFrameRef.current);
			settledHighlightFrameRef.current = null;
		}
		clearCurrentHighlight();
		setCursor({
			criteriaKey,
			matchKey: match.key
		});
		if (match.type === "message-group") {
			pendingNavigationRef.current = null;
			locateMessage(match.messageId);
			return;
		}
		pendingNavigationRef.current = {
			criteriaKey,
			match,
			isWaitingForHighlight: false
		};
		setNavigationSequence((sequence) => sequence + 1);
		const scope = scopeRef.current;
		if (!scope || !getMountedMessagePartElements(scope).has(match.partId)) locateMessage(match.messageId);
	}, [
		criteriaKey,
		locateMessage,
		scopeRef
	]);
	const step = (0, import_react.useCallback)((delta) => {
		if (matches.length === 0) return;
		navigateToMatch(matches[currentIndex >= 0 ? (currentIndex + delta + matches.length) % matches.length : delta > 0 ? 0 : matches.length - 1]);
	}, [
		currentIndex,
		matches,
		navigateToMatch
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FindBar, {
		ref: searchRef,
		matchCount: matches.length,
		currentIndex,
		onNavigate: step,
		onStateChange: setSearchState
	});
};
var EXCLUDED_SELECTORS = [
	".MessageFooter",
	".code-toolbar",
	".group-menu-bar",
	".code-block",
	".message-editor",
	".table-wrapper"
];
var RIGHT_GAP = 16;
var TRIGGER_WIDTH = 60;
var MessageNavigation = ({ scrollContainerRef, getMessageElement, messages, scrollToMessageId, scrollToTop, scrollToBottom }) => {
	const { t: t$2 } = useTranslation();
	const [isVisible, setIsVisible] = (0, import_react.useState)(false);
	const timerKey = "hide";
	const { setTimeoutTimer, clearTimeoutTimer } = useTimer();
	const [manuallyClosedUntil, setManuallyClosedUntil] = (0, import_react.useState)(null);
	const lastMoveTime = (0, import_react.useRef)(0);
	const isHoveringNavigationRef = (0, import_react.useRef)(false);
	const isPointerInTriggerAreaRef = (0, import_react.useRef)(false);
	const clearHideTimer = (0, import_react.useCallback)(() => {
		clearTimeoutTimer(timerKey);
	}, [clearTimeoutTimer]);
	const scheduleHide = (0, import_react.useCallback)((delay) => {
		setTimeoutTimer(timerKey, () => {
			setIsVisible(false);
		}, delay);
	}, [setTimeoutTimer]);
	const showNavigation = (0, import_react.useCallback)(() => {
		if (manuallyClosedUntil && Date.now() < manuallyClosedUntil) return;
		setIsVisible(true);
		clearHideTimer();
	}, [clearHideTimer, manuallyClosedUntil]);
	const handleNavigationMouseEnter = (0, import_react.useCallback)(() => {
		if (manuallyClosedUntil && Date.now() < manuallyClosedUntil) return;
		isHoveringNavigationRef.current = true;
		showNavigation();
	}, [manuallyClosedUntil, showNavigation]);
	const handleNavigationMouseLeave = (0, import_react.useCallback)(() => {
		isHoveringNavigationRef.current = false;
		scheduleHide(500);
	}, [scheduleHide]);
	const getCurrentVisibleIndex = () => {
		const userMessages = messages.filter((message) => message.role === "user");
		const assistantMessages = messages.filter((message) => message.role === "assistant");
		const userIndexById = new Map(userMessages.map((message, index) => [message.id, index]));
		const precedingUserIndexByAssistantId = /* @__PURE__ */ new Map();
		let precedingUserIndex;
		for (const message of messages) if (message.role === "user") precedingUserIndex = userIndexById.get(message.id);
		else if (message.role === "assistant" && precedingUserIndex !== void 0) precedingUserIndexByAssistantId.set(message.id, precedingUserIndex);
		const scrollContainer = scrollContainerRef.current;
		if (!scrollContainer) return -1;
		const containerRect = scrollContainer.getBoundingClientRect();
		const visibleThreshold = containerRect.height * .1;
		let visibleIndices = [];
		for (let i$1 = 0; i$1 < userMessages.length; i$1++) {
			const messageElement = getMessageElement(userMessages[i$1].id);
			if (!messageElement) continue;
			const messageRect = messageElement.getBoundingClientRect();
			const visibleHeight = Math.min(messageRect.bottom, containerRect.bottom) - Math.max(messageRect.top, containerRect.top);
			if (visibleHeight > 0 && visibleHeight >= Math.min(messageRect.height, visibleThreshold)) visibleIndices.push(i$1);
		}
		if (visibleIndices.length > 0) return Math.min(...visibleIndices);
		visibleIndices = [];
		for (let i$1 = 0; i$1 < assistantMessages.length; i$1++) {
			const assistantMessage = assistantMessages[i$1];
			const messageElement = getMessageElement(assistantMessage.id);
			if (!messageElement) continue;
			const messageRect = messageElement.getBoundingClientRect();
			const visibleHeight = Math.min(messageRect.bottom, containerRect.bottom) - Math.max(messageRect.top, containerRect.top);
			if (visibleHeight > 0 && visibleHeight >= Math.min(messageRect.height, visibleThreshold)) {
				const userIndex = assistantMessage.parentId != null ? userIndexById.get(assistantMessage.parentId) : precedingUserIndexByAssistantId.get(assistantMessage.id);
				if (userIndex !== void 0) visibleIndices.push(userIndex);
			}
		}
		if (visibleIndices.length > 0) return Math.min(...visibleIndices);
		return -1;
	};
	const handleCloseMessageNavigation = () => {
		setIsVisible(false);
		isHoveringNavigationRef.current = false;
		isPointerInTriggerAreaRef.current = false;
		clearHideTimer();
		setManuallyClosedUntil(Date.now() + 6e4);
	};
	const handleScrollToTop = () => {
		showNavigation();
		scrollToTop();
	};
	const handleScrollToBottom = () => {
		showNavigation();
		scrollToBottom();
	};
	const handleNextMessage = () => {
		showNavigation();
		const userMessages = messages.filter((message) => message.role === "user");
		const assistantMessages = messages.filter((message) => message.role === "assistant");
		if (userMessages.length === 0 && assistantMessages.length === 0) return scrollToBottom();
		const visibleIndex = getCurrentVisibleIndex();
		if (visibleIndex === -1) return scrollToBottom();
		const targetIndex = visibleIndex + 1;
		if (targetIndex >= userMessages.length) return scrollToBottom();
		scrollToMessageId(userMessages[targetIndex].id);
	};
	const handlePrevMessage = () => {
		showNavigation();
		const userMessages = messages.filter((message) => message.role === "user");
		const assistantMessages = messages.filter((message) => message.role === "assistant");
		if (userMessages.length === 0 && assistantMessages.length === 0) return scrollToTop();
		const visibleIndex = getCurrentVisibleIndex();
		if (visibleIndex === -1) return scrollToTop();
		const targetIndex = visibleIndex - 1;
		if (targetIndex < 0) return scrollToTop();
		scrollToMessageId(userMessages[targetIndex].id);
	};
	(0, import_react.useEffect)(() => {
		const scrollContainer = scrollContainerRef.current;
		if (!scrollContainer) return;
		const handleScroll = () => {
			if (isPointerInTriggerAreaRef.current || isHoveringNavigationRef.current) showNavigation();
		};
		const handleMouseMove = (event) => {
			if (manuallyClosedUntil && Date.now() < manuallyClosedUntil) return;
			const now = Date.now();
			if (now - lastMoveTime.current < 50) return;
			lastMoveTime.current = now;
			const containerRect = scrollContainer.getBoundingClientRect();
			const rightPosition = containerRect.right - RIGHT_GAP - TRIGGER_WIDTH;
			const topPosition = containerRect.top + containerRect.height * .35;
			const height = containerRect.height * .3;
			const target = event.target;
			if (!EXCLUDED_SELECTORS.some((selector) => target.closest(selector)) && event.clientX > rightPosition && event.clientX < rightPosition + TRIGGER_WIDTH + RIGHT_GAP && event.clientY > topPosition && event.clientY < topPosition + height) {
				if (!isPointerInTriggerAreaRef.current) {
					isPointerInTriggerAreaRef.current = true;
					showNavigation();
				}
			} else if (isPointerInTriggerAreaRef.current) {
				isPointerInTriggerAreaRef.current = false;
				if (!isHoveringNavigationRef.current) scheduleHide(500);
			}
		};
		scrollContainer.addEventListener("scroll", handleScroll, { passive: true });
		window.addEventListener("mousemove", handleMouseMove);
		const handleMessagesMouseLeave = () => {
			if (!isHoveringNavigationRef.current) {
				isPointerInTriggerAreaRef.current = false;
				scheduleHide(500);
			}
		};
		scrollContainer.addEventListener("mouseleave", handleMessagesMouseLeave);
		return () => {
			scrollContainer.removeEventListener("scroll", handleScroll);
			window.removeEventListener("mousemove", handleMouseMove);
			scrollContainer.removeEventListener("mouseleave", handleMessagesMouseLeave);
			clearHideTimer();
		};
	}, [
		scrollContainerRef,
		manuallyClosedUntil,
		scheduleHide,
		showNavigation,
		clearHideTimer
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationContainer, {
		$isVisible: isVisible,
		onMouseEnter: handleNavigationMouseEnter,
		onMouseLeave: handleNavigationMouseLeave,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ButtonGroup, {
			$isVisible: isVisible,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					placement: "left",
					content: t$2("chat.navigation.close"),
					delay: 500,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationButton, {
						variant: "ghost",
						onClick: handleCloseMessageNavigation,
						"aria-label": t$2("chat.navigation.close"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, {})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Divider, {}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					placement: "left",
					content: t$2("chat.navigation.top"),
					delay: 500,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationButton, {
						variant: "ghost",
						onClick: handleScrollToTop,
						"aria-label": t$2("chat.navigation.top"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronsUp, {})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Divider, {}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					placement: "left",
					content: t$2("chat.navigation.prev"),
					delay: 500,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationButton, {
						variant: "ghost",
						onClick: handlePrevMessage,
						"aria-label": t$2("chat.navigation.prev"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrowUp, {})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Divider, {}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					placement: "left",
					content: t$2("chat.navigation.next"),
					delay: 500,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationButton, {
						variant: "ghost",
						onClick: handleNextMessage,
						"aria-label": t$2("chat.navigation.next"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrowDown, {})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Divider, {}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					placement: "left",
					content: t$2("chat.navigation.bottom"),
					delay: 500,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationButton, {
						variant: "ghost",
						onClick: handleScrollToBottom,
						"aria-label": t$2("chat.navigation.bottom"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronsDown, {})
					})
				})
			]
		})
	});
};
var NavigationContainer = ({ className, $isVisible, style, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.navigation",
	className: ["absolute top-1/2 right-4 z-[80] transition-[transform,opacity] duration-300 ease-in-out", className].filter(Boolean).join(" "),
	style: {
		opacity: $isVisible ? 1 : 0,
		pointerEvents: $isVisible ? "auto" : "none",
		transform: `translateY(-50%) translateX(${$isVisible ? "0" : "32px"})`,
		...style
	},
	...mergeUiProps(props, "chat.navigation")
});
var ButtonGroup = ({ className, $isVisible, style, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.button-group",
	className: ["flex flex-col overflow-hidden rounded-lg border border-border bg-background shadow-[0_2px_8px_rgba(0,0,0,0.1)] transition-[backdrop-filter,background] duration-250 ease-in-out", className].filter(Boolean).join(" "),
	style: {
		backdropFilter: $isVisible ? "blur(8px)" : "blur(0px)",
		...style
	},
	...mergeUiProps(props, "chat.button-group")
});
var NavigationButton = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
	className: ["flex h-7 w-7 items-center justify-center rounded-none border-none text-foreground transition-all duration-200 ease-in-out hover:bg-accent hover:text-primary [&_svg]:size-3.5", className].filter(Boolean).join(" "),
	...props
});
var Divider = ({ className, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	"data-ui": "chat.divider",
	className: ["m-0 h-px bg-border", className].filter(Boolean).join(" "),
	...mergeUiProps(props, "chat.divider")
});
var MessageNavigation_default = MessageNavigation, import_react_dom = require_react_dom(), d = null, { min: a, max: h, abs: p, floor: g } = Math, v = (e$1, t$2, o$1) => a(o$1, h(t$2, e$1)), m = (e$1) => [...e$1].sort((e$2, t$2) => e$2 - t$2), _ = "function" == typeof queueMicrotask ? queueMicrotask : (e$1) => {
	Promise.resolve().then(e$1);
}, w = () => {
	let e$1;
	return [new Promise((t$2) => {
		e$1 = t$2;
	}), e$1];
}, S = (e$1) => {
	let t$2;
	return () => (e$1 && (t$2 = e$1(), e$1 = void 0), t$2);
}, $ = (e$1, t$2, o$1) => {
	const n$1 = o$1 ? "unshift" : "push";
	for (let o$2 = 0; o$2 < t$2; o$2++) e$1[n$1](-1);
	return e$1;
}, z = (e$1, t$2) => {
	const o$1 = e$1.t[t$2];
	return -1 === o$1 ? e$1.o : o$1;
}, y = (e$1, t$2, o$1) => {
	const n$1 = -1 === e$1.t[t$2];
	return e$1.t[t$2] = o$1, e$1.i = a(t$2, e$1.i), n$1;
}, b = (e$1, t$2) => {
	if (!e$1.l) return 0;
	if (e$1.i >= t$2) return e$1.u[t$2];
	e$1.i < 0 && (e$1.u[0] = 0, e$1.i = 0);
	let o$1 = e$1.i, n$1 = e$1.u[o$1];
	for (; o$1 < t$2;) n$1 += z(e$1, o$1), e$1.u[++o$1] = n$1;
	return e$1.i = t$2, n$1;
}, x = (e$1, t$2, o$1 = 0, n$1 = e$1.l - 1) => {
	let r$1 = o$1;
	for (; o$1 <= n$1;) {
		const i$1 = g((o$1 + n$1) / 2);
		b(e$1, i$1) <= t$2 ? (r$1 = i$1, o$1 = i$1 + 1) : n$1 = i$1 - 1;
	}
	return v(r$1, 0, e$1.l - 1);
}, I = (e$1, t$2, o$1) => {
	const n$1 = t$2 - e$1.l;
	return e$1.i = o$1 ? -1 : a(t$2 - 1, e$1.i), e$1.l = t$2, n$1 > 0 ? ($(e$1.u, n$1), $(e$1.t, n$1, o$1), e$1.o * n$1) : (e$1.u.splice(n$1), (o$1 ? e$1.t.splice(0, -n$1) : e$1.t.splice(n$1)).reduce((t$3, o$2) => t$3 - (-1 === o$2 ? e$1.o : o$2), 0));
}, k = "undefined" != typeof window, R = (e$1) => e$1.documentElement, T = (e$1) => e$1.ownerDocument, C = (e$1) => e$1.defaultView, M = /* @__PURE__ */ S(() => !!/iP(hone|od|ad)/.test(navigator.userAgent) || "MacIntel" === navigator.platform && navigator.maxTouchPoints > 0), O = /* @__PURE__ */ S(() => "scrollBehavior" in R(document).style), E = (e$1) => h(e$1.h(), e$1.p()), H = (e$1, t$2 = 40, o$1 = 0, n$1, r$1 = !1) => {
	let i$1 = !!o$1, s$1 = 1, l$1 = 0, c$1 = 0, f$1 = 0, u$1 = 0, g$1 = 0, v$1 = 0, _$1 = 0, w$1 = 0, S$1 = d, k$1 = [0, i$1 ? h(o$1 - 1, 0) : -1], R$1 = 0, T$1 = !1;
	const C$1 = ((e$2, t$3, o$2) => ({
		o: t$3,
		t: o$2 ? $(o$2.slice(0, a(e$2, o$2.length)), h(0, e$2 - o$2.length)) : $([], e$2),
		l: e$2,
		i: -1,
		u: $([], e$2 + 1)
	}))(e$1, n$1 ? n$1[1] : t$2, n$1 && n$1[0]), O$1 = /* @__PURE__ */ new Set(), E$1 = () => f$1 - c$1, H$1 = () => E$1() + g$1 + u$1, W$1 = (e$2, t$3) => ((e$3, t$4, o$2, n$2) => {
		if (n$2 = a(n$2, e$3.l - 1), b(e$3, n$2) <= t$4) {
			const r$2 = x(e$3, o$2, n$2);
			return [x(e$3, t$4, n$2, r$2), r$2];
		}
		{
			const r$2 = x(e$3, t$4, void 0, n$2);
			return [r$2, x(e$3, o$2, r$2)];
		}
	})(C$1, e$2, t$3, k$1[0]), B$1 = () => b(C$1, C$1.l), J$1 = (e$2, t$3) => {
		const o$2 = b(C$1, e$2) - g$1;
		return t$3 ? B$1() - o$2 - A$1(e$2) : o$2;
	}, A$1 = (e$2) => z(C$1, e$2), L$1 = (e$2, t$3 = -1) => C$1.t[e$2] === t$3, N$1 = (e$2) => {
		e$2 && (M() && 0 !== _$1 || S$1 && 1 === w$1 ? g$1 += e$2 : u$1 += e$2);
	};
	return {
		v: () => {
			O$1.clear();
		},
		m: () => s$1,
		_: () => ((e$2) => [e$2.t.slice(), e$2.o])(C$1),
		S: (e$2 = 200) => {
			if (!T$1 || i$1) return k$1;
			let t$3, o$2;
			if (v$1) [t$3, o$2] = k$1;
			else {
				let n$2 = h(0, H$1()), i$2 = n$2 + l$1;
				r$1 || (e$2 = h(0, e$2), 1 !== _$1 && (n$2 -= e$2), 2 !== _$1 && (i$2 += e$2)), [t$3, o$2] = k$1 = W$1(h(0, n$2), h(0, i$2)), S$1 && (t$3 = a(t$3, S$1[0]), o$2 = h(o$2, S$1[1]));
			}
			return [h(t$3, 0), a(o$2, C$1.l - 1)];
		},
		$: (e$2) => x(C$1, e$2 - c$1),
		I: L$1,
		k: J$1,
		R: A$1,
		T: () => C$1.l,
		C: () => f$1,
		M: () => 0 !== _$1,
		p: () => l$1,
		O: () => c$1,
		h: B$1,
		H: () => (v$1 = u$1, u$1 = 0, [v$1, 2 === w$1]),
		W: (e$2, t$3) => {
			const o$2 = [e$2, t$3];
			return O$1.add(o$2), () => {
				O$1.delete(o$2);
			};
		},
		B: (e$2, t$3) => {
			let o$2, n$2, a$1 = 0;
			switch (e$2) {
				case 1: {
					if (t$3 === f$1 && 0 === w$1) break;
					const e$3 = v$1;
					v$1 = 0;
					const o$3 = t$3 - f$1, r$2 = p(o$3);
					e$3 && r$2 < p(e$3) + 1 || 0 !== w$1 || (_$1 = o$3 < 0 ? 2 : 1), i$1 && (i$1 = !1), f$1 = t$3, a$1 = 4;
					const s$2 = E$1();
					s$2 >= -l$1 && s$2 <= B$1() && (a$1 += 1, n$2 = r$2 > l$1);
					break;
				}
				case 2:
					a$1 = 8, 0 !== _$1 && (o$2 = !0, a$1 += 1), _$1 = 0, w$1 = 0, S$1 = d;
					break;
				case 3: {
					const e$3 = t$3.filter(([e$4, t$4]) => !L$1(e$4, t$4));
					if (!e$3.length) break;
					N$1(e$3.reduce((e$4, [t$4, o$3]) => {
						let n$3;
						if (2 === w$1) n$3 = !0;
						else if (S$1 && 1 === w$1) n$3 = t$4 < S$1[0];
						else {
							const e$5 = E$1(), o$4 = J$1(t$4), r$2 = A$1(t$4);
							n$3 = 1 !== _$1 && 0 === w$1 ? o$4 + r$2 < e$5 : o$4 < e$5 && o$4 + r$2 < e$5 + l$1;
						}
						return n$3 && (e$4 += o$3 - A$1(t$4)), e$4;
					}, 0));
					for (const [t$4, o$3] of e$3) {
						const e$4 = A$1(t$4), n$3 = y(C$1, t$4, o$3);
						r$1 && (R$1 += n$3 ? o$3 : o$3 - e$4);
					}
					r$1 && l$1 && R$1 > l$1 && (N$1(((e$4, t$4) => {
						let o$3 = 0;
						const n$3 = [];
						e$4.t.forEach((e$5, r$3) => {
							-1 !== e$5 && (n$3.push(e$5), r$3 < t$4 && o$3++);
						}), e$4.i = -1;
						const r$2 = m(n$3), i$2 = r$2.length, s$2 = i$2 / 2 | 0, l$2 = i$2 % 2 == 0 ? (r$2[s$2 - 1] + r$2[s$2]) / 2 : r$2[s$2], c$2 = e$4.o;
						return ((e$4.o = l$2) - c$2) * h(t$4 - o$3, 0);
					})(C$1, x(C$1, H$1()))), r$1 = !1), a$1 = 3, n$2 = !0;
					break;
				}
				case 4:
					l$1 !== t$3 && (l$1 || (T$1 = n$2 = !0), l$1 = t$3, a$1 = 3);
					break;
				case 5:
					t$3[1] ? (N$1(I(C$1, t$3[0], !0)), w$1 = 2, a$1 = 1) : (I(C$1, t$3[0]), a$1 = 1);
					break;
				case 6:
					c$1 = t$3;
					break;
				case 7:
					w$1 = 1;
					break;
				case 8: S$1 = W$1(t$3, t$3 + l$1), a$1 = 1;
			}
			a$1 && (s$1 = 1 + (2147483647 & s$1), o$2 && g$1 && (u$1 += g$1, g$1 = 0), O$1.forEach(([e$3, t$4]) => {
				a$1 & e$3 && t$4(n$2);
			}));
		}
	};
}, W = setTimeout, B = (e$1, t$2) => t$2 ? -e$1 : e$1, J = (e$1, t$2, o$1, n$1, r$1, i$1) => {
	const s$1 = Date.now;
	let l$1 = 0, c$1 = !1, f$1 = !1, u$1 = !1, a$1 = !1;
	const h$1 = (() => {
		let t$3;
		const o$2 = () => {
			t$3 != d && clearTimeout(t$3);
		}, n$2 = () => {
			o$2(), t$3 = W(() => {
				t$3 = d, (() => {
					if (c$1 || f$1) return c$1 = !1, void h$1();
					u$1 = !1, e$1.B(2);
				})();
			}, 150);
		};
		return n$2.J = o$2, n$2;
	})(), p$1 = () => {
		l$1 = s$1(), u$1 && (a$1 = !0), i$1 && e$1.B(6, i$1()), e$1.B(1, n$1()), h$1();
	}, g$1 = (t$3) => {
		if (c$1 || !e$1.M() || t$3.ctrlKey) return;
		const n$2 = s$1() - l$1;
		150 > n$2 && 50 < n$2 && (o$1 ? t$3.deltaX : t$3.deltaY) && (c$1 = !0);
	}, v$1 = () => {
		f$1 = !0, u$1 = a$1 = !1;
	}, m$1 = () => {
		f$1 = !1, M() && (u$1 = !0);
	};
	return t$2.addEventListener("scroll", p$1), t$2.addEventListener("wheel", g$1, { passive: !0 }), t$2.addEventListener("touchstart", v$1, { passive: !0 }), t$2.addEventListener("touchend", m$1, { passive: !0 }), {
		A: () => {
			t$2.removeEventListener("scroll", p$1), t$2.removeEventListener("wheel", g$1), t$2.removeEventListener("touchstart", v$1), t$2.removeEventListener("touchend", m$1), h$1.J();
		},
		L: () => {
			const [t$3, o$2] = e$1.H();
			t$3 && (r$1(t$3, o$2, a$1), a$1 = !1, o$2 && e$1.p() > e$1.h() && e$1.B(1, n$1()));
		}
	};
}, A = (e$1, t$2, o$1) => {
	let n$1;
	return [async (r$1, i$1) => {
		if (!await t$2()) return;
		n$1 && n$1();
		const s$1 = () => {
			const [t$3, o$2] = w();
			return n$1 = () => {
				o$2(!1);
			}, e$1.p() && W(n$1, 150), [t$3, e$1.W(2, () => {
				o$2(!0);
			})];
		};
		if (i$1 && O()) e$1.B(8, r$1()), _(async () => {
			for (;;) {
				let t$3 = !0;
				for (let [o$3, n$3] = e$1.S(); o$3 <= n$3; o$3++) if (e$1.I(o$3)) {
					t$3 = !1;
					break;
				}
				if (t$3) break;
				const [o$2, n$2] = s$1();
				try {
					if (!await o$2) return;
				} finally {
					n$2();
				}
			}
			e$1.B(7), o$1(r$1(), i$1);
		});
		else for (;;) {
			const [t$3, n$2] = s$1();
			try {
				if (e$1.B(7), o$1(r$1()), !await t$3) return;
			} finally {
				n$2();
			}
		}
	}, () => {
		n$1 && n$1();
	}];
}, L = (e$1, t$2) => {
	let o$1, n$1, r$1 = w(), i$1 = !1;
	const s$1 = t$2 ? "scrollLeft" : "scrollTop", l$1 = t$2 ? "overflowX" : "overflowY", [c$1, f$1] = A(e$1, () => r$1[0], (e$2, n$2) => {
		e$2 = B(e$2, i$1), n$2 ? o$1.scrollTo({
			[t$2 ? "left" : "top"]: e$2,
			behavior: "smooth"
		}) : o$1[s$1] = e$2;
	});
	return {
		N(c$2, u$1) {
			o$1 = u$1, t$2 && (i$1 = "rtl" === getComputedStyle(u$1).direction), n$1 = J(e$1, u$1, t$2, () => B(u$1[s$1], i$1), (t$3, o$2, n$2) => {
				if (n$2) {
					const e$2 = u$1.style, t$4 = e$2[l$1];
					e$2[l$1] = "hidden", W(() => {
						e$2[l$1] = t$4;
					});
				}
				u$1[s$1] = B(e$1.C() + t$3, i$1), o$2 && f$1();
			}), r$1[1](!0);
		},
		v() {
			n$1 && n$1.A(), r$1[1](!1), r$1 = w();
		},
		P: () => i$1,
		V(e$2) {
			c$1(() => e$2);
		},
		X(t$3) {
			t$3 += e$1.C(), c$1(() => t$3);
		},
		Y(t$3, { align: o$2, smooth: n$2, offset: r$2 = 0 } = {}) {
			if (t$3 = v(t$3, 0, e$1.T() - 1), "nearest" === o$2) {
				const n$3 = e$1.k(t$3), r$3 = e$1.C();
				if (n$3 < r$3) o$2 = "start";
				else {
					if (!(n$3 + e$1.R(t$3) > r$3 + e$1.p())) return;
					o$2 = "end";
				}
			}
			c$1(() => r$2 + e$1.O() + e$1.k(t$3) + ("end" === o$2 ? e$1.R(t$3) - e$1.p() : "center" === o$2 ? (e$1.R(t$3) - e$1.p()) / 2 : 0), n$2);
		},
		q: () => {
			n$1 && n$1.L();
		}
	};
}, V = (e$1) => {
	let t$2;
	return {
		D(o$1) {
			(t$2 || (t$2 = new (C(T(o$1))).ResizeObserver(e$1))).observe(o$1);
		},
		j(e$2) {
			t$2.unobserve(e$2);
		},
		A() {
			t$2 && t$2.disconnect();
		}
	};
}, X$1 = (e$1, t$2) => {
	let o$1;
	const n$1 = t$2 ? "width" : "height", r$1 = /* @__PURE__ */ new WeakMap(), i$1 = V((t$3) => {
		const i$2 = [];
		for (const { target: s$1, contentRect: l$1 } of t$3) if (s$1.offsetParent) if (s$1 === o$1) e$1.B(4, l$1[n$1]);
		else {
			const e$2 = r$1.get(s$1);
			e$2 != d && i$2.push([e$2, l$1[n$1]]);
		}
		i$2.length && e$1.B(3, i$2);
	});
	return {
		G(e$2) {
			i$1.D(o$1 = e$2);
		},
		U: (e$2, t$3) => (r$1.set(e$2, t$3), i$1.D(e$2), () => {
			r$1.delete(e$2), i$1.j(e$2);
		}),
		v: i$1.A
	};
}, D = k ? import_react.useLayoutEffect : import_react.useEffect, j = "current", G = (e$1, t$2) => {
	if (Array.isArray(e$1)) for (const o$1 of e$1) G(o$1, t$2);
	else null == e$1 || "boolean" == typeof e$1 || t$2.push(e$1);
}, U = (e$1, t$2) => {
	const o$1 = e$1.key;
	return null != o$1 ? o$1 : "_" + t$2;
}, F = (e$1) => {
	const t$2 = (0, import_react.useRef)(null);
	return t$2[j] || (t$2[j] = e$1());
}, K = (e$1) => {
	const t$2 = (0, import_react.useRef)(e$1);
	return D(() => {
		t$2[j] = e$1;
	}, [e$1]), t$2;
}, Q = /* @__PURE__ */ (0, import_react.memo)(({ Z: t$2, ee: o$1, te: r$1, oe: s$1, ne: l$1, re: c$1, ie: f$1, se: u$1 }) => {
	const d$1 = (0, import_react.useRef)(null);
	D(() => o$1(d$1[j], r$1), [r$1]);
	const a$1 = (0, import_react.useMemo)(() => {
		const e$1 = {
			contain: "layout style",
			position: l$1 && u$1 ? void 0 : "absolute",
			[f$1 ? "height" : "width"]: "100%",
			[f$1 ? "top" : "left"]: 0,
			[f$1 ? "left" : "top"]: s$1,
			visibility: !l$1 || u$1 ? void 0 : "hidden"
		};
		return f$1 && (e$1.display = "inline-flex"), e$1;
	}, [
		s$1,
		l$1,
		u$1,
		f$1
	]);
	return (0, import_jsx_runtime.jsx)(c$1, "string" == typeof c$1 ? {
		ref: d$1,
		style: a$1,
		children: t$2
	} : {
		ref: d$1,
		style: a$1,
		index: r$1,
		children: t$2
	});
}), Z = (e$1, t$2) => (0, import_react.useMemo)(() => {
	if ("function" == typeof e$1) return [(o$2) => e$1(t$2[o$2], o$2), t$2.length];
	const o$1 = ((e$2) => {
		const t$3 = [];
		return G(e$2, t$3), t$3;
	})(e$1);
	return [(e$2) => o$1[e$2], o$1.length];
}, [e$1, t$2]), ee = /* @__PURE__ */ (0, import_react.forwardRef)(({ children: t$2, data: o$1, bufferSize: r$1, itemSize: i$1, shift: s$1, horizontal: f$1, keepMounted: d$1, cache: a$1, startMargin: h$1 = 0, ssrCount: p$1, as: g$1 = "div", item: v$1 = "div", scrollRef: w$1, onScroll: S$1, onScrollEnd: $$1 }, z$1) => {
	const [y$1, b$1] = Z(t$2, o$1), x$1 = (0, import_react.useRef)(null), I$1 = (0, import_react.useRef)(!!p$1), k$1 = K(S$1), R$1 = K($$1), [T$1, C$1, M$1, O$1] = F(() => {
		const e$1 = !!f$1, t$3 = H(b$1, i$1, p$1, a$1, !i$1);
		return [
			t$3,
			X$1(t$3, e$1),
			L(t$3, e$1),
			e$1
		];
	});
	b$1 !== T$1.T() && T$1.B(5, [b$1, s$1]), h$1 !== T$1.O() && T$1.B(6, h$1);
	const [W$1, B$1] = (0, import_react.useReducer)(T$1.m, void 0, T$1.m), J$1 = T$1.M(), A$1 = T$1.h(), N$1 = M$1.P(), P$1 = [], V$1 = (t$3) => {
		const o$2 = y$1(t$3);
		return (0, import_jsx_runtime.jsx)(Q, {
			ee: C$1.U,
			te: t$3,
			oe: T$1.k(t$3, N$1),
			ne: T$1.I(t$3),
			re: v$1,
			Z: o$2,
			ie: O$1,
			se: I$1[j]
		}, U(o$2, t$3));
	};
	if (D(() => {
		I$1[j] = !1, T$1.W(1, (e$2) => {
			e$2 ? (0, import_react_dom.flushSync)(B$1) : B$1();
		}), T$1.W(4, () => {
			k$1[j] && k$1[j](T$1.C());
		}), T$1.W(8, () => {
			R$1[j] && R$1[j]();
		});
		const e$1 = x$1[j], t$3 = (t$4) => {
			C$1.G(t$4), M$1.N(e$1, t$4);
		};
		return w$1 ? _(() => {
			w$1[j] && t$3(w$1[j]);
		}) : t$3(e$1.parentElement), () => {
			T$1.v(), C$1.v(), M$1.v();
		};
	}, []), D(() => {
		M$1.q();
	}, [W$1]), (0, import_react.useImperativeHandle)(z$1, () => ({
		get cache() {
			return T$1._();
		},
		get scrollOffset() {
			return T$1.C();
		},
		get scrollSize() {
			return E(T$1);
		},
		get viewportSize() {
			return T$1.p();
		},
		findItemIndex: T$1.$,
		getItemOffset: T$1.k,
		getItemSize: T$1.R,
		scrollToIndex: M$1.Y,
		scrollTo: M$1.V,
		scrollBy: M$1.X
	}), []), d$1) {
		const e$1 = new Set(d$1);
		for (let [t$3, o$2] = T$1.S(r$1); t$3 <= o$2; t$3++) e$1.add(t$3);
		m([...e$1]).forEach((e$2) => {
			P$1.push(V$1(e$2));
		});
	} else for (let [e$1, t$3] = T$1.S(r$1); e$1 <= t$3; e$1++) P$1.push(V$1(e$1));
	return (0, import_jsx_runtime.jsx)(g$1, {
		ref: x$1,
		style: {
			contain: "size style",
			overflowAnchor: "none",
			flex: "none",
			position: "relative",
			width: O$1 ? A$1 : "100%",
			height: O$1 ? "100%" : A$1,
			pointerEvents: J$1 ? "none" : void 0
		},
		children: P$1
	});
});
function normalizeInset(bottomInset) {
	return Math.max(0, bottomInset);
}
function getEffectiveScrollSize(element, bottomInset = 0) {
	return Math.max(element.clientHeight, element.scrollHeight - normalizeInset(bottomInset));
}
function getRealBottom(element, bottomInset = 0) {
	return Math.max(0, getEffectiveScrollSize(element, bottomInset) - element.clientHeight);
}
function getDistanceToBottom(element, bottomInset = 0) {
	return getRealBottom(element, bottomInset) - element.scrollTop;
}
function isMoreThanOneViewportFromBottom(element, bottomInset = 0) {
	const viewportSize = element.clientHeight;
	if (viewportSize <= 0) return false;
	return getDistanceToBottom(element, bottomInset) > viewportSize;
}
function useAutoStickToBottom({ isFollowing, stickToBottom }) {
	const onContentSizeChange = (0, import_react.useCallback)(() => {
		if (isFollowing()) stickToBottom();
	}, [isFollowing, stickToBottom]);
	return (0, import_react.useMemo)(() => ({ onContentSizeChange }), [onContentSizeChange]);
}
function computeScrollAnchor({ following, scrollOffset, topIndex, getKeyAtIndex, getOffsetAtIndex }) {
	if (following) return null;
	const key = getKeyAtIndex(topIndex);
	if (!key) return null;
	return {
		key,
		offset: Math.max(0, scrollOffset - getOffsetAtIndex(topIndex))
	};
}
function resolveRestoreTarget(saved, findIndexByKey, lastIndex, bottomOffset) {
	if (saved) {
		const index = findIndexByKey(saved.key);
		if (index >= 0) return {
			index,
			align: "start",
			offset: saved.offset
		};
	}
	return {
		index: lastIndex,
		align: "end",
		offset: bottomOffset
	};
}
var cacheKeyFor = (topicId) => `chat.scroll_anchor.${topicId}`;
var SAVE_THROTTLE_MS = 200;
function useScrollPositionMemory(inputs) {
	const inputsRef = (0, import_react.useRef)(inputs);
	inputsRef.current = inputs;
	const suppressSaveRef = (0, import_react.useRef)(true);
	const didRestoreRef = (0, import_react.useRef)(false);
	const lastSaveAtRef = (0, import_react.useRef)(0);
	const save = (0, import_react.useCallback)((immediate = false) => {
		const i$1 = inputsRef.current;
		if (suppressSaveRef.current || !i$1.topicId) return;
		const el = i$1.scrollerRef.current;
		const handle = i$1.vlistHandleRef.current;
		if (!el || !handle || i$1.isAnimating()) return;
		const now = Date.now();
		if (!immediate && now - lastSaveAtRef.current < SAVE_THROTTLE_MS) return;
		lastSaveAtRef.current = now;
		const anchor = computeScrollAnchor({
			following: i$1.isFollowing(),
			scrollOffset: el.scrollTop,
			topIndex: handle.findItemIndex(el.scrollTop),
			getKeyAtIndex: i$1.getDataKeyAtIndex,
			getOffsetAtIndex: (index) => handle.getItemOffset(index)
		});
		cacheService.set(cacheKeyFor(i$1.topicId), anchor);
	}, []);
	const ready = inputs.itemCount > 0;
	(0, import_react.useEffect)(() => {
		if (didRestoreRef.current || !ready) return;
		didRestoreRef.current = true;
		const i$1 = inputsRef.current;
		const target = resolveRestoreTarget(i$1.topicId ? cacheService.get(cacheKeyFor(i$1.topicId)) : null, i$1.findDataIndexByKey, i$1.itemCount - 1, i$1.bottomPadding);
		let settleRaf = 0;
		const raf = requestAnimationFrame(() => {
			if (!i$1.shouldRestore()) {
				suppressSaveRef.current = false;
				return;
			}
			const el = i$1.scrollerRef.current;
			const handle = i$1.vlistHandleRef.current;
			if (target.align === "end") i$1.enterFollowingAfterRestore();
			else i$1.enterReadingForRestore();
			if (handle) handle.scrollToIndex(target.index, {
				align: target.align,
				offset: target.offset
			});
			else if (el && target.align === "end") el.scrollTop = Math.max(0, el.scrollHeight - el.clientHeight);
			settleRaf = requestAnimationFrame(() => {
				if (target.align === "start") i$1.settleReadingRestore();
				suppressSaveRef.current = false;
			});
		});
		return () => {
			cancelAnimationFrame(raf);
			if (settleRaf) cancelAnimationFrame(settleRaf);
		};
	}, [ready]);
	return { save };
}
var DEFAULT_FRAMES = 50;
var DEFAULT_EASING = (t$2) => 1 - 2 ** (-10 * t$2);
function useSmoothScrollAnimation(scrollerRef, { raf, caf } = {}) {
	const rafIdRef = (0, import_react.useRef)(null);
	const animatingRef = (0, import_react.useRef)(false);
	const requestFrame = (0, import_react.useMemo)(() => raf ?? ((cb) => requestAnimationFrame(cb)), [raf]);
	const cancelFrame = (0, import_react.useMemo)(() => caf ?? ((id) => cancelAnimationFrame(id)), [caf]);
	const cancel = (0, import_react.useCallback)(() => {
		if (rafIdRef.current != null) {
			cancelFrame(rafIdRef.current);
			rafIdRef.current = null;
		}
		animatingRef.current = false;
	}, [cancelFrame]);
	const scrollTo = (0, import_react.useCallback)((getTargetOffset, options = {}) => {
		const el = scrollerRef.current;
		if (!el) return;
		if (rafIdRef.current != null) cancelFrame(rafIdRef.current);
		const frames = Math.max(1, options.frames ?? DEFAULT_FRAMES);
		const easing = options.easing ?? DEFAULT_EASING;
		const onComplete = options.onComplete;
		const startOffset = el.scrollTop;
		let frame = 0;
		animatingRef.current = true;
		const step = () => {
			const node = scrollerRef.current;
			if (!node) {
				animatingRef.current = false;
				rafIdRef.current = null;
				return;
			}
			frame += 1;
			const progress = Math.min(1, easing(frame / frames));
			node.scrollTop = startOffset + (getTargetOffset() - startOffset) * progress;
			if (frame >= frames) {
				node.scrollTop = getTargetOffset();
				animatingRef.current = false;
				rafIdRef.current = null;
				onComplete?.();
				return;
			}
			rafIdRef.current = requestFrame(step);
		};
		rafIdRef.current = requestFrame(step);
	}, [
		cancelFrame,
		requestFrame,
		scrollerRef
	]);
	const isAnimating = (0, import_react.useCallback)(() => animatingRef.current, []);
	(0, import_react.useEffect)(() => {
		return () => cancel();
	}, [cancel]);
	return (0, import_react.useMemo)(() => ({
		scrollTo,
		cancel,
		isAnimating
	}), [
		cancel,
		isAnimating,
		scrollTo
	]);
}
function useViewportFollowState() {
	const stateRef = (0, import_react.useRef)({
		mode: "reading",
		reason: "initializing"
	});
	const getState = (0, import_react.useCallback)(() => stateRef.current, []);
	const isFollowing = (0, import_react.useCallback)(() => stateRef.current.mode === "following", []);
	const enterFollowing = (0, import_react.useCallback)((reason) => {
		stateRef.current = {
			mode: "following",
			reason
		};
	}, []);
	const enterReading = (0, import_react.useCallback)((reason) => {
		stateRef.current = {
			mode: "reading",
			reason
		};
	}, []);
	return (0, import_react.useMemo)(() => ({
		enterFollowing,
		enterReading,
		getState,
		isFollowing
	}), [
		enterFollowing,
		enterReading,
		getState,
		isFollowing
	]);
}
var SCROLL_WHEEL_DEBOUNCE_MS = 100;
var LONG_JUMP_VIEWPORTS = 3;
var USER_SCROLL_INPUT_WINDOW_MS = 250;
var FREEZE_REASSERT_TOLERANCE_PX = 2;
var FREEZE_SEMANTIC_ANCHOR_SELECTOR = "button,[role=\"button\"],a,input,textarea,select,h1,h2,h3,h4,h5,h6,.block-wrapper,[data-message-id],p,pre,li,table";
function keysMatchAt(container, candidate, offset) {
	return candidate.every((key, index) => container[index + offset] === key);
}
function useChatVirtualizerRuntime({ items, getItemKey, renderItem, onReachTop, hasMoreTop, handleRef, topReachOverscanItems, topPadding = 0, topicId, bottomPadding, keepMountedKeys = [] }) {
	const scrollerRef = (0, import_react.useRef)(null);
	const contentRef = (0, import_react.useRef)(null);
	const freezeSpacerRef = (0, import_react.useRef)(null);
	const vlistHandleRef = (0, import_react.useRef)(null);
	const smoothScroll = useSmoothScrollAnimation(scrollerRef);
	const [isScrollToBottomButtonVisible, setIsScrollToBottomButtonVisible] = (0, import_react.useState)(false);
	const isScrollToBottomButtonVisibleRef = (0, import_react.useRef)(false);
	const viewportFollow = useViewportFollowState();
	const freezeAnchorRef = (0, import_react.useRef)(null);
	const freezeSpacerHeightRef = (0, import_react.useRef)(0);
	const freezeBaselineScrollHeightRef = (0, import_react.useRef)(null);
	const lastUserInputAtRef = (0, import_react.useRef)(0);
	const lastUserInputDirectionRef = (0, import_react.useRef)("none");
	const userScrollGestureRef = (0, import_react.useRef)(false);
	const scrollbarDragActiveRef = (0, import_react.useRef)(false);
	const readNavigationActiveRef = (0, import_react.useRef)(false);
	const lastScrollOffsetRef = (0, import_react.useRef)(0);
	const markUserInput = (0, import_react.useCallback)(() => {
		lastUserInputAtRef.current = performance.now();
		lastUserInputDirectionRef.current = "none";
	}, []);
	const itemsRef = (0, import_react.useRef)(items);
	itemsRef.current = items;
	const getItemKeyRef = (0, import_react.useRef)(getItemKey);
	getItemKeyRef.current = getItemKey;
	const renderItemRef = (0, import_react.useRef)(renderItem);
	renderItemRef.current = renderItem;
	const findDataIndexByKey = (0, import_react.useCallback)((key) => {
		const list = itemsRef.current;
		const get = getItemKeyRef.current;
		for (let i$1 = 0; i$1 < list.length; i$1++) if (get(list[i$1], i$1) === key) return i$1;
		return -1;
	}, []);
	const getDataKeyAtIndex = (0, import_react.useCallback)((index) => {
		const list = itemsRef.current;
		if (index < 0 || index >= list.length) return null;
		return getItemKeyRef.current(list[index], index);
	}, []);
	const bottomFollowInsetRef = (0, import_react.useRef)(0);
	bottomFollowInsetRef.current = freezeSpacerHeightRef.current;
	const setFreezeSpacerHeight = (0, import_react.useCallback)((height) => {
		const next = Math.max(0, height);
		if (Math.abs(next - freezeSpacerHeightRef.current) <= FREEZE_REASSERT_TOLERANCE_PX) return;
		freezeSpacerHeightRef.current = next;
		bottomFollowInsetRef.current = next;
		if (freezeSpacerRef.current) freezeSpacerRef.current.style.height = `${next}px`;
	}, []);
	const getNaturalScrollHeight = (0, import_react.useCallback)(() => {
		const el = scrollerRef.current;
		return el ? Math.max(el.clientHeight, el.scrollHeight - freezeSpacerHeightRef.current) : 0;
	}, []);
	const maintainFreezeScrollRange = (0, import_react.useCallback)(() => {
		const naturalHeight = getNaturalScrollHeight();
		const baseline = freezeBaselineScrollHeightRef.current ?? naturalHeight;
		freezeBaselineScrollHeightRef.current = baseline;
		setFreezeSpacerHeight(Math.max(0, baseline - naturalHeight));
	}, [getNaturalScrollHeight, setFreezeSpacerHeight]);
	const clearFreeze = (0, import_react.useCallback)(() => {
		freezeAnchorRef.current = null;
		freezeBaselineScrollHeightRef.current = null;
		userScrollGestureRef.current = false;
		setFreezeSpacerHeight(0);
	}, [setFreezeSpacerHeight]);
	const hideScrollToBottomButton = (0, import_react.useCallback)(() => {
		if (!isScrollToBottomButtonVisibleRef.current) return;
		isScrollToBottomButtonVisibleRef.current = false;
		setIsScrollToBottomButtonVisible(false);
	}, []);
	const stickToEffectiveBottom = (0, import_react.useCallback)(() => {
		const el = scrollerRef.current;
		if (!el) return;
		smoothScroll.cancel();
		const target = getRealBottom(el, bottomFollowInsetRef.current);
		el.scrollTop = target;
		lastScrollOffsetRef.current = target;
		hideScrollToBottomButton();
	}, [hideScrollToBottomButton, smoothScroll]);
	const autoStick = useAutoStickToBottom({
		isFollowing: viewportFollow.isFollowing,
		stickToBottom: stickToEffectiveBottom
	});
	const updateScrollToBottomButtonVisibility = (0, import_react.useCallback)(() => {
		const el = scrollerRef.current;
		const nextVisible = el ? isMoreThanOneViewportFromBottom(el, bottomFollowInsetRef.current) : false;
		if (isScrollToBottomButtonVisibleRef.current === nextVisible) return;
		isScrollToBottomButtonVisibleRef.current = nextVisible;
		setIsScrollToBottomButtonVisible(nextVisible);
	}, []);
	const resolveSemanticAnchor = (0, import_react.useCallback)((preferredAnchor) => {
		const scroller = scrollerRef.current;
		if (!scroller) return null;
		let candidate = preferredAnchor;
		if (!candidate) {
			const rect = scroller.getBoundingClientRect();
			candidate = scroller.ownerDocument.elementFromPoint?.(rect.left + rect.width / 2, rect.top + 1) ?? null;
		}
		const htmlCandidate = candidate instanceof HTMLElement ? candidate : candidate?.parentElement;
		if (!htmlCandidate || !scroller.contains(htmlCandidate)) return null;
		const itemElement = htmlCandidate.closest("[data-message-key]");
		const itemKey = itemElement?.dataset.messageKey;
		if (!itemElement || !itemKey) return null;
		const semanticElement = htmlCandidate.closest(FREEZE_SEMANTIC_ANCHOR_SELECTOR) ?? htmlCandidate;
		return itemElement.contains(semanticElement) ? {
			element: semanticElement,
			itemKey
		} : null;
	}, []);
	const captureFreezeAnchor = (0, import_react.useCallback)((preferredAnchor) => {
		const el = scrollerRef.current;
		const handle = vlistHandleRef.current;
		if (!el || !handle) return;
		const semantic = resolveSemanticAnchor(preferredAnchor);
		const visibleIndex = handle.findItemIndex(el.scrollTop);
		const fallbackIndex = Math.min(Math.max(visibleIndex, 0), itemsRef.current.length - 1);
		const semanticIndex = semantic ? findDataIndexByKey(semantic.itemKey) : -1;
		const itemIndex = semanticIndex >= 0 ? semanticIndex : fallbackIndex;
		const itemKey = getDataKeyAtIndex(itemIndex);
		if (!itemKey) {
			freezeAnchorRef.current = null;
			return;
		}
		const scrollerTop = el.getBoundingClientRect().top;
		const element = semantic?.element ?? null;
		freezeAnchorRef.current = {
			itemKey,
			offsetInItem: el.scrollTop - (Math.max(0, topPadding) + handle.getItemOffset(itemIndex)),
			element,
			elementViewportTop: element ? element.getBoundingClientRect().top - scrollerTop : null
		};
	}, [
		findDataIndexByKey,
		getDataKeyAtIndex,
		resolveSemanticAnchor,
		topPadding
	]);
	const reassertFreeze = (0, import_react.useCallback)(() => {
		const frozen = freezeAnchorRef.current;
		const el = scrollerRef.current;
		const content = contentRef.current;
		const handle = vlistHandleRef.current;
		if (!frozen || !el || !handle) return;
		if (smoothScroll.isAnimating() || userScrollGestureRef.current || scrollbarDragActiveRef.current) return;
		const itemIndex = findDataIndexByKey(frozen.itemKey);
		if (itemIndex < 0) {
			freezeAnchorRef.current = null;
			return;
		}
		const elementItemKey = frozen.element?.closest("[data-message-key]")?.dataset.messageKey;
		if (frozen.element && frozen.elementViewportTop != null && frozen.element.isConnected && content?.contains(frozen.element) && elementItemKey === frozen.itemKey) {
			const drift = frozen.element.getBoundingClientRect().top - el.getBoundingClientRect().top - frozen.elementViewportTop;
			if (Math.abs(drift) > FREEZE_REASSERT_TOLERANCE_PX) el.scrollTop += drift;
			return;
		}
		const target = Math.max(0, topPadding) + handle.getItemOffset(itemIndex) + frozen.offsetInItem;
		if (Math.abs(el.scrollTop - target) > FREEZE_REASSERT_TOLERANCE_PX) el.scrollTop = target;
	}, [
		findDataIndexByKey,
		smoothScroll,
		topPadding
	]);
	const takeUserControl = (0, import_react.useCallback)((reason, preferredAnchor) => {
		readNavigationActiveRef.current = false;
		smoothScroll.cancel();
		const wasFollowing = viewportFollow.isFollowing();
		viewportFollow.enterReading(reason);
		if (wasFollowing || freezeBaselineScrollHeightRef.current === null) {
			setFreezeSpacerHeight(0);
			freezeBaselineScrollHeightRef.current = getNaturalScrollHeight();
		}
		captureFreezeAnchor(preferredAnchor);
		updateScrollToBottomButtonVisibility();
	}, [
		captureFreezeAnchor,
		getNaturalScrollHeight,
		setFreezeSpacerHeight,
		smoothScroll,
		updateScrollToBottomButtonVisibility,
		viewportFollow
	]);
	const beginScrollbarDrag = (0, import_react.useCallback)(() => {
		scrollbarDragActiveRef.current = true;
		markUserInput();
	}, [markUserInput]);
	const beginUserScrollGesture = (0, import_react.useCallback)(() => {
		if (userScrollGestureRef.current) return;
		setFreezeSpacerHeight(0);
		freezeBaselineScrollHeightRef.current = getNaturalScrollHeight();
		userScrollGestureRef.current = true;
	}, [getNaturalScrollHeight, setFreezeSpacerHeight]);
	const settleUserScrollGesture = (0, import_react.useCallback)(() => {
		if (viewportFollow.isFollowing() || !userScrollGestureRef.current) {
			userScrollGestureRef.current = false;
			return;
		}
		setFreezeSpacerHeight(0);
		freezeBaselineScrollHeightRef.current = getNaturalScrollHeight();
		captureFreezeAnchor();
		userScrollGestureRef.current = false;
	}, [
		captureFreezeAnchor,
		getNaturalScrollHeight,
		setFreezeSpacerHeight,
		viewportFollow
	]);
	const endScrollbarDrag = (0, import_react.useCallback)(() => {
		if (!scrollbarDragActiveRef.current) return;
		scrollbarDragActiveRef.current = false;
		settleUserScrollGesture();
	}, [settleUserScrollGesture]);
	const enterFollowingMode = (0, import_react.useCallback)((reason) => {
		viewportFollow.enterFollowing(reason);
		clearFreeze();
	}, [clearFreeze, viewportFollow]);
	const prepareReadingNavigation = (0, import_react.useCallback)(() => {
		viewportFollow.enterReading("navigation");
		clearFreeze();
	}, [clearFreeze, viewportFollow]);
	const enterReadingForRestore = (0, import_react.useCallback)(() => {
		viewportFollow.enterReading("restored-anchor");
		clearFreeze();
	}, [clearFreeze, viewportFollow]);
	const settleReadingRestore = (0, import_react.useCallback)(() => {
		freezeBaselineScrollHeightRef.current = getNaturalScrollHeight();
		captureFreezeAnchor();
	}, [captureFreezeAnchor, getNaturalScrollHeight]);
	const enterFollowingAfterRestore = (0, import_react.useCallback)(() => {
		enterFollowingMode("restored-bottom");
	}, [enterFollowingMode]);
	const dataKeys = (0, import_react.useMemo)(() => items.map((value, i$1) => getItemKey(value, i$1)), [items, getItemKey]);
	const previousDataKeysRef = (0, import_react.useRef)(dataKeys);
	const previousDataKeys = previousDataKeysRef.current;
	const lengthDelta = dataKeys.length - previousDataKeys.length;
	const addedAtStart = previousDataKeys.length > 0 && lengthDelta > 0 && keysMatchAt(dataKeys, previousDataKeys, lengthDelta);
	const removedFromStart = dataKeys.length > 0 && lengthDelta < 0 && keysMatchAt(previousDataKeys, dataKeys, -lengthDelta);
	const shift = addedAtStart || removedFromStart;
	(0, import_react.useEffect)(() => {
		previousDataKeysRef.current = dataKeys;
	}, [dataKeys]);
	const wrappedItems = (0, import_react.useMemo)(() => items.map((value, i$1) => ({
		key: dataKeys[i$1],
		value,
		originalIndex: i$1
	})), [dataKeys, items]);
	const wrappedRenderItem = (0, import_react.useCallback)((item) => {
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "chat.virtualizer",
			"data-message-index": item.originalIndex,
			"data-message-key": item.key,
			style: { width: "100%" },
			children: renderItemRef.current(item.value, item.originalIndex)
		}, item.key);
	}, []);
	const { save: saveScrollPosition } = useScrollPositionMemory({
		topicId,
		itemCount: items.length,
		bottomPadding,
		scrollerRef,
		vlistHandleRef,
		getDataKeyAtIndex,
		findDataIndexByKey,
		shouldRestore: () => viewportFollow.getState().reason === "initializing",
		isFollowing: viewportFollow.isFollowing,
		enterFollowingAfterRestore,
		enterReadingForRestore,
		settleReadingRestore,
		isAnimating: smoothScroll.isAnimating
	});
	(0, import_react.useLayoutEffect)(() => {
		const content = contentRef.current;
		const scroller = scrollerRef.current;
		if (!content || typeof ResizeObserver === "undefined") return;
		const observer = new ResizeObserver(() => {
			const shouldHoldRestingViewport = !viewportFollow.isFollowing() && !userScrollGestureRef.current && !scrollbarDragActiveRef.current;
			if (shouldHoldRestingViewport) maintainFreezeScrollRange();
			autoStick.onContentSizeChange();
			if (shouldHoldRestingViewport) {
				maintainFreezeScrollRange();
				reassertFreeze();
			}
			updateScrollToBottomButtonVisibility();
		});
		observer.observe(content);
		if (scroller) observer.observe(scroller);
		return () => observer.disconnect();
	}, [
		autoStick,
		maintainFreezeScrollRange,
		reassertFreeze,
		updateScrollToBottomButtonVisibility,
		viewportFollow
	]);
	const wheelTimeoutRef = (0, import_react.useRef)(null);
	const lastWheelDirRef = (0, import_react.useRef)("none");
	const onWheel = (0, import_react.useCallback)((event) => {
		markUserInput();
		const dir = event.deltaY < 0 ? "up" : event.deltaY > 0 ? "down" : "none";
		lastUserInputDirectionRef.current = dir;
		if (readNavigationActiveRef.current && dir !== "none") takeUserControl("navigation");
		if (smoothScroll.isAnimating() && dir === "up") smoothScroll.cancel();
		lastWheelDirRef.current = dir;
		if (wheelTimeoutRef.current) clearTimeout(wheelTimeoutRef.current);
		wheelTimeoutRef.current = setTimeout(() => {
			lastWheelDirRef.current = "none";
		}, SCROLL_WHEEL_DEBOUNCE_MS);
	}, [
		markUserInput,
		smoothScroll,
		takeUserControl
	]);
	const onReachTopRef = (0, import_react.useRef)(onReachTop);
	onReachTopRef.current = onReachTop;
	const maybeNotifyReachTop = (0, import_react.useCallback)((offset) => {
		if (!hasMoreTop) return;
		const handle = vlistHandleRef.current;
		if (!handle) return;
		if (handle.findItemIndex(offset) < topReachOverscanItems) onReachTopRef.current?.();
	}, [hasMoreTop, topReachOverscanItems]);
	const onScroll = (0, import_react.useCallback)(() => {
		const el = scrollerRef.current;
		if (!el) return;
		const offset = el.scrollTop;
		const delta = offset - lastScrollOffsetRef.current;
		const recentInputDirection = lastUserInputDirectionRef.current;
		const inputDirectionMatchesScroll = recentInputDirection === "none" || delta === 0 || (recentInputDirection === "up" ? delta < 0 : delta > 0);
		const hasRecentUserScrollIntent = performance.now() - lastUserInputAtRef.current < USER_SCROLL_INPUT_WINDOW_MS && inputDirectionMatchesScroll;
		const isUserInitiated = scrollbarDragActiveRef.current || userScrollGestureRef.current || hasRecentUserScrollIntent;
		const wheelDir = lastWheelDirRef.current;
		const direction = wheelDir !== "none" ? wheelDir : delta < 0 ? "up" : delta > 0 ? "down" : "none";
		if (hasRecentUserScrollIntent && recentInputDirection === "none" && direction !== "none") lastUserInputDirectionRef.current = direction;
		if (smoothScroll.isAnimating()) {
			if (!isUserInitiated || direction === "none") {
				lastScrollOffsetRef.current = offset;
				updateScrollToBottomButtonVisibility();
				return;
			}
			smoothScroll.cancel();
			readNavigationActiveRef.current = false;
		}
		const distanceToBottom = getDistanceToBottom(el, bottomFollowInsetRef.current);
		if (!isUserInitiated && viewportFollow.isFollowing() && Math.abs(distanceToBottom) > FREEZE_REASSERT_TOLERANCE_PX) {
			lastScrollOffsetRef.current = offset;
			stickToEffectiveBottom();
			return;
		}
		lastScrollOffsetRef.current = offset;
		if (!viewportFollow.isFollowing()) if (isUserInitiated) {
			beginUserScrollGesture();
			if (hasRecentUserScrollIntent && direction !== "up" && distanceToBottom <= FREEZE_REASSERT_TOLERANCE_PX) {
				enterFollowingMode("user-reached-bottom");
				stickToEffectiveBottom();
			}
		} else {
			maintainFreezeScrollRange();
			reassertFreeze();
			updateScrollToBottomButtonVisibility();
			saveScrollPosition();
			maybeNotifyReachTop(offset);
			return;
		}
		else if (isUserInitiated && direction === "up") {
			beginUserScrollGesture();
			takeUserControl("user-scrolled-up");
		}
		updateScrollToBottomButtonVisibility();
		saveScrollPosition();
		maybeNotifyReachTop(offset);
	}, [
		beginUserScrollGesture,
		enterFollowingMode,
		maintainFreezeScrollRange,
		maybeNotifyReachTop,
		reassertFreeze,
		saveScrollPosition,
		smoothScroll,
		stickToEffectiveBottom,
		takeUserControl,
		updateScrollToBottomButtonVisibility,
		viewportFollow
	]);
	const onScrollEnd = (0, import_react.useCallback)(() => {
		lastWheelDirRef.current = "none";
		if (!scrollbarDragActiveRef.current) {
			settleUserScrollGesture();
			if (!viewportFollow.isFollowing() && !freezeAnchorRef.current) captureFreezeAnchor();
		}
		saveScrollPosition(true);
	}, [
		captureFreezeAnchor,
		saveScrollPosition,
		settleUserScrollGesture,
		viewportFollow
	]);
	const scrollerProps = (0, import_react.useMemo)(() => ({
		onWheel,
		onScroll,
		onScrollEnd
	}), [
		onScroll,
		onScrollEnd,
		onWheel
	]);
	const [selectionIndex, setSelectionIndex] = (0, import_react.useState)(null);
	(0, import_react.useEffect)(() => {
		const handler = () => {
			const sel = typeof document !== "undefined" ? document.getSelection() : null;
			if (!sel || sel.rangeCount === 0 || sel.isCollapsed) {
				setSelectionIndex(null);
				return;
			}
			const anchorNode = sel.anchorNode;
			if (!anchorNode) {
				setSelectionIndex(null);
				return;
			}
			const indexed = (anchorNode instanceof Element ? anchorNode : anchorNode.parentElement)?.closest("[data-message-index]");
			const scroller = scrollerRef.current;
			const ownerScroller = indexed?.closest("[data-message-virtual-list-scroller]");
			if (!indexed || !scroller || ownerScroller !== scroller) {
				setSelectionIndex(null);
				return;
			}
			const idx = Number(indexed.getAttribute("data-message-index"));
			setSelectionIndex(Number.isInteger(idx) ? idx : null);
		};
		document.addEventListener("selectionchange", handler);
		return () => document.removeEventListener("selectionchange", handler);
	}, []);
	const keepMounted = (0, import_react.useMemo)(() => {
		const indices = /* @__PURE__ */ new Set();
		if (selectionIndex != null) indices.add(selectionIndex);
		for (const key of keepMountedKeys) {
			const index = items.findIndex((item, itemIndex) => getItemKey(item, itemIndex) === key);
			if (index >= 0) indices.add(index);
		}
		return [...indices].filter((index) => Number.isInteger(index) && index >= 0 && index < items.length);
	}, [
		getItemKey,
		items,
		keepMountedKeys,
		selectionIndex
	]);
	const clampToReachable = (0, import_react.useCallback)((scroller, target) => Math.min(getRealBottom(scroller, bottomFollowInsetRef.current), Math.max(0, target)), []);
	const navigateForReading = (0, import_react.useCallback)((getTarget, behavior, getPreferredAnchor) => {
		const el = scrollerRef.current;
		if (!el) return;
		readNavigationActiveRef.current = false;
		smoothScroll.cancel();
		prepareReadingNavigation();
		const resolveTarget = () => {
			const current = scrollerRef.current;
			if (!current) return 0;
			return clampToReachable(current, getTarget(current));
		};
		const finish = () => {
			if (!readNavigationActiveRef.current) return;
			readNavigationActiveRef.current = false;
			takeUserControl("navigation", getPreferredAnchor?.() ?? null);
		};
		if (behavior === "smooth") {
			readNavigationActiveRef.current = true;
			smoothScroll.scrollTo(resolveTarget, { onComplete: finish });
		} else {
			el.scrollTop = resolveTarget();
			takeUserControl("navigation", getPreferredAnchor?.() ?? null);
		}
	}, [
		clampToReachable,
		prepareReadingNavigation,
		smoothScroll,
		takeUserControl
	]);
	const scrollToBottom = (0, import_react.useCallback)(() => {
		readNavigationActiveRef.current = false;
		enterFollowingMode("scroll-to-bottom");
		stickToEffectiveBottom();
	}, [enterFollowingMode, stickToEffectiveBottom]);
	const scrollToTop = (0, import_react.useCallback)((behavior = "instant") => {
		navigateForReading(() => 0, behavior);
	}, [navigateForReading]);
	const scrollToElement = (0, import_react.useCallback)((element, align = "start") => {
		navigateForReading((scroller) => {
			if (!element.isConnected) return scroller.scrollTop;
			const elementRect = element.getBoundingClientRect();
			const scrollerRect = scroller.getBoundingClientRect();
			const start = scroller.scrollTop + elementRect.top - scrollerRect.top;
			return align === "center" ? start - (scroller.clientHeight - elementRect.height) / 2 : start;
		}, "smooth", () => element.isConnected ? element : null);
	}, [navigateForReading]);
	const scrollToRange = (0, import_react.useCallback)((range) => {
		const getRangeElement$1 = () => {
			const container = range.commonAncestorContainer;
			const element = container instanceof Element ? container : container.parentElement;
			return element?.isConnected ? element : null;
		};
		if (!scrollerRef.current || !getRangeElement$1()) return;
		navigateForReading((currentScroller) => {
			if (!getRangeElement$1()) return currentScroller.scrollTop;
			const rangeRect = range.getBoundingClientRect();
			const scrollerRect = currentScroller.getBoundingClientRect();
			return currentScroller.scrollTop + rangeRect.top - scrollerRect.top - (currentScroller.clientHeight - rangeRect.height) / 2;
		}, "instant", getRangeElement$1);
	}, [navigateForReading]);
	(0, import_react.useImperativeHandle)(handleRef, () => ({
		scrollToBottom,
		scrollToTop,
		scrollToKey: (key, align = "start") => {
			if (findDataIndexByKey(key) < 0) return;
			const resolveTarget = (scroller$1) => {
				const handle = vlistHandleRef.current;
				const idx = findDataIndexByKey(key);
				if (!handle || idx < 0) return scroller$1.scrollTop;
				const start = Math.max(0, topPadding) + handle.getItemOffset(idx);
				const size = handle.getItemSize(idx);
				if (align === "center") return start - (scroller$1.clientHeight - size) / 2;
				if (align === "end") return start + size - scroller$1.clientHeight;
				return start;
			};
			const scroller = scrollerRef.current;
			navigateForReading(resolveTarget, scroller && Math.abs(clampToReachable(scroller, resolveTarget(scroller)) - scroller.scrollTop) > scroller.clientHeight * LONG_JUMP_VIEWPORTS ? "instant" : "smooth", () => {
				const elements = contentRef.current?.querySelectorAll("[data-message-key]") ?? [];
				return Array.from(elements).find((element) => element.dataset.messageKey === key) ?? null;
			});
		},
		scrollToElement,
		scrollToRange,
		isFollowing: viewportFollow.isFollowing,
		getScrollElement: () => scrollerRef.current
	}), [
		clampToReachable,
		findDataIndexByKey,
		navigateForReading,
		scrollToBottom,
		scrollToElement,
		scrollToRange,
		scrollToTop,
		topPadding,
		viewportFollow.isFollowing
	]);
	return {
		scrollerRef,
		contentRef,
		freezeSpacerRef,
		vlistHandleRef,
		wrappedItems,
		wrappedRenderItem,
		shift,
		keepMounted,
		scrollerProps,
		isScrollToBottomButtonVisible,
		takeUserControl,
		scrollToBottom,
		scrollToElement,
		markUserInput,
		beginScrollbarDrag,
		endScrollbarDrag
	};
}
var MESSAGE_SCROLL_TO_BOTTOM_BUTTON_DEFAULT_BOTTOM_OFFSET_PX = 24;
var KEYBOARD_SCROLL_KEYS = new Set([
	"ArrowUp",
	"ArrowDown",
	"PageUp",
	"PageDown",
	"Home",
	"End"
]);
var KEYBOARD_ACTIVATION_SELECTOR = "button,a,input,textarea,select,[role=\"button\"]";
var NESTED_SCROLL_INPUT_WINDOW_MS = 250;
function isKeyboardScrollIntent(event, scroller) {
	if (KEYBOARD_SCROLL_KEYS.has(event.key)) return true;
	if (event.key !== " " && event.key !== "Spacebar") return false;
	const target = event.target instanceof HTMLElement ? event.target : null;
	return target === scroller || !target?.closest(KEYBOARD_ACTIVATION_SELECTOR);
}
function ownsVerticalWheel(element, deltaY) {
	const style = getComputedStyle(element);
	const overflowY = style.overflowY;
	if (overflowY !== "auto" && overflowY !== "scroll") return false;
	const maxScrollTop = element.scrollHeight - element.clientHeight;
	if (maxScrollTop <= 0) return false;
	if (style.overscrollBehaviorY === "contain" || style.overscrollBehaviorY === "none") return true;
	return deltaY < 0 ? element.scrollTop > 0 : element.scrollTop < maxScrollTop;
}
function isWheelOwnedByNestedScroller(event, scroller) {
	const target = event.target instanceof Element ? event.target : null;
	let element = target instanceof HTMLElement ? target : target?.parentElement;
	while (element && element !== scroller) {
		if (ownsVerticalWheel(element, event.deltaY)) return true;
		element = element.parentElement;
	}
	return false;
}
function MessageVirtualList({ items, getItemKey, renderItem, estimateSize, overscan = 6, onReachTop, hasMoreTop = false, handleRef, className, onScrollContainerReady, style, topPadding = 6, bottomPadding = 12, keepMountedKeys, showScrollToBottomButton = false, scrollToBottomButtonBottomOffset = MESSAGE_SCROLL_TO_BOTTOM_BUTTON_DEFAULT_BOTTOM_OFFSET_PX, topicId }) {
	const { t: t$2 } = useTranslation();
	const runtime = useChatVirtualizerRuntime({
		items,
		getItemKey,
		renderItem,
		onReachTop,
		hasMoreTop,
		handleRef,
		topReachOverscanItems: overscan,
		topPadding,
		topicId,
		bottomPadding,
		keepMountedKeys
	});
	const [scrollerElement, setScrollerElement] = (0, import_react.useState)(null);
	const { beginScrollbarDrag, endScrollbarDrag, scrollToBottom, markUserInput, takeUserControl } = runtime;
	const { onWheel } = runtime.scrollerProps;
	const setScrollerRef = (0, import_react.useCallback)((element) => {
		runtime.scrollerRef.current = element;
		if (element) {
			setScrollerElement(element);
			onScrollContainerReady?.(element);
		}
	}, [onScrollContainerReady, runtime.scrollerRef]);
	(0, import_react.useEffect)(() => {
		if (!scrollerElement) return;
		const handleWheel = (event) => {
			if (event.deltaY === 0) return;
			if (isWheelOwnedByNestedScroller(event, scrollerElement)) {
				takeUserControl("nested-scroll", event.target instanceof Element ? event.target : null);
				return;
			}
			onWheel(event);
		};
		scrollerElement.addEventListener("wheel", handleWheel, { passive: true });
		return () => scrollerElement.removeEventListener("wheel", handleWheel);
	}, [
		onWheel,
		scrollerElement,
		takeUserControl
	]);
	const pointerDownInsideScrollerRef = (0, import_react.useRef)(false);
	const lastLocalInputAtRef = (0, import_react.useRef)(0);
	(0, import_react.useEffect)(() => {
		if (!scrollerElement) return;
		const ownerDocument = scrollerElement.ownerDocument;
		const onPointerDown = (event) => {
			pointerDownInsideScrollerRef.current = true;
			lastLocalInputAtRef.current = performance.now();
			if (event.target === scrollerElement) beginScrollbarDrag();
		};
		const onPointerMove = (event) => {
			if (event.buttons !== 0 && pointerDownInsideScrollerRef.current) {
				lastLocalInputAtRef.current = performance.now();
				markUserInput();
			}
		};
		const onPointerEnd = () => {
			pointerDownInsideScrollerRef.current = false;
			endScrollbarDrag();
		};
		const onKeyDown = (event) => {
			if (isKeyboardScrollIntent(event, scrollerElement)) {
				lastLocalInputAtRef.current = performance.now();
				markUserInput();
			}
		};
		const onScrollCapture = (event) => {
			const target = event.target;
			if (!(target instanceof HTMLElement) || target === scrollerElement) return;
			if (!scrollerElement.contains(target)) return;
			if (target.scrollHeight <= target.clientHeight) return;
			if (performance.now() - lastLocalInputAtRef.current > NESTED_SCROLL_INPUT_WINDOW_MS) return;
			takeUserControl("nested-scroll", target);
		};
		scrollerElement.addEventListener("pointerdown", onPointerDown, { passive: true });
		scrollerElement.addEventListener("pointermove", onPointerMove, { passive: true });
		ownerDocument.addEventListener("pointerup", onPointerEnd, { passive: true });
		ownerDocument.addEventListener("pointercancel", onPointerEnd, { passive: true });
		scrollerElement.addEventListener("keydown", onKeyDown);
		scrollerElement.addEventListener("scroll", onScrollCapture, {
			capture: true,
			passive: true
		});
		return () => {
			scrollerElement.removeEventListener("pointerdown", onPointerDown);
			scrollerElement.removeEventListener("pointermove", onPointerMove);
			ownerDocument.removeEventListener("pointerup", onPointerEnd);
			ownerDocument.removeEventListener("pointercancel", onPointerEnd);
			scrollerElement.removeEventListener("keydown", onKeyDown);
			scrollerElement.removeEventListener("scroll", onScrollCapture, { capture: true });
		};
	}, [
		beginScrollbarDrag,
		endScrollbarDrag,
		markUserInput,
		scrollerElement,
		takeUserControl
	]);
	const handleScrollToBottom = (0, import_react.useCallback)(() => {
		scrollToBottom();
	}, [scrollToBottom]);
	const requestDisclosureReadingControl = (0, import_react.useCallback)((anchor) => takeUserControl("disclosure", anchor), [takeUserControl]);
	const shouldShowScrollToBottomButton = showScrollToBottomButton && runtime.isScrollToBottomButtonVisible;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.message-virtual-list",
		"data-message-virtual-list-root": true,
		className: "relative flex min-h-0",
		style,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(scrollbar_default, {
			ref: setScrollerRef,
			"data-message-virtual-list-scroller": true,
			className,
			style: {
				flex: 1,
				minHeight: 0,
				overflowY: "auto",
				overflowX: "hidden",
				overflowAnchor: "none"
			},
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				ref: runtime.contentRef,
				style: { paddingBottom: bottomPadding },
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ScrollOwnershipProvider, {
					scrollContainerRef: runtime.scrollerRef,
					requestReadingControl: requestDisclosureReadingControl,
					scrollToElement: runtime.scrollToElement,
					children: [topPadding > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						"aria-hidden": "true",
						"data-message-virtual-list-top-spacer": true,
						style: { height: topPadding }
					}), scrollerElement && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ee, {
						ref: runtime.vlistHandleRef,
						scrollRef: runtime.scrollerRef,
						data: runtime.wrappedItems,
						itemSize: estimateSize,
						bufferSize: Math.max(200, overscan * (estimateSize ?? 200)),
						shift: runtime.shift,
						keepMounted: runtime.keepMounted,
						startMargin: topPadding,
						onScroll: runtime.scrollerProps.onScroll,
						onScrollEnd: runtime.scrollerProps.onScrollEnd,
						children: runtime.wrappedRenderItem
					})]
				})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				ref: runtime.freezeSpacerRef,
				"aria-hidden": "true",
				"data-message-virtual-list-freeze-spacer": true
			})]
		}), shouldShowScrollToBottomButton && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ScrollToBottomButton, {
			bottomOffset: scrollToBottomButtonBottomOffset,
			label: t$2("chat.navigation.bottom"),
			onClick: handleScrollToBottom
		})]
	});
}
function ScrollToBottomButton({ bottomOffset, label, onClick }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.scroll-to-bottom-button",
		"data-message-scroll-to-bottom-button-layer": true,
		className: "pointer-events-none absolute inset-x-0 z-5 flex justify-center",
		style: { bottom: bottomOffset },
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
			content: label,
			delay: 500,
			placement: "top",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				type: "button",
				variant: "outline",
				size: "icon",
				"aria-label": label,
				className: "[&_svg]:!size-5 pointer-events-auto h-9 w-9 rounded-full border-border bg-background/95 text-foreground shadow-[0_10px_24px_rgba(15,23,42,0.14),0_3px_8px_rgba(15,23,42,0.08)] backdrop-blur-sm transition-[background-color,color,box-shadow] duration-200 ease-out hover:bg-background hover:text-foreground dark:shadow-[0_12px_28px_rgba(0,0,0,0.34),0_3px_10px_rgba(0,0,0,0.22)]",
				"data-testid": "message-scroll-to-bottom-button",
				onClick,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrowDown, {})
			})
		})
	});
}
var INTERACTIVE_CHECKBOX_SELECTOR = "input[type=\"checkbox\"], [data-slot=checkbox], [role=\"checkbox\"]";
var MESSAGE_SELECT_CHECKBOX_SELECTOR = "[data-message-select-checkbox]";
function getMessageCheckbox(element) {
	return element.querySelector(MESSAGE_SELECT_CHECKBOX_SELECTOR);
}
function isCheckboxSelected(checkbox) {
	if (checkbox instanceof HTMLInputElement) return checkbox.checked;
	return checkbox.getAttribute("aria-checked") === "true" || checkbox.getAttribute("data-state") === "checked";
}
var SelectionBox = ({ isMultiSelectMode, scrollContainerRef, messageElements, handleSelectMessage }) => {
	const [isDragging, setIsDragging] = (0, import_react.useState)(false);
	const [dragStart, setDragStart] = (0, import_react.useState)({
		x: 0,
		y: 0
	});
	const [dragCurrent, setDragCurrent] = (0, import_react.useState)({
		x: 0,
		y: 0
	});
	const [isMouseDown, setIsMouseDown] = (0, import_react.useState)(false);
	const dragSelectedIds = (0, import_react.useRef)(/* @__PURE__ */ new Set());
	const DRAG_THRESHOLD = 5;
	(0, import_react.useEffect)(() => {
		let handleMouseMoveTimer;
		if (!isMultiSelectMode) return;
		const updateDragPos = (e$1) => {
			const container$1 = scrollContainerRef.current;
			if (!container$1) return {
				x: 0,
				y: 0
			};
			const rect = container$1.getBoundingClientRect();
			return {
				x: e$1.clientX - rect.left + container$1.scrollLeft,
				y: e$1.clientY - rect.top + container$1.scrollTop
			};
		};
		const handleMouseDown = (e$1) => {
			if (e$1.target.closest(INTERACTIVE_CHECKBOX_SELECTOR)) return;
			if (e$1.target.closest(".MessageFooter")) return;
			e$1.preventDefault();
			setIsMouseDown(true);
			const pos = updateDragPos(e$1);
			setDragStart(pos);
			setDragCurrent(pos);
			dragSelectedIds.current.clear();
		};
		const handleMouseMove = (e$1) => {
			if (!isMouseDown) return;
			const pos = updateDragPos(e$1);
			const deltaX = Math.abs(pos.x - dragStart.x);
			const deltaY = Math.abs(pos.y - dragStart.y);
			const distance = Math.sqrt(deltaX * deltaX + deltaY * deltaY);
			if (!isDragging && distance > DRAG_THRESHOLD) {
				setIsDragging(true);
				document.body.classList.add("no-select");
			}
			if (!isDragging) return;
			e$1.preventDefault();
			setDragCurrent(pos);
			const left = Math.min(dragStart.x, pos.x);
			const right = Math.max(dragStart.x, pos.x);
			const top = Math.min(dragStart.y, pos.y);
			const bottom = Math.max(dragStart.y, pos.y);
			messageElements.forEach((el, id) => {
				const checkbox = getMessageCheckbox(el);
				const isAlreadySelected = checkbox ? isCheckboxSelected(checkbox) : false;
				if (!checkbox) return;
				const rect = el.getBoundingClientRect();
				const container$1 = scrollContainerRef.current;
				const eTop = rect.top - container$1.getBoundingClientRect().top + container$1.scrollTop;
				const eLeft = rect.left - container$1.getBoundingClientRect().left + container$1.scrollLeft;
				const eBottom = eTop + rect.height;
				if (!!(eLeft + rect.width < left || eLeft > right || eBottom < top || eTop > bottom)) {
					if (dragSelectedIds.current.delete(id)) handleSelectMessage(id, false);
					return;
				}
				if (!dragSelectedIds.current.has(id) && !isAlreadySelected) {
					handleSelectMessage(id, true);
					dragSelectedIds.current.add(id);
					el.classList.add("selection-highlight");
					handleMouseMoveTimer = setTimeout(() => el.classList.remove("selection-highlight"), 300);
				}
			});
		};
		const handleMouseUp = () => {
			setIsMouseDown(false);
			if (isDragging) {
				setIsDragging(false);
				document.body.classList.remove("no-select");
			}
		};
		const container = scrollContainerRef.current;
		container?.addEventListener("mousedown", handleMouseDown);
		window.addEventListener("mousemove", handleMouseMove);
		window.addEventListener("mouseup", handleMouseUp);
		return () => {
			container?.removeEventListener("mousedown", handleMouseDown);
			window.removeEventListener("mousemove", handleMouseMove);
			window.removeEventListener("mouseup", handleMouseUp);
			document.body.classList.remove("no-select");
			clearTimeout(handleMouseMoveTimer);
		};
	}, [
		isMultiSelectMode,
		isDragging,
		isMouseDown,
		dragStart,
		scrollContainerRef,
		messageElements,
		handleSelectMessage
	]);
	if (!isDragging || !isMultiSelectMode) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.selection-box",
		className: "pointer-events-none absolute z-100 border border-primary border-dashed bg-[rgba(0,114,245,0.1)]",
		style: {
			left: Math.min(dragStart.x, dragCurrent.x),
			top: Math.min(dragStart.y, dragCurrent.y),
			width: Math.abs(dragCurrent.x - dragStart.x),
			height: Math.abs(dragCurrent.y - dragStart.y)
		}
	});
};
var SelectionBox_default = SelectionBox;
function createStableAnchorMessagesCache() {
	return { previous: [] };
}
function sameTopology(previous, message) {
	return previous.id === message.id && previous.role === message.role && previous.isActiveBranch === message.isActiveBranch && previous.isContextBoundary === message.isContextBoundary;
}
function stableAnchorMessages(messages, cache) {
	const previous = cache.previous;
	if (previous.length === messages.length && messages.every((message, index) => sameTopology(previous[index], message))) return previous;
	const next = messages.map((message) => ({
		id: message.id,
		role: message.role,
		isActiveBranch: message.isActiveBranch,
		isContextBoundary: message.isContextBoundary
	}));
	cache.previous = next;
	return next;
}
function arraysShallowEqual(a$1, b$1) {
	if (a$1 === b$1) return true;
	if (a$1.length !== b$1.length) return false;
	for (let i$1 = 0; i$1 < a$1.length; i$1++) if (a$1[i$1] !== b$1[i$1]) return false;
	return true;
}
function entriesShallowEqual(a$1, b$1) {
	if (a$1 === b$1) return true;
	if (a$1.length !== b$1.length) return false;
	for (let i$1 = 0; i$1 < a$1.length; i$1++) {
		const [ak, av] = a$1[i$1];
		const [bk, bv] = b$1[i$1];
		if (ak !== bk) return false;
		if (av !== bv) return false;
	}
	return true;
}
function createStableGroupedMessagesCache() {
	return {
		prevEntries: [],
		prevEntryByKey: /* @__PURE__ */ new Map(),
		prevByKey: /* @__PURE__ */ new Map()
	};
}
function stableGroupedMessages(messages, cache) {
	const grouped = groupMessageListItems(messages);
	const nextEntries = [];
	const nextEntryByKey = /* @__PURE__ */ new Map();
	const nextByKey = /* @__PURE__ */ new Map();
	for (const key of Object.keys(grouped)) {
		const candidate = grouped[key];
		const prev = cache.prevByKey.get(key);
		const adopted = prev && arraysShallowEqual(prev, candidate) ? prev : candidate;
		const previousEntry = cache.prevEntryByKey.get(key);
		const entry = previousEntry?.[1] === adopted ? previousEntry : [key, adopted];
		nextEntries.push(entry);
		nextEntryByKey.set(key, entry);
		nextByKey.set(key, adopted);
	}
	cache.prevEntryByKey = nextEntryByKey;
	cache.prevByKey = nextByKey;
	if (entriesShallowEqual(cache.prevEntries, nextEntries)) return cache.prevEntries;
	cache.prevEntries = nextEntries;
	return nextEntries;
}
var MULTI_SELECT_BOTTOM_PADDING_PX = 96;
var MESSAGE_OUTLINE_LAYOUTS = [
	"horizontal",
	"vertical",
	"fold",
	"grid"
];
var CHAT_SIDE_PADDING_PX = 24;
var RAIL_GUTTER_MAX_PX = 24;
var RAIL_GUTTER_START_PX = 700;
var RAIL_GUTTER_FADE_PX = 120;
var EMPTY_LIVE_MESSAGE_IDS = [];
var EMPTY_PARTS_BY_MESSAGE_ID = {};
var pendingTopicImageActionsByTopic = /* @__PURE__ */ new Map();
function enqueuePendingTopicImageAction(topicId, action) {
	const pendingActions = pendingTopicImageActionsByTopic.get(topicId) ?? [];
	pendingActions.push(action);
	pendingTopicImageActionsByTopic.set(topicId, pendingActions);
}
function takePendingTopicImageActions(topicId) {
	const pendingActions = pendingTopicImageActionsByTopic.get(topicId);
	if (!pendingActions) return [];
	pendingTopicImageActionsByTopic.delete(topicId);
	return pendingActions;
}
function rejectPendingTopicImageActions(topicId, reason) {
	for (const pendingAction of takePendingTopicImageActions(topicId)) pendingAction.reject(reason);
}
function getMessageElementLayout(element) {
	return MESSAGE_OUTLINE_LAYOUTS.find((layout) => element.classList.contains(layout)) ?? "fold";
}
function MessageGroupLayer({ groupKey, isLive, narrowMode, railGutterPx, messages, partsByMessageId, ...messageGroupProps }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PartsProvider, {
		value: partsByMessageId ?? null,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NarrowLayout_default, {
			narrowMode,
			withSidePadding: true,
			style: {
				paddingLeft: CHAT_SIDE_PADDING_PX + railGutterPx,
				paddingRight: CHAT_SIDE_PADDING_PX + railGutterPx
			},
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageGroup_default, {
				...messageGroupProps,
				messages,
				partsByMessageId
			}, groupKey)
		})
	});
}
function groupPartsShallowEqual(previous, next, messages) {
	if (previous === next) return true;
	return messages.every((message) => previous?.[message.id] === next?.[message.id]);
}
var MessageLayer = (0, import_react.memo)(MessageGroupLayer, (previous, next) => {
	if (previous.isLive || next.isLive) return false;
	return previous.groupKey === next.groupKey && previous.narrowMode === next.narrowMode && previous.railGutterPx === next.railGutterPx && previous.messages === next.messages && groupPartsShallowEqual(previous.partsByMessageId, next.partsByMessageId, next.messages) && previous.captureMode === next.captureMode && previous.registerMessageElement === next.registerMessageElement && previous.isLatestAssistantGroup === next.isLatestAssistantGroup && previous.directAssistantModelsByUserId === next.directAssistantModelsByUserId && previous.messageTail === next.messageTail;
});
var MessageList = ({ enableSearch = false }) => {
	const data = useMessageListData();
	const actions = useMessageListActions();
	const meta = useMessageListMeta();
	const renderConfig = useMessageRenderConfig() ?? defaultMessageRenderConfig;
	const selection = useMessageListSelection();
	const messageUi = useMessageListUi();
	const partsByMessageId = usePartsMap();
	const { setForceWideLayout, railGutterPx, setRailGutterPx } = useChatLayoutMode();
	const { topic, messages, beforeList, messageTail, hasOlder = false, messageNavigation } = data;
	const [isLoadingMore, setIsLoadingMore] = (0, import_react.useState)(false);
	const { setTimeoutTimer } = useTimer();
	const isMultiSelectMode = selection?.isMultiSelectMode ?? false;
	const selectedMessageIds = selection?.selectedMessageIds ?? [];
	const [activeOutline, setActiveOutline] = (0, import_react.useState)(null);
	const [activeAnchorMessageId, setActiveAnchorMessageId] = (0, import_react.useState)(null);
	const bottomOverlayInsets = useChatBottomOverlayInset();
	const messageListRef = (0, import_react.useRef)(null);
	const messageListScopeRef = (0, import_react.useRef)(null);
	const scrollContainerRef = (0, import_react.useRef)(null);
	const topicImageCaptureRef = (0, import_react.useRef)(null);
	const messageElements = (0, import_react.useRef)(/* @__PURE__ */ new Map());
	const isLoadingMoreRef = (0, import_react.useRef)(false);
	const [groupLayoutOverrides, setGroupLayoutOverrides] = (0, import_react.useState)({});
	const [topicImageCaptureActions, setTopicImageCaptureActions] = (0, import_react.useState)([]);
	const topicImageCaptureActionsRef = (0, import_react.useRef)([]);
	const groupedMessagesCacheRef = (0, import_react.useRef)(createStableGroupedMessagesCache());
	const groupedMessages = (0, import_react.useMemo)(() => stableGroupedMessages(messages, groupedMessagesCacheRef.current), [messages]);
	const anchorMessagesCacheRef = (0, import_react.useRef)(createStableAnchorMessagesCache());
	const anchorMessages = (0, import_react.useMemo)(() => stableAnchorMessages(messages, anchorMessagesCacheRef.current), [messages]);
	const messageById = (0, import_react.useMemo)(() => new Map(messages.map((message) => [message.id, message])), [messages]);
	const directAssistantModelsByUserIdRef = (0, import_react.useRef)(void 0);
	const directAssistantModelsByUserId = (0, import_react.useMemo)(() => {
		const next = getDirectAssistantModelsByUserId(messages);
		const shared = shareDirectAssistantModelsByUserId(directAssistantModelsByUserIdRef.current, next);
		directAssistantModelsByUserIdRef.current = shared;
		return shared;
	}, [messages]);
	const messageByIdRef = (0, import_react.useRef)(messageById);
	messageByIdRef.current = messageById;
	const latestAssistantGroupKey = (0, import_react.useMemo)(() => getLatestAssistantGroupKey(messages), [messages]);
	const streamingLayers = data.streamingLayers;
	const liveMessageIds = streamingLayers?.liveMessageIds ?? EMPTY_LIVE_MESSAGE_IDS;
	const liveMessageIdSet = (0, import_react.useMemo)(() => new Set(liveMessageIds), [liveMessageIds]);
	const isSearchStreaming = (0, import_react.useMemo)(() => liveMessageIds.length > 0 || messages.some((message) => message.status === "pending"), [liveMessageIds.length, messages]);
	const firstLiveGroupIndex = (0, import_react.useMemo)(() => {
		if (!streamingLayers) return 0;
		if (liveMessageIds.length === 0) return groupedMessages.length;
		const liveIndex = groupedMessages.findIndex(([, groupMessages]) => groupMessages.some((message) => liveMessageIdSet.has(message.id)));
		return liveIndex >= 0 ? liveIndex : groupedMessages.length;
	}, [
		groupedMessages,
		liveMessageIdSet,
		liveMessageIds.length,
		streamingLayers
	]);
	const { bindRuntime, copyImage, loadOlder, saveImage } = actions;
	const getMessageUiState = (0, import_react.useCallback)((messageId) => messageUi.getMessageUiState?.(messageId) ?? {}, [messageUi]);
	const useWideMessageLayout = (0, import_react.useMemo)(() => groupedMessages.some(([key, groupMessages]) => shouldUseWideLayoutForMessageGroup(groupMessages, (messageId) => {
		const uiState = getMessageUiState(messageId);
		return {
			...uiState,
			multiModelMessageStyle: groupLayoutOverrides[key] ?? uiState.multiModelMessageStyle
		};
	}, renderConfig.multiModelMessageStyle, isMultiSelectMode)), [
		getMessageUiState,
		groupLayoutOverrides,
		groupedMessages,
		isMultiSelectMode,
		renderConfig.multiModelMessageStyle
	]);
	const messageListNarrowMode = renderConfig.narrowMode && !useWideMessageLayout;
	const shouldTrackMessageOutline = renderConfig.showMessageOutline && !isMultiSelectMode;
	(0, import_react.useEffect)(() => {
		setForceWideLayout(useWideMessageLayout);
		return () => setForceWideLayout(false);
	}, [setForceWideLayout, useWideMessageLayout]);
	const registerMessageElement = (0, import_react.useCallback)((id, element) => {
		if (element) messageElements.current.set(id, element);
		else messageElements.current.delete(id);
	}, []);
	const getMessageElement = (0, import_react.useCallback)((id) => messageElements.current.get(id) ?? null, []);
	const scrollToBottom = (0, import_react.useCallback)(() => {
		messageListRef.current?.scrollToBottom();
	}, []);
	const navigateToTop = (0, import_react.useCallback)(() => {
		messageListRef.current?.scrollToTop("smooth");
	}, []);
	const navigateToBottom = (0, import_react.useCallback)(() => {
		messageListRef.current?.scrollToBottom();
	}, []);
	const scrollToMessageById = (0, import_react.useCallback)((messageId) => {
		const target = messageByIdRef.current.get(messageId);
		if (!target) return;
		const groupKey = target.role === "assistant" && target.parentId ? "assistant" + target.parentId : target.role + target.id;
		messageListRef.current?.scrollToKey(groupKey, "start");
	}, []);
	const scrollToOutlineElement = (0, import_react.useCallback)((element) => {
		messageListRef.current?.scrollToElement(element);
	}, []);
	const scrollToRange = (0, import_react.useCallback)((range) => {
		messageListRef.current?.scrollToRange(range);
	}, []);
	const getOuterScroller = (0, import_react.useCallback)(() => messageListRef.current?.getScrollElement() ?? null, []);
	const updateActiveMessageOutline = (0, import_react.useCallback)(() => {
		if (!shouldTrackMessageOutline) {
			setActiveOutline((current) => current ? null : current);
			return;
		}
		const scrollElement = scrollContainerRef.current ?? messageListRef.current?.getScrollElement();
		if (!scrollElement) {
			setActiveOutline(null);
			return;
		}
		const containerRect = scrollElement.getBoundingClientRect();
		const viewportCenter = containerRect.top + containerRect.height / 2;
		let bestMatch = null;
		for (const [messageId, element] of messageElements.current) {
			const message = messageById.get(messageId);
			if (!message) {
				messageElements.current.delete(messageId);
				continue;
			}
			if (message.role !== "assistant" || message.isContextBoundary) continue;
			if (!element.isConnected || !scrollElement.contains(element)) {
				messageElements.current.delete(messageId);
				continue;
			}
			const rect = element.getBoundingClientRect();
			if (Math.min(rect.bottom, containerRect.bottom) - Math.max(rect.top, containerRect.top) <= 0) continue;
			const distance = rect.top <= viewportCenter && rect.bottom >= viewportCenter ? 0 : Math.min(Math.abs(rect.top - viewportCenter), Math.abs(rect.bottom - viewportCenter));
			if (!bestMatch || distance < bestMatch.distance) bestMatch = {
				messageId: message.id,
				multiModelMessageStyle: getMessageElementLayout(element),
				distance
			};
		}
		setActiveOutline((current) => {
			if (current?.messageId === bestMatch?.messageId && current?.multiModelMessageStyle === bestMatch?.multiModelMessageStyle) return current;
			return bestMatch ? {
				messageId: bestMatch.messageId,
				multiModelMessageStyle: bestMatch.multiModelMessageStyle
			} : null;
		});
	}, [messageById, shouldTrackMessageOutline]);
	const updateActiveMessageOutlineRef = (0, import_react.useRef)(updateActiveMessageOutline);
	updateActiveMessageOutlineRef.current = updateActiveMessageOutline;
	const activeMessageOutlineFrameRef = (0, import_react.useRef)(null);
	const requestActiveMessageOutlineUpdate = (0, import_react.useCallback)(() => {
		if (activeMessageOutlineFrameRef.current !== null) return;
		activeMessageOutlineFrameRef.current = requestAnimationFrame(() => {
			activeMessageOutlineFrameRef.current = null;
			updateActiveMessageOutlineRef.current();
		});
	}, []);
	const cancelActiveMessageOutlineUpdate = (0, import_react.useCallback)(() => {
		if (activeMessageOutlineFrameRef.current === null) return;
		cancelAnimationFrame(activeMessageOutlineFrameRef.current);
		activeMessageOutlineFrameRef.current = null;
	}, []);
	const shouldTrackAnchorPosition = messageNavigation === "anchor";
	const updateActiveAnchorMessage = (0, import_react.useCallback)(() => {
		if (!shouldTrackAnchorPosition) return;
		const scrollElement = scrollContainerRef.current ?? messageListRef.current?.getScrollElement();
		if (!scrollElement) return;
		const containerRect = scrollElement.getBoundingClientRect();
		const scrollRange = scrollElement.scrollHeight - scrollElement.clientHeight;
		const readingLineY = scrollElement.scrollTop >= scrollRange - 2 ? containerRect.bottom - 1 : containerRect.top + Math.min(120, containerRect.height * .25);
		let bestMatch = null;
		for (const [messageId, element] of messageElements.current) {
			const message = messageById.get(messageId);
			if (!message || message.isContextBoundary) continue;
			if (!element.isConnected || !scrollElement.contains(element)) continue;
			const rect = element.getBoundingClientRect();
			if (Math.min(rect.bottom, containerRect.bottom) - Math.max(rect.top, containerRect.top) <= 0) continue;
			const distance = rect.top <= readingLineY && rect.bottom >= readingLineY ? 0 : Math.min(Math.abs(rect.top - readingLineY), Math.abs(rect.bottom - readingLineY));
			if (!bestMatch || distance < bestMatch.distance) bestMatch = {
				messageId,
				distance
			};
		}
		const nextId = bestMatch?.messageId ?? null;
		setActiveAnchorMessageId((current) => current === nextId ? current : nextId);
	}, [messageById, shouldTrackAnchorPosition]);
	const updateActiveAnchorMessageRef = (0, import_react.useRef)(updateActiveAnchorMessage);
	updateActiveAnchorMessageRef.current = updateActiveAnchorMessage;
	const loadMoreMessages = (0, import_react.useCallback)(() => {
		if (!hasOlder || isLoadingMoreRef.current || !loadOlder) return;
		isLoadingMoreRef.current = true;
		setIsLoadingMore(true);
		setTimeoutTimer("message-list-load-older", () => {
			try {
				loadOlder();
			} finally {
				setTimeoutTimer("message-list-load-older-spinner", () => {
					isLoadingMoreRef.current = false;
					setIsLoadingMore(false);
				}, data.loadingResetDelayMs);
			}
		}, data.loadOlderDelayMs);
	}, [
		data.loadOlderDelayMs,
		data.loadingResetDelayMs,
		hasOlder,
		loadOlder,
		setTimeoutTimer
	]);
	const executeTopicImageAction = (0, import_react.useCallback)(async (action, captureRef) => {
		if (action === "copy") {
			const canvas = await captureScrollable(captureRef);
			const blob = canvas ? await new Promise((resolve) => canvas.toBlob(resolve, "image/png")) : null;
			if (!blob) throw new Error("Failed to capture topic image");
			await copyImage?.(blob);
			return;
		}
		if (!meta.imageExportFileName || !saveImage) throw new Error("Topic image export is unavailable");
		const imageData = await captureScrollableAsDataUrl(captureRef);
		if (!imageData) throw new Error("Failed to capture topic image");
		if (await saveImage(removeSpecialCharactersForFileName(meta.imageExportFileName), imageData) === false) throw new Error("Failed to save topic image");
	}, [
		copyImage,
		meta.imageExportFileName,
		saveImage
	]);
	const enqueueTopicImageCaptureAction = (0, import_react.useCallback)((action) => {
		return new Promise((resolve, reject) => {
			const scrollContainer = scrollContainerRef.current;
			const captureAction = {
				action,
				captureWidth: scrollContainer?.clientWidth || scrollContainer?.getBoundingClientRect().width || void 0,
				reject,
				resolve
			};
			setTopicImageCaptureActions((current) => {
				const nextActions = [...current, captureAction];
				topicImageCaptureActionsRef.current = nextActions;
				return nextActions;
			});
		});
	}, []);
	const runTopicImageAction = (0, import_react.useCallback)(async (action) => {
		if (data.isInitialLoading || !scrollContainerRef.current) return new Promise((resolve, reject) => {
			enqueuePendingTopicImageAction(topic.id, {
				action,
				reject,
				resolve
			});
		});
		await enqueueTopicImageCaptureAction(action);
	}, [
		data.isInitialLoading,
		enqueueTopicImageCaptureAction,
		topic.id
	]);
	const runtimeActionsRef = (0, import_react.useRef)({
		scrollToBottom,
		scrollToMessageById,
		runTopicImageAction
	});
	runtimeActionsRef.current = {
		scrollToBottom,
		scrollToMessageById,
		runTopicImageAction
	};
	const flushPendingTopicImageAction = (0, import_react.useCallback)(() => {
		if (data.isInitialLoading || !scrollContainerRef.current) return;
		for (const pendingAction of takePendingTopicImageActions(topic.id)) enqueueTopicImageCaptureAction(pendingAction.action).then(pendingAction.resolve, pendingAction.reject);
	}, [
		data.isInitialLoading,
		enqueueTopicImageCaptureAction,
		topic.id
	]);
	const handleScrollContainerReady = (0, import_react.useCallback)((element) => {
		scrollContainerRef.current = element;
		flushPendingTopicImageAction();
	}, [flushPendingTopicImageAction]);
	(0, import_react.useEffect)(() => {
		const topicId = topic.id;
		return () => {
			const cancelReason = /* @__PURE__ */ new Error("Topic image export was cancelled");
			rejectPendingTopicImageActions(topicId, cancelReason);
			for (const pendingAction of topicImageCaptureActionsRef.current) pendingAction.reject(cancelReason);
			topicImageCaptureActionsRef.current = [];
		};
	}, [topic.id]);
	const activeTopicImageCaptureAction = topicImageCaptureActions[0] ?? null;
	(0, import_react.useEffect)(() => {
		topicImageCaptureActionsRef.current = topicImageCaptureActions;
	}, [topicImageCaptureActions]);
	(0, import_react.useEffect)(() => {
		if (!activeTopicImageCaptureAction || !topicImageCaptureRef.current) return;
		let cancelled = false;
		let secondFrame;
		const firstFrame = requestAnimationFrame(() => {
			secondFrame = requestAnimationFrame(() => {
				if (cancelled) return;
				executeTopicImageAction(activeTopicImageCaptureAction.action, topicImageCaptureRef).then(activeTopicImageCaptureAction.resolve, activeTopicImageCaptureAction.reject).finally(() => {
					if (cancelled) return;
					setTopicImageCaptureActions((current) => {
						const nextActions = current[0] === activeTopicImageCaptureAction ? current.slice(1) : current.filter((captureAction) => captureAction !== activeTopicImageCaptureAction);
						topicImageCaptureActionsRef.current = nextActions;
						return nextActions;
					});
				});
			});
		});
		return () => {
			cancelled = true;
			cancelAnimationFrame(firstFrame);
			if (secondFrame !== void 0) cancelAnimationFrame(secondFrame);
		};
	}, [activeTopicImageCaptureAction, executeTopicImageAction]);
	(0, import_react.useEffect)(() => {
		scrollContainerRef.current = messageListRef.current?.getScrollElement() ?? null;
		flushPendingTopicImageAction();
	}, [flushPendingTopicImageAction, groupedMessages]);
	(0, import_react.useEffect)(() => {
		if (shouldTrackMessageOutline) {
			requestActiveMessageOutlineUpdate();
			return;
		}
		cancelActiveMessageOutlineUpdate();
		setActiveOutline((current) => current ? null : current);
	}, [
		cancelActiveMessageOutlineUpdate,
		groupedMessages,
		requestActiveMessageOutlineUpdate,
		shouldTrackMessageOutline
	]);
	(0, import_react.useEffect)(() => cancelActiveMessageOutlineUpdate, [cancelActiveMessageOutlineUpdate]);
	(0, import_react.useEffect)(() => {
		if (!shouldTrackMessageOutline) return;
		const scrollElement = messageListRef.current?.getScrollElement();
		if (!scrollElement) return;
		const handleOutlineUpdate = requestActiveMessageOutlineUpdate;
		scrollElement.addEventListener("scroll", handleOutlineUpdate, { passive: true });
		window.addEventListener("resize", handleOutlineUpdate);
		return () => {
			scrollElement.removeEventListener("scroll", handleOutlineUpdate);
			window.removeEventListener("resize", handleOutlineUpdate);
		};
	}, [
		data.isInitialLoading,
		data.listKey,
		requestActiveMessageOutlineUpdate,
		shouldTrackMessageOutline,
		topic.id
	]);
	(0, import_react.useEffect)(() => {
		if (!shouldTrackAnchorPosition) {
			setActiveAnchorMessageId((current) => current ? null : current);
			return;
		}
		updateActiveAnchorMessage();
	}, [
		groupedMessages,
		shouldTrackAnchorPosition,
		updateActiveAnchorMessage
	]);
	(0, import_react.useEffect)(() => {
		if (!shouldTrackAnchorPosition) {
			setRailGutterPx(0);
			return;
		}
		const scrollElement = messageListRef.current?.getScrollElement();
		if (!scrollElement) return;
		const updateRailGutter = () => {
			const ramp = (scrollElement.clientWidth - RAIL_GUTTER_START_PX) / RAIL_GUTTER_FADE_PX;
			setRailGutterPx(Math.round(Math.max(0, Math.min(1, ramp)) * RAIL_GUTTER_MAX_PX));
		};
		updateRailGutter();
		const resizeObserver = new ResizeObserver(updateRailGutter);
		resizeObserver.observe(scrollElement);
		let frame = null;
		const handleAnchorUpdate = () => {
			if (frame !== null) return;
			frame = requestAnimationFrame(() => {
				frame = null;
				updateActiveAnchorMessageRef.current();
			});
		};
		scrollElement.addEventListener("scroll", handleAnchorUpdate, { passive: true });
		window.addEventListener("resize", handleAnchorUpdate);
		return () => {
			resizeObserver.disconnect();
			if (frame !== null) cancelAnimationFrame(frame);
			scrollElement.removeEventListener("scroll", handleAnchorUpdate);
			window.removeEventListener("resize", handleAnchorUpdate);
			setRailGutterPx(0);
		};
	}, [
		data.isInitialLoading,
		data.listKey,
		setRailGutterPx,
		shouldTrackAnchorPosition,
		topic.id
	]);
	(0, import_react.useEffect)(() => {
		return bindRuntime?.({
			scrollToBottom: () => runtimeActionsRef.current.scrollToBottom(),
			locateMessage: (messageId) => runtimeActionsRef.current.scrollToMessageById(messageId),
			copyTopicImage: () => runtimeActionsRef.current.runTopicImageAction("copy"),
			exportTopicImage: () => runtimeActionsRef.current.runTopicImageAction("export")
		});
	}, [bindRuntime]);
	if (data.isInitialLoading && (messages.length === 0 || data.isMessagesStale)) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageListInitialLoading, {});
	const activeOutlineMessage = activeOutline ? messages.find((message) => message.id === activeOutline.messageId) : void 0;
	const keepMountedKeys = ((latestAssistantGroupKey ? groupedMessages.find(([key]) => key === latestAssistantGroupKey)?.[1] : void 0)?.some((message) => message.role === "assistant" && (messageUi.getMessageActivityState?.(message).isProcessing ?? message.status === "pending")) ?? false) && latestAssistantGroupKey ? [latestAssistantGroupKey] : [];
	const defaultBottomPadding = isMultiSelectMode ? MULTI_SELECT_BOTTOM_PADDING_PX : 12;
	const bottomPadding = bottomOverlayInsets == null ? defaultBottomPadding : Math.max(bottomOverlayInsets.contentBottomPadding, isMultiSelectMode ? defaultBottomPadding : 0);
	const scrollerBottomMargin = bottomOverlayInsets?.scrollerBottomMargin ?? 0;
	const topPadding = 6;
	const topicImageCaptureWidth = activeTopicImageCaptureAction?.captureWidth;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(MessagesContainer, {
		id: "messages",
		className: classNames(["messages-container", { "multi-select-mode": isMultiSelectMode }]),
		children: [
			beforeList && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NarrowLayout_default, {
				narrowMode: messageListNarrowMode,
				withSidePadding: true,
				className: "shrink-0",
				style: {
					paddingLeft: CHAT_SIDE_PADDING_PX + railGutterPx,
					paddingRight: CHAT_SIDE_PADDING_PX + railGutterPx
				},
				children: beforeList
			}),
			enableSearch && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageListSearch, {
				messages,
				partsByMessageId: partsByMessageId ?? EMPTY_PARTS_BY_MESSAGE_ID,
				renderUserTextAsMarkdown: renderConfig.renderInputMessageAsMarkdown,
				excludedMessageIds: liveMessageIdSet,
				isStreaming: isSearchStreaming,
				locateMessage: scrollToMessageById,
				scrollToRange,
				getOuterScroller,
				scopeRef: messageListScopeRef
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectionContextMenu_default, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				ref: messageListScopeRef,
				style: {
					display: "flex",
					flexDirection: "column",
					flex: 1,
					minHeight: 0
				},
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageVirtualList, {
					handleRef: messageListRef,
					items: groupedMessages,
					getItemKey: ([key]) => key,
					estimateSize: data.estimateSize,
					overscan: data.overscan,
					topPadding,
					bottomPadding,
					keepMountedKeys,
					showScrollToBottomButton: true,
					scrollToBottomButtonBottomOffset: Math.max(24, bottomPadding),
					topicId: topic.id,
					hasMoreTop: hasOlder,
					onScrollContainerReady: handleScrollContainerReady,
					onReachTop: loadMoreMessages,
					renderItem: ([key, groupMessages], index) => {
						const groupMessageTail = messageTail && groupMessages.some((message) => message.id === messageTail.messageId) ? messageTail : void 0;
						return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageLayer, {
							groupKey: key,
							isLive: index >= firstLiveGroupIndex,
							narrowMode: messageListNarrowMode,
							railGutterPx,
							isLatestAssistantGroup: key === latestAssistantGroupKey,
							directAssistantModelsByUserId,
							messageTail: groupMessageTail,
							messages: groupMessages,
							partsByMessageId: index < firstLiveGroupIndex && streamingLayers ? streamingLayers.historyPartsByMessageId : partsByMessageId,
							registerMessageElement,
							onMultiModelMessageStyleChange: (style) => {
								setGroupLayoutOverrides((current) => current[key] === style ? current : {
									...current,
									[key]: style
								});
							}
						});
					},
					style: {
						flex: 1,
						minHeight: 0,
						marginBottom: scrollerBottomMargin
					}
				}), isLoadingMore && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "pointer-events-none flex w-full justify-center py-2.5",
					style: { background: "var(--background)" },
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingIcon_default, { color: "var(--muted-foreground)" })
				})]
			}) }),
			topicImageCaptureActions.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				ref: topicImageCaptureRef,
				"aria-hidden": "true",
				"data-topic-image-capture": true,
				className: classNames("-left-[10000px] pointer-events-none fixed top-0 overflow-visible bg-background text-foreground", !topicImageCaptureWidth && "w-full"),
				style: topicImageCaptureWidth ? { width: `${topicImageCaptureWidth}px` } : void 0,
				children: groupedMessages.map(([key, groupMessages]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NarrowLayout_default, {
					narrowMode: messageListNarrowMode,
					withSidePadding: true,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageGroup_default, {
						captureMode: true,
						isLatestAssistantGroup: key === latestAssistantGroupKey,
						directAssistantModelsByUserId,
						messages: groupMessages,
						partsByMessageId
					})
				}, key))
			}),
			messageNavigation === "anchor" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageAnchorLine_default, {
				messages: anchorMessages,
				activeMessageId: activeAnchorMessageId,
				hasOlder,
				historyPartsByMessageId: streamingLayers?.historyPartsByMessageId ?? partsByMessageId ?? EMPTY_PARTS_BY_MESSAGE_ID,
				liveMessageIds,
				railOpacity: railGutterPx / RAIL_GUTTER_MAX_PX,
				scrollToMessageId: scrollToMessageById
			}),
			activeOutline && activeOutlineMessage && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageOutline_default, {
				message: activeOutlineMessage,
				multiModelMessageStyle: activeOutline.multiModelMessageStyle,
				onNavigateToElement: scrollToOutlineElement
			}),
			messageNavigation === "buttons" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageNavigation_default, {
				scrollContainerRef,
				getMessageElement,
				messages,
				scrollToMessageId: scrollToMessageById,
				scrollToTop: navigateToTop,
				scrollToBottom: navigateToBottom
			}),
			meta.selectionLayer && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectionBox_default, {
				isMultiSelectMode,
				scrollContainerRef,
				messageElements: messageElements.current,
				handleSelectMessage: (messageId, selected) => actions.selectMessage?.(messageId, selected)
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(MultiSelectActionPopup_default, {
				selectedMessageIds,
				isMultiSelectMode,
				deleteDisabledReason: selectedMessageIds.map((messageId) => actions.getMessageDeleteAvailability?.(messageId)).find((availability) => availability?.enabled === false)?.reason,
				onSave: actions.saveSelectedMessages ? () => void actions.saveSelectedMessages?.(selectedMessageIds) : void 0,
				onCopy: actions.copySelectedMessages ? () => void actions.copySelectedMessages?.(selectedMessageIds) : void 0,
				onDelete: actions.deleteSelectedMessages ? () => void actions.deleteSelectedMessages?.(selectedMessageIds) : void 0,
				onClose: () => actions.toggleMultiSelectMode?.(false)
			})
		]
	}, data.listKey);
};
var MessageList_default = MessageList;
function useMessageActivityState(topicId, partsMap) {
	const { activeExecutions, awaitingApprovalAnchors } = useTopicStreamStatus(topicId);
	return (0, import_react.useCallback)((message) => {
		const isActiveExecutionTarget = activeExecutions.some((execution) => execution.anchorMessageId === message.id);
		const isApprovalAnchor = awaitingApprovalAnchors.some((execution) => execution.anchorMessageId === message.id);
		const isProcessing = isMessageListItemProcessing(message) || isActiveExecutionTarget || isApprovalAnchor;
		return {
			isProcessing,
			isStreamTarget: isProcessing,
			isApprovalAnchor
		};
	}, [activeExecutions, awaitingApprovalAnchors]);
}
var AI_CLASSIFY_TTL_MS = 3600 * 1e3;
var aiClassifyCacheKey = (message, language) => `error.classify.${message}:${language}`;
function useMessageErrorActions(options = {}) {
	const navigate = useNavigate();
	const { persistDiagnosis } = options;
	const diagnoseMessageError = (0, import_react.useCallback)(({ error, language }) => {
		const errorMessage = error.message;
		if (!errorMessage) return Promise.resolve(null);
		const cacheKey = aiClassifyCacheKey(errorMessage, language);
		const cached = cacheService.getCasual(cacheKey);
		if (cached) return cached;
		const promise = classifyErrorByAI(error, language).catch((classificationError) => {
			cacheService.deleteCasual(cacheKey);
			throw classificationError;
		});
		cacheService.setCasual(cacheKey, promise, AI_CLASSIFY_TTL_MS);
		return promise;
	}, []);
	const openErrorDetail = (0, import_react.useCallback)((input) => {
		showErrorDetailPopup({
			error: input.error,
			blockId: input.partId,
			cachedDiagnosis: input.cachedDiagnosis,
			diagnosisContext: input.diagnosisContext,
			onDiagnosisComplete: persistDiagnosis
		});
	}, [persistDiagnosis]);
	const navigateErrorTarget = (0, import_react.useCallback)((target) => {
		navigate({ to: target });
	}, [navigate]);
	return (0, import_react.useMemo)(() => ({
		diagnoseMessageError,
		openErrorDetail,
		navigateErrorTarget
	}), [
		diagnoseMessageError,
		navigateErrorTarget,
		openErrorDetail
	]);
}
function useMessageExportActions({ topicName }) {
	const { notesPath } = useNotesSettings();
	const saveTextFile = (0, import_react.useCallback)((fileName, content) => {
		return window.api.file.save(fileName, content);
	}, []);
	const saveImage = (0, import_react.useCallback)((fileName, dataUrl) => {
		return window.api.file.saveImage(fileName, dataUrl);
	}, []);
	const exportToWord = (0, import_react.useCallback)((markdown, title) => {
		return ipcApi.request("export.word.from_markdown", {
			markdown,
			fileName: title
		});
	}, []);
	const saveToKnowledge = (0, import_react.useCallback)((message) => {
		SaveToKnowledgePopup_default.showForMessage(message);
	}, []);
	const exportMessageAsMarkdown$1 = (0, import_react.useCallback)((message, includeReasoning) => {
		return exportMessageAsMarkdown(message, includeReasoning);
	}, []);
	const exportToNotes = (0, import_react.useCallback)(async (message) => {
		return exportMessageToNotes(await getMessageTitle(message), await messageToMarkdown(message), notesPath);
	}, [notesPath]);
	const exportToNotion = (0, import_react.useCallback)(async (message) => {
		await exportMessageToNotion(await getMessageTitle(message), await messageToMarkdown(message), message);
	}, []);
	const exportToYuque = (0, import_react.useCallback)(async (message) => {
		await exportMarkdownToYuque(await getMessageTitle(message), await messageToMarkdown(message));
	}, []);
	const exportToObsidian = (0, import_react.useCallback)(async (message) => {
		const title = topicName?.replace(/\\/g, "_") || "Untitled";
		await ObsidianExportPopup_default.show({
			title,
			message,
			processingMethod: "1"
		});
	}, [topicName]);
	const exportToJoplin = (0, import_react.useCallback)(async (message) => {
		await exportMarkdownToJoplin(await getMessageTitle(message), message);
	}, []);
	const exportToSiyuan = (0, import_react.useCallback)(async (message) => {
		return exportMarkdownToSiyuan(await getMessageTitle(message), await messageToMarkdown(message));
	}, []);
	return (0, import_react.useMemo)(() => ({
		saveTextFile,
		saveImage,
		saveToKnowledge,
		exportMessageAsMarkdown: exportMessageAsMarkdown$1,
		exportToNotes,
		exportToWord,
		exportToNotion,
		exportToYuque,
		exportToObsidian,
		exportToJoplin,
		exportToSiyuan
	}), [
		exportMessageAsMarkdown$1,
		exportToJoplin,
		exportToNotes,
		exportToNotion,
		exportToObsidian,
		exportToSiyuan,
		exportToWord,
		exportToYuque,
		saveImage,
		saveTextFile,
		saveToKnowledge
	]);
}
function useMessageHeaderCapabilities() {
	const avatar = useAvatar();
	const openUserProfile = (0, import_react.useCallback)(() => {
		UserPopup_default.show();
	}, []);
	return (0, import_react.useMemo)(() => ({
		userProfile: { avatar },
		openUserProfile
	}), [avatar, openUserProfile]);
}
var PopupContainer = ({ text, title, extension, open, resolve }) => {
	const [fontSize] = usePreference("chat.message.font_size");
	const { activeCmTheme } = useCodeStyle();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog, {
		open,
		onOpenChange: (nextOpen) => !nextOpen && resolve(),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogContent, {
			className: "h-[80vh] max-h-[calc(100vh-2rem)] max-w-[700px] grid-rows-[auto_minmax(0,1fr)] overflow-hidden rounded-[20px] p-0 sm:max-w-[700px]",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogHeader, {
				className: "px-6 pt-6",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogTitle, { children: title })
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "min-h-0 overflow-hidden",
				children: extension !== void 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(code_editor_default, {
					className: "[&_.cm-line]:cursor-text",
					theme: activeCmTheme,
					fontSize: fontSize - 1,
					readOnly: true,
					expanded: false,
					height: "100%",
					style: { height: "100%" },
					value: text,
					language: extension,
					options: { keymap: true }
				}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "h-full cursor-text overflow-auto whitespace-pre p-4 text-foreground text-sm",
					children: text
				})
			})]
		})
	});
};
var TextFilePreviewPopupHandle = createPopup(PopupContainer);
var TextFilePreviewPopup_default = {
	show: (text, title, extension) => TextFilePreviewPopupHandle.show({
		text,
		title,
		extension
	}),
	hide: () => TextFilePreviewPopupHandle.hide()
};
var logger$6 = loggerService.withContext("FileAction");
function useAttachment() {
	const { t: t$2 } = useTranslation();
	const preview = async (path, title, fileType, extension) => {
		try {
			if (fileType === FILE_TYPE.TEXT) {
				const content = await window.api.fs.readText(path);
				let ext = extension;
				if (ext?.startsWith(".")) ext = ext.replace(".", "");
				TextFilePreviewPopup_default.show(content, title, ext);
			} else window.api.file.openPath(path);
		} catch (err) {
			logger$6.error(`Error opening ${path}:`, err);
			popup.error({
				content: t$2("files.preview.error"),
				centered: true
			});
		}
	};
	return { preview };
}
var import_dayjs_min = /* @__PURE__ */ __toESM(require_dayjs_min());
var logger$5 = loggerService.withContext("useMessageLeafCapabilities");
var warnedFileViewPaths = /* @__PURE__ */ new Set();
function isRecord(value) {
	return typeof value === "object" && value !== null && !Array.isArray(value);
}
function isMcpToolPart(part) {
	const partType = part.type;
	if (partType === "dynamic-tool") return true;
	if (!partType?.startsWith("tool-")) return false;
	const record = part;
	const output = isRecord(record.output) ? record.output : void 0;
	if ((isRecord(output?.metadata) ? output.metadata : void 0)?.type === "mcp") return true;
	const providerMetadata = isRecord(record.providerMetadata) ? record.providerMetadata : void 0;
	const cherry = isRecord(providerMetadata?.cherry) ? providerMetadata.cherry : void 0;
	return (isRecord(cherry?.tool) ? cherry.tool : void 0)?.type === "mcp";
}
function fileMetadataToHandle(file) {
	if (file.path) try {
		return createFilePathHandle(AbsoluteFilePathSchema.parse(file.path));
	} catch {
		logger$5.debug("fileMetadataToHandle: falling back to entry id for non-absolute path", {
			fileId: file.id,
			path: file.path
		});
	}
	return createFileEntryHandle(file.id);
}
function formatMessageAttachmentFileName(file, t$2) {
	if (!file.origin_name) return "";
	const date = (0, import_dayjs_min.default)(file.created_at).format("YYYY-MM-DD");
	if (file.origin_name.includes("pasted_text")) return date + " " + t$2("message.attachments.pasted_text") + file.ext;
	if (file.origin_name.startsWith("temp_file") && file.origin_name.includes("image")) return date + " " + t$2("message.attachments.pasted_image") + file.ext;
	return file.origin_name;
}
function useMessageLeafCapabilities({ partsByMessageId, streamingLayers }) {
	const { t: t$2 } = useTranslation();
	const { preview } = useAttachment();
	const platformActions = useMessagePlatformActions();
	const historyPartsByMessageId = streamingLayers?.historyPartsByMessageId;
	const historyHasMcpToolParts = (0, import_react.useMemo)(() => historyPartsByMessageId ? Object.values(historyPartsByMessageId).some((parts) => parts.some(isMcpToolPart)) : false, [historyPartsByMessageId]);
	const { data: mcpServersData } = useQuery("/mcp-servers", { enabled: (0, import_react.useMemo)(() => {
		if (!streamingLayers) return Object.values(partsByMessageId).some((parts) => parts.some(isMcpToolPart));
		if (historyHasMcpToolParts) return true;
		return streamingLayers.liveMessageIds.some((messageId) => partsByMessageId[messageId]?.some(isMcpToolPart));
	}, [
		historyHasMcpToolParts,
		partsByMessageId,
		streamingLayers
	]) });
	const { data: externalApps } = useExternalApps();
	const mcpServers = (0, import_react.useMemo)(() => mcpServersData?.items ?? [], [mcpServersData]);
	const externalCodeEditors = (0, import_react.useMemo)(() => externalApps?.filter((app) => app.tags.includes("code-editor")) ?? [], [externalApps]);
	const previewFile = (0, import_react.useCallback)(async (file) => {
		const fileType = parseFileTypes(file.type);
		if (fileType === null) {
			popup.error({
				content: t$2("files.preview.error"),
				centered: true
			});
			return;
		}
		if (fileType === "text") {
			await preview(file.path, formatMessageAttachmentFileName(file, t$2), fileType, file.ext);
			return;
		}
		try {
			await safeOpen(fileMetadataToHandle(file));
		} catch {
			popup.error({
				content: t$2("files.preview.error"),
				centered: true
			});
		}
	}, [preview, t$2]);
	const getFileView = (0, import_react.useCallback)((file) => {
		const parsedPath = file.path ? AbsoluteFilePathSchema.safeParse(file.path) : void 0;
		if (parsedPath && !parsedPath.success && file.path && !warnedFileViewPaths.has(file.path)) {
			warnedFileViewPaths.add(file.path);
			logger$5.warn("getFileView: non-canonical/invalid attachment path", {
				fileId: file.id,
				path: file.path
			});
		}
		return {
			displayName: formatMessageAttachmentFileName(file, t$2),
			previewUrl: parsedPath?.success ? toSafeFileUrl(parsedPath.data, file.ext || null) : void 0
		};
	}, [t$2]);
	const openFile = (0, import_react.useCallback)((file) => {
		return safeOpen(fileMetadataToHandle(file));
	}, []);
	const subscribeToolProgress = (0, import_react.useCallback)((toolId, onProgress) => {
		return ipcApi.on("mcp.tool.call_progress", (data) => {
			if (data.callId === toolId) onProgress(data.progress);
		});
	}, []);
	const openInExternalApp = (0, import_react.useCallback)((app, path) => {
		window.open(buildEditorUrl(app, path));
	}, []);
	const openExternalUrl = (0, import_react.useCallback)((url) => {
		window.open(url, "_blank", "noopener,noreferrer");
	}, []);
	const isToolAutoApproved = (0, import_react.useCallback)((tool, allowedTools) => {
		if (allowedTools?.includes(tool.id)) return true;
		if (tool.serverId === "hub") return tool.name === "list" || tool.name === "inspect";
		const server = mcpServers.find((item) => item.id === tool.serverId);
		return server ? !server.disabledAutoApproveTools?.includes(tool.name) : false;
	}, [mcpServers]);
	return (0, import_react.useMemo)(() => ({
		previewFile,
		openFile,
		subscribeToolProgress,
		openExternalUrl,
		openInExternalApp,
		...platformActions,
		getFileView,
		isToolAutoApproved,
		externalCodeEditors
	}), [
		externalCodeEditors,
		getFileView,
		isToolAutoApproved,
		openExternalUrl,
		openFile,
		openInExternalApp,
		platformActions,
		previewFile,
		subscribeToolProgress
	]);
}
var MESSAGE_EXPORT_MENU_PREFERENCE_KEYS = {
	image: "data.export.menus.image",
	markdown: "data.export.menus.markdown",
	markdown_reason: "data.export.menus.markdown_reason",
	notion: "data.export.menus.notion",
	yuque: "data.export.menus.yuque",
	joplin: "data.export.menus.joplin",
	obsidian: "data.export.menus.obsidian",
	siyuan: "data.export.menus.siyuan",
	docx: "data.export.menus.docx",
	plain_text: "data.export.menus.plain_text"
};
function useMessageMenuConfig() {
	const [enableDeveloperMode] = usePreference("app.developer_mode.enabled");
	const [confirmDeleteMessage] = usePreference("chat.message.confirm_delete");
	const [exportMenuOptions] = useMultiplePreferences(MESSAGE_EXPORT_MENU_PREFERENCE_KEYS);
	return (0, import_react.useMemo)(() => ({
		confirmDeleteMessage,
		enableDeveloperMode,
		exportMenuOptions: {
			...defaultMessageMenuExportOptions,
			...exportMenuOptions
		}
	}), [
		confirmDeleteMessage,
		enableDeveloperMode,
		exportMenuOptions
	]);
}
function getOrderedSelectedMessageIds(messageIds, orderedMessages) {
	if (orderedMessages.length === 0) return [...messageIds];
	const selected = new Set(messageIds);
	const orderedIds = orderedMessages.filter((message) => selected.has(message.id)).map((message) => message.id);
	const orderedIdSet = new Set(orderedIds);
	return [...orderedIds, ...messageIds.filter((messageId) => !orderedIdSet.has(messageId))];
}
function createSelectedMessageExportViews(messageIds, orderedMessages, partsByMessageId) {
	const messageById = new Map(orderedMessages.map((message) => [message.id, message]));
	return getOrderedSelectedMessageIds(messageIds, orderedMessages).map((messageId) => {
		const message = messageById.get(messageId);
		if (!message) return null;
		return createMessageExportView(message, partsByMessageId[messageId] ?? []);
	}).filter((message) => message !== null);
}
function getSelectedMessagesPlainText(messageIds, orderedMessages, partsByMessageId) {
	return getOrderedSelectedMessageIds(messageIds, orderedMessages).map((messageId) => getComposerTextFromParts(partsByMessageId[messageId] ?? [])).filter(Boolean).join("\n\n---\n\n");
}
function getSelectedMessagesRichClipboardContent(messageIds, orderedMessages, partsByMessageId) {
	return createComposerRichClipboardContentFromPartGroups(getOrderedSelectedMessageIds(messageIds, orderedMessages).map((messageId) => partsByMessageId[messageId] ?? []), "\n\n---\n\n");
}
var logger$4 = loggerService.withContext("useMessageSelectionController");
function useMessageSelectionController({ topicId, messages, partsByMessageId, deleteMessage, saveTextFile, copyRichContent }) {
	const { t: t$2 } = useTranslation();
	const [isMultiSelectMode, setIsMultiSelectMode] = useCache("chat.multi_select_mode");
	const [selectedMessageIds, setSelectedMessageIds] = useCache("chat.selected_message_ids");
	const latestExportDataRef = (0, import_react.useRef)({
		messages,
		partsByMessageId,
		copyRichContent
	});
	latestExportDataRef.current = {
		messages,
		partsByMessageId,
		copyRichContent
	};
	const selectedIds = (0, import_react.useMemo)(() => selectedMessageIds ?? [], [selectedMessageIds]);
	const toggleMultiSelectMode = (0, import_react.useCallback)((enabled) => {
		setIsMultiSelectMode(enabled);
		if (!enabled) setSelectedMessageIds([]);
	}, [setIsMultiSelectMode, setSelectedMessageIds]);
	(0, import_react.useEffect)(() => {
		toggleMultiSelectMode(false);
		return () => {
			toggleMultiSelectMode(false);
		};
	}, [topicId, toggleMultiSelectMode]);
	const selectMessage = (0, import_react.useCallback)((messageId, selected) => {
		setSelectedMessageIds((prev) => selected ? prev.includes(messageId) ? [...prev] : [...prev, messageId] : prev.filter((id) => id !== messageId));
	}, [setSelectedMessageIds]);
	const resolveMessageIds = (0, import_react.useCallback)((messageIds) => {
		return messageIds?.length ? [...messageIds] : selectedIds;
	}, [selectedIds]);
	const ensureSelection = (0, import_react.useCallback)((messageIds) => {
		const ids = resolveMessageIds(messageIds);
		if (ids.length === 0) {
			toast.warning(t$2("chat.multiple.select.empty"));
			return null;
		}
		return ids;
	}, [resolveMessageIds, t$2]);
	const copySelectedMessages = (0, import_react.useCallback)(async (messageIds) => {
		const ids = ensureSelection(messageIds);
		if (!ids) return;
		const latest = latestExportDataRef.current;
		const richContent = latest.copyRichContent ? getSelectedMessagesRichClipboardContent(ids, latest.messages, latest.partsByMessageId) : null;
		const contentToCopy = richContent?.plainText ?? getSelectedMessagesPlainText(ids, latest.messages, latest.partsByMessageId);
		if (!contentToCopy) return;
		try {
			if (richContent && latest.copyRichContent) await latest.copyRichContent(richContent, { successMessage: t$2("message.copied") });
			else {
				await navigator.clipboard.writeText(contentToCopy);
				toast.success(t$2("message.copied"));
			}
			toggleMultiSelectMode(false);
		} catch (error) {
			logger$4.error("Failed to copy selected messages:", error);
			toast.error(formatErrorMessageWithPrefix(error, t$2("common.copy_failed")));
		}
	}, [
		ensureSelection,
		t$2,
		toggleMultiSelectMode
	]);
	const saveSelectedMessages = (0, import_react.useCallback)(async (messageIds) => {
		const ids = ensureSelection(messageIds);
		if (!ids) return;
		if (!saveTextFile) {
			toast.error(t$2("common.save_failed"));
			return;
		}
		const { messages: latestMessages, partsByMessageId: latestPartsByMessageId } = latestExportDataRef.current;
		const exportMessages = createSelectedMessageExportViews(ids, latestMessages, latestPartsByMessageId);
		const contentToSave = exportMessages.length > 0 ? await messagesToMarkdown(exportMessages) : getSelectedMessagesPlainText(ids, latestMessages, latestPartsByMessageId);
		if (!contentToSave) return;
		const fileName = `chat_export_${(/* @__PURE__ */ new Date()).toISOString().slice(0, 19).replace(/[T:]/g, "-")}.md`;
		try {
			if (await saveTextFile(fileName, contentToSave) === null) return;
			toast.success(t$2("message.save.success.title"));
			toggleMultiSelectMode(false);
		} catch (error) {
			logger$4.error("Failed to save selected messages:", error);
			toast.error(formatErrorMessageWithPrefix(error, t$2("common.save_failed")));
		}
	}, [
		ensureSelection,
		saveTextFile,
		t$2,
		toggleMultiSelectMode
	]);
	const deleteSelectedMessages = (0, import_react.useCallback)(async (messageIds) => {
		const ids = ensureSelection(messageIds);
		if (!ids) return;
		if (!deleteMessage) {
			toast.error(t$2("message.delete.failed"));
			return;
		}
		if (!await popup.confirm({
			title: t$2("message.delete.confirm.title"),
			content: t$2("message.delete.confirm.content", { count: ids.length }),
			okButtonProps: { danger: true },
			centered: true
		})) return;
		try {
			for (const messageId of ids) await deleteMessage(messageId, { selectedMessageIds: ids });
			toast.success(t$2("message.delete.success"));
		} catch (error) {
			logger$4.error("Failed to delete selected messages:", error);
			toast.error(t$2("message.delete.failed"));
		} finally {
			toggleMultiSelectMode(false);
		}
	}, [
		deleteMessage,
		ensureSelection,
		t$2,
		toggleMultiSelectMode
	]);
	const selection = (0, import_react.useMemo)(() => ({
		enabled: true,
		isMultiSelectMode: isMultiSelectMode ?? false,
		selectedMessageIds: selectedIds
	}), [isMultiSelectMode, selectedIds]);
	const actions = (0, import_react.useMemo)(() => ({
		selectMessage,
		toggleMultiSelectMode,
		copySelectedMessages,
		saveSelectedMessages: saveTextFile ? saveSelectedMessages : void 0,
		deleteSelectedMessages: deleteMessage ? deleteSelectedMessages : void 0
	}), [
		copySelectedMessages,
		deleteMessage,
		deleteSelectedMessages,
		saveSelectedMessages,
		saveTextFile,
		selectMessage,
		toggleMultiSelectMode
	]);
	return (0, import_react.useMemo)(() => ({
		selection,
		actions
	}), [actions, selection]);
}
function useMessageUiStateCache() {
	const getMessageUiState = (0, import_react.useCallback)((messageId) => getCachedMessageUiState(messageId), []);
	const updateMessageUiState = (0, import_react.useCallback)((messageId, updates) => {
		updateCachedMessageUiState(messageId, updates);
	}, []);
	return (0, import_react.useMemo)(() => ({
		getMessageUiState,
		updateMessageUiState
	}), [getMessageUiState, updateMessageUiState]);
}
function useMessageListAdapterCapabilities({ topicId, topicName, messages, partsByMessageId, streamingLayers, deleteMessage, persistDiagnosis }) {
	const getMessageActivityState = useMessageActivityState(topicId, partsByMessageId);
	const { renderConfig, updateRenderConfig } = useMessageListRenderConfig();
	const menuConfig = useMessageMenuConfig();
	const exportActions = useMessageExportActions({ topicName });
	const leafCapabilities = useMessageLeafCapabilities({
		partsByMessageId,
		streamingLayers
	});
	const headerCapabilities = useMessageHeaderCapabilities();
	const messageUiStateCache = useMessageUiStateCache();
	return {
		errorActions: useMessageErrorActions({ persistDiagnosis }),
		exportActions,
		getMessageActivityState,
		headerCapabilities,
		leafCapabilities,
		menuConfig,
		messageUiStateCache,
		renderConfig,
		selectionController: useMessageSelectionController({
			topicId,
			messages,
			partsByMessageId,
			deleteMessage,
			saveTextFile: exportActions.saveTextFile,
			copyRichContent: leafCapabilities.copyRichContent
		}),
		updateRenderConfig
	};
}
function pickMessageLeafState(capabilities) {
	const state = {};
	if (capabilities.getFileView) state.getFileView = capabilities.getFileView;
	if (capabilities.isToolAutoApproved) state.isToolAutoApproved = capabilities.isToolAutoApproved;
	if (capabilities.externalCodeEditors !== void 0) state.externalCodeEditors = capabilities.externalCodeEditors;
	return state;
}
function pickMessageLeafActions(capabilities) {
	const actions = {};
	if (capabilities.previewFile) actions.previewFile = capabilities.previewFile;
	if (capabilities.openFile) actions.openFile = capabilities.openFile;
	if (capabilities.subscribeToolProgress) actions.subscribeToolProgress = capabilities.subscribeToolProgress;
	if (capabilities.openExternalUrl) actions.openExternalUrl = capabilities.openExternalUrl;
	if (capabilities.openInExternalApp) actions.openInExternalApp = capabilities.openInExternalApp;
	if (capabilities.copyText) actions.copyText = capabilities.copyText;
	if (capabilities.copyRichContent) actions.copyRichContent = capabilities.copyRichContent;
	if (capabilities.copyImage) actions.copyImage = capabilities.copyImage;
	if (capabilities.exportTableAsExcel) actions.exportTableAsExcel = capabilities.exportTableAsExcel;
	if (capabilities.notifyInfo) actions.notifyInfo = capabilities.notifyInfo;
	if (capabilities.notifySuccess) actions.notifySuccess = capabilities.notifySuccess;
	if (capabilities.notifyWarning) actions.notifyWarning = capabilities.notifyWarning;
	if (capabilities.notifyError) actions.notifyError = capabilities.notifyError;
	return actions;
}
function pickMessageHeaderActions(capabilities) {
	const actions = {};
	if (capabilities.openUserProfile) actions.openUserProfile = capabilities.openUserProfile;
	return actions;
}
var LOCATE_SUBSCRIBER_WAIT_FRAMES = 120;
function dispatchLocateMessage(runtime, messageId, highlight) {
	const eventName = EVENT_NAMES.LOCATE_MESSAGE + ":" + messageId;
	if (!runtime) {
		EventEmitter.emit(eventName, highlight);
		return;
	}
	runtime.locateMessage(messageId);
	let remainingFrames = LOCATE_SUBSCRIBER_WAIT_FRAMES;
	const deliver = () => {
		if (EventEmitter.listenerCount(eventName) > 0) {
			EventEmitter.emit(eventName, highlight);
			return;
		}
		remainingFrames -= 1;
		if (remainingFrames <= 0) return;
		requestAnimationFrame(deliver);
	};
	requestAnimationFrame(deliver);
}
var MESSAGE_PART_ID_PATTERN = /^(.+)-(?:part|block)-(\d+)$/;
function parseMessagePartId(partId) {
	const match = partId.match(MESSAGE_PART_ID_PATTERN);
	if (!match) return null;
	return {
		messageId: match[1],
		partIndex: Number.parseInt(match[2], 10)
	};
}
function withMessagePartDiagnosis(parts, partIndex, diagnosis) {
	if (partIndex < 0 || partIndex >= parts.length) return null;
	const target = parts[partIndex];
	const updatedPart = withCherryMeta(target, { diagnosis });
	return parts.map((part, index) => index === partIndex ? updatedPart : part);
}
function runMessageImageAction(runtime, type) {
	if (type === "copy") return runtime.copyTopicImage();
	return runtime.exportTopicImage();
}
function flushPendingMessageImageActions({ consumePendingActions, runtime, settleActionRequest, targetId }) {
	const requests = consumePendingActions(targetId);
	for (const request of requests) settleActionRequest(request, runMessageImageAction(runtime, request.type));
}
function bindCaptureMessageImageRuntime({ cancelMessage, rejectPendingActions, ...flushOptions }) {
	flushPendingMessageImageActions(flushOptions);
	return () => rejectPendingActions(flushOptions.targetId, new Error(cancelMessage));
}
function createImageActionBus({ getTargetId, onRequest, targetKey }) {
	let nextRequestId = 1;
	let pendingRequests = [];
	const settlements = /* @__PURE__ */ new Map();
	const getRequestTarget = (request) => request[targetKey];
	function requestImageAction(type, target, options) {
		let settlement;
		const promise = new Promise((resolve, reject) => {
			settlement = {
				resolve,
				reject
			};
		});
		const request = {
			id: nextRequestId++,
			promise,
			type,
			[targetKey]: target
		};
		settlements.set(request.id, settlement);
		pendingRequests.push(request);
		onRequest?.(type, target, options);
		return request;
	}
	function settleImageActionRequest(request, actionPromise) {
		const settlement = settlements.get(request.id);
		if (!settlement) return;
		settlements.delete(request.id);
		Promise.resolve(actionPromise).then(settlement.resolve, settlement.reject);
	}
	function consumePendingImageActions(targetId, type) {
		const matches = [];
		const remaining = [];
		for (const request of pendingRequests) if (getTargetId(getRequestTarget(request)) === targetId && (!type || request.type === type)) matches.push(request);
		else remaining.push(request);
		pendingRequests = remaining;
		return matches;
	}
	function rejectPendingImageActions(targetId, reason) {
		const remaining = [];
		for (const request of pendingRequests) if (targetId === void 0 || getTargetId(getRequestTarget(request)) === targetId) {
			const settlement = settlements.get(request.id);
			settlements.delete(request.id);
			settlement?.reject(reason);
		} else remaining.push(request);
		pendingRequests = remaining;
	}
	function clearPendingImageActionsForTest() {
		pendingRequests = [];
		settlements.clear();
		nextRequestId = 1;
	}
	return {
		clearPendingImageActionsForTest,
		consumePendingImageActions,
		rejectPendingImageActions,
		requestImageAction,
		settleImageActionRequest
	};
}
function stringArraysEqual(previous, next) {
	if (previous === next) return true;
	if (previous.length !== next.length) return false;
	return previous.every((value, index) => value === next[index]);
}
function useStableStringArray(values) {
	const stableRef = (0, import_react.useRef)(values);
	if (!stringArraysEqual(stableRef.current, values)) stableRef.current = values;
	return stableRef.current;
}
function partsContentEqual(a$1, b$1) {
	if (a$1 === b$1) return true;
	if (a$1.length !== b$1.length) return false;
	for (let i$1 = 0; i$1 < a$1.length; i$1++) {
		const previousPart = a$1[i$1];
		const nextPart = b$1[i$1];
		if (previousPart === nextPart) continue;
		if (previousPart.type !== "data-translation" || nextPart.type !== "data-translation") return false;
		if (previousPart.id !== nextPart.id || previousPart.data.content !== nextPart.data.content || previousPart.data.targetLanguage !== nextPart.data.targetLanguage || previousPart.data.sourceLanguage !== nextPart.data.sourceLanguage || previousPart.data.sourceBlockId !== nextPart.data.sourceBlockId) return false;
	}
	return true;
}
function appendTranslation(parts, trEntry) {
	if (!trEntry) return parts;
	const filtered = parts.filter((part) => part.type !== "data-translation");
	const translationPart = {
		type: "data-translation",
		data: {
			content: trEntry.content,
			targetLanguage: trEntry.targetLanguage,
			...trEntry.sourceLanguage && { sourceLanguage: trEntry.sourceLanguage }
		}
	};
	return [...filtered, translationPart];
}
function useStableMessagePartsLayers(messages, overlay, translationOverlay) {
	const cacheRef = (0, import_react.useRef)({
		messageCount: 0,
		value: {
			historyPartsByMessageId: {},
			partsByMessageId: {}
		}
	});
	return (0, import_react.useMemo)(() => {
		const previous = cacheRef.current;
		const previousHistory = previous.value.historyPartsByMessageId;
		const previousCurrent = previous.value.partsByMessageId;
		const nextHistory = {};
		const nextCurrent = {};
		let historyChanged = previous.messageCount !== messages.length;
		let currentChanged = previous.messageCount !== messages.length;
		let hasExecutionOverlay = false;
		for (const message of messages) {
			const baseParts = message.parts ?? [];
			const translation = translationOverlay[message.id];
			const historyCandidate = appendTranslation(baseParts, translation);
			const previousHistoryParts = previousHistory[message.id];
			const historyParts = previousHistoryParts && partsContentEqual(previousHistoryParts, historyCandidate) ? previousHistoryParts : historyCandidate;
			nextHistory[message.id] = historyParts;
			historyChanged ||= historyParts !== previousHistoryParts;
			const executionParts = overlay[message.id];
			const usesExecutionOverlay = executionParts !== void 0 && executionParts.length > 0;
			hasExecutionOverlay ||= usesExecutionOverlay;
			const currentCandidate = usesExecutionOverlay ? appendTranslation(executionParts, translation) : historyParts;
			const previousCurrentParts = previousCurrent[message.id];
			const currentParts = previousCurrentParts && partsContentEqual(previousCurrentParts, currentCandidate) ? previousCurrentParts : currentCandidate;
			nextCurrent[message.id] = currentParts;
			currentChanged ||= currentParts !== previousCurrentParts;
		}
		for (const [messageId, executionParts] of Object.entries(overlay)) {
			if (messageId in nextCurrent || executionParts.length === 0) continue;
			hasExecutionOverlay = true;
			const currentCandidate = appendTranslation(executionParts, translationOverlay[messageId]);
			const previousCurrentParts = previousCurrent[messageId];
			const currentParts = previousCurrentParts && partsContentEqual(previousCurrentParts, currentCandidate) ? previousCurrentParts : currentCandidate;
			nextCurrent[messageId] = currentParts;
			currentChanged ||= currentParts !== previousCurrentParts;
		}
		if (!currentChanged) {
			for (const messageId in previousCurrent) if (!(messageId in nextCurrent)) {
				currentChanged = true;
				break;
			}
		}
		const historyPartsByMessageId = historyChanged ? nextHistory : previousHistory;
		let partsByMessageId = previousCurrent;
		if (!hasExecutionOverlay) partsByMessageId = historyPartsByMessageId;
		else if (currentChanged) partsByMessageId = nextCurrent;
		if (previous.messageCount === messages.length && historyPartsByMessageId === previousHistory && partsByMessageId === previousCurrent) return previous.value;
		const value = {
			historyPartsByMessageId,
			partsByMessageId
		};
		cacheRef.current = {
			messageCount: messages.length,
			value
		};
		return value;
	}, [
		messages,
		overlay,
		translationOverlay
	]);
}
var EMPTY_TRANSLATION_OVERLAY = {};
function createOverlayRefreshHandoff(refresh, resetOverlay) {
	return async () => {
		try {
			await refresh();
		} finally {
			resetOverlay();
		}
	};
}
function useMessageStreamingLayers({ messages, overlay, executions, liveAssistants, translationOverlay = EMPTY_TRANSLATION_OVERLAY }) {
	const { historyPartsByMessageId, partsByMessageId } = useStableMessagePartsLayers(messages, overlay, translationOverlay);
	const liveMessageIds = useStableStringArray((0, import_react.useMemo)(() => Array.from(new Set([...executions.flatMap((execution) => execution.anchorMessageId ? [execution.anchorMessageId] : []), ...liveAssistants.map((message) => message.id)])), [executions, liveAssistants]));
	return {
		partsByMessageId,
		liveMessageIds,
		streamingLayers: (0, import_react.useMemo)(() => ({
			historyPartsByMessageId,
			liveMessageIds
		}), [historyPartsByMessageId, liveMessageIds])
	};
}
var logger$3 = loggerService.withContext("AskUserQuestionComposer");
function createAskUserQuestionComposerOverride({ request, onRespond }) {
	return {
		id: `ask-user-question:${request.approvalId}`,
		priority: 100,
		render: ({ className }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AskUserQuestionComposer, {
			request,
			onRespond,
			className
		})
	};
}
function AskUserQuestionComposer({ request, onRespond, className }) {
	const { t: t$2 } = useTranslation();
	const questions = request.input.questions;
	const [currentIndex, setCurrentIndex] = (0, import_react.useState)(0);
	const [selectedAnswers, setSelectedAnswers] = (0, import_react.useState)({});
	const [customAnswers, setCustomAnswers] = (0, import_react.useState)({});
	const [isSubmitting, setIsSubmitting] = (0, import_react.useState)(false);
	const currentQuestion = questions[currentIndex];
	const totalQuestions = questions.length;
	const isFirstQuestion = currentIndex === 0;
	const isLastQuestion = currentIndex === totalQuestions - 1;
	const currentCustomAnswer = customAnswers[currentIndex] ?? "";
	const currentCustomAnswerText = currentCustomAnswer.trim();
	const hasAnswerAt = (0, import_react.useCallback)((index, answersByIndex = selectedAnswers) => {
		return (answersByIndex[index] ?? []).length > 0;
	}, [selectedAnswers]);
	const hasAnyAnswer = (0, import_react.useCallback)((answersByIndex = selectedAnswers) => questions.some((_$1, index) => hasAnswerAt(index, answersByIndex)), [
		hasAnswerAt,
		questions,
		selectedAnswers
	]);
	const selectedForCurrent = selectedAnswers[currentIndex] ?? [];
	const hasAnySelectedAnswer = (0, import_react.useMemo)(() => hasAnyAnswer(selectedAnswers), [hasAnyAnswer, selectedAnswers]);
	const customActionSubmitsAll = isLastQuestion && (hasAnySelectedAnswer || !!currentCustomAnswerText);
	const buildAnswers = (0, import_react.useCallback)((answersByIndex = selectedAnswers) => {
		const answers = {};
		questions.forEach((question, index) => {
			const values = answersByIndex[index] ?? [];
			if (values.length > 0) answers[question.question] = values.join(", ");
		});
		return answers;
	}, [questions, selectedAnswers]);
	const respond = (0, import_react.useCallback)(async (input) => {
		setIsSubmitting(true);
		try {
			await onRespond(input);
		} catch (error) {
			logger$3.error("Failed to send ask-user-question response", error, {
				approvalId: request.approvalId,
				messageId: request.messageId,
				toolCallId: request.toolCallId
			});
			toast.error(t$2("agent.toolPermission.error.sendFailed"));
			setIsSubmitting(false);
		}
	}, [
		onRespond,
		request.approvalId,
		request.messageId,
		request.toolCallId,
		t$2
	]);
	const submitAnswers = (0, import_react.useCallback)(async (answersByIndex = selectedAnswers) => {
		if (!hasAnyAnswer(answersByIndex) || isSubmitting) return;
		await respond({
			match: request.match,
			approved: true,
			updatedInput: {
				...request.input,
				answers: buildAnswers(answersByIndex)
			}
		});
	}, [
		buildAnswers,
		hasAnyAnswer,
		isSubmitting,
		request.input,
		request.match,
		respond,
		selectedAnswers
	]);
	const handleDismiss = (0, import_react.useCallback)(async () => {
		if (isSubmitting) return;
		await respond({
			match: request.match,
			approved: false,
			reason: "User dismissed AskUserQuestion"
		});
	}, [
		isSubmitting,
		request.match,
		respond
	]);
	const completeCurrentQuestion = (0, import_react.useCallback)((answersByIndex) => {
		if (isLastQuestion) {
			submitAnswers(answersByIndex);
			return;
		}
		setCurrentIndex((index) => Math.min(totalQuestions - 1, index + 1));
	}, [
		isLastQuestion,
		submitAnswers,
		totalQuestions
	]);
	const handleSelectOption = (0, import_react.useCallback)((label) => {
		const isMultiSelect = currentQuestion?.multiSelect;
		if (!currentQuestion || isSubmitting) return;
		const current = selectedAnswers[currentIndex] ?? [];
		const nextForCurrent = isMultiSelect ? current.includes(label) ? current.filter((value) => value !== label) : [...current, label] : [label];
		const nextSelectedAnswers = {
			...selectedAnswers,
			[currentIndex]: nextForCurrent
		};
		setSelectedAnswers(nextSelectedAnswers);
		if (!isMultiSelect) completeCurrentQuestion(nextSelectedAnswers);
	}, [
		completeCurrentQuestion,
		currentIndex,
		currentQuestion,
		isSubmitting,
		selectedAnswers
	]);
	const handleCustomAction = (0, import_react.useCallback)(async () => {
		if (isSubmitting) return;
		if (currentCustomAnswerText) {
			const nextSelectedAnswers = {
				...selectedAnswers,
				[currentIndex]: [currentCustomAnswerText]
			};
			setSelectedAnswers(nextSelectedAnswers);
			completeCurrentQuestion(nextSelectedAnswers);
			return;
		}
		if (customActionSubmitsAll) {
			await submitAnswers(selectedAnswers);
			return;
		}
		if (!isLastQuestion) setCurrentIndex((index) => index + 1);
	}, [
		completeCurrentQuestion,
		currentCustomAnswerText,
		currentIndex,
		customActionSubmitsAll,
		isLastQuestion,
		isSubmitting,
		selectedAnswers,
		submitAnswers
	]);
	if (!currentQuestion) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.ask-user-question-composer",
		"data-composer-viewport-inset-target": "",
		className: cn("pointer-events-auto relative z-2 flex flex-col px-4.5 pt-0 pb-4.5", className),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "rounded-[17px] border-[0.5px] border-border p-2.5 backdrop-blur",
			style: { backgroundColor: "color-mix(in srgb, var(--background) 88%, transparent)" },
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex items-center justify-between gap-3 px-1",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", {
						className: "line-clamp-1 min-w-0 flex-1 font-semibold text-foreground text-sm leading-5",
						children: currentQuestion.question
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex shrink-0 items-center gap-0.5 text-muted-foreground",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
								type: "button",
								variant: "ghost",
								size: "icon-sm",
								className: "size-7 shadow-none",
								"aria-label": t$2("agent.askUserQuestion.previous"),
								disabled: isFirstQuestion || isSubmitting,
								onClick: () => setCurrentIndex((index) => Math.max(0, index - 1)),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronLeft, { className: "size-4" })
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "min-w-11 text-center text-xs",
								children: t$2("agent.askUserQuestion.progress", {
									current: currentIndex + 1,
									total: totalQuestions
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
								type: "button",
								variant: "ghost",
								size: "icon-sm",
								className: "size-7 shadow-none",
								"aria-label": isLastQuestion ? t$2("agent.askUserQuestion.submit") : t$2("agent.askUserQuestion.next"),
								disabled: isLastQuestion && !hasAnySelectedAnswer || isSubmitting,
								onClick: isLastQuestion ? () => void submitAnswers() : () => setCurrentIndex((index) => Math.min(totalQuestions - 1, index + 1)),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRight, { className: "size-4" })
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
								type: "button",
								variant: "ghost",
								size: "icon-sm",
								className: "size-7 shadow-none",
								"aria-label": t$2("agent.askUserQuestion.close"),
								disabled: isSubmitting,
								onClick: handleDismiss,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { className: "size-4" })
							})
						]
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "mt-2 flex flex-col gap-1.5",
					children: currentQuestion.options.map((option, optionIndex) => {
						const isSelected = selectedForCurrent.includes(option.label);
						return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
							type: "button",
							variant: "ghost",
							className: cn("group h-auto min-h-11 w-full justify-start gap-3 whitespace-normal rounded-[12px] px-3 py-2 text-left shadow-none", "hover:bg-muted focus-visible:bg-muted", isSelected && "bg-muted"),
							disabled: isSubmitting,
							"aria-pressed": isSelected,
							onClick: () => handleSelectOption(option.label),
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: cn("flex size-8 shrink-0 items-center justify-center rounded-full font-semibold text-sm transition-colors", isSelected ? "bg-foreground text-background" : "bg-muted text-muted-foreground group-hover:bg-foreground group-hover:text-background"),
									children: optionIndex + 1
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
									className: "min-w-0 flex-1",
									children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
										className: "block truncate font-semibold text-foreground text-sm leading-5",
										children: option.label
									}), option.description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
										className: "block truncate font-medium text-muted-foreground text-xs leading-4",
										children: option.description
									})]
								}),
								currentQuestion.multiSelect ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Checkbox, {
									checked: isSelected,
									size: "sm",
									"aria-hidden": "true",
									tabIndex: -1,
									className: "pointer-events-none"
								}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrowRight, { className: cn("size-4 shrink-0 text-muted-foreground transition-opacity", isSelected ? "opacity-100" : "opacity-0 group-hover:opacity-100") })
							]
						}, `${option.label}-${optionIndex}`);
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "mt-2 flex items-center gap-2 border-border-subtle border-t pt-2",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "relative min-w-0 flex-1",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Pencil, { className: "-translate-y-1/2 absolute top-1/2 left-3 size-3.5 text-muted-foreground" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
							value: currentCustomAnswer,
							disabled: isSubmitting,
							placeholder: t$2("agent.askUserQuestion.customPlaceholder"),
							className: "h-9 rounded-full border-transparent bg-muted/70 pl-9 text-sm shadow-none focus-visible:border-transparent",
							onChange: (event) => setCustomAnswers((prev) => ({
								...prev,
								[currentIndex]: event.target.value
							})),
							onKeyDown: (event) => {
								if (event.key === "Enter" && !event.nativeEvent.isComposing) {
									event.preventDefault();
									handleCustomAction();
								}
							}
						})]
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						variant: "ghost",
						className: "h-9 px-2.5 font-semibold text-muted-foreground text-sm shadow-none hover:bg-transparent hover:text-foreground",
						loading: customActionSubmitsAll && isSubmitting,
						disabled: isSubmitting,
						onClick: handleCustomAction,
						children: currentCustomAnswerText || customActionSubmitsAll ? t$2("agent.askUserQuestion.submit") : t$2("agent.askUserQuestion.skip")
					})]
				})
			]
		})
	});
}
function getToolName$1(part) {
	if (part.toolName?.trim()) return part.toolName;
	if (part.type.startsWith("tool-")) return part.type.replace(/^tool-/, "");
	return "";
}
function findLatestPendingAskUserQuestionRequest(partsByMessageId) {
	let latest = null;
	for (const [messageId, parts] of Object.entries(partsByMessageId)) for (const part of parts) {
		if (!isToolUIPart(part)) continue;
		const toolPart = part;
		const approvalId = toolPart.approval?.id;
		if (!isAskUserQuestionToolName(getToolName$1(toolPart)) || toolPart.state !== "approval-requested" || !approvalId) continue;
		const input = parseAskUserQuestionToolInput(toolPart.input);
		if (!input?.questions.length) continue;
		latest = {
			messageId,
			toolCallId: toolPart.toolCallId,
			approvalId,
			input,
			match: {
				part,
				state: toolPart.state,
				toolCallId: toolPart.toolCallId,
				messageId,
				approvalId,
				input: toolPart.input
			}
		};
	}
	return latest;
}
function getToolName(part) {
	if (part.toolName?.trim()) return part.toolName;
	if (part.type.startsWith("tool-")) return part.type.replace(/^tool-/, "");
	return "";
}
function getToolDisplayName(toolResponse) {
	return toolResponse.tool.name;
}
function getStringField(value, fields) {
	if (!value || typeof value !== "object" || Array.isArray(value)) return void 0;
	const record = value;
	for (const field of fields) {
		const candidate = record[field];
		if (typeof candidate === "string" && candidate.trim()) return candidate.trim();
	}
}
function getPermissionTitle(part, fallback) {
	return getStringField(part.input, [
		"question",
		"message",
		"prompt",
		"title",
		"description"
	]) ?? fallback;
}
function findLatestPendingPermissionRequest(partsByMessageId) {
	let latest = null;
	for (const [messageId, parts] of Object.entries(partsByMessageId)) for (const part of parts) {
		if (!isToolUIPart(part)) continue;
		const toolPart = part;
		const toolName = getToolName(toolPart);
		const approvalId = toolPart.approval?.id;
		if (toolName === AgentToolsType.AskUserQuestion || toolPart.state !== "approval-requested" || !approvalId) continue;
		const toolResponse = buildToolResponseFromPart(part);
		if (!toolResponse) continue;
		latest = {
			messageId,
			toolCallId: toolPart.toolCallId,
			approvalId,
			title: getPermissionTitle(toolPart, getToolDisplayName(toolResponse)),
			toolResponse,
			match: {
				part,
				state: toolPart.state,
				toolCallId: toolPart.toolCallId,
				messageId,
				approvalId,
				input: toolPart.input
			}
		};
	}
	return latest;
}
var logger$2 = loggerService.withContext("PermissionRequestComposer");
function isMcpToolResponse(toolResponse) {
	return toolResponse.tool.type === "mcp";
}
function normalizeArgs(args) {
	if (args === void 0 || args === null) return null;
	if (typeof args === "object") return args;
	return { value: args };
}
var BUILTIN_TOOLS_WITH_OWN_PREVIEW_SCROLL = new Set([
	AgentToolsType.Bash,
	AgentToolsType.BashOutput,
	AgentToolsType.Glob,
	AgentToolsType.Grep,
	AgentToolsType.Read,
	AgentToolsType.Skill,
	AgentToolsType.Write
]);
function renderBuiltinPreviewChildren(toolName, children) {
	if (children === void 0 || children === null || BUILTIN_TOOLS_WITH_OWN_PREVIEW_SCROLL.has(toolName)) return children;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Scrollbar_default, {
		className: "max-h-60 overflow-x-hidden",
		"data-testid": "permission-builtin-body-scroll",
		children
	});
}
function createPermissionRequestComposerOverride({ request, onRespond }) {
	return {
		id: `tool-permission:${request.approvalId}`,
		priority: 90,
		render: ({ className }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PermissionRequestComposer, {
			request,
			onRespond,
			className
		})
	};
}
function BuiltinPermissionPreview({ toolResponse }) {
	const toolName = toolResponse.tool.name;
	const input = toolResponse.arguments;
	const renderedItem = isValidAgentToolsType(toolName) ? renderTool(toolName, input) : UnknownToolRenderer({
		toolName,
		input
	});
	const item = {
		...renderedItem,
		label: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PermissionPreviewHeader, { toolName }),
		children: renderBuiltinPreviewChildren(toolName, renderedItem.children),
		classNames: {
			...renderedItem.classNames,
			header: cn("px-3 py-2", renderedItem.classNames?.header),
			body: cn("max-h-none overflow-visible bg-transparent p-2 text-foreground", renderedItem.classNames?.body)
		}
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolDisclosure, {
		className: "w-full",
		variant: "light",
		defaultActiveKey: [String(renderedItem.key ?? toolName)],
		items: [item]
	});
}
function McpPermissionPreview({ toolResponse }) {
	const { t: t$2 } = useTranslation();
	const args = normalizeArgs(toolResponse.arguments);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.mcp-permission-preview",
		className: "px-3 py-2",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PermissionPreviewHeader, {
			toolName: toolResponse.tool.name,
			description: toolResponse.tool.description
		}), args ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Scrollbar_default, {
			className: "max-h-60 overflow-x-hidden",
			"data-testid": "permission-mcp-args-scroll",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolArgsTable, {
				args,
				title: t$2("message.tools.sections.input")
			})
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "py-2 text-muted-foreground text-xs",
			children: t$2("message.tools.noData")
		})]
	});
}
function PermissionPreview({ toolResponse }) {
	if (isMcpToolResponse(toolResponse)) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(McpPermissionPreview, { toolResponse });
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BuiltinPermissionPreview, { toolResponse });
}
function getPermissionRequestSubtitle(request) {
	const title = request.title.trim();
	const toolName = request.toolResponse.tool.name.trim();
	if (!title || title === toolName) return null;
	return title;
}
function PermissionPreviewHeader({ toolName, description }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "chat.permission-preview-header",
		className: "min-w-0 text-foreground text-sm",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "truncate font-medium",
			children: toolName
		}), description ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "mt-0.5 line-clamp-2 text-muted-foreground text-xs leading-4",
			children: description
		}) : null]
	});
}
function PermissionOption({ index, label, ariaLabel, destructive, disabled, onSelect }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
		type: "button",
		variant: "ghost",
		className: cn("group h-auto min-h-11 w-full justify-start gap-3 whitespace-normal rounded-[12px] px-3 py-2 text-left shadow-none", "hover:bg-muted focus-visible:bg-muted"),
		disabled,
		"aria-label": ariaLabel,
		onClick: onSelect,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: cn("flex size-8 shrink-0 items-center justify-center rounded-full font-semibold text-sm transition-colors", "bg-muted text-muted-foreground group-hover:bg-foreground group-hover:text-background"),
				children: index
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: cn("block min-w-0 flex-1 truncate font-semibold text-foreground text-sm leading-5", destructive && "text-destructive"),
				children: label
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrowRight, { className: cn("size-4 shrink-0 text-muted-foreground transition-opacity", "opacity-0 group-hover:opacity-100") })
		]
	});
}
function PermissionRequestComposer({ request, onRespond, className }) {
	const { t: t$2 } = useTranslation();
	const [isSubmitting, setIsSubmitting] = (0, import_react.useState)(false);
	const subtitle = getPermissionRequestSubtitle(request);
	const ToolIcon = getToolGroupIcon(request.toolResponse.tool, request.toolResponse.arguments);
	const toolTitle = getToolGroupSemanticTitle(request.toolResponse, "waiting", t$2);
	const respond = (0, import_react.useCallback)(async (input, action) => {
		setIsSubmitting(true);
		try {
			await onRespond(input);
		} catch (error) {
			logger$2.error("Failed to send permission response", error, {
				action,
				approvalId: request.approvalId
			});
			toast.error(t$2("agent.toolPermission.error.sendFailed"));
			setIsSubmitting(false);
		}
	}, [
		onRespond,
		request.approvalId,
		t$2
	]);
	const approve = (0, import_react.useCallback)(async () => {
		if (isSubmitting) return;
		await respond({
			match: request.match,
			approved: true
		}, "approve");
	}, [
		isSubmitting,
		request.match,
		respond
	]);
	const deny = (0, import_react.useCallback)(async () => {
		if (isSubmitting) return;
		await respond({
			match: request.match,
			approved: false,
			reason: t$2("agent.toolPermission.defaultDenyMessage")
		}, "deny");
	}, [
		isSubmitting,
		request.match,
		respond,
		t$2
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.permission-request-composer",
		"data-composer-viewport-inset-target": "",
		className: cn("pointer-events-auto relative z-2 flex flex-col px-4.5 pt-0 pb-4.5", className),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "rounded-[17px] border-[0.5px] border-border p-2.5 shadow-[0_1px_5px_rgba(15,23,42,0.05)] backdrop-blur dark:shadow-[0_1px_5px_rgba(0,0,0,0.14)]",
			style: { backgroundColor: "color-mix(in srgb, var(--background) 88%, transparent)" },
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex items-center justify-between gap-3 px-1",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "min-w-0 flex-1",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("h2", {
							className: "line-clamp-1 flex min-w-0 items-center gap-2 font-semibold text-foreground text-sm leading-5",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "inline-flex shrink-0 text-muted-foreground",
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolIcon, {
									"aria-hidden": "true",
									className: "size-4"
								})
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "truncate",
								children: toolTitle
							})]
						}), subtitle ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "mt-0.5 line-clamp-1 text-muted-foreground text-xs leading-4",
							children: subtitle
						}) : null]
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "rounded-full border border-warning-border bg-warning-subtle px-2 py-1 font-medium text-[11px] text-warning-subtle-foreground",
						children: t$2("agent.toolPermission.pending")
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					"data-ui": "chat.permission-request-composer.permission-preview",
					className: "mt-2 overflow-hidden rounded-[12px] bg-muted dark:bg-muted/30",
					"data-testid": "permission-preview",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PermissionPreview, { toolResponse: request.toolResponse })
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "mt-2 flex flex-col gap-1.5",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PermissionOption, {
						index: 1,
						label: t$2("agent.toolPermission.button.allow"),
						ariaLabel: t$2("agent.toolPermission.button.allow"),
						disabled: isSubmitting,
						onSelect: () => void approve()
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PermissionOption, {
						index: 2,
						label: t$2("agent.toolPermission.button.deny"),
						ariaLabel: t$2("agent.toolPermission.button.deny"),
						destructive: true,
						disabled: isSubmitting,
						onSelect: () => void deny()
					})]
				})
			]
		})
	});
}
function useToolApprovalComposerOverrides({ partsByMessageId, streamingLayers, onRespond }) {
	const [dismissedApprovalIds, setDismissedApprovalIds] = (0, import_react.useState)(() => /* @__PURE__ */ new Set());
	const settledHistoryParts = (0, import_react.useMemo)(() => {
		if (!streamingLayers) return null;
		const liveMessageIdSet = new Set(streamingLayers.liveMessageIds);
		const historyParts = {};
		for (const [messageId, parts] of Object.entries(streamingLayers.historyPartsByMessageId)) if (!liveMessageIdSet.has(messageId)) historyParts[messageId] = parts;
		return historyParts;
	}, [streamingLayers]);
	const currentParts = (0, import_react.useMemo)(() => {
		if (!streamingLayers) return partsByMessageId;
		const liveParts = {};
		for (const messageId of streamingLayers.liveMessageIds) {
			const parts = partsByMessageId[messageId];
			if (parts) liveParts[messageId] = parts;
		}
		return liveParts;
	}, [streamingLayers, partsByMessageId]);
	const historyAskUserQuestionRequest = (0, import_react.useMemo)(() => settledHistoryParts ? findLatestPendingAskUserQuestionRequest(settledHistoryParts) : null, [settledHistoryParts]);
	const askUserQuestionRequest = (0, import_react.useMemo)(() => findLatestPendingAskUserQuestionRequest(currentParts), [currentParts]) ?? historyAskUserQuestionRequest;
	const historyPermissionRequest = (0, import_react.useMemo)(() => settledHistoryParts ? findLatestPendingPermissionRequest(settledHistoryParts) : null, [settledHistoryParts]);
	const permissionRequest = (0, import_react.useMemo)(() => findLatestPendingPermissionRequest(currentParts), [currentParts]) ?? historyPermissionRequest;
	const visiblePermissionRequest = permissionRequest && !dismissedApprovalIds.has(permissionRequest.approvalId) ? permissionRequest : null;
	const optimisticallyRespond = (0, import_react.useCallback)(async (input) => {
		const approvalId = input.match.approvalId;
		setDismissedApprovalIds((current) => new Set(current).add(approvalId));
		try {
			await onRespond(input);
		} catch (error) {
			setDismissedApprovalIds((current) => {
				const next = new Set(current);
				next.delete(approvalId);
				return next;
			});
			throw error;
		}
	}, [onRespond]);
	return (0, import_react.useMemo)(() => {
		const overrides = [];
		if (askUserQuestionRequest) overrides.push(createAskUserQuestionComposerOverride({
			request: askUserQuestionRequest,
			onRespond
		}));
		if (visiblePermissionRequest) overrides.push(createPermissionRequestComposerOverride({
			request: visiblePermissionRequest,
			onRespond: optimisticallyRespond
		}));
		return overrides;
	}, [
		askUserQuestionRequest,
		onRespond,
		optimisticallyRespond,
		visiblePermissionRequest
	]);
}
var logger$1 = loggerService.withContext("useChatWithHistory");
var EMPTY_EXECUTIONS = Object.freeze([]);
function useChatWithHistory(topicId, initialMessages, refresh) {
	const enabled = Boolean(topicId);
	const chat = (0, import_react.useMemo)(() => new Chat({
		id: topicId,
		transport: ipcChatTransport,
		messages: initialMessages,
		onError: (streamError) => {
			logger$1.error("AI stream error", {
				topicId,
				streamError
			});
		}
	}), [topicId]);
	const { setMessages, stop: sdkStop, status, error, sendMessage, regenerate, resumeStream } = useChat({
		chat,
		experimental_throttle: 0
	});
	const stop = (0, import_react.useCallback)(async () => {
		if (enabled) ipcApi.request("ai.stream.abort", { topicId }).catch((err) => {
			logger$1.warn("streamAbort failed", {
				topicId,
				err
			});
		});
		await sdkStop();
	}, [
		enabled,
		sdkStop,
		topicId
	]);
	const refreshRef = (0, import_react.useRef)(refresh);
	refreshRef.current = refresh;
	const { status: topicStreamStatus, activeExecutions: liveExecutions } = useTopicStreamStatus(topicId);
	const activeExecutions = liveExecutions.length > 0 ? liveExecutions : EMPTY_EXECUTIONS;
	const topicSelectionToken = (0, import_react.useMemo)(() => Symbol(topicId), [topicId]);
	const currentTopicSelectionTokenRef = (0, import_react.useRef)(topicSelectionToken);
	currentTopicSelectionTokenRef.current = topicSelectionToken;
	const resumeInFlightRef = (0, import_react.useRef)(null);
	const statusRef = (0, import_react.useRef)(status);
	statusRef.current = status;
	const resumeStreamRef = (0, import_react.useRef)(resumeStream);
	resumeStreamRef.current = resumeStream;
	const resumeActiveStream = (0, import_react.useCallback)((reason) => {
		if (!enabled) return;
		if (reason === "mount" && (statusRef.current === "streaming" || statusRef.current === "submitted")) return;
		if (resumeInFlightRef.current?.ownerToken === topicSelectionToken) return;
		const token = Symbol(topicId);
		resumeInFlightRef.current = {
			ownerToken: topicSelectionToken,
			token
		};
		(async () => {
			if (reason === "started-event") try {
				await refreshRef.current();
			} catch (err) {
				logger$1.warn("Failed to refresh messages before resuming stream", {
					topicId,
					err
				});
			}
			if (resumeInFlightRef.current?.token !== token || currentTopicSelectionTokenRef.current !== topicSelectionToken) return;
			if (statusRef.current === "streaming" || statusRef.current === "submitted") return;
			await resumeStreamRef.current();
		})().catch((err) => {
			logger$1.warn("Failed to resume active stream", {
				topicId,
				reason,
				err
			});
		}).finally(() => {
			if (resumeInFlightRef.current?.token === token) resumeInFlightRef.current = null;
		});
	}, [
		enabled,
		topicId,
		topicSelectionToken
	]);
	(0, import_react.useEffect)(() => {
		resumeActiveStream("mount");
	}, [resumeActiveStream]);
	useTopicDbRefreshOnAwaitingApproval(topicId, refresh);
	const prevTopicStatusRef = (0, import_react.useRef)(void 0);
	(0, import_react.useEffect)(() => {
		const previous = prevTopicStatusRef.current;
		const prev = previous?.topicId === topicId ? previous.status : void 0;
		prevTopicStatusRef.current = {
			status: topicStreamStatus,
			topicId
		};
		if (!enabled) return;
		if (topicStreamStatus === "pending" && prev !== "pending") resumeActiveStream("started-event");
	}, [
		enabled,
		resumeActiveStream,
		topicId,
		topicStreamStatus
	]);
	return {
		sendMessage,
		regenerate,
		stop,
		error,
		status,
		setMessages,
		activeExecutions,
		chat
	};
}
var logger = loggerService.withContext("useConversationTurnController");
function useConversationTurnController({ scopeKey, historyAdapter, ensureConversation, buildStreamRequest, refreshMetadata }) {
	const [phase, setPhase] = (0, import_react.useState)("draft");
	const scopeEpochRef = (0, import_react.useRef)(0);
	(0, import_react.useLayoutEffect)(() => {
		scopeEpochRef.current += 1;
	}, [scopeKey]);
	(0, import_react.useEffect)(() => {
		setPhase("draft");
	}, [scopeKey]);
	const send = (0, import_react.useCallback)(async (input) => {
		const scopeEpoch = scopeEpochRef.current;
		const isCurrentScope = () => scopeEpochRef.current === scopeEpoch;
		let conversation = null;
		try {
			setPhase("persisting");
			conversation = await ensureConversation(input);
			if (!conversation) {
				if (isCurrentScope()) setPhase("draft");
				return null;
			}
			if (isCurrentScope()) setPhase("opening");
			const ack = await ipcApi.request("ai.stream.open", buildStreamRequest(input, conversation));
			if (ack.mode === "blocked") {
				toast.error(getStreamBlockedMessage(ack));
				if (isCurrentScope()) setPhase("ready");
				Promise.resolve(refreshMetadata?.(conversation, ack)).catch((err) => {
					logger.warn("Failed to refresh conversation metadata after blocked turn", err);
				});
				return ack;
			}
			const reservedMessages = ack.reservedMessages ?? [];
			if (reservedMessages.length > 0) await historyAdapter.seedReservedMessages(reservedMessages);
			if (isCurrentScope()) setPhase("streaming");
			Promise.resolve(refreshMetadata?.(conversation, ack)).catch((err) => {
				logger.warn("Failed to refresh conversation metadata after stream open", err);
			});
			return ack;
		} catch (err) {
			try {
				await historyAdapter.rollback();
			} catch (rollbackErr) {
				logger.warn("Failed to rollback conversation history after stream open failure", rollbackErr);
			}
			if (isCurrentScope()) setPhase("draft");
			throw err;
		}
	}, [
		buildStreamRequest,
		ensureConversation,
		historyAdapter,
		refreshMetadata
	]);
	return {
		phase,
		layout: phase === "draft" ? "draft" : "docked",
		send
	};
}
function mergeMessagesById(...sources) {
	const order = [];
	const byId = /* @__PURE__ */ new Map();
	for (const messages of sources) for (const message of messages) {
		const existing = byId.get(message.id);
		if (!existing) {
			order.push(message.id);
			byId.set(message.id, message);
			continue;
		}
		const metadata = existing.metadata || message.metadata ? {
			...existing.metadata,
			...message.metadata
		} : void 0;
		byId.set(message.id, {
			...existing,
			...message,
			...metadata && { metadata }
		});
	}
	return order.flatMap((id) => {
		const message = byId.get(id);
		return message ? [message] : [];
	});
}
function ConversationNavigationPane({ children, className, ...props }) {
	const isWindowFrame = useWindowFrame().mode === "window";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.conversation-navigation-pane",
		className: cn("conversation-navigation-pane relative flex w-[var(--assistants-width)] flex-col overflow-hidden transition-[width] duration-300", isWindowFrame ? "h-full" : "h-[calc(100vh_-_var(--navbar-height))]", className),
		...mergeUiProps(props, "chat.conversation-navigation-pane"),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "conversation-navigation-pane-content flex flex-1 flex-col overflow-hidden transition-[width] duration-300",
			children
		})
	});
}
function ResourceRefreshErrorBanner({ onRetry, retrying }) {
	const { t: t$2 } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Alert, {
		type: "warning",
		showIcon: true,
		className: "mb-2 shrink-0 rounded-md px-3 py-2 text-xs shadow-none",
		message: t$2("common.refresh_failed"),
		action: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			size: "sm",
			variant: "outline",
			onClick: onRetry,
			disabled: retrying,
			children: t$2("common.retry")
		})
	});
}
function useImageCaptureTargets({ cancelMessage, delayMs, rejectPendingActions }) {
	const [targets, setTargets] = (0, import_react.useState)([]);
	const mountedRef = (0, import_react.useRef)(true);
	const startTimersRef = (0, import_react.useRef)(/* @__PURE__ */ new Set());
	const queueTarget = (0, import_react.useCallback)((request, target) => {
		const startTimerId = window.setTimeout(() => {
			startTimersRef.current.delete(startTimerId);
			if (!mountedRef.current) return;
			setTargets((current) => [...current, {
				requestId: request.id,
				target
			}]);
		}, delayMs);
		startTimersRef.current.add(startTimerId);
		request.promise.finally(() => {
			window.clearTimeout(startTimerId);
			startTimersRef.current.delete(startTimerId);
			if (!mountedRef.current) return;
			setTargets((current) => current.filter((candidate) => candidate.requestId !== request.id));
		}).catch(() => void 0);
	}, [delayMs]);
	(0, import_react.useEffect)(() => {
		mountedRef.current = true;
		const startTimers = startTimersRef.current;
		return () => {
			mountedRef.current = false;
			for (const timerId of startTimers) window.clearTimeout(timerId);
			startTimers.clear();
			rejectPendingActions(void 0, new Error(cancelMessage));
		};
	}, [cancelMessage, rejectPendingActions]);
	return {
		queueTarget,
		targets
	};
}
function exportViewToUIMessage(message) {
	const metadata = {
		status: message.status,
		createdAt: message.createdAt
	};
	if (message.updatedAt) metadata.updatedAt = message.updatedAt;
	if (message.parentId !== void 0) metadata.parentId = message.parentId;
	if (message.siblingsGroupId !== void 0) metadata.siblingsGroupId = message.siblingsGroupId;
	if (message.modelId) metadata.modelId = message.modelId;
	if (message.messageSnapshot) metadata.messageSnapshot = message.messageSnapshot;
	if (message.stats) {
		metadata.stats = message.stats;
		if (message.stats.totalTokens) metadata.totalTokens = message.stats.totalTokens;
	}
	return {
		id: message.id,
		role: message.role,
		parts: message.parts,
		metadata
	};
}
function createPartsByMessageId(messages) {
	const partsByMessageId = {};
	for (const message of messages) partsByMessageId[message.id] = message.parts ?? [];
	return partsByMessageId;
}
function useMessageImageCaptureMessages({ loadMessages, mapMessage, onError }) {
	const [messages, setMessages] = (0, import_react.useState)(null);
	const toUIMessage = (0, import_react.useMemo)(() => mapMessage ?? ((message) => exportViewToUIMessage(message)), [mapMessage]);
	(0, import_react.useEffect)(() => {
		let cancelled = false;
		setMessages(null);
		loadMessages().then((loadedMessages) => {
			if (!cancelled) setMessages(loadedMessages.map(toUIMessage));
		}).catch((error) => {
			if (!cancelled) onError(error);
		});
		return () => {
			cancelled = true;
		};
	}, [
		loadMessages,
		onError,
		toUIMessage
	]);
	return {
		messages,
		partsByMessageId: (0, import_react.useMemo)(() => messages ? createPartsByMessageId(messages) : {}, [messages])
	};
}
var MessageImageCaptureHost = ({ captureHostAttribute, messageList, ready, testId }) => {
	if (!ready) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "chat.message-image-capture-host",
		"aria-hidden": "true",
		className: "-left-[10000px] pointer-events-none fixed top-0 h-px w-[960px] overflow-hidden bg-background text-foreground",
		"data-testid": testId,
		inert: true,
		...mergeUiProps({ [captureHostAttribute]: "" }, "chat.message-image-capture-host"),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageListProvider, {
			value: messageList,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MessageList_default, {})
		})
	});
};
var MessageImageCaptureHost_default = (0, import_react.memo)(MessageImageCaptureHost);
export { ConversationShell as $, SESSION_PINNED_GROUP_ID as $t, exportAgentSessionAsMarkdown as A, useConversationCenterSurface as At, buildComposerQueuedPayload as B, buildTopicDropAnchor as Bt, MessageList_default as C, ResourceEntityRail as Cn, CitationsPanel_default as Ct, agentSessionToMarkdown as D, isUntouchedSinceCreation as Dt, useEntityReferenceMentionSource as E, findLatestUpdated as Et, useComposerFileCapabilities as F, TOPIC_PINNED_GROUP_ID as Ft, useFollowupQueue as G, normalizeTopicDropPayload as Gt, hasUnsyncedComposerAttachments as H, getAssistantIdFromTopicGroupId as Ht, ComposerToolbarShortcuts as I, TOPIC_PINNED_SECTION_ID as It, withComposerFilePartMeta as J, EditNameDialog_default as Jt, QueuedFollowupsDock as K, sortTopicsForDisplayGroups as Kt, ComposerSpeedControl as L, TOPIC_UNLINKED_ASSISTANT_GROUP_ID as Lt, getAgentSessionMessagesForExport as M, ConversationResourceView as Mt, useComposerToolbarPinnedTools as N, HistoryRecordsView_default as Nt, copyAgentSessionAsMarkdown as O, pickNeighbourAfterRemoval as Ot, useComposerKnowledgeBaseScope as P, TOPIC_ASSISTANT_SECTION_ID as Pt, ConversationComposerSlot as Q, SESSION_NO_WORKDIR_GROUP_ID as Qt, resolveComposerReasoningEffort as R, applyOptimisticTopicDisplayMove as Rt, useMessageListAdapterCapabilities as S, sortResourceItemsByPinnedTime as Sn, useChatLayoutMode as St, useEntityReferenceMentionItems as T, actionsToCommandMenuExtraItems as Tn, SECOND_MIN_WINDOW_WIDTH as Tt, emptyActions as U, getTopicAssistantDisplayGroupId as Ut, getComposerHistoryText as V, createTopicDisplayGroupResolver as Vt, useInputHistory as W, moveAssistantGroupAfterDrop as Wt, buildEditorUrl as X, SESSION_NO_PROJECT_GROUP_ID as Xt, NewConversationIcon as Y, SESSION_AGENT_SECTION_ID as Yt, useExternalApps as Z, SESSION_NO_PROJECT_SECTION_ID as Zt, withMessagePartDiagnosis as _, useSessionMenuActions as _n, useOptionalRightPanelActions as _t, ConversationNavigationPane as a, buildSessionDropAnchor as an, ConversationCenterState as at, pickMessageLeafActions as b, ConversationSidebarToggleButton as bn, useRightPanelState as bt, useChatWithHistory as c, createSessionDisplayGroupResolver as cn, EmptyState as ct, useMessageStreamingLayers as d, getWorkdirPathFromSessionGroupId as dn, ResourcePaneLocateOpener as dt, SESSION_PINNED_SECTION_ID as en, ConversationTopBarPortal as et, createImageActionBus as f, isSystemWorkspaceSession as fn, createResourcePaneCapability as ft, parseMessagePartId as g, sortSessionsForDisplayGroups as gn, RightPanelViewport as gt, runMessageImageAction as h, normalizeSessionDropPayload as hn, RightPanelShortcut as ht, ResourceRefreshErrorBanner as i, buildSessionAgentGroupDropAnchor as in, ConversationGreeting as it, getAgentSessionExportTitle as j, useClassicLayoutRightPaneOpen as jt, copyAgentSessionAsPlainText as k, useConversationShellPaneState as kt, useToolApprovalComposerOverrides as l, createSessionWorkdirDisplayMaps as ln, ResourcePaneCountButton as lt, flushPendingMessageImageActions as m, moveSessionWorkdirGroupAfterDrop as mn, RightPanelProvider as mt, useMessageImageCaptureMessages as n, SESSION_WORKDIR_SECTION_ID as nn, useConversationTopBarPortalLayout as nt, mergeMessagesById as o, buildSessionWorkdirGroupDropAnchor as on, ConversationStageCenter as ot, bindCaptureMessageImageRuntime as p, moveSessionAgentGroupAfterDrop as pn, RightPanelHeaderControls as pt, buildFilePartsForAttachments as q, useTopicMenuActions as qt, useImageCaptureTargets as r, applyOptimisticSessionDisplayMove as rn, ChatAppShell as rt, useConversationTurnController as s, canDropSessionItemInDisplayGroup as sn, LoadingState as st, MessageImageCaptureHost_default as t, SESSION_UNKNOWN_AGENT_GROUP_ID as tn, ConversationTopBarPortalHost as tt, createOverlayRefreshHandoff as u, getAgentIdFromSessionGroupId as un, RESOURCE_PANE_TAB as ut, dispatchLocateMessage as v, createActionRegistry as vn, useOptionalRightPanelState as vt, useLatest as w, ResourceListActionContextMenu as wn, MIN_WINDOW_HEIGHT as wt, pickMessageLeafState as x, useResourceEntityRail as xn, ChatLayoutModeProvider as xt, pickMessageHeaderActions as y, isGlobalSearchSelectionForTab as yn, useRightPanelActions as yt, useComposerQuoteInsertion as z, buildAssistantGroupDropAnchor as zt };
