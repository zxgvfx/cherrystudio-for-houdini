import { s as __toESM } from "./chunk-DiqNceaa.js";
import { t as loggerService } from "./LoggerService-oVV4iwe6.js";
import { C as string, S as strictObject, _ as object, a as array, g as number, n as _enum, o as boolean, w as union } from "./schemas-CV_EtlSZ.js";
import { u as serializeHealthCheckError } from "./error-DgGhUgCT.js";
import { y as CodeCli } from "./PreferenceService-Ba0ofBX2.js";
import { n as usePersistCache } from "./useCache-SsOQx-L2.js";
import { n as isBuffer } from "./isEqual-DO7BtJs5.js";
import { t as isArrayLike } from "./isArrayLike-qCM8LrC4.js";
import { c as DynamicVirtualList_default, d as getModelDisplayTags, m as sortBy, p as CustomTag_default, t as isProviderSettingsListVisibleProvider, u as ModelTag } from "./providerSettings-BcvJgtqn.js";
import { r as times } from "./getSymbolsIn-BtmoyFWl.js";
import { n as isTypedArray, t as isPrototype } from "./isPrototype-CEcR1wZx.js";
import { n as omit, t as useSWRImmutable } from "./immutable-J0MUNQkZ.js";
import { t as debounce } from "./debounce-BIuc7S_h.js";
import { t as isEmpty } from "./isEmpty-D8zepxXt.js";
import { a as maskApiKey, l as trim, n as formatApiKeys, o as splitApiKeyString, r as isWithTrailingSharp, t as formatApiHost } from "./format-BU2M4Rvo.js";
import { r as resolver_default, t as getLanguageCode } from "./resolver-DYDQGqns.js";
import { t as require_react } from "./react-BgPOU4At.js";
import { t as Trans } from "./Trans-gaWfeLk6.js";
import { t as useTranslation } from "./useTranslation-DnuRkr5k.js";
import { n as usePreference } from "./usePreference-78czMD_R.js";
import { a as mergeUiProps, i as UiDataSlot, r as cn$1 } from "./dist-BGahvVh8.js";
import { t as require_jsx_runtime } from "./jsx-runtime-DCB_IiL2.js";
import { n as AvatarFallback, t as Avatar } from "./avatar-BMK8th-L.js";
import { c as SelectValue, i as SelectItem, n as SelectContent, s as SelectTrigger, t as Select } from "./select-BPh2DI-f.js";
import { t as Alert } from "./alert-BwuKGcCL.js";
import { t as Button } from "./button-Db6_VSWw.js";
import { n as Tooltip } from "./tooltip-ZuayyV11.js";
import { t as Spinner } from "./spinner-DFTIxGhU.js";
import { n as Switch } from "./switch-DEX3m-Zc.js";
import { a as DialogFooter, i as DialogDescription, o as DialogHeader, r as DialogContent, s as DialogTitle, t as Dialog } from "./dialog-9s9VTSCz.js";
import { n as RadioGroupItem, t as RadioGroup } from "./radio-group-BLEiejOL.js";
import { t as Input } from "./input-dvr72LyA.js";
import { i as PopoverTrigger, n as PopoverAnchor, r as PopoverContent, t as Popover } from "./popover-n5iWPIgn.js";
import { t as EmptyState } from "./empty-state-C4JfZPek.js";
import { a as RowFlex, r as ColFlex } from "./flex-BmIgOnUt.js";
import { t as scrollbar_default } from "./scrollbar-u8WAeDey.js";
import { n as PageSidePanelItem, t as PageSidePanel } from "./page-side-panel-CF7JqFV7.js";
import { t as Input$1 } from "./textarea-Djv1FqZD.js";
import { i as InputGroupInput, n as InputGroupAddon, t as InputGroup } from "./input-group-Cx4HnAhR.js";
import { t as editable_number_default } from "./editable-number-Dhr-NqcZ.js";
import { t as Label } from "./label-CUymrXZH.js";
import { t as Markdown } from "./markdown-CUccgRk8.js";
import { i as withMath, t as defaultMarkdownPlugins } from "./presets-DvKIcYCC.js";
import { n as MenuItem, r as MenuList } from "./menu-item-Boj_QdAM.js";
import { p as closestCenter } from "./sortable.esm-D8gcNQZE.js";
import { t as sortable_default } from "./sortable-BMb6uoEl.js";
import { t as ReorderableList } from "./reorderable-list-CM96dcTS.js";
import { i as AccordionTrigger, n as AccordionContent, r as AccordionItem, t as Accordion } from "./accordion-YSkmiq4a.js";
import { t as Badge } from "./badge-HGyBlzr8.js";
import { t as Separator } from "./separator-DFnWafbO.js";
import { n as ButtonGroupItem, t as ButtonGroup } from "./button-group-DoZw5rJv.js";
import { t as Combobox } from "./combobox-D7zDQ7j5.js";
import { c as FieldSet, i as FieldError, o as FieldLabel, s as FieldLegend, t as Field } from "./field-v-3EhHEn.js";
import { t as SegmentedControl } from "./segmented-control-DMdgjBZR.js";
import { t as Skeleton } from "./skeleton-CmSX8uoi.js";
import { t as Slider } from "./slider-_Xxahv5d.js";
import { i as TabsTrigger, r as TabsList, t as Tabs } from "./tabs-C7Ec2BdI.js";
import { n as useTheme } from "./useTheme-CbMe73se.js";
import { n as CommandContextMenu, r as CommandPopupMenu } from "./command-lPk0rc3k.js";
import { n as ipcApi } from "./ipc-BDTAufGC.js";
import { r as openSettingsTab } from "./mainWindowNavigation-m8-JFOdz.js";
import { i as getForegroundColor, n as cn, r as generateColorFromChar } from "./style-qqUWb85F.js";
import { _ as ENDPOINT_TYPE, g as CURRENCY, i as createUniqueModelId, n as RuntimeModelPricingSchema, o as parseUniqueModelId, r as UniqueModelIdSchema, t as ParameterSupportDbSchema, v as MODALITY, w as objectValues, y as MODEL_CAPABILITY } from "./model-CfoN7z8F.js";
import { a as isDataApiError, i as ErrorCode, o as isSerializedDataApiError, s as toDataApiError, t as dataApiService } from "./DataApiService-DP44jQXR.js";
import { a as useInvalidateCache, c as useQuery, o as useMutation } from "./useDataApi-DhDD9bgI.js";
import { t as useReorder } from "./useReorder-CHjMnht2.js";
import { t as toast } from "./toast-DKTI9lZN.js";
import { t as Activity } from "./activity-dM8Sm7da.js";
import { t as ArrowRight } from "./arrow-right-10DRsfJt.js";
import { t as ArrowUpDown } from "./arrow-up-down-R1Q9LYGF.js";
import { t as ArrowUpRight } from "./arrow-up-right-BwH4Qepa.js";
import { t as AudioLines } from "./audio-lines-Cfo98GO9.js";
import { t as Bolt } from "./bolt-D-cIiSWi.js";
import { t as Boxes } from "./boxes-DT47b11r.js";
import { t as Braces } from "./braces-BiLfkX9b.js";
import { t as BrainCircuit } from "./brain-circuit-DMedjQeb.js";
import { t as Check } from "./check-DGAvfDpR.js";
import { t as ChevronDown } from "./chevron-down-BVtofl6V.js";
import { t as ChevronRight } from "./chevron-right-CvFIUMTW.js";
import { t as ChevronUp } from "./chevron-up-C-an45UL.js";
import { t as ChevronsDownUp } from "./chevrons-down-up-DBIwXZL5.js";
import { t as ChevronsUpDown } from "./chevrons-up-down-Dn-WN3pT.js";
import { t as CircleAlert } from "./circle-alert-BkebTlp2.js";
import { t as CircleCheck } from "./circle-check-8iqFGUCC.js";
import { t as CircleDollarSign } from "./circle-dollar-sign-CaEHnfDZ.js";
import { t as CircleQuestionMark } from "./circle-question-mark-B2xJCLbL.js";
import { t as CircleX } from "./circle-x-DObvF9JM.js";
import { t as CopyPlus } from "./copy-plus-D5A9G9RO.js";
import { t as Copy } from "./copy-DLNIVOlq.js";
import { t as Ear } from "./ear-C7aQYfiK.js";
import { t as EllipsisVertical } from "./ellipsis-vertical-VELOguHq.js";
import { t as EyeOff } from "./eye-off-B0yNvNAf.js";
import { t as Eye } from "./eye-B3h_cTL2.js";
import { t as FileText } from "./file-text-C2NTQkH7.js";
import { t as Funnel } from "./funnel--VzREmnf.js";
import { t as Gift } from "./gift-BV2kGnP7.js";
import { t as GripVertical } from "./grip-vertical-Dlsr9QCa.js";
import { t as ImagePlus } from "./image-plus-zkmSdUqW.js";
import { t as Image } from "./image-BueQcb3X.js";
import { t as Info } from "./info-rKrg7-xW.js";
import { t as KeyRound } from "./key-round-dQ4z5cO2.js";
import { t as ListMinus } from "./list-minus-CQLuKUkR.js";
import { t as ListPlus } from "./list-plus-DOQuvTeV.js";
import { t as List } from "./list-DvMZwghn.js";
import { t as LoaderCircle } from "./loader-circle-CyzyZXyF.js";
import { t as LogIn } from "./log-in-C5849gat.js";
import { t as Mic } from "./mic-BVcrISa6.js";
import { t as Minus } from "./minus-B8LlKIu6.js";
import { t as PenLine } from "./pen-line-ChJ7Ihtl.js";
import { t as Plus } from "./plus-DvXnghWX.js";
import { t as ReceiptText } from "./receipt-text-BMB2HYwn.js";
import { t as RefreshCw } from "./refresh-cw-9yFUmry6.js";
import { t as RotateCcw } from "./rotate-ccw-BS5J7gIn.js";
import { t as Search } from "./search-CQaFGVmj.js";
import { t as Settings } from "./settings-DM9xfvj6.js";
import { t as Speech } from "./speech-Cruv4R1s.js";
import { t as SquareArrowOutUpRight } from "./square-arrow-out-up-right-BxPZ4dXU.js";
import { t as SquarePen } from "./square-pen-BnetLbPt.js";
import { t as SquareTerminal } from "./square-terminal-DZvGtDtP.js";
import { t as Trash2 } from "./trash-2-_PaXFSXZ.js";
import { t as TriangleAlert } from "./triangle-alert-S57Y3qwG.js";
import { t as Type } from "./type-CO8oHR1j.js";
import { t as UserPen } from "./user-pen-D5XhsOJt.js";
import { t as Video } from "./video-CLi91f1t.js";
import { t as Wrench } from "./wrench-B0uARSuO.js";
import { t as X } from "./x-DelRxIMm.js";
import { n as createPopup, t as popup } from "./popup-BqV2ZD7D.js";
import { n as loadProviderIconCatalog, t as useIcon } from "./use-icon-DpFtqCDZ.js";
import { t as CherryinIcon } from "./cherryin-BfWgql21.js";
import { t as DmxapiIcon } from "./dmxapi-CrDc1qoY.js";
import { a as resolveProviderIconRef, o as PROVIDER_ICON_META_CATALOG } from "./registry-8iQY-Y1a.js";
import { t as getIconDisplayConfig } from "./iconDisplayConfig-Cn5AH07m.js";
import { o as getProviderLabelKey } from "./label-B7fCsuWT.js";
import { n as isSystemProviderId, t as SystemProviderIds } from "./provider-DxZtFw5B.js";
import { B as getProviderHostTopology, C as isAzureOpenAIProvider, F as isSystemProvider, H as getDefaultGroupName, I as isVertexProvider, L as matchesPreset, M as isPerplexityProvider, N as isProviderSupportAuth, O as isLoginBasedProvider, S as isAwsBedrockProvider, T as isCherryAIProvider, b as isAnthropicProvider, j as isOpenAICompatibleProvider, k as isNewApiProvider, n as getSearchMatchScore, u as getModelLogoRef, v as canManageProvider, x as isAnthropicSupportedProvider, y as hasApiKeys } from "./model-J88ZZ_lQ.js";
import { p as prepareEntityImageBytes, s as checkEntityImageSize } from "./image-C_pRJZT2.js";
import { t as useSearch } from "./useSearch-wm23OOBq.js";
import { t as useNavigate } from "./useNavigate-DEeZmEwY.js";
import { t as v4_default } from "./v4-BSqPk4tJ.js";
import { t as uuid } from "./uuid-DNJDgQB1.js";
import { i as useModels, r as useModelMutations } from "./useModel-CIp4kGrE.js";
import { a as useProviderAuthConfig, c as useProviderPreset, i as useProviderApiKeys, l as useProviders, n as useProvider, r as useProviderActions, s as useProviderMutations } from "./useProvider-DEPjZhOC.js";
import { S as isTextToSpeechModel$1, b as isSpeechToTextModel, f as isGenerateAudioModel, h as isNonChatModel, m as isGenerateVideoModel, o as isEmbeddingModel, p as isGenerateImageModel, t as deriveModelGroupName, y as isRerankModel } from "./model-D4kd6G9w.js";
import { n as formatVertexApiHost, r as validateApiHost, t as formatOllamaApiHost } from "./api-BxGlwl7U.js";
import { r as ConfirmActionPopup_default, t as showErrorDetailPopup } from "./ErrorDetailModal-D2XPz4-l.js";
import { t as Scrollbar_default } from "./Scrollbar-CRo8NoWr.js";
import { t as ProviderAvatarPrimitive } from "./ProviderAvatar-DEFL8YDx.js";
import { t as useTimer } from "./useTimer-B4RsTlvl.js";
import { t as CopyIcon_default } from "./CopyIcon-oeHf1okg.js";
import { t as LoadingIcon_default } from "./LoadingIcon-CmUpDics.js";
import { t as IndicatorLight_default } from "./IndicatorLight-BpN6p5mZ.js";
function keys(object$1) {
	if (isArrayLike(object$1)) return arrayLikeKeys(object$1);
	const result = Object.keys(Object(object$1));
	if (!isPrototype(object$1)) return result;
	return result.filter((key) => key !== "constructor");
}
function arrayLikeKeys(object$1) {
	const indices = times(object$1.length, (index) => `${index}`);
	const filteredKeys = new Set(indices);
	if (isBuffer(object$1)) {
		filteredKeys.add("offset");
		filteredKeys.add("parent");
	}
	if (isTypedArray(object$1)) {
		filteredKeys.add("buffer");
		filteredKeys.add("byteLength");
		filteredKeys.add("byteOffset");
	}
	const inheritedKeys = Object.keys(object$1).filter((key) => !filteredKeys.has(key));
	if (Array.isArray(object$1)) return [...indices, ...inheritedKeys];
	return [...indices.filter((index) => Object.hasOwn(object$1, index)), ...inheritedKeys];
}
function mapToEntries(map) {
	const arr = new Array(map.size);
	const keys$1 = map.keys();
	const values = map.values();
	for (let i = 0; i < arr.length; i++) arr[i] = [keys$1.next().value, values.next().value];
	return arr;
}
function setToEntries(set) {
	const arr = new Array(set.size);
	const values = set.values();
	for (let i = 0; i < arr.length; i++) {
		const value = values.next().value;
		arr[i] = [value, value];
	}
	return arr;
}
function toPairs(object$1) {
	if (object$1 == null) return [];
	if (object$1 instanceof Set) return setToEntries(object$1);
	if (object$1 instanceof Map) return mapToEntries(object$1);
	const keys$1 = keys(object$1);
	const result = new Array(keys$1.length);
	for (let i = 0; i < keys$1.length; i++) {
		const key = keys$1[i];
		result[i] = [key, object$1[key]];
	}
	return result;
}
object({
	providerId: string().optional(),
	capability: _enum(objectValues(MODEL_CAPABILITY)).optional(),
	enabled: boolean().optional()
});
const CreateModelSchema = strictObject({
	providerId: string().min(1),
	modelId: string().min(1),
	presetModelId: string().optional(),
	name: string().optional(),
	description: string().optional(),
	group: string().optional(),
	capabilities: array(_enum(objectValues(MODEL_CAPABILITY))).optional(),
	inputModalities: array(_enum(objectValues(MODALITY))).optional(),
	outputModalities: array(_enum(objectValues(MODALITY))).optional(),
	endpointTypes: array(_enum(objectValues(ENDPOINT_TYPE))).optional(),
	contextWindow: number().int().positive().optional(),
	maxInputTokens: number().int().positive().optional(),
	maxOutputTokens: number().int().positive().optional(),
	supportsStreaming: boolean().optional(),
	parameterSupport: ParameterSupportDbSchema.optional(),
	pricing: RuntimeModelPricingSchema.optional()
});
const MODELS_BULK_UPDATE_MAX_ITEMS = 1e3;
const MODELS_DELETE_MAX_IDS = 1e3;
const MODELS_RECONCILE_MAX_ITEMS = 5e3;
array(CreateModelSchema).min(1).max(500);
array(object({
	uniqueModelId: UniqueModelIdSchema,
	patch: CreateModelSchema.omit({
		providerId: true,
		modelId: true,
		presetModelId: true
	}).partial().extend({
		isEnabled: boolean().optional(),
		isHidden: boolean().optional(),
		isDeprecated: boolean().optional(),
		notes: string().optional()
	})
})).min(1).max(MODELS_BULK_UPDATE_MAX_ITEMS);
strictObject({ ids: union([UniqueModelIdSchema.transform((id) => [id]), array(UniqueModelIdSchema).min(1).max(MODELS_DELETE_MAX_IDS)]) });
strictObject({
	toAdd: array(CreateModelSchema).max(MODELS_RECONCILE_MAX_ITEMS),
	toRemove: array(UniqueModelIdSchema).max(MODELS_RECONCILE_MAX_ITEMS)
});
strictObject({ ids: union([string().min(1), array(string().min(1)).min(1)]) });
function chunkArray(items, chunkSize) {
	const chunks = [];
	for (let index = 0; index < items.length; index += chunkSize) chunks.push(items.slice(index, index + chunkSize));
	return chunks;
}
var logger$27 = loggerService.withContext("ProviderModelSync");
function resolveCreateModelEndpointTypes(provider, model) {
	if (model.endpointTypes?.length) return [...model.endpointTypes];
	if (!provider || !isNewApiProvider(provider)) return;
	return provider.defaultChatEndpoint ? [provider.defaultChatEndpoint] : void 0;
}
function getRawModelId(model) {
	return model.apiModelId ?? (model.id ? parseUniqueModelId(model.id).modelId : "");
}
function toCreateModelDto(providerId, model, endpointTypes) {
	const modelId = getRawModelId(model);
	const resolvedEndpointTypes = endpointTypes?.length ? endpointTypes : model.endpointTypes;
	return {
		providerId,
		modelId,
		name: model.name,
		group: model.group,
		...resolvedEndpointTypes?.length ? { endpointTypes: [...resolvedEndpointTypes] } : {}
	};
}
async function enrichFetchedModels(providerId, fetchedModels) {
	const filteredModels = fetchedModels.filter((model) => !isEmpty(model.name));
	if (filteredModels.length === 0) return [];
	const resolveModelsPath = `/providers/${providerId}/models:resolve`;
	const resolved = await dataApiService.get(resolveModelsPath, { query: { ids: filteredModels.map((model) => model.apiModelId ?? "").filter(Boolean) } });
	const resolvedMap = /* @__PURE__ */ new Map();
	for (const model of resolved) {
		const key = model.apiModelId ?? parseUniqueModelId(model.id).modelId;
		if (!resolvedMap.has(key)) resolvedMap.set(key, model);
	}
	const REGISTRY_FIELDS = [
		"name",
		"description",
		"group",
		"capabilities",
		"inputModalities",
		"outputModalities",
		"endpointTypes",
		"contextWindow",
		"maxOutputTokens",
		"maxInputTokens",
		"reasoning",
		"pricing",
		"family",
		"ownedBy"
	];
	return filteredModels.map((fetched) => {
		const base = fetched;
		const apiId = fetched.apiModelId ?? "";
		const registry = resolvedMap.get(apiId) ?? resolvedMap.get(apiId.includes("/") ? apiId.substring(apiId.lastIndexOf("/") + 1) : apiId) ?? resolvedMap.get((apiId.includes("/") ? apiId.substring(apiId.lastIndexOf("/") + 1) : apiId).replaceAll(".", "-"));
		if (!registry) return base;
		const merged = { ...base };
		for (const field of REGISTRY_FIELDS) {
			if (field === "endpointTypes" && base.endpointTypes?.length) continue;
			const value = registry[field];
			if (value !== void 0 && value !== null && !(Array.isArray(value) && value.length === 0)) merged[field] = value;
		}
		return merged;
	});
}
async function fetchResolvedProviderModels(providerId) {
	try {
		logger$27.info("Fetching provider models via IPC", { providerId });
		const fetched = await ipcApi.request("ai.provider.model.list", {
			providerId,
			throwOnError: true
		});
		logger$27.info("Fetched provider models", {
			providerId,
			fetchedModelCount: fetched.length
		});
		return await enrichFetchedModels(providerId, fetched);
	} catch (error) {
		logger$27.error("Failed to fetch and resolve provider models", {
			providerId,
			error
		});
		throw error;
	}
}
async function fetchProviderCatalogModels(providerId) {
	const presetPath = `/providers/${providerId}/preset`;
	return (await dataApiService.get(presetPath, { query: { fields: "models" } })).models ?? [];
}
const PROVIDER_SETTINGS_MODEL_SWR_OPTIONS = {
	revalidateOnMount: true,
	revalidateIfStale: false
};
var import_react = /* @__PURE__ */ __toESM(require_react());
var logger$26 = loggerService.withContext("ProviderSettings:ModelSync");
function useProviderModelSync(providerId, options = {}) {
	const fallbackModelsQuery = useModels({ providerId }, {
		fetchEnabled: options.existingModels ? false : void 0,
		swrOptions: PROVIDER_SETTINGS_MODEL_SWR_OPTIONS
	});
	const models = options.existingModels ?? fallbackModelsQuery.models;
	const { provider } = useProvider(providerId);
	const { createModels, isCreating } = useModelMutations();
	return {
		syncProviderModels: (0, import_react.useCallback)(async (endpointProvider = provider) => {
			logger$26.info("Checking provider models before sync", {
				providerId,
				localModelCount: models.length
			});
			const latestModels = models.length > 0 ? [...models] : await dataApiService.get("/models", { query: { providerId } });
			if (latestModels.length > 0) {
				logger$26.info("Skipping provider model creation because models already exist", {
					providerId,
					modelCount: latestModels.length
				});
				return latestModels;
			}
			logger$26.info("Fetching remote provider models for sync", { providerId });
			const resolvedModels = await fetchResolvedProviderModels(providerId);
			if (resolvedModels.length === 0) {
				logger$26.info("No remote provider models were resolved for sync", { providerId });
				return [];
			}
			logger$26.info("Resolved remote provider models for sync", {
				providerId,
				resolvedModelCount: resolvedModels.length
			});
			const existingModelIds = new Set(latestModels.map((model) => model.id));
			const payload = resolvedModels.filter((model) => !existingModelIds.has(model.id)).map((model) => toCreateModelDto(providerId, model, resolveCreateModelEndpointTypes(endpointProvider, model)));
			if (payload.length === 0) {
				logger$26.info("Skipping provider model creation because resolved models are already present", {
					providerId,
					resolvedModelCount: resolvedModels.length
				});
				return latestModels;
			}
			const chunks = chunkArray(payload, 500);
			const createdModels = [];
			logger$26.info("Creating provider models from resolved remote list", {
				providerId,
				createCount: payload.length,
				chunkCount: chunks.length
			});
			for (const chunk of chunks) {
				const created = await createModels(chunk);
				createdModels.push(...created);
			}
			logger$26.info("Completed provider model sync", {
				providerId,
				createdModelCount: createdModels.length
			});
			return [...latestModels, ...createdModels];
		}, [
			createModels,
			models,
			provider,
			providerId
		]),
		isSyncingModels: isCreating
	};
}
function isCanonicalPresetProvider(provider) {
	return provider.presetProviderId != null && provider.id === provider.presetProviderId;
}
function isProviderPresetInstanceSource(provider) {
	if (!isCanonicalPresetProvider(provider) || isLoginBasedProvider(provider) || provider.id === "copilot") return false;
	if (provider.authType !== "api-key" && provider.authType !== "iam-azure") return false;
	const endpoint = provider.defaultChatEndpoint;
	if (endpoint != null) return provider.endpointConfigs?.[endpoint] != null;
	return provider.presetProviderId === "new-api" && provider.endpointConfigs?.[ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS] != null;
}
function getFancyProviderName(provider) {
	if (isCanonicalPresetProvider(provider)) {
		const presetProviderId = provider.presetProviderId;
		if (presetProviderId) return resolver_default.t(getProviderLabelKey(presetProviderId));
	}
	return provider.name;
}
function getProviderSearchString(provider) {
	if (isCanonicalPresetProvider(provider)) {
		const presetProviderId = provider.presetProviderId;
		if (presetProviderId) return `${resolver_default.t(getProviderLabelKey(presetProviderId))} ${provider.id}`;
	}
	return `${provider.id} ${provider.name}`;
}
function matchKeywordsInProvider(keywords, provider, extraSearchString) {
	if (keywords.length === 0) return true;
	const base = getProviderSearchString(provider);
	const searchStr = (extraSearchString ? `${base} ${extraSearchString}` : base).toLowerCase();
	return keywords.every((kw) => searchStr.includes(kw));
}
function replaceEndpointConfigDomain(endpointConfigs, newDomain) {
	if (!endpointConfigs) return {};
	const result = {};
	for (const [key, config] of Object.entries(endpointConfigs)) {
		if (!config) continue;
		const ep = key;
		const baseUrl = config.baseUrl;
		if (!baseUrl) {
			result[ep] = config;
			continue;
		}
		try {
			const parsed = new URL(baseUrl);
			parsed.hostname = newDomain;
			result[ep] = {
				...config,
				baseUrl: parsed.toString().replace(/\/$/, "")
			};
		} catch {
			result[ep] = config;
		}
	}
	return result;
}
var import_jsx_runtime = /* @__PURE__ */ __toESM(require_jsx_runtime());
var PopupContainer$2 = ({ id, apiKey: newApiKey, baseUrl, type, name, open, resolve }) => {
	const { t } = useTranslation();
	const { providers: rawProviders } = useProviders();
	const [showFullKey, setShowFullKey] = (0, import_react.useState)(false);
	const foundProvider = (0, import_react.useMemo)(() => Array.isArray(rawProviders) ? rawProviders : [], [rawProviders]).find((p) => p.id === id);
	const existingApiHost = getProviderHostTopology(foundProvider).primaryBaseUrl;
	const { data: apiKeysData, isLoading: apiKeysLoading } = useQuery("/providers/:providerId/api-keys", {
		params: { providerId: id },
		enabled: foundProvider !== void 0
	});
	const baseProvider = foundProvider ? {
		id: foundProvider.id,
		name: foundProvider.name,
		type: type || "openai",
		apiKey: "",
		apiHost: existingApiHost
	} : {
		id,
		name: name || id,
		type: type || "openai",
		apiKey: "",
		apiHost: baseUrl || ""
	};
	const displayName = foundProvider ? getFancyProviderName(foundProvider) : baseProvider.name;
	const existingKeys = apiKeysData?.keys?.map((k) => k.key.trim()).filter(Boolean) ?? [];
	const trimmedNewKey = newApiKey.trim();
	const keyAlreadyExists = existingKeys.includes(trimmedNewKey);
	const baseUrlChanged = Boolean(baseUrl) && baseUrl !== baseProvider.apiHost;
	const okDisabled = foundProvider !== void 0 && apiKeysLoading || keyAlreadyExists && !baseUrlChanged;
	const confirmMessage = keyAlreadyExists ? t("settings.models.provider_key_already_exists", { provider: displayName }) : t("settings.models.provider_key_add_confirm", { provider: displayName });
	const okText = apiKeysLoading ? t("common.loading") : keyAlreadyExists ? t("common.confirm") : t("common.add");
	const handleOk = () => {
		const finalApiKey = keyAlreadyExists ? "" : trimmedNewKey;
		const finalApiHost = baseUrlChanged ? baseUrl : baseProvider.apiHost;
		if (finalApiKey === baseProvider.apiKey && finalApiHost === baseProvider.apiHost) {
			resolve({
				updatedProvider: void 0,
				isNew: !foundProvider,
				displayName
			});
			return;
		}
		resolve({
			updatedProvider: {
				...baseProvider,
				apiKey: finalApiKey,
				apiHost: finalApiHost
			},
			isNew: !foundProvider,
			displayName
		});
	};
	const handleCancel = () => {
		resolve({
			updatedProvider: void 0,
			isNew: !foundProvider,
			displayName
		});
	};
	const rows = [
		{
			label: t("settings.models.provider_name"),
			value: displayName
		},
		{
			label: t("settings.models.provider_id"),
			value: baseProvider.id
		},
		...baseUrl ? [{
			label: t("settings.models.base_url"),
			value: baseUrl
		}] : []
	];
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog, {
		open,
		onOpenChange: (nextOpen) => {
			if (!nextOpen) handleCancel();
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogContent, {
			className: "gap-5 rounded-2xl border-border-subtle bg-popover p-5 sm:max-w-md",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogHeader, {
					className: "gap-1.5 pr-6",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogTitle, {
						className: "text-foreground text-sm leading-5",
						children: t("settings.models.provider_key_confirm_title", { provider: displayName })
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogDescription, {
						className: "text-muted-foreground text-sm leading-5",
						children: confirmMessage
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "space-y-4",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "overflow-hidden rounded-xl border border-border-subtle bg-transparent",
						children: [rows.map((row) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "grid grid-cols-[7.5rem_minmax(0,1fr)] gap-3 border-border-subtle border-b px-3 py-2.5 last:border-b-0",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "text-muted-foreground text-xs",
								children: row.label
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "min-w-0 truncate text-foreground text-sm",
								children: row.value
							})]
						}, row.label)), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "grid grid-cols-[7.5rem_minmax(0,1fr)] gap-3 px-3 py-2.5",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "text-muted-foreground text-xs",
								children: t("settings.models.api_key")
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: "flex min-w-0 items-center justify-between gap-2",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: "min-w-0 truncate font-mono text-foreground text-sm",
									children: showFullKey ? newApiKey : maskApiKey(newApiKey)
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
									variant: "ghost",
									size: "icon-sm",
									onClick: () => setShowFullKey((prev) => !prev),
									children: showFullKey ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Eye, { size: 16 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EyeOff, { size: 16 })
								})]
							})]
						})]
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogFooter, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					variant: "outline",
					onClick: handleCancel,
					children: t("common.cancel")
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					disabled: okDisabled,
					onClick: handleOk,
					children: okText
				})] })
			]
		})
	});
};
var UrlSchemaInfoPopup_default = createPopup(PopupContainer$2, { dismissResult: {
	updatedProvider: void 0,
	isNew: false,
	displayName: ""
} });
var logger$25 = loggerService.withContext("useProviderDeepLinkImport");
function resolveDefaultEndpoint(type) {
	switch (type) {
		case "anthropic":
		case "vertex-anthropic": return ENDPOINT_TYPE.ANTHROPIC_MESSAGES;
		case "openai-response": return ENDPOINT_TYPE.OPENAI_RESPONSES;
		case "gemini":
		case "vertexai": return ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT;
		case "ollama": return ENDPOINT_TYPE.OLLAMA_CHAT;
		default: return ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS;
	}
}
function useProviderDeepLinkImport(searchAddProviderData, onSelectProvider) {
	const { t } = useTranslation();
	const navigate = useNavigate();
	const { createProvider } = useProviders();
	const { updateProviderById } = useProviderActions();
	const { trigger: addApiKeyTrigger } = useMutation("POST", "/providers/:providerId/api-keys", { refresh: ({ args }) => [
		"/providers",
		`/providers/${args.params.providerId}`,
		`/providers/${args.params.providerId}/*`
	] });
	(0, import_react.useEffect)(() => {
		if (!searchAddProviderData) return;
		const importProvider = async (providerData) => {
			try {
				const { updatedProvider, isNew, displayName } = await UrlSchemaInfoPopup_default.show(providerData);
				if (!updatedProvider) {
					navigate({ to: "/settings/provider" });
					return;
				}
				const providerId = updatedProvider.id;
				const defaultChatEndpoint = resolveDefaultEndpoint(updatedProvider.type);
				if (updatedProvider.apiHost && !validateApiHost(updatedProvider.apiHost)) {
					logger$25.warn("Rejected deep-link apiHost with invalid scheme", { providerId });
					toast.error(t("settings.models.provider_key_add_failed_by_invalid_data"));
					navigate({ to: "/settings/provider" });
					return;
				}
				const endpointConfigs = updatedProvider.apiHost ? { [defaultChatEndpoint]: { baseUrl: updatedProvider.apiHost } } : void 0;
				if (isNew) await createProvider({
					providerId,
					name: updatedProvider.name || providerData.id,
					defaultChatEndpoint,
					endpointConfigs
				});
				else await updateProviderById(providerId, {
					name: updatedProvider.name,
					defaultChatEndpoint,
					endpointConfigs
				});
				if (updatedProvider.apiKey.trim()) await addApiKeyTrigger({
					params: { providerId },
					body: { key: updatedProvider.apiKey.trim() }
				});
				onSelectProvider(providerId);
				navigate({
					to: "/settings/provider",
					search: { id: providerId }
				});
				toast.success(t("settings.models.provider_key_added", { provider: displayName }));
			} catch (error) {
				logger$25.error("Failed to import provider deep link data", error);
				toast.error(t("settings.models.provider_key_add_failed_by_invalid_data"));
				navigate({ to: "/settings/provider" });
			}
		};
		try {
			const parsed = JSON.parse(searchAddProviderData);
			if (!parsed.id || !parsed.apiKey || !parsed.baseUrl) {
				toast.error(t("settings.models.provider_key_add_failed_by_invalid_data"));
				navigate({ to: "/settings/provider" });
				return;
			}
			importProvider(parsed);
		} catch (error) {
			logger$25.error("Failed to parse provider deep link import data", error);
			toast.error(t("settings.models.provider_key_add_failed_by_invalid_data"));
			navigate({ to: "/settings/provider" });
		}
	}, [
		addApiKeyTrigger,
		createProvider,
		navigate,
		onSelectProvider,
		searchAddProviderData,
		t,
		updateProviderById
	]);
}
const providerSettingsTypography = {
	menu: "text-sm leading-5",
	body: "text-sm leading-5",
	label: "text-xs leading-tight",
	micro: "text-xs leading-tight",
	caption: "text-xs leading-tight",
	subtitle: "text-sm leading-5"
};
var providerSettingsInputGroupBase = "h-8 rounded-lg border border-border-subtle bg-muted/30 px-2.5 shadow-none";
var sectionHeadingBase = "m-0 text-sm text-foreground leading-[1.3]";
const sectionHeadingClasses = cn(sectionHeadingBase, "font-medium");
const authConnectionClasses = {
	shell: "",
	body: "flex flex-col gap-5"
};
const providerDetailColumnClasses = {
	headerPad: "shrink-0 px-6 py-2.5",
	scrollStrip: "min-h-0 flex-1 overflow-x-hidden px-6 pt-1.5 pb-6",
	contentMaxWidth: "mx-auto w-full max-w-3xl",
	headerContentMaxWidth: "mx-auto w-full max-w-3xl",
	sectionStack: "mx-auto flex min-h-full w-full min-w-0 max-w-3xl flex-col gap-5"
};
const actionClasses = {
	row: "flex flex-wrap items-center gap-3",
	icon: "size-3 shrink-0",
	btnBase: "h-auto min-h-0 gap-2 rounded-lg px-3 py-1.5 text-[13px] leading-tight shadow-none",
	btnNeutral: "border-border-subtle bg-transparent text-muted-foreground hover:bg-accent hover:text-foreground"
};
var providerListItemFrame = "relative flex h-8 w-full items-center justify-between rounded-[10px] border border-transparent py-0 pr-2.5 pl-0.5 text-left shadow-none outline-none transition-colors focus-visible:ring-0";
const providerListClasses = {
	shell: "flex h-full w-[248px] shrink-0 basis-[248px] flex-col border-border border-r-[0.5px]",
	headerIconButton: "flex size-6 shrink-0 items-center justify-center rounded-md text-muted-foreground transition-colors hover:bg-accent/40 hover:text-foreground disabled:pointer-events-none disabled:opacity-30",
	searchInlineAddButton: "flex size-[22px] shrink-0 items-center justify-center rounded-md transition-colors hover:bg-accent/40 disabled:pointer-events-none disabled:opacity-30",
	searchRow: "flex items-center gap-1.5 px-2.5 pt-2.5",
	searchWrap: "flex h-8 items-center gap-1 rounded-[10px] border border-input bg-background py-1 pl-2.5 pr-1",
	searchIcon: "mr-0.5 size-3.5 shrink-0 text-muted-foreground",
	searchInput: "min-w-0 flex-1 bg-transparent text-sm leading-5 text-foreground outline-none placeholder:text-muted-foreground",
	searchClearButton: "flex size-[22px] shrink-0 items-center justify-center rounded-md text-muted-foreground transition-colors hover:bg-accent/40 hover:text-foreground disabled:pointer-events-none disabled:opacity-30",
	scroller: "min-h-0 flex-1 px-2.5 pt-2 pb-0",
	sectionStack: "flex flex-col gap-2",
	section: "space-y-2",
	sectionHeader: "pb-0.5 pl-2 pr-2 pt-1.5",
	sectionHeaderAfterEnabled: "pt-2",
	sectionLabel: "mb-0.5 text-xs leading-[1.2] text-foreground-tertiary",
	emptyState: "flex h-full min-h-40 items-center justify-center px-3 text-center text-foreground-tertiary text-[14px]",
	addFooter: "shrink-0 px-2.5 pb-2.5",
	addButton: "flex h-8 w-full items-center justify-center gap-1.5 rounded-lg border border-border bg-transparent px-2.5 py-0 text-xs text-foreground shadow-none transition-colors hover:border-border-strong hover:bg-accent/50 disabled:pointer-events-none disabled:opacity-40",
	item: providerListItemFrame,
	itemSelected: "bg-muted",
	itemIdle: "hover:bg-muted",
	itemMain: "flex min-w-0 flex-1 items-center gap-0",
	itemIdentity: "flex min-w-0 flex-1 items-center gap-2.5",
	itemDragHandle: "flex w-2.5 shrink-0 items-center justify-center text-muted-foreground opacity-0 transition-opacity duration-150 group-hover/row:opacity-100 group-focus-within/row:opacity-100 data-[dragging=true]:opacity-100",
	itemAvatar: "shrink-0 rounded-md border border-border-subtle [&_[data-slot=avatar-fallback]]:rounded-[inherit] [&_[data-slot=avatar-image]]:rounded-[inherit]",
	itemLabel: "min-w-0 truncate text-sm leading-[1.35] text-foreground font-normal group-data-[selected=true]/row:font-medium",
	itemTrailingSlot: "relative -mr-1 ml-1 flex shrink-0 items-center justify-center",
	itemTrailingSlotAction: "size-5",
	itemTrailingSlotIndicatorOnly: "size-2",
	itemMenuContent: "w-fit min-w-32 rounded-xl p-1.5",
	itemMenuEntry: "h-8 rounded-lg px-2.5 text-sm",
	groupHeader: cn(providerListItemFrame, "group/row pr-2.5 hover:bg-muted"),
	groupHeaderHasSelected: "bg-muted",
	groupTrailing: "relative ml-2 flex size-3 shrink-0 items-center justify-center",
	groupChevron: "shrink-0 text-muted-foreground transition-transform duration-150",
	groupChevronOpen: "rotate-90",
	groupChevronHiddenUntilHover: "absolute opacity-0 transition-[opacity,transform] group-hover/row:opacity-100 group-focus-within/row:opacity-100",
	groupEnabledDot: "pointer-events-none absolute size-1.5 rounded-full bg-success transition-opacity group-hover/row:opacity-0 group-focus-within/row:opacity-0",
	groupBody: "mt-1 flex flex-col gap-2 pl-3.5",
	itemMoreActions: "absolute top-1/2 right-0 flex size-5 -translate-y-1/2 items-center justify-center rounded-md text-muted-foreground opacity-0 transition-[color,opacity,background-color] hover:bg-accent/40 hover:text-foreground group-hover/row:opacity-100 group-focus-within/row:opacity-100 focus-visible:opacity-100",
	itemEnabledDot: "pointer-events-none absolute top-1/2 right-1.5 size-1.5 -translate-y-1/2 rounded-full bg-success transition-opacity group-hover/row:opacity-0 group-focus-within/row:opacity-0",
	groupAddRow: "flex w-full items-center gap-2 rounded-[10px] border border-dashed border-border-subtle bg-transparent px-2 py-[6px] text-xs leading-[1.35] text-muted-foreground shadow-none transition-colors hover:border-border hover:bg-accent/40 hover:text-foreground",
	disclosureToggle: "flex w-full items-center gap-1.5 rounded-md bg-transparent px-1 py-1 text-left text-xs leading-none text-muted-foreground shadow-none outline-none transition-colors hover:text-foreground focus-visible:ring-0",
	disclosureChevron: "size-3 shrink-0 text-muted-foreground transition-transform duration-150",
	disclosureChevronOpen: "rotate-90",
	disclosureBody: "mt-2 flex flex-col gap-3 pl-1"
};
const customHeaderDrawerClasses = {
	bodyScroll: "flex flex-col gap-4",
	headersJsonEditor: "min-h-[120px] w-full resize-y rounded-xl border border-border-subtle bg-muted/50 px-3 py-2.5 font-mono text-xs leading-relaxed text-foreground shadow-none outline-none focus-visible:border-ring placeholder:text-muted-foreground",
	headerList: "flex flex-col gap-2",
	headerRow: "grid grid-cols-[minmax(0,1fr)_minmax(0,1.4fr)_auto] items-center gap-2",
	removeIconButton: "size-7 shrink-0 rounded-lg text-muted-foreground shadow-none transition-colors hover:bg-accent hover:text-destructive [&_svg]:size-3.5",
	addRowButton: "flex h-auto w-full items-center justify-center gap-1.5 rounded-xl border border-dashed border-border-subtle py-2 text-xs text-muted-foreground shadow-none transition-colors hover:border-border-strong hover:bg-accent/40 hover:text-foreground"
};
const drawerClasses = {
	form: "flex min-h-0 flex-col gap-4 py-0",
	section: "space-y-3",
	sectionCard: "space-y-3.5 rounded-lg border border-border bg-background px-3 py-3 text-foreground shadow-none",
	sectionDescription: "text-xs leading-tight text-muted-foreground",
	fieldList: "space-y-3.5",
	field: "space-y-1.5",
	fieldTitle: "text-sm leading-5 text-muted-foreground",
	input: "h-8 min-h-8 w-full rounded-md border border-input bg-background px-3 py-1 text-sm leading-5 text-foreground shadow-none outline-none transition-[border-color,box-shadow] placeholder:text-muted-foreground disabled:cursor-not-allowed disabled:opacity-60 focus-visible:border-ring",
	inputDisabled: "bg-muted text-foreground-disabled",
	selectTrigger: "h-auto w-full rounded-md border-input bg-background px-3 py-2 text-sm leading-5 text-foreground shadow-none data-[placeholder]:text-muted-foreground focus-visible:border-ring",
	selectContent: "rounded-lg border-[0.5px] border-border bg-popover text-popover-foreground shadow-lg",
	helpText: "text-xs leading-tight text-muted-foreground",
	errorText: "text-xs leading-tight text-destructive",
	emptyInline: "rounded-md border border-dashed border-border-subtle px-3 py-2 text-[13px] leading-tight text-muted-foreground",
	toggleButton: "h-auto justify-start gap-1.5 px-0 py-0 text-sm leading-5 text-muted-foreground shadow-none hover:bg-transparent hover:text-foreground",
	inlineRow: "flex flex-wrap items-center gap-2",
	valueRow: "flex min-w-0 items-center gap-2",
	responsiveValueRow: "flex min-w-0 flex-col items-stretch gap-2 sm:flex-row sm:items-center",
	valueSuffix: "shrink-0 text-xs leading-tight text-foreground-tertiary",
	divider: "h-px bg-border-subtle",
	switchCard: "rounded-lg border border-border bg-background px-3 py-3",
	endpointChipRow: "flex min-w-0 flex-wrap items-center gap-2",
	splitFooter: "flex w-full items-center justify-between gap-3",
	footer: "flex items-center justify-end gap-2",
	footerTextButton: "h-auto min-h-0 rounded-md px-0 py-0 text-xs leading-tight text-muted-foreground shadow-none hover:bg-transparent hover:text-foreground",
	healthCostWarning: "shrink-0 rounded-lg border-warning-border bg-warning-subtle px-3 py-2.5 text-xs leading-tight text-warning-subtle-foreground shadow-none [&_[data-slot=alert-icon]]:mt-0 [&_[data-slot=alert-icon]_svg]:size-4 [&_[data-slot=alert-message]]:font-medium",
	healthProgressTrack: "h-1.5 w-full overflow-hidden rounded-full bg-[color:color-mix(in_srgb,var(--muted-foreground)_12%,transparent)]",
	healthProgressFill: "h-full rounded-full bg-primary transition-[width] duration-300 ease-out",
	healthProgressMeta: "text-[13px] tabular-nums text-foreground-tertiary",
	healthProgressCurrent: "truncate text-[13px] text-foreground"
};
const modelListClasses = {
	cqRoot: "flex h-full min-h-0 min-w-0 w-full flex-1 flex-col gap-2.5",
	section: "flex h-full min-h-0 min-w-0 w-full flex-1 flex-col gap-2.5",
	headerBlock: "flex min-h-0 min-w-0 w-full flex-1 flex-col gap-3",
	titleRow: "flex min-w-0 w-full flex-wrap items-center justify-between gap-2.5",
	headerInlineRow: "flex min-w-0 w-full flex-wrap items-center justify-between gap-2",
	headerInlineActions: "flex max-w-full shrink-0 flex-wrap items-center justify-end gap-2",
	headerToolStack: "flex min-w-0 w-full flex-col gap-2",
	titleWrap: "flex w-full min-w-0 items-center gap-3",
	titleActions: "flex max-w-full shrink-0 flex-wrap items-center justify-end gap-2",
	toolbarDesignIcon: "size-4 shrink-0",
	subsectionIconButton: "size-8 rounded-lg text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground",
	subsectionIcon: "size-4",
	toolbarButtonGroup: "max-w-full shrink-0",
	sectionTitleLine: "flex min-w-0 flex-wrap items-center gap-x-1 gap-y-1",
	sectionTitle: cn(sectionHeadingBase, "shrink-0 whitespace-nowrap font-semibold"),
	titleHelpRow: "ml-1 flex min-w-0 flex-wrap items-center gap-x-1.5 self-center text-foreground-tertiary",
	titleHelpText: "shrink-0 opacity-60",
	titleHelpLink: "mx-0 inline-flex shrink-0 items-center leading-[1.3] !text-link transition-colors hover:underline",
	titleHelpIconLink: "inline-flex h-[1.3em] w-[1.3em] shrink-0 items-center justify-center rounded-md align-middle text-link transition-colors hover:underline",
	titleHelpIcon: "relative -top-px size-3.5 shrink-0",
	titleHelpSeparator: "inline-flex shrink-0 items-center leading-[1.3] text-foreground-tertiary",
	countMeta: "text-xs leading-tight text-foreground-tertiary tabular-nums",
	toolbarGhost: "h-auto rounded-3xs px-2.5 py-[5px] text-[13px] leading-tight text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground",
	toolbarHeaderGhost: "h-auto min-h-0 rounded px-2 py-0.5 text-xs leading-tight text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground",
	toolbarHeaderIconButton: "size-8 rounded p-0 text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground",
	toolbarIcon: "size-3 shrink-0",
	toolbarHeaderIcon: "size-3 shrink-0",
	searchExpandRow: "flex min-w-0 w-full flex-wrap items-center gap-2",
	searchRow: "flex min-w-0 w-full flex-wrap items-center gap-2",
	searchActions: "flex max-w-full shrink-0 flex-wrap items-center gap-2",
	searchWrap: "flex h-8 min-w-0 flex-1 items-center gap-1 rounded-[10px] border border-border-subtle bg-background px-2.5 py-1",
	searchCompactWrap: "flex h-8 w-[min(38vw,220px)] min-w-36 items-center gap-1 rounded-[10px] border border-border-subtle bg-background px-2.5 py-1",
	searchIconButton: "inline-flex size-6 shrink-0 items-center justify-center rounded-md bg-transparent text-muted-foreground shadow-none transition-colors hover:bg-accent/40 hover:text-foreground disabled:opacity-40",
	groupToggleIconButton: "inline-flex size-6 shrink-0 items-center justify-center rounded-md bg-transparent text-muted-foreground shadow-none transition-colors hover:text-foreground disabled:opacity-40",
	searchIcon: "size-3 shrink-0 text-muted-foreground",
	searchInput: "min-w-0 flex-1 border-none bg-transparent text-sm leading-5 text-foreground outline-none placeholder:text-muted-foreground disabled:cursor-not-allowed disabled:opacity-60",
	searchClear: "flex h-[18px] w-[18px] items-center justify-center rounded-full text-muted-foreground transition-colors hover:bg-accent/40 hover:text-foreground",
	fetchActionButton: "h-8 min-h-0 gap-1.5 rounded-md border-border-subtle border-r-border bg-background px-2.5 py-0 text-sm leading-5 text-foreground shadow-none hover:bg-accent/40 hover:text-foreground disabled:opacity-40 [&_svg]:size-3.5",
	fetchGuideWrap: "relative flex shrink-0",
	fetchGuideArrow: "animation-provider-model-pull-guide pointer-events-none absolute top-1/2 right-full z-10 mr-2 flex h-4 w-7 items-center justify-end text-muted-foreground motion-reduce:animate-none",
	addModelIconButton: "size-8 min-h-0 rounded-md border-border-subtle bg-background p-0 text-foreground shadow-none hover:bg-accent/40 hover:text-foreground disabled:opacity-40 [&_svg]:size-3.5",
	addIconButton: "size-8 rounded-lg border-border-subtle bg-transparent text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground",
	emptyState: "flex min-h-40 items-center justify-center rounded-2xl border border-border border-dashed bg-muted/30 px-4 text-center text-sm leading-5 text-foreground-tertiary",
	listScroller: "min-h-0 min-w-0 w-full flex-1 overflow-x-hidden pt-1",
	virtualGroupRow: "",
	virtualGroupRowCollapsed: "mb-2.5",
	virtualModelRow: "border-x border-border-subtle bg-transparent",
	virtualModelRowLast: "mb-2.5 rounded-b-lg border-b border-border-subtle pb-1",
	manageListGroupShell: "mb-1",
	manageListGroupHeader: "flex items-center gap-1.5 px-1 py-[3px]",
	manageListGroupTitle: "text-xs leading-tight text-foreground-tertiary",
	manageListGroupRule: "h-px min-w-0 flex-1 bg-muted/50",
	manageListRow: "group flex items-center gap-2 rounded-lg px-1.5 py-[5px] transition-colors hover:bg-accent/50",
	manageListRowLast: "mb-0.5",
	manageDrawerFilterChipBase: "h-auto min-h-0 rounded-full px-2 py-[2px] text-xs transition-colors",
	manageDrawerFilterChipActive: "bg-accent/50 !text-accent-foreground",
	manageDrawerFilterChipIdle: "text-muted-foreground hover:bg-accent/50 hover:text-foreground",
	manageDrawerCapChipBase: "h-auto min-h-0 min-w-0 items-center gap-[3px] rounded-full px-1.5 py-[2px] text-xs transition-colors",
	manageDrawerCapChipActive: "bg-accent/50 !text-accent-foreground",
	manageDrawerCapChipIdle: "text-muted-foreground hover:bg-accent/50 hover:text-foreground",
	manageDrawerCountBadge: "shrink-0 rounded-full bg-muted/50 px-1.5 py-[1px] text-xs text-foreground-tertiary tabular-nums",
	manageDrawerCloseInTitle: "ml-1 !size-6 !min-h-6 shrink-0 gap-0 rounded-lg p-0 text-muted-foreground shadow-none hover:bg-accent hover:text-foreground [&_svg:not([class*='size-'])]:size-[11px]",
	manageDrawerBulkGhost: "inline-flex !h-auto !min-h-0 items-center justify-center gap-1 rounded-lg px-1.5 py-[2px] text-xs tracking-[-0.14px] text-muted-foreground shadow-none transition-colors hover:bg-accent has-[>svg]:px-1.5",
	manageDrawerBulkGhostEnableHover: "hover:!text-primary",
	manageDrawerBulkGhostDisableHover: "hover:!text-destructive",
	groupCard: "group/modelGroup min-w-0 w-full overflow-hidden rounded-lg border border-border-subtle bg-transparent",
	groupCardOpen: "rounded-b-none border-b-0",
	groupHeader: "group/groupRow flex min-h-9 w-full items-center justify-between gap-2 bg-muted/30 px-4 text-left outline-none focus-visible:outline-none",
	groupHeaderOpen: "[border-bottom:0.5px_solid_var(--border-subtle)]",
	groupToggleButton: "flex min-w-0 max-w-full items-center gap-2 bg-transparent text-left outline-none focus-visible:outline-none",
	groupHeaderActions: "flex h-6 shrink-0 items-center gap-1",
	groupHeaderIconTooltipTrigger: "inline-flex h-6 shrink-0 items-center justify-center leading-none",
	groupTitle: "min-w-0 truncate text-sm leading-5 text-foreground",
	groupChevron: "size-4 shrink-0 text-muted-foreground transition-transform duration-150",
	groupChevronOpen: "rotate-90",
	groupBody: "grid overflow-hidden transition-[grid-template-rows] duration-200 ease-out",
	groupBodyOpen: "grid-rows-[1fr]",
	groupBodyClosed: "grid-rows-[0fr]",
	groupBodyInner: "min-h-0 overflow-hidden",
	groupBodyList: "flex flex-col gap-0",
	groupOverflowHint: "mt-1 rounded-lg px-3 py-2 text-left text-[13px] leading-tight text-muted-foreground transition-colors hover:bg-accent/40 hover:text-foreground",
	row: "group flex min-h-[42px] items-center gap-2.5 px-4 py-1 text-foreground leading-none",
	rowMain: "min-w-0 flex-1 items-center gap-2.5 self-center",
	rowAvatar: "inline-flex size-6.5 shrink-0 items-center justify-center overflow-hidden rounded-full border border-border [&_*]:overflow-hidden [&_*]:rounded-[inherit] [&_img]:rounded-[inherit] [&_svg]:rounded-[inherit]",
	rowBody: "flex min-w-0 max-w-full flex-1 items-center overflow-hidden",
	rowNameCopyable: "cursor-pointer",
	modelIdBadge: "min-w-0 max-w-[50%] shrink truncate rounded-md bg-background-subtle px-1.5 py-[1px] font-mono text-xs text-foreground-tertiary leading-tight",
	rowBadges: "mt-1 flex min-h-[18px] min-w-0 max-w-full flex-wrap items-center gap-1.5",
	rowCapabilityStrip: "flex h-7 min-w-0 max-w-[min(100%,20rem)] shrink items-center gap-1.5 overflow-x-auto overflow-y-hidden [scrollbar-width:none] [-ms-overflow-style:none] [&::-webkit-scrollbar]:hidden",
	rowCapabilityTagCluster: "flex min-w-0 shrink items-center",
	rowMeta: "mt-[3px] block min-w-0 max-w-full truncate text-xs leading-tight text-foreground-tertiary",
	healthStatusSlot: "shrink-0",
	rowActionsCluster: "flex min-h-8 min-w-0 items-center gap-2",
	rowActions: "ml-auto min-w-0 shrink-0 items-center gap-2 self-center",
	rowInlineActions: "flex shrink-0 items-center gap-1",
	rowActionButton: "inline-flex size-[30px] shrink-0 items-center justify-center rounded-lg p-0 !text-muted-foreground shadow-none transition-colors hover:bg-accent/40 hover:!text-foreground",
	rowDangerActionButton: "hover:!text-foreground",
	rowIconButton: "size-7 rounded-lg border border-border-subtle bg-transparent text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground"
};
const modelSyncClasses = {
	panel: "flex min-h-0 flex-1 flex-col gap-4",
	summaryCard: "rounded-2xl border border-border-subtle bg-muted/30 px-4 py-3",
	summaryTitle: "text-sm leading-5 text-foreground",
	summaryMeta: "text-[13px] leading-tight text-muted-foreground",
	summaryGrid: "mt-3 grid gap-2 sm:grid-cols-3",
	summaryMetric: "rounded-xl border border-border-subtle bg-background/75 px-3 py-2 text-[13px] leading-tight text-foreground",
	section: "rounded-2xl border border-border-subtle bg-background px-4 py-4 shadow-none",
	sectionHeader: "flex flex-wrap items-center justify-between gap-3",
	sectionTitleWrap: "min-w-0",
	sectionTitle: "text-sm leading-5 text-foreground",
	sectionMeta: "text-[13px] leading-tight text-muted-foreground",
	sectionActions: "flex flex-wrap items-center gap-2",
	toggleButton: cn(actionClasses.btnBase, actionClasses.btnNeutral, "rounded-lg border-border-subtle px-3 py-1.25 text-muted-foreground hover:bg-accent/40 hover:text-foreground"),
	list: "mt-4 space-y-2",
	row: "flex items-start gap-3 rounded-xl border border-border-subtle bg-muted/30 px-3 py-3",
	rowBody: "min-w-0 flex-1",
	rowTitle: "truncate text-sm leading-5 text-foreground",
	rowMeta: "mt-1 text-[13px] leading-tight text-muted-foreground",
	rowBadgeRow: "mt-2 flex flex-wrap items-center gap-1.5",
	rowBadge: "rounded-full border border-border-subtle bg-background px-2 py-0.5 text-xs leading-tight text-muted-foreground",
	impactList: "mt-3 space-y-2",
	impactItem: "rounded-xl border border-border-subtle bg-background/80 px-3 py-2 text-[13px] leading-5 text-foreground",
	emptyState: "rounded-2xl border border-dashed border-border-subtle bg-muted/30 px-4 py-8 text-center text-sm leading-5 text-foreground-tertiary",
	footer: "flex items-center justify-end gap-2",
	fetchEmpty: "flex flex-col items-center justify-center px-4 py-12 text-center",
	fetchEmptyIconWrap: "mb-3 flex h-10 w-10 items-center justify-center rounded-full bg-muted",
	fetchEmptyIcon: "size-4 text-foreground-tertiary",
	fetchEmptyTitle: "text-xs leading-tight text-muted-foreground",
	fetchEmptyDescription: "mt-1 text-xs leading-tight text-foreground-tertiary",
	fetchSection: "min-w-0",
	fetchSectionHeader: "mb-2.5 flex items-center justify-between gap-3",
	fetchSectionTitleRow: "flex items-center gap-1.5",
	fetchDotNew: "h-[6px] w-[6px] shrink-0 rounded-full bg-primary",
	fetchDotRemoved: "h-[6px] w-[6px] shrink-0 rounded-full bg-destructive",
	fetchSectionTitle: "text-sm text-foreground leading-5",
	fetchSectionCount: "text-xs leading-tight text-foreground-tertiary tabular-nums",
	fetchGhostAll: "inline-flex !h-auto !min-h-0 items-center justify-center rounded-lg px-2 py-[3px] !text-xs !leading-none text-muted-foreground shadow-none hover:bg-accent hover:text-foreground",
	fetchGhostAllRemoved: "inline-flex !h-auto !min-h-0 items-center justify-center rounded-lg px-2 py-[3px] !text-xs !leading-none text-muted-foreground shadow-none hover:bg-destructive hover:text-destructive-foreground",
	fetchList: "space-y-1",
	fetchWarning: "my-2 gap-2 rounded-lg border-warning-border bg-warning-subtle px-2.5 py-2 text-warning-subtle-foreground text-xs leading-tight shadow-none [&_[data-slot=alert-icon]]:mt-0 [&_[data-slot=alert-icon]_svg]:size-3.5 [&_[data-slot=alert-message]]:font-normal",
	fetchRowNew: "flex min-h-11 cursor-pointer items-center gap-2 rounded-lg border border-transparent px-2.5 py-2 transition-colors hover:border-border-subtle hover:bg-accent/30 focus-visible:border-ring focus-visible:bg-accent/30 focus-visible:outline-none data-[checked=true]:border-border-subtle data-[checked=true]:bg-background",
	fetchRowRemoved: "flex min-h-11 cursor-pointer items-center gap-2 rounded-lg border border-transparent px-2.5 py-2 transition-colors hover:border-error-border hover:bg-error-subtle focus-visible:border-error-border focus-visible:bg-error-subtle focus-visible:outline-none data-[checked=true]:border-error-border data-[checked=true]:bg-background",
	fetchAvatar: "flex h-6 w-6 shrink-0 items-center justify-center rounded-md bg-muted text-xs leading-none text-foreground-tertiary",
	fetchRowId: "truncate text-sm leading-tight text-foreground",
	fetchRowIdStrike: "truncate text-sm leading-tight text-foreground-tertiary line-through decoration-foreground-tertiary",
	fetchContextValue: "shrink-0 text-xs leading-tight text-foreground-tertiary tabular-nums",
	fetchCapabilityStrip: "flex shrink-0 items-center justify-end gap-[3px]",
	manageTitle: "inline-flex min-w-0 items-center gap-2",
	manageTitleText: "min-w-0 truncate",
	manageTitleCountBadge: "h-5 rounded-md px-1.5 py-0 text-xs leading-5 tabular-nums",
	manageTitleActionButton: "h-8 rounded-lg px-2.5 text-xs text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground [&_svg]:size-3.5",
	manageTitleErrorRetryButton: "relative size-7 shrink-0 rounded-lg p-0 text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground [&_svg]:size-3.5",
	manageTitleErrorDot: "pointer-events-none absolute right-1.5 top-1.5 size-1.5 rounded-full bg-error ring-1 ring-card",
	manageStickyHeader: "sticky top-0 z-10 shrink-0 bg-card pt-1 pb-3",
	manageToolbar: "mb-2.5 flex items-center gap-2",
	manageSearchIcon: "pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground",
	manageSearchInput: "h-9 rounded-lg bg-muted/30 pr-7 pl-8 text-sm shadow-none",
	manageSearchClear: "absolute right-2 top-1/2 flex h-[18px] w-[18px] -translate-y-1/2 items-center justify-center rounded-full text-muted-foreground transition-colors hover:bg-accent/40 hover:text-foreground",
	manageIconButton: "size-8 rounded-lg text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground",
	manageTabs: "w-full gap-0",
	manageTabsList: "h-auto w-full max-w-full justify-start gap-1 overflow-x-auto overflow-y-hidden rounded-lg bg-muted/40 p-1 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden",
	manageTabsTrigger: "h-7 shrink-0 cursor-pointer gap-1.5 rounded-md px-2 text-xs",
	manageTabsTriggerDestructive: "text-error-subtle-foreground hover:text-error-subtle-foreground data-[state=active]:border-error-border data-[state=active]:bg-error-subtle data-[state=active]:text-error-subtle-foreground dark:text-error-subtle-foreground dark:data-[state=active]:border-error-border dark:data-[state=active]:bg-error-subtle dark:data-[state=active]:text-error-subtle-foreground",
	manageTabCount: "text-[11px] text-foreground-tertiary tabular-nums",
	manageScrollArea: "min-h-0 flex-1 pr-1 [scrollbar-gutter:stable]",
	manageLoading: "flex min-h-52 items-center justify-center text-muted-foreground",
	manageList: "pb-1",
	manageGroup: "contents",
	manageVirtualGroupRow: "bg-card pb-1",
	manageVirtualModelRow: "bg-card pb-1",
	manageGroupHeader: "sticky top-0 z-30 mt-2.5 first:mt-0",
	manageGroupHeaderSurface: "flex min-h-9 items-center gap-2 border-b border-border-subtle bg-card py-0 text-left transition-colors",
	manageGroupHeaderSurfaceCollapsed: "",
	manageGroupToggle: "flex min-w-0 flex-1 items-center gap-2 text-left",
	manageGroupChevron: "size-4 shrink-0 text-muted-foreground transition-transform",
	manageGroupTitle: "min-w-0 truncate text-sm font-semibold leading-5 text-foreground",
	manageGroupBadge: "h-4 rounded-md px-1.5 py-0 text-[10px] leading-4",
	manageGroupBody: "space-y-1 overflow-hidden bg-card py-1.5",
	manageRow: "flex min-h-10 items-center gap-2 rounded-md px-2 py-1 transition-colors hover:bg-accent/30 data-[added=true]:bg-success-subtle",
	manageRowTitleLine: "flex min-w-0 items-center gap-1.5",
	manageRowTitle: "truncate text-sm leading-5 text-foreground select-text",
	manageRowDescriptionHelp: "inline-flex size-4 shrink-0 items-center justify-center rounded text-muted-foreground outline-none transition-colors hover:bg-accent/40 hover:text-foreground focus-visible:bg-accent/40 focus-visible:text-foreground",
	manageStaleBadge: "h-4 shrink-0 rounded-md border border-error-border bg-error-subtle px-1.5 py-0 text-[10px] leading-4 text-error-subtle-foreground tabular-nums",
	manageRowAction: "size-8 rounded-lg text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground"
};
const apiKeyListClasses = {
	summaryMeta: "text-xs leading-tight text-foreground-tertiary tabular-nums",
	helperText: "text-[13px] leading-tight text-muted-foreground",
	listWrap: "overflow-hidden rounded-lg border border-border-subtle",
	listScroller: "max-h-[60vh] overflow-x-hidden",
	keyRow: "flex flex-col gap-2 border-b border-border-subtle px-4 py-3 last:border-b-0",
	keyDisplayRow: "flex min-w-0 items-center gap-3",
	keyTextBlock: "min-w-0 flex-1",
	keyRowActions: "flex shrink-0 items-center gap-1.5",
	keyLabel: "min-w-0 truncate text-[13px] leading-tight text-foreground",
	keyValue: "min-w-0 flex-1 truncate font-mono text-xs leading-tight text-foreground-tertiary",
	keyDraftRow: "flex min-w-0 items-center gap-2",
	keyDraftInputs: "grid min-w-0 flex-1 gap-2 sm:grid-cols-[minmax(4.5rem,6rem)_minmax(0,1fr)]",
	keyDraftInput: "h-8 rounded-md bg-background px-2.5 text-sm leading-5",
	keyIconButton: "inline-flex size-5 shrink-0 items-center justify-center rounded-md text-muted-foreground transition-colors hover:bg-accent/40 hover:text-foreground disabled:pointer-events-none disabled:opacity-30 [&_svg]:size-3",
	keySaveIconButton: "inline-flex size-5 shrink-0 items-center justify-center rounded-md text-muted-foreground transition-colors hover:bg-success-subtle hover:text-success-subtle-foreground disabled:pointer-events-none disabled:opacity-30 [&_svg]:size-3",
	keyDestructiveIconButton: "inline-flex size-5 shrink-0 items-center justify-center rounded-md text-muted-foreground transition-colors hover:bg-destructive hover:text-destructive-foreground disabled:pointer-events-none disabled:opacity-30 [&_svg]:size-3",
	actionRow: "flex items-center justify-between gap-3"
};
const oauthCardClasses = {
	container: "w-full min-w-0",
	shell: "w-full min-w-0 overflow-hidden rounded-xl border border-border-subtle px-3 py-2.5",
	loginFooterRow: "mt-2.5 flex items-center justify-center gap-4",
	loginFooterLink: "h-auto min-h-0 p-0 text-xs text-muted-foreground shadow-none hover:bg-transparent hover:text-foreground",
	loginFooterDivider: "text-xs text-foreground-tertiary",
	externalLink: "mt-1 inline-block text-xs leading-tight text-link",
	shellLoggedIn: "w-full min-w-0 overflow-hidden rounded-xl border border-border-subtle px-3 py-2.5",
	loggedInRow: "flex w-full min-w-0 flex-wrap items-center justify-between gap-3",
	profileMeta: "flex min-w-0 flex-1 items-center gap-3",
	avatarSm: "flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-primary text-xs font-semibold text-primary-foreground",
	nameBlock: "min-w-0",
	nameRow: "flex flex-wrap items-center gap-1.5",
	name: "truncate text-[15px] leading-[1.2] font-semibold tracking-tight text-foreground",
	loggedInName: "truncate text-xs leading-tight text-foreground",
	loggedInEmail: "mt-0.5 truncate text-xs leading-[1.35] text-foreground-tertiary",
	badge: "inline-flex items-center rounded border border-warning-border bg-warning-subtle px-1 py-[0.5px] text-[10px] leading-tight text-warning-subtle-foreground",
	loggedInActions: "flex shrink-0 flex-wrap items-center justify-end gap-2",
	inlineBalanceBlock: "text-right",
	inlineBalanceLabel: "text-xs text-muted-foreground",
	inlineBalanceValue: "text-sm leading-tight text-foreground tabular-nums",
	balanceValueSkeleton: "inline-block w-20",
	topupPrimaryButton: "h-auto min-h-0 px-2.5 py-[3px] text-xs shadow-none",
	logoutCompact: "h-auto min-h-0 rounded-md px-1.5 py-[3px] text-xs text-muted-foreground shadow-none hover:bg-transparent hover:text-foreground",
	serviceAttribution: "mt-2.5 border-t border-border-subtle pt-2.5 text-xs text-foreground-tertiary",
	serviceLink: "text-muted-foreground transition-colors hover:text-foreground",
	actionsRow: "flex flex-wrap items-center gap-2",
	footer: "mt-4 text-[12px] leading-[1.35] text-foreground-tertiary"
};
var fieldIconButtonBase = "flex shrink-0 items-center justify-center rounded-lg border border-border-subtle bg-background text-muted-foreground transition-colors hover:bg-accent/40 hover:text-foreground disabled:pointer-events-none disabled:opacity-40";
const fieldClasses = {
	inputRow: "flex min-w-0 items-center gap-1.5",
	inputRowEndSlot: "inline-flex h-6 w-6 shrink-0",
	inputGroup: ["flex min-h-0 min-w-0 flex-1 items-center", providerSettingsInputGroupBase].join(" "),
	inputGroupBlock: ["flex w-full items-center", providerSettingsInputGroupBase].join(" "),
	input: "min-h-0 h-auto min-w-0 flex-1 border-0 bg-transparent p-0 shadow-none outline-none focus-visible:ring-0 text-sm leading-5 text-foreground placeholder:text-muted-foreground md:text-sm",
	iconButton: cn(fieldIconButtonBase, "size-6"),
	inputActionButton: cn(fieldIconButtonBase, "size-8"),
	apiKeyVisibilityToggle: "flex size-5 shrink-0 items-center justify-center text-muted-foreground transition-colors hover:text-foreground disabled:pointer-events-none disabled:opacity-40",
	titleWithHelp: "flex min-w-0 flex-wrap items-baseline gap-x-2 gap-y-1",
	titleHelpLink: "mx-0 inline-flex shrink-0 items-center font-normal text-xs leading-5 text-link transition-colors hover:underline"
};
function ProviderSettingsContainer({ className, children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Scrollbar_default, {
		className: cn("flex min-w-0 flex-1 flex-col [scrollbar-width:none] [&::-webkit-scrollbar]:hidden", className),
		children
	});
}
function ProviderSettingsSubtitle({ children, className }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.provider-settings-subtitle",
		className: cn("mt-4 select-none font-semibold text-foreground", providerSettingsTypography.subtitle, className),
		children
	});
}
function ProviderHelpText({ children, className }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.provider-help-text",
		className: cn("text-foreground opacity-40", providerSettingsTypography.label, className),
		children
	});
}
function ProviderHelpTextRow({ children, className }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.provider-help-text-row",
		className: cn("flex flex-row items-center py-1.25", className),
		children
	});
}
function ProviderHelpLink({ children, className, ...props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
		"data-ui": "settings.provider-help-link",
		className: cn("mx-[5px] cursor-pointer text-link", providerSettingsTypography.label, className),
		...mergeUiProps(props, "settings.provider-help-link"),
		children
	});
}
async function loadOvmsSupport() {
	try {
		return await ipcApi.request("ovms.is_supported");
	} catch {
		return false;
	}
}
function useOvmsSupport() {
	const { data } = useSWRImmutable("ovms/isSupported", loadOvmsSupport);
	return { isSupported: data };
}
var logger$24 = loggerService.withContext("ProviderLogoPicker");
var ProviderLogoPicker = ({ onProviderClick }) => {
	const { t } = useTranslation();
	const [searchText, setSearchText] = (0, import_react.useState)("");
	const [iconCatalog, setIconCatalog] = (0, import_react.useState)();
	(0, import_react.useEffect)(() => {
		let cancelled = false;
		loadProviderIconCatalog().then((catalog) => {
			if (!cancelled) setIconCatalog(catalog);
		}).catch((error) => {
			logger$24.warn("Failed to load provider icon catalog:", error);
		});
		return () => {
			cancelled = true;
		};
	}, []);
	const filteredProviders = (0, import_react.useMemo)(() => {
		const providers = Object.keys(PROVIDER_ICON_META_CATALOG).map((id) => ({
			id,
			icon: iconCatalog?.[id],
			name: isSystemProviderId(id) ? t(getProviderLabelKey(id)) : id
		}));
		if (!searchText) return providers;
		const searchLower = searchText.toLowerCase();
		return providers.filter((p) => p.name.toLowerCase().includes(searchLower));
	}, [
		searchText,
		t,
		iconCatalog
	]);
	const handleProviderClick = (event, providerId) => {
		event.stopPropagation();
		onProviderClick(providerId);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "ui.provider-logo-picker",
		className: "flex max-h-[300px] w-[350px] flex-col",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(InputGroup, {
			className: "mb-3",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupAddon, {
				align: "inline-start",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Search, {})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupInput, {
				placeholder: t("common.search"),
				value: searchText,
				onChange: (event) => setSearchText(event.target.value)
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "grid flex-1 grid-cols-5 gap-2 overflow-y-auto p-1",
			children: filteredProviders.map(({ id, name, icon }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
				content: name,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
					type: "button",
					"aria-label": name,
					className: "flex size-[52px] shrink-0 items-center justify-center overflow-hidden rounded-full border border-border-subtle bg-muted/50 transition-all hover:scale-105 hover:border-primary hover:bg-muted",
					onClick: (event) => handleProviderClick(event, id),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderAvatarPrimitive, {
						providerId: id,
						style: {
							width: "52px",
							height: "52px"
						},
						providerName: name,
						logo: icon
					})
				})
			}, id))
		})]
	});
};
var ProviderLogoPicker_default = ProviderLogoPicker;
function ProviderAvatar({ provider, size, className, style, displayContext }) {
	const systemIconRef = resolveProviderIconRef(provider.id);
	const systemIcon = useIcon(systemIconRef);
	const customLogo = systemIconRef ? void 0 : provider.logo ?? provider.logoSrc;
	const displayIconId = typeof customLogo === "string" && customLogo.startsWith("icon:") ? customLogo.slice(5) : provider.id;
	const displayConfig = displayContext ? getIconDisplayConfig(displayContext, displayIconId) : void 0;
	const iconStyle = displayConfig ? {
		width: `${displayConfig.scale * 100}%`,
		height: `${displayConfig.scale * 100}%`,
		flexShrink: 0,
		borderRadius: displayConfig.borderRadius === void 0 ? void 0 : `${displayConfig.borderRadius}px`,
		overflow: displayConfig.borderRadius === void 0 ? void 0 : "hidden"
	} : void 0;
	if (systemIconRef) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderAvatarPrimitive, {
		providerId: provider.id,
		providerName: provider.name,
		logo: systemIcon,
		size,
		className,
		style,
		iconStyle
	});
	if (customLogo) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderAvatarPrimitive, {
		providerId: provider.id,
		providerName: provider.name,
		logo: customLogo,
		size,
		className,
		style,
		iconStyle
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderAvatarPrimitive, {
		providerId: provider.id,
		providerName: provider.name,
		size,
		className,
		style,
		iconStyle
	});
}
function ProviderSettingsDrawer({ open, onClose, title, titleActions, description, footer, children, bodyClassName, contentClassName, headerClassName, footerClassName, showHeaderCloseButton = true }) {
	const { t } = useTranslation();
	const header = description || titleActions ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-settings-drawer",
		className: "flex min-w-0 flex-1 items-center gap-3",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex min-w-0 flex-1 flex-col gap-1",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "font-semibold text-base text-foreground",
				children: title
			}), description ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "text-muted-foreground text-xs leading-tight",
				children: description
			}) : null]
		}), titleActions ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "flex shrink-0 items-center",
			children: titleActions
		}) : null]
	}) : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageSidePanel, {
		open,
		onClose,
		title: header ? void 0 : title,
		header,
		footer,
		closeLabel: t("common.close"),
		showCloseButton: showHeaderCloseButton,
		contentClassName,
		headerClassName,
		bodyClassName,
		footerClassName,
		children
	});
}
const CUSTOM_PROVIDER_TEXT_ENDPOINTS = [
	ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS,
	ENDPOINT_TYPE.OPENAI_RESPONSES,
	ENDPOINT_TYPE.ANTHROPIC_MESSAGES,
	ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT
];
const CUSTOM_PROVIDER_IMAGE_ENDPOINTS = [ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION, ENDPOINT_TYPE.OPENAI_IMAGE_EDIT];
const CUSTOM_PROVIDER_ENDPOINTS = [...CUSTOM_PROVIDER_TEXT_ENDPOINTS, ...CUSTOM_PROVIDER_IMAGE_ENDPOINTS];
var ENDPOINT_PATHS = {
	[ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS]: "/chat/completions",
	[ENDPOINT_TYPE.OPENAI_RESPONSES]: "/responses",
	[ENDPOINT_TYPE.ANTHROPIC_MESSAGES]: "/messages",
	[ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT]: "/models/{model}:generateContent",
	[ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION]: "/images/generations",
	[ENDPOINT_TYPE.OPENAI_IMAGE_EDIT]: "/images/edits"
};
function getCustomProviderDefaultChatEndpoint(endpointUrls, preferredChatEndpoint) {
	if (preferredChatEndpoint && trim(endpointUrls[preferredChatEndpoint])) return preferredChatEndpoint;
	return CUSTOM_PROVIDER_TEXT_ENDPOINTS.find((endpointType) => trim(endpointUrls[endpointType])) ?? ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS;
}
function buildCustomProviderCreationPayload(input) {
	const endpointConfigs = {};
	for (const endpointType of CUSTOM_PROVIDER_ENDPOINTS) {
		const baseUrl = trim(input.endpointUrls[endpointType]);
		if (baseUrl) endpointConfigs[endpointType] = { baseUrl };
	}
	return {
		defaultChatEndpoint: getCustomProviderDefaultChatEndpoint(input.endpointUrls, input.preferredChatEndpoint),
		endpointConfigs
	};
}
function buildCustomProviderEndpointPreview(baseUrl, endpointType) {
	const value = trim(baseUrl);
	if (!value || !validateApiHost(value)) return "";
	return `${endpointType === ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT ? formatApiHost(value, true, "v1beta") : formatApiHost(value)}${ENDPOINT_PATHS[endpointType]}`;
}
function findInvalidCustomProviderCreationUrl(input) {
	if (!CUSTOM_PROVIDER_TEXT_ENDPOINTS.some((endpointType) => trim(input.endpointUrls[endpointType]))) return { field: "textEndpointRequired" };
	return findInvalidCustomProviderEndpointUrl(input.endpointUrls);
}
function findInvalidCustomProviderEndpointUrl(endpointUrls) {
	for (const endpointType of CUSTOM_PROVIDER_ENDPOINTS) {
		const value = trim(endpointUrls[endpointType]);
		if (value && !validateApiHost(value)) return {
			field: "endpointUrl",
			endpointType
		};
	}
	return null;
}
var logger$23 = loggerService.withContext("ProviderEditorDrawer");
var COMMON_CUSTOM_PROVIDER_ENDPOINTS = [ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS, ENDPOINT_TYPE.ANTHROPIC_MESSAGES];
var ADDITIONAL_CUSTOM_PROVIDER_ENDPOINTS = [
	ENDPOINT_TYPE.OPENAI_RESPONSES,
	ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT,
	ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION,
	ENDPOINT_TYPE.OPENAI_IMAGE_EDIT
];
var SECONDARY_ENDPOINT_LABELS = [
	{
		type: ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS,
		labelKey: "settings.provider.more_endpoints.openai_chat"
	},
	{
		type: ENDPOINT_TYPE.ANTHROPIC_MESSAGES,
		labelKey: "settings.provider.more_endpoints.anthropic"
	},
	{
		type: ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT,
		labelKey: "settings.provider.more_endpoints.gemini"
	},
	{
		type: ENDPOINT_TYPE.OPENAI_RESPONSES,
		labelKey: "settings.provider.more_endpoints.openai_responses"
	}
];
function emptyAuthConfigFor(authType) {
	switch (authType) {
		case "iam-azure": return {
			type: "iam-azure",
			apiVersion: ""
		};
		case "iam-aws": return {
			type: "iam-aws",
			region: ""
		};
		case "api-key-aws": return {
			type: "api-key-aws",
			region: ""
		};
		case "iam-gcp": return {
			type: "iam-gcp",
			project: "",
			location: ""
		};
		case "oauth": return {
			type: "oauth",
			clientId: ""
		};
		case "api-key":
		default: return { type: "api-key" };
	}
}
function duplicateNeedsBaseUrl(authType) {
	return authType === "api-key" || authType === "iam-azure";
}
function isCustomProviderTextEndpoint(endpointType) {
	return CUSTOM_PROVIDER_TEXT_ENDPOINTS.some((type) => type === endpointType);
}
function mergeSecondaryEndpoints(target, secondaryUrls, primary) {
	for (const type of CUSTOM_PROVIDER_ENDPOINTS) {
		if (type === primary) continue;
		const value = secondaryUrls[type]?.trim();
		if (value) target[type] = { baseUrl: value };
	}
}
function ProviderEditorDrawer({ open, mode, initialLogo, presetSources = [], onClose, onSelectPreset, onSubmit }) {
	const { t } = useTranslation();
	const uploadInputRef = (0, import_react.useRef)(null);
	const firstTextEndpointRef = (0, import_react.useRef)(null);
	const [name, setName] = (0, import_react.useState)("");
	const [baseUrl, setBaseUrl] = (0, import_react.useState)("");
	const [apiKey, setApiKey] = (0, import_react.useState)("");
	const [secondaryUrls, setSecondaryUrls] = (0, import_react.useState)({});
	const [moreEndpointsOpen, setMoreEndpointsOpen] = (0, import_react.useState)(false);
	const [endpointUrls, setEndpointUrls] = (0, import_react.useState)({});
	const [preferredChatEndpoint, setPreferredChatEndpoint] = (0, import_react.useState)(ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS);
	const [invalidCreationUrl, setInvalidCreationUrl] = (0, import_react.useState)(null);
	const [logo, setLogo] = (0, import_react.useState)(null);
	const [stagedFile, setStagedFile] = (0, import_react.useState)(null);
	const [logoDirty, setLogoDirty] = (0, import_react.useState)(false);
	const [logoPickerOpen, setLogoPickerOpen] = (0, import_react.useState)(false);
	const [isSubmitting, setIsSubmitting] = (0, import_react.useState)(false);
	const [nameTouched, setNameTouched] = (0, import_react.useState)(false);
	const [baseUrlTouched, setBaseUrlTouched] = (0, import_react.useState)(false);
	const previousOpenRef = (0, import_react.useRef)(false);
	const previewObjectUrlRef = (0, import_react.useRef)(null);
	const revokePreviewObjectUrl = () => {
		if (previewObjectUrlRef.current) {
			URL.revokeObjectURL(previewObjectUrlRef.current);
			previewObjectUrlRef.current = null;
		}
	};
	(0, import_react.useEffect)(() => () => revokePreviewObjectUrl(), []);
	const editingProvider = mode?.kind === "edit" ? mode.provider : null;
	const duplicateSource = mode?.kind === "duplicate" ? mode.source : null;
	const urlForm = (() => {
		if (!mode || mode.kind === "edit" || mode.kind === "create-custom") return null;
		if (!duplicateNeedsBaseUrl(mode.source.authType)) return null;
		return {
			primary: mode.source.defaultChatEndpoint ?? ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS,
			requireBaseUrl: false
		};
	})();
	const duplicateUsesEndpointFields = Boolean(duplicateSource && urlForm && (duplicateSource.presetProviderId === "new-api" || isCustomProviderTextEndpoint(urlForm.primary)));
	const duplicateDefaultTextEndpoint = urlForm && isCustomProviderTextEndpoint(urlForm.primary) ? urlForm.primary : ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS;
	const duplicatePrimaryEndpoints = [duplicateDefaultTextEndpoint, ...COMMON_CUSTOM_PROVIDER_ENDPOINTS.filter((endpointType) => endpointType !== duplicateDefaultTextEndpoint)].slice(0, 2);
	const duplicateAdditionalEndpoints = CUSTOM_PROVIDER_ENDPOINTS.filter((endpointType) => !duplicatePrimaryEndpoints.some((primaryEndpoint) => primaryEndpoint === endpointType));
	const duplicateEndpointUrls = (0, import_react.useMemo)(() => {
		if (!duplicateUsesEndpointFields || !urlForm) return {};
		const values = {};
		for (const endpointType of CUSTOM_PROVIDER_ENDPOINTS) {
			const override = secondaryUrls[endpointType];
			if (override?.trim()) values[endpointType] = override;
		}
		if (duplicateSource?.presetProviderId === "new-api") {
			for (const endpointType of CUSTOM_PROVIDER_TEXT_ENDPOINTS) if (!values[endpointType]?.trim()) values[endpointType] = baseUrl;
		} else values[duplicateDefaultTextEndpoint] = baseUrl;
		return values;
	}, [
		baseUrl,
		duplicateDefaultTextEndpoint,
		duplicateSource?.presetProviderId,
		duplicateUsesEndpointFields,
		secondaryUrls,
		urlForm
	]);
	(0, import_react.useEffect)(() => {
		const wasOpen = previousOpenRef.current;
		previousOpenRef.current = open;
		if (!open || wasOpen) return;
		setName(editingProvider?.name ?? "");
		setNameTouched(false);
		setBaseUrl("");
		setBaseUrlTouched(false);
		setApiKey("");
		setSecondaryUrls({});
		setMoreEndpointsOpen(false);
		setEndpointUrls({});
		setPreferredChatEndpoint(duplicateSource?.defaultChatEndpoint && isCustomProviderTextEndpoint(duplicateSource.defaultChatEndpoint) ? duplicateSource.defaultChatEndpoint : ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS);
		setInvalidCreationUrl(null);
		setLogoDirty(false);
		setLogoPickerOpen(false);
		revokePreviewObjectUrl();
		setStagedFile(null);
	}, [
		open,
		editingProvider,
		duplicateSource
	]);
	(0, import_react.useEffect)(() => {
		if (!open || logoDirty) return;
		setLogo(initialLogo ?? null);
	}, [
		initialLogo,
		logoDirty,
		open
	]);
	const previewName = name.trim();
	const avatarBackgroundColor = (0, import_react.useMemo)(() => previewName ? generateColorFromChar(previewName) : void 0, [previewName]);
	const avatarForegroundColor = (0, import_react.useMemo)(() => avatarBackgroundColor ? getForegroundColor(avatarBackgroundColor) : void 0, [avatarBackgroundColor]);
	const handleUploadChange = (event) => {
		const file = event.target.files?.[0];
		event.target.value = "";
		if (!file) return;
		const sizeError = checkEntityImageSize(file);
		if (sizeError) {
			toast.error(sizeError);
			return;
		}
		revokePreviewObjectUrl();
		previewObjectUrlRef.current = URL.createObjectURL(file);
		setLogo(previewObjectUrlRef.current);
		setStagedFile(file);
		setLogoDirty(true);
	};
	const handleEndpointUrlChange = (endpointType, value) => {
		const nextEndpointUrls = {
			...endpointUrls,
			[endpointType]: value
		};
		setEndpointUrls(nextEndpointUrls);
		if (CUSTOM_PROVIDER_TEXT_ENDPOINTS.some((type) => type === endpointType)) {
			const textEndpoint = endpointType;
			const configuredTextEndpoints = CUSTOM_PROVIDER_TEXT_ENDPOINTS.filter((type) => nextEndpointUrls[type]?.trim());
			if (configuredTextEndpoints.length === 1 && configuredTextEndpoints[0] === textEndpoint) setPreferredChatEndpoint(textEndpoint);
			else if (!nextEndpointUrls[preferredChatEndpoint]?.trim()) setPreferredChatEndpoint(configuredTextEndpoints[0] ?? ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS);
		}
		setInvalidCreationUrl(null);
	};
	const handleSelectPreset = (source) => {
		const defaultEndpointUrl = endpointUrls[getCustomProviderDefaultChatEndpoint(endpointUrls, preferredChatEndpoint)]?.trim();
		if (defaultEndpointUrl) setBaseUrl(defaultEndpointUrl);
		setPreferredChatEndpoint(source.defaultChatEndpoint && isCustomProviderTextEndpoint(source.defaultChatEndpoint) ? source.defaultChatEndpoint : ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS);
		setSecondaryUrls({});
		setEndpointUrls({});
		setInvalidCreationUrl(null);
		onSelectPreset?.(source);
	};
	const buildSubmit = () => {
		const trimmedName = name.trim();
		if (!trimmedName || !mode) return null;
		const logoEdit = stagedFile ? {
			kind: "image",
			file: stagedFile
		} : logoDirty ? logo ? {
			kind: "key",
			key: logo
		} : { kind: "default" } : void 0;
		const logoField = logoEdit ? { logo: logoEdit } : {};
		if (mode.kind === "edit") return {
			mode: "edit",
			name: trimmedName,
			defaultChatEndpoint: mode.provider.defaultChatEndpoint ?? ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS,
			...logoField
		};
		const trimmedApiKey = apiKey.trim();
		const apiKeysPayload = trimmedApiKey ? [{
			id: uuid(),
			key: trimmedApiKey,
			isEnabled: true
		}] : void 0;
		if (mode.kind === "create-custom") return {
			mode: "create",
			name: trimmedName,
			...buildCustomProviderCreationPayload({
				endpointUrls,
				preferredChatEndpoint
			}),
			authConfig: { type: "api-key" },
			apiKeys: apiKeysPayload,
			...logoField
		};
		if (mode.kind === "duplicate") {
			const { source } = mode;
			const defaultChatEndpoint = source.defaultChatEndpoint ?? ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS;
			const submit = {
				mode: "create",
				name: trimmedName,
				defaultChatEndpoint,
				presetProviderId: source.presetProviderId,
				authConfig: emptyAuthConfigFor(source.authType),
				...logoField
			};
			if (duplicateNeedsBaseUrl(source.authType)) {
				const endpointConfigs = {};
				const trimmedBaseUrl = baseUrl.trim();
				if (trimmedBaseUrl) {
					const primaryEndpoints = source.presetProviderId === "new-api" ? CUSTOM_PROVIDER_TEXT_ENDPOINTS : [defaultChatEndpoint];
					for (const endpointType of primaryEndpoints) endpointConfigs[endpointType] = { baseUrl: trimmedBaseUrl };
				}
				mergeSecondaryEndpoints(endpointConfigs, secondaryUrls, defaultChatEndpoint);
				if (!isEmpty(endpointConfigs)) submit.endpointConfigs = endpointConfigs;
				if (apiKeysPayload) submit.apiKeys = apiKeysPayload;
			}
			return submit;
		}
		const _exhaustive = mode;
		throw new Error(`Unhandled provider editor mode kind: ${_exhaustive.kind}`);
	};
	const submittable = Boolean(mode);
	const showNameError = nameTouched && !name.trim();
	const showDuplicateBaseUrlError = Boolean(urlForm?.requireBaseUrl) && baseUrlTouched && !baseUrl.trim();
	const handleSubmit = async () => {
		setNameTouched(true);
		setBaseUrlTouched(true);
		if (mode?.kind === "create-custom") {
			const invalidUrl = findInvalidCustomProviderCreationUrl({
				endpointUrls,
				preferredChatEndpoint
			});
			setInvalidCreationUrl(invalidUrl);
			if (invalidUrl) {
				if (invalidUrl.field === "textEndpointRequired") firstTextEndpointRef.current?.focus();
				if (invalidUrl.field === "endpointUrl" && duplicateAdditionalEndpoints.some((endpointType) => endpointType === invalidUrl.endpointType)) setMoreEndpointsOpen(true);
				return;
			}
		} else if (mode?.kind === "duplicate" && duplicateUsesEndpointFields) {
			const invalidUrl = findInvalidCustomProviderEndpointUrl(duplicateEndpointUrls);
			setInvalidCreationUrl(invalidUrl);
			if (invalidUrl) {
				if (invalidUrl.field === "endpointUrl" && duplicateAdditionalEndpoints.some((endpointType) => endpointType === invalidUrl.endpointType)) setMoreEndpointsOpen(true);
				return;
			}
		}
		const payload = buildSubmit();
		if (!payload) return;
		setIsSubmitting(true);
		try {
			await onSubmit(payload);
		} catch (error) {
			logger$23.error("Provider editor submit failed", error);
			toast.error(t("settings.provider.save_failed"));
		} finally {
			setIsSubmitting(false);
		}
	};
	const title = (() => {
		if (!mode) return t("settings.provider.add.title");
		if (mode.kind === "edit") return t("common.edit");
		if (mode.kind === "duplicate") return t("settings.provider.duplicate.drawer_title", { name: mode.source.presetProviderId ? t(getProviderLabelKey(mode.source.presetProviderId)) : mode.source.name });
		return t("settings.provider.create_custom.title");
	})();
	const submitLabel = (() => {
		if (mode?.kind === "edit") return t("common.save");
		if (mode?.kind === "duplicate") return t("settings.provider.duplicate.menu_label");
		return t("button.add");
	})();
	const footerActions = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-editor-drawer",
		className: "flex items-center justify-end gap-2",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			variant: "outline",
			onClick: onClose,
			children: t("common.cancel")
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			disabled: !submittable || isSubmitting,
			loading: isSubmitting,
			onClick: () => void handleSubmit(),
			children: submitLabel
		})]
	});
	const avatarSection = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AvatarSection, {
		uploadInputRef,
		name,
		logo,
		initialLogo,
		logoPickerOpen,
		editingProviderId: editingProvider?.id,
		avatarBackgroundColor,
		avatarForegroundColor,
		onUpload: (event) => handleUploadChange(event),
		onPick: (providerId) => {
			revokePreviewObjectUrl();
			setStagedFile(null);
			setLogo(`icon:${providerId}`);
			setLogoDirty(true);
			setLogoPickerOpen(false);
		},
		onReset: () => {
			revokePreviewObjectUrl();
			setStagedFile(null);
			setLogo(null);
			setLogoDirty(true);
		},
		onLogoPickerOpenChange: setLogoPickerOpen
	});
	const nameField = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NameField, {
		name,
		showError: showNameError,
		onNameChange: setName,
		onBlur: () => setNameTouched(true),
		onEnter: handleSubmit,
		disableEnter: isSubmitting
	});
	const customAdditionalConfiguredCount = ADDITIONAL_CUSTOM_PROVIDER_ENDPOINTS.filter((endpointType) => endpointUrls[endpointType]?.trim()).length;
	const duplicateAdditionalConfiguredCount = duplicateAdditionalEndpoints.filter((endpointType) => secondaryUrls[endpointType]?.trim()).length;
	const presetPicker = onSelectPreset && presetSources.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PresetInstancePicker, {
		sources: presetSources,
		value: duplicateSource?.id ?? "",
		onSelect: handleSelectPreset
	}) : void 0;
	const formContent = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-editor-drawer",
		className: "flex flex-col gap-5",
		children: [
			avatarSection,
			nameField,
			mode?.kind === "create-custom" ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiKeyField, {
				value: apiKey,
				onChange: setApiKey
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CustomProviderEndpointFields, {
				endpointUrls,
				preferredChatEndpoint,
				invalidUrl: invalidCreationUrl,
				moreOpen: moreEndpointsOpen,
				additionalConfiguredCount: customAdditionalConfiguredCount,
				additionalContent: presetPicker,
				firstTextEndpointRef,
				onMoreOpenChange: setMoreEndpointsOpen,
				onEndpointUrlChange: handleEndpointUrlChange,
				onPreferredChatEndpointChange: setPreferredChatEndpoint
			})] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [duplicateSource?.presetProviderId && !presetPicker && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DuplicateHeader, { source: duplicateSource }), urlForm && (duplicateUsesEndpointFields ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiKeyField, {
				value: apiKey,
				onChange: setApiKey
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CustomProviderEndpointFields, {
				endpointUrls: duplicateEndpointUrls,
				preferredChatEndpoint: duplicateDefaultTextEndpoint,
				invalidUrl: invalidCreationUrl,
				moreOpen: moreEndpointsOpen,
				additionalConfiguredCount: duplicateAdditionalConfiguredCount,
				additionalContent: presetPicker,
				primaryEndpoints: duplicatePrimaryEndpoints,
				additionalEndpoints: duplicateAdditionalEndpoints,
				showPreferredEndpointAsDefault: true,
				onMoreOpenChange: setMoreEndpointsOpen,
				onEndpointUrlChange: (endpointType, value) => {
					if (endpointType === urlForm.primary) setBaseUrl(value);
					else setSecondaryUrls((prev) => ({
						...prev,
						[endpointType]: value
					}));
					setInvalidCreationUrl(null);
				}
			})] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(BaseUrlField, {
					label: t("settings.provider.base_url.label"),
					placeholder: t("settings.provider.base_url.placeholder"),
					value: baseUrl,
					onChange: setBaseUrl,
					required: urlForm.requireBaseUrl,
					error: showDuplicateBaseUrlError ? t("settings.provider.base_url.required") : void 0,
					onBlur: () => setBaseUrlTouched(true)
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiKeyField, {
					value: apiKey,
					onChange: setApiKey
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(MoreEndpointsDisclosure, {
					open: moreEndpointsOpen,
					onToggle: () => setMoreEndpointsOpen((v) => !v),
					primary: urlForm.primary,
					values: secondaryUrls,
					onChange: (type, value) => setSecondaryUrls((prev) => ({
						...prev,
						[type]: value
					}))
				})
			] }))] }),
			duplicateSource && !duplicateNeedsBaseUrl(duplicateSource.authType) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
				className: "text-muted-foreground text-xs leading-[1.4]",
				children: t("settings.provider.duplicate.fill_after_create")
			})
		]
	});
	if (mode?.kind === "edit") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsDrawer, {
		open,
		onClose,
		title,
		footer: footerActions,
		children: formContent
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog, {
		open,
		onOpenChange: (nextOpen) => {
			if (!nextOpen && !isSubmitting) onClose();
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogContent, {
			"aria-describedby": void 0,
			closeOnOverlayClick: !isSubmitting,
			showCloseButton: !isSubmitting,
			size: "lg",
			"data-testid": "provider-editor-dialog",
			className: "grid max-h-[calc(100vh-2rem)] grid-rows-[auto_minmax(0,1fr)_auto] gap-0 overflow-hidden p-0",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogHeader, {
					className: "px-6 pt-6 pb-4",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogTitle, {
						className: "text-base leading-5",
						children: title
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(scrollbar_default, {
					"data-testid": "provider-editor-scrollbar",
					className: "min-h-0 px-6 py-2",
					children: formContent
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogFooter, {
					className: "mt-4 border-border border-t px-6 py-4",
					children: footerActions
				})
			]
		})
	});
}
var CUSTOM_PROVIDER_ENDPOINT_LABEL_KEYS = {
	[ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS]: "settings.provider.more_endpoints.openai_chat",
	[ENDPOINT_TYPE.OPENAI_RESPONSES]: "settings.provider.more_endpoints.openai_responses",
	[ENDPOINT_TYPE.ANTHROPIC_MESSAGES]: "settings.provider.more_endpoints.anthropic",
	[ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT]: "settings.provider.more_endpoints.gemini",
	[ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION]: "settings.provider.image_endpoints.image_generation_base_url.label",
	[ENDPOINT_TYPE.OPENAI_IMAGE_EDIT]: "settings.provider.image_endpoints.image_edit_base_url.label"
};
function CustomProviderEndpointFields({ endpointUrls, preferredChatEndpoint, invalidUrl, moreOpen, additionalConfiguredCount, additionalContent, primaryEndpoints = COMMON_CUSTOM_PROVIDER_ENDPOINTS, additionalEndpoints = ADDITIONAL_CUSTOM_PROVIDER_ENDPOINTS, showPreferredEndpointAsDefault = false, firstTextEndpointRef, onMoreOpenChange, onEndpointUrlChange, onPreferredChatEndpointChange }) {
	const { t } = useTranslation();
	const textEndpointRequired = invalidUrl?.field === "textEndpointRequired";
	const renderEndpointField = (endpointType, labelAccessory) => {
		const endpointValue = endpointUrls[endpointType] ?? "";
		const invalidEndpoint = invalidUrl?.field === "endpointUrl" && invalidUrl.endpointType === endpointType;
		const missingTextEndpoint = textEndpointRequired && endpointType === ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS;
		const requestPreview = buildCustomProviderEndpointPreview(endpointValue, endpointType);
		const emptyValueHelp = CUSTOM_PROVIDER_TEXT_ENDPOINTS.some((type) => type === endpointType) ? t("settings.provider.create_custom.endpoint_fields.url_help") : t(endpointType === ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION ? "settings.provider.image_endpoints.image_generation_base_url.help" : "settings.provider.image_endpoints.image_edit_base_url.help");
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BaseUrlField, {
			label: t(CUSTOM_PROVIDER_ENDPOINT_LABEL_KEYS[endpointType]),
			placeholder: t("settings.provider.base_url.placeholder"),
			value: endpointValue,
			error: invalidEndpoint ? t("settings.provider.base_url.invalid") : missingTextEndpoint ? t("settings.provider.create_custom.endpoint_fields.text_endpoint_required") : void 0,
			description: requestPreview ? t("settings.provider.create_custom.request_preview", { path: requestPreview }) : emptyValueHelp,
			labelAccessory,
			inputRef: endpointType === ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS ? firstTextEndpointRef : void 0,
			onChange: (nextValue) => onEndpointUrlChange(endpointType, nextValue)
		});
	};
	const renderEndpointControl = (endpointType) => {
		const isTextEndpoint = CUSTOM_PROVIDER_TEXT_ENDPOINTS.some((type) => type === endpointType);
		const isConfiguredTextEndpoint = Boolean(isTextEndpoint && endpointUrls[endpointType]?.trim());
		const labelAccessory = isTextEndpoint && preferredChatEndpoint === endpointType && (isConfiguredTextEndpoint || showPreferredEndpointAsDefault) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
			variant: "secondary",
			className: "h-5 border-0 px-1.5 py-0 font-normal text-foreground-tertiary text-xs",
			children: t("settings.provider.create_custom.endpoint_fields.default_chat")
		}) : isConfiguredTextEndpoint && onPreferredChatEndpointChange ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			type: "button",
			variant: "outline",
			size: "sm",
			className: "before:-top-5 relative h-5 min-h-0 rounded-full px-2 text-xs transition-transform before:absolute before:inset-x-0 before:bottom-0 before:content-[''] active:scale-[0.96]",
			onClick: () => onPreferredChatEndpointChange(endpointType),
			children: t("settings.provider.create_custom.endpoint_fields.set_default_chat")
		}) : null;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "settings.render-endpoint-control",
			children: renderEndpointField(endpointType, labelAccessory)
		}, endpointType);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
		"data-ui": "settings.custom-provider-endpoint-fields",
		className: "flex flex-col gap-3",
		"aria-labelledby": "custom-provider-endpoints-title",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h3", {
				"data-ui": "settings.custom-provider-endpoint-fields.custom-provider-endpoints-title",
				id: "custom-provider-endpoints-title",
				className: "font-medium text-[13px] text-foreground",
				children: t("settings.provider.create_custom.endpoint_fields.label")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex flex-col gap-5",
				children: primaryEndpoints.map(renderEndpointControl)
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Accordion, {
				type: "single",
				collapsible: true,
				value: moreOpen ? "more-settings" : "",
				onValueChange: (value) => onMoreOpenChange(value === "more-settings"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(AccordionItem, {
					value: "more-settings",
					className: "border-0",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(AccordionTrigger, {
						className: "min-h-10 cursor-pointer py-0 font-normal text-muted-foreground text-xs hover:text-foreground disabled:cursor-not-allowed",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
							className: "flex min-w-0 flex-1 items-center justify-between gap-2",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.provider.create_custom.endpoint_fields.more") }), additionalConfiguredCount > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "truncate text-foreground-tertiary",
								children: t("settings.provider.create_custom.endpoint_fields.more_configured", { count: additionalConfiguredCount })
							})]
						})
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(AccordionContent, {
						className: "flex flex-col gap-5 pt-3 pb-0 text-foreground",
						children: [additionalEndpoints.map(renderEndpointControl), additionalContent && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Separator, { className: "bg-border-subtle" }), additionalContent] })]
					})]
				})
			})
		]
	});
}
function PresetInstancePicker({ sources, value, onSelect }) {
	const { t } = useTranslation();
	const options = (0, import_react.useMemo)(() => sources.map((source) => {
		const presetId = source.presetProviderId ?? source.id;
		const label = t(getProviderLabelKey(presetId));
		return {
			value: source.id,
			label,
			icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderAvatar, {
				provider: {
					id: presetId,
					name: label
				},
				size: 20
			}),
			source
		};
	}), [sources, t]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.preset-instance-picker",
		className: "flex flex-col gap-2",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex flex-col gap-0.5",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "text-[13px] text-foreground",
				children: t("settings.provider.create_custom.preset_instance.title")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "text-foreground-tertiary text-xs",
				children: t("settings.provider.create_custom.preset_instance.description")
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Combobox, {
			options,
			value,
			onChange: (value$1) => {
				const selectedValue = Array.isArray(value$1) ? value$1[0] : value$1;
				const selected = options.find((option) => option.value === selectedValue);
				if (selected) onSelect(selected.source);
			},
			className: "min-h-10 w-full justify-between px-3 text-left font-normal",
			emptyText: t("settings.provider.create_custom.preset_instance.empty"),
			filterOption: (option, search) => {
				return `${option.label} ${option.value} ${option.source.name}`.toLocaleLowerCase().includes(search.trim().toLocaleLowerCase());
			},
			placeholder: t("settings.provider.create_custom.preset_instance.placeholder"),
			popoverAlign: "start",
			popoverClassName: "w-(--radix-popover-trigger-width)! [&_[data-slot=command-list]]:max-h-[280px]",
			searchPlaceholder: t("settings.provider.create_custom.preset_instance.search_placeholder")
		})]
	});
}
function DuplicateHeader({ source }) {
	const { t } = useTranslation();
	const presetId = source.presetProviderId;
	const label = presetId ? t(getProviderLabelKey(presetId)) : source.name;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.duplicate-header",
		className: "flex items-center gap-2 rounded-lg border border-border-subtle bg-muted/40 px-3 py-2",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderAvatar, {
			provider: {
				id: presetId ?? source.id,
				name: label
			},
			size: 18
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: "truncate text-foreground text-sm",
			children: label
		})]
	});
}
function AvatarSection({ uploadInputRef, name, logo, initialLogo, logoPickerOpen, editingProviderId, avatarBackgroundColor, avatarForegroundColor, onUpload, onPick, onReset, onLogoPickerOpenChange }) {
	const { t } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.avatar-section",
		className: "flex flex-col items-center gap-3",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex h-19 w-19 items-center justify-center overflow-hidden rounded-full border border-border bg-muted/50",
				style: avatarBackgroundColor && avatarForegroundColor ? {
					backgroundColor: avatarBackgroundColor,
					color: avatarForegroundColor
				} : void 0,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderAvatarPrimitive, {
					providerId: editingProviderId ?? "provider-editor-preview",
					providerName: name || "Provider",
					logo: logo ?? void 0,
					size: 76
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex flex-wrap items-center justify-center gap-2",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						variant: "outline",
						onClick: () => uploadInputRef.current?.click(),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ImagePlus, { size: 16 }), t("settings.general.image_upload")]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, {
						open: logoPickerOpen,
						onOpenChange: onLogoPickerOpenChange,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverTrigger, {
							asChild: true,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
								variant: "outline",
								children: t("settings.general.avatar.builtin")
							}) })
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContent, {
							align: "center",
							sideOffset: 8,
							className: "w-auto",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderLogoPicker_default, { onProviderClick: onPick })
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						variant: "outline",
						disabled: !logo && !initialLogo,
						onClick: onReset,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RotateCcw, { size: 16 }), t("settings.general.avatar.reset")]
					})
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
				"data-ui": "settings.avatar-section.action.upload",
				ref: uploadInputRef,
				type: "file",
				accept: "image/png,image/jpeg,image/gif",
				className: "hidden",
				onChange: onUpload
			})
		]
	});
}
function NameField({ name, showError, onNameChange, onBlur, onEnter, disableEnter }) {
	const { t } = useTranslation();
	const uid = (0, import_react.useId)();
	const inputId = `${uid}-name-input`;
	const errorId = `${uid}-name-error`;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Field, {
		className: "gap-2",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldLabel, {
				required: true,
				htmlFor: inputId,
				className: "text-[13px] text-foreground",
				children: t("settings.provider.add.name.label")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				id: inputId,
				value: name,
				placeholder: t("settings.provider.add.name.placeholder"),
				maxLength: 32,
				"aria-invalid": showError,
				"aria-describedby": showError ? errorId : void 0,
				onChange: (event) => onNameChange(event.target.value),
				onBlur,
				onKeyDown: (event) => {
					if (event.key === "Enter" && !event.nativeEvent.isComposing && !disableEnter) onEnter();
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldError, {
				id: errorId,
				className: "text-xs",
				errors: showError ? [{ message: t("settings.provider.add.name.required") }] : void 0
			})
		]
	});
}
function MoreEndpointsDisclosure({ open, onToggle, primary, values, onChange }) {
	const { t } = useTranslation();
	const contentId = `${(0, import_react.useId)()}-more-endpoints`;
	const entries = SECONDARY_ENDPOINT_LABELS.filter((entry) => entry.type !== primary);
	if (entries.length === 0) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.more-endpoints-disclosure",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
			"data-ui": "settings.more-endpoints-disclosure.action.toggle",
			type: "button",
			"aria-expanded": open,
			"aria-controls": contentId,
			onClick: onToggle,
			className: cn(providerListClasses.disclosureToggle, "px-0"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRight, { className: cn(providerListClasses.disclosureChevron, open && providerListClasses.disclosureChevronOpen) }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.provider.more_endpoints.toggle") })]
		}), open && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			id: contentId,
			className: cn(providerListClasses.disclosureBody, "pl-0"),
			children: entries.map(({ type, labelKey }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BaseUrlField, {
				label: t(labelKey),
				placeholder: t("settings.provider.base_url.placeholder"),
				value: values[type] ?? "",
				onChange: (value) => onChange(type, value)
			}, type))
		})]
	});
}
function BaseUrlField({ label, labelAccessory, placeholder, value, onChange, required, error, description, inputRef, onBlur }) {
	const uid = (0, import_react.useId)();
	const inputId = `${uid}-url-input`;
	const errorId = `${uid}-url-error`;
	const descriptionId = `${uid}-url-description`;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Field, {
		className: "gap-2",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex min-h-5 items-center gap-2",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldLabel, {
					required,
					htmlFor: inputId,
					className: "text-[13px] text-foreground",
					children: label
				}), labelAccessory]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				ref: inputRef,
				id: inputId,
				value,
				placeholder,
				"aria-invalid": Boolean(error),
				"aria-describedby": [description ? descriptionId : null, error ? errorId : null].filter(Boolean).join(" ") || void 0,
				onChange: (event) => onChange(event.target.value),
				onBlur
			}),
			description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
				id: descriptionId,
				"aria-live": "polite",
				className: "break-all text-muted-foreground text-xs",
				children: description
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldError, {
				id: errorId,
				className: "text-xs",
				errors: error ? [{ message: error }] : void 0
			})
		]
	});
}
function ApiKeyField({ value, onChange }) {
	const { t } = useTranslation();
	const [visible, setVisible] = (0, import_react.useState)(false);
	const inputId = `${(0, import_react.useId)()}-api-key-input`;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Field, {
		className: "gap-2",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldLabel, {
			htmlFor: inputId,
			className: "text-[13px] text-foreground",
			children: t("settings.provider.api_key.label")
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "relative",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				id: inputId,
				type: visible ? "text" : "password",
				value,
				placeholder: t("settings.provider.api_key.placeholder"),
				className: "pr-10",
				autoComplete: "off",
				spellCheck: false,
				onChange: (event) => onChange(event.target.value)
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				type: "button",
				variant: "ghost",
				size: "icon-lg",
				"aria-label": t(visible ? "settings.provider.api_key.hide_key" : "settings.provider.api_key.show_key"),
				onClick: () => setVisible((v) => !v),
				className: "-translate-y-1/2 absolute top-1/2 right-0 text-muted-foreground hover:text-foreground",
				children: visible ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EyeOff, { className: "size-3.5" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Eye, { className: "size-3.5" })
			})]
		})]
	});
}
function getGroupedPresetIds(providers) {
	const counts = /* @__PURE__ */ new Map();
	for (const provider of providers) {
		const preset = provider.presetProviderId;
		if (!preset) continue;
		counts.set(preset, (counts.get(preset) ?? 0) + 1);
	}
	const grouped = /* @__PURE__ */ new Set();
	for (const [preset, count] of counts) if (count >= 2) grouped.add(preset);
	return grouped;
}
function groupProvidersByPreset(providers) {
	const groupedPresets = getGroupedPresetIds(providers);
	const entries = [];
	const groupIndexByPreset = /* @__PURE__ */ new Map();
	for (const provider of providers) {
		const preset = provider.presetProviderId;
		if (preset && groupedPresets.has(preset)) {
			const existingIndex = groupIndexByPreset.get(preset);
			if (existingIndex === void 0) {
				groupIndexByPreset.set(preset, entries.length);
				entries.push({
					kind: "group",
					presetProviderId: preset,
					members: [provider]
				});
			} else {
				const existing = entries[existingIndex];
				if (existing.kind === "group") existing.members.push(provider);
			}
		} else entries.push({
			kind: "single",
			provider
		});
	}
	return entries;
}
function ProviderListGroup({ presetProviderId, members, items, expanded, containsSelected, onToggle, onAddAnother, onDragStateChange, onReorder, onReorderError, renderItem }) {
	const { t } = useTranslation();
	const bodyId = (0, import_react.useId)();
	const label = t(getProviderLabelKey(presetProviderId));
	const headerHighlight = !expanded && containsSelected;
	const hasEnabledMember = members.some((member) => member.isEnabled);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-list-group",
		className: "w-full",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
			"data-ui": "settings.provider-list-group.action.toggle",
			type: "button",
			"aria-expanded": expanded,
			"aria-controls": bodyId,
			"data-testid": `provider-list-group-${presetProviderId}`,
			"data-has-selected": containsSelected ? "true" : "false",
			onClick: onToggle,
			className: cn(providerListClasses.groupHeader, headerHighlight && providerListClasses.groupHeaderHasSelected),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: providerListClasses.itemMain,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					"aria-hidden": true,
					"data-testid": `provider-list-group-drag-handle-${presetProviderId}`,
					className: providerListClasses.itemDragHandle,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GripVertical, { size: 16 })
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: providerListClasses.itemIdentity,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderAvatar, {
						provider: {
							id: presetProviderId,
							name: label
						},
						size: 26,
						className: providerListClasses.itemAvatar,
						displayContext: "provider-list"
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: cn(providerListClasses.itemLabel, "text-foreground"),
						children: label
					})]
				})]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: providerListClasses.groupTrailing,
				children: [hasEnabledMember && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					"aria-hidden": true,
					"data-testid": `provider-list-group-enabled-dot-${presetProviderId}`,
					className: providerListClasses.groupEnabledDot
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRight, {
					size: 12,
					"data-testid": `provider-list-group-chevron-${presetProviderId}`,
					className: cn(providerListClasses.groupChevron, expanded && providerListClasses.groupChevronOpen, hasEnabledMember && providerListClasses.groupChevronHiddenUntilHover)
				})]
			})]
		}), expanded && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			id: bodyId,
			className: providerListClasses.groupBody,
			onPointerDown: (event) => event.stopPropagation(),
			onKeyDown: (event) => event.stopPropagation(),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReorderableList, {
				items,
				visibleItems: members,
				getId: (provider) => provider.id,
				onDragStateChange,
				onReorder,
				onReorderError,
				className: "w-full",
				gap: "0.5rem",
				restrictions: { scrollableAncestor: true },
				renderItem
			}), onAddAnother && members[0] && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
				type: "button",
				"data-testid": `provider-list-group-add-${presetProviderId}`,
				onClick: () => onAddAnother(members[0]),
				className: providerListClasses.groupAddRow,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, { size: 12 }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "truncate",
					children: t("settings.provider.duplicate.add_another", { name: label })
				})]
			})]
		})]
	});
}
function reorderProviderBlocks({ providers, items, oldIndex, newIndex }) {
	if (oldIndex === newIndex) return providers;
	const source = items[oldIndex];
	const target = items[newIndex];
	if (!source || !target) return providers;
	const sourceIds = new Set(source.providerIds);
	const targetIds = new Set(target.providerIds);
	const moving = providers.filter((provider) => sourceIds.has(provider.id));
	const remaining = providers.filter((provider) => !sourceIds.has(provider.id));
	if (moving.length === 0) return providers;
	const targetIndexes = remaining.flatMap((provider, index) => targetIds.has(provider.id) ? [index] : []);
	const targetIndex = oldIndex < newIndex ? targetIndexes.at(-1) : targetIndexes[0];
	if (targetIndex === void 0) return providers;
	const insertIndex = oldIndex < newIndex ? targetIndex + 1 : targetIndex;
	const nextProviders = [...remaining];
	nextProviders.splice(insertIndex, 0, ...moving);
	return nextProviders;
}
function ProviderListContent({ providers, visibleProviders, selectedProviderId, searchActive, expandedGroups, onToggleGroup, onAddAnotherInGroup, scrollerRef, onDragStateChange, onReorder, onReorderError, renderItem }) {
	const { t } = useTranslation();
	const entries = (0, import_react.useMemo)(() => groupProvidersByPreset(visibleProviders), [visibleProviders]);
	const hasResults = visibleProviders.length > 0;
	const visibleIndexById = (0, import_react.useMemo)(() => new Map(visibleProviders.map((provider, index) => [provider.id, index])), [visibleProviders]);
	const renderFlat = () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReorderableList, {
		items: providers,
		visibleItems: visibleProviders,
		getId: (provider) => provider.id,
		onDragStateChange,
		onReorder,
		onReorderError,
		className: "w-full",
		gap: "0.5rem",
		restrictions: { scrollableAncestor: true },
		renderItem
	});
	const renderGrouped = () => {
		const sortableItems = entries.map((entry) => {
			if (entry.kind === "single") return {
				id: `single:${entry.provider.id}`,
				kind: "single",
				provider: entry.provider,
				providerIds: [entry.provider.id]
			};
			return {
				id: `group:${entry.presetProviderId}`,
				kind: "group",
				presetProviderId: entry.presetProviderId,
				members: entry.members,
				providerIds: providers.filter((provider) => provider.presetProviderId === entry.presetProviderId).map((provider) => provider.id)
			};
		});
		const handleSortEnd = ({ oldIndex, newIndex }) => {
			const nextProviders = reorderProviderBlocks({
				providers,
				items: sortableItems,
				oldIndex,
				newIndex
			});
			if (nextProviders === providers) return;
			try {
				Promise.resolve(onReorder(nextProviders)).catch((error) => {
					onReorderError?.(error);
				});
			} catch (error) {
				onReorderError?.(error);
			}
		};
		const handleDragStart = () => {
			onDragStateChange(true);
		};
		const handleDragEnd = () => {
			onDragStateChange(false);
		};
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(sortable_default, {
			items: sortableItems,
			itemKey: (item) => item.id,
			onDragStart: handleDragStart,
			onDragEnd: handleDragEnd,
			onDragCancel: handleDragEnd,
			onSortEnd: handleSortEnd,
			collisionDetection: closestCenter,
			adjustScale: false,
			className: "w-full",
			gap: "0.5rem",
			restrictions: { scrollableAncestor: true },
			renderItem: (item, state) => {
				if (item.kind === "single") return renderItem(item.provider, visibleIndexById.get(item.provider.id) ?? -1, state);
				const expanded = !state.overlay && (searchActive || (expandedGroups[item.presetProviderId] ?? false));
				const containsSelected = !!selectedProviderId && item.members.some((m) => m.id === selectedProviderId);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderListGroup, {
					presetProviderId: item.presetProviderId,
					members: item.members,
					items: providers,
					expanded,
					containsSelected,
					onToggle: () => onToggleGroup(item.presetProviderId),
					onAddAnother: onAddAnotherInGroup,
					onDragStateChange,
					onReorder,
					onReorderError,
					renderItem
				});
			}
		});
	};
	const hasAnyGroup = entries.some((entry) => entry.kind === "group");
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Scrollbar_default, {
		ref: scrollerRef,
		className: providerListClasses.scroller,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: providerListClasses.sectionStack,
			children: hasResults ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: providerListClasses.section,
				children: hasAnyGroup ? renderGrouped() : renderFlat()
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: providerListClasses.emptyState,
				children: t("common.no_results")
			})
		})
	});
}
var FILTER_MENU_OPTIONS = [
	{
		mode: "all",
		labelKey: "settings.provider.filter.all"
	},
	{
		mode: "agent",
		labelKey: "settings.provider.filter.agent"
	},
	{
		mode: "enabled",
		labelKey: "settings.provider.filter.enabled"
	},
	{
		mode: "disabled",
		labelKey: "settings.provider.filter.disabled"
	}
];
function ProviderListHeaderFilterMenu({ filterMode, disabled, triggerClassName = providerListClasses.headerIconButton, triggerIconSize = 14, onFilterChange }) {
	const { t } = useTranslation();
	const [open, setOpen] = (0, import_react.useState)(false);
	const hasActiveFilter = filterMode !== "all";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, {
		open,
		onOpenChange: setOpen,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverTrigger, {
			asChild: true,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				"aria-label": t("settings.provider.filter.label"),
				disabled,
				className: cn("group", triggerClassName),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Funnel, {
					size: triggerIconSize,
					className: cn("shrink-0", hasActiveFilter ? "text-primary!" : "text-muted-foreground group-hover:text-foreground")
				})
			}) })
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContent, {
			align: "end",
			className: "w-fit min-w-32 rounded-xl p-1.5",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuList, {
				className: "gap-1",
				children: FILTER_MENU_OPTIONS.map(({ mode, labelKey }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem, {
					label: t(labelKey),
					className: "h-8 rounded-lg px-2.5 text-sm",
					icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Check, { className: cn("size-3.5", filterMode === mode ? "opacity-100" : "opacity-0") }),
					onClick: () => {
						onFilterChange(mode);
						setOpen(false);
					}
				}, mode))
			})
		})]
	});
}
var MarkdownEditor = ({ value, onChange, placeholder = "请输入Markdown格式文本...", height = "300px", autoFocus = false }) => {
	const { t } = useTranslation();
	const markdownId = (0, import_react.useId)();
	const [inputValue, setInputValue] = (0, import_react.useState)(value || "");
	(0, import_react.useEffect)(() => {
		setInputValue(value || "");
	}, [value]);
	const handleChange = (e) => {
		const newValue = e.target.value;
		setInputValue(newValue);
		onChange(newValue);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "ui.markdown-editor",
		className: "flex w-full overflow-hidden rounded-lg border border-border",
		style: { height },
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("textarea", {
			className: "flex-1 resize-none border-0 border-border border-r bg-background p-3 font-[var(--font-family)] text-foreground text-sm leading-[1.5] outline-none placeholder:text-muted-foreground focus:outline-none",
			value: inputValue,
			onChange: handleChange,
			placeholder,
			autoFocus
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "markdown flex-1 overflow-auto bg-background p-3",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Markdown, {
				id: markdownId,
				plugins: {
					cjk: defaultMarkdownPlugins.cjk,
					math: withMath()
				},
				children: inputValue || t("settings.provider.notes.markdown_editor_default_value")
			})
		})]
	});
};
var MarkdownEditor_default = MarkdownEditor;
var PopupContainer$1 = ({ providerId, open, resolve }) => {
	const { t } = useTranslation();
	const { provider, updateProvider } = useProvider(providerId);
	const [notes, setNotes] = (0, import_react.useState)(provider?.settings?.notes || "");
	const [edited, setEdited] = (0, import_react.useState)(false);
	const [saving, setSaving] = (0, import_react.useState)(false);
	(0, import_react.useEffect)(() => {
		if (edited) return;
		setNotes(provider?.settings?.notes || "");
	}, [edited, provider?.settings?.notes]);
	const handleSave = async () => {
		setSaving(true);
		try {
			await updateProvider({ providerSettings: {
				...provider?.settings,
				notes
			} });
			resolve({});
		} catch {
			toast.error(t("blocks.edit.save.failed.label"));
		} finally {
			setSaving(false);
		}
	};
	const onCancel = () => {
		resolve({});
	};
	const footer = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.popup",
		className: drawerClasses.footer,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			variant: "outline",
			onClick: onCancel,
			children: t("common.cancel")
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			loading: saving,
			disabled: saving,
			onClick: () => void handleSave(),
			children: t("common.save")
		})]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsDrawer, {
		title: t("settings.provider.notes.title"),
		open,
		onClose: onCancel,
		footer,
		bodyClassName: "flex min-h-0 flex-1 flex-col px-5 py-4",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "min-h-0 flex-1",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MarkdownEditor_default, {
				value: notes,
				onChange: (value) => {
					setEdited(true);
					setNotes(value);
				},
				placeholder: t("settings.provider.notes.placeholder"),
				height: "400px"
			})
		})
	});
};
var ModelNotesPopup_default = createPopup(PopupContainer$1, { dismissResult: {} });
function ProviderListItem({ provider, selected, dragging, onClick, onOpenMenu, renderMenuButton }) {
	const { t } = useTranslation();
	const handleOpenMenu = (event) => {
		event.stopPropagation();
		onOpenMenu?.();
	};
	const hasTrailingSlot = provider.isEnabled || onOpenMenu;
	const menuButton = onOpenMenu ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		"data-ui": "settings.provider-list-item",
		type: "button",
		"data-testid": `provider-list-menu-${provider.id}`,
		onClick: handleOpenMenu,
		className: providerListClasses.itemMoreActions,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EllipsisVertical, { size: 14 })
	}) : null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-list-item.button",
		"data-testid": `provider-list-item-${provider.id}`,
		"data-selected": selected ? "true" : "false",
		"data-dragging": dragging ? "true" : "false",
		role: "button",
		tabIndex: 0,
		onClick,
		onKeyDown: (event) => {
			if (event.currentTarget !== event.target) return;
			if (event.key === "Enter" || event.key === " ") {
				event.preventDefault();
				onClick();
			}
		},
		className: cn("group/row cursor-pointer", providerListClasses.item, selected ? providerListClasses.itemSelected : providerListClasses.itemIdle, dragging && "opacity-65"),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: providerListClasses.itemMain,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				"aria-hidden": true,
				"data-testid": `provider-list-drag-handle-${provider.id}`,
				"data-dragging": dragging ? "true" : "false",
				className: providerListClasses.itemDragHandle,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GripVertical, { size: 16 })
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: providerListClasses.itemIdentity,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderAvatar, {
						provider,
						size: 26,
						className: providerListClasses.itemAvatar,
						displayContext: "provider-list"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: providerListClasses.itemLabel,
						children: provider.name
					}),
					provider.id === "radeon-cloud" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						"data-ui": "settings.provider-list-item.radeon-cloud-free-badge",
						"data-testid": "radeon-cloud-free-badge",
						className: "inline-flex h-4 shrink-0 items-center rounded bg-success/10 px-1 font-medium text-[9px] text-success leading-none",
						children: t("models.type.free")
					})
				]
			})]
		}), hasTrailingSlot && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: cn(providerListClasses.itemTrailingSlot, provider.isEnabled ? providerListClasses.itemTrailingSlotIndicatorOnly : providerListClasses.itemTrailingSlotAction),
			children: [provider.isEnabled && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				"aria-hidden": true,
				className: providerListClasses.itemEnabledDot
			}), menuButton && (renderMenuButton ? renderMenuButton(menuButton) : menuButton)]
		})]
	});
}
function ProviderListItemWithContextMenu({ provider, selected, contextOpen, onContextOpenChange, onSelect, onEdit, onDelete, onDuplicate, showManagementActions, listState, onSetListItemRef }) {
	const { t } = useTranslation();
	const menuItems = (0, import_react.useMemo)(() => {
		const items = [];
		if (showManagementActions) items.push({
			type: "item",
			id: "edit",
			label: t("common.edit"),
			icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SquarePen, { size: 14 }),
			onSelect: onEdit
		});
		if (onDuplicate) items.push({
			type: "item",
			id: "duplicate",
			label: t("settings.provider.duplicate.menu_label"),
			icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyPlus, { size: 14 }),
			onSelect: onDuplicate
		});
		items.push({
			type: "item",
			id: "notes",
			label: t("settings.provider.notes.title"),
			icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserPen, { size: 14 }),
			onSelect: () => ModelNotesPopup_default.show({ providerId: provider.id })
		});
		if (showManagementActions) items.push({
			type: "item",
			id: "delete",
			label: t("common.delete"),
			icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trash2, { size: 14 }),
			destructive: true,
			onSelect: onDelete
		});
		return items;
	}, [
		onDelete,
		onDuplicate,
		onEdit,
		provider.id,
		showManagementActions,
		t
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandContextMenu, {
		location: "webcontents.context",
		extraItems: menuItems,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "w-full",
			ref: (element) => onSetListItemRef(provider.id, element),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderListItem, {
				provider: {
					...provider,
					name: getFancyProviderName(provider)
				},
				selected,
				dragging: listState.dragging,
				onClick: onSelect,
				onOpenMenu: () => onContextOpenChange(true),
				renderMenuButton: (button) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandPopupMenu, {
					location: "webcontents.context",
					extraItems: menuItems,
					open: contextOpen,
					onOpenChange: onContextOpenChange,
					align: "end",
					contentClassName: providerListClasses.itemMenuContent,
					children: button
				})
			})
		})
	});
}
function ProviderListSearchField({ value, disabled, onValueChange, trailing }) {
	const { t } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.provider-list-search-field",
		className: providerListClasses.searchRow,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: `${providerListClasses.searchWrap} min-w-0 flex-1`,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Search, { className: providerListClasses.searchIcon }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
					value,
					placeholder: t("settings.provider.search"),
					onChange: (event) => onValueChange(event.target.value),
					onKeyDown: (event) => {
						if (event.key === "Escape") {
							event.stopPropagation();
							onValueChange("");
						}
					},
					disabled,
					className: providerListClasses.searchInput
				}),
				value ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
					type: "button",
					"aria-label": t("common.clear"),
					disabled,
					onClick: () => onValueChange(""),
					className: providerListClasses.searchClearButton,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { size: 9 })
				}) : null,
				trailing
			]
		})
	});
}
function useProviderDelete() {
	const { deleteProviderById } = useProviderActions();
	return { deleteProvider: (0, import_react.useCallback)(async (providerId) => {
		await deleteProviderById(providerId);
	}, [deleteProviderById]) };
}
var logger$22 = loggerService.withContext("useProviderEditor");
function useProviderEditor({ onProviderCreated }) {
	const { t } = useTranslation();
	const { createProvider } = useProviders();
	const { updateProviderById } = useProviderActions();
	const invalidate = useInvalidateCache();
	const [mode, setMode] = (0, import_react.useState)(null);
	const modeRef = (0, import_react.useRef)(null);
	const submitTokenRef = (0, import_react.useRef)(0);
	const editingProvider = mode?.kind === "edit" ? mode.provider : null;
	const initialLogo = editingProvider?.logo ?? editingProvider?.logoSrc;
	const updateMode = (0, import_react.useCallback)((next) => {
		submitTokenRef.current += 1;
		modeRef.current = next;
		setMode(next);
	}, []);
	const cancel = (0, import_react.useCallback)(() => updateMode(null), [updateMode]);
	const startAdd = (0, import_react.useCallback)(() => updateMode({ kind: "create-custom" }), [updateMode]);
	const startAddFrom = (0, import_react.useCallback)((source) => updateMode({
		kind: "duplicate",
		source
	}), [updateMode]);
	const startEdit = (0, import_react.useCallback)((provider) => updateMode({
		kind: "edit",
		provider
	}), [updateMode]);
	const applyLogo = (0, import_react.useCallback)(async (providerId, edit) => {
		try {
			const image = edit.kind === "image" ? {
				kind: "image",
				data: await prepareEntityImageBytes(edit.file)
			} : edit.kind === "key" ? {
				kind: "key",
				key: edit.key
			} : { kind: "default" };
			await ipcApi.request("provider.set_logo", {
				providerId,
				image
			});
		} catch (error) {
			logger$22.error("Failed to set provider logo", error);
			toast.error(t("settings.provider.logo_upload_failed"));
			return;
		}
		try {
			await invalidate([
				"/providers",
				`/providers/${providerId}`,
				`/providers/${providerId}/*`
			]);
		} catch (error) {
			logger$22.error("Failed to refresh provider data after logo update", error);
		}
	}, [invalidate, t]);
	const submit = (0, import_react.useCallback)(async (params) => {
		const trimmedName = params.name.trim();
		if (!trimmedName) return;
		if (params.mode === "edit") {
			if (!editingProvider) return;
			const originalEditingId = editingProvider.id;
			await updateProviderById(originalEditingId, {
				name: trimmedName,
				defaultChatEndpoint: params.defaultChatEndpoint
			});
			if (params.logo) await applyLogo(originalEditingId, params.logo);
			if (modeRef.current?.kind === "edit" && modeRef.current.provider.id === originalEditingId) cancel();
			return;
		}
		const providerId = uuid();
		const submitToken = ++submitTokenRef.current;
		const provider = await createProvider({
			providerId,
			name: trimmedName,
			...params.presetProviderId ? { presetProviderId: params.presetProviderId } : {},
			defaultChatEndpoint: params.defaultChatEndpoint,
			...params.endpointConfigs ? { endpointConfigs: params.endpointConfigs } : {},
			...params.authConfig ? { authConfig: params.authConfig } : {},
			...params.apiKeys && params.apiKeys.length > 0 ? { apiKeys: params.apiKeys } : {},
			...params.logo?.kind === "key" ? { logo: {
				kind: "key",
				key: params.logo.key
			} } : {}
		});
		if (params.logo?.kind === "image") await applyLogo(provider.id, params.logo);
		if (submitTokenRef.current === submitToken && modeRef.current?.kind !== "edit") {
			onProviderCreated(provider.id);
			cancel();
		}
	}, [
		applyLogo,
		cancel,
		createProvider,
		editingProvider,
		onProviderCreated,
		updateProviderById
	]);
	return {
		isOpen: mode != null,
		mode,
		editingProvider,
		initialLogo,
		startAdd,
		startAddFrom,
		startEdit,
		cancel,
		submit
	};
}
function ProviderList({ selectedProviderId, filterModeHint, onSelectProvider }) {
	const { t } = useTranslation();
	const { providers } = useProviders();
	const { applyReorderedList } = useReorder("/providers", { revalidateOnSuccess: false });
	const { isSupported: isOvmsSupported } = useOvmsSupport();
	const [filterMode, setFilterMode] = (0, import_react.useState)(filterModeHint ?? "all");
	const [searchText, setSearchText] = (0, import_react.useState)("");
	const { models: allModels } = useModels(void 0, { fetchEnabled: Boolean(searchText.trim()) });
	const [dragging, setDragging] = (0, import_react.useState)(false);
	const [contextProviderId, setContextProviderId] = (0, import_react.useState)(null);
	const [expandedGroups, setExpandedGroups] = (0, import_react.useState)({});
	const handleToggleGroup = (0, import_react.useCallback)((presetProviderId) => {
		setExpandedGroups((prev) => ({
			...prev,
			[presetProviderId]: !prev[presetProviderId]
		}));
	}, []);
	const { isOpen: editorOpen, mode: editorMode, initialLogo, startAdd, startAddFrom, startEdit, cancel: cancelEditor, submit: submitEditor } = useProviderEditor({ onProviderCreated: onSelectProvider });
	const { deleteProvider } = useProviderDelete();
	const itemRefs = (0, import_react.useRef)(/* @__PURE__ */ new Map());
	const scrollerRef = (0, import_react.useRef)(null);
	const lastProvidersRef = (0, import_react.useRef)(providers);
	const lastSelectedProviderIdRef = (0, import_react.useRef)(selectedProviderId);
	(0, import_react.useEffect)(() => {
		if (!filterModeHint) return;
		setFilterMode(filterModeHint);
	}, [filterModeHint]);
	(0, import_react.useEffect)(() => {
		if (!selectedProviderId) return;
		const preset = providers.find((p) => p.id === selectedProviderId)?.presetProviderId;
		if (!preset) return;
		setExpandedGroups((prev) => prev[preset] ? prev : {
			...prev,
			[preset]: true
		});
	}, [providers, selectedProviderId]);
	const providerModelsIndex = (0, import_react.useMemo)(() => {
		if (!searchText.trim()) return null;
		const map = /* @__PURE__ */ new Map();
		for (const m of allModels) {
			const prev = map.get(m.providerId);
			const next = `${m.name} ${m.apiModelId ?? ""}`;
			map.set(m.providerId, prev ? `${prev} ${next}` : next);
		}
		return map;
	}, [allModels, searchText]);
	const filteredProviders = (0, import_react.useMemo)(() => {
		return providers.filter((provider) => {
			if (!isProviderSettingsListVisibleProvider(provider)) return false;
			if (provider.id === "ovms" && !isOvmsSupported) return false;
			if (filterMode === "enabled" && !provider.isEnabled) return false;
			if (filterMode === "disabled" && provider.isEnabled) return false;
			return matchKeywordsInProvider(searchText.toLowerCase().split(/\s+/).filter(Boolean), provider, providerModelsIndex?.get(provider.id));
		});
	}, [
		filterMode,
		isOvmsSupported,
		providers,
		providerModelsIndex,
		searchText
	]);
	const providerCounts = (0, import_react.useMemo)(() => providers.reduce((counts, provider) => {
		counts.set(provider.id, (counts.get(provider.id) ?? 0) + 1);
		return counts;
	}, /* @__PURE__ */ new Map()), [providers]);
	const groupedPresetIds = (0, import_react.useMemo)(() => getGroupedPresetIds(filteredProviders), [filteredProviders]);
	const presetSources = (0, import_react.useMemo)(() => providers.filter((provider) => isProviderPresetInstanceSource(provider) && isProviderSettingsListVisibleProvider(provider)), [providers]);
	const setProviderItemRef = (0, import_react.useCallback)((providerId, element) => {
		if (element) {
			itemRefs.current.set(providerId, element);
			return;
		}
		itemRefs.current.delete(providerId);
	}, []);
	const setScrollerRef = (0, import_react.useCallback)((element) => {
		scrollerRef.current = element;
	}, []);
	(0, import_react.useEffect)(() => {
		if (!selectedProviderId) return;
		const providersChanged = providers !== lastProvidersRef.current;
		const selectionChanged = selectedProviderId !== lastSelectedProviderIdRef.current;
		const wasEmpty = lastProvidersRef.current.length === 0;
		lastProvidersRef.current = providers;
		lastSelectedProviderIdRef.current = selectedProviderId;
		if (providersChanged && !selectionChanged && !wasEmpty) return;
		const scrollSelectedItem = () => {
			const selectedItem = itemRefs.current.get(selectedProviderId);
			const scroller = scrollerRef.current;
			if (!selectedItem || !scroller) return;
			const itemRect = selectedItem.getBoundingClientRect();
			const scrollerRect = scroller.getBoundingClientRect();
			if (itemRect.top >= scrollerRect.top && itemRect.bottom <= scrollerRect.bottom) return;
			selectedItem.scrollIntoView?.({
				block: "nearest",
				behavior: "auto"
			});
		};
		if (typeof window.requestAnimationFrame !== "function") {
			scrollSelectedItem();
			return;
		}
		const frameId = window.requestAnimationFrame(scrollSelectedItem);
		return () => window.cancelAnimationFrame(frameId);
	}, [providers, selectedProviderId]);
	const handleDragStateChange = (0, import_react.useCallback)((nextDragging) => {
		setDragging(nextDragging);
		if (nextDragging) setContextProviderId(null);
	}, []);
	const handleReorderError = (0, import_react.useCallback)(() => {
		toast.error(t("settings.provider.reorder_failed"));
	}, [t]);
	const handleSubmitEditor = (0, import_react.useCallback)(async (providerInput) => {
		await submitEditor(providerInput);
	}, [submitEditor]);
	const handleDeleteProvider = (0, import_react.useCallback)(async (providerId) => {
		await ConfirmActionPopup_default.show({
			title: t("settings.provider.delete.title"),
			content: t("settings.provider.delete.content"),
			danger: true,
			okText: t("common.delete"),
			action: () => deleteProvider(providerId)
		});
	}, [deleteProvider, t]);
	const renderProviderItem = (provider, _index, state) => {
		const showManagementActions = (providerCounts.get(provider.id) ?? 0) > 1 || canManageProvider(provider);
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderListItemWithContextMenu, {
			provider,
			selected: provider.id === selectedProviderId,
			contextOpen: contextProviderId === provider.id,
			onContextOpenChange: (open) => setContextProviderId(open ? provider.id : null),
			onSelect: () => onSelectProvider(provider.id),
			onEdit: () => startEdit(provider),
			onDelete: () => handleDeleteProvider(provider.id),
			onDuplicate: provider.presetProviderId && !groupedPresetIds.has(provider.presetProviderId) ? () => startAddFrom(provider) : void 0,
			showManagementActions,
			listState: state,
			onSetListItemRef: setProviderItemRef
		});
	};
	const handleAddAnother = (0, import_react.useCallback)((template) => startAddFrom(template), [startAddFrom]);
	const addProviderButton = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
		"data-ui": "settings.provider-list",
		type: "button",
		"aria-label": t("settings.provider.add.button_title"),
		disabled: dragging,
		onClick: startAdd,
		className: providerListClasses.addButton,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, {
			size: 14,
			strokeWidth: 2.5
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.provider.add.button_title") })]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("aside", {
		"data-ui": "settings.provider-list",
		className: `${providerListClasses.shell}`,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderListSearchField, {
				value: searchText,
				disabled: dragging,
				onValueChange: setSearchText,
				trailing: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderListHeaderFilterMenu, {
					filterMode,
					disabled: dragging,
					triggerClassName: providerListClasses.searchInlineAddButton,
					triggerIconSize: 12,
					onFilterChange: setFilterMode
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderListContent, {
				providers,
				visibleProviders: filteredProviders,
				selectedProviderId,
				searchActive: Boolean(searchText),
				expandedGroups,
				onToggleGroup: handleToggleGroup,
				onAddAnotherInGroup: handleAddAnother,
				scrollerRef: setScrollerRef,
				onDragStateChange: handleDragStateChange,
				onReorder: applyReorderedList,
				onReorderError: handleReorderError,
				renderItem: renderProviderItem
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: providerListClasses.addFooter,
				children: addProviderButton
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderEditorDrawer, {
				open: editorOpen,
				mode: editorMode,
				initialLogo,
				presetSources,
				onClose: cancelEditor,
				onSelectPreset: startAddFrom,
				onSubmit: handleSubmitEditor
			})
		]
	});
}
function useProviderEnable(providerId) {
	const { provider } = useProvider(providerId);
	const { updateProvider, enableProvider } = useProviderMutations(providerId);
	return { toggleProviderEnabled: (0, import_react.useCallback)(async (enabled) => {
		if (!provider) return;
		if (enabled) {
			await enableProvider();
			return;
		}
		await updateProvider({ isEnabled: false });
	}, [
		enableProvider,
		provider,
		updateProvider
	]) };
}
function isOpenAIOptionsProvider(provider) {
	return isOpenAICompatibleProvider(provider) || isAzureOpenAIProvider(provider);
}
function getProviderApiOptionsVisibility(provider) {
	const showApiFeatureSettings = !isSystemProvider(provider);
	const isSupportAnthropicPromptCache = isAnthropicSupportedProvider(provider);
	return {
		isOpenAIProvider: isOpenAIOptionsProvider(provider),
		isSupportAnthropicPromptCache,
		showApiFeatureSettings,
		hasVisibleApiOptions: showApiFeatureSettings || isSupportAnthropicPromptCache
	};
}
function hasVisibleProviderApiOptions(provider) {
	return getProviderApiOptionsVisibility(provider).hasVisibleApiOptions;
}
function useProviderMeta(providerId) {
	const { provider } = useProvider(providerId);
	const { i18n } = useTranslation();
	return (0, import_react.useMemo)(() => {
		const hideApiInput = provider ? isAwsBedrockProvider(provider) : false;
		const hideApiKeyInput = provider ? matchesPreset(provider, "copilot") || isVertexProvider(provider) : false;
		const isDmxapi = provider ? matchesPreset(provider, "dmxapi") : false;
		return {
			provider,
			fancyProviderName: provider ? getFancyProviderName(provider) : "",
			officialWebsite: provider?.websites?.official,
			apiKeyWebsite: provider?.websites?.apiKey,
			docsWebsite: provider?.websites?.docs,
			modelsWebsite: provider?.websites?.models,
			isAzureOpenAI: provider ? isAzureOpenAIProvider(provider) : false,
			isCherryIN: provider ? matchesPreset(provider, "cherryin") : false,
			isDmxapi,
			isChineseUser: i18n.language.startsWith("zh"),
			showApiOptionsButton: provider ? hasVisibleProviderApiOptions(provider) : false,
			isApiKeyFieldVisible: !hideApiInput && !hideApiKeyInput,
			isConnectionFieldVisible: !hideApiInput && !isDmxapi
		};
	}, [i18n.language, provider]);
}
const ANTHROPIC_CACHE_DEFAULT_TOKEN_THRESHOLD = 1024;
var CACHE_TOKEN_THRESHOLD_MAX = 1e5;
var CACHE_LAST_N_MAX = 10;
function clampInteger(value, min, max) {
	const parsed = Number(value);
	if (!Number.isFinite(parsed)) return min;
	return Math.min(max, Math.max(min, Math.trunc(parsed)));
}
function apiOptionId(providerId, key) {
	return `provider-api-option-${providerId}-${key}`;
}
function OptionTitle({ id, label, help }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
		"data-ui": "settings.option-title",
		className: "flex min-w-0 items-center gap-1.5",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("label", {
			htmlFor: id,
			className: "min-w-0 cursor-pointer truncate",
			children: label
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
			content: help,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "inline-flex size-4 shrink-0 items-center justify-center rounded-full text-foreground-tertiary",
				"aria-label": help,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Info, {
					className: "size-3",
					"aria-hidden": true
				})
			})
		})]
	});
}
function ProviderApiOptionsDrawer({ providerId, open, onClose }) {
	const { t } = useTranslation();
	const { provider, updateProvider } = useProvider(providerId);
	const cacheControl = provider?.settings?.cacheControl;
	const cacheTokenThreshold = cacheControl?.enabled === false ? 0 : cacheControl?.tokenThreshold ?? 1024;
	const cacheLastNMessages = cacheControl?.cacheLastNMessages ?? 2;
	const [tokenThresholdDraft, setTokenThresholdDraft] = (0, import_react.useState)(String(cacheTokenThreshold));
	const [cacheLastNDraft, setCacheLastNDraft] = (0, import_react.useState)(String(cacheLastNMessages));
	const effectiveCacheTokenThreshold = clampInteger(tokenThresholdDraft, 0, CACHE_TOKEN_THRESHOLD_MAX);
	(0, import_react.useEffect)(() => {
		if (!open) return;
		setTokenThresholdDraft(String(cacheTokenThreshold));
		setCacheLastNDraft(String(cacheLastNMessages));
	}, [
		cacheLastNMessages,
		cacheTokenThreshold,
		open
	]);
	const openAIOptions = (0, import_react.useMemo)(() => [
		{
			key: "developerRole",
			label: t("settings.provider.api.options.developer_role.label"),
			help: t("settings.provider.api.options.developer_role.help")
		},
		{
			key: "streamOptions",
			label: t("settings.provider.api.options.stream_options.label"),
			help: t("settings.provider.api.options.stream_options.help")
		},
		{
			key: "serviceTier",
			label: t("settings.provider.api.options.service_tier.label"),
			help: t("settings.provider.api.options.service_tier.help")
		},
		{
			key: "verbosity",
			label: t("settings.provider.api.options.verbosity.label"),
			help: t("settings.provider.api.options.verbosity.help")
		}
	], [t]);
	const options = (0, import_react.useMemo)(() => {
		if (!provider) return [];
		const visibility = getProviderApiOptionsVisibility(provider);
		if (!visibility.showApiFeatureSettings) return [];
		const items = [{
			key: "arrayContent",
			label: t("settings.provider.api.options.array_content.label"),
			help: t("settings.provider.api.options.array_content.help")
		}];
		if (visibility.isOpenAIProvider) items.push(...openAIOptions);
		return items;
	}, [
		openAIOptions,
		provider,
		t
	]);
	const handleSaveError = (0, import_react.useCallback)(() => {
		toast.error(t("settings.provider.save_failed"));
	}, [t]);
	const updateApiFeature = (0, import_react.useCallback)((key, checked) => {
		if (!provider) return;
		updateProvider({ apiFeatures: { [key]: checked } }).catch(handleSaveError);
	}, [
		handleSaveError,
		provider,
		updateProvider
	]);
	const updateCacheSettings = (0, import_react.useCallback)((updates) => {
		if (!provider) return;
		const next = {
			tokenThreshold: ANTHROPIC_CACHE_DEFAULT_TOKEN_THRESHOLD,
			cacheSystemMessage: true,
			cacheLastNMessages: 2,
			...provider.settings.cacheControl,
			...updates
		};
		updateProvider({ providerSettings: {
			...provider.settings,
			cacheControl: {
				...next,
				enabled: (next.tokenThreshold ?? 0) > 0
			}
		} }).catch(handleSaveError);
	}, [
		handleSaveError,
		provider,
		updateProvider
	]);
	const commitTokenThreshold = (0, import_react.useCallback)(() => {
		const next = clampInteger(tokenThresholdDraft, 0, CACHE_TOKEN_THRESHOLD_MAX);
		setTokenThresholdDraft(String(next));
		updateCacheSettings({
			enabled: next > 0,
			tokenThreshold: next
		});
	}, [tokenThresholdDraft, updateCacheSettings]);
	const commitCacheLastNMessages = (0, import_react.useCallback)(() => {
		const next = clampInteger(cacheLastNDraft, 0, CACHE_LAST_N_MAX);
		setCacheLastNDraft(String(next));
		updateCacheSettings({
			enabled: effectiveCacheTokenThreshold > 0,
			tokenThreshold: effectiveCacheTokenThreshold,
			cacheLastNMessages: next
		});
	}, [
		cacheLastNDraft,
		effectiveCacheTokenThreshold,
		updateCacheSettings
	]);
	if (!provider) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsDrawer, {
		open,
		onClose,
		title: t("settings.provider.api.options.label")
	});
	const isSupportAnthropicPromptCache = isAnthropicSupportedProvider(provider);
	const showCacheDetailOptions = effectiveCacheTokenThreshold > 0;
	const cacheSystemMessage = cacheControl?.cacheSystemMessage ?? true;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsDrawer, {
		open,
		onClose,
		title: t("settings.provider.api.options.label"),
		headerClassName: "px-6 pt-4 pb-2",
		bodyClassName: "space-y-0 px-6 pt-1 pb-6",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex min-w-0 flex-col gap-4",
			children: [options.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex flex-col gap-4",
				children: options.map((item) => {
					const id = apiOptionId(providerId, item.key);
					return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageSidePanelItem, {
						title: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OptionTitle, {
							id,
							label: item.label,
							help: item.help
						}),
						action: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Switch, {
							id,
							checked: provider.apiFeatures[item.key],
							onCheckedChange: (checked) => updateApiFeature(item.key, checked)
						})
					}, item.key);
				})
			}) : null, isSupportAnthropicPromptCache ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [options.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: drawerClasses.divider }) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex flex-col gap-4",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageSidePanelItem, {
					title: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OptionTitle, {
						id: apiOptionId(providerId, "cache-token-threshold"),
						label: t("settings.provider.api.options.anthropic_cache.token_threshold"),
						help: t("settings.provider.api.options.anthropic_cache.token_threshold_help")
					}),
					action: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
						id: apiOptionId(providerId, "cache-token-threshold"),
						type: "number",
						min: 0,
						max: CACHE_TOKEN_THRESHOLD_MAX,
						value: tokenThresholdDraft,
						onChange: (event) => setTokenThresholdDraft(event.target.value),
						onBlur: commitTokenThreshold,
						onKeyDown: (event) => {
							if (event.key === "Enter") event.currentTarget.blur();
						},
						className: cn(drawerClasses.input, "h-9 w-24 shrink-0 text-center")
					})
				}), showCacheDetailOptions ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageSidePanelItem, {
					title: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OptionTitle, {
						id: apiOptionId(providerId, "cache-last-n"),
						label: t("settings.provider.api.options.anthropic_cache.cache_last_n"),
						help: t("settings.provider.api.options.anthropic_cache.cache_last_n_help")
					}),
					action: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
						id: apiOptionId(providerId, "cache-last-n"),
						type: "number",
						min: 0,
						max: CACHE_LAST_N_MAX,
						value: cacheLastNDraft,
						onChange: (event) => setCacheLastNDraft(event.target.value),
						onBlur: commitCacheLastNMessages,
						onKeyDown: (event) => {
							if (event.key === "Enter") event.currentTarget.blur();
						},
						className: cn(drawerClasses.input, "h-9 w-24 shrink-0 text-center")
					})
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PageSidePanelItem, {
					title: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OptionTitle, {
						id: apiOptionId(providerId, "cache-system-message"),
						label: t("settings.provider.api.options.anthropic_cache.cache_system"),
						help: t("settings.provider.api.options.anthropic_cache.cache_system_help")
					}),
					action: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Switch, {
						id: apiOptionId(providerId, "cache-system-message"),
						checked: cacheSystemMessage,
						onCheckedChange: (checked) => updateCacheSettings({
							enabled: effectiveCacheTokenThreshold > 0,
							tokenThreshold: effectiveCacheTokenThreshold,
							cacheSystemMessage: checked
						})
					})
				})] }) : null]
			})] }) : null]
		})
	});
}
function ProviderHeader({ providerId }) {
	const { t } = useTranslation();
	const { provider } = useProvider(providerId);
	const meta = useProviderMeta(providerId);
	const { toggleProviderEnabled } = useProviderEnable(providerId);
	const [apiOptionsOpen, setApiOptionsOpen] = (0, import_react.useState)(false);
	const [isTogglingEnabled, setIsTogglingEnabled] = (0, import_react.useState)(false);
	const handleToggleEnabled = (0, import_react.useCallback)(async (enabled) => {
		if (isTogglingEnabled) return;
		setIsTogglingEnabled(true);
		try {
			await toggleProviderEnabled(enabled);
		} catch {
			toast.error(t("settings.provider.save_failed"));
		} finally {
			setIsTogglingEnabled(false);
		}
	}, [
		isTogglingEnabled,
		t,
		toggleProviderEnabled
	]);
	if (!provider) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-header",
		className: "flex min-h-7 items-center gap-3",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "flex min-w-0 flex-1 items-center gap-1.5",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "min-w-0 self-center",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex min-w-0 flex-wrap items-center gap-2",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h1", {
						className: "truncate font-semibold text-[15px] text-foreground leading-tight",
						children: meta.officialWebsite ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
							href: meta.officialWebsite,
							target: "_blank",
							rel: "noreferrer",
							className: "truncate",
							"aria-label": `${meta.fancyProviderName} · ${t("settings.provider.oauth.official_website")}`,
							children: meta.fancyProviderName
						}) : meta.fancyProviderName
					}), meta.showApiOptionsButton && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						content: t("settings.provider.api.options.label"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							type: "button",
							variant: "ghost",
							size: "icon",
							className: "size-7 shrink-0 rounded-lg p-0 text-muted-foreground shadow-none hover:bg-accent/40 hover:text-foreground",
							"aria-label": t("settings.provider.api.options.label"),
							onClick: () => setApiOptionsOpen(true),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bolt, {
								className: "size-3.5",
								"aria-hidden": true
							})
						})
					})]
				})
			})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Switch, {
			checked: provider.isEnabled,
			disabled: isTogglingEnabled,
			onCheckedChange: (enabled) => void handleToggleEnabled(enabled)
		})]
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderApiOptionsDrawer, {
		providerId,
		open: apiOptionsOpen,
		onClose: () => setApiOptionsOpen(false)
	})] });
}
var ApiKeyContext = (0, import_react.createContext)(null);
const ApiKeyProvider = ApiKeyContext.Provider;
function useAuthenticationApiKey() {
	const value = (0, import_react.use)(ApiKeyContext);
	if (!value) throw new Error("useAuthenticationApiKey must be used within AuthenticationSection");
	return value;
}
var logger$21 = loggerService.withContext("useProviderApiKey");
function getEnabledApiKeyString(apiKeysData) {
	return apiKeysData?.keys?.filter((item) => item.isEnabled).map((item) => item.key).join(",") ?? "";
}
function parseApiKeys(value) {
	const seenKeys = /* @__PURE__ */ new Set();
	return splitApiKeyString(formatApiKeys(value)).filter((key) => {
		if (seenKeys.has(key)) return false;
		seenKeys.add(key);
		return true;
	});
}
function toEnabledApiKeyString(value) {
	return parseApiKeys(value).join(",");
}
function toApiKeyEntries(value, apiKeysData) {
	const nextEnabledKeys = parseApiKeys(value);
	const existingKeys = apiKeysData?.keys ?? [];
	const existingEnabledKeys = existingKeys.filter((item) => item.isEnabled);
	const usedEntryIds = /* @__PURE__ */ new Set();
	const nextEntries = [];
	let enabledCursor = 0;
	for (const key of nextEnabledKeys) {
		const matchedEntry = existingKeys.find((item) => !usedEntryIds.has(item.id) && item.key.trim() === key);
		if (matchedEntry) {
			usedEntryIds.add(matchedEntry.id);
			nextEntries.push({
				...matchedEntry,
				key,
				isEnabled: true
			});
			continue;
		}
		while (enabledCursor < existingEnabledKeys.length && usedEntryIds.has(existingEnabledKeys[enabledCursor].id)) enabledCursor += 1;
		const reusableEnabledEntry = existingEnabledKeys[enabledCursor];
		if (reusableEnabledEntry) {
			usedEntryIds.add(reusableEnabledEntry.id);
			nextEntries.push({
				...reusableEnabledEntry,
				key,
				isEnabled: true
			});
			enabledCursor += 1;
			continue;
		}
		nextEntries.push({
			id: v4_default(),
			key,
			isEnabled: true
		});
	}
	const untouchedDisabledEntries = existingKeys.filter((item) => !item.isEnabled && !usedEntryIds.has(item.id));
	return [...nextEntries, ...untouchedDisabledEntries];
}
function createApiKeyValue(serverApiKey) {
	return {
		serverApiKey,
		inputApiKey: serverApiKey,
		hasPendingSync: false
	};
}
function isSameApiKeyValue(left, right) {
	return left.serverApiKey === right.serverApiKey && left.inputApiKey === right.inputApiKey && left.hasPendingSync === right.hasPendingSync;
}
function syncApiKeyValueFromServer(value, serverApiKey) {
	if (!value.hasPendingSync) return createApiKeyValue(serverApiKey);
	if (toEnabledApiKeyString(value.inputApiKey) === serverApiKey) return createApiKeyValue(serverApiKey);
	return {
		...value,
		serverApiKey,
		hasPendingSync: true
	};
}
function useProviderApiKey(providerId) {
	const { provider } = useProvider(providerId);
	const { data: apiKeysData } = useProviderApiKeys(providerId);
	const { updateApiKeys } = useProviderMutations(providerId);
	const serverApiKey = (0, import_react.useMemo)(() => getEnabledApiKeyString(apiKeysData), [apiKeysData]);
	const [value, setValue] = (0, import_react.useState)(() => createApiKeyValue(serverApiKey));
	const previousProviderIdRef = (0, import_react.useRef)(providerId);
	const valueRef = (0, import_react.useRef)(value);
	const saveApiKeyRef = (0, import_react.useRef)(async () => void 0);
	const saveApiKey = (0, import_react.useCallback)(async (value$1) => {
		if (!provider) return;
		if ([...value$1].some((character) => character.charCodeAt(0) > 255)) throw new Error("API key contains characters unsupported by HTTP headers");
		await updateApiKeys(toApiKeyEntries(value$1, apiKeysData));
	}, [
		apiKeysData,
		provider,
		updateApiKeys
	]);
	(0, import_react.useEffect)(() => {
		valueRef.current = value;
	}, [value]);
	(0, import_react.useEffect)(() => {
		saveApiKeyRef.current = saveApiKey;
	}, [saveApiKey]);
	const saveLater = (0, import_react.useMemo)(() => debounce((nextValue) => {
		saveApiKeyRef.current(nextValue).catch((error) => {
			logger$21.error("Failed to save API keys", error);
			toast.error(resolver_default.t("settings.provider.api_key.save_failed"));
			setValue((current) => ({
				...current,
				hasPendingSync: true
			}));
		});
	}, 150), []);
	(0, import_react.useEffect)(() => {
		const providerChanged = previousProviderIdRef.current !== providerId;
		previousProviderIdRef.current = providerId;
		const nextValue = providerChanged ? createApiKeyValue(serverApiKey) : syncApiKeyValueFromServer(valueRef.current, serverApiKey);
		if (!nextValue.hasPendingSync) saveLater.cancel();
		setValue((previousValue) => isSameApiKeyValue(previousValue, nextValue) ? previousValue : nextValue);
	}, [
		providerId,
		saveLater,
		serverApiKey
	]);
	(0, import_react.useEffect)(() => () => saveLater.flush(), [saveLater]);
	const setInputApiKey = (0, import_react.useCallback)((nextInputApiKey) => {
		const normalizedInputApiKey = toEnabledApiKeyString(nextInputApiKey);
		const hasPendingSync = normalizedInputApiKey !== valueRef.current.serverApiKey;
		setValue((previousValue) => {
			const nextValue = {
				...previousValue,
				inputApiKey: nextInputApiKey,
				hasPendingSync
			};
			return isSameApiKeyValue(previousValue, nextValue) ? previousValue : nextValue;
		});
		if (hasPendingSync) {
			saveLater(normalizedInputApiKey);
			return;
		}
		saveLater.cancel();
	}, [saveLater]);
	const commitInputApiKeyNow = (0, import_react.useCallback)(async () => {
		saveLater.cancel();
		const currentValue = valueRef.current;
		const normalizedInputApiKey = toEnabledApiKeyString(currentValue.inputApiKey);
		if (normalizedInputApiKey === currentValue.serverApiKey) return;
		try {
			await saveApiKeyRef.current(normalizedInputApiKey);
		} catch (error) {
			logger$21.error("Failed to save API keys", error);
			toast.error(resolver_default.t("settings.provider.api_key.save_failed"));
			setValue((current) => ({
				...current,
				hasPendingSync: true
			}));
			throw error;
		}
	}, [saveLater]);
	return (0, import_react.useMemo)(() => ({
		...value,
		setInputApiKey,
		commitInputApiKeyNow
	}), [
		commitInputApiKeyNow,
		setInputApiKey,
		value
	]);
}
const CLAUDE_CODE_PROVIDER_ID = "claude-code";
function isClaudeCodeProviderId(providerId) {
	return providerId === CLAUDE_CODE_PROVIDER_ID;
}
const OPENAI_CODEX_PROVIDER_ID = "openai-codex";
function isCodexProviderId(providerId) {
	return providerId === OPENAI_CODEX_PROVIDER_ID;
}
const GROK_CLI_PROVIDER_ID = "grok-cli";
function isGrokCliProviderId(providerId) {
	return providerId === GROK_CLI_PROVIDER_ID;
}
var logger$20 = loggerService.withContext("AwsBedrockSettings");
var AwsBedrockSettings = ({ providerId }) => {
	const { t } = useTranslation();
	const { provider, updateAuthConfig } = useProvider(providerId);
	const { data: authConfig } = useProviderAuthConfig(providerId);
	const { inputApiKey, setInputApiKey, commitInputApiKeyNow } = useAuthenticationApiKey();
	const isIamMode = provider?.authType === "iam-aws";
	const iamConfig = authConfig?.type === "iam-aws" ? authConfig : null;
	const apiKeyAwsConfig = authConfig?.type === "api-key-aws" ? authConfig : null;
	const currentRegion = iamConfig?.region ?? apiKeyAwsConfig?.region ?? "";
	const apiKeyWebsite = provider?.websites?.apiKey;
	const [localAccessKeyId, setLocalAccessKeyId] = (0, import_react.useState)(iamConfig?.accessKeyId ?? "");
	const [localSecretAccessKey, setLocalSecretAccessKey] = (0, import_react.useState)(iamConfig?.secretAccessKey ?? "");
	const [localRegion, setLocalRegion] = (0, import_react.useState)(currentRegion);
	const isIamDraftDirtyRef = (0, import_react.useRef)(false);
	const resetLocalIamConfig = (0, import_react.useCallback)(() => {
		setLocalAccessKeyId(iamConfig?.accessKeyId ?? "");
		setLocalSecretAccessKey(iamConfig?.secretAccessKey ?? "");
		setLocalRegion(currentRegion);
	}, [
		iamConfig?.accessKeyId,
		iamConfig?.secretAccessKey,
		currentRegion
	]);
	(0, import_react.useEffect)(() => {
		if (!isIamDraftDirtyRef.current) resetLocalIamConfig();
	}, [resetLocalIamConfig]);
	const markIamDraftDirty = () => {
		isIamDraftDirtyRef.current = true;
	};
	const ensureRegionProvided = () => {
		if (localRegion.trim().length > 0) return true;
		toast.warning(t("settings.provider.aws-bedrock.region_required"));
		return false;
	};
	const handleAuthTypeChange = async (value) => {
		if (!ensureRegionProvided()) return;
		try {
			const region = localRegion.trim();
			if (value === "iam") await updateAuthConfig({
				type: "iam-aws",
				region
			});
			else await updateAuthConfig({
				type: "api-key-aws",
				region
			});
			isIamDraftDirtyRef.current = false;
		} catch (error) {
			logger$20.error("Failed to update AWS Bedrock auth type", {
				providerId,
				error
			});
			toast.error(t("settings.provider.save_failed"));
		}
	};
	const saveIamConfig = async () => {
		if (!ensureRegionProvided()) return;
		try {
			await updateAuthConfig({
				type: "iam-aws",
				region: localRegion.trim(),
				accessKeyId: localAccessKeyId,
				secretAccessKey: localSecretAccessKey
			});
			isIamDraftDirtyRef.current = false;
		} catch (error) {
			logger$20.error("Failed to save AWS Bedrock IAM config", {
				providerId,
				error
			});
			toast.error(t("settings.provider.save_failed"));
			isIamDraftDirtyRef.current = false;
			resetLocalIamConfig();
		}
	};
	const saveApiKeyAwsRegion = async () => {
		try {
			await updateAuthConfig({
				type: "api-key-aws",
				region: localRegion.trim()
			});
			isIamDraftDirtyRef.current = false;
		} catch (error) {
			logger$20.error("Failed to save AWS Bedrock api-key region", {
				providerId,
				error
			});
			toast.error(t("settings.provider.save_failed"));
			isIamDraftDirtyRef.current = false;
			resetLocalIamConfig();
		}
	};
	const saveRegion = async () => {
		if (!ensureRegionProvided()) return;
		if (isIamMode) await saveIamConfig();
		else await saveApiKeyAwsRegion();
	};
	const authMode = isIamMode ? "iam" : "apiKey";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
			className: "mt-1.5",
			children: t("settings.provider.aws-bedrock.title")
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			"data-ui": "settings.aws-bedrock-settings.status",
			className: "mt-1.5 flex gap-2 rounded-md border border-primary/20 bg-primary/5 px-3 py-2.5 text-foreground text-sm",
			role: "status",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Info, {
				className: "mt-0.5 size-4 shrink-0 text-primary",
				"aria-hidden": true
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.provider.aws-bedrock.description") })]
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
			className: "mt-4",
			children: t("settings.provider.aws-bedrock.auth_type")
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RadioGroup, {
			className: "mt-1.5 flex flex-col gap-2",
			value: authMode,
			onValueChange: (v) => {
				handleAuthTypeChange(v);
			},
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex items-start gap-2",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroupItem, {
					value: "iam",
					id: "aws-bedrock-auth-iam",
					className: "mt-0.5"
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
					htmlFor: "aws-bedrock-auth-iam",
					className: "cursor-pointer font-normal leading-snug",
					children: t("settings.provider.aws-bedrock.auth_type_iam")
				})]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex items-start gap-2",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroupItem, {
					value: "apiKey",
					id: "aws-bedrock-auth-apikey",
					className: "mt-0.5"
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
					htmlFor: "aws-bedrock-auth-apikey",
					className: "cursor-pointer font-normal leading-snug",
					children: t("settings.provider.aws-bedrock.auth_type_api_key")
				})]
			})]
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("settings.provider.aws-bedrock.auth_type_help") }) }),
		isIamMode && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
				className: "mt-4",
				children: t("settings.provider.aws-bedrock.access_key_id")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				className: "mt-1.5 w-full",
				value: localAccessKeyId,
				placeholder: t("settings.provider.aws-bedrock.access_key_id"),
				onChange: (e) => {
					markIamDraftDirty();
					setLocalAccessKeyId(e.target.value);
				},
				onBlur: saveIamConfig
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("settings.provider.aws-bedrock.access_key_id_help") }) }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
				className: "mt-4",
				children: t("settings.provider.aws-bedrock.secret_access_key")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				className: "mt-1.5 w-full",
				type: "password",
				value: localSecretAccessKey,
				placeholder: t("settings.provider.aws-bedrock.secret_access_key"),
				onChange: (e) => {
					markIamDraftDirty();
					setLocalSecretAccessKey(e.target.value);
				},
				onBlur: saveIamConfig,
				spellCheck: false
			}),
			apiKeyWebsite && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ProviderHelpTextRow, {
				className: "justify-between",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RowFlex, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpLink, {
					target: "_blank",
					href: apiKeyWebsite,
					children: t("settings.provider.get_api_key")
				}) }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("settings.provider.aws-bedrock.secret_access_key_help") })]
			})
		] }),
		!isIamMode && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
				className: "mt-4",
				children: t("settings.provider.aws-bedrock.api_key")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				className: "mt-1.5 w-full",
				type: "password",
				value: inputApiKey,
				placeholder: t("settings.provider.aws-bedrock.api_key"),
				onChange: (e) => setInputApiKey(e.target.value),
				onBlur: () => void commitInputApiKeyNow(),
				spellCheck: false
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("settings.provider.aws-bedrock.api_key_help") }) })
		] }),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
			className: "mt-4",
			children: t("settings.provider.aws-bedrock.region")
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
			className: "mt-1.5 w-full",
			value: localRegion,
			placeholder: "us-east-1",
			onChange: (e) => {
				markIamDraftDirty();
				setLocalRegion(e.target.value);
			},
			onBlur: saveRegion
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("settings.provider.aws-bedrock.region_help") }) })
	] });
};
var AwsBedrockSettings_default = AwsBedrockSettings;
var logger$19 = loggerService.withContext("oauth");
var SILICON_CLIENT_ID = "SFaJLLq0y6CAMoyDm81aMu";
var PPIO_CLIENT_ID = "37d0828c96b34936a600b62c";
const oauthWithSiliconFlow = async (setKey) => {
	const authUrl = `https://account.siliconflow.cn/oauth?client_id=${SILICON_CLIENT_ID}`;
	const popup$1 = window.open(authUrl, "oauth", "width=720,height=720,toolbar=no,location=no,status=no,menubar=no,scrollbars=yes,resizable=yes,alwaysOnTop=yes,alwaysRaised=yes");
	const messageHandler = (event) => {
		if (event.data.length > 0 && event.data[0]["secretKey"] !== void 0) {
			setKey(event.data[0]["secretKey"]);
			popup$1?.close();
			window.removeEventListener("message", messageHandler);
		}
	};
	window.removeEventListener("message", messageHandler);
	window.addEventListener("message", messageHandler);
};
const oauthWithAihubmix = async (setKey) => {
	const authUrl = ` https://console.inferera.com/token?client_id=cherry_studio_oauth&lang=${await getLanguageCode()}&aff=SJyh`;
	const popup$1 = window.open(authUrl, "oauth", "width=720,height=720,toolbar=no,location=no,status=no,menubar=no,scrollbars=yes,resizable=yes,alwaysOnTop=yes,alwaysRaised=yes");
	const messageHandler = async (event) => {
		const data = event.data;
		if (data && data.key === "cherry_studio_oauth_callback") {
			const { iv, encryptedData } = data.data;
			try {
				const decryptedData = await window.api.aes.decrypt(encryptedData, iv, "");
				const { api_keys } = JSON.parse(decryptedData);
				if (api_keys && api_keys.length > 0) {
					setKey(api_keys[0].value);
					popup$1?.close();
					window.removeEventListener("message", messageHandler);
				}
			} catch (error) {
				logger$19.error("[oauthWithAihubmix] error", error);
				popup$1?.close();
				toast.error(resolver_default.t("settings.provider.oauth.error"));
			}
		}
	};
	window.removeEventListener("message", messageHandler);
	window.addEventListener("message", messageHandler);
};
const oauthWithPPIO = async (setKey) => {
	const authUrl = `https://ppio.com/oauth/authorize?invited_by=JYT9GD&client_id=${PPIO_CLIENT_ID}&scope=api%20openid&response_type=code&redirect_uri=${encodeURIComponent("cherrystudio://")}`;
	window.open(authUrl, "oauth", "width=720,height=720,toolbar=no,location=no,status=no,menubar=no,scrollbars=yes,resizable=yes,alwaysOnTop=yes,alwaysRaised=yes");
	if (!setKey) {
		logger$19.debug("[PPIO OAuth] No setKey callback provided, returning early");
		return;
	}
	logger$19.debug("[PPIO OAuth] Setting up protocol listener");
	return new Promise((resolve, reject) => {
		const removeListener = ipcApi.on("navigation.protocol_data", async (data) => {
			try {
				const url = new URL(data.url);
				if (!new URLSearchParams(url.search).get("code")) {
					reject(/* @__PURE__ */ new Error("No authorization code received"));
					return;
				}
				reject(/* @__PURE__ */ new Error("PPIO_APP_SECRET not configured. Please set RENDERER_VITE_PPIO_APP_SECRET environment variable."));
				return;
			} catch (error) {
				logger$19.error("[PPIO OAuth] Error processing callback:", error);
				reject(error);
			} finally {
				removeListener();
			}
		});
	});
};
const oauthWith302AI = async (setKey) => {
	const popup$1 = window.open("https://dash.302.ai/sso/login?app=cherry-ai.com&name=Cherry%20Studio", "oauth", "width=720,height=720,toolbar=no,location=no,status=no,menubar=no,scrollbars=yes,resizable=yes,alwaysOnTop=yes,alwaysRaised=yes");
	const messageHandler = (event) => {
		if (event.data && event.data.data.apikey !== void 0) {
			setKey(event.data.data.apikey);
			popup$1?.close();
			window.removeEventListener("message", messageHandler);
		}
	};
	window.removeEventListener("message", messageHandler);
	window.addEventListener("message", messageHandler);
};
const oauthWithAiOnly = async (setKey) => {
	const popup$1 = window.open(`https://maas.aiionly.com/login?inviteCode=1755481173663DrZBBOC0&cherryCode=01`, "login", "width=720,height=720,toolbar=no,location=no,status=no,menubar=no,scrollbars=yes,resizable=yes,alwaysOnTop=yes,alwaysRaised=yes");
	const messageHandler = (event) => {
		if (event.data.length > 0 && event.data[0]["secretKey"] !== void 0) {
			setKey(event.data[0]["secretKey"]);
			popup$1?.close();
			window.removeEventListener("message", messageHandler);
		}
	};
	window.removeEventListener("message", messageHandler);
	window.addEventListener("message", messageHandler);
};
const oauthWithCherryIn = async (setKey, config) => {
	const { oauthServer, apiHost } = config;
	const { authUrl, state } = await ipcApi.request("oauth.start_deep_link_flow", {
		providerId: SystemProviderIds.cherryin,
		oauthServer,
		apiHost
	});
	logger$19.debug("Opening authorization URL");
	window.open(authUrl, "oauth", "width=720,height=720,toolbar=no,location=no,status=no,menubar=no,scrollbars=yes,resizable=yes,alwaysOnTop=yes,alwaysRaised=yes");
	return new Promise((resolve, reject) => {
		let timeoutId = null;
		const removeListener = ipcApi.on("oauth.deep_link_result", async (result) => {
			if (result.state !== state) return;
			cleanup();
			if ("error" in result) {
				logger$19.error(`OAuth error: ${result.error}`);
				reject(new Error(result.error));
				return;
			}
			if (!result.apiKeys) {
				reject(/* @__PURE__ */ new Error("No API keys received"));
				return;
			}
			logger$19.debug("Successfully obtained API keys");
			try {
				await setKey(result.apiKeys);
			} catch (err) {
				reject(err);
				return;
			}
			resolve(result.apiKeys);
		});
		function cleanup() {
			removeListener();
			if (timeoutId) {
				clearTimeout(timeoutId);
				timeoutId = null;
			}
		}
		timeoutId = setTimeout(() => {
			logger$19.warn("Flow timed out");
			cleanup();
			reject(/* @__PURE__ */ new Error("OAuth flow timed out"));
		}, 600 * 1e3);
	});
};
const providerCharge = async (provider) => {
	const { url, width, height } = {
		silicon: {
			url: "https://cloud.siliconflow.cn/expensebill",
			width: 900,
			height: 700
		},
		aihubmix: {
			url: `https://console.inferera.com/topup?client_id=cherry_studio_oauth&lang=${await getLanguageCode()}&aff=SJyh`,
			width: 720,
			height: 900
		},
		ppio: {
			url: "https://ppio.com/user/register?invited_by=JYT9GD&utm_source=github_cherry-studio&redirect=/billing",
			width: 900,
			height: 700
		},
		"302ai": {
			url: "https://dash.302.ai/charge",
			width: 900,
			height: 700
		},
		aionly: {
			url: `https://maas.aiionly.com/recharge`,
			width: 900,
			height: 700
		}
	}[provider];
	window.open(url, "oauth", `width=${width},height=${height},toolbar=no,location=no,status=no,menubar=no,scrollbars=yes,resizable=yes,alwaysOnTop=yes,alwaysRaised=yes`);
};
const providerBills = async (provider) => {
	const { url, width, height } = {
		silicon: {
			url: "https://cloud.siliconflow.cn/bills",
			width: 900,
			height: 700
		},
		aihubmix: {
			url: `https://console.inferera.com/statistics?client_id=cherry_studio_oauth&lang=${await getLanguageCode()}&aff=SJyh`,
			width: 900,
			height: 700
		},
		ppio: {
			url: "https://ppio.com/user/register?invited_by=JYT9GD&utm_source=github_cherry-studio&redirect=/billing/billing-details",
			width: 900,
			height: 700
		},
		"302ai": {
			url: "https://dash.302.ai/charge",
			width: 900,
			height: 700
		},
		aionly: {
			url: `https://maas.aiionly.com/billManagement`,
			width: 900,
			height: 700
		}
	}[provider];
	window.open(url, "oauth", `width=${width},height=${height},toolbar=no,location=no,status=no,menubar=no,scrollbars=yes,resizable=yes,alwaysOnTop=yes,alwaysRaised=yes`);
};
var logger$18 = loggerService.withContext("CherryInOauth");
var CHERRYIN_OAUTH_SERVER = "https://open.cherryin.ai";
var CHERRYIN_TOPUP_URL = "https://open.cherryin.ai/console/topup";
function formatCurrency(value) {
	if (typeof value !== "number" || Number.isNaN(value)) return "-";
	return `$${value.toFixed(2)}`;
}
var CherryInOauth = ({ providerId }) => {
	const { provider, updateProvider, addApiKey, deleteApiKey } = useProvider(providerId);
	const { t } = useTranslation();
	const [isLoggingOut, setIsLoggingOut] = (0, import_react.useState)(false);
	const [isLoadingData, setIsLoadingData] = (0, import_react.useState)(false);
	const [balanceInfo, setBalanceInfo] = (0, import_react.useState)(null);
	const [oauthTokenOverride, setOauthTokenOverride] = (0, import_react.useState)(null);
	const [remoteHasOAuthToken, setRemoteHasOAuthToken] = (0, import_react.useState)(null);
	const refreshHasToken = (0, import_react.useCallback)(async () => {
		try {
			setRemoteHasOAuthToken(await ipcApi.request("oauth.has_token", { providerId }));
		} catch (error) {
			logger$18.warn("Failed to check CherryIN OAuth token status:", error);
			setRemoteHasOAuthToken(false);
		}
	}, [providerId]);
	(0, import_react.useEffect)(() => {
		refreshHasToken();
	}, [refreshHasToken]);
	const hasKeys = provider ? hasApiKeys(provider) : false;
	const isOAuthLoggedIn = hasKeys && (oauthTokenOverride ?? remoteHasOAuthToken ?? false);
	const fetchData = (0, import_react.useCallback)(async () => {
		setIsLoadingData(true);
		try {
			setBalanceInfo(await ipcApi.request("cherryin.get_balance", { apiHost: CHERRYIN_OAUTH_SERVER }));
		} catch (error) {
			logger$18.warn("Failed to fetch balance:", error);
			setBalanceInfo(null);
		} finally {
			setIsLoadingData(false);
		}
	}, []);
	(0, import_react.useEffect)(() => {
		if (isOAuthLoggedIn) fetchData();
		else setBalanceInfo(null);
	}, [fetchData, isOAuthLoggedIn]);
	(0, import_react.useEffect)(() => {
		if (oauthTokenOverride !== null && remoteHasOAuthToken !== null && remoteHasOAuthToken === oauthTokenOverride) setOauthTokenOverride(null);
	}, [oauthTokenOverride, remoteHasOAuthToken]);
	const handleOAuthLogin = (0, import_react.useCallback)(async () => {
		try {
			await oauthWithCherryIn(async (apiKeys) => {
				const keys$1 = apiKeys.split(",").map((key) => key.trim()).filter(Boolean);
				await Promise.all(keys$1.map((key) => addApiKey(key, "OAuth")));
				await updateProvider({ isEnabled: true });
				setOauthTokenOverride(true);
				refreshHasToken();
				await fetchData();
				toast.success(t("auth.get_key_success"));
			}, { oauthServer: CHERRYIN_OAUTH_SERVER });
		} catch (error) {
			logger$18.error("OAuth error:", error);
			toast.error(t("settings.provider.oauth.error"));
		}
	}, [
		addApiKey,
		fetchData,
		refreshHasToken,
		t,
		updateProvider
	]);
	const handleLogout = (0, import_react.useCallback)(async () => {
		if (!await popup.confirm({
			title: t("settings.provider.oauth.logout"),
			content: t("settings.provider.oauth.logout_confirm"),
			centered: true
		})) return;
		setIsLoggingOut(true);
		try {
			await ipcApi.request("cherryin.logout", { apiHost: CHERRYIN_OAUTH_SERVER });
			setOauthTokenOverride(false);
			setBalanceInfo(null);
			refreshHasToken();
			const oauthKeys = provider?.apiKeys.filter((key) => key.label === "OAuth") ?? [];
			const rejectedDeletes = (await Promise.allSettled(oauthKeys.map((key) => deleteApiKey(key.id)))).filter((result) => result.status === "rejected");
			if (rejectedDeletes.length > 0) {
				logger$18.warn(`Failed to delete ${rejectedDeletes.length} CherryIN OAuth key(s) after logout`);
				toast.warning(t("settings.provider.oauth.logout_warning"));
				return;
			}
			toast.success(t("settings.provider.oauth.logout_success"));
		} catch (error) {
			logger$18.error("Logout error:", error);
			toast.warning(t("settings.provider.oauth.logout_warning"));
		} finally {
			setIsLoggingOut(false);
		}
	}, [
		deleteApiKey,
		provider?.apiKeys,
		refreshHasToken,
		t
	]);
	const handleTopup = (0, import_react.useCallback)(() => {
		window.open(CHERRYIN_TOPUP_URL, "_blank");
	}, []);
	if (!provider) return null;
	if (remoteHasOAuthToken === null && hasKeys) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.cherry-in-oauth",
		className: oauthCardClasses.container,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: oauthCardClasses.shell,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "h-5 w-55" }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "mt-2 h-4 w-full" }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: "mt-2 h-4 w-[82%]" })
			]
		})
	});
	if (!isOAuthLoggedIn) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.cherry-in-oauth",
		className: oauthCardClasses.container,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: oauthCardClasses.shell,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: oauthCardClasses.loggedInRow,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: oauthCardClasses.profileMeta,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CherryinIcon.Avatar, {
						shape: "circle",
						size: 40
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: oauthCardClasses.nameBlock,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: oauthCardClasses.loggedInName,
							children: t("settings.provider.oauth.cherryIn.not_logged_in")
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: cn(oauthCardClasses.loggedInEmail, "text-muted-foreground"),
							children: t("settings.provider.oauth.cherryIn.tagline")
						})]
					})]
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					variant: "emphasis",
					onClick: handleOAuthLogin,
					children: t("settings.provider.oauth.cherryIn.login_button")
				})]
			})
		})
	});
	const profileName = balanceInfo?.profile?.displayName || balanceInfo?.profile?.username || balanceInfo?.profile?.email || provider.name;
	const profileEmail = balanceInfo?.profile?.email || t("settings.provider.oauth.cherryIn.logged_in");
	const profileGroup = balanceInfo?.profile?.group && balanceInfo.profile.group !== "default" ? balanceInfo.profile.group : null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.cherry-in-oauth",
		className: oauthCardClasses.container,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: cn(oauthCardClasses.shellLoggedIn, "text-muted-foreground"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: oauthCardClasses.loggedInRow,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: oauthCardClasses.profileMeta,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CherryinIcon.Avatar, {
						shape: "circle",
						size: 40
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: oauthCardClasses.nameBlock,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: oauthCardClasses.nameRow,
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: cn(oauthCardClasses.loggedInName, "text-foreground"),
								children: profileName
							}), profileGroup ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: oauthCardClasses.badge,
								children: profileGroup
							}) : null]
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: cn(oauthCardClasses.loggedInEmail, "text-muted-foreground"),
							children: profileEmail
						})]
					})]
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: cn(oauthCardClasses.loggedInActions, "gap-1.5"),
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: cn(oauthCardClasses.inlineBalanceBlock, "mr-1 flex items-baseline gap-1.5 text-left"),
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
								className: cn(oauthCardClasses.inlineBalanceLabel, "text-muted-foreground"),
								children: t("settings.provider.oauth.balance")
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: cn(oauthCardClasses.inlineBalanceValue, "text-foreground"),
								children: isLoadingData && !balanceInfo ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, { className: `${oauthCardClasses.balanceValueSkeleton} h-5` }) : formatCurrency(balanceInfo?.balance)
							})]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							className: cn(oauthCardClasses.topupPrimaryButton, "h-7 px-2.5 py-0"),
							onClick: handleTopup,
							size: "sm",
							variant: "default",
							children: t("settings.provider.oauth.topup")
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							className: cn(oauthCardClasses.logoutCompact, "h-7 px-2 py-0 text-muted-foreground"),
							disabled: isLoggingOut,
							onClick: handleLogout,
							variant: "ghost",
							children: t("settings.provider.oauth.logout")
						})
					]
				})]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
				className: cn(oauthCardClasses.serviceAttribution, "text-muted-foreground"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trans, {
					i18nKey: "settings.provider.oauth.cherryIn.service_attribution",
					components: { link: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
						className: cn(oauthCardClasses.serviceLink, "text-muted-foreground"),
						href: CHERRYIN_OAUTH_SERVER,
						rel: "noreferrer",
						target: "_blank"
					}, "cherryin-service-link") }
				})
			})]
		})
	});
};
var CherryInOauth_default = CherryInOauth;
var LEGAL_AND_COMPLIANCE_URL = "https://code.claude.com/docs/en/legal-and-compliance";
var logger$17 = loggerService.withContext("ClaudeCodeSettings");
var LOGIN_COMMAND = "claude /login";
var ClaudeCodeSettings = ({ providerId }) => {
	const { t } = useTranslation();
	const [loggedIn, setLoggedIn] = (0, import_react.useState)(null);
	const [checking, setChecking] = (0, import_react.useState)(false);
	const [launching, setLaunching] = (0, import_react.useState)(false);
	const checkLogin = (0, import_react.useCallback)(async () => {
		setChecking(true);
		try {
			setLoggedIn(await ipcApi.request("oauth.check_external_login", { providerId }));
		} catch (error) {
			logger$17.error("Failed to check Claude login status", error);
			setLoggedIn(false);
		} finally {
			setChecking(false);
		}
	}, [providerId]);
	(0, import_react.useEffect)(() => {
		checkLogin();
	}, [checkLogin]);
	const handleOpenTerminal = (0, import_react.useCallback)(async () => {
		setLaunching(true);
		try {
			const { homePath } = await ipcApi.request("app.get_info");
			const result = await ipcApi.request("code_cli.run", {
				mode: "login-flow",
				cliTool: CodeCli.CLAUDE_CODE,
				directory: homePath
			});
			if (!result.success) {
				logger$17.error("Failed to launch Claude login terminal", { message: result.message });
				toast.error(t("settings.provider.claude_code.launch_failed"));
			}
		} catch (error) {
			logger$17.error("Failed to launch Claude login terminal", error);
			toast.error(t("settings.provider.claude_code.launch_failed"));
		} finally {
			setLaunching(false);
		}
	}, [t]);
	const handleCopyCommand = (0, import_react.useCallback)(async () => {
		try {
			await navigator.clipboard.writeText(LOGIN_COMMAND);
			toast.success(t("common.copied"));
		} catch (error) {
			logger$17.error("Failed to copy login command", error);
			toast.error(t("common.copy_failed"));
		}
	}, [t]);
	if (loggedIn === null) return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.claude-code-settings",
		className: "flex items-center gap-2 text-foreground-tertiary text-xs",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RefreshCw, {
			className: "size-4 animate-spin",
			"aria-hidden": true
		}), t("common.loading")]
	});
	const agentOnlyNote = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.claude-code-settings",
		className: "mt-1 text-xs",
		children: [
			t("settings.provider.claude_code.agent_only_note"),
			" ",
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpLink, {
				className: "mx-0",
				href: LEGAL_AND_COMPLIANCE_URL,
				target: "_blank",
				rel: "noreferrer",
				children: t("settings.provider.claude_code.legal_link")
			})
		]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.claude-code-settings",
		className: "flex flex-col gap-3",
		children: loggedIn ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex items-center gap-3 rounded-lg border border-success-border bg-success-subtle p-3 text-success-subtle-foreground",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleCheck, {
					className: "size-5 shrink-0 text-success",
					"aria-hidden": true
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "min-w-0 flex-1",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "text-sm",
							children: t("settings.provider.claude_code.logged_in")
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "mt-1 text-xs",
							children: t("settings.provider.claude_code.logged_in_detail")
						}),
						agentOnlyNote
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
					variant: "secondary",
					size: "sm",
					disabled: checking,
					onClick: () => void checkLogin(),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RefreshCw, { className: "size-4" }), t("settings.provider.claude_code.recheck")]
				})
			]
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex flex-col gap-3 rounded-lg border border-info-border bg-info-subtle p-3 text-info-subtle-foreground",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex gap-3",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleAlert, {
					className: "mt-0.5 size-5 shrink-0 text-info",
					"aria-hidden": true
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "min-w-0 flex-1",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "text-sm",
							children: t("settings.provider.claude_code.description")
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "mt-1 text-xs",
							children: t("settings.provider.claude_code.description_detail")
						}),
						agentOnlyNote
					]
				})]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex flex-wrap items-center gap-2",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						disabled: launching,
						onClick: () => void handleOpenTerminal(),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SquareTerminal, { className: "size-4" }), t("settings.provider.claude_code.open_terminal")]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						variant: "secondary",
						onClick: () => void handleCopyCommand(),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Copy, { className: "size-4" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("code", {
							className: "font-mono",
							children: LOGIN_COMMAND
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						variant: "secondary",
						disabled: checking,
						onClick: () => void checkLogin(),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RefreshCw, { className: "size-4" }), t("settings.provider.claude_code.recheck")]
					})
				]
			})]
		})
	});
};
var ClaudeCodeSettings_default = ClaudeCodeSettings;
var PlatformDomain = /* @__PURE__ */ function(PlatformDomain$1) {
	PlatformDomain$1["OFFICIAL"] = "www.DMXAPI.cn";
	PlatformDomain$1["INTERNATIONAL"] = "www.DMXAPI.com";
	PlatformDomain$1["OVERSEA"] = "ssvip.DMXAPI.com";
	return PlatformDomain$1;
}(PlatformDomain || {});
function resolveDmxPlatformFromProvider(provider) {
	if (!provider?.endpointConfigs) return PlatformDomain.OFFICIAL;
	const firstUrl = Object.values(provider.endpointConfigs)[0]?.baseUrl;
	if (!firstUrl) return PlatformDomain.OFFICIAL;
	if (firstUrl.includes("DMXAPI.com") || firstUrl.includes("dmxapi.com")) return firstUrl.includes("ssvip") ? PlatformDomain.OVERSEA : PlatformDomain.INTERNATIONAL;
	return PlatformDomain.OFFICIAL;
}
var DmxapiSettings = ({ providerId }) => {
	const { provider, updateProvider } = useProvider(providerId);
	const { t } = useTranslation();
	const PlatformOptions = [
		{
			label: t("settings.provider.dmxapi.platform_official"),
			value: PlatformDomain.OFFICIAL,
			apiKeyWebsite: "https://www.dmxapi.cn/register?aff=bwwY"
		},
		{
			label: t("settings.provider.dmxapi.platform_international"),
			value: PlatformDomain.INTERNATIONAL,
			apiKeyWebsite: "https://www.dmxapi.com/register"
		},
		{
			label: t("settings.provider.dmxapi.platform_enterprise"),
			value: PlatformDomain.OVERSEA,
			apiKeyWebsite: "https://ssvip.dmxapi.com/register"
		}
	];
	const [selectedPlatform, setSelectedPlatform] = (0, import_react.useState)(() => resolveDmxPlatformFromProvider(provider));
	(0, import_react.useEffect)(() => {
		setSelectedPlatform(resolveDmxPlatformFromProvider(provider));
	}, [provider]);
	const handlePlatformChange = (0, import_react.useCallback)(async (domain) => {
		const next = domain;
		const previous = resolveDmxPlatformFromProvider(provider);
		if (next === previous) return;
		setSelectedPlatform(next);
		const newEndpointConfigs = replaceEndpointConfigDomain(provider?.endpointConfigs, next);
		try {
			await updateProvider({ endpointConfigs: newEndpointConfigs });
		} catch {
			setSelectedPlatform(previous);
			toast.error(t("settings.provider.save_failed"));
		}
	}, [
		provider,
		t,
		updateProvider
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.dmxapi-settings",
		className: "mt-4 mb-7.5",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "mb-7.5 flex flex-col items-center justify-center",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DmxapiIcon, {
				height: 70,
				width: "auto"
			})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex w-full flex-col gap-2",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
				className: "mt-1.5",
				children: t("settings.provider.dmxapi.select_platform")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroup, {
				className: "flex w-full flex-col gap-2",
				value: selectedPlatform,
				onValueChange: (v) => {
					handlePlatformChange(v);
				},
				children: PlatformOptions.map((option) => {
					const id = `dmx-platform-${option.value}`;
					return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex items-start gap-2",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroupItem, {
							value: option.value,
							id,
							className: "mt-0.5"
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
							htmlFor: id,
							className: "max-w-full cursor-pointer font-normal leading-snug",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
								option.label,
								" ",
								/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("a", {
									href: option.apiKeyWebsite,
									target: "_blank",
									rel: "noopener noreferrer",
									className: "text-link",
									children: [
										"(",
										t("settings.provider.get_api_key"),
										")"
									]
								})
							] })
						})]
					}, option.value);
				})
			})]
		})]
	});
};
var DmxapiSettings_default = DmxapiSettings;
var logger$16 = loggerService.withContext("GithubCopilotSettings");
var AuthStatus = /* @__PURE__ */ function(AuthStatus$1) {
	AuthStatus$1[AuthStatus$1["NOT_STARTED"] = 0] = "NOT_STARTED";
	AuthStatus$1[AuthStatus$1["CODE_GENERATED"] = 1] = "CODE_GENERATED";
	AuthStatus$1[AuthStatus$1["AUTHENTICATED"] = 2] = "AUTHENTICATED";
	return AuthStatus$1;
}(AuthStatus || {});
var GithubCopilotSettings = ({ providerId }) => {
	const { t } = useTranslation();
	const { provider, updateProvider, addApiKey, deleteApiKey } = useProvider(providerId);
	const username = provider?.settings?.oauthUsername;
	const avatar = provider?.settings?.oauthAvatar;
	const defaultHeaders = provider?.settings?.extraHeaders;
	const [authStatus, setAuthStatus] = (0, import_react.useState)(AuthStatus.NOT_STARTED);
	const [deviceCode, setDeviceCode] = (0, import_react.useState)("");
	const [userCode, setUserCode] = (0, import_react.useState)("");
	const [verificationUri, setVerificationUri] = (0, import_react.useState)("");
	const [loading, setLoading] = (0, import_react.useState)(false);
	const [verificationPageOpened, setVerificationPageOpened] = (0, import_react.useState)(false);
	const [currentStep, setCurrentStep] = (0, import_react.useState)(0);
	const providerRateLimit = provider?.settings?.rateLimit ?? 10;
	const rateLimitRef = (0, import_react.useRef)(providerRateLimit);
	const [rateLimit, setRateLimit] = (0, import_react.useState)(providerRateLimit);
	(0, import_react.useEffect)(() => {
		if (provider?.settings?.isAuthed) {
			setAuthStatus(AuthStatus.AUTHENTICATED);
			setCurrentStep(3);
		} else {
			setAuthStatus(AuthStatus.NOT_STARTED);
			setCurrentStep(0);
			setDeviceCode("");
			setUserCode("");
			setVerificationUri("");
			setVerificationPageOpened(false);
		}
	}, [provider?.settings?.isAuthed]);
	(0, import_react.useEffect)(() => {
		setRateLimit(providerRateLimit);
		rateLimitRef.current = providerRateLimit;
	}, [providerRateLimit]);
	const handleGetDeviceCode = (0, import_react.useCallback)(async () => {
		try {
			setLoading(true);
			setCurrentStep(1);
			const { device_code, user_code, verification_uri } = await window.api.copilot.getAuthMessage(defaultHeaders);
			logger$16.debug("device_code", device_code);
			logger$16.debug("user_code", user_code);
			logger$16.debug("verification_uri", verification_uri);
			setDeviceCode(device_code);
			setUserCode(user_code);
			setVerificationUri(verification_uri);
			setAuthStatus(AuthStatus.CODE_GENERATED);
			try {
				await navigator.clipboard.writeText(user_code);
				toast.success(t("settings.provider.copilot.code_copied"));
			} catch (error) {
				logger$16.error("Failed to copy to clipboard:", error);
			}
		} catch (error) {
			logger$16.error("Failed to get device code:", error);
			toast.error(t("settings.provider.copilot.code_failed"));
			setCurrentStep(0);
		} finally {
			setLoading(false);
		}
	}, [t, defaultHeaders]);
	const handleGetToken = (0, import_react.useCallback)(async () => {
		try {
			setLoading(true);
			setCurrentStep(3);
			const { access_token } = await window.api.copilot.getCopilotToken(deviceCode, defaultHeaders);
			await window.api.copilot.saveCopilotToken(access_token);
			const { token } = await window.api.copilot.getToken(defaultHeaders);
			if (token) {
				const { login, avatar: userAvatar } = await window.api.copilot.getUser(access_token);
				setAuthStatus(AuthStatus.AUTHENTICATED);
				await addApiKey(token, "Copilot");
				await updateProvider({
					isEnabled: true,
					providerSettings: {
						...provider?.settings,
						isAuthed: true,
						oauthUsername: login,
						oauthAvatar: userAvatar
					}
				});
				toast.success(t("settings.provider.copilot.auth_success"));
			}
		} catch (error) {
			logger$16.error("Failed to get token:", error);
			toast.error(t("settings.provider.copilot.auth_failed"));
			setCurrentStep(2);
		} finally {
			setLoading(false);
		}
	}, [
		deviceCode,
		t,
		provider?.settings,
		addApiKey,
		updateProvider,
		defaultHeaders
	]);
	const handleLogout = (0, import_react.useCallback)(async () => {
		try {
			setLoading(true);
			const copilotKey = provider?.apiKeys.find((k) => k.label === "Copilot");
			if (copilotKey) await deleteApiKey(copilotKey.id);
			await updateProvider({ providerSettings: {
				...provider?.settings,
				isAuthed: false,
				oauthUsername: "",
				oauthAvatar: "",
				extraHeaders: {}
			} });
			await window.api.copilot.logout();
			setAuthStatus(AuthStatus.NOT_STARTED);
			setDeviceCode("");
			setUserCode("");
			setVerificationUri("");
			setVerificationPageOpened(false);
			setCurrentStep(0);
			toast.success(t("settings.provider.copilot.logout_success"));
		} catch (error) {
			logger$16.error("Failed to logout:", error);
			toast.error(t("settings.provider.copilot.logout_failed"));
		} finally {
			setLoading(false);
		}
	}, [
		t,
		provider?.apiKeys,
		provider?.settings,
		deleteApiKey,
		updateProvider
	]);
	const handleCopyUserCode = (0, import_react.useCallback)(async () => {
		try {
			await navigator.clipboard.writeText(userCode);
			toast.success(t("common.copied"));
		} catch (error) {
			logger$16.error("Failed to copy to clipboard:", error);
			toast.error(t("common.copy_failed"));
		}
	}, [userCode, t]);
	const handleOpenVerificationPage = (0, import_react.useCallback)(() => {
		if (verificationUri) {
			window.open(verificationUri, "_blank");
			setVerificationPageOpened(true);
			setCurrentStep(2);
		}
	}, [verificationUri]);
	const getSteps = () => [
		{
			title: t("settings.provider.copilot.step_get_code"),
			description: t("settings.provider.copilot.step_get_code_desc"),
			status: currentStep > 0 ? "finish" : currentStep === 0 ? "process" : "wait"
		},
		{
			title: t("settings.provider.copilot.step_copy_code"),
			description: t("settings.provider.copilot.step_copy_code_desc"),
			status: currentStep > 1 ? "finish" : currentStep === 1 ? "process" : "wait"
		},
		{
			title: t("settings.provider.copilot.step_authorize"),
			description: t("settings.provider.copilot.step_authorize_desc"),
			status: currentStep > 2 ? "finish" : currentStep === 2 ? "process" : "wait"
		},
		{
			title: t("settings.provider.copilot.step_connect"),
			description: t("settings.provider.copilot.step_connect_desc"),
			status: currentStep >= 3 ? "finish" : "wait"
		}
	];
	const handleRateLimitChange = async (value) => {
		try {
			await updateProvider({ providerSettings: {
				...provider?.settings,
				rateLimit: value
			} });
		} catch (error) {
			logger$16.error("Failed to save Copilot rate limit", {
				providerId,
				error
			});
			toast.error(t("settings.provider.save_failed"));
			setRateLimit(providerRateLimit);
			rateLimitRef.current = providerRateLimit;
		}
	};
	const stepDotClass = (status) => cn("mt-0.5 mb-0.5 size-2.5 shrink-0 rounded-full border-2 border-background", status === "finish" && "bg-primary", status === "process" && "bg-primary ring-2 ring-primary/30", status === "wait" && "bg-muted", status === "error" && "bg-destructive");
	const renderAuthContent = () => {
		switch (authStatus) {
			case AuthStatus.AUTHENTICATED: return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-ui": "settings.render-auth-content",
				className: "mb-5",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex gap-3 rounded-lg border border-success-border bg-success-subtle p-3 text-success-subtle-foreground",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleCheck, {
						className: "mt-0.5 size-5 shrink-0 text-success",
						"aria-hidden": true
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex min-w-0 flex-1 items-center justify-between gap-3",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "flex min-w-0 items-center gap-2",
							children: [avatar ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
								src: avatar,
								alt: "",
								className: "size-5 shrink-0 rounded-full",
								loading: "lazy"
							}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "truncate text-sm",
								children: username || t("settings.provider.copilot.auth_success_title")
							})]
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							variant: "destructive",
							size: "sm",
							disabled: loading,
							onClick: handleLogout,
							children: t("settings.provider.copilot.logout")
						})]
					})]
				})
			});
			case AuthStatus.CODE_GENERATED: return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				"data-ui": "settings.render-auth-content",
				className: "mb-5 flex flex-col gap-4 md:flex-row md:gap-6",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex min-w-50 flex-1 flex-col gap-2",
					children: getSteps().map((step, idx) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex gap-2",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: stepDotClass(step.status) }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "min-w-0",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "text-foreground text-sm leading-tight",
								children: step.title
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "mt-1 text-muted-foreground text-xs leading-snug",
								children: step.description
							})]
						})]
					}, idx))
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex min-w-0 flex-2 flex-col gap-4",
					children: [
						currentStep >= 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "rounded-lg border border-border bg-muted/40 p-4 transition-colors hover:border-border",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: "mb-3 flex items-start gap-3",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: cn("flex size-6 shrink-0 items-center justify-center rounded-full font-bold text-primary-foreground text-xs", currentStep > 1 ? "bg-primary" : "bg-primary"),
									children: "2"
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: "text-foreground text-sm",
									children: t("settings.provider.copilot.step_copy_code")
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: "mt-0.5 text-foreground-tertiary text-xs",
									children: t("settings.provider.copilot.step_copy_code_detail")
								})] })]
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: "flex min-h-6 flex-row items-center justify-between",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
									value: userCode,
									readOnly: true,
									className: "mr-2 font-mono font-semibold text-sm"
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
									type: "button",
									variant: "secondary",
									onClick: handleCopyUserCode,
									children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Copy, { className: "size-4" }), t("common.copy")]
								})]
							})]
						}),
						currentStep >= 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "rounded-lg border border-border bg-muted/40 p-4 transition-colors hover:border-border",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
									className: "mb-3 flex items-start gap-3",
									children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
										className: "flex size-6 shrink-0 items-center justify-center rounded-full bg-primary font-bold text-primary-foreground text-xs",
										children: "3"
									}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
										className: "text-foreground text-sm",
										children: t("settings.provider.copilot.step_authorize")
									}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
										className: "mt-0.5 text-foreground-tertiary text-xs",
										children: t("settings.provider.copilot.step_authorize_detail")
									})] })]
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
									type: "button",
									variant: "secondary",
									className: "mb-2",
									onClick: handleOpenVerificationPage,
									children: t("settings.provider.copilot.open_verification_page")
								}),
								verificationUri ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
									className: "ml-1 break-all text-foreground-tertiary text-xs",
									children: verificationUri
								}) : null
							]
						}),
						currentStep >= 2 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "rounded-lg border border-border bg-muted/40 p-4 transition-colors hover:border-border",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: "mb-3 flex items-start gap-3",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: "flex size-6 shrink-0 items-center justify-center rounded-full bg-primary font-bold text-primary-foreground text-xs",
									children: "4"
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: "text-foreground text-sm",
									children: t("settings.provider.copilot.step_connect")
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: "mt-0.5 text-foreground-tertiary text-xs",
									children: t("settings.provider.copilot.step_connect_detail")
								})] })]
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
								content: !verificationPageOpened ? t("settings.provider.copilot.open_verification_first") : "",
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
									disabled: !verificationPageOpened || loading,
									onClick: handleGetToken,
									children: t("settings.provider.copilot.connect")
								})
							})]
						})
					]
				})]
			});
			default: return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-ui": "settings.render-auth-content",
				className: "mb-5",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex gap-3 rounded-lg border border-info-border bg-info-subtle p-3 text-info-subtle-foreground",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleAlert, {
							className: "mt-0.5 size-5 shrink-0 text-info",
							"aria-hidden": true
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "min-w-0 flex-1",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "text-sm",
								children: t("settings.provider.copilot.description")
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "mt-1 text-xs",
								children: t("settings.provider.copilot.description_detail")
							})]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							className: "shrink-0 self-center",
							disabled: loading,
							onClick: handleGetDeviceCode,
							children: t("settings.provider.copilot.start_auth")
						})
					]
				})
			});
		}
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.github-copilot-settings",
		className: "pt-3.75",
		children: [renderAuthContent(), authStatus === AuthStatus.AUTHENTICATED && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "mt-5 flex min-h-6 flex-row items-center justify-between",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
				className: "mt-0",
				children: t("settings.provider.copilot.rate_limit")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "w-50",
				onPointerUp: () => {
					handleRateLimitChange(rateLimitRef.current);
				},
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Slider, {
					className: "w-full",
					value: [rateLimit],
					min: 1,
					max: 60,
					step: 1,
					marks: [
						{
							value: 1,
							label: "1"
						},
						{
							value: 10,
							label: t("common.default")
						},
						{
							value: 60,
							label: "60"
						}
					],
					onValueChange: ([v]) => {
						setRateLimit(v);
						rateLimitRef.current = v;
					}
				})
			})]
		})]
	});
};
var GithubCopilotSettings_default = GithubCopilotSettings;
var logger$15 = loggerService.withContext("GpuStackSettings");
var GpuStackSettings = ({ providerId }) => {
	const { provider, updateProvider } = useProvider(providerId);
	const { t } = useTranslation();
	const keepAliveTime = provider?.settings?.keepAliveTime ?? 0;
	const [keepAliveMinutes, setKeepAliveMinutes] = (0, import_react.useState)(keepAliveTime);
	(0, import_react.useEffect)(() => {
		setKeepAliveMinutes(provider?.settings?.keepAliveTime ?? 0);
	}, [provider?.settings?.keepAliveTime]);
	const handleBlur = async () => {
		if (keepAliveMinutes === keepAliveTime) return;
		try {
			await updateProvider({ providerSettings: {
				...provider?.settings,
				keepAliveTime: keepAliveMinutes
			} });
		} catch (error) {
			logger$15.error("Failed to save GPUStack keep alive time", {
				providerId,
				error
			});
			toast.error(t("settings.provider.save_failed"));
			setKeepAliveMinutes(keepAliveTime);
		}
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.gpu-stack-settings",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
				className: "mb-1",
				children: t("gpustack.keep_alive_time.title")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "w-full [&>div]:block [&>div]:w-full",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(editable_number_default, {
					value: keepAliveMinutes,
					min: 0,
					step: 5,
					suffix: t("gpustack.keep_alive_time.placeholder"),
					align: "start",
					changeOnBlur: false,
					onChange: (v) => setKeepAliveMinutes(Number(v ?? 0)),
					onBlur: () => {
						handleBlur();
					}
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("gpustack.keep_alive_time.description") }) })
		]
	});
};
var GpuStackSettings_default = GpuStackSettings;
var logger$14 = loggerService.withContext("LmStudioSettings");
var LmStudioSettings = ({ providerId }) => {
	const { provider, updateProvider } = useProvider(providerId);
	const { t } = useTranslation();
	const keepAliveTime = provider?.settings?.keepAliveTime ?? 0;
	const [keepAliveMinutes, setKeepAliveMinutes] = (0, import_react.useState)(keepAliveTime);
	(0, import_react.useEffect)(() => {
		setKeepAliveMinutes(provider?.settings?.keepAliveTime ?? 0);
	}, [provider?.settings?.keepAliveTime]);
	const handleBlur = async () => {
		if (keepAliveMinutes === keepAliveTime) return;
		try {
			await updateProvider({ providerSettings: {
				...provider?.settings,
				keepAliveTime: keepAliveMinutes
			} });
		} catch (error) {
			logger$14.error("Failed to save LM Studio keep alive time", {
				providerId,
				error
			});
			toast.error(t("settings.provider.save_failed"));
			setKeepAliveMinutes(keepAliveTime);
		}
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.lm-studio-settings",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
				className: "mb-1",
				children: t("lmstudio.keep_alive_time.title")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "w-full [&>div]:block [&>div]:w-full",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(editable_number_default, {
					value: keepAliveMinutes,
					min: 0,
					step: 5,
					suffix: t("lmstudio.keep_alive_time.placeholder"),
					align: "start",
					changeOnBlur: false,
					onChange: (v) => setKeepAliveMinutes(Math.floor(Number(v ?? 0))),
					onBlur: () => {
						handleBlur();
					}
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("lmstudio.keep_alive_time.description") }) })
		]
	});
};
var LmStudioSettings_default = LmStudioSettings;
var logger$13 = loggerService.withContext("LoginOauthPanel");
var LoginOauthPanel = ({ providerId, i18nNs, showAccountId = false }) => {
	const { t } = useTranslation();
	const { updateProvider } = useProvider(providerId);
	const ns = `settings.provider.${i18nNs}`;
	const [loggedIn, setLoggedIn] = (0, import_react.useState)(null);
	const [accountId, setAccountId] = (0, import_react.useState)(null);
	const [signingIn, setSigningIn] = (0, import_react.useState)(false);
	const [loggingOut, setLoggingOut] = (0, import_react.useState)(false);
	const refreshStatus = (0, import_react.useCallback)(async () => {
		try {
			const hasToken = await ipcApi.request("oauth.has_token", { providerId });
			setLoggedIn(hasToken);
			setAccountId(hasToken && showAccountId ? (await ipcApi.request("oauth.get_account", { providerId })).accountId : null);
		} catch (error) {
			logger$13.error(`Failed to check ${providerId} login status`, error);
			setLoggedIn(false);
		}
	}, [providerId, showAccountId]);
	(0, import_react.useEffect)(() => {
		refreshStatus();
	}, [refreshStatus]);
	const handleSignIn = (0, import_react.useCallback)(async () => {
		setSigningIn(true);
		try {
			const account = await ipcApi.request("oauth.sign_in", { providerId });
			setLoggedIn(true);
			setAccountId(account.accountId);
			await updateProvider({ isEnabled: true });
			toast.success(t(`${ns}.sign_in_success`));
		} catch (error) {
			logger$13.error(`${providerId} sign-in failed`, error);
			toast.error(t(`${ns}.sign_in_failed`));
		} finally {
			setSigningIn(false);
		}
	}, [
		providerId,
		ns,
		t,
		updateProvider
	]);
	const handleLogout = (0, import_react.useCallback)(async () => {
		if (!await popup.confirm({
			title: t("settings.provider.oauth.logout"),
			content: t("settings.provider.oauth.logout_confirm"),
			centered: true
		})) return;
		setLoggingOut(true);
		try {
			await ipcApi.request("oauth.logout", { providerId });
			await updateProvider({
				authConfig: { type: "api-key" },
				isEnabled: false
			});
			setLoggedIn(false);
			setAccountId(null);
			toast.success(t("settings.provider.oauth.logout_success"));
		} catch (error) {
			logger$13.error(`${providerId} logout failed`, error);
			toast.warning(t("settings.provider.oauth.logout_warning"));
		} finally {
			setLoggingOut(false);
		}
	}, [
		providerId,
		t,
		updateProvider
	]);
	if (loggedIn === null) return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.login-oauth-panel",
		className: "flex items-center gap-2 pt-3.75 text-foreground-tertiary text-xs",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RefreshCw, {
			className: "size-4 animate-spin",
			"aria-hidden": true
		}), t("common.loading")]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.login-oauth-panel",
		className: "flex flex-col gap-3",
		children: loggedIn ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex items-center gap-3 rounded-lg border border-success-border bg-success-subtle p-3 text-success-subtle-foreground",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleCheck, {
					className: "size-5 shrink-0 text-success",
					"aria-hidden": true
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "min-w-0 flex-1",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "text-sm",
						children: t(`${ns}.logged_in`)
					}), showAccountId && accountId ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "mt-1 truncate text-xs",
						children: t(`${ns}.account`, { accountId })
					}) : null]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					variant: "ghost",
					size: "sm",
					disabled: loggingOut,
					onClick: handleLogout,
					children: t("settings.provider.oauth.logout")
				})
			]
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex flex-col gap-3 rounded-lg border border-info-border bg-info-subtle p-3 text-info-subtle-foreground",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex gap-3",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleAlert, {
					className: "mt-0.5 size-5 shrink-0 text-info",
					"aria-hidden": true
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "min-w-0 flex-1",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "text-sm",
						children: t(`${ns}.description`)
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "mt-1 text-xs",
						children: t(`${ns}.description_detail`)
					})]
				})]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
				disabled: signingIn,
				onClick: () => void handleSignIn(),
				children: [signingIn ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RefreshCw, { className: "size-4 animate-spin" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LogIn, { className: "size-4" }), signingIn ? t(`${ns}.signing_in`) : t(`${ns}.sign_in_button`)]
			}) })]
		})
	});
};
var LoginOauthPanel_default = LoginOauthPanel;
var statusIcon = {
	running: CircleCheck,
	"not-running": TriangleAlert,
	"not-installed": CircleX
};
var OvmsSettings = () => {
	const { t } = useTranslation();
	const { isSupported } = useOvmsSupport();
	const [ovmsStatus, setOvmsStatus] = (0, import_react.useState)("not-running");
	const [isInstallingOvms, setIsInstallingOvms] = (0, import_react.useState)(false);
	const [isRunningOvms, setIsRunningOvms] = (0, import_react.useState)(false);
	const [isStoppingOvms, setIsStoppingOvms] = (0, import_react.useState)(false);
	(0, import_react.useEffect)(() => {
		const checkStatus = async () => {
			if (!isSupported) return;
			setOvmsStatus(await ipcApi.request("ovms.get_status"));
		};
		checkStatus();
	}, [isSupported]);
	const installOvms = async () => {
		try {
			setIsInstallingOvms(true);
			await ipcApi.request("ovms.install_binary");
			setOvmsStatus(await ipcApi.request("ovms.get_status"));
			setIsInstallingOvms(false);
		} catch (error) {
			const errMsg = error instanceof Error ? error.message : String(error);
			const errCodeMsg = {
				"100": t("ovms.failed.install_code_100"),
				"101": t("ovms.failed.install_code_101"),
				"102": t("ovms.failed.install_code_102"),
				"103": t("ovms.failed.install_code_103"),
				"104": t("ovms.failed.install_code_104"),
				"105": t("ovms.failed.install_code_105"),
				"106": t("ovms.failed.install_code_106"),
				"110": t("ovms.failed.install_code_110")
			};
			const match = errMsg.match(/code (\d+)/);
			const code = match ? match[1] : "unknown";
			const errorMsg = code in errCodeMsg ? errCodeMsg[code] ?? errMsg : errMsg;
			toast.error(t("ovms.failed.install") + errorMsg);
			setIsInstallingOvms(false);
		}
	};
	const runOvms = async () => {
		try {
			setIsRunningOvms(true);
			await ipcApi.request("ovms.start");
			setOvmsStatus(await ipcApi.request("ovms.get_status"));
			setIsRunningOvms(false);
		} catch (error) {
			toast.error(t("ovms.failed.run") + (error instanceof Error ? error.message : String(error)));
			setIsRunningOvms(false);
		}
	};
	const stopOvms = async () => {
		try {
			setIsStoppingOvms(true);
			await ipcApi.request("ovms.stop");
			setOvmsStatus(await ipcApi.request("ovms.get_status"));
			setIsStoppingOvms(false);
		} catch (error) {
			toast.error(t("ovms.failed.stop") + (error instanceof Error ? error.message : String(error)));
			setIsStoppingOvms(false);
		}
	};
	const bannerClasses = cn$1("w-full rounded-lg border px-3 py-3 text-sm", ovmsStatus === "running" && "border-success-border bg-success-subtle text-success-subtle-foreground", ovmsStatus === "not-running" && "border-warning-border bg-warning-subtle text-warning-subtle-foreground", ovmsStatus === "not-installed" && "border-error-border bg-error-subtle text-error-subtle-foreground");
	const getStatusMessage = () => {
		switch (ovmsStatus) {
			case "running": return t("ovms.status.running");
			case "not-running": return t("ovms.status.not_running");
			case "not-installed": return t("ovms.status.not_installed");
			default: return t("ovms.status.unknown");
		}
	};
	const StatusIcon = statusIcon[ovmsStatus];
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.ovms-settings.status",
		className: bannerClasses,
		role: "status",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ColFlex, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex min-h-6 w-full flex-row items-center justify-between",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex min-w-0 flex-1 items-start gap-2",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusIcon, {
						className: cn$1("mt-0.5 size-4 shrink-0", ovmsStatus === "running" && "text-success", ovmsStatus === "not-running" && "text-warning", ovmsStatus === "not-installed" && "text-destructive"),
						"aria-hidden": true
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
						className: "mt-0 font-normal",
						children: getStatusMessage()
					})]
				}),
				ovmsStatus === "not-installed" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					onClick: installOvms,
					disabled: isInstallingOvms,
					size: "sm",
					children: isInstallingOvms ? t("ovms.action.installing") : t("ovms.action.install")
				}),
				ovmsStatus === "not-running" && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex gap-2",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						onClick: installOvms,
						disabled: isInstallingOvms || isRunningOvms,
						size: "sm",
						children: isInstallingOvms ? t("ovms.action.installing") : t("ovms.action.reinstall")
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						onClick: runOvms,
						disabled: isRunningOvms || isInstallingOvms,
						size: "sm",
						children: isRunningOvms ? t("ovms.action.starting") : t("ovms.action.run")
					})]
				}),
				ovmsStatus === "running" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					variant: "destructive",
					onClick: stopOvms,
					disabled: isStoppingOvms,
					size: "sm",
					children: isStoppingOvms ? t("ovms.action.stopping") : t("ovms.action.stop")
				})
			]
		}) })
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.ovms-settings.status",
		className: "mt-1.5 flex gap-2 rounded-md border border-primary/20 bg-primary/5 px-3 py-2.5 text-foreground text-sm",
		role: "status",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Info, {
			className: "mt-0.5 size-4 shrink-0 text-primary",
			"aria-hidden": true
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "min-w-0 flex-1 space-y-1",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
				className: "font-medium",
				children: t("ovms.guide")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trans, {
				i18nKey: "ovms.description",
				components: {
					div: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {}),
					dev: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {}),
					p: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {}),
					a: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
						className: "text-link",
						href: "https://github.com/openvinotoolkit/model_server/blob/c55551763d02825829337b62c2dcef9339706f79/docs/deploying_server_baremetal.md",
						rel: "noreferrer",
						target: "_blank"
					})
				}
			}) })]
		})]
	})] });
};
var OvmsSettings_default = OvmsSettings;
var API_KEY_OAUTH_LAUNCHERS = {
	silicon: oauthWithSiliconFlow,
	aihubmix: oauthWithAihubmix,
	ppio: oauthWithPPIO,
	"302ai": oauthWith302AI,
	aionly: oauthWithAiOnly
};
var OauthButton = ({ provider, onSuccess, ...buttonProps }) => {
	const { t } = useTranslation();
	const onAuth = () => {
		const handleSuccess = (key) => {
			if (key.trim()) {
				onSuccess?.(key);
				toast.success(t("auth.get_key_success"));
			}
		};
		API_KEY_OAUTH_LAUNCHERS[provider.id]?.(handleSuccess);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		onClick: onAuth,
		className: "rounded-full",
		...buttonProps,
		children: t("settings.provider.oauth.button", { provider: t(getProviderLabelKey(provider.id)) })
	});
};
var OauthButton_default = OauthButton;
var ProviderOauth = ({ providerId }) => {
	const { t } = useTranslation();
	const { provider, updateProvider, addApiKey } = useProvider(providerId);
	const Icon = useIcon(resolveProviderIconRef(providerId));
	const setApiKey = async (newKey) => {
		await addApiKey(newKey, "OAuth");
		await updateProvider({ isEnabled: true });
	};
	if (!provider) return null;
	const officialWebsite = provider.websites?.official;
	const providerWebsite = officialWebsite?.replace(/^https?:\/\//, "").replace(/\/.*$/, "") || provider.name;
	const serviceDescription = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trans, {
		i18nKey: "settings.provider.oauth.description",
		components: { website: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
			className: "text-inherit",
			href: officialWebsite ?? "",
			rel: "noreferrer",
			target: "_blank"
		}) },
		values: { provider: providerWebsite }
	});
	if (!hasApiKeys(provider)) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.provider-oauth",
		className: oauthCardClasses.container,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: oauthCardClasses.shell,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: oauthCardClasses.loggedInRow,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: oauthCardClasses.profileMeta,
					children: [Icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon.Avatar, { size: 40 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "flex size-10 shrink-0 items-center justify-center rounded-full bg-muted font-bold text-[18px]",
						children: provider.name[0]
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: oauthCardClasses.nameBlock,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: oauthCardClasses.loggedInName,
							children: t(getProviderLabelKey(provider.id))
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: oauthCardClasses.loggedInEmail,
							children: serviceDescription
						})]
					})]
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OauthButton_default, {
					provider: { id: provider.id },
					onSuccess: setApiKey,
					variant: "emphasis",
					className: ""
				})]
			})
		})
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-oauth",
		className: "flex flex-col items-center justify-center gap-3 py-3 pb-2",
		children: [
			Icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon.Avatar, { size: 60 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex size-15 shrink-0 items-center justify-center rounded-full bg-muted font-bold text-[24px]",
				children: provider.name[0]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RowFlex, {
				className: "gap-2.5",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
					className: "rounded-lg px-3 py-1.5 text-[13px] shadow-none",
					onClick: () => providerCharge(provider.id),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleDollarSign, {
						"aria-hidden": true,
						className: "size-4 shrink-0 text-primary-foreground"
					}), t("settings.provider.charge")]
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
					className: "rounded-lg px-3 py-1.5 text-[13px] shadow-none",
					onClick: () => providerBills(provider.id),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReceiptText, {
						"aria-hidden": true,
						className: "size-4 shrink-0 text-primary-foreground"
					}), t("settings.provider.bills")]
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex items-center gap-1.5 text-[13px] text-muted-foreground leading-[1.35]",
				children: serviceDescription
			})
		]
	});
};
var ProviderOauth_default = ProviderOauth;
var TOKEN_FACTORY_URL = "https://developer.amd.com.cn/radeon/tokenfactory?source=cherry-studio";
function RadeonCloudBenefits() {
	const { t } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.radeon-cloud-benefits.note",
		"data-testid": "radeon-cloud-benefits",
		className: "flex gap-3 rounded-lg border border-success/30 bg-success/10 p-3",
		role: "note",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Gift, {
			className: "mt-0.5 size-5 shrink-0 text-success",
			"aria-hidden": true
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "min-w-0 flex-1",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "font-medium text-foreground text-sm",
					children: t("settings.provider.radeon_cloud.benefits.title")
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "mt-1 text-foreground-muted text-xs leading-relaxed",
					children: t("settings.provider.radeon_cloud.benefits.description")
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ProviderHelpLink, {
					className: "mx-0 mt-2 inline-flex items-center gap-1",
					href: TOKEN_FACTORY_URL,
					target: "_blank",
					rel: "noreferrer",
					children: [t("settings.provider.radeon_cloud.benefits.cta"), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SquareArrowOutUpRight, {
						className: "size-3",
						"aria-hidden": true
					})]
				})
			]
		})]
	});
}
const DEFAULT_VERTEX_AI_LOCATIONS = [
	{
		value: "global",
		label: "global"
	},
	{
		value: "us-central1",
		label: "us-central1"
	},
	{
		value: "us-east1",
		label: "us-east1"
	},
	{
		value: "us-west1",
		label: "us-west1"
	},
	{
		value: "europe-west1",
		label: "europe-west1"
	},
	{
		value: "europe-west4",
		label: "europe-west4"
	},
	{
		value: "asia-east1",
		label: "asia-east1"
	},
	{
		value: "asia-northeast1",
		label: "asia-northeast1"
	},
	{
		value: "asia-southeast1",
		label: "asia-southeast1"
	}
];
var getStringField = (value, field) => {
	const fieldValue = value[field];
	if (typeof fieldValue !== "string") return;
	return fieldValue.trim() || void 0;
};
function parseVertexAIServiceAccountJson(value) {
	const trimmed = value.trim().replace(/^\uFEFF/, "");
	if (!trimmed.startsWith("{")) return;
	try {
		const parsed = JSON.parse(trimmed);
		if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) return;
		const credentials = parsed;
		const privateKey = getStringField(credentials, "private_key");
		const clientEmail = getStringField(credentials, "client_email");
		if (!privateKey || !clientEmail) return;
		return {
			privateKey,
			clientEmail,
			projectId: getStringField(credentials, "project_id")
		};
	} catch {
		return;
	}
}
var logger$12 = loggerService.withContext("VertexAiSettings");
var VertexAiSettings = ({ providerId }) => {
	const { t } = useTranslation();
	const { provider } = useProvider(providerId);
	const { data: authConfig } = useProviderAuthConfig(providerId);
	const { updateAuthConfig: saveAuthConfigToServer } = useProviderMutations(providerId);
	const gcpConfig = authConfig?.type === "iam-gcp" ? authConfig : null;
	const credentials = gcpConfig?.credentials;
	const [localProjectId, setLocalProjectId] = (0, import_react.useState)(gcpConfig?.project ?? "");
	const [localLocation, setLocalLocation] = (0, import_react.useState)(gcpConfig?.location ?? "");
	const [localPrivateKey, setLocalPrivateKey] = (0, import_react.useState)(credentials?.privateKey ?? "");
	const [localClientEmail, setLocalClientEmail] = (0, import_react.useState)(credentials?.clientEmail ?? "");
	const [serviceAccountJson, setServiceAccountJson] = (0, import_react.useState)("");
	const [serviceAccountJsonError, setServiceAccountJsonError] = (0, import_react.useState)(false);
	const [showClientEmail, setShowClientEmail] = (0, import_react.useState)(false);
	const [showPrivateKey, setShowPrivateKey] = (0, import_react.useState)(false);
	const [showProjectId, setShowProjectId] = (0, import_react.useState)(false);
	const [dropdownOpen, setDropdownOpen] = (0, import_react.useState)(false);
	const isDraftDirtyRef = (0, import_react.useRef)(false);
	const activeSaveRequestsRef = (0, import_react.useRef)(0);
	const isJsonImportingRef = (0, import_react.useRef)(false);
	const isSelectingLocationRef = (0, import_react.useRef)(false);
	const resetLocalAuthConfig = (0, import_react.useCallback)(() => {
		setLocalProjectId(gcpConfig?.project ?? "");
		setLocalLocation(gcpConfig?.location ?? "");
		setLocalPrivateKey(credentials?.privateKey ?? "");
		setLocalClientEmail(credentials?.clientEmail ?? "");
		if (!serviceAccountJsonError) {
			setServiceAccountJson("");
			setServiceAccountJsonError(false);
		}
	}, [
		credentials?.clientEmail,
		credentials?.privateKey,
		gcpConfig?.location,
		gcpConfig?.project,
		serviceAccountJsonError
	]);
	(0, import_react.useEffect)(() => {
		if (!isDraftDirtyRef.current && activeSaveRequestsRef.current === 0) resetLocalAuthConfig();
	}, [resetLocalAuthConfig]);
	const markDraftDirty = () => {
		isDraftDirtyRef.current = true;
	};
	const apiKeyWebsite = provider?.websites?.apiKey;
	const saveAuthConfig = async () => {
		activeSaveRequestsRef.current++;
		try {
			await saveAuthConfigToServer({
				type: "iam-gcp",
				project: localProjectId,
				location: localLocation,
				credentials: {
					privateKey: localPrivateKey,
					clientEmail: localClientEmail
				}
			});
			if (activeSaveRequestsRef.current === 1) isDraftDirtyRef.current = false;
		} catch (error) {
			logger$12.error("Failed to save Vertex AI auth config", {
				providerId,
				error
			});
			toast.error(t("settings.provider.save_failed"));
			if (activeSaveRequestsRef.current === 1) {
				isDraftDirtyRef.current = false;
				resetLocalAuthConfig();
			}
		} finally {
			activeSaveRequestsRef.current--;
		}
	};
	const handleServiceAccountJsonChange = async (e) => {
		const value = e.target.value;
		setServiceAccountJson(value);
		if (serviceAccountJsonError) setServiceAccountJsonError(false);
		const trimmed = value.trim();
		if (!trimmed) return;
		const parsed = parseVertexAIServiceAccountJson(trimmed);
		if (parsed) {
			setLocalPrivateKey(parsed.privateKey);
			setLocalClientEmail(parsed.clientEmail);
			if (parsed.projectId) setLocalProjectId(parsed.projectId);
			isJsonImportingRef.current = true;
			activeSaveRequestsRef.current++;
			try {
				await saveAuthConfigToServer({
					type: "iam-gcp",
					project: parsed.projectId ?? localProjectId,
					location: localLocation,
					credentials: {
						privateKey: parsed.privateKey,
						clientEmail: parsed.clientEmail
					}
				});
				setTimeout(() => {
					setServiceAccountJson("");
				}, 0);
				setServiceAccountJsonError(false);
				toast.success(t("settings.provider.vertex_ai.service_account.json_parse_success"));
			} catch (error) {
				logger$12.error("Failed to save Vertex AI auth config from JSON import", {
					providerId,
					error
				});
				toast.error(t("settings.provider.save_failed"));
				setServiceAccountJsonError(true);
			} finally {
				activeSaveRequestsRef.current--;
				isJsonImportingRef.current = false;
			}
		}
	};
	const handleServiceAccountJsonBlur = async () => {
		if (isJsonImportingRef.current) return;
		const value = serviceAccountJson.trim();
		if (!value) return;
		const parsed = parseVertexAIServiceAccountJson(value);
		if (!parsed) {
			toast.error(t("settings.provider.vertex_ai.service_account.json_parse_error"));
			setServiceAccountJsonError(true);
			return;
		}
		setLocalPrivateKey(parsed.privateKey);
		setLocalClientEmail(parsed.clientEmail);
		if (parsed.projectId) setLocalProjectId(parsed.projectId);
		activeSaveRequestsRef.current++;
		try {
			await saveAuthConfigToServer({
				type: "iam-gcp",
				project: parsed.projectId ?? localProjectId,
				location: localLocation,
				credentials: {
					privateKey: parsed.privateKey,
					clientEmail: parsed.clientEmail
				}
			});
			setServiceAccountJson("");
			setServiceAccountJsonError(false);
			toast.success(t("settings.provider.vertex_ai.service_account.json_parse_success"));
		} catch (error) {
			logger$12.error("Failed to save Vertex AI auth config from JSON import", {
				providerId,
				error
			});
			toast.error(t("settings.provider.save_failed"));
			setServiceAccountJsonError(true);
		} finally {
			activeSaveRequestsRef.current--;
		}
	};
	const handleLocationSelect = (value) => {
		setLocalLocation(value);
		setDropdownOpen(false);
		saveAuthConfigWithLocation(value);
		isSelectingLocationRef.current = false;
	};
	const saveAuthConfigWithLocation = async (locationValue) => {
		const trimmedLocation = locationValue.trim();
		const previousLocation = localLocation;
		activeSaveRequestsRef.current++;
		try {
			await saveAuthConfigToServer({
				type: "iam-gcp",
				project: localProjectId,
				location: trimmedLocation,
				credentials: {
					privateKey: localPrivateKey,
					clientEmail: localClientEmail
				}
			});
			if (activeSaveRequestsRef.current === 1) isDraftDirtyRef.current = false;
		} catch (error) {
			logger$12.error("Failed to save Vertex AI auth config with location", {
				providerId,
				error
			});
			toast.error(t("settings.provider.save_failed"));
			setLocalLocation(previousLocation);
			if (activeSaveRequestsRef.current === 1) isDraftDirtyRef.current = false;
		} finally {
			activeSaveRequestsRef.current--;
		}
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.vertex-ai-settings",
		className: "flex flex-col",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
				className: "mt-1.5",
				children: t("settings.provider.vertex_ai.service_account.title")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				"data-ui": "settings.vertex-ai-settings.status",
				className: "mt-1.5 flex gap-2 rounded-md border border-primary/20 bg-primary/5 px-3 py-2.5 text-foreground text-sm",
				role: "status",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Info, {
					className: "mt-0.5 size-4 shrink-0 text-primary",
					"aria-hidden": true
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.provider.vertex_ai.service_account.description") })]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "mt-1.5 flex items-baseline gap-1.5",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
					className: "mt-0",
					children: t("settings.provider.vertex_ai.service_account.json_input")
				}), apiKeyWebsite && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpLink, {
					target: "_blank",
					href: apiKeyWebsite,
					className: "mx-0",
					children: t("settings.provider.get_api_key")
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input$1, {
				className: "mt-1.5 min-h-10 w-full px-3 py-1.5 text-sm",
				value: serviceAccountJson,
				placeholder: t("settings.provider.vertex_ai.service_account.json_input_placeholder"),
				onChange: handleServiceAccountJsonChange,
				onBlur: handleServiceAccountJsonBlur,
				spellCheck: false,
				rows: 1
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, {
				className: serviceAccountJsonError ? "text-destructive" : void 0,
				children: serviceAccountJsonError ? t("settings.provider.vertex_ai.service_account.json_parse_error") : t("settings.provider.vertex_ai.service_account.json_input_help")
			}) }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
				className: "mt-1.5",
				children: t("settings.provider.vertex_ai.service_account.client_email")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "relative mt-1.5 w-full",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
					className: "w-full pr-10",
					type: showClientEmail ? "text" : "password",
					value: localClientEmail,
					placeholder: t("settings.provider.vertex_ai.service_account.client_email_placeholder"),
					onChange: (e) => {
						markDraftDirty();
						setLocalClientEmail(e.target.value);
					},
					onBlur: saveAuthConfig
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
					type: "button",
					onClick: () => setShowClientEmail(!showClientEmail),
					className: "-translate-y-1/2 absolute top-1/2 right-3 text-muted-foreground hover:text-foreground",
					"aria-label": t("settings.provider.vertex_ai.service_account.toggle_client_email_visibility"),
					children: showClientEmail ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Eye, { className: "size-4" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EyeOff, { className: "size-4" })
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("settings.provider.vertex_ai.service_account.client_email_help") }) }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
				className: "mt-1.5",
				children: t("settings.provider.vertex_ai.service_account.private_key")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "relative mt-1.5 w-full",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input$1, {
					className: "min-h-10 w-full resize-none overflow-y-auto px-3 py-1.5 pr-10 text-sm",
					style: {
						WebkitTextSecurity: showPrivateKey ? "none" : "disc",
						maxHeight: localPrivateKey ? "52px" : "32px"
					},
					value: localPrivateKey,
					placeholder: t("settings.provider.vertex_ai.service_account.private_key_placeholder"),
					onChange: (e) => {
						markDraftDirty();
						setLocalPrivateKey(e.target.value);
					},
					onBlur: saveAuthConfig,
					spellCheck: false,
					rows: localPrivateKey ? 2 : 1
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
					type: "button",
					onClick: () => setShowPrivateKey(!showPrivateKey),
					className: "-translate-y-1/2 absolute top-1/2 right-3 text-muted-foreground hover:text-foreground",
					"aria-label": t("settings.provider.vertex_ai.service_account.toggle_private_key_visibility"),
					children: showPrivateKey ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Eye, { className: "size-4" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EyeOff, { className: "size-4" })
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("settings.provider.vertex_ai.service_account.private_key_help") }) }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
					className: "mt-1.5",
					children: t("settings.provider.vertex_ai.project_id")
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "relative mt-1.5 w-full",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
						className: "w-full pr-10",
						type: showProjectId ? "text" : "password",
						value: localProjectId,
						placeholder: t("settings.provider.vertex_ai.project_id_placeholder"),
						onChange: (e) => {
							markDraftDirty();
							setLocalProjectId(e.target.value);
						},
						onBlur: saveAuthConfig
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						type: "button",
						onClick: () => setShowProjectId(!showProjectId),
						className: "-translate-y-1/2 absolute top-1/2 right-3 text-muted-foreground hover:text-foreground",
						"aria-label": t("settings.provider.vertex_ai.service_account.toggle_project_id_visibility"),
						children: showProjectId ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Eye, { className: "size-4" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EyeOff, { className: "size-4" })
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("settings.provider.vertex_ai.project_id_help") }) }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsSubtitle, {
					className: "mt-1.5",
					children: t("settings.provider.vertex_ai.location")
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, {
					open: dropdownOpen,
					onOpenChange: setDropdownOpen,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverAnchor, {
						asChild: true,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "relative mt-1.5 w-full",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
								className: "w-full pr-10",
								"aria-invalid": !localLocation.trim(),
								value: localLocation,
								placeholder: t("settings.provider.vertex_ai.location_placeholder"),
								onChange: (e) => {
									markDraftDirty();
									setLocalLocation(e.target.value);
								},
								onClick: () => setDropdownOpen(true),
								onFocus: () => setDropdownOpen(true),
								onBlur: () => {
									if (!isSelectingLocationRef.current) saveAuthConfig();
								}
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverTrigger, {
								asChild: true,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
									type: "button",
									onMouseDown: (e) => e.preventDefault(),
									className: "-translate-y-1/2 absolute top-1/2 right-3 text-muted-foreground hover:text-foreground",
									"aria-label": t("settings.provider.vertex_ai.select_location"),
									"aria-haspopup": "listbox",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown, { className: "size-4" })
								}) })
							})]
						}) })
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContent, {
						className: "max-h-60 w-[var(--radix-popover-trigger-width)] overflow-y-auto p-1",
						align: "start",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							"data-ui": "settings.vertex-ai-settings.listbox",
							role: "listbox",
							"aria-label": t("settings.provider.vertex_ai.location"),
							children: DEFAULT_VERTEX_AI_LOCATIONS.map((loc) => {
								const isSelected = localLocation === loc.value;
								return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
									"data-ui": "settings.vertex-ai-settings.option",
									type: "button",
									role: "option",
									"aria-selected": isSelected,
									onMouseDown: () => {
										isSelectingLocationRef.current = true;
									},
									onClick: () => handleLocationSelect(loc.value),
									className: "w-full cursor-pointer rounded px-2 py-1.5 text-left text-sm hover:bg-accent hover:text-accent-foreground aria-selected:bg-accent aria-selected:text-accent-foreground",
									children: loc.label
								}, loc.value);
							})
						})
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpTextRow, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpText, { children: t("settings.provider.vertex_ai.location_help") }) })
			] })
		]
	});
};
var VertexAiSettings_default = VertexAiSettings;
const PROVIDER_SPECIFIC_SETTINGS_REGISTRY = {
	beforeAuth: [
		{
			key: "radeon-cloud-benefits",
			when: ({ provider }) => matchesPreset(provider, "radeon-cloud"),
			render: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadeonCloudBenefits, {})
		},
		{
			key: "oauth",
			when: ({ provider }) => isProviderSupportAuth(provider),
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderOauth_default, { providerId })
		},
		{
			key: "cherryin-oauth",
			when: ({ meta }) => meta.isCherryIN,
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CherryInOauth_default, { providerId })
		},
		{
			key: "ovms-settings",
			when: ({ provider }) => matchesPreset(provider, "ovms"),
			render: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OvmsSettings_default, {})
		},
		{
			key: "dmxapi-settings",
			when: ({ meta }) => meta.isDmxapi,
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DmxapiSettings_default, { providerId })
		},
		{
			key: "claude-code-settings",
			when: ({ provider }) => isClaudeCodeProviderId(provider.id),
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ClaudeCodeSettings_default, { providerId })
		},
		{
			key: "codex-oauth",
			when: ({ provider }) => isCodexProviderId(provider.id),
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoginOauthPanel_default, {
				providerId,
				i18nNs: "codex",
				showAccountId: true
			})
		},
		{
			key: "grok-cli-oauth",
			when: ({ provider }) => isGrokCliProviderId(provider.id),
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoginOauthPanel_default, {
				providerId,
				i18nNs: "grok_cli"
			})
		}
	],
	afterAuth: [
		{
			key: "lmstudio-settings",
			when: ({ provider }) => matchesPreset(provider, "lmstudio"),
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LmStudioSettings_default, { providerId })
		},
		{
			key: "gpustack-settings",
			when: ({ provider }) => matchesPreset(provider, "gpustack"),
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GpuStackSettings_default, { providerId })
		},
		{
			key: "copilot-settings",
			when: ({ provider }) => matchesPreset(provider, "copilot"),
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GithubCopilotSettings_default, { providerId })
		},
		{
			key: "aws-bedrock-settings",
			when: ({ provider }) => isAwsBedrockProvider(provider),
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AwsBedrockSettings_default, { providerId })
		},
		{
			key: "vertexai-settings",
			when: ({ provider }) => isVertexProvider(provider),
			render: (providerId) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VertexAiSettings_default, { providerId })
		}
	]
};
function ProviderSpecificSettings({ providerId, placement }) {
	const { provider } = useProvider(providerId);
	const meta = useProviderMeta(providerId);
	if (!provider) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: PROVIDER_SPECIFIC_SETTINGS_REGISTRY[placement].filter((entry) => entry.when({
		provider,
		meta
	})).map((entry) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: entry.render(provider.id) }, entry.key)) });
}
function AuthConnectionSlotsLayout({ providerId, children }) {
	const { provider } = useProvider(providerId);
	const isAwsBedrock = providerId === "aws-bedrock" || (provider ? matchesPreset(provider, "aws-bedrock") : false);
	const isLoginBased = provider ? isLoginBasedProvider(provider) : false;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
		"data-ui": "settings.auth-connection-slots-layout",
		className: "shrink-0 space-y-4",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSpecificSettings, {
			providerId,
			placement: "beforeAuth"
		}), !isLoginBased && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "flex flex-col gap-3",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: authConnectionClasses.shell,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: cn(authConnectionClasses.body, isAwsBedrock && "gap-1"),
					children: [children, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSpecificSettings, {
						providerId,
						placement: "afterAuth"
					})]
				})
			})
		})]
	});
}
let HealthStatus = /* @__PURE__ */ function(HealthStatus$1) {
	HealthStatus$1["SUCCESS"] = "success";
	HealthStatus$1["FAILED"] = "failed";
	HealthStatus$1["NOT_CHECKED"] = "not_checked";
	return HealthStatus$1;
}({});
function healthCheckErrorToDisplayString(error) {
	if (error == null) return "";
	if (typeof error === "string") return error.trim();
	const msg = error.message?.trim();
	if (msg) return msg;
	const name = error.name?.trim();
	if (name) return name;
	return "";
}
function aggregateApiKeyResults(keyResults) {
	const successResults = keyResults.filter((result) => result.status === HealthStatus.SUCCESS);
	const failedResults = keyResults.filter((result) => result.status === HealthStatus.FAILED);
	if (failedResults.length > 0) {
		const errorStrings = failedResults.map((result) => healthCheckErrorToDisplayString(result.error)).filter((s) => s !== "");
		const errors = [...new Set(errorStrings)].join("; ");
		return {
			status: HealthStatus.FAILED,
			error: errors ? {
				name: "HealthCheckError",
				message: errors,
				stack: null
			} : void 0,
			latency: successResults.length > 0 ? Math.min(...successResults.map((result) => result.latency)) : void 0
		};
	}
	return {
		status: HealthStatus.SUCCESS,
		latency: successResults.length > 0 ? Math.min(...successResults.map((result) => result.latency)) : void 0
	};
}
function getModelHealthCheckGenerationOutput(model) {
	if (isGenerateImageModel(model)) return "image";
	if (isGenerateVideoModel(model)) return "video";
	if (isGenerateAudioModel(model)) return "audio";
	return null;
}
function getModelHealthCheckSkipReason(model) {
	const generationOutput = getModelHealthCheckGenerationOutput(model);
	if (generationOutput) return {
		kind: "generation_cost",
		output: generationOutput
	};
	if (isTextToSpeechModel$1(model) || isSpeechToTextModel(model)) return { kind: "unsupported_probe" };
	return null;
}
async function checkApi(uniqueModelId, options) {
	options?.signal?.throwIfAborted();
	return await ipcApi.request("ai.provider.model.check", {
		apiKeyOverride: options?.apiKey,
		uniqueModelId,
		timeout: options?.timeout ?? 15e3
	});
}
var logger$11 = loggerService.withContext("ProviderSettings:EnableProviderWhenModelsAvailable");
async function enableProviderWhenModelsAvailable(provider, enableProvider, modelCount, source) {
	if (!provider || provider.isEnabled || modelCount <= 0) return;
	try {
		await enableProvider();
	} catch (error) {
		logger$11.error("Failed to enable provider with pin-to-top when models are available", {
			providerId: provider.id,
			modelCount,
			source,
			error
		});
		throw error;
	}
}
function useProviderEndpoints(provider) {
	const topology = getProviderHostTopology(provider);
	const providerId = provider?.id;
	const primaryEndpoint = topology.primaryEndpoint;
	const providerApiHost = topology.primaryBaseUrl;
	const providerAnthropicHost = topology.anthropicBaseUrl;
	const providerApiVersion = provider?.settings?.apiVersion ?? "";
	const isCherryIN = provider?.id === "cherryin";
	const [apiHost, setApiHostValue] = (0, import_react.useState)(providerApiHost);
	const [anthropicApiHost, setAnthropicApiHost] = (0, import_react.useState)(providerAnthropicHost);
	const [apiVersion, setApiVersion] = (0, import_react.useState)(providerApiVersion);
	const previousServerEndpoint = (0, import_react.useRef)({
		providerId,
		apiHost: providerApiHost,
		anthropicApiHost: providerAnthropicHost,
		apiVersion: providerApiVersion
	});
	(0, import_react.useEffect)(() => {
		const previous = previousServerEndpoint.current;
		const providerChanged = previous.providerId !== providerId;
		setApiHostValue((current) => providerChanged || current === previous.apiHost ? providerApiHost : current);
		setAnthropicApiHost((current) => providerChanged || current === previous.anthropicApiHost ? providerAnthropicHost : current);
		setApiVersion((current) => providerChanged || current === previous.apiVersion ? providerApiVersion : current);
		previousServerEndpoint.current = {
			providerId,
			apiHost: providerApiHost,
			anthropicApiHost: providerAnthropicHost,
			apiVersion: providerApiVersion
		};
	}, [
		providerId,
		providerApiHost,
		providerAnthropicHost,
		providerApiVersion
	]);
	return {
		apiHost,
		setApiHost: setApiHostValue,
		anthropicApiHost,
		setAnthropicApiHost,
		apiVersion,
		setApiVersion,
		primaryEndpoint,
		providerApiHost,
		providerAnthropicHost,
		isVertexProvider: provider ? isVertexProvider(provider) : false,
		isAnthropicProvider: provider ? isAnthropicProvider(provider) : false,
		isCherryIN
	};
}
var logger$10 = loggerService.withContext("ProviderSettings:ConnectionCheck");
function useProviderConnectionCheck(providerId) {
	const { provider, enableProvider } = useProvider(providerId);
	const [connectionCheckOpen, setConnectionCheckOpen] = (0, import_react.useState)(false);
	const { models } = useModels({ providerId }, {
		fetchEnabled: connectionCheckOpen,
		swrOptions: PROVIDER_SETTINGS_MODEL_SWR_OPTIONS
	});
	const { setTimeoutTimer } = useTimer();
	const { t, i18n } = useTranslation();
	const { commitInputApiKeyNow, inputApiKey } = useAuthenticationApiKey();
	const { apiHost, anthropicApiHost } = useProviderEndpoints(provider);
	const [apiKeyConnectivity, setApiKeyConnectivity] = (0, import_react.useState)({
		kind: "idle",
		status: HealthStatus.NOT_CHECKED,
		checking: false
	});
	const checkableModels = models;
	const checkableApiKeys = (0, import_react.useMemo)(() => splitApiKeyString(formatApiKeys(inputApiKey)).filter(Boolean), [inputApiKey]);
	const requiresApiKey = !provider?.authOptional;
	const abortControllerRef = (0, import_react.useRef)(null);
	const runIdRef = (0, import_react.useRef)(0);
	const abortInFlightCheck = (0, import_react.useCallback)(() => {
		if (abortControllerRef.current) {
			abortControllerRef.current.abort();
			abortControllerRef.current = null;
		}
		runIdRef.current += 1;
	}, []);
	const resetApiKeyConnectivity = (0, import_react.useCallback)(() => {
		setApiKeyConnectivity({
			kind: "idle",
			status: HealthStatus.NOT_CHECKED,
			checking: false
		});
	}, []);
	const closeConnectionCheck = (0, import_react.useCallback)(() => {
		setConnectionCheckOpen(false);
	}, []);
	const openConnectionCheck = (0, import_react.useCallback)(() => {
		if (!provider) return;
		if (requiresApiKey && isEmpty(checkableApiKeys)) {
			toast.error(i18n.t("message.error.enter.api.label"));
			return;
		}
		setApiKeyConnectivity({
			kind: "idle",
			status: HealthStatus.NOT_CHECKED,
			checking: false
		});
		setConnectionCheckOpen(true);
	}, [
		checkableApiKeys,
		i18n,
		provider,
		requiresApiKey
	]);
	const startConnectionCheck = (0, import_react.useCallback)(async ({ model, apiKey }) => {
		if (!provider || !model) {
			toast.error(i18n.t("message.error.enter.model"));
			return;
		}
		if (requiresApiKey && !apiKey) {
			toast.error(i18n.t("message.error.enter.api.label"));
			return;
		}
		abortInFlightCheck();
		const controller = new AbortController();
		abortControllerRef.current = controller;
		const runId = ++runIdRef.current;
		let didCommitApiKey = false;
		try {
			setApiKeyConnectivity({
				kind: "checking",
				checking: true,
				status: HealthStatus.NOT_CHECKED,
				model
			});
			await commitInputApiKeyNow();
			didCommitApiKey = true;
			if (runId !== runIdRef.current || controller.signal.aborted) return;
			await checkApi(model.id, {
				apiKey: apiKey || void 0,
				signal: controller.signal
			});
			if (runId !== runIdRef.current) return;
			try {
				await enableProviderWhenModelsAvailable(provider, enableProvider, checkableModels.length, "connection_check");
			} catch (error) {
				if (runId !== runIdRef.current || controller.signal.aborted) return;
				logger$10.error("Provider connection succeeded but enablement failed", {
					providerId: provider.id,
					modelId: model.id,
					error
				});
				toast.warning(i18n.t("settings.provider.enable_failed_after_connection"));
			}
			if (runId !== runIdRef.current || controller.signal.aborted) return;
			toast.success({
				timeout: 2e3,
				title: i18n.t("message.api.connection.success")
			});
			setApiKeyConnectivity({
				kind: "ok",
				checking: false,
				status: HealthStatus.SUCCESS,
				model
			});
			setConnectionCheckOpen(false);
			setTimeoutTimer("provider-setting-check-api", () => setApiKeyConnectivity({
				kind: "idle",
				status: HealthStatus.NOT_CHECKED,
				checking: false
			}), 3e3);
		} catch (error) {
			if (runId !== runIdRef.current || controller.signal.aborted) return;
			if (didCommitApiKey) logger$10.error("Provider connection check failed", {
				providerId: provider.id,
				modelId: model.id,
				error
			});
			else logger$10.error("Failed to persist pending API key before connection check", {
				providerId: provider.id,
				modelId: model.id,
				error
			});
			if (!didCommitApiKey) toast.error({
				timeout: 8e3,
				title: i18n.t("settings.provider.api_key.save_failed")
			});
			setApiKeyConnectivity({
				kind: "failed",
				checking: false,
				status: HealthStatus.FAILED,
				model,
				error: serializeHealthCheckError(error)
			});
		}
	}, [
		abortInFlightCheck,
		checkableModels.length,
		commitInputApiKeyNow,
		i18n,
		provider,
		requiresApiKey,
		setTimeoutTimer,
		enableProvider
	]);
	const checkApi$1 = (0, import_react.useCallback)(async () => {
		if (isEmpty(checkableModels)) {
			toast.error({
				timeout: 5e3,
				title: t("settings.provider.no_models_for_check")
			});
			return;
		}
		const firstModel = checkableModels[0];
		if (!firstModel) {
			toast.error(i18n.t("message.error.enter.model"));
			return;
		}
		await startConnectionCheck({
			model: firstModel,
			apiKey: checkableApiKeys[0] ?? ""
		});
	}, [
		checkableApiKeys,
		checkableModels,
		i18n,
		startConnectionCheck,
		t
	]);
	(0, import_react.useEffect)(() => {
		abortInFlightCheck();
		setApiKeyConnectivity({
			kind: "idle",
			status: HealthStatus.NOT_CHECKED,
			checking: false
		});
		setConnectionCheckOpen(false);
	}, [
		abortInFlightCheck,
		anthropicApiHost,
		apiHost,
		inputApiKey,
		provider?.id
	]);
	(0, import_react.useEffect)(() => () => abortInFlightCheck(), [abortInFlightCheck]);
	return {
		apiKeyConnectivity,
		checkableApiKeys,
		checkableModels,
		checkApi: checkApi$1,
		connectionCheckOpen,
		openConnectionCheck,
		closeConnectionCheck,
		startConnectionCheck,
		requiresApiKey,
		resetApiKeyConnectivity
	};
}
var logger$9 = loggerService.withContext("ProviderSettings:EndpointActions");
function getEndpointActionErrorMessage(error, fallback) {
	if (isDataApiError(error) || isSerializedDataApiError(error)) {
		const dataError = toDataApiError(error);
		switch (dataError.code) {
			case ErrorCode.VALIDATION_ERROR:
			case ErrorCode.UNAUTHORIZED:
			case ErrorCode.PERMISSION_DENIED:
			case ErrorCode.NOT_FOUND:
			case ErrorCode.CONFLICT:
			case ErrorCode.SERVICE_UNAVAILABLE:
			case ErrorCode.TIMEOUT: return dataError.message;
			default: return fallback;
		}
	}
	if (error instanceof Error && error.message.trim()) return `${fallback}: ${error.message}`;
	return fallback;
}
function useProviderEndpointActions({ provider, primaryEndpoint, apiHost, setApiHost, providerApiHost, anthropicApiHost, setAnthropicApiHost, apiVersion, defaultApiHost, patchProvider }) {
	const { t } = useTranslation();
	const lastPersistedApiHostRef = (0, import_react.useRef)(trim(providerApiHost));
	(0, import_react.useEffect)(() => {
		lastPersistedApiHostRef.current = trim(providerApiHost);
	}, [providerApiHost]);
	const buildNextApiEndpointConfigs = (0, import_react.useCallback)((baseUrl) => {
		if (!provider) return;
		return {
			...provider.endpointConfigs,
			[primaryEndpoint]: {
				...provider.endpointConfigs?.[primaryEndpoint],
				baseUrl
			}
		};
	}, [primaryEndpoint, provider]);
	const persistApiHostDraft = (0, import_react.useCallback)(async (nextApiHost) => {
		if (!provider) return false;
		const trimmedApiHost = trim(nextApiHost);
		if (!validateApiHost(trimmedApiHost)) return false;
		if (!isVertexProvider(provider) && !trimmedApiHost) return false;
		const nextEndpointConfigs = buildNextApiEndpointConfigs(trimmedApiHost);
		if (!nextEndpointConfigs) return false;
		await patchProvider({ endpointConfigs: nextEndpointConfigs });
		lastPersistedApiHostRef.current = trimmedApiHost;
		return true;
	}, [
		buildNextApiEndpointConfigs,
		patchProvider,
		provider
	]);
	const debouncedPersistApiHost = (0, import_react.useMemo)(() => debounce((nextApiHost) => void persistApiHostDraft(nextApiHost), 150), [persistApiHostDraft]);
	(0, import_react.useEffect)(() => {
		if (!provider) return;
		const trimmedApiHost = trim(apiHost);
		if (!validateApiHost(trimmedApiHost)) {
			debouncedPersistApiHost.cancel();
			return;
		}
		if (!isVertexProvider(provider) && !trimmedApiHost) {
			debouncedPersistApiHost.cancel();
			return;
		}
		if (trimmedApiHost === lastPersistedApiHostRef.current) {
			debouncedPersistApiHost.cancel();
			return;
		}
		debouncedPersistApiHost(apiHost);
		return () => debouncedPersistApiHost.cancel();
	}, [
		apiHost,
		debouncedPersistApiHost,
		provider
	]);
	(0, import_react.useEffect)(() => () => debouncedPersistApiHost.cancel(), [debouncedPersistApiHost]);
	return {
		commitApiHost: (0, import_react.useCallback)(async (explicitNext) => {
			try {
				if (!provider) return false;
				debouncedPersistApiHost.cancel();
				const trimmedApiHost = trim(explicitNext !== void 0 ? explicitNext : apiHost);
				if (!validateApiHost(trimmedApiHost)) {
					setApiHost(providerApiHost);
					toast.error(t("settings.provider.api_host_no_valid"));
					return false;
				}
				if (!isVertexProvider(provider) && !trimmedApiHost) {
					setApiHost(providerApiHost);
					return false;
				}
				const nextEndpointConfigs = buildNextApiEndpointConfigs(trimmedApiHost);
				if (!nextEndpointConfigs) return false;
				if (trimmedApiHost !== trim(apiHost)) setApiHost(trimmedApiHost);
				if (trimmedApiHost !== lastPersistedApiHostRef.current) {
					await patchProvider({ endpointConfigs: nextEndpointConfigs });
					lastPersistedApiHostRef.current = trimmedApiHost;
				}
				return true;
			} catch (error) {
				logger$9.error("Failed to commit provider API host", {
					providerId: provider?.id,
					error
				});
				toast.error(getEndpointActionErrorMessage(error, t("settings.provider.save_failed")));
				return false;
			}
		}, [
			apiHost,
			buildNextApiEndpointConfigs,
			debouncedPersistApiHost,
			patchProvider,
			provider,
			providerApiHost,
			setApiHost,
			t
		]),
		commitAnthropicApiHost: (0, import_react.useCallback)(async (explicitNext) => {
			if (!provider) return false;
			const trimmedHost = trim(explicitNext !== void 0 ? explicitNext : anthropicApiHost);
			try {
				if (trimmedHost) {
					await patchProvider({ endpointConfigs: {
						...provider.endpointConfigs,
						[ENDPOINT_TYPE.ANTHROPIC_MESSAGES]: {
							...provider.endpointConfigs?.[ENDPOINT_TYPE.ANTHROPIC_MESSAGES],
							baseUrl: trimmedHost
						}
					} });
					setAnthropicApiHost(trimmedHost);
					return true;
				}
				const nextConfigs = { ...provider.endpointConfigs };
				delete nextConfigs[ENDPOINT_TYPE.ANTHROPIC_MESSAGES];
				await patchProvider({ endpointConfigs: nextConfigs });
				setAnthropicApiHost("");
				return true;
			} catch (error) {
				logger$9.error("Failed to commit Anthropic API host", {
					providerId: provider?.id,
					error
				});
				toast.error(getEndpointActionErrorMessage(error, t("settings.provider.save_failed")));
				return false;
			}
		}, [
			anthropicApiHost,
			patchProvider,
			provider,
			setAnthropicApiHost,
			t
		]),
		commitApiVersion: (0, import_react.useCallback)(async () => {
			if (!provider) return false;
			try {
				await patchProvider({ providerSettings: {
					...provider.settings,
					apiVersion
				} });
				return true;
			} catch (error) {
				logger$9.error("Failed to commit API version", {
					providerId: provider.id,
					error
				});
				toast.error(getEndpointActionErrorMessage(error, t("settings.provider.save_failed")));
				return false;
			}
		}, [
			apiVersion,
			patchProvider,
			provider,
			t
		]),
		resetApiHost: (0, import_react.useCallback)(async () => {
			if (!provider) return false;
			const nextBaseUrl = defaultApiHost;
			const nextEndpointConfigs = {
				...provider.endpointConfigs,
				[primaryEndpoint]: {
					...provider.endpointConfigs?.[primaryEndpoint],
					baseUrl: nextBaseUrl
				}
			};
			setApiHost(nextBaseUrl);
			try {
				await patchProvider({ endpointConfigs: nextEndpointConfigs });
				return true;
			} catch (error) {
				logger$9.error("Failed to reset provider API host", {
					providerId: provider.id,
					error
				});
				toast.error(getEndpointActionErrorMessage(error, t("settings.provider.save_failed")));
				return false;
			}
		}, [
			defaultApiHost,
			patchProvider,
			primaryEndpoint,
			provider,
			setApiHost,
			t
		])
	};
}
function buildHostEndpointPreviews(params) {
	const { provider, authConfig, primaryEndpoint, apiHost, anthropicApiHost, providerAnthropicHost } = params;
	const appendVersion = !isWithTrailingSharp(apiHost);
	let formattedHost;
	if (primaryEndpoint === ENDPOINT_TYPE.ANTHROPIC_MESSAGES) formattedHost = formatApiHost(anthropicApiHost || apiHost, appendVersion);
	else if (provider.id === "copilot" || provider.id === "github" || isCherryAIProvider(provider) || isPerplexityProvider(provider) || isNewApiProvider(provider) || isAzureOpenAIProvider(provider)) formattedHost = formatApiHost(apiHost, false);
	else if (primaryEndpoint === ENDPOINT_TYPE.OLLAMA_CHAT) formattedHost = formatOllamaApiHost(apiHost);
	else if (primaryEndpoint === ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT) formattedHost = formatApiHost(apiHost, appendVersion, "v1beta");
	else if (isVertexProvider(provider)) formattedHost = formatVertexApiHost({
		apiHost,
		project: authConfig?.type === "iam-gcp" ? authConfig.project : "",
		location: authConfig?.type === "iam-gcp" ? authConfig.location : ""
	});
	else formattedHost = formatApiHost(apiHost, appendVersion);
	return {
		hostPreview: (() => {
			if (primaryEndpoint === ENDPOINT_TYPE.OLLAMA_CHAT) return `${formattedHost}/chat`;
			if (provider.id === "gateway") return `${formattedHost}/language-model`;
			if (isAzureOpenAIProvider(provider)) {
				const version = provider.settings?.apiVersion || "";
				const path = !["preview", "v1"].includes(version) ? "/v1/chat/completions?apiVersion=v1" : "/v1/responses?apiVersion=v1";
				return `${formattedHost}${path}`;
			}
			if (primaryEndpoint === ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS) return `${formattedHost}/chat/completions`;
			if (primaryEndpoint === ENDPOINT_TYPE.OPENAI_RESPONSES) return `${formattedHost}/responses`;
			if (primaryEndpoint === ENDPOINT_TYPE.ANTHROPIC_MESSAGES) return `${formattedHost}/messages`;
			if (primaryEndpoint === ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT) return `${formattedHost}/models`;
			if (isVertexProvider(provider)) return `${formattedHost}/publishers/google`;
			return formattedHost;
		})(),
		anthropicHostPreview: `${formatApiHost(anthropicApiHost || providerAnthropicHost)}/messages`
	};
}
function useProviderHostPreview(params) {
	const { provider, apiHost, anthropicApiHost, defaultApiHost } = params;
	const { data: authConfig } = useProviderAuthConfig(provider?.id ?? "");
	return (0, import_react.useMemo)(() => {
		if (!provider) return {
			hostPreview: "",
			anthropicHostPreview: "",
			isApiHostResettable: false
		};
		const topology = getProviderHostTopology(provider);
		return {
			...buildHostEndpointPreviews({
				provider,
				authConfig,
				primaryEndpoint: topology.primaryEndpoint,
				apiHost,
				anthropicApiHost,
				providerAnthropicHost: topology.anthropicBaseUrl
			}),
			isApiHostResettable: Boolean(defaultApiHost && apiHost !== defaultApiHost)
		};
	}, [
		anthropicApiHost,
		apiHost,
		authConfig,
		defaultApiHost,
		provider
	]);
}
function ProviderField({ title, titleClassName, action, help, children, className, layout = "vertical" }) {
	const isHorizontal = layout === "horizontal";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-field",
		className: cn("space-y-2", className, isHorizontal && "grid grid-cols-[7rem_minmax(0,1fr)] items-start gap-x-3 gap-y-1.5 space-y-0"),
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: cn("flex items-center justify-between gap-3", isHorizontal && "min-h-8 justify-start"),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: cn("font-medium text-muted-foreground text-sm leading-5", titleClassName),
					children: title
				}), action]
			}),
			children,
			help && isHorizontal ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "col-start-2",
				children: help
			}) : help
		]
	});
}
function ProviderSection({ id, title, description, action, children, className }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
		"data-ui": "settings.provider-section",
		id,
		className: cn("space-y-2.5", className),
		children: [(title || description || action) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "flex items-start justify-between gap-3",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "min-w-0",
				children: [title && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: sectionHeadingClasses,
					children: title
				}), description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "mt-1 text-muted-foreground text-xs leading-tight",
					children: description
				})]
			}), action]
		}), children]
	});
}
var API_HOST_OPTIONS = [{
	value: "open.cherryin.net",
	labelKey: "settings.provider.cherryin.api_host.acceleration",
	description: "open.cherryin.net"
}, {
	value: "open.cherryin.ai",
	labelKey: "settings.provider.cherryin.api_host.international",
	description: "open.cherryin.ai"
}];
var CherryInSettings = ({ providerId }) => {
	const { provider, updateProvider } = useProvider(providerId);
	const { t } = useTranslation();
	const [open, setOpen] = (0, import_react.useState)(false);
	const currentHost = (0, import_react.useMemo)(() => {
		if (!provider?.endpointConfigs) return API_HOST_OPTIONS[0].value;
		const firstUrl = Object.values(provider.endpointConfigs)[0]?.baseUrl;
		if (!firstUrl) return API_HOST_OPTIONS[0].value;
		try {
			const hostname = new URL(firstUrl).hostname;
			return API_HOST_OPTIONS.find((option) => hostname.includes(option.value))?.value ?? API_HOST_OPTIONS[0].value;
		} catch {
			return API_HOST_OPTIONS[0].value;
		}
	}, [provider?.endpointConfigs]);
	const handleHostChange = (0, import_react.useCallback)(async (value) => {
		setOpen(false);
		const newEndpointConfigs = replaceEndpointConfigDomain(provider?.endpointConfigs, value);
		try {
			await updateProvider({ endpointConfigs: newEndpointConfigs });
		} catch {
			toast.error(t("settings.provider.save_failed"));
		}
	}, [
		provider?.endpointConfigs,
		t,
		updateProvider
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, {
		open,
		onOpenChange: setOpen,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(PopoverTrigger, {
			className: cn(fieldClasses.inputGroupBlock, "group cursor-pointer justify-between text-left outline-none"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: cn(fieldClasses.input, "block min-h-[1.25em] min-w-0 flex-1 truncate bg-transparent py-0 font-mono tabular-nums"),
				children: currentHost
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown, {
				size: 12,
				className: "ml-2 shrink-0 text-muted-foreground transition-transform group-data-[state=open]:rotate-180",
				"aria-hidden": true
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContent, {
			align: "start",
			sideOffset: 4,
			className: "w-(--radix-popover-trigger-width) rounded-lg border-[0.5px] border-border bg-popover p-1.5 text-popover-foreground shadow-lg",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuList, { children: API_HOST_OPTIONS.map((option) => {
				const isSelected = option.value === currentHost;
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem, {
					label: t(option.labelKey),
					description: option.description,
					active: isSelected,
					suffix: isSelected ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Check, {
						size: 14,
						className: "text-foreground",
						"aria-hidden": true
					}) : null,
					className: "rounded-lg px-2.5 text-sm",
					descriptionClassName: "font-mono text-foreground-tertiary text-xs tabular-nums",
					onClick: () => void handleHostChange(option.value)
				}, option.value);
			}) })
		})]
	});
};
var CherryInSettings_default = CherryInSettings;
var logger$8 = loggerService.withContext("copyApiKeyToClipboard");
async function copyApiKeyToClipboard(apiKey, t) {
	try {
		await navigator.clipboard.writeText(apiKey);
		toast.success(t("message.copied"));
	} catch (error) {
		logger$8.warn("Failed to copy API key to clipboard", error);
		toast.error(t("common.copy_failed"));
	}
}
function ApiHostEndpointButton({ onClick }) {
	const { t } = useTranslation();
	const label = t("settings.provider.more_endpoints.add");
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		"data-ui": "settings.api-host-endpoint-button",
		type: "button",
		"aria-label": label,
		className: fieldClasses.titleHelpLink,
		onClick,
		children: label
	});
}
function AzureApiVersionField({ className, apiVersion, onApiVersionChange, onApiVersionCommit }) {
	const { t } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
		className,
		title: t("settings.provider.api_version"),
		help: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "pt-1 text-[12px] text-muted-foreground leading-[1.35]",
			children: t("settings.provider.azure.apiversion.tip")
		}),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroup, {
			className: fieldClasses.inputGroupBlock,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupInput, {
				className: fieldClasses.input,
				value: apiVersion,
				placeholder: "2024-xx-xx-preview",
				onChange: (event) => onApiVersionChange(event.target.value),
				onBlur: onApiVersionCommit
			})
		})
	});
}
function ApiHostField({ providerIdForSettings, apiHost, isCherryIN, isChineseUser, isVertexAI, isApiHostResettable, onApiHostChange, onApiHostCommit, onResetApiHost, onOpenRequestConfig }) {
	const { t } = useTranslation();
	const trimmedApiHost = apiHost.trim();
	const help = isVertexAI ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.api-host-field",
		className: "space-y-1 pt-1",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "text-[12px] text-muted-foreground leading-[1.35]",
			children: t("settings.provider.vertex_ai.api_host_help")
		})
	}) : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
		title: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
			className: fieldClasses.titleWithHelp,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.provider.api_host") }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiHostEndpointButton, { onClick: onOpenRequestConfig })]
		}),
		titleClassName: "text-foreground",
		help,
		children: isCherryIN && isChineseUser ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: cn(fieldClasses.inputRow, "group"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex min-w-0 flex-1",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CherryInSettings_default, { providerId: providerIdForSettings })
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
				content: t("settings.provider.request_configuration_tooltip"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "inline-flex shrink-0",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						"data-ui": "settings.api-host-field.action.open",
						type: "button",
						className: fieldClasses.inputActionButton,
						"aria-label": t("settings.provider.request_configuration_tooltip"),
						onClick: onOpenRequestConfig,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Settings, {
							size: 14,
							"aria-hidden": true
						})
					})
				})
			})]
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: cn(fieldClasses.inputRow, "group"),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(InputGroup, {
					className: `${fieldClasses.inputGroup} min-w-0 flex-1`,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupInput, {
						className: cn(fieldClasses.input, "font-mono tabular-nums"),
						value: apiHost,
						placeholder: t("settings.provider.api_host_placeholder"),
						"aria-label": t("settings.provider.api_host"),
						title: trimmedApiHost,
						onChange: (event) => onApiHostChange(event.target.value),
						onBlur: onApiHostCommit,
						autoComplete: "off"
					}), trimmedApiHost ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupAddon, {
						align: "inline-end",
						className: "-mr-0.5 pr-0",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
							content: t("common.copy"),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
								type: "button",
								variant: "ghost",
								size: "icon-sm",
								className: "size-5 shrink-0 rounded-md p-0 text-muted-foreground opacity-0 shadow-none transition-opacity hover:bg-accent/50 hover:text-foreground focus-visible:opacity-100 group-hover:opacity-100",
								"aria-label": t("common.copy"),
								onClick: () => {
									copyApiKeyToClipboard(trimmedApiHost, t);
								},
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Copy, { className: "size-2.5" })
							})
						})
					}) : null]
				}),
				isApiHostResettable ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("settings.provider.api.url.reset"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "inline-flex shrink-0",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
							type: "button",
							className: fieldClasses.inputActionButton,
							"aria-label": t("settings.provider.api.url.reset"),
							onClick: () => {
								onResetApiHost();
							},
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RotateCcw, { size: 14 })
						})
					})
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("settings.provider.request_configuration_tooltip"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "inline-flex shrink-0",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
							"data-ui": "settings.api-host-field.action.open",
							type: "button",
							className: fieldClasses.inputActionButton,
							"aria-label": t("settings.provider.request_configuration_tooltip"),
							onClick: onOpenRequestConfig,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Settings, {
								size: 14,
								"aria-hidden": true
							})
						})
					})
				})
			]
		})
	});
}
function AnthropicApiHostField({ anthropicApiHost, anthropicHostPreview, onAnthropicApiHostChange, onAnthropicApiHostCommit, onOpenRequestConfig }) {
	const { t } = useTranslation();
	const trimmedAnthropicApiHost = anthropicApiHost.trim();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
		title: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
			className: fieldClasses.titleWithHelp,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.provider.anthropic_api_host") }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiHostEndpointButton, { onClick: onOpenRequestConfig })]
		}),
		help: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "break-all pt-1 text-[12px] text-muted-foreground leading-[1.35]",
			children: t("settings.provider.anthropic_api_host_preview", { url: anthropicHostPreview || "—" })
		}),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: cn(fieldClasses.inputRow, "group"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(InputGroup, {
				className: `${fieldClasses.inputGroup} min-w-0 flex-1`,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupInput, {
					className: cn(fieldClasses.input, "font-mono tabular-nums"),
					value: anthropicApiHost,
					placeholder: t("settings.provider.api_host_placeholder"),
					"aria-label": t("settings.provider.anthropic_api_host"),
					title: trimmedAnthropicApiHost,
					onChange: (event) => onAnthropicApiHostChange(event.target.value),
					onBlur: onAnthropicApiHostCommit,
					autoComplete: "off"
				}), trimmedAnthropicApiHost ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupAddon, {
					align: "inline-end",
					className: "-mr-0.5 pr-0",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						content: t("common.copy"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							type: "button",
							variant: "ghost",
							size: "icon-sm",
							className: "size-5 shrink-0 rounded-md p-0 text-muted-foreground opacity-0 shadow-none transition-opacity hover:bg-accent/50 hover:text-foreground focus-visible:opacity-100 group-hover:opacity-100",
							"aria-label": t("common.copy"),
							onClick: () => {
								copyApiKeyToClipboard(trimmedAnthropicApiHost, t);
							},
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Copy, { className: "size-2.5" })
						})
					})
				}) : null]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
				content: t("settings.provider.request_configuration_tooltip"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "inline-flex shrink-0",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						"data-ui": "settings.anthropic-api-host-field.action.open",
						type: "button",
						className: fieldClasses.inputActionButton,
						"aria-label": t("settings.provider.request_configuration_tooltip"),
						onClick: onOpenRequestConfig,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Settings, {
							size: 14,
							"aria-hidden": true
						})
					})
				})
			})]
		})
	});
}
function ApiHostSection({ children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSection, { children });
}
function ProviderImageEndpointFields({ value, invalidField, onChange }) {
	const { t } = useTranslation();
	const uid = (0, import_react.useId)();
	const generationInputId = `${uid}-image-generation-base-url`;
	const generationHelpId = `${uid}-image-generation-base-url-help`;
	const generationErrorId = `${uid}-image-generation-base-url-error`;
	const editInputId = `${uid}-image-edit-base-url`;
	const editHelpId = `${uid}-image-edit-base-url-help`;
	const editErrorId = `${uid}-image-edit-base-url-error`;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-image-endpoint-fields",
		className: "flex flex-col gap-4",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Field, {
			className: "gap-2",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldLabel, {
					htmlFor: generationInputId,
					className: "text-[13px] text-foreground",
					children: t("settings.provider.image_endpoints.image_generation_base_url.label")
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
					id: generationInputId,
					value: value.imageGenerationBaseUrl,
					placeholder: t("settings.provider.base_url.placeholder"),
					"aria-invalid": invalidField === "imageGenerationBaseUrl",
					"aria-describedby": invalidField === "imageGenerationBaseUrl" ? generationErrorId : generationHelpId,
					onChange: (event) => onChange({
						...value,
						imageGenerationBaseUrl: event.target.value
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
					id: generationHelpId,
					className: "text-muted-foreground text-xs leading-tight",
					children: t("settings.provider.image_endpoints.image_generation_base_url.help")
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldError, {
					id: generationErrorId,
					className: "text-xs",
					errors: invalidField === "imageGenerationBaseUrl" ? [{ message: t("settings.provider.base_url.invalid") }] : void 0
				})
			]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Field, {
			className: "gap-2",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldLabel, {
					htmlFor: editInputId,
					className: "text-[13px] text-foreground",
					children: t("settings.provider.image_endpoints.image_edit_base_url.label")
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
					id: editInputId,
					value: value.imageEditBaseUrl,
					placeholder: t("settings.provider.base_url.placeholder"),
					"aria-invalid": invalidField === "imageEditBaseUrl",
					"aria-describedby": invalidField === "imageEditBaseUrl" ? editErrorId : editHelpId,
					onChange: (event) => onChange({
						...value,
						imageEditBaseUrl: event.target.value
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
					id: editHelpId,
					className: "text-muted-foreground text-xs leading-tight",
					children: t("settings.provider.image_endpoints.image_edit_base_url.help")
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldError, {
					id: editErrorId,
					className: "text-xs",
					errors: invalidField === "imageEditBaseUrl" ? [{ message: t("settings.provider.base_url.invalid") }] : void 0
				})
			]
		})]
	});
}
function ProviderActions({ children, className }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.provider-actions",
		className: cn("flex items-center gap-2", className),
		children
	});
}
function setEndpointBaseUrl(endpointConfigs, type, baseUrl) {
	const value = trim(baseUrl);
	if (value) {
		endpointConfigs[type] = {
			...endpointConfigs[type],
			baseUrl: value
		};
		return;
	}
	const remainingConfig = { ...endpointConfigs[type] };
	delete remainingConfig.baseUrl;
	if (isEmpty(remainingConfig)) delete endpointConfigs[type];
	else endpointConfigs[type] = remainingConfig;
}
function readProviderImageEndpointDraft(endpointConfigs) {
	return {
		imageGenerationBaseUrl: trim(endpointConfigs?.[ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION]?.baseUrl ?? ""),
		imageEditBaseUrl: trim(endpointConfigs?.[ENDPOINT_TYPE.OPENAI_IMAGE_EDIT]?.baseUrl ?? "")
	};
}
function mergeProviderImageEndpointDraft(existing, draft) {
	const endpointConfigs = { ...existing };
	setEndpointBaseUrl(endpointConfigs, ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION, trim(draft.imageGenerationBaseUrl));
	setEndpointBaseUrl(endpointConfigs, ENDPOINT_TYPE.OPENAI_IMAGE_EDIT, trim(draft.imageEditBaseUrl));
	return endpointConfigs;
}
function findInvalidProviderImageEndpointDraft(draft) {
	const imageGenerationBaseUrl = trim(draft.imageGenerationBaseUrl);
	if (imageGenerationBaseUrl && !validateApiHost(imageGenerationBaseUrl)) return "imageGenerationBaseUrl";
	const imageEditBaseUrl = trim(draft.imageEditBaseUrl);
	if (imageEditBaseUrl && !validateApiHost(imageEditBaseUrl)) return "imageEditBaseUrl";
	return null;
}
var logger$7 = loggerService.withContext("ProviderCustomHeaderDrawer");
var ENDPOINT_TYPE_LABEL_KEYS = {
	[ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS]: "settings.provider.more_endpoints.openai_chat",
	[ENDPOINT_TYPE.ANTHROPIC_MESSAGES]: "settings.provider.more_endpoints.anthropic",
	[ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT]: "settings.provider.more_endpoints.gemini",
	[ENDPOINT_TYPE.OPENAI_RESPONSES]: "settings.provider.more_endpoints.openai_responses"
};
var IMAGE_ENDPOINT_TYPES = new Set([ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION, ENDPOINT_TYPE.OPENAI_IMAGE_EDIT]);
var DEFAULT_CHAT_ENDPOINT_TYPES = new Set([
	ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS,
	ENDPOINT_TYPE.OPENAI_RESPONSES,
	ENDPOINT_TYPE.ANTHROPIC_MESSAGES,
	ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT,
	ENDPOINT_TYPE.OLLAMA_CHAT,
	ENDPOINT_TYPE.OLLAMA_GENERATE,
	ENDPOINT_TYPE.OPENAI_TEXT_COMPLETIONS
]);
function newRow(partial) {
	return {
		id: v4_default(),
		key: partial?.key ?? "",
		value: partial?.value ?? ""
	};
}
function headersObjectToRows(obj) {
	const entries = Object.entries(obj);
	if (entries.length === 0) return [];
	return entries.map(([key, value]) => newRow({
		key,
		value
	}));
}
function rowsToHeadersObject(rows) {
	const out = {};
	for (const row of rows) {
		const k = row.key.trim();
		if (!k) continue;
		out[k] = row.value;
	}
	return out;
}
function parseHeadersJsonDraft(raw) {
	const t = trim(raw);
	if (t === "") return {
		ok: true,
		headers: {}
	};
	let parsed;
	try {
		parsed = JSON.parse(t);
	} catch {
		return { ok: false };
	}
	if (parsed === null || typeof parsed !== "object" || Array.isArray(parsed)) return { ok: false };
	const out = {};
	for (const [key, val] of Object.entries(parsed)) {
		const kk = trim(key);
		if (!kk) continue;
		if (val !== null && typeof val === "object") return { ok: false };
		out[kk] = val === null || val === void 0 ? "" : String(val);
	}
	return {
		ok: true,
		headers: out
	};
}
function resolveEndpointTypes(provider, primary) {
	const others = Object.keys(provider?.endpointConfigs ?? {}).filter((type) => type !== primary && !IMAGE_ENDPOINT_TYPES.has(type)).sort();
	return IMAGE_ENDPOINT_TYPES.has(primary) ? others : [primary, ...others];
}
function mergeEndpointConfigs(existing, drafts) {
	const out = { ...existing };
	for (const [type, draft] of Object.entries(drafts)) {
		const next = { ...out[type] };
		const value = trim(draft.baseUrl);
		if (value) next.baseUrl = value;
		else delete next.baseUrl;
		if (!isEmpty(next)) out[type] = next;
		else delete out[type];
	}
	return out;
}
function findInvalidSecondaryEndpointUrl(drafts, primary) {
	for (const [type, draft] of Object.entries(drafts)) {
		if (type === primary) continue;
		const value = trim(draft.baseUrl);
		if (value && !validateApiHost(value)) return type;
	}
	return null;
}
function ProviderCustomHeaderDrawer({ providerId, open, onClose }) {
	const { t } = useTranslation();
	const { provider, updateProvider } = useProvider(providerId);
	const { syncProviderModels } = useProviderModelSync(providerId);
	const primaryEndpoint = getProviderHostTopology(provider).primaryEndpoint;
	const endpointTypes = (0, import_react.useMemo)(() => resolveEndpointTypes(provider, primaryEndpoint), [provider, primaryEndpoint]);
	const sourceHeaders = (0, import_react.useMemo)(() => ({ ...provider?.settings?.extraHeaders }), [provider?.settings?.extraHeaders]);
	const [rows, setRows] = (0, import_react.useState)([]);
	const [endpointDrafts, setEndpointDrafts] = (0, import_react.useState)({});
	const [defaultChatEndpoint, setDefaultChatEndpoint] = (0, import_react.useState)(primaryEndpoint);
	const [imageEndpointDraft, setImageEndpointDraft] = (0, import_react.useState)(() => readProviderImageEndpointDraft(void 0));
	const [invalidImageEndpointField, setInvalidImageEndpointField] = (0, import_react.useState)(null);
	const [visibleEndpointTypes, setVisibleEndpointTypes] = (0, import_react.useState)([]);
	const [addEndpointOpen, setAddEndpointOpen] = (0, import_react.useState)(false);
	const [headersUiMode, setHeadersUiMode] = (0, import_react.useState)("list");
	const [jsonDraft, setJsonDraft] = (0, import_react.useState)("");
	const wasOpenRef = (0, import_react.useRef)(false);
	(0, import_react.useEffect)(() => {
		const justOpened = open && !wasOpenRef.current;
		wasOpenRef.current = open;
		if (!justOpened) return;
		const drafts = {};
		for (const type of endpointTypes) drafts[type] = { baseUrl: trim(provider?.endpointConfigs?.[type]?.baseUrl ?? "") };
		setEndpointDrafts(drafts);
		setDefaultChatEndpoint(primaryEndpoint);
		setImageEndpointDraft(readProviderImageEndpointDraft(provider?.endpointConfigs));
		setInvalidImageEndpointField(null);
		setVisibleEndpointTypes(endpointTypes);
		setAddEndpointOpen(false);
		setRows(headersObjectToRows(sourceHeaders));
		setJsonDraft(JSON.stringify(sourceHeaders, null, 2));
		setHeadersUiMode("list");
	}, [
		open,
		sourceHeaders,
		endpointTypes,
		primaryEndpoint,
		provider?.endpointConfigs
	]);
	const syncListToJson = (0, import_react.useCallback)(() => {
		setJsonDraft(JSON.stringify(rowsToHeadersObject(rows), null, 2));
	}, [rows]);
	const applyJsonToRowsOrToast = (0, import_react.useCallback)(() => {
		const parsed = parseHeadersJsonDraft(jsonDraft);
		if (!parsed.ok) {
			toast.error(t("settings.provider.copilot.invalid_json"));
			return false;
		}
		setRows(headersObjectToRows(parsed.headers));
		return true;
	}, [jsonDraft, t]);
	const toggleHeadersUiMode = (0, import_react.useCallback)(() => {
		if (headersUiMode === "list") {
			syncListToJson();
			setHeadersUiMode("json");
			return;
		}
		if (!applyJsonToRowsOrToast()) return;
		setHeadersUiMode("list");
	}, [
		applyJsonToRowsOrToast,
		headersUiMode,
		syncListToJson
	]);
	const handleSave = (0, import_react.useCallback)(async () => {
		if (!provider) return;
		const defaultEndpointDraft = trim(endpointDrafts[defaultChatEndpoint]?.baseUrl ?? "");
		if (!(provider.authType === "iam-gcp") && (!defaultEndpointDraft || !validateApiHost(defaultEndpointDraft))) {
			toast.error(t("settings.provider.api_host_no_valid"));
			return;
		}
		if (findInvalidSecondaryEndpointUrl(endpointDrafts, defaultChatEndpoint)) {
			toast.error(t("settings.provider.api_host_no_valid"));
			return;
		}
		const invalidImageEndpoint = findInvalidProviderImageEndpointDraft(imageEndpointDraft);
		if (invalidImageEndpoint) {
			setInvalidImageEndpointField(invalidImageEndpoint);
			toast.error(t("settings.provider.api_host_no_valid"));
			return;
		}
		const nextEndpointConfigs = mergeProviderImageEndpointDraft(mergeEndpointConfigs(provider.endpointConfigs, endpointDrafts), imageEndpointDraft);
		const previousDefaultBaseUrl = trim(provider.endpointConfigs?.[primaryEndpoint]?.baseUrl ?? "");
		const defaultEndpointChanged = defaultChatEndpoint !== primaryEndpoint;
		let parsedHeaders;
		if (headersUiMode === "json") {
			const parsed = parseHeadersJsonDraft(jsonDraft);
			if (!parsed.ok) {
				toast.error(t("settings.provider.copilot.invalid_json"));
				return;
			}
			parsedHeaders = parsed.headers;
		} else parsedHeaders = rowsToHeadersObject(rows);
		try {
			await updateProvider({
				endpointConfigs: nextEndpointConfigs,
				defaultChatEndpoint,
				providerSettings: {
					...provider.settings,
					extraHeaders: parsedHeaders
				}
			});
		} catch (error) {
			logger$7.error("Failed to save provider request config", error, { providerId });
			toast.error(t("settings.provider.save_failed"));
			return;
		}
		if (defaultEndpointChanged || defaultEndpointDraft !== previousDefaultBaseUrl) syncProviderModels({
			...provider,
			endpointConfigs: nextEndpointConfigs,
			defaultChatEndpoint
		}).catch((error) => {
			logger$7.error("Background model sync after endpoint change failed", error, { providerId });
		});
		toast.success(t("message.save.success.title"));
		onClose();
	}, [
		defaultChatEndpoint,
		endpointDrafts,
		headersUiMode,
		imageEndpointDraft,
		jsonDraft,
		onClose,
		primaryEndpoint,
		provider,
		providerId,
		rows,
		syncProviderModels,
		t,
		updateProvider
	]);
	const footer = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ProviderActions, {
		className: drawerClasses.footer,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			type: "button",
			variant: "outline",
			onClick: onClose,
			children: t("common.cancel")
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			type: "button",
			onClick: () => void handleSave(),
			children: t("common.save")
		})]
	});
	const toggleLabel = headersUiMode === "list" ? t("settings.provider.copilot.toggle_headers_editor_json") : t("settings.provider.copilot.toggle_headers_editor_list");
	const addableEndpointTypes = Object.keys(ENDPOINT_TYPE_LABEL_KEYS).filter((type) => !visibleEndpointTypes.includes(type));
	const handleAddEndpoint = (type) => {
		setVisibleEndpointTypes((prev) => prev.includes(type) ? prev : [...prev, type]);
		setEndpointDrafts((prev) => ({
			...prev,
			[type]: prev[type] ?? { baseUrl: "" }
		}));
		setAddEndpointOpen(false);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsDrawer, {
		open,
		onClose,
		title: t("settings.provider.request_configuration"),
		footer,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: customHeaderDrawerClasses.bodyScroll,
			children: [
				visibleEndpointTypes.map((type, index) => {
					const isInitialPrimary = index === 0;
					const isDefault = type === defaultChatEndpoint;
					const labelKey = ENDPOINT_TYPE_LABEL_KEYS[type];
					const label = labelKey ? t(labelKey) : isInitialPrimary ? t("settings.provider.api_host") : type;
					const inputId = `provider-request-config-endpoint-${type}`;
					const isConfiguredDefaultCandidate = type === primaryEndpoint || Object.prototype.hasOwnProperty.call(provider?.endpointConfigs ?? {}, type) || Boolean(trim(endpointDrafts[type]?.baseUrl ?? ""));
					return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "space-y-1.5",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: "flex min-h-5 items-center gap-2",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
									className: "text-[13px] text-foreground",
									htmlFor: inputId,
									children: label
								}), isDefault ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
									variant: "secondary",
									className: "h-5 border-0 px-1.5 py-0 font-normal text-foreground-tertiary text-xs",
									children: t("settings.provider.create_custom.endpoint_fields.default_chat")
								}) : DEFAULT_CHAT_ENDPOINT_TYPES.has(type) && isConfiguredDefaultCandidate ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
									type: "button",
									variant: "outline",
									size: "sm",
									className: "before:-top-5 relative h-5 min-h-0 rounded-full px-2 text-xs transition-transform before:absolute before:inset-x-0 before:bottom-0 before:content-[''] active:scale-[0.96]",
									onClick: () => setDefaultChatEndpoint(type),
									children: t("settings.provider.create_custom.endpoint_fields.set_default_chat")
								}) : null]
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroup, {
								className: fieldClasses.inputGroup,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupInput, {
									id: inputId,
									className: fieldClasses.input,
									value: endpointDrafts[type]?.baseUrl ?? "",
									placeholder: t("settings.provider.api_host"),
									onChange: (e) => setEndpointDrafts((prev) => ({
										...prev,
										[type]: {
											...prev[type] ?? { baseUrl: "" },
											baseUrl: e.target.value
										}
									})),
									autoComplete: "off"
								})
							}),
							isDefault && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
								className: "wrap-break-word text-muted-foreground text-xs leading-relaxed",
								children: t("settings.provider.api_host_drawer_hint")
							})
						]
					}, type);
				}),
				addableEndpointTypes.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, {
					open: addEndpointOpen,
					onOpenChange: setAddEndpointOpen,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverTrigger, {
						asChild: true,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
							type: "button",
							variant: "ghost",
							className: customHeaderDrawerClasses.addRowButton,
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, {
								className: "size-2.5 shrink-0",
								"aria-hidden": true
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.provider.more_endpoints.add") })]
						}) })
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContent, {
						align: "start",
						className: "w-56 p-1.5",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuList, { children: addableEndpointTypes.map((type) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem, {
							label: t(ENDPOINT_TYPE_LABEL_KEYS[type]),
							onClick: () => handleAddEndpoint(type)
						}, type)) })
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderImageEndpointFields, {
					value: imageEndpointDraft,
					invalidField: invalidImageEndpointField,
					onChange: (value) => {
						setImageEndpointDraft(value);
						setInvalidImageEndpointField(null);
					}
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "space-y-2.5",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex min-w-0 flex-wrap items-center gap-x-3 gap-y-1",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "text-muted-foreground text-xs",
							children: t("settings.provider.copilot.custom_headers")
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
							content: toggleLabel,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
								"data-ui": "settings.provider-custom-header-drawer.action.toggle",
								type: "button",
								"aria-label": toggleLabel,
								className: cn(fieldClasses.iconButton, "shrink-0"),
								onClick: toggleHeadersUiMode,
								children: headersUiMode === "list" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Braces, {
									className: "size-3",
									"aria-hidden": true
								}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(List, {
									className: "size-3",
									"aria-hidden": true
								})
							})
						})]
					}), headersUiMode === "list" ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [rows.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: customHeaderDrawerClasses.headerList,
						children: rows.map((row) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: customHeaderDrawerClasses.headerRow,
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroup, {
									className: fieldClasses.inputGroup,
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupInput, {
										id: `provider-hdr-key-${row.id}`,
										className: fieldClasses.input,
										value: row.key,
										onChange: (e) => {
											const v = e.target.value;
											setRows((prev) => prev.map((r) => r.id === row.id ? {
												...r,
												key: v
											} : r));
										},
										placeholder: t("settings.provider.copilot.header_name_placeholder"),
										"aria-label": t("settings.provider.copilot.header_field_name"),
										autoComplete: "off"
									})
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroup, {
									className: fieldClasses.inputGroup,
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupInput, {
										id: `provider-hdr-val-${row.id}`,
										className: fieldClasses.input,
										value: row.value,
										onChange: (e) => {
											const v = e.target.value;
											setRows((prev) => prev.map((r) => r.id === row.id ? {
												...r,
												value: v
											} : r));
										},
										placeholder: t("settings.provider.copilot.header_value_placeholder"),
										"aria-label": t("settings.provider.copilot.header_field_value"),
										autoComplete: "off"
									})
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
									type: "button",
									variant: "ghost",
									size: "icon",
									className: customHeaderDrawerClasses.removeIconButton,
									onClick: () => setRows((prev) => prev.filter((r) => r.id !== row.id)),
									"aria-label": t("common.delete"),
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trash2, { "aria-hidden": true })
								})
							]
						}, row.id))
					}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						type: "button",
						variant: "ghost",
						className: customHeaderDrawerClasses.addRowButton,
						onClick: () => setRows((prev) => [...prev, newRow()]),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, {
							className: "size-2.5 shrink-0",
							"aria-hidden": true
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.provider.copilot.add_request_header") })]
					})] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "space-y-1.5",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("textarea", {
							value: jsonDraft,
							onChange: (e) => {
								setJsonDraft(e.target.value);
							},
							spellCheck: false,
							autoComplete: "off",
							rows: 8,
							"aria-label": t("settings.provider.copilot.custom_headers"),
							placeholder: t("settings.provider.copilot.headers_json_placeholder"),
							className: customHeaderDrawerClasses.headersJsonEditor
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
							className: "text-muted-foreground text-xs leading-relaxed",
							children: t("settings.provider.copilot.headers_description")
						})]
					})]
				})
			]
		})
	});
}
var ENDPOINT_CONFIG_PRESET_FIELDS = ["endpointConfigs"];
function ApiHost({ providerId, onRequestModelPullGuide }) {
	const { provider } = useProvider(providerId);
	const { updateProvider } = useProviderMutations(providerId);
	const [customHeaderOpen, setCustomHeaderOpen] = (0, import_react.useState)(false);
	const [apiHostEdited, setApiHostEdited] = (0, import_react.useState)(false);
	const [anthropicApiHostEdited, setAnthropicApiHostEdited] = (0, import_react.useState)(false);
	const meta = useProviderMeta(providerId);
	const { primaryEndpoint, apiHost, setApiHost, anthropicApiHost, setAnthropicApiHost, apiVersion, setApiVersion } = useProviderEndpoints(provider);
	const topology = getProviderHostTopology(provider);
	const { data: preset } = useProviderPreset(providerId, ENDPOINT_CONFIG_PRESET_FIELDS);
	const defaultApiHost = preset?.endpointConfigs?.[topology.primaryEndpoint]?.baseUrl ?? "";
	const isAnthropicPrimaryEndpoint = primaryEndpoint === ENDPOINT_TYPE.ANTHROPIC_MESSAGES;
	const hostPreview = useProviderHostPreview({
		provider,
		apiHost,
		anthropicApiHost,
		defaultApiHost
	});
	const endpointActions = useProviderEndpointActions({
		provider,
		primaryEndpoint: topology.primaryEndpoint,
		apiHost,
		setApiHost,
		providerApiHost: topology.primaryBaseUrl,
		anthropicApiHost,
		setAnthropicApiHost,
		apiVersion,
		defaultApiHost,
		patchProvider: updateProvider
	});
	const handleApiHostChange = (value) => {
		setApiHostEdited(true);
		setApiHost(value);
	};
	const handleApiHostCommit = async () => {
		if (await endpointActions.commitApiHost() && apiHostEdited) {
			setApiHostEdited(false);
			onRequestModelPullGuide?.();
		}
	};
	const handleAnthropicApiHostChange = (value) => {
		setAnthropicApiHostEdited(true);
		setAnthropicApiHost(value);
	};
	const handleAnthropicApiHostCommit = async () => {
		if (await endpointActions.commitAnthropicApiHost() && anthropicApiHostEdited) {
			setAnthropicApiHostEdited(false);
			onRequestModelPullGuide?.();
		}
	};
	if (!provider) return null;
	if (!meta.isConnectionFieldVisible) return meta.isAzureOpenAI ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiHostSection, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AzureApiVersionField, {
		apiVersion,
		onApiVersionChange: setApiVersion,
		onApiVersionCommit: endpointActions.commitApiVersion
	}) }) : null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ApiHostSection, { children: [!isAnthropicPrimaryEndpoint ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiHostField, {
		providerIdForSettings: provider.id,
		apiHost,
		isCherryIN: meta.isCherryIN,
		isChineseUser: meta.isChineseUser,
		isVertexAI: provider.id === "vertexai",
		isApiHostResettable: hostPreview.isApiHostResettable,
		onApiHostChange: handleApiHostChange,
		onApiHostCommit: () => void handleApiHostCommit(),
		onResetApiHost: endpointActions.resetApiHost,
		onOpenRequestConfig: () => setCustomHeaderOpen(true)
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AnthropicApiHostField, {
		anthropicApiHost,
		anthropicHostPreview: hostPreview.anthropicHostPreview,
		onAnthropicApiHostChange: handleAnthropicApiHostChange,
		onAnthropicApiHostCommit: () => void handleAnthropicApiHostCommit(),
		onOpenRequestConfig: () => setCustomHeaderOpen(true)
	}), meta.isAzureOpenAI && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AzureApiVersionField, {
		className: "mt-4",
		apiVersion,
		onApiVersionChange: setApiVersion,
		onApiVersionCommit: endpointActions.commitApiVersion
	})] }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderCustomHeaderDrawer, {
		providerId,
		open: customHeaderOpen,
		onClose: () => setCustomHeaderOpen(false)
	})] });
}
var createEmptyDraft = () => ({
	id: v4_default(),
	key: "",
	label: "",
	isEnabled: true,
	isNew: true
});
var logger$6 = loggerService.withContext("ProviderApiKeyListDrawer");
function normalizeApiKeyValue(value) {
	return value.trim();
}
function toDraft(entry) {
	return {
		id: entry.id,
		key: entry.key,
		label: entry.label ?? "",
		isEnabled: entry.isEnabled,
		isNew: false
	};
}
function toEntry(draft) {
	return {
		id: draft.id,
		key: normalizeApiKeyValue(draft.key),
		label: draft.label.trim() || void 0,
		isEnabled: draft.isEnabled
	};
}
function ProviderApiKeyListDrawer({ providerId, open, onClose }) {
	const { t } = useTranslation();
	const { data: apiKeysData } = useProviderApiKeys(providerId);
	const { updateApiKeys } = useProviderMutations(providerId);
	const apiKeys = (0, import_react.useMemo)(() => apiKeysData?.keys ?? [], [apiKeysData?.keys]);
	const [editingId, setEditingId] = (0, import_react.useState)(null);
	const [draft, setDraft] = (0, import_react.useState)(null);
	const [saving, setSaving] = (0, import_react.useState)(false);
	const savingRef = (0, import_react.useRef)(false);
	(0, import_react.useEffect)(() => {
		if (!open) {
			setEditingId(null);
			setDraft(null);
		}
	}, [open]);
	const enabledCount = apiKeys.filter((item) => item.isEnabled).length;
	const persist = (0, import_react.useCallback)(async (nextKeys) => {
		if (savingRef.current) return false;
		savingRef.current = true;
		setSaving(true);
		try {
			await updateApiKeys(nextKeys);
			return true;
		} catch (error) {
			logger$6.error("Failed to persist provider API keys", {
				providerId,
				error
			});
			toast.error(t("settings.provider.api_key.save_failed"));
			return false;
		} finally {
			savingRef.current = false;
			setSaving(false);
		}
	}, [
		providerId,
		t,
		updateApiKeys
	]);
	const validateDraft = (0, import_react.useCallback)((nextDraft) => {
		const key = normalizeApiKeyValue(nextDraft.key);
		if (!key) {
			toast.warning(t("settings.provider.api.key.error.empty"));
			return null;
		}
		if (apiKeys.some((item) => item.id !== nextDraft.id && item.key.trim() === key)) {
			toast.warning(t("settings.provider.api.key.error.duplicate"));
			return null;
		}
		return toEntry(nextDraft);
	}, [apiKeys, t]);
	const startAdd = (0, import_react.useCallback)(() => {
		const nextDraft = createEmptyDraft();
		setEditingId(nextDraft.id);
		setDraft(nextDraft);
	}, []);
	const startEdit = (0, import_react.useCallback)((entry) => {
		const nextDraft = toDraft(entry);
		setEditingId(nextDraft.id);
		setDraft(nextDraft);
	}, []);
	const cancelEdit = (0, import_react.useCallback)(() => {
		setEditingId(null);
		setDraft(null);
	}, []);
	const saveDraft = (0, import_react.useCallback)(async () => {
		if (!draft) return;
		const entry = validateDraft(draft);
		if (!entry) return;
		if (await persist(draft.isNew ? [...apiKeys, entry] : apiKeys.map((item) => item.id === entry.id ? entry : item))) cancelEdit();
	}, [
		apiKeys,
		cancelEdit,
		draft,
		persist,
		validateDraft
	]);
	const removeKey = (0, import_react.useCallback)(async (id) => {
		if (await persist(apiKeys.filter((item) => item.id !== id)) && editingId === id) cancelEdit();
	}, [
		apiKeys,
		cancelEdit,
		editingId,
		persist
	]);
	const toggleEnabled = (0, import_react.useCallback)(async (entry, isEnabled) => {
		await persist(apiKeys.map((item) => item.id === entry.id ? {
			...item,
			isEnabled
		} : item));
	}, [apiKeys, persist]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsDrawer, {
		open,
		onClose,
		title: t("settings.provider.api.key.list.title"),
		description: t("settings.provider.api_key.list_description"),
		footer: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: apiKeyListClasses.summaryMeta,
			children: [
				enabledCount,
				" / ",
				apiKeys.length,
				" ",
				t("settings.provider.api_key.enabled_suffix")
			]
		}),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "space-y-4",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: apiKeyListClasses.listWrap,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Scrollbar_default, {
					className: apiKeyListClasses.listScroller,
					children: [
						apiKeys.length === 0 && !draft ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "px-4 py-6 text-center text-muted-foreground text-sm",
							children: t("error.no_api_key")
						}) : null,
						apiKeys.map((entry) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: apiKeyListClasses.keyRow,
							children: editingId === entry.id && draft ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiKeyDraftRow, {
								draft,
								saving,
								onChange: setDraft,
								onSave: saveDraft,
								onCancel: cancelEdit
							}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiKeyDisplayRow, {
								entry,
								saving,
								onEdit: () => startEdit(entry),
								onRemove: () => void removeKey(entry.id),
								onToggleEnabled: (next) => void toggleEnabled(entry, next)
							})
						}, entry.id)),
						draft?.isNew ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: apiKeyListClasses.keyRow,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiKeyDraftRow, {
								draft,
								saving,
								onChange: setDraft,
								onSave: saveDraft,
								onCancel: cancelEdit
							})
						}) : null
					]
				})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: apiKeyListClasses.actionRow,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: apiKeyListClasses.helperText,
					children: t("settings.provider.api_key.tip")
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
					variant: "secondary",
					size: "sm",
					disabled: !!draft || saving,
					onClick: startAdd,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, { size: 14 }), t("common.add")]
				})]
			})]
		})
	});
}
function ApiKeyDraftRow({ draft, saving, onChange, onSave, onCancel }) {
	const { t } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.api-key-draft-row",
		className: apiKeyListClasses.keyDraftRow,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: apiKeyListClasses.keyDraftInputs,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				value: draft.label,
				placeholder: t("settings.provider.api_key.label_placeholder"),
				className: apiKeyListClasses.keyDraftInput,
				disabled: saving,
				onChange: (event) => onChange({
					...draft,
					label: event.target.value
				})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				value: draft.key,
				placeholder: t("settings.provider.api.key.new_key.placeholder"),
				className: apiKeyListClasses.keyDraftInput,
				disabled: saving,
				spellCheck: false,
				autoFocus: true,
				onChange: (event) => onChange({
					...draft,
					key: event.target.value
				}),
				onKeyDown: (event) => {
					if (event.key === "Enter") {
						event.preventDefault();
						onSave();
					}
					if (event.key === "Escape") {
						event.preventDefault();
						onCancel();
					}
				}
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: apiKeyListClasses.keyRowActions,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
				content: t("common.save"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
					"data-ui": "settings.api-key-draft-row.action.save",
					type: "button",
					className: apiKeyListClasses.keySaveIconButton,
					"aria-label": t("common.save"),
					disabled: saving,
					onClick: onSave,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Check, {})
				})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
				content: t("common.cancel"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
					"data-ui": "settings.api-key-draft-row.action.cancel",
					type: "button",
					className: apiKeyListClasses.keyDestructiveIconButton,
					"aria-label": t("common.cancel"),
					disabled: saving,
					onClick: onCancel,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, {})
				})
			})]
		})]
	});
}
function ApiKeyDisplayRow({ entry, saving, onEdit, onRemove, onToggleEnabled }) {
	const { t } = useTranslation();
	const handleCopy = (0, import_react.useCallback)(() => {
		copyApiKeyToClipboard(entry.key, t);
	}, [entry.key, t]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.api-key-display-row",
		className: apiKeyListClasses.keyDisplayRow,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: apiKeyListClasses.keyTextBlock,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: apiKeyListClasses.keyLabel,
				children: entry.label || t("settings.provider.api_key.unnamed")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				"data-ui": "settings.api-key-display-row.action.copy",
				type: "button",
				title: t("settings.provider.api_key.copy"),
				className: `${apiKeyListClasses.keyValue} block cursor-pointer text-left transition-colors hover:text-foreground`,
				onClick: handleCopy,
				children: maskApiKey(entry.key)
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: apiKeyListClasses.keyRowActions,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("settings.provider.api_key.copy"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						"data-ui": "settings.api-key-display-row.action.copy",
						type: "button",
						className: apiKeyListClasses.keyIconButton,
						"aria-label": t("settings.provider.api_key.copy"),
						disabled: saving,
						onClick: handleCopy,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Copy, {})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("common.edit"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						"data-ui": "settings.api-key-display-row.action.edit",
						type: "button",
						className: apiKeyListClasses.keyIconButton,
						"aria-label": t("common.edit"),
						disabled: saving,
						onClick: onEdit,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PenLine, {})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("common.delete"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						"data-ui": "settings.api-key-display-row.action.remove",
						type: "button",
						className: apiKeyListClasses.keyDestructiveIconButton,
						"aria-label": t("common.delete"),
						disabled: saving,
						onClick: onRemove,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Minus, {})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Switch, {
					size: "xs",
					checked: entry.isEnabled,
					disabled: saving,
					onCheckedChange: onToggleEnabled
				})
			]
		})]
	});
}
function ApiKey({ providerId, apiKeyConnectivity, onOpenConnectionCheck, requiresApiKey = true, onRequestModelPullGuide }) {
	const { t } = useTranslation();
	const { provider } = useProvider(providerId);
	const meta = useProviderMeta(providerId);
	const { inputApiKey, setInputApiKey, hasPendingSync, commitInputApiKeyNow } = useAuthenticationApiKey();
	const [showApiKey, setShowApiKey] = (0, import_react.useState)(false);
	const [keyListOpen, setKeyListOpen] = (0, import_react.useState)(false);
	const [apiKeyEdited, setApiKeyEdited] = (0, import_react.useState)(false);
	const isCentralized = Boolean(provider?.settings?.isCentralized);
	(0, import_react.useEffect)(() => {
		setShowApiKey(false);
	}, [provider?.id]);
	const handleApiKeyBlur = (0, import_react.useCallback)(async () => {
		if (!apiKeyEdited && !hasPendingSync) return;
		try {
			await commitInputApiKeyNow();
			setApiKeyEdited(false);
			onRequestModelPullGuide?.();
		} catch {}
	}, [
		apiKeyEdited,
		commitInputApiKeyNow,
		hasPendingSync,
		onRequestModelPullGuide
	]);
	if (!provider || !meta.isApiKeyFieldVisible) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSection, {
		id: provider.id === "cherryin" ? "cherryin-api-key-section" : void 0,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ProviderField, {
			className: "space-y-2",
			title: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: fieldClasses.titleWithHelp,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.provider.api_key.label") }), meta.apiKeyWebsite && !meta.isDmxapi ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHelpLink, {
					target: "_blank",
					rel: "noreferrer",
					href: meta.apiKeyWebsite,
					className: fieldClasses.titleHelpLink,
					children: t("settings.provider.get_api_key")
				}) : null]
			}),
			titleClassName: "text-foreground",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: fieldClasses.inputRow,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(InputGroup, {
						className: fieldClasses.inputGroup,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupInput, {
							type: showApiKey && !isCentralized ? "text" : "password",
							className: fieldClasses.input,
							value: inputApiKey,
							placeholder: t("settings.provider.api_key.placeholder"),
							onChange: (event) => {
								setApiKeyEdited(true);
								setInputApiKey(event.target.value);
							},
							onBlur: () => void handleApiKeyBlur(),
							disabled: provider.id === "copilot" || isCentralized
						}), provider.id !== "copilot" && !isCentralized && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InputGroupAddon, {
							align: "inline-end",
							className: "-mr-0.5 pr-0",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
								content: showApiKey ? t("settings.provider.api_key.hide_key") : t("settings.provider.api_key.show_key"),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
									type: "button",
									className: fieldClasses.apiKeyVisibilityToggle,
									onClick: () => setShowApiKey((v) => !v),
									children: showApiKey ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EyeOff, { size: 12 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Eye, { size: 12 })
								})
							})
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						content: t("settings.provider.api.key.list.title"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "inline-flex shrink-0",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
								type: "button",
								disabled: provider.id === "copilot" || isCentralized,
								className: fieldClasses.inputActionButton,
								"aria-label": t("settings.provider.api.key.list.title"),
								onClick: () => setKeyListOpen(true),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(KeyRound, { size: 14 })
							})
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						content: t("settings.provider.check"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "inline-flex shrink-0",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
								"data-ui": "settings.api-key.action.open",
								type: "button",
								disabled: provider.id === "copilot" || requiresApiKey && !inputApiKey || apiKeyConnectivity.checking,
								className: fieldClasses.inputActionButton,
								"aria-label": t("settings.provider.check"),
								onClick: onOpenConnectionCheck,
								children: apiKeyConnectivity.checking ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoaderCircle, {
									size: 14,
									className: "animate-spin"
								}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Activity, { size: 14 })
							})
						})
					})
				]
			}), isCentralized ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
				className: "text-[11px] text-foreground-tertiary leading-4",
				children: t("settings.provider.api_key.centralized_hint")
			}) : null]
		})
	}), !isCentralized && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderApiKeyListDrawer, {
		providerId,
		open: keyListOpen,
		onClose: () => setKeyListOpen(false)
	})] });
}
var CONNECTION_ERROR_DESCRIPTION_COLOR = "var(--muted-foreground)";
var CONNECTION_ERROR_DETAIL_COLOR = "var(--foreground-tertiary)";
function ModelOptionIcon({ model, size = 20 }) {
	const Icon = useIcon(getModelLogoRef(model));
	return Icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon.Avatar, { size }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Avatar, {
		size: "sm",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AvatarFallback, { children: model.name.trim().charAt(0) || "M" })
	});
}
function renderModelOptionContent(model) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.render-model-option-content",
		className: "flex min-w-0 flex-1 items-center gap-2",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelOptionIcon, { model }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: "min-w-0 flex-1 truncate",
			title: model.name,
			children: model.name
		})]
	});
}
function ProviderConnectionCheckDrawer({ open, models, apiKeys, connectionError, isSubmitting, requiresApiKey = true, onClose, onStart, onOpenModelHealthCheck }) {
	const { t } = useTranslation();
	const sortedModels = (0, import_react.useMemo)(() => sortBy(models, "name"), [models]);
	const modelOptions = (0, import_react.useMemo)(() => sortedModels.map((model) => ({
		value: model.id,
		label: model.name,
		model
	})), [sortedModels]);
	const apiKeyOptions = (0, import_react.useMemo)(() => apiKeys.map((key, index) => ({
		value: String(index),
		label: maskApiKey(key)
	})), [apiKeys]);
	const [selectedModelId, setSelectedModelId] = (0, import_react.useState)("");
	const [selectedKeyIndex, setSelectedKeyIndex] = (0, import_react.useState)(0);
	(0, import_react.useEffect)(() => {
		if (!open) return;
		setSelectedModelId(sortedModels[0]?.id ?? "");
		setSelectedKeyIndex(0);
	}, [open, sortedModels]);
	const selectedModel = (0, import_react.useMemo)(() => sortedModels.find((item) => item.id === selectedModelId) ?? sortedModels[0], [selectedModelId, sortedModels]);
	const selectedApiKey = apiKeys[selectedKeyIndex] ?? apiKeys[0] ?? "";
	const hasMultipleKeys = apiKeys.length > 1;
	const connectionErrorText = healthCheckErrorToDisplayString(connectionError);
	const handleShowConnectionErrorDetail = () => {
		showErrorDetailPopup({ error: connectionError });
	};
	const handleOpenModelHealthCheck = () => {
		onClose();
		onOpenModelHealthCheck?.();
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog, {
		open,
		onOpenChange: (nextOpen) => !nextOpen && onClose(),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogContent, {
			className: "gap-4 sm:max-w-[520px]",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogHeader, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogTitle, {
					className: "text-base leading-5",
					children: t("message.api.check.model.title")
				}) }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "space-y-4",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "space-y-3",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
							className: "mb-2.5 block text-[13px] text-foreground",
							children: t("button.select_model")
						}), sortedModels.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Combobox, {
							className: "h-9 w-full justify-between px-2.5 text-left font-normal",
							emptyText: t("common.no_results"),
							options: modelOptions,
							value: selectedModel?.id ?? "",
							onChange: (value) => setSelectedModelId(Array.isArray(value) ? value[0] ?? "" : value),
							placeholder: t("settings.models.empty"),
							popoverClassName: "w-(--radix-popover-trigger-width) [&_[data-slot=command-list]]:max-h-[280px]",
							renderOption: (option) => renderModelOptionContent(option.model),
							renderValue: (value, options) => {
								const selectedValue = Array.isArray(value) ? value[0] : value;
								const option = options.find((item) => item.value === selectedValue);
								return option ? renderModelOptionContent(option.model) : null;
							},
							searchPlaceholder: t("common.search")
						}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: drawerClasses.emptyInline,
							children: t("settings.provider.no_models_for_check")
						})] }), hasMultipleKeys ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
							className: "mb-2.5 block text-[13px] text-foreground",
							children: t("settings.models.check.select_api_key")
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Combobox, {
							className: "h-9 w-full justify-between px-2.5 text-left font-mono text-[12px]",
							emptyText: t("common.no_results"),
							options: apiKeyOptions,
							value: String(selectedKeyIndex),
							onChange: (value) => setSelectedKeyIndex(Number(Array.isArray(value) ? value[0] ?? 0 : value)),
							placeholder: t("settings.models.check.select_api_key"),
							popoverClassName: "w-(--radix-popover-trigger-width)",
							renderOption: (option) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "truncate font-mono text-[12px]",
								children: option.label
							}),
							renderValue: (value, options) => {
								const selectedValue = Array.isArray(value) ? value[0] : value;
								const option = options.find((item) => item.value === selectedValue);
								return option ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: "truncate font-mono text-[12px]",
									children: option.label
								}) : null;
							},
							searchPlaceholder: t("common.search")
						})] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "space-y-1.5",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "text-[13px] text-muted-foreground",
								children: t("settings.provider.api_key.label")
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "rounded-md border border-border-subtle bg-muted/20 px-3 py-2 font-mono text-[12px] text-foreground",
								children: selectedApiKey ? maskApiKey(selectedApiKey) : "—"
							})]
						})]
					})
				}),
				connectionErrorText ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
					type: "button",
					"aria-label": `${t("message.api.connection.failed")}: ${connectionErrorText}. ${t("common.detail")}`,
					className: "group w-full cursor-pointer rounded-lg border border-border border-l-[3px] border-l-error-border bg-transparent px-3.5 py-3 text-left text-[13px] transition-all duration-200 focus-visible:border-ring focus-visible:bg-accent/30 focus-visible:outline-none",
					onClick: handleShowConnectionErrorDetail,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "mb-1.5 flex items-center gap-2",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "flex shrink-0 items-center justify-center text-error",
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TriangleAlert, {
									size: 15,
									className: "lucide-custom"
								})
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: "pr-5 text-[13px] leading-[1.4]",
								children: t("message.api.connection.failed")
							})]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "wrap-break-word ml-5.75 line-clamp-3 text-xs leading-normal",
							style: { color: CONNECTION_ERROR_DESCRIPTION_COLOR },
							children: connectionErrorText
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "mt-2.5 ml-5.75 flex items-center",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: "ml-auto inline-flex items-center gap-0.5 text-xs transition-colors duration-150 group-hover:text-foreground",
								style: { color: CONNECTION_ERROR_DETAIL_COLOR },
								children: [t("common.detail"), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRight, { size: 14 })]
							})
						})
					]
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogFooter, {
					className: "mt-1 flex-row items-center justify-between gap-3 sm:justify-between",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: onOpenModelHealthCheck ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						variant: "outline",
						className: "h-9 px-3 text-sm",
						onClick: handleOpenModelHealthCheck,
						children: t("settings.models.check.model_button_caption")
					}) : null }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: drawerClasses.footer,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							variant: "outline",
							onClick: onClose,
							children: t("common.cancel")
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							disabled: !selectedModel || requiresApiKey && !selectedApiKey,
							loading: isSubmitting,
							onClick: () => selectedModel && void onStart({
								model: selectedModel,
								apiKey: selectedApiKey
							}),
							children: t("settings.models.check.start")
						})]
					})]
				})
			]
		})
	});
}
function AuthenticationSectionContent({ providerId, onOpenModelHealthCheck, onRequestModelPullGuide }) {
	const connectionCheck = useProviderConnectionCheck(providerId);
	const { provider } = useProvider(providerId);
	if (provider && isLoginBasedProvider(provider)) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiKey, {
			providerId,
			apiKeyConnectivity: connectionCheck.apiKeyConnectivity,
			onOpenConnectionCheck: connectionCheck.openConnectionCheck,
			requiresApiKey: connectionCheck.requiresApiKey,
			onRequestModelPullGuide
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiHost, {
			providerId,
			onRequestModelPullGuide
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderConnectionCheckDrawer, {
			open: connectionCheck.connectionCheckOpen,
			models: connectionCheck.checkableModels,
			apiKeys: connectionCheck.checkableApiKeys,
			connectionError: connectionCheck.apiKeyConnectivity.error,
			isSubmitting: connectionCheck.apiKeyConnectivity.checking ?? false,
			requiresApiKey: connectionCheck.requiresApiKey,
			onClose: connectionCheck.closeConnectionCheck,
			onStart: connectionCheck.startConnectionCheck,
			onOpenModelHealthCheck
		})
	] });
}
function AuthenticationSection({ providerId, onOpenModelHealthCheck, onRequestModelPullGuide }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ApiKeyProvider, {
		value: useProviderApiKey(providerId),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AuthConnectionSlotsLayout, {
			providerId,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AuthenticationSectionContent, {
				providerId,
				onOpenModelHealthCheck,
				onRequestModelPullGuide
			})
		})
	});
}
var logger$5 = loggerService.withContext("ProviderSettings:OnboardingAutoEnable");
function useProviderOnboardingAutoEnable({ providerId, isOnboarding }) {
	const { provider } = useProvider(providerId);
	const { data: apiKeysData } = useProviderApiKeys(providerId);
	const { updateProvider } = useProviderMutations(providerId);
	const previousHasServerApiKeyRef = (0, import_react.useRef)(/* @__PURE__ */ new Map());
	const hasServerApiKey = apiKeysData ? apiKeysData.keys.some((item) => item.isEnabled && item.key.trim().length > 0) : void 0;
	(0, import_react.useEffect)(() => {
		if (!isOnboarding || !provider || hasServerApiKey === void 0) return;
		const previousHasServerApiKey = previousHasServerApiKeyRef.current.get(provider.id);
		previousHasServerApiKeyRef.current.set(provider.id, hasServerApiKey);
		if (provider.isEnabled || !hasServerApiKey || previousHasServerApiKey !== false) return;
		updateProvider({ isEnabled: true }).catch((error) => {
			logger$5.error("Failed to auto-enable onboarding provider", {
				providerId: provider.id,
				error
			});
		});
	}, [
		hasServerApiKey,
		isOnboarding,
		provider,
		updateProvider
	]);
}
var logger$4 = loggerService.withContext("ProviderSettings:checkModelsHealth");
async function checkModelWithMultipleKeys(model, apiKeys, timeout, signal) {
	if (apiKeys.length === 0) return [];
	return Promise.all(apiKeys.map(async (key) => {
		signal?.throwIfAborted();
		try {
			const { latency } = await checkApi(model.id, {
				apiKey: key,
				timeout,
				signal
			});
			return {
				kind: "ok",
				key,
				status: HealthStatus.SUCCESS,
				checking: false,
				latency
			};
		} catch (error) {
			return {
				kind: "failed",
				key,
				status: HealthStatus.FAILED,
				checking: false,
				error: serializeHealthCheckError(error)
			};
		}
	}));
}
async function checkModelsHealth(options, onModelChecked) {
	const { models, apiKeys, isConcurrent, timeout, signal } = options;
	const results = [];
	try {
		const runModelCheck = async (model, index) => {
			signal?.throwIfAborted();
			const keyResults = await checkModelWithMultipleKeys(model, apiKeys, timeout, signal);
			signal?.throwIfAborted();
			const analysis = aggregateApiKeyResults(keyResults);
			const result = analysis.status === HealthStatus.SUCCESS ? {
				kind: "ok",
				model,
				keyResults,
				status: HealthStatus.SUCCESS,
				checking: false,
				latency: analysis.latency
			} : {
				kind: "failed",
				model,
				keyResults,
				status: HealthStatus.FAILED,
				checking: false,
				error: analysis.error,
				latency: analysis.latency
			};
			if (isConcurrent) results[index] = result;
			else results.push(result);
			onModelChecked?.(result, index);
			return result;
		};
		if (isConcurrent) await Promise.all(models.map(runModelCheck));
		else for (let index = 0; index < models.length; index++) {
			const model = models[index];
			if (!model) continue;
			signal?.throwIfAborted();
			await runModelCheck(model, index);
		}
	} catch (error) {
		logger$4.error("[ProviderSettings checkModelsHealth] Model health check failed:", error);
		throw error;
	}
	return results;
}
var logger$3 = loggerService.withContext("ProviderSettings:ModelHealthCheck");
const useHealthCheck = (providerId) => {
	const { provider } = useProvider(providerId);
	const { models } = useModels({ providerId }, { swrOptions: PROVIDER_SETTINGS_MODEL_SWR_OPTIONS });
	const { data: apiKeysData } = useProviderApiKeys(providerId);
	const [modelStatuses, setModelStatuses] = (0, import_react.useState)([]);
	const [isChecking, setIsChecking] = (0, import_react.useState)(false);
	const [healthCheckOpen, setHealthCheckOpen] = (0, import_react.useState)(false);
	const runIdRef = (0, import_react.useRef)(0);
	const abortControllerRef = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		abortControllerRef.current?.abort();
		abortControllerRef.current = null;
		runIdRef.current += 1;
		setModelStatuses([]);
		setIsChecking(false);
		setHealthCheckOpen(false);
		return () => {
			abortControllerRef.current?.abort();
			abortControllerRef.current = null;
		};
	}, [providerId]);
	return {
		isChecking,
		modelStatuses,
		availableApiKeys: (0, import_react.useMemo)(() => splitApiKeyString(apiKeysData?.keys?.filter((item) => item.isEnabled).map((item) => item.key).join(",") ?? ""), [apiKeysData?.keys]),
		healthCheckOpen,
		openHealthCheck: (0, import_react.useCallback)(() => {
			setHealthCheckOpen(true);
		}, []),
		closeHealthCheck: (0, import_react.useCallback)(() => {
			abortControllerRef.current?.abort();
			abortControllerRef.current = null;
			runIdRef.current += 1;
			setIsChecking(false);
			setHealthCheckOpen(false);
		}, []),
		resetHealthCheckRun: (0, import_react.useCallback)(() => {
			abortControllerRef.current?.abort();
			abortControllerRef.current = null;
			runIdRef.current += 1;
			setModelStatuses([]);
			setIsChecking(false);
		}, []),
		startHealthCheck: (0, import_react.useCallback)(async ({ apiKeys, isConcurrent, timeout }) => {
			if (!provider) return;
			const modelCheckEntries = models.map((model, index) => ({
				model,
				index,
				skipReason: getModelHealthCheckSkipReason(model)
			}));
			if (isEmpty(modelCheckEntries)) {
				toast.error({
					timeout: 5e3,
					title: resolver_default.t("settings.provider.no_models_for_check")
				});
				setHealthCheckOpen(false);
				return;
			}
			abortControllerRef.current?.abort();
			abortControllerRef.current = null;
			const runId = runIdRef.current + 1;
			runIdRef.current = runId;
			setModelStatuses(modelCheckEntries.map(({ model, skipReason }) => skipReason ? {
				kind: "skipped",
				model,
				checking: false,
				status: HealthStatus.NOT_CHECKED,
				keyResults: [],
				skipReason
			} : {
				kind: "checking",
				model,
				checking: true,
				status: HealthStatus.NOT_CHECKED,
				keyResults: []
			}));
			const checkableEntries = modelCheckEntries.filter((entry) => entry.skipReason == null);
			if (isEmpty(checkableEntries)) {
				setIsChecking(false);
				return;
			}
			const keys$1 = apiKeys.length > 0 ? [...apiKeys] : [""];
			const abortController = new AbortController();
			abortControllerRef.current = abortController;
			setIsChecking(true);
			try {
				await checkModelsHealth({
					models: checkableEntries.map((entry) => entry.model),
					apiKeys: keys$1,
					isConcurrent,
					timeout,
					signal: abortController.signal
				}, (checkResult, index) => {
					if (runIdRef.current !== runId) return;
					const originalIndex = checkableEntries[index]?.index;
					if (originalIndex == null) return;
					setModelStatuses((current) => {
						const updated = [...current];
						if (updated[originalIndex]) updated[originalIndex] = checkResult;
						return updated;
					});
				});
			} catch (error) {
				if (abortController.signal.aborted) return;
				if (runIdRef.current === runId) {
					logger$3.error("Model health check run failed", {
						providerId: provider.id,
						runId,
						error
					});
					toast.error(resolver_default.t("settings.models.check.failed_to_start"));
				}
			} finally {
				if (runIdRef.current === runId) {
					abortControllerRef.current = null;
					setIsChecking(false);
				}
			}
		}, [models, provider])
	};
};
var ModelListHealthRunContext = (0, import_react.createContext)(null);
var ModelListHealthResultsContext = (0, import_react.createContext)(null);
function ModelListHealthProvider({ providerId, children }) {
	const { isChecking: isHealthChecking, modelStatuses, availableApiKeys, healthCheckOpen, openHealthCheck, closeHealthCheck, resetHealthCheckRun, startHealthCheck } = useHealthCheck(providerId);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelListHealthRunContext, {
		value: (0, import_react.useMemo)(() => ({
			isHealthChecking,
			availableApiKeys,
			healthCheckOpen,
			openHealthCheck,
			closeHealthCheck,
			resetHealthCheckRun,
			startHealthCheck
		}), [
			availableApiKeys,
			closeHealthCheck,
			healthCheckOpen,
			isHealthChecking,
			openHealthCheck,
			resetHealthCheckRun,
			startHealthCheck
		]),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelListHealthResultsContext, {
			value: (0, import_react.useMemo)(() => ({
				modelStatusMap: new Map(modelStatuses.map((status) => [status.model.id, status])),
				modelStatuses
			}), [modelStatuses]),
			children
		})
	});
}
function useModelListHealthRun() {
	const context = (0, import_react.use)(ModelListHealthRunContext);
	if (!context) throw new Error("useModelListHealthRun must be used within ModelListHealthProvider");
	return context;
}
function useModelListHealth() {
	const run = useModelListHealthRun();
	const results = (0, import_react.use)(ModelListHealthResultsContext);
	const context = (0, import_react.useMemo)(() => results ? {
		...run,
		...results
	} : null, [results, run]);
	if (!context) throw new Error("useModelListHealth must be used within ModelListHealthProvider");
	return context;
}
var TOGGLE_TO_CAPABILITY = {
	[MODEL_CAPABILITY.REASONING]: MODEL_CAPABILITY.REASONING,
	[MODEL_CAPABILITY.FUNCTION_CALL]: MODEL_CAPABILITY.FUNCTION_CALL
};
var CAPABILITY_TO_TOGGLE = Object.fromEntries(Object.entries(TOGGLE_TO_CAPABILITY).map(([key, value]) => [value, key]));
const MODEL_DRAWER_CURRENCY_SYMBOLS = ["$", "¥"];
const MODEL_ENDPOINT_OPTIONS = [
	{
		id: ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS,
		label: "endpoint_type.openai"
	},
	{
		id: ENDPOINT_TYPE.OPENAI_RESPONSES,
		label: "endpoint_type.openai-response"
	},
	{
		id: ENDPOINT_TYPE.ANTHROPIC_MESSAGES,
		label: "endpoint_type.anthropic"
	},
	{
		id: ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT,
		label: "endpoint_type.gemini"
	},
	{
		id: ENDPOINT_TYPE.OPENAI_EMBEDDINGS,
		label: "endpoint_type.openai-embeddings"
	},
	{
		id: ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION,
		label: "endpoint_type.image-generation"
	},
	{
		id: ENDPOINT_TYPE.OPENAI_IMAGE_EDIT,
		label: "endpoint_type.image-edit"
	},
	{
		id: ENDPOINT_TYPE.JINA_RERANK,
		label: "endpoint_type.jina-rerank"
	}
];
function getModelApiId(model) {
	return model.apiModelId ?? parseUniqueModelId(model.id).modelId;
}
function resolveInitialEndpointTypes(prefill, defaultEndpointType) {
	if (prefill?.endpointTypes?.length) return [...prefill.endpointTypes];
	if (prefill?.model?.endpointTypes?.length) return [...prefill.model.endpointTypes];
	if (prefill?.endpointType) return [prefill.endpointType];
	return [defaultEndpointType];
}
function getInitialAddModelFormState(prefill, defaultEndpointType = ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS) {
	return {
		modelId: prefill?.model ? getModelApiId(prefill.model) : "",
		name: prefill?.model?.name ?? "",
		group: prefill?.model?.group ?? "",
		contextWindow: prefill?.model?.contextWindow != null ? String(prefill.model.contextWindow) : "",
		maxInputTokens: prefill?.model?.maxInputTokens != null ? String(prefill.model.maxInputTokens) : "",
		maxOutputTokens: prefill?.model?.maxOutputTokens != null ? String(prefill.model.maxOutputTokens) : "",
		endpointTypes: resolveInitialEndpointTypes(prefill, defaultEndpointType)
	};
}
function splitModelIds(rawModelId) {
	return rawModelId.split(",").map((item) => item.trim()).filter(Boolean);
}
function readCurrency(model) {
	return model.pricing?.input?.currency ?? model.pricing?.output?.currency ?? "$";
}
function capsToToggleSet(capabilities) {
	const selected = /* @__PURE__ */ new Set();
	for (const capability of capabilities) {
		const toggle = CAPABILITY_TO_TOGGLE[capability];
		if (toggle) selected.add(toggle);
	}
	return selected;
}
var MODEL_PRIMARY_TYPE_CAPABILITIES = [
	MODEL_CAPABILITY.IMAGE_GENERATION,
	MODEL_CAPABILITY.EMBEDDING,
	MODEL_CAPABILITY.RERANK
];
var UNEDITABLE_MODEL_TYPE_CAPABILITIES = new Set([
	MODEL_CAPABILITY.AUDIO_GENERATION,
	MODEL_CAPABILITY.VIDEO_GENERATION,
	MODEL_CAPABILITY.AUDIO_TRANSCRIPT
]);
var LEGACY_INPUT_MODALITY_CAPABILITIES = [
	MODEL_CAPABILITY.IMAGE_RECOGNITION,
	MODEL_CAPABILITY.AUDIO_RECOGNITION,
	MODEL_CAPABILITY.VIDEO_RECOGNITION
];
var PRIMARY_TYPE_TO_CAPABILITY = {
	image: MODEL_CAPABILITY.IMAGE_GENERATION,
	embedding: MODEL_CAPABILITY.EMBEDDING,
	rerank: MODEL_CAPABILITY.RERANK
};
var INPUT_MODALITY_TO_LEGACY_CAPABILITY = {
	[MODALITY.IMAGE]: MODEL_CAPABILITY.IMAGE_RECOGNITION,
	[MODALITY.AUDIO]: MODEL_CAPABILITY.AUDIO_RECOGNITION,
	[MODALITY.VIDEO]: MODEL_CAPABILITY.VIDEO_RECOGNITION
};
function getInitialModelClassification(model) {
	const capabilities = model?.capabilities ?? [];
	let primaryType = "text";
	if (capabilities.includes(MODEL_CAPABILITY.RERANK)) primaryType = "rerank";
	else if (capabilities.includes(MODEL_CAPABILITY.EMBEDDING)) primaryType = "embedding";
	else if (capabilities.includes(MODEL_CAPABILITY.IMAGE_GENERATION)) primaryType = "image";
	else if (capabilities.some((capability) => UNEDITABLE_MODEL_TYPE_CAPABILITIES.has(capability))) primaryType = null;
	const inputModalities = /* @__PURE__ */ new Set();
	for (const modality of [
		MODALITY.IMAGE,
		MODALITY.AUDIO,
		MODALITY.VIDEO
	]) if (model?.inputModalities?.includes(modality) || capabilities.includes(INPUT_MODALITY_TO_LEGACY_CAPABILITY[modality])) inputModalities.add(modality);
	return {
		primaryType,
		capabilities: capsToToggleSet(capabilities),
		inputModalities
	};
}
function buildModelCapabilities(original, classification) {
	const managedCapabilities = new Set([
		...MODEL_PRIMARY_TYPE_CAPABILITIES,
		...LEGACY_INPUT_MODALITY_CAPABILITIES,
		...Object.values(TOGGLE_TO_CAPABILITY)
	]);
	if (classification.primaryType !== null) for (const capability of UNEDITABLE_MODEL_TYPE_CAPABILITIES) managedCapabilities.add(capability);
	const next = original.filter((capability) => !managedCapabilities.has(capability));
	if (classification.primaryType && classification.primaryType !== "text") next.push(PRIMARY_TYPE_TO_CAPABILITY[classification.primaryType]);
	for (const toggle of classification.capabilities) next.push(TOGGLE_TO_CAPABILITY[toggle]);
	return next;
}
function buildModelInputModalities(original, classification) {
	const managedModalities = new Set([
		MODALITY.IMAGE,
		MODALITY.AUDIO,
		MODALITY.VIDEO
	]);
	const next = original.filter((modality) => !managedModalities.has(modality));
	for (const modality of [
		MODALITY.IMAGE,
		MODALITY.AUDIO,
		MODALITY.VIDEO
	]) if (classification.inputModalities.has(modality)) next.push(modality);
	return next;
}
function areModelClassificationsEqual(left, right) {
	if (left.primaryType !== right.primaryType) return false;
	return left.capabilities.size === right.capabilities.size && [...left.capabilities].every((capability) => right.capabilities.has(capability)) && left.inputModalities.size === right.inputModalities.size && [...left.inputModalities].every((modality) => right.inputModalities.has(modality));
}
function ModelEndpointTypeChips({ value, onChange }) {
	const { t } = useTranslation();
	const selected = new Set(value);
	const toggle = (id) => {
		const next = new Set(selected);
		if (next.has(id)) next.delete(id);
		else next.add(id);
		onChange(MODEL_ENDPOINT_OPTIONS.map((option) => option.id).filter((optionId) => next.has(optionId)));
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.model-endpoint-type-chips",
		className: drawerClasses.endpointChipRow,
		children: MODEL_ENDPOINT_OPTIONS.map((option) => {
			const active = selected.has(option.id);
			return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
				type: "button",
				variant: active ? "secondary" : "outline",
				size: "sm",
				"aria-pressed": active,
				className: active ? "border border-border text-foreground" : void 0,
				onClick: () => toggle(option.id),
				children: [active ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Check, {
					"aria-hidden": true,
					className: "size-3"
				}) : null, t(option.label)]
			}, option.id);
		})
	});
}
function ModelEndpointTypeSelect({ value, onChange }) {
	const { t } = useTranslation();
	const options = MODEL_ENDPOINT_OPTIONS.map((option) => ({
		value: option.id,
		label: t(option.label)
	}));
	const handleChange = (nextValue) => {
		const next = Array.isArray(nextValue) ? nextValue : [nextValue];
		const nextSet = new Set(next);
		onChange(MODEL_ENDPOINT_OPTIONS.map((option) => option.id).filter((optionId) => nextSet.has(optionId)));
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Combobox, {
		multiple: true,
		searchable: false,
		options,
		value: [...value],
		placeholder: t("settings.models.add.endpoint_type.placeholder"),
		className: cn(drawerClasses.selectTrigger, "h-8 min-h-8 py-1"),
		popoverClassName: "w-(--radix-popover-trigger-width)",
		renderValue: (selectedValues, availableOptions) => {
			const values = Array.isArray(selectedValues) ? selectedValues : [selectedValues];
			const labels = availableOptions.filter((option) => values.includes(option.value)).map((option) => option.label);
			return labels.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "min-w-0 flex-1 truncate text-left",
				children: labels.join(", ")
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "min-w-0 flex-1 truncate text-left text-muted-foreground",
				children: t("settings.models.add.endpoint_type.placeholder")
			});
		},
		onChange: handleChange
	});
}
function ModelBasicFields({ values, showEndpointType, endpointTypeControl = "select", showRequiredIndicator = false, layout = "vertical", modelIdDisabled = false, modelIdAutoFocus = false, modelIdInputRef, modelIdAction, modelIdError, endpointTypeError, onModelIdChange, onNameChange, onNameBlur, onGroupChange, onGroupBlur, onEndpointTypesChange }) {
	const { t } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
			title: showRequiredIndicator ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
				className: "inline-flex items-baseline gap-1",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.models.add.model_id.label") }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					"aria-hidden": true,
					className: "text-destructive",
					children: "*"
				})]
			}) : t("settings.models.add.model_id.label"),
			titleClassName: drawerClasses.fieldTitle,
			layout,
			className: drawerClasses.field,
			help: modelIdError ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: drawerClasses.errorText,
				children: modelIdError
			}) : null,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: drawerClasses.valueRow,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
					autoFocus: modelIdAutoFocus,
					ref: modelIdInputRef,
					required: true,
					spellCheck: false,
					maxLength: 200,
					"aria-label": t("settings.models.add.model_id.label"),
					value: values.modelId,
					readOnly: modelIdDisabled,
					"aria-readonly": modelIdDisabled,
					"aria-invalid": Boolean(modelIdError),
					placeholder: t("settings.models.add.model_id.placeholder"),
					className: cn(drawerClasses.input, modelIdDisabled && drawerClasses.inputDisabled),
					onChange: (event) => onModelIdChange(event.target.value)
				}), modelIdAction]
			})
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
			title: t("settings.models.add.model_name.label"),
			titleClassName: drawerClasses.fieldTitle,
			layout,
			className: drawerClasses.field,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				spellCheck: false,
				"aria-label": t("settings.models.add.model_name.label"),
				value: values.name,
				placeholder: t("settings.models.add.model_name.placeholder"),
				className: drawerClasses.input,
				onChange: (event) => onNameChange(event.target.value),
				onBlur: onNameBlur
			})
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
			title: t("settings.models.add.group_name.label"),
			titleClassName: drawerClasses.fieldTitle,
			layout,
			className: drawerClasses.field,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				spellCheck: false,
				"aria-label": t("settings.models.add.group_name.label"),
				value: values.group,
				placeholder: t("settings.models.add.group_name.placeholder"),
				className: drawerClasses.input,
				onChange: (event) => onGroupChange(event.target.value),
				onBlur: onGroupBlur
			})
		}),
		showEndpointType && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
			title: t("settings.models.add.endpoint_type.label"),
			titleClassName: drawerClasses.fieldTitle,
			layout,
			className: drawerClasses.field,
			help: endpointTypeError ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: drawerClasses.errorText,
				children: endpointTypeError
			}) : null,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-ui": "settings.model-basic-fields.provider-settings-model-endpoint",
				"data-testid": "provider-settings-model-endpoint-type-field",
				children: endpointTypeControl === "chips" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelEndpointTypeChips, {
					value: values.endpointTypes ?? [],
					onChange: onEndpointTypesChange
				}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelEndpointTypeSelect, {
					value: values.endpointTypes ?? [],
					onChange: onEndpointTypesChange
				})
			})
		})
	] });
}
var MODEL_TYPE_OPTIONS = [
	{
		value: "text",
		label: "models.type.text",
		icon: Type
	},
	{
		value: "image",
		label: "models.type.image",
		icon: Image
	},
	{
		value: "embedding",
		label: "models.type.embedding",
		icon: Boxes
	},
	{
		value: "rerank",
		label: "models.type.rerank",
		icon: ArrowUpDown
	}
];
var MODEL_CAPABILITY_OPTIONS = [{
	value: MODEL_CAPABILITY.REASONING,
	label: "models.type.reasoning",
	icon: BrainCircuit
}, {
	value: MODEL_CAPABILITY.FUNCTION_CALL,
	label: "models.type.function_calling",
	icon: Wrench
}];
var INPUT_MODALITY_OPTIONS = [
	{
		value: MODALITY.IMAGE,
		label: "models.type.vision",
		icon: Eye
	},
	{
		value: MODALITY.AUDIO,
		label: "models.type.audio",
		icon: Ear
	},
	{
		value: MODALITY.VIDEO,
		label: "models.type.video",
		icon: Video
	}
];
var optionButtonClassName = "h-7 min-h-7 gap-1.5 rounded-md px-2.5 text-xs font-normal shadow-none [&_svg]:size-3.5";
function OptionButton({ option, selected, onClick }) {
	const { t } = useTranslation();
	const Icon = option.icon;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
		type: "button",
		variant: selected ? "secondary" : "outline",
		size: "sm",
		"aria-pressed": selected,
		className: cn(optionButtonClassName, selected && "border border-border text-foreground"),
		onClick,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, { "aria-hidden": true }), t(option.label)]
	});
}
function ModelClassificationControls({ value, hasChanges = false, onPrimaryTypeChange, onCapabilityToggle, onInputModalityToggle, onReset }) {
	const { t } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.model-classification-controls",
		className: "space-y-4",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				"data-ui": "settings.model-classification-controls.group",
				className: "space-y-2",
				role: "group",
				"aria-label": t("settings.models.add.model_type.label"),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex items-center justify-between gap-3",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: drawerClasses.fieldTitle,
						children: t("settings.models.add.model_type.label")
					}), onReset ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						variant: "ghost",
						size: "icon-sm",
						className: cn("size-7", !hasChanges && "invisible"),
						"aria-label": t("common.reset"),
						"aria-hidden": !hasChanges,
						tabIndex: hasChanges ? void 0 : -1,
						onClick: onReset,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RotateCcw, {
							"aria-hidden": true,
							className: "size-3.5"
						})
					}) : null]
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex flex-wrap items-center gap-2",
					children: MODEL_TYPE_OPTIONS.map((option) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OptionButton, {
						option,
						selected: value.primaryType === option.value,
						onClick: () => onPrimaryTypeChange(option.value)
					}, option.value))
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				"data-ui": "settings.model-classification-controls.group",
				className: "space-y-2",
				role: "group",
				"aria-label": t("settings.models.add.capabilities.label"),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: drawerClasses.fieldTitle,
					children: t("settings.models.add.capabilities.label")
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex flex-wrap items-center gap-2",
					children: MODEL_CAPABILITY_OPTIONS.map((option) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OptionButton, {
						option,
						selected: value.capabilities.has(option.value),
						onClick: () => onCapabilityToggle(option.value)
					}, option.value))
				})]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				"data-ui": "settings.model-classification-controls.group",
				className: "space-y-2",
				role: "group",
				"aria-label": t("settings.models.add.input_modalities.label"),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: drawerClasses.fieldTitle,
					children: t("settings.models.add.input_modalities.label")
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex flex-wrap items-center gap-2",
					children: INPUT_MODALITY_OPTIONS.map((option) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OptionButton, {
						option,
						selected: value.inputModalities.has(option.value),
						onClick: () => onInputModalityToggle(option.value)
					}, option.value))
				})]
			})
		]
	});
}
function ModelContextWindowFields({ contextWindow, maxInputTokens, maxOutputTokens, onContextWindowChange, onContextWindowBlur, onMaxInputTokensChange, onMaxInputTokensBlur, onMaxOutputTokensChange, onMaxOutputTokensBlur }) {
	const { t } = useTranslation();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
			title: t("settings.models.add.context_window.label"),
			titleClassName: drawerClasses.fieldTitle,
			className: drawerClasses.field,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				type: "number",
				min: 1,
				step: 1,
				inputMode: "numeric",
				"aria-label": t("settings.models.add.context_window.label"),
				value: contextWindow,
				placeholder: t("settings.models.add.context_window.placeholder"),
				className: drawerClasses.input,
				onChange: (event) => onContextWindowChange(event.target.value.replace(/[^\d]/g, "")),
				onBlur: onContextWindowBlur
			})
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
			title: t("settings.models.add.max_input_tokens.label"),
			titleClassName: drawerClasses.fieldTitle,
			className: drawerClasses.field,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				type: "number",
				min: 1,
				step: 1,
				inputMode: "numeric",
				"aria-label": t("settings.models.add.max_input_tokens.label"),
				value: maxInputTokens,
				placeholder: t("settings.models.add.max_input_tokens.placeholder"),
				className: drawerClasses.input,
				onChange: (event) => onMaxInputTokensChange(event.target.value.replace(/[^\d]/g, "")),
				onBlur: onMaxInputTokensBlur
			})
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
			title: t("settings.models.add.max_output_tokens.label"),
			titleClassName: drawerClasses.fieldTitle,
			className: drawerClasses.field,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				type: "number",
				min: 1,
				step: 1,
				inputMode: "numeric",
				"aria-label": t("settings.models.add.max_output_tokens.label"),
				value: maxOutputTokens,
				placeholder: t("settings.models.add.max_output_tokens.placeholder"),
				className: drawerClasses.input,
				onChange: (event) => onMaxOutputTokensChange(event.target.value.replace(/[^\d]/g, "")),
				onBlur: onMaxOutputTokensBlur
			})
		})
	] });
}
const MODEL_CHAT_ENDPOINT_TYPES = [
	ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS,
	ENDPOINT_TYPE.OPENAI_RESPONSES,
	ENDPOINT_TYPE.ANTHROPIC_MESSAGES,
	ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT
];
function isModelChatEndpointType(endpointType) {
	return MODEL_CHAT_ENDPOINT_TYPES.some((candidate) => candidate === endpointType);
}
function addUnique(items, item) {
	const next = [...items ?? []];
	if (!next.includes(item)) next.push(item);
	return next;
}
function removeItem(items, item) {
	return items?.filter((candidate) => candidate !== item);
}
function getModelDrawerMode(provider) {
	if (matchesPreset(provider, "new-api") || matchesPreset(provider, "cherryin") || matchesPreset(provider, "aionly")) return "endpoint-types";
	if (provider.presetProviderId == null && !isSystemProviderId(provider.id)) return "purpose";
	return "legacy";
}
function getProviderChatEndpointTypes(provider) {
	const endpointTypes = [];
	if (isModelChatEndpointType(provider.defaultChatEndpoint)) endpointTypes.push(provider.defaultChatEndpoint);
	for (const endpointType of Object.keys(provider.endpointConfigs ?? {})) if (isModelChatEndpointType(endpointType) && !endpointTypes.includes(endpointType)) endpointTypes.push(endpointType);
	return endpointTypes;
}
function inferModelPurpose(fields) {
	const primaryEndpointType = fields.endpointTypes?.[0];
	if (primaryEndpointType === ENDPOINT_TYPE.OPENAI_IMAGE_EDIT) return "image-edit";
	if (primaryEndpointType === ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION) return "image-generation";
	if (!primaryEndpointType && fields.capabilities?.includes(MODEL_CAPABILITY.IMAGE_GENERATION)) return "image-generation";
	return "chat";
}
function getInitialChatEndpointType(fields, fallback = ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS) {
	return fields.endpointTypes?.find(isModelChatEndpointType) ?? fallback;
}
function applyModelPurpose(fields, purpose, options = {}) {
	const previousPurpose = options.previousPurpose ?? inferModelPurpose(fields);
	let capabilities = [...fields.capabilities ?? []];
	let inputModalities = fields.inputModalities ? [...fields.inputModalities] : void 0;
	let outputModalities = fields.outputModalities ? [...fields.outputModalities] : void 0;
	if (previousPurpose !== "chat") {
		capabilities = capabilities.filter((capability) => capability !== MODEL_CAPABILITY.IMAGE_GENERATION);
		outputModalities = removeItem(outputModalities, MODALITY.IMAGE);
		if (previousPurpose === "image-edit" && !capabilities.includes(MODEL_CAPABILITY.IMAGE_RECOGNITION)) inputModalities = removeItem(inputModalities, MODALITY.IMAGE);
	}
	if (purpose === "chat") return {
		endpointTypes: [options.chatEndpointType ?? getInitialChatEndpointType(fields, ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS)],
		capabilities,
		inputModalities,
		outputModalities
	};
	capabilities = addUnique(capabilities, MODEL_CAPABILITY.IMAGE_GENERATION);
	outputModalities = addUnique(outputModalities, MODALITY.IMAGE);
	if (purpose === "image-edit") inputModalities = addUnique(inputModalities, MODALITY.IMAGE);
	return {
		endpointTypes: [purpose === "image-edit" ? ENDPOINT_TYPE.OPENAI_IMAGE_EDIT : ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION],
		capabilities,
		inputModalities,
		outputModalities
	};
}
var PURPOSES = [
	"chat",
	"image-generation",
	"image-edit"
];
var ENDPOINT_LABEL_KEYS = {
	[ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS]: "settings.provider.more_endpoints.openai_chat",
	[ENDPOINT_TYPE.OPENAI_RESPONSES]: "settings.provider.more_endpoints.openai_responses",
	[ENDPOINT_TYPE.ANTHROPIC_MESSAGES]: "settings.provider.more_endpoints.anthropic",
	[ENDPOINT_TYPE.GOOGLE_GENERATE_CONTENT]: "settings.provider.more_endpoints.gemini"
};
var PURPOSE_LABEL_KEYS = {
	chat: {
		label: "settings.models.add.purpose.chat.label",
		description: "settings.models.add.purpose.chat.description"
	},
	"image-generation": {
		label: "settings.models.add.purpose.image_generation.label",
		description: "settings.models.add.purpose.image_generation.description"
	},
	"image-edit": {
		label: "settings.models.add.purpose.image_edit.label",
		description: "settings.models.add.purpose.image_edit.description"
	}
};
function ModelPurposeFields({ purpose, chatEndpointType, chatEndpointTypes, onPurposeChange, onChatEndpointTypeChange }) {
	const { t } = useTranslation();
	const uid = (0, import_react.useId)();
	const descriptionId = `${uid}-purpose-description`;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(FieldSet, {
		className: "gap-2",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldLegend, {
				variant: "label",
				className: "mb-0 text-[13px] text-foreground",
				children: t("settings.models.add.purpose.label")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
				id: descriptionId,
				className: "text-muted-foreground text-xs",
				children: t("settings.models.add.purpose.description")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroup, {
				value: purpose,
				"aria-describedby": descriptionId,
				onValueChange: (value) => onPurposeChange(value),
				children: PURPOSES.map((option) => {
					const optionId = `${uid}-${option}`;
					const label = PURPOSE_LABEL_KEYS[option];
					return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Label, {
						htmlFor: optionId,
						className: cn("flex min-h-10 cursor-pointer items-start gap-3 rounded-lg border border-border-subtle px-3 py-2.5", "transition-[background-color,border-color,box-shadow] duration-150 hover:bg-accent", purpose === option && "border-primary bg-accent"),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroupItem, {
							id: optionId,
							value: option,
							className: "mt-0.5"
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "block text-[13px] text-foreground",
							children: t(label.label)
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "mt-0.5 block font-normal text-foreground-tertiary text-xs",
							children: t(label.description)
						})] })]
					}, option);
				})
			}),
			purpose === "chat" && chatEndpointTypes.length > 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "mt-1 flex flex-col gap-2",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
					htmlFor: `${uid}-chat-protocol`,
					className: "text-[13px] text-foreground",
					children: t("settings.models.add.purpose.chat_protocol")
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Select, {
					value: chatEndpointType,
					onValueChange: (value) => onChatEndpointTypeChange(value),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectTrigger, {
						id: `${uid}-chat-protocol`,
						className: "min-h-10 w-full",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectValue, {})
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectContent, { children: chatEndpointTypes.map((endpointType) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
						value: endpointType,
						children: t(ENDPOINT_LABEL_KEYS[endpointType])
					}, endpointType)) })]
				})]
			})
		]
	});
}
function getInitialPurposeFields(prefill, defaultEndpointType) {
	return {
		endpointTypes: getInitialAddModelFormState(prefill, defaultEndpointType).endpointTypes,
		capabilities: prefill?.model?.capabilities,
		inputModalities: prefill?.model?.inputModalities,
		outputModalities: prefill?.model?.outputModalities
	};
}
function AddModelFormPanel({ providerId, prefill, onSuccess, onCancel, onDrawerFooterBinding, formId = "provider-settings-model-add-form", "data-testid": dataTestId = "provider-settings-model-add-drawer-content" }) {
	const { t } = useTranslation();
	const { provider } = useProvider(providerId);
	const { models } = useModels({ providerId });
	const { createModel } = useModelMutations();
	const [formState, setFormState] = (0, import_react.useState)(() => getInitialAddModelFormState(null, ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS));
	const [purposeFields, setPurposeFields] = (0, import_react.useState)(() => getInitialPurposeFields(null, ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS));
	const [classification, setClassification] = (0, import_react.useState)(() => getInitialModelClassification());
	const [modelIdTouched, setModelIdTouched] = (0, import_react.useState)(false);
	const [endpointTypeTouched, setEndpointTypeTouched] = (0, import_react.useState)(false);
	const [showMoreSettings, setShowMoreSettings] = (0, import_react.useState)(false);
	const [isSubmitting, setIsSubmitting] = (0, import_react.useState)(false);
	const [submitError, setSubmitError] = (0, import_react.useState)(null);
	const submitInFlightRef = (0, import_react.useRef)(false);
	const modelIdInputRef = (0, import_react.useRef)(null);
	const mode = provider ? getModelDrawerMode(provider) : "legacy";
	const providerChatEndpointTypes = provider ? getProviderChatEndpointTypes(provider) : [];
	const defaultChatEndpoint = providerChatEndpointTypes[0] ?? ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS;
	const modelPurpose = inferModelPurpose(purposeFields);
	const chatEndpointType = getInitialChatEndpointType(purposeFields, defaultChatEndpoint);
	(0, import_react.useEffect)(() => {
		setFormState(getInitialAddModelFormState(prefill, defaultChatEndpoint));
		setPurposeFields(getInitialPurposeFields(prefill, defaultChatEndpoint));
		setClassification(getInitialModelClassification(prefill?.model));
		setModelIdTouched(false);
		setEndpointTypeTouched(false);
		setShowMoreSettings(false);
		setSubmitError(null);
	}, [defaultChatEndpoint, prefill]);
	const handleModelIdChange = (0, import_react.useCallback)((value) => {
		if (!provider) return;
		setFormState((current) => ({
			...current,
			modelId: value,
			name: value,
			group: getDefaultGroupName(value, provider.id)
		}));
		setSubmitError(null);
		if (value.trim()) setModelIdTouched(false);
	}, [provider]);
	const addSingleModel = (0, import_react.useCallback)(async (values) => {
		if (!provider) return false;
		const modelId = values.modelId.trim();
		if (models.some((model) => model.id.endsWith(`::${modelId}`))) {
			setSubmitError(t("error.model.exists"));
			return false;
		}
		const classifiedCapabilities = buildModelCapabilities(prefill?.model?.capabilities ?? [], classification);
		const classifiedInputModalities = buildModelInputModalities(prefill?.model?.inputModalities ?? [], classification);
		const submittedPurposeFields = mode === "purpose" ? applyModelPurpose({
			...purposeFields,
			capabilities: classifiedCapabilities,
			inputModalities: classifiedInputModalities
		}, modelPurpose, {
			previousPurpose: "chat",
			chatEndpointType
		}) : null;
		await createModel({
			providerId,
			modelId,
			name: values.name ? values.name : modelId.toUpperCase(),
			group: values.group || getDefaultGroupName(modelId),
			endpointTypes: submittedPurposeFields != null ? [...submittedPurposeFields.endpointTypes] : mode === "endpoint-types" && values.endpointTypes?.length ? [...values.endpointTypes] : void 0,
			capabilities: submittedPurposeFields?.capabilities ?? classifiedCapabilities,
			inputModalities: submittedPurposeFields?.inputModalities ?? classifiedInputModalities,
			outputModalities: submittedPurposeFields?.outputModalities,
			...values.contextWindow ? { contextWindow: Number(values.contextWindow) } : {},
			...values.maxInputTokens ? { maxInputTokens: Number(values.maxInputTokens) } : {},
			...values.maxOutputTokens ? { maxOutputTokens: Number(values.maxOutputTokens) } : {}
		});
		return true;
	}, [
		chatEndpointType,
		classification,
		createModel,
		mode,
		modelPurpose,
		models,
		prefill?.model,
		provider,
		providerId,
		purposeFields,
		t
	]);
	const submitAddModel = (0, import_react.useCallback)(async () => {
		if (submitInFlightRef.current) return;
		const normalizedId = formState.modelId.trim().replaceAll("，", ",");
		if (!normalizedId) {
			setModelIdTouched(true);
			modelIdInputRef.current?.focus();
			return;
		}
		if (mode === "endpoint-types" && !(formState.endpointTypes?.length ?? 0)) {
			setEndpointTypeTouched(true);
			return;
		}
		submitInFlightRef.current = true;
		setIsSubmitting(true);
		setSubmitError(null);
		try {
			if (normalizedId.includes(",")) {
				let addedCount = 0;
				for (const singleId of splitModelIds(normalizedId)) if (await addSingleModel({
					modelId: singleId,
					name: singleId,
					group: "",
					contextWindow: "",
					maxInputTokens: "",
					maxOutputTokens: "",
					endpointTypes: formState.endpointTypes
				})) addedCount += 1;
				if (addedCount > 0) onSuccess();
				return;
			}
			if (await addSingleModel({
				...formState,
				modelId: normalizedId
			})) onSuccess();
		} catch {
			setSubmitError(t("settings.models.manage.operation_failed"));
		} finally {
			submitInFlightRef.current = false;
			setIsSubmitting(false);
		}
	}, [
		addSingleModel,
		formState,
		mode,
		onSuccess,
		t
	]);
	const handlePrimaryTypeChange = (0, import_react.useCallback)((primaryType) => {
		setClassification((current) => ({
			...current,
			primaryType
		}));
	}, []);
	const handleCapabilityToggle = (0, import_react.useCallback)((capability) => {
		setClassification((current) => {
			const capabilities = new Set(current.capabilities);
			if (capabilities.has(capability)) capabilities.delete(capability);
			else capabilities.add(capability);
			return {
				...current,
				capabilities
			};
		});
	}, []);
	const handleInputModalityToggle = (0, import_react.useCallback)((modality) => {
		setClassification((current) => {
			const inputModalities = new Set(current.inputModalities);
			if (inputModalities.has(modality)) inputModalities.delete(modality);
			else inputModalities.add(modality);
			return {
				...current,
				inputModalities
			};
		});
	}, []);
	const handleFormSubmit = (0, import_react.useCallback)(async (event) => {
		event.preventDefault();
		await submitAddModel();
	}, [submitAddModel]);
	const submitRunnerRef = (0, import_react.useRef)(submitAddModel);
	submitRunnerRef.current = submitAddModel;
	const runSubmit = (0, import_react.useCallback)(() => {
		submitRunnerRef.current();
	}, []);
	(0, import_react.useLayoutEffect)(() => {
		if (!onDrawerFooterBinding) return;
		if (!provider) {
			onDrawerFooterBinding(null);
			return;
		}
		onDrawerFooterBinding({
			isSubmitting,
			cancel: onCancel,
			submit: runSubmit
		});
	}, [
		provider,
		isSubmitting,
		onCancel,
		onDrawerFooterBinding,
		runSubmit
	]);
	(0, import_react.useEffect)(() => {
		if (!onDrawerFooterBinding) return;
		return () => {
			onDrawerFooterBinding(null);
		};
	}, [onDrawerFooterBinding]);
	if (!provider) return null;
	const form = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("form", {
		"data-ui": "settings.add-model-form-panel",
		id: formId,
		"data-testid": dataTestId,
		className: "flex min-h-0 flex-col gap-4 py-0",
		onSubmit: (event) => void handleFormSubmit(event),
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSection, {
				className: drawerClasses.section,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: drawerClasses.fieldList,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelBasicFields, {
						values: formState,
						showEndpointType: mode === "endpoint-types",
						showRequiredIndicator: true,
						layout: "horizontal",
						modelIdAutoFocus: true,
						modelIdInputRef,
						modelIdError: modelIdTouched && !formState.modelId.trim() ? t("settings.models.add.model_id.required") : void 0,
						endpointTypeError: endpointTypeTouched ? t("settings.models.add.endpoint_type.required") : void 0,
						onModelIdChange: handleModelIdChange,
						onNameChange: (value) => setFormState((current) => ({
							...current,
							name: value
						})),
						onGroupChange: (value) => setFormState((current) => ({
							...current,
							group: value
						})),
						onEndpointTypesChange: (next) => {
							setEndpointTypeTouched(false);
							setFormState((current) => ({
								...current,
								endpointTypes: [...next]
							}));
						}
					}), mode === "purpose" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelPurposeFields, {
						purpose: modelPurpose,
						chatEndpointType,
						chatEndpointTypes: providerChatEndpointTypes,
						onPurposeChange: (nextPurpose) => {
							setClassification((current) => ({
								...current,
								primaryType: nextPurpose === "chat" ? current.primaryType === "image" ? "text" : current.primaryType : "image"
							}));
							setPurposeFields((current) => applyModelPurpose(current, nextPurpose, {
								previousPurpose: inferModelPurpose(current),
								chatEndpointType
							}));
						},
						onChatEndpointTypeChange: (nextEndpointType) => {
							setPurposeFields((current) => applyModelPurpose(current, "chat", {
								previousPurpose: inferModelPurpose(current),
								chatEndpointType: nextEndpointType
							}));
						}
					})]
				})
			}),
			submitError && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-ui": "settings.add-model-form-panel.alert",
				role: "alert",
				className: "rounded-md border border-error-border bg-error-subtle px-3 py-2 text-error-subtle-foreground text-xs leading-4",
				children: submitError
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderActions, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
				type: "button",
				variant: "ghost",
				className: drawerClasses.toggleButton,
				onClick: () => setShowMoreSettings((current) => !current),
				children: [t("settings.moresetting.label"), showMoreSettings ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronUp, { size: 16 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown, { size: 16 })]
			}) }),
			showMoreSettings && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSection, {
				className: drawerClasses.section,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "space-y-4",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: drawerClasses.sectionCard,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelClassificationControls, {
							value: classification,
							onPrimaryTypeChange: handlePrimaryTypeChange,
							onCapabilityToggle: handleCapabilityToggle,
							onInputModalityToggle: handleInputModalityToggle
						})
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: drawerClasses.sectionCard,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelContextWindowFields, {
							contextWindow: formState.contextWindow,
							maxInputTokens: formState.maxInputTokens,
							maxOutputTokens: formState.maxOutputTokens,
							onContextWindowChange: (value) => setFormState((current) => ({
								...current,
								contextWindow: value
							})),
							onMaxInputTokensChange: (value) => setFormState((current) => ({
								...current,
								maxInputTokens: value
							})),
							onMaxOutputTokensChange: (value) => setFormState((current) => ({
								...current,
								maxOutputTokens: value
							}))
						})
					})]
				})
			})
		]
	});
	if (!onDrawerFooterBinding) return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [form, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ProviderActions, {
		className: drawerClasses.footer,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			variant: "outline",
			type: "button",
			disabled: isSubmitting,
			onClick: onCancel,
			children: t("common.cancel")
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			type: "button",
			loading: isSubmitting,
			onClick: () => void submitAddModel(),
			children: t("settings.models.add.add_model")
		})]
	})] });
	return form;
}
function AddModelDrawer({ providerId, open, prefill, onClose }) {
	const { t } = useTranslation();
	const [footerBinding, setFooterBinding] = (0, import_react.useState)(null);
	const footer = footerBinding != null ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogFooter, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		variant: "outline",
		type: "button",
		disabled: footerBinding.isSubmitting,
		onClick: footerBinding.cancel,
		children: t("common.cancel")
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		type: "button",
		loading: footerBinding.isSubmitting,
		onClick: () => footerBinding.submit(),
		children: t("settings.models.add.add_model")
	})] }) : null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog, {
		open,
		onOpenChange: (nextOpen) => !nextOpen && onClose(),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogContent, {
			"aria-describedby": void 0,
			"data-testid": "provider-settings-model-add-dialog",
			className: "max-h-[calc(100vh-6rem)] grid-rows-[auto_minmax(0,1fr)_auto] gap-4 sm:max-w-[520px]",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogHeader, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogTitle, {
					className: "text-base leading-5",
					children: t("settings.models.add.add_model")
				}) }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "-m-1 min-h-0 overflow-y-auto p-1",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AddModelFormPanel, {
						providerId,
						prefill,
						onSuccess: onClose,
						onCancel: onClose,
						onDrawerFooterBinding: setFooterBinding
					})
				}),
				footer
			]
		})
	});
}
var isModelDrawerCurrencySymbol = (value) => MODEL_DRAWER_CURRENCY_SYMBOLS.includes(value);
var CURRENCY_SYMBOL_TO_CODE = {
	$: CURRENCY.USD,
	"¥": CURRENCY.CNY
};
var CURRENCY_CODE_TO_SYMBOL = {
	[CURRENCY.USD]: "$",
	[CURRENCY.CNY]: "¥"
};
var symbolToCurrency = (symbol) => CURRENCY_SYMBOL_TO_CODE[symbol];
var currencyToSymbol = (currency) => CURRENCY_CODE_TO_SYMBOL[currency];
function EditModelDrawer({ providerId, open, model: modelProp, onClose }) {
	const { t } = useTranslation();
	const { provider } = useProvider(providerId);
	const { updateModel } = useModelMutations();
	const previousModelRef = (0, import_react.useRef)(modelProp);
	if (modelProp) previousModelRef.current = modelProp;
	const model = modelProp ?? previousModelRef.current;
	const [name, setName] = (0, import_react.useState)("");
	const [group, setGroup] = (0, import_react.useState)("");
	const [endpointTypes, setEndpointTypes] = (0, import_react.useState)([]);
	const [purposeFields, setPurposeFields] = (0, import_react.useState)({});
	const [showMoreSettings, setShowMoreSettings] = (0, import_react.useState)(true);
	const [classification, setClassification] = (0, import_react.useState)(() => getInitialModelClassification());
	const [supportsStreaming, setSupportsStreaming] = (0, import_react.useState)(true);
	const [currencySymbol, setCurrencySymbol] = (0, import_react.useState)("$");
	const [inputPrice, setInputPrice] = (0, import_react.useState)("0");
	const [outputPrice, setOutputPrice] = (0, import_react.useState)("0");
	const [cacheReadPrice, setCacheReadPrice] = (0, import_react.useState)("");
	const [contextWindow, setContextWindow] = (0, import_react.useState)("");
	const [maxInputTokens, setMaxInputTokens] = (0, import_react.useState)("");
	const [maxOutputTokens, setMaxOutputTokens] = (0, import_react.useState)("");
	const [initializedModel, setInitializedModel] = (0, import_react.useState)(null);
	const autoSavePendingItemsRef = (0, import_react.useRef)(/* @__PURE__ */ new Map());
	const autoSaveRunningRef = (0, import_react.useRef)(false);
	const mode = provider ? getModelDrawerMode(provider) : "legacy";
	const providerChatEndpointTypes = provider ? getProviderChatEndpointTypes(provider) : [];
	const defaultChatEndpoint = providerChatEndpointTypes[0];
	const modelPurpose = inferModelPurpose(purposeFields);
	const chatEndpointType = getInitialChatEndpointType(purposeFields, defaultChatEndpoint);
	const apiModelId = (0, import_react.useMemo)(() => model ? getModelApiId(model) : "", [model]);
	const savedClassification = (0, import_react.useMemo)(() => getInitialModelClassification(model), [model]);
	const hasClassificationChanges = !areModelClassificationsEqual(classification, savedClassification);
	(0, import_react.useLayoutEffect)(() => {
		if (!open || !model) return;
		const nextCurrencySymbol = currencyToSymbol(readCurrency(model));
		setName(model.name);
		setGroup(model.group ?? "");
		setEndpointTypes(model.endpointTypes?.length ? [...model.endpointTypes] : []);
		setPurposeFields({
			endpointTypes: model.endpointTypes,
			capabilities: model.capabilities,
			inputModalities: model.inputModalities,
			outputModalities: model.outputModalities
		});
		setShowMoreSettings(true);
		setClassification(getInitialModelClassification(model));
		setSupportsStreaming(model.supportsStreaming);
		setCurrencySymbol(nextCurrencySymbol ?? "$");
		setInputPrice(String(model.pricing?.input?.perMillionTokens ?? 0));
		setOutputPrice(String(model.pricing?.output?.perMillionTokens ?? 0));
		const cacheReadRate = model.pricing?.cacheRead?.perMillionTokens;
		setCacheReadPrice(cacheReadRate == null ? "" : String(cacheReadRate));
		setContextWindow(model.contextWindow != null ? String(model.contextWindow) : "");
		setMaxInputTokens(model.maxInputTokens != null ? String(model.maxInputTokens) : "");
		setMaxOutputTokens(model.maxOutputTokens != null ? String(model.maxOutputTokens) : "");
		setInitializedModel(model);
	}, [model, open]);
	const handleUpdateModel = (0, import_react.useCallback)(async ({ providerId: providerId$1, modelId, patch }) => {
		await updateModel(providerId$1, modelId, {
			name: patch.name,
			group: patch.group,
			capabilities: patch.capabilities,
			inputModalities: patch.inputModalities,
			outputModalities: patch.outputModalities,
			supportsStreaming: patch.supportsStreaming,
			endpointTypes: patch.endpointTypes,
			contextWindow: patch.contextWindow,
			maxInputTokens: patch.maxInputTokens,
			maxOutputTokens: patch.maxOutputTokens,
			pricing: patch.pricing
		});
	}, [updateModel]);
	const buildPatch = (0, import_react.useCallback)((overrides) => {
		if (!model) return {};
		const finalCurrency = symbolToCurrency(overrides?.currencySymbol ?? currencySymbol) ?? symbolToCurrency(readCurrency(model)) ?? CURRENCY.USD;
		const nextName = overrides?.name ?? name;
		const nextGroup = overrides?.group ?? group;
		const nextCacheReadPriceValue = overrides?.cacheReadPrice ?? cacheReadPrice;
		const nextCacheReadPrice = nextCacheReadPriceValue.trim() === "" ? void 0 : Number(nextCacheReadPriceValue);
		const hasCacheReadPrice = nextCacheReadPrice !== void 0 && Number.isFinite(nextCacheReadPrice) && nextCacheReadPrice >= 0;
		const hasEndpointTypesOverride = overrides != null && Object.hasOwn(overrides, "endpointTypes");
		const hasPurposeFieldsOverride = overrides != null && Object.hasOwn(overrides, "purposeFields");
		const nextPurposeFields = overrides?.purposeFields ?? purposeFields;
		const nextClassification = overrides?.classification;
		const shouldApplyPurpose = mode === "purpose" && (hasPurposeFieldsOverride || nextClassification != null);
		const effectiveClassification = nextClassification ?? classification;
		const classifiedCapabilities = shouldApplyPurpose || nextClassification ? buildModelCapabilities(model.capabilities ?? [], effectiveClassification) : void 0;
		const classifiedInputModalities = shouldApplyPurpose || nextClassification ? buildModelInputModalities(model.inputModalities ?? [], effectiveClassification) : void 0;
		const resolvedPurposeFields = shouldApplyPurpose && classifiedCapabilities && classifiedInputModalities ? applyModelPurpose({
			...nextPurposeFields,
			capabilities: classifiedCapabilities,
			inputModalities: classifiedInputModalities
		}, inferModelPurpose(nextPurposeFields), {
			previousPurpose: inferModelPurpose(nextPurposeFields),
			chatEndpointType: getInitialChatEndpointType(nextPurposeFields, defaultChatEndpoint)
		}) : null;
		return {
			name: nextName || model.name,
			group: nextGroup || model.group,
			...hasPurposeFieldsOverride && resolvedPurposeFields ? { endpointTypes: [...resolvedPurposeFields.endpointTypes] } : hasEndpointTypesOverride ? { endpointTypes: mode === "endpoint-types" ? [...overrides.endpointTypes ?? []] : void 0 } : {},
			...resolvedPurposeFields ? {
				capabilities: resolvedPurposeFields.capabilities,
				inputModalities: resolvedPurposeFields.inputModalities
			} : nextClassification && classifiedCapabilities && classifiedInputModalities ? {
				capabilities: classifiedCapabilities,
				inputModalities: classifiedInputModalities
			} : {},
			...hasPurposeFieldsOverride && resolvedPurposeFields ? { outputModalities: resolvedPurposeFields.outputModalities } : {},
			supportsStreaming: overrides?.supportsStreaming ?? supportsStreaming,
			contextWindow: Number(overrides?.contextWindow ?? contextWindow) || void 0,
			maxInputTokens: Number(overrides?.maxInputTokens ?? maxInputTokens) || void 0,
			maxOutputTokens: Number(overrides?.maxOutputTokens ?? maxOutputTokens) || void 0,
			pricing: {
				input: {
					perMillionTokens: Number(overrides?.inputPrice ?? inputPrice) || 0,
					currency: finalCurrency
				},
				output: {
					perMillionTokens: Number(overrides?.outputPrice ?? outputPrice) || 0,
					currency: finalCurrency
				},
				...hasCacheReadPrice ? { cacheRead: {
					perMillionTokens: nextCacheReadPrice,
					currency: finalCurrency
				} } : {},
				...model.pricing?.cacheWrite ? { cacheWrite: {
					...model.pricing.cacheWrite,
					currency: finalCurrency
				} } : {}
			}
		};
	}, [
		currencySymbol,
		group,
		cacheReadPrice,
		contextWindow,
		inputPrice,
		maxInputTokens,
		maxOutputTokens,
		mode,
		model,
		name,
		outputPrice,
		purposeFields,
		classification,
		defaultChatEndpoint,
		supportsStreaming
	]);
	const processAutoSaveQueue = (0, import_react.useCallback)(async () => {
		if (autoSaveRunningRef.current) return;
		autoSaveRunningRef.current = true;
		try {
			while (autoSavePendingItemsRef.current.size > 0) {
				const [key, item] = autoSavePendingItemsRef.current.entries().next().value;
				autoSavePendingItemsRef.current.delete(key);
				try {
					await handleUpdateModel(item);
				} catch {
					toast.error(t("common.error"));
				}
			}
		} finally {
			autoSaveRunningRef.current = false;
		}
	}, [handleUpdateModel, t]);
	const autoSave = (0, import_react.useCallback)((overrides) => {
		if (!model) return;
		const { modelId } = parseUniqueModelId(model.id);
		const item = {
			providerId: model.providerId ?? providerId,
			modelId,
			patch: buildPatch(overrides)
		};
		autoSavePendingItemsRef.current.set(`${item.providerId}/${item.modelId}`, item);
		processAutoSaveQueue();
	}, [
		buildPatch,
		model,
		processAutoSaveQueue,
		providerId
	]);
	const commitClassification = (0, import_react.useCallback)((next) => {
		setClassification(next);
		autoSave({ classification: next });
	}, [autoSave]);
	const handlePrimaryTypeChange = (0, import_react.useCallback)((primaryType) => {
		commitClassification({
			...classification,
			primaryType
		});
	}, [classification, commitClassification]);
	const handleToggleCapability = (0, import_react.useCallback)((capability) => {
		const capabilities = new Set(classification.capabilities);
		if (capabilities.has(capability)) capabilities.delete(capability);
		else capabilities.add(capability);
		commitClassification({
			...classification,
			capabilities
		});
	}, [classification, commitClassification]);
	const handleToggleInputModality = (0, import_react.useCallback)((modality) => {
		const inputModalities = new Set(classification.inputModalities);
		if (inputModalities.has(modality)) inputModalities.delete(modality);
		else inputModalities.add(modality);
		commitClassification({
			...classification,
			inputModalities
		});
	}, [classification, commitClassification]);
	const handleResetClassification = (0, import_react.useCallback)(() => {
		const nextClassification = {
			...savedClassification,
			capabilities: new Set(savedClassification.capabilities),
			inputModalities: new Set(savedClassification.inputModalities)
		};
		setClassification(nextClassification);
		autoSave({ classification: nextClassification });
	}, [autoSave, savedClassification]);
	if (!provider || !model) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsDrawer, {
		open,
		onClose,
		title: t("models.edit")
	});
	if (initializedModel !== model) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsDrawer, {
		open,
		onClose,
		title: t("models.edit")
	});
	const currentCurrency = currencySymbol || "$";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsDrawer, {
		open,
		onClose,
		title: t("models.edit"),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("form", {
			"data-ui": "settings.edit-model-drawer.provider-settings-model-edit",
			id: "provider-settings-model-edit-form",
			"data-testid": "provider-settings-model-edit-drawer-content",
			className: "flex min-h-0 flex-col gap-4 py-0",
			onSubmit: (event) => event.preventDefault(),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSection, {
					className: drawerClasses.section,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: drawerClasses.fieldList,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelBasicFields, {
							values: {
								modelId: apiModelId,
								name,
								group,
								contextWindow,
								maxInputTokens,
								maxOutputTokens,
								endpointTypes
							},
							showEndpointType: mode === "endpoint-types",
							endpointTypeControl: "chips",
							modelIdDisabled: true,
							modelIdAction: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
								type: "button",
								"aria-label": t("message.copied"),
								className: fieldClasses.inputActionButton,
								onClick: () => {
									navigator.clipboard.writeText(apiModelId);
									toast.success(t("message.copied"));
								},
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyIcon_default, { size: 14 })
							}),
							onModelIdChange: (value) => {
								setName(value);
								setGroup(getDefaultGroupName(value));
							},
							onNameChange: setName,
							onNameBlur: () => autoSave({ name }),
							onGroupChange: setGroup,
							onGroupBlur: () => autoSave({ group }),
							onEndpointTypesChange: (next) => {
								const nextEndpointTypes = [...next];
								setEndpointTypes(nextEndpointTypes);
								autoSave({ endpointTypes: nextEndpointTypes });
							}
						}), mode === "purpose" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelPurposeFields, {
							purpose: modelPurpose,
							chatEndpointType,
							chatEndpointTypes: providerChatEndpointTypes,
							onPurposeChange: (nextPurpose) => {
								const nextPurposeFields = applyModelPurpose(purposeFields, nextPurpose, {
									previousPurpose: modelPurpose,
									chatEndpointType
								});
								const nextClassification = {
									...classification,
									primaryType: nextPurpose === "chat" ? classification.primaryType === "image" ? "text" : classification.primaryType : "image"
								};
								setPurposeFields(nextPurposeFields);
								setEndpointTypes(nextPurposeFields.endpointTypes);
								setClassification(nextClassification);
								autoSave({
									purposeFields: nextPurposeFields,
									classification: nextClassification
								});
							},
							onChatEndpointTypeChange: (nextEndpointType) => {
								const nextPurposeFields = applyModelPurpose(purposeFields, "chat", {
									previousPurpose: modelPurpose,
									chatEndpointType: nextEndpointType
								});
								setPurposeFields(nextPurposeFields);
								setEndpointTypes(nextPurposeFields.endpointTypes);
								autoSave({ purposeFields: nextPurposeFields });
							}
						})]
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderActions, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
					type: "button",
					variant: "ghost",
					className: drawerClasses.toggleButton,
					onClick: () => setShowMoreSettings((current) => !current),
					children: [t("settings.moresetting.label"), showMoreSettings ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronUp, { size: 16 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown, { size: 16 })]
				}) }),
				showMoreSettings && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSection, {
					className: drawerClasses.section,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						"data-ui": "settings.edit-model-drawer.provider-settings-model-more",
						"data-testid": "provider-settings-model-more-settings",
						className: "space-y-4",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: drawerClasses.sectionCard,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelClassificationControls, {
									value: classification,
									hasChanges: hasClassificationChanges,
									onPrimaryTypeChange: handlePrimaryTypeChange,
									onCapabilityToggle: handleToggleCapability,
									onInputModalityToggle: handleToggleInputModality,
									onReset: handleResetClassification
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: drawerClasses.sectionCard,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelContextWindowFields, {
									contextWindow,
									maxInputTokens,
									maxOutputTokens,
									onContextWindowChange: setContextWindow,
									onContextWindowBlur: () => autoSave({ contextWindow }),
									onMaxInputTokensChange: setMaxInputTokens,
									onMaxInputTokensBlur: () => autoSave({ maxInputTokens }),
									onMaxOutputTokensChange: setMaxOutputTokens,
									onMaxOutputTokensBlur: () => autoSave({ maxOutputTokens })
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: drawerClasses.switchCard,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
									className: "flex min-w-0 items-center justify-between gap-3",
									children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: "flex min-w-0 items-center gap-1.5",
										children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
											className: "truncate font-normal text-[13px] text-muted-foreground leading-5",
											children: t("settings.models.add.supported_text_delta.label")
										}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
											content: t("settings.models.add.supported_text_delta.tooltip"),
											children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
												className: "inline-flex h-5 w-4 shrink-0 items-center justify-center text-muted-foreground",
												children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleQuestionMark, {
													"aria-hidden": true,
													className: "size-3"
												})
											})
										})]
									}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Switch, {
										size: "sm",
										"aria-label": t("settings.models.add.supported_text_delta.label"),
										checked: supportsStreaming ?? false,
										onCheckedChange: (checked) => {
											setSupportsStreaming(checked);
											autoSave({ supportsStreaming: checked });
										}
									})]
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: drawerClasses.sectionCard,
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
										title: t("models.price.currency"),
										titleClassName: drawerClasses.fieldTitle,
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
											className: drawerClasses.inlineRow,
											children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Select, {
												value: currencySymbol,
												onValueChange: (nextValue) => {
													if (!isModelDrawerCurrencySymbol(nextValue)) return;
													setCurrencySymbol(nextValue);
													autoSave({ currencySymbol: nextValue });
												},
												children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectTrigger, {
													"aria-label": t("models.price.currency"),
													className: drawerClasses.selectTrigger,
													children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectValue, {})
												}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectContent, {
													className: drawerClasses.selectContent,
													children: MODEL_DRAWER_CURRENCY_SYMBOLS.map((symbol) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
														value: symbol,
														children: symbol
													}, symbol))
												})]
											})
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
										title: t("models.price.input"),
										titleClassName: drawerClasses.fieldTitle,
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
											className: drawerClasses.responsiveValueRow,
											children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
												type: "number",
												min: "0",
												step: "0.01",
												"aria-label": t("models.price.input"),
												value: inputPrice,
												placeholder: "0.00",
												className: drawerClasses.input,
												onChange: (event) => {
													setInputPrice(event.target.value);
												},
												onBlur: () => autoSave({ inputPrice })
											}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
												className: drawerClasses.valueSuffix,
												children: [
													currentCurrency,
													" / ",
													t("models.price.million_tokens")
												]
											})]
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
										title: t("models.price.output"),
										titleClassName: drawerClasses.fieldTitle,
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
											className: drawerClasses.responsiveValueRow,
											children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
												type: "number",
												min: "0",
												step: "0.01",
												"aria-label": t("models.price.output"),
												value: outputPrice,
												placeholder: "0.00",
												className: drawerClasses.input,
												onChange: (event) => {
													setOutputPrice(event.target.value);
												},
												onBlur: () => autoSave({ outputPrice })
											}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
												className: drawerClasses.valueSuffix,
												children: [
													currentCurrency,
													" / ",
													t("models.price.million_tokens")
												]
											})]
										})
									}),
									/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderField, {
										title: t("models.price.cache_read"),
										titleClassName: drawerClasses.fieldTitle,
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
											className: drawerClasses.responsiveValueRow,
											children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
												type: "number",
												min: "0",
												step: "0.01",
												"aria-label": t("models.price.cache_read"),
												value: cacheReadPrice,
												placeholder: "0.00",
												className: drawerClasses.input,
												onChange: (event) => {
													setCacheReadPrice(event.target.value);
												},
												onBlur: () => autoSave({ cacheReadPrice })
											}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
												className: drawerClasses.valueSuffix,
												children: [
													currentCurrency,
													" / ",
													t("models.price.million_tokens")
												]
											})]
										})
									})
								]
							})
						]
					})
				})
			]
		})
	});
}
MODEL_CAPABILITY.REASONING, MODEL_CAPABILITY.FUNCTION_CALL;
MODALITY.IMAGE, MODALITY.AUDIO, MODALITY.VIDEO;
var ProviderModelAdd = ({ providerId, disabled }) => {
	const { t } = useTranslation();
	const [drawerOpen, setDrawerOpen] = (0, import_react.useState)(false);
	const openDrawer = (0, import_react.useCallback)(() => {
		setDrawerOpen(true);
	}, []);
	const closeDrawer = (0, import_react.useCallback)(() => {
		setDrawerOpen(false);
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		type: "button",
		variant: "outline",
		size: "icon-sm",
		className: modelListClasses.addModelIconButton,
		disabled,
		"aria-label": t("settings.models.add.add_model"),
		onClick: openDrawer,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, { className: modelListClasses.toolbarDesignIcon })
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AddModelDrawer, {
		providerId,
		open: drawerOpen,
		prefill: null,
		onClose: closeDrawer
	})] });
};
var ProviderModelAdd_default = ProviderModelAdd;
var logger$2 = loggerService.withContext("OVMSClient");
var HUGGINGFACE_SOURCE_VALUE = "__huggingface__";
var OVMS_DOWNLOAD_TASK_LABEL_KEYS = {
	embeddings: "ovms.download.task.embeddings",
	image_generation: "ovms.download.task.image_generation",
	rerank: "ovms.download.task.rerank",
	text_generation: "ovms.download.task.text_generation"
};
var PRESET_MODELS = [
	{
		modelId: "OpenVINO/Qwen3-4B-int4-ov",
		modelName: "Qwen3-4B-int4-ov",
		modelSource: "https://www.modelscope.cn/models",
		task: "text_generation"
	},
	{
		modelId: "OpenVINO/Qwen3-8B-int4-ov",
		modelName: "Qwen3-8B-int4-ov",
		modelSource: "https://www.modelscope.cn/models",
		task: "text_generation"
	},
	{
		modelId: "OpenVINO/bge-base-en-v1.5-fp16-ov",
		modelName: "bge-base-en-v1.5-fp16-ov",
		modelSource: "https://www.modelscope.cn/models",
		task: "embeddings"
	},
	{
		modelId: "OpenVINO/bge-reranker-base-fp16-ov",
		modelName: "bge-reranker-base-fp16-ov",
		modelSource: "https://www.modelscope.cn/models",
		task: "rerank"
	},
	{
		modelId: "OpenVINO/DeepSeek-R1-Distill-Qwen-7B-int4-ov",
		modelName: "DeepSeek-R1-Distill-Qwen-7B-int4-ov",
		modelSource: "https://www.modelscope.cn/models",
		task: "text_generation"
	},
	{
		modelId: "OpenVINO/stable-diffusion-v1-5-int8-ov",
		modelName: "stable-diffusion-v1-5-int8-ov",
		modelSource: "https://www.modelscope.cn/models",
		task: "image_generation"
	},
	{
		modelId: "OpenVINO/FLUX.1-schnell-int4-ov",
		modelName: "FLUX.1-schnell-int4-ov",
		modelSource: "https://www.modelscope.cn/models",
		task: "image_generation"
	}
];
var PopupContainer = ({ title, resolve, open }) => {
	const [loading, setLoading] = (0, import_react.useState)(false);
	const [progress, setProgress] = (0, import_react.useState)(0);
	const [cancelled, setCancelled] = (0, import_react.useState)(false);
	const [formValues, setFormValues] = (0, import_react.useState)({
		modelId: "",
		modelName: "",
		modelSource: "https://www.modelscope.cn/models",
		task: "text_generation"
	});
	const [error, setError] = (0, import_react.useState)(null);
	const { t } = useTranslation();
	const { setIntervalTimer, clearIntervalTimer, setTimeoutTimer } = useTimer();
	const getPresetTooltipLabel = (model) => `${model.modelName} (${t(OVMS_DOWNLOAD_TASK_LABEL_KEYS[model.task])})`;
	const updateField = (field, value) => {
		setFormValues((current) => ({
			...current,
			[field]: value
		}));
	};
	const startFakeProgress = () => {
		setProgress(0);
		setIntervalTimer("progress", () => {
			setProgress((prev) => {
				if (prev >= 95) return prev;
				const increment = prev < 30 ? Math.random() * 1 + .25 : prev < 60 ? Math.random() * .5 + .125 : Math.random() * .25 + .03125;
				return Math.min(prev + increment, 95);
			});
		}, 500);
	};
	const stopFakeProgress = (complete = false) => {
		clearIntervalTimer("progress");
		if (complete) {
			setProgress(100);
			setTimeoutTimer("progress-reset", () => setProgress(0), 1500);
		} else setProgress(0);
	};
	const handlePresetSelect = (value) => {
		const selectedPreset = PRESET_MODELS.find((model) => model.modelId === value);
		if (selectedPreset) setFormValues({
			modelId: selectedPreset.modelId,
			modelName: selectedPreset.modelName,
			modelSource: selectedPreset.modelSource,
			task: selectedPreset.task
		});
	};
	const handleModelIdChange = (value) => {
		if (value) {
			const lastSlashIndex = value.lastIndexOf("/");
			if (lastSlashIndex !== -1 && lastSlashIndex < value.length - 1) updateField("modelName", value.substring(lastSlashIndex + 1));
		}
	};
	const onCancel = async () => {
		if (loading) {
			try {
				setCancelled(true);
				logger$2.info("Stopping download...");
				await ipcApi.request("ovms.cancel_add_model");
				stopFakeProgress(false);
				setLoading(false);
			} catch (error$1) {
				logger$2.error(`Failed to stop download: ${error$1}`);
			}
			return;
		}
		resolve({});
	};
	const onFinish = async () => {
		const values = formValues;
		if (!values.modelId) {
			setError(t("ovms.download.model_id.required"));
			return;
		}
		if (!/^OpenVINO\/.+/.test(values.modelId)) {
			setError(t("ovms.download.model_id.model_id_pattern"));
			return;
		}
		if (!values.modelName) {
			setError(t("ovms.download.model_name.required"));
			return;
		}
		setError(null);
		setLoading(true);
		setCancelled(false);
		startFakeProgress();
		try {
			const { modelName, modelId, modelSource, task } = values;
			const normalizedModelSource = modelSource === HUGGINGFACE_SOURCE_VALUE ? "" : modelSource;
			logger$2.info(`🔄 Downloading model: ${modelName} with ID: ${modelId}, source: ${normalizedModelSource}, task: ${task}`);
			const result = await ipcApi.request("ovms.add_model", {
				modelName,
				modelId,
				modelSource: normalizedModelSource,
				task
			});
			if (result.success) {
				stopFakeProgress(true);
				toast.success(t("ovms.download.success_desc", {
					modelName,
					modelId
				}));
				resolve({});
			} else {
				stopFakeProgress(false);
				logger$2.error(`Download failed, is it cancelled? ${cancelled}`);
				if (!cancelled) setError(result.message ?? null);
			}
		} catch (error$1) {
			stopFakeProgress(false);
			logger$2.error(`Download crashed, is it cancelled? ${cancelled}`);
			if (!cancelled) setError(error$1.message);
		} finally {
			setLoading(false);
		}
	};
	const footer = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.popup",
		className: drawerClasses.footer,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			variant: loading ? "default" : "outline",
			type: "button",
			onClick: () => void onCancel(),
			children: loading ? t("common.cancel") : t("common.cancel")
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			disabled: loading,
			onClick: () => void onFinish(),
			children: t("ovms.download.button")
		})]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsDrawer, {
		title,
		open,
		onClose: () => void onCancel(),
		showHeaderCloseButton: !loading,
		footer,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: drawerClasses.fieldList,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "space-y-2",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
							className: "text-[13px] text-foreground",
							children: t("ovms.download.model_id.label")
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
							className: drawerClasses.input,
							value: formValues.modelId,
							onChange: (event) => {
								updateField("modelId", event.target.value);
								handleModelIdChange(event.target.value);
							},
							placeholder: t("ovms.download.model_id.placeholder"),
							disabled: loading
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "flex flex-wrap gap-1.5",
							children: PRESET_MODELS.map((model) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
								content: getPresetTooltipLabel(model),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
									type: "button",
									variant: "outline",
									className: "h-auto rounded-full px-2 py-1 text-[11px]",
									disabled: loading,
									onClick: () => handlePresetSelect(model.modelId),
									children: model.modelName
								})
							}, model.modelId))
						})
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
						className: "text-[13px] text-foreground",
						children: t("ovms.download.model_name.label")
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
						className: drawerClasses.input,
						value: formValues.modelName,
						onChange: (event) => updateField("modelName", event.target.value),
						placeholder: t("ovms.download.model_name.placeholder"),
						spellCheck: false,
						maxLength: 200,
						disabled: loading
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
						className: "text-[13px] text-foreground",
						children: t("ovms.download.model_source")
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Select, {
						value: formValues.modelSource,
						onValueChange: (value) => updateField("modelSource", value),
						disabled: loading,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectTrigger, {
							className: drawerClasses.selectTrigger,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectValue, {})
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(SelectContent, {
							className: drawerClasses.selectContent,
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
									value: HUGGINGFACE_SOURCE_VALUE,
									children: "HuggingFace"
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
									value: "https://hf-mirror.com",
									children: "HF-Mirror"
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
									value: "https://www.modelscope.cn/models",
									children: "ModelScope"
								})
							]
						})]
					})]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
						className: "text-[13px] text-foreground",
						children: t("ovms.download.model_task")
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Select, {
						value: formValues.task,
						onValueChange: (value) => updateField("task", value),
						disabled: loading,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectTrigger, {
							className: drawerClasses.selectTrigger,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectValue, {})
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(SelectContent, {
							className: drawerClasses.selectContent,
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
									value: "text_generation",
									children: t("ovms.download.task.text_generation")
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
									value: "embeddings",
									children: t("ovms.download.task.embeddings")
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
									value: "rerank",
									children: t("ovms.download.task.rerank")
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
									value: "image_generation",
									children: t("ovms.download.task.image_generation")
								})
							]
						})]
					})]
				}),
				loading && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: drawerClasses.healthProgressTrack,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: drawerClasses.healthProgressFill,
							style: { width: `${Math.round(progress)}%` }
						})
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "text-center text-foreground-tertiary text-sm",
						children: [
							Math.round(progress),
							"% · ",
							t("ovms.download.tip")
						]
					})]
				}),
				error ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: drawerClasses.errorText,
					children: error
				}) : null
			]
		})
	});
};
var DownloadOvmsModelPopup_default = createPopup(PopupContainer, { dismissResult: {} });
function useOvmsModelDownloadAction(providerId) {
	const { t } = useTranslation();
	const { provider } = useProvider(providerId);
	return { openOvmsModelDownload: (0, import_react.useCallback)(() => {
		if (provider) DownloadOvmsModelPopup_default.show({
			title: t("ovms.download.title"),
			provider
		});
	}, [provider, t]) };
}
var ProviderModelDownload = ({ providerId, disabled }) => {
	const { t } = useTranslation();
	const { openOvmsModelDownload } = useOvmsModelDownloadAction(providerId);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		type: "button",
		variant: "outline",
		size: "icon-sm",
		className: modelListClasses.addModelIconButton,
		disabled,
		"aria-label": t("button.download"),
		onClick: openOvmsModelDownload,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, { className: modelListClasses.toolbarDesignIcon })
	});
};
var ProviderModelDownload_default = ProviderModelDownload;
function HealthCheckModelAvatar({ model }) {
	const Icon = useIcon(getModelLogoRef(model));
	return Icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon.Avatar, { size: 22 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Avatar, {
		className: "size-5.5 shrink-0 rounded-md text-[10px]",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AvatarFallback, {
			className: "rounded-md",
			children: model.name?.[0]?.toUpperCase()
		})
	});
}
function HealthCheckDrawer({ open, title, apiKeys, isChecking, modelStatuses, onClose, onResetRun, onStart }) {
	const { t } = useTranslation();
	const [selectedKeyIndex, setSelectedKeyIndex] = (0, import_react.useState)(0);
	const [keyCheckMode, setKeyCheckMode] = (0, import_react.useState)("all");
	const [isConcurrent, setIsConcurrent] = (0, import_react.useState)(true);
	const [timeoutSeconds, setTimeoutSeconds] = (0, import_react.useState)(15);
	const [isStarting, setIsStarting] = (0, import_react.useState)(false);
	const showPipeline = modelStatuses.length > 0;
	const progressStats = (0, import_react.useMemo)(() => {
		if (modelStatuses.length === 0) return null;
		const total = modelStatuses.length;
		const done = modelStatuses.filter((s) => !s.checking).length;
		return {
			done,
			total,
			pct: total > 0 ? Math.min(100, Math.round(done / total * 100)) : 0
		};
	}, [modelStatuses]);
	const successCount = (0, import_react.useMemo)(() => modelStatuses.filter((s) => s.status === HealthStatus.SUCCESS).length, [modelStatuses]);
	const failCount = (0, import_react.useMemo)(() => modelStatuses.filter((s) => s.status === HealthStatus.FAILED).length, [modelStatuses]);
	const skippedCount = (0, import_react.useMemo)(() => modelStatuses.filter((s) => s.kind === "skipped").length, [modelStatuses]);
	const getGenerationOutputText = (output) => {
		switch (output) {
			case "image": return t("settings.models.check.generation_output_image");
			case "video": return t("settings.models.check.generation_output_video");
			case "audio": return t("settings.models.check.generation_output_audio");
		}
	};
	const getSkipReasonText = (reason) => {
		switch (reason.kind) {
			case "generation_cost": return t("settings.models.check.skip_reason_generation_cost", { output: getGenerationOutputText(reason.output) });
			case "unsupported_probe": return t("settings.models.check.skip_reason_unsupported_probe");
		}
	};
	(0, import_react.useEffect)(() => {
		if (!open) return;
		setSelectedKeyIndex(0);
		setKeyCheckMode("all");
		setIsConcurrent(true);
		setTimeoutSeconds(15);
	}, [open]);
	const hasMultipleKeys = apiKeys.length > 1;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ProviderSettingsDrawer, {
		open,
		onClose,
		title,
		footer: !showPipeline ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			"data-ui": "settings.health-check-drawer",
			className: drawerClasses.footer,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				variant: "outline",
				onClick: onClose,
				children: t("common.cancel")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				loading: isStarting,
				onClick: async () => {
					setIsStarting(true);
					try {
						await onStart({
							apiKeys: keyCheckMode === "single" ? apiKeys[selectedKeyIndex] ? [apiKeys[selectedKeyIndex]] : [] : apiKeys,
							isConcurrent,
							timeout: timeoutSeconds * 1e3
						});
					} finally {
						setIsStarting(false);
					}
				},
				children: t("settings.models.check.start")
			})]
		}) : isChecking ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "settings.health-check-drawer",
			className: drawerClasses.footer,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				variant: "outline",
				onClick: onClose,
				children: t("common.cancel")
			})
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			"data-ui": "settings.health-check-drawer",
			className: drawerClasses.footer,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				variant: "outline",
				onClick: onClose,
				children: t("common.close")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				variant: "default",
				onClick: () => {
					onResetRun();
				},
				children: t("settings.models.check.retry")
			})]
		}),
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Alert, {
				type: "warning",
				showIcon: true,
				role: "alert",
				message: t("settings.models.check.disclaimer"),
				className: drawerClasses.healthCostWarning
			}),
			showPipeline && progressStats ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "flex min-h-0 flex-1 flex-col gap-0",
				children: [
					isChecking ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "px-4 pt-3 pb-2",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "mb-2 flex items-center justify-between gap-3",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "text-[13px] text-foreground",
								children: t("settings.models.check.pipeline_heading")
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: drawerClasses.healthProgressMeta,
								children: t("settings.models.check.progress_count", {
									done: progressStats.done,
									total: progressStats.total
								})
							})]
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							"data-ui": "settings.health-check-drawer.progressbar",
							className: drawerClasses.healthProgressTrack,
							role: "progressbar",
							"aria-valuenow": progressStats.pct,
							"aria-valuemin": 0,
							"aria-valuemax": 100,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: drawerClasses.healthProgressFill,
								style: { width: `${progressStats.pct}%` }
							})
						})]
					}) : null,
					!isChecking && showPipeline ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "mx-4 mt-3 mb-2 flex flex-wrap items-center gap-x-3 gap-y-2 rounded-xl border border-border-subtle bg-muted/50 px-3.5 py-2.5",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: "flex shrink-0 items-center gap-1.5",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: "flex size-3.5 items-center justify-center rounded-full bg-muted",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleCheck, {
										size: 9,
										className: "text-foreground-tertiary"
									})
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: "text-foreground-tertiary text-xs",
									children: t("settings.models.check.outcome_success_short", { count: successCount })
								})]
							}),
							failCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: "flex shrink-0 items-center gap-1.5",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: "flex size-3.5 items-center justify-center rounded-full bg-error-subtle",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleX, {
										size: 9,
										className: "text-error"
									})
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: "text-error text-xs",
									children: t("settings.models.check.outcome_fail_short", { count: failCount })
								})]
							}) : null,
							skippedCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: "flex shrink-0 items-center gap-1.5",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: "flex size-3.5 items-center justify-center rounded-full bg-muted",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Info, {
										size: 9,
										className: "text-foreground-tertiary"
									})
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: "text-foreground-tertiary text-xs",
									children: t("settings.models.check.outcome_skipped_short", { count: skippedCount })
								})]
							}) : null,
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "ml-auto shrink-0 text-foreground-tertiary text-xs",
								children: t("settings.models.check.outcome_total", { count: modelStatuses.length })
							})
						]
					}) : null,
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Scrollbar_default, {
						className: "min-h-0 flex-1 px-2 pb-0",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("ul", {
							className: "divide-y divide-border-subtle pt-1 pb-0",
							children: modelStatuses.map((row) => {
								const { model, checking, status, latency, error } = row;
								const pending = !checking && status === HealthStatus.NOT_CHECKED;
								let statusCell;
								let rightCell;
								if (row.kind === "skipped") {
									const skipReasonText = getSkipReasonText(row.skipReason);
									statusCell = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Info, {
										className: "size-4 shrink-0 text-foreground-tertiary",
										"aria-hidden": true
									});
									rightCell = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
										content: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
											className: "block max-w-full whitespace-pre-wrap text-left text-[12px] leading-snug",
											children: skipReasonText
										}),
										placement: "top",
										classNames: { placeholder: "block min-w-0 w-full max-w-full overflow-hidden" },
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
											className: "block w-full min-w-0 cursor-default truncate text-end text-[12px] text-foreground-tertiary",
											children: t("settings.models.check.status_skipped")
										})
									});
								} else if (checking) {
									statusCell = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoaderCircle, {
										className: "size-4 shrink-0 animate-spin text-warning",
										"aria-hidden": true
									});
									rightCell = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
										className: "shrink-0 font-medium text-[12px] text-warning",
										children: t("settings.models.check.status_checking")
									});
								} else if (pending) {
									statusCell = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
										className: "mx-auto block size-1.5 shrink-0 rounded-full bg-muted-foreground/35",
										"aria-hidden": true
									});
									rightCell = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "shrink-0 text-[12px] text-foreground-tertiary" });
								} else if (status === HealthStatus.SUCCESS) {
									statusCell = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleCheck, {
										className: "size-4 shrink-0 text-foreground-tertiary",
										"aria-hidden": true
									});
									rightCell = latency != null ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
										className: "shrink-0 text-[12px] text-foreground-tertiary tabular-nums",
										children: [Math.round(latency), "ms"]
									}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
										className: "shrink-0 text-[12px] text-foreground-tertiary",
										children: t("settings.models.check.passed")
									});
								} else {
									statusCell = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleX, {
										className: "size-4 shrink-0 text-error",
										"aria-hidden": true
									});
									const errText = healthCheckErrorToDisplayString(error);
									rightCell = errText !== "" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
										content: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
											className: "block max-w-full whitespace-pre-wrap break-all text-left text-[12px] leading-snug",
											children: errText
										}),
										placement: "top",
										classNames: { placeholder: "block min-w-0 w-full max-w-full overflow-hidden" },
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
											className: "block w-full min-w-0 cursor-default truncate text-end text-[12px] text-error",
											children: errText
										})
									}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
										className: "shrink-0 text-[12px] text-error",
										children: t("settings.models.check.failed")
									});
								}
								return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", {
									className: cn("flex min-h-11 min-w-0 items-center gap-3 rounded-lg px-2 py-2.5", status === HealthStatus.FAILED ? "bg-error-subtle" : ""),
									children: [
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
											className: "flex w-5 shrink-0 justify-center",
											children: statusCell
										}),
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)(HealthCheckModelAvatar, { model }),
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
											className: "min-w-0 flex-1 truncate font-mono text-[13px] text-foreground",
											children: model.name
										}),
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
											className: cn("min-w-0 text-end", status === HealthStatus.FAILED ? "max-w-[min(52%,15rem)] flex-1 basis-0" : "max-w-[min(42%,14rem)] shrink-0"),
											children: rightCell
										})
									]
								}, model.id);
							})
						})
					})
				]
			}) : null,
			!showPipeline ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "space-y-4",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex items-center justify-between gap-3",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "text-foreground text-sm",
							children: t("settings.models.check.use_all_keys")
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SegmentedControl, {
							size: "sm",
							value: keyCheckMode,
							onValueChange: (value) => setKeyCheckMode(value),
							options: [{
								value: "single",
								label: t("settings.models.check.single")
							}, {
								value: "all",
								label: t("settings.models.check.all")
							}]
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex items-center justify-between gap-3",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "text-foreground text-sm",
							children: t("settings.models.check.enable_concurrent")
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SegmentedControl, {
							size: "sm",
							value: isConcurrent ? "on" : "off",
							onValueChange: (value) => setIsConcurrent(value === "on"),
							options: [{
								value: "off",
								label: t("settings.models.check.disabled")
							}, {
								value: "on",
								label: t("settings.models.check.enabled")
							}]
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "flex items-center justify-between gap-3",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "text-foreground text-sm",
							children: t("settings.models.check.timeout")
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "flex w-28 items-center gap-2",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
								type: "number",
								min: 5,
								max: 60,
								value: String(timeoutSeconds),
								onChange: (event) => setTimeoutSeconds(Math.min(60, Math.max(5, Number(event.target.value) || 15)))
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "text-foreground-tertiary text-xs",
								children: "s"
							})]
						})]
					})
				]
			}), keyCheckMode === "single" && hasMultipleKeys ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "space-y-3 rounded-xl border border-border-subtle bg-muted/20 p-4",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "text-[13px] text-foreground",
					children: t("settings.models.check.select_api_key")
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroup, {
					value: String(selectedKeyIndex),
					onValueChange: (value) => setSelectedKeyIndex(Number(value)),
					children: apiKeys.map((key, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", {
						className: "flex cursor-pointer items-center gap-3 rounded-lg border border-transparent px-2 py-1.5 hover:bg-accent/30",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroupItem, {
							value: String(index),
							size: "sm"
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "truncate font-mono text-[12px] text-muted-foreground",
							children: maskApiKey(key)
						})]
					}, `${key}-${index}`))
				})]
			}) : null] }) : null
		]
	});
}
var ProviderModelHealthCheck = ({ disabled, hasVisibleModels, renderTrigger = true, renderDrawer = true }) => {
	const { t } = useTranslation();
	const health = useModelListHealth();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [renderTrigger ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
		content: t("settings.models.check.button_caption"),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			type: "button",
			variant: "ghost",
			size: "icon-sm",
			"aria-label": t("settings.models.check.button_caption"),
			className: modelListClasses.subsectionIconButton,
			disabled: !hasVisibleModels || disabled,
			onClick: health.openHealthCheck,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Activity, { className: modelListClasses.subsectionIcon })
		})
	}) : null, renderDrawer ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HealthCheckDrawer, {
		open: health.healthCheckOpen,
		title: t("settings.models.check.title"),
		apiKeys: health.availableApiKeys,
		isChecking: health.isHealthChecking,
		modelStatuses: health.modelStatuses,
		onClose: health.closeHealthCheck,
		onResetRun: health.resetHealthCheckRun,
		onStart: health.startHealthCheck
	}) : null] });
};
var ProviderModelHealthCheck_default = ProviderModelHealthCheck;
var MODEL_TYPE_FILTERS = [
	"text",
	"image",
	"embedding",
	"audio",
	"video",
	"rerank",
	"speech",
	"transcription"
];
var CAPABILITY_FILTER_LABEL_KEYS = {
	text: "models.type.text",
	image: "models.type.image",
	embedding: "models.type.embedding",
	audio: "models.type.audio",
	video: "models.type.video",
	rerank: "models.type.rerank",
	speech: "models.type.speech",
	transcription: "models.type.transcription"
};
var CAPABILITY_FILTER_ICONS = {
	text: Type,
	image: Image,
	embedding: Boxes,
	audio: AudioLines,
	video: Video,
	rerank: ArrowUpDown,
	speech: Speech,
	transcription: Mic
};
function ModelTypeFilterTabs({ value, onValueChange, counts, extraTabs = [], className, listClassName }) {
	const { t } = useTranslation();
	const [optimisticValue, setOptimisticValue] = (0, import_react.useState)(value);
	const [, startFilterTransition] = (0, import_react.useTransition)();
	(0, import_react.useEffect)(() => {
		setOptimisticValue(value);
	}, [value]);
	const handleValueChange = (next) => {
		setOptimisticValue(next);
		startFilterTransition(() => onValueChange(next));
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tabs, {
		value: optimisticValue,
		onValueChange: handleValueChange,
		className: cn(modelSyncClasses.manageTabs, className),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(TabsList, {
			className: cn(modelSyncClasses.manageTabsList, listClassName),
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(TabsTrigger, {
					value: "all",
					className: modelSyncClasses.manageTabsTrigger,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "truncate",
						children: t("models.all")
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: modelSyncClasses.manageTabCount,
						"aria-hidden": true,
						children: counts.all
					})]
				}),
				extraTabs.map((tab) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(TabsTrigger, {
					value: tab.value,
					className: cn(modelSyncClasses.manageTabsTrigger, tab.destructive && modelSyncClasses.manageTabsTriggerDestructive),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "truncate",
						children: tab.label
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: cn(modelSyncClasses.manageTabCount, tab.destructive && "text-error-subtle-foreground"),
						"aria-hidden": true,
						children: tab.count
					})]
				}, tab.value)),
				MODEL_TYPE_FILTERS.map((filter) => {
					const Icon = CAPABILITY_FILTER_ICONS[filter];
					return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(TabsTrigger, {
						value: filter,
						className: modelSyncClasses.manageTabsTrigger,
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
								className: "size-3.5 shrink-0",
								"aria-hidden": true
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: "truncate",
								children: t(CAPABILITY_FILTER_LABEL_KEYS[filter])
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: modelSyncClasses.manageTabCount,
								"aria-hidden": true,
								children: counts[filter]
							})
						]
					}, filter);
				})
			]
		})
	});
}
var ModelListHeader = ({ isBusy, hasNoModels, searchText, setSearchText, selectedTypeFilter, setSelectedTypeFilter, typeCounts, groupsExpanded, onToggleGroupsExpanded, docsWebsite, modelsWebsite, actions }) => {
	const { t } = useTranslation();
	const docsLink = modelsWebsite || docsWebsite;
	const [searchOpen, setSearchOpen] = (0, import_react.useState)(false);
	const [filterOpen, setFilterOpen] = (0, import_react.useState)(false);
	const searchInputRef = (0, import_react.useRef)(null);
	const isSearchExpanded = searchOpen || Boolean(searchText);
	const isFilterActive = selectedTypeFilter !== "all";
	const GroupExpansionIcon = groupsExpanded ? ChevronsDownUp : ChevronsUpDown;
	(0, import_react.useEffect)(() => {
		if (isSearchExpanded) searchInputRef.current?.focus();
	}, [isSearchExpanded]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.model-list-header",
		className: modelListClasses.headerInlineRow,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: modelListClasses.sectionTitleLine,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", {
					className: modelListClasses.sectionTitle,
					children: t("settings.models.list_title")
				}),
				docsLink ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: modelListClasses.titleHelpRow,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						content: t("settings.models.docs"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
							target: "_blank",
							rel: "noreferrer",
							href: docsLink,
							"aria-label": t("settings.models.docs"),
							className: modelListClasses.searchIconButton,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FileText, {
								className: modelListClasses.toolbarHeaderIcon,
								"aria-hidden": true
							})
						})
					})
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t(groupsExpanded ? "settings.models.collapse_all" : "settings.models.expand_all"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						"data-ui": "settings.model-list-header.action.toggle",
						type: "button",
						className: modelListClasses.groupToggleIconButton,
						"aria-label": t(groupsExpanded ? "settings.models.collapse_all" : "settings.models.expand_all"),
						disabled: isBusy || hasNoModels,
						onClick: onToggleGroupsExpanded,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GroupExpansionIcon, { className: modelListClasses.toolbarHeaderIcon })
					})
				}),
				isSearchExpanded ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: modelListClasses.searchCompactWrap,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Search, { className: modelListClasses.searchIcon }),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
							ref: searchInputRef,
							type: "text",
							value: searchText,
							placeholder: t("models.search.placeholder"),
							disabled: isBusy,
							onChange: (event) => setSearchText(event.target.value),
							onFocus: () => setSearchOpen(true),
							onBlur: () => {
								if (!searchText) setSearchOpen(false);
							},
							className: modelListClasses.searchInput
						}),
						searchText ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
							type: "button",
							onClick: () => {
								setSearchText("");
								setSearchOpen(false);
							},
							className: modelListClasses.searchClear,
							"aria-label": t("common.clear"),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { size: 9 })
						}) : null
					]
				}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("common.search"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						type: "button",
						className: modelListClasses.searchIconButton,
						"aria-label": t("common.search"),
						disabled: isBusy,
						onClick: () => setSearchOpen(true),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Search, { className: modelListClasses.toolbarHeaderIcon })
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("settings.models.filter.label"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						type: "button",
						className: cn(modelListClasses.searchIconButton, (filterOpen || isFilterActive) && "bg-accent/40 text-accent-foreground"),
						"aria-label": t("settings.models.filter.label"),
						"aria-pressed": filterOpen,
						disabled: isBusy || hasNoModels,
						onClick: () => setFilterOpen((open) => !open),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Funnel, { className: modelListClasses.toolbarHeaderIcon })
					})
				})
			]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: modelListClasses.headerInlineActions,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: modelListClasses.titleActions,
				children: actions
			})
		})]
	}), filterOpen ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelTypeFilterTabs, {
		value: selectedTypeFilter,
		onValueChange: (next) => setSelectedTypeFilter(next),
		counts: typeCounts
	}) : null] });
};
var ModelListHeader_default = ModelListHeader;
var MODEL_IN_USE_BY_KNOWLEDGE_BASE_REASON = "model is in use by a knowledge base";
var MODEL_IN_USE_AS_DEFAULT_REASON = "model is in use as the default model";
function getInvalidOperationReason(details) {
	if (typeof details !== "object" || details === null || !("reason" in details)) return;
	const reason = details.reason;
	return typeof reason === "string" ? reason : void 0;
}
function getInvalidOperationOperation(details) {
	if (typeof details !== "object" || details === null || !("operation" in details)) return;
	const operation = details.operation;
	return typeof operation === "string" ? operation : void 0;
}
function getModelInUseAsDefaultUniqueModelId(error) {
	if (!isDataApiError(error) && !isSerializedDataApiError(error)) return;
	const dataError = toDataApiError(error);
	if (dataError.code !== ErrorCode.INVALID_OPERATION || getInvalidOperationReason(dataError.details) !== MODEL_IN_USE_AS_DEFAULT_REASON) return;
	const operation = getInvalidOperationOperation(dataError.details);
	const modelPath = operation?.startsWith("delete model ") ? operation.slice(13) : void 0;
	const separatorIndex = modelPath?.indexOf("/");
	if (!modelPath || separatorIndex === void 0 || separatorIndex <= 0 || separatorIndex === modelPath.length - 1) return;
	return createUniqueModelId(modelPath.slice(0, separatorIndex), modelPath.slice(separatorIndex + 1));
}
function getModelOperationErrorMessage(error, messages) {
	if (isDataApiError(error) || isSerializedDataApiError(error)) {
		const dataError = toDataApiError(error);
		if (dataError.code === ErrorCode.INVALID_OPERATION && getInvalidOperationReason(dataError.details) === MODEL_IN_USE_BY_KNOWLEDGE_BASE_REASON) return messages.modelInUseByKnowledgeBase;
		if (dataError.code === ErrorCode.INVALID_OPERATION && getInvalidOperationReason(dataError.details) === MODEL_IN_USE_AS_DEFAULT_REASON) return messages.modelInUseAsDefault;
		if (dataError.code === ErrorCode.INVALID_OPERATION || dataError.code === ErrorCode.CONFLICT || dataError.code === ErrorCode.NOT_FOUND) return dataError.message;
	}
	return messages.fallback;
}
const UNGROUPED_MODEL_GROUP_KEY = "__ungrouped__";
function normalizeModelGroupName(group, fallback) {
	const normalizedGroup = group?.trim();
	if (normalizedGroup && normalizedGroup.toLowerCase() !== "undefined") return normalizedGroup;
	const normalizedFallback = fallback?.trim();
	if (normalizedFallback && normalizedFallback.toLowerCase() !== "undefined") return normalizedFallback;
	return UNGROUPED_MODEL_GROUP_KEY;
}
function getModelGroupLabel(groupName, t) {
	return groupName === "__ungrouped__" ? t("models.group.ungrouped") : groupName;
}
var logger$1 = loggerService.withContext("ModelListGroup");
var ModelListGroup = ({ groupName, items, defaultOpen, open = defaultOpen, disabled, bulkActionDisabled, pendingModelIds, defaultModelIds = /* @__PURE__ */ new Set(), onDeleteModels, onToggleOpen }) => {
	const { t } = useTranslation();
	const groupLabel = getModelGroupLabel(groupName, t);
	const groupModels$1 = (0, import_react.useMemo)(() => items.map(({ model }) => model), [items]);
	const deletableGroupModels = (0, import_react.useMemo)(() => groupModels$1.filter((model) => !defaultModelIds.has(model.id)), [defaultModelIds, groupModels$1]);
	const hasPendingModel = groupModels$1.some((model) => pendingModelIds.has(model.id));
	const toggleOpen = (0, import_react.useCallback)(() => {
		onToggleOpen?.();
	}, [onToggleOpen]);
	const handleGroupHeaderKeyDown = (0, import_react.useCallback)((event) => {
		if (event.key !== "Enter" && event.key !== " ") return;
		event.preventDefault();
		toggleOpen();
	}, [toggleOpen]);
	const handleDeleteGroupModels = (0, import_react.useCallback)((event) => {
		event?.stopPropagation();
		onDeleteModels(deletableGroupModels).catch((error) => {
			logger$1.error("Failed to delete provider model group", {
				groupName,
				error
			});
			toast.error(getModelOperationErrorMessage(error, {
				fallback: t("settings.models.manage.operation_failed"),
				modelInUseByKnowledgeBase: t("settings.models.manage.model_in_use_by_knowledge_base"),
				modelInUseAsDefault: t("settings.models.manage.sync_apply_default_in_use")
			}));
		});
	}, [
		deletableGroupModels,
		groupName,
		onDeleteModels,
		t
	]);
	const handleDeleteGroupKeyDown = (0, import_react.useCallback)((event) => {
		if (event.key === "Enter" || event.key === " ") event.stopPropagation();
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.model-list-group",
		className: cn(modelListClasses.groupCard, open && modelListClasses.groupCardOpen),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			"data-ui": "settings.model-list-group.action.toggle",
			className: cn(modelListClasses.groupHeader, open && modelListClasses.groupHeaderOpen, "cursor-pointer"),
			role: "button",
			tabIndex: 0,
			"aria-expanded": open,
			onClick: toggleOpen,
			onKeyDown: handleGroupHeaderKeyDown,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex min-w-0 flex-1 items-center gap-1",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: modelListClasses.groupToggleButton,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRight, {
						className: cn(modelListClasses.groupChevron, open && modelListClasses.groupChevronOpen),
						"aria-hidden": true
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: modelListClasses.groupTitle,
						children: groupLabel
					})]
				})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: modelListClasses.groupHeaderActions,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: deletableGroupModels.length === 0 ? t("settings.models.manage.default_model_cannot_remove") : t("settings.models.manage.remove_whole_group"),
					placement: "top",
					classNames: { placeholder: modelListClasses.groupHeaderIconTooltipTrigger },
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						variant: "ghost",
						size: "icon-sm",
						"aria-label": t("settings.models.manage.remove_whole_group"),
						disabled: disabled || bulkActionDisabled || hasPendingModel || deletableGroupModels.length === 0,
						className: `${modelListClasses.rowActionButton} ${modelListClasses.rowDangerActionButton} opacity-0 transition-opacity focus-visible:opacity-100 group-focus-within/modelGroup:opacity-100 group-hover/modelGroup:opacity-100`,
						onKeyDown: handleDeleteGroupKeyDown,
						onClick: handleDeleteGroupModels,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Minus, { className: "size-3.5" })
					})
				})
			})]
		})
	});
};
var ModelListGroup_default = (0, import_react.memo)(ModelListGroup);
const FreeTrialModelTag = ({ modelId, providerId, showLabel = true }) => {
	const { t } = useTranslation();
	if (providerId !== "cherryai") return null;
	const rawId = modelId.includes("::") ? modelId.slice(modelId.indexOf("::") + 2) : modelId;
	const linkedProviderId = ["Qwen/Qwen3-8B", "Qwen/Qwen3-Next-80B-A3B-Instruct"].includes(rawId) ? "cherryin" : "";
	if (!linkedProviderId) return null;
	const onSelectProvider = () => {
		openSettingsTab(`/settings/provider?id=${linkedProviderId}`);
	};
	const onNavigateProvider = (e) => {
		e.stopPropagation();
		openSettingsTab(`/settings/provider?id=${linkedProviderId}`);
	};
	if (!showLabel) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.free-trial-model-tag",
		className: "flex flex-row items-center gap-1",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(CustomTag_default, {
			color: "var(--primary)",
			size: 11,
			onClick: onNavigateProvider,
			style: {
				display: "flex",
				alignItems: "center",
				gap: 2
			},
			children: [t(getProviderLabelKey(linkedProviderId)), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrowUpRight, { size: 12 })]
		})
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.free-trial-model-tag",
		className: "flex flex-row items-center gap-1",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(IndicatorLight_default, {
				size: 6,
				color: "var(--primary)",
				animation: false,
				shadow: false
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "text-foreground-tertiary text-xs",
				children: t("common.powered_by")
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				"data-ui": "settings.free-trial-model-tag.action.select",
				type: "button",
				className: "text-link text-xs hover:underline",
				onClick: onSelectProvider,
				children: t(getProviderLabelKey(linkedProviderId))
			})
		]
	});
};
var ModelTagsWithLabel = ({ model, provider, showFree = true, showReasoning = true, showToolsCalling = true, size = 8, showTooltip = true, style }) => {
	const tagProps = {
		size,
		showTooltip,
		showLabel: false
	};
	const tags = getModelDisplayTags(model, {
		showFree,
		showReasoning,
		showToolsCalling
	}, provider);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.model-tags-with-label",
		className: "flex min-w-0 max-w-full flex-row flex-wrap items-center gap-0.5 overflow-visible",
		style,
		children: tags.map((tag) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: "inline-flex",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelTag, {
				tag,
				...tagProps
			})
		}, tag))
	});
};
var ModelTagsWithLabel_default = (0, import_react.memo)(ModelTagsWithLabel);
var ModelListItem = ({ ref, model, provider, disabled, isDefaultModel, onEdit, onDelete }) => {
	const { t } = useTranslation();
	const Icon = useIcon(getModelLogoRef(model));
	const deleteTooltip = isDefaultModel ? t("settings.models.manage.default_model_cannot_remove") : t("settings.models.manage.remove_model");
	const handleEdit = (0, import_react.useCallback)(() => {
		onEdit(model);
	}, [model, onEdit]);
	const handleDelete = (0, import_react.useCallback)(() => {
		onDelete(model).catch((error) => {
			toast.error(getModelOperationErrorMessage(error, {
				fallback: t("settings.models.manage.operation_failed"),
				modelInUseByKnowledgeBase: t("settings.models.manage.model_in_use_by_knowledge_base"),
				modelInUseAsDefault: t("settings.models.manage.sync_apply_default_in_use")
			}));
		});
	}, [
		model,
		onDelete,
		t
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.model-list-item",
		ref,
		className: modelListClasses.row,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RowFlex, {
			className: modelListClasses.rowMain,
			children: [(() => {
				return Icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: modelListClasses.rowAvatar,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon.Avatar, {
						size: 26,
						shape: "circle"
					})
				}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Avatar, {
					className: modelListClasses.rowAvatar,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AvatarFallback, {
						className: "rounded-[inherit]",
						children: model.name?.[0]?.toUpperCase()
					})
				});
			})(), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: modelListClasses.rowBody,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: "flex h-7 min-w-0 items-center gap-1.5",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "inline-flex h-7 min-w-0 shrink select-text items-center overflow-hidden text-ellipsis whitespace-nowrap text-left font-normal text-foreground text-sm leading-none",
						children: model.name
					})
				})
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RowFlex, {
			className: modelListClasses.rowActions,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: modelListClasses.rowActionsCluster,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: modelListClasses.rowCapabilityStrip,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: modelListClasses.rowCapabilityTagCluster,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelTagsWithLabel_default, {
							model,
							provider,
							size: 12,
							style: { flexWrap: "nowrap" }
						})
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FreeTrialModelTag, {
						modelId: model.id,
						providerId: model.providerId
					})]
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: modelListClasses.rowInlineActions,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						content: t("common.settings"),
						placement: "top",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							type: "button",
							variant: "ghost",
							size: "icon-sm",
							className: modelListClasses.rowActionButton,
							"aria-label": t("common.settings"),
							onClick: handleEdit,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bolt, { className: "size-4" })
						})
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
						content: deleteTooltip,
						placement: "top",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							type: "button",
							variant: "ghost",
							size: "icon-sm",
							className: `${modelListClasses.rowActionButton} ${modelListClasses.rowDangerActionButton}`,
							"aria-label": t("settings.models.manage.remove_model"),
							disabled: disabled || isDefaultModel,
							onClick: handleDelete,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Minus, { className: "size-4" })
						})
					})]
				})]
			})
		})]
	});
};
var ModelListItem_default = (0, import_react.memo)(ModelListItem);
var ModelListSections = ({ provider, isLoading, hasNoModels, hasVisibleModels, enabledSections, disabled, pendingModelIds, defaultModelIds, onEditModel, onDeleteModel, onDeleteModels, bulkActionDisabled, expansionCommand }) => {
	const { t } = useTranslation();
	const [groupOpenOverrides, setGroupOpenOverrides] = (0, import_react.useState)({});
	(0, import_react.useEffect)(() => {
		if (!expansionCommand) return;
		setGroupOpenOverrides(Object.fromEntries(enabledSections.map(({ groupName }) => [groupName, expansionCommand.expanded])));
	}, [enabledSections, expansionCommand]);
	const toggleGroupOpen = (0, import_react.useCallback)((groupName, defaultOpen) => {
		setGroupOpenOverrides((current) => ({
			...current,
			[groupName]: !(current[groupName] ?? defaultOpen)
		}));
	}, []);
	const virtualRows = (0, import_react.useMemo)(() => {
		return enabledSections.flatMap(({ groupName, items }, index) => {
			const defaultOpen = index <= 5;
			const open = groupOpenOverrides[groupName] ?? defaultOpen;
			const groupRow = {
				type: "group",
				key: `group:${groupName}`,
				groupName,
				items,
				defaultOpen,
				open
			};
			if (!open) return [groupRow];
			return [groupRow, ...items.map(({ model }, modelIndex) => ({
				type: "model",
				key: `model:${model.id}`,
				model,
				isLastInGroup: modelIndex === items.length - 1
			}))];
		});
	}, [enabledSections, groupOpenOverrides]);
	if (isLoading) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.model-list-sections",
		className: "flex items-center justify-center py-6",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingIcon_default, { color: "var(--muted-foreground)" })
	});
	if (hasNoModels) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmptyState, {
		compact: true,
		title: t("settings.models.empty"),
		description: t("settings.models.empty_hint"),
		className: "min-h-40"
	});
	if (!hasVisibleModels) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.model-list-sections",
		className: modelListClasses.emptyState,
		children: t("common.no_results")
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DynamicVirtualList_default, {
		list: virtualRows,
		className: modelListClasses.listScroller,
		role: "list",
		estimateSize: (index) => virtualRows[index]?.type === "group" ? 48 : 44,
		overscan: 10,
		isSticky: (index) => virtualRows[index]?.type === "group",
		getItemKey: (index) => virtualRows[index]?.key ?? index,
		children: (row) => {
			if (row.type === "group") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: cn(modelListClasses.virtualGroupRow, !row.open && modelListClasses.virtualGroupRowCollapsed),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelListGroup_default, {
					groupName: row.groupName,
					items: row.items,
					defaultOpen: row.defaultOpen,
					open: row.open,
					disabled,
					bulkActionDisabled,
					pendingModelIds,
					defaultModelIds,
					onDeleteModels,
					onToggleOpen: () => toggleGroupOpen(row.groupName, row.defaultOpen)
				})
			});
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: cn(modelListClasses.virtualModelRow, row.isLastInGroup && modelListClasses.virtualModelRowLast),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelListItem_default, {
					provider,
					model: row.model,
					onEdit: onEditModel,
					onDelete: onDeleteModel,
					disabled: disabled || pendingModelIds.has(row.model.id),
					isDefaultModel: defaultModelIds.has(row.model.id)
				})
			});
		}
	});
};
var ModelListSections_default = ModelListSections;
const filterProviderSettingModelsByKeywords = (keywords, models) => {
	if (!keywords.trim()) return models;
	return models.flatMap((model, index) => {
		const searchScore = getSearchMatchScore(keywords, [
			{
				value: model.name,
				weight: 0,
				allowAbbreviation: true
			},
			{
				value: model.apiModelId,
				weight: 1,
				allowAbbreviation: true
			},
			{
				value: model.id,
				weight: 1,
				allowAbbreviation: true
			},
			{
				value: model.group,
				weight: 2,
				allowAbbreviation: true
			},
			{
				value: model.description,
				weight: 30,
				allowAbbreviation: false
			}
		]);
		return searchScore === null ? [] : [{
			model,
			searchScore,
			index
		}];
	}).sort((a, b) => a.searchScore - b.searchScore || a.index - b.index).map(({ model }) => model);
};
const getDuplicateProviderSettingModelNames = (models) => {
	const counts = /* @__PURE__ */ new Map();
	for (const model of models) counts.set(model.name, (counts.get(model.name) ?? 0) + 1);
	return new Set(Array.from(counts.entries()).filter(([, count]) => count > 1).map(([name]) => name));
};
const MODEL_LIST_CAPABILITY_FILTERS = [
	"all",
	"text",
	"image",
	"embedding",
	"audio",
	"video",
	"rerank",
	"speech",
	"transcription"
];
function getModelIdGroupName(model) {
	return deriveModelGroupName(model.apiModelId ?? parseUniqueModelId(model.id).modelId);
}
const groupModels = (models, preserveGroupOrder = false, options = {}) => {
	const grouped = models.reduce((acc, model) => {
		const inferredGroup = getModelIdGroupName(model);
		const hasLegacyProviderGroup = inferredGroup !== void 0 && model.group?.trim() === model.providerId;
		const shouldPreferStoredGroup = options.preferModelGroup && !hasLegacyProviderGroup;
		const groupName = normalizeModelGroupName(shouldPreferStoredGroup ? model.group : inferredGroup, (shouldPreferStoredGroup ? inferredGroup : model.group) ?? model.providerId);
		if (!acc[groupName]) acc[groupName] = [];
		acc[groupName].push(model);
		return acc;
	}, {});
	if (preserveGroupOrder) return grouped;
	return sortBy(toPairs(grouped), [0]).reduce((acc, [key, value]) => {
		acc[key] = value;
		return acc;
	}, {});
};
var isTextToSpeechModel = (model) => model.endpointTypes?.includes(ENDPOINT_TYPE.OPENAI_TEXT_TO_SPEECH) ?? false;
const matchesCapabilityFilter = (model, selectedCapabilityFilter) => {
	switch (selectedCapabilityFilter) {
		case "text": return !isNonChatModel(model);
		case "image": return isGenerateImageModel(model);
		case "embedding": return isEmbeddingModel(model);
		case "audio": return isGenerateAudioModel(model) && !isTextToSpeechModel(model);
		case "video": return isGenerateVideoModel(model);
		case "rerank": return isRerankModel(model);
		case "speech": return isTextToSpeechModel(model);
		case "transcription": return isSpeechToTextModel(model);
		default: return true;
	}
};
const applyModelFilters = (models, searchText, selectedCapabilityFilter) => {
	const searchedModels = searchText ? filterProviderSettingModelsByKeywords(searchText, models) : models;
	if (selectedCapabilityFilter === "all") return searchedModels;
	return searchedModels.filter((model) => matchesCapabilityFilter(model, selectedCapabilityFilter));
};
const countModelsInGroups = (groups) => {
	return Object.values(groups).reduce((acc, group) => acc + group.length, 0);
};
const getCapabilityModelCounts = (models) => {
	const counts = Object.fromEntries(MODEL_LIST_CAPABILITY_FILTERS.map((filter) => [filter, 0]));
	counts.all = models.length;
	for (const model of models) for (const filter of MODEL_LIST_CAPABILITY_FILTERS) if (filter !== "all" && matchesCapabilityFilter(model, filter)) counts[filter] += 1;
	return counts;
};
const calculateModelListDerivedState = ({ models, searchText, selectedCapabilityFilter, modelStatuses }) => {
	const filteredModels = applyModelFilters(models, searchText, selectedCapabilityFilter);
	return {
		filteredModels,
		capabilityOptions: MODEL_LIST_CAPABILITY_FILTERS,
		capabilityModelCounts: getCapabilityModelCounts(applyModelFilters(models, searchText, "all")),
		duplicateModelNames: getDuplicateProviderSettingModelNames(models),
		modelCount: filteredModels.length,
		hasVisibleModels: filteredModels.length > 0,
		hasNoModels: models.length === 0,
		modelStatusMap: new Map(modelStatuses.map((status) => [status.model.id, status]))
	};
};
var toGroupSections = (groups) => {
	return Object.entries(groups).map(([groupName, models]) => ({
		groupName,
		items: models.map((model) => ({ model }))
	}));
};
var withPrunedModelIds = (entries, validIds) => {
	let changed = false;
	const next = {};
	for (const [modelId, value] of Object.entries(entries)) {
		if (!validIds.has(modelId)) {
			changed = true;
			continue;
		}
		next[modelId] = value;
	}
	return changed ? next : entries;
};
function useProviderModelList({ providerId, disabled = false }) {
	const { models, isLoading: isModelsLoading } = useModels({ providerId }, { swrOptions: PROVIDER_SETTINGS_MODEL_SWR_OPTIONS });
	const { deleteModel, deleteModels } = useModelMutations();
	const [defaultModelId] = usePreference("chat.default_model_id");
	const [quickAssistantModelId] = usePreference("feature.quick_assistant.model_id");
	const [translateModelId] = usePreference("feature.translate.model_id");
	const [searchInputText, setSearchInputText] = (0, import_react.useState)("");
	const searchText = (0, import_react.useDeferredValue)(searchInputText);
	const [selectedTypeFilter, setSelectedTypeFilter] = (0, import_react.useState)("all");
	const [editingModel, setEditingModel] = (0, import_react.useState)(null);
	const [optimisticDeletedByModelId, setOptimisticDeletedByModelId] = (0, import_react.useState)({});
	const [pendingModelIdMap, setPendingModelIdMap] = (0, import_react.useState)({});
	const defaultModelIds = (0, import_react.useMemo)(() => new Set([
		defaultModelId,
		quickAssistantModelId,
		translateModelId
	].filter((modelId) => modelId != null)), [
		defaultModelId,
		quickAssistantModelId,
		translateModelId
	]);
	const optimisticModels = (0, import_react.useMemo)(() => models.filter((model) => !optimisticDeletedByModelId[model.id]), [models, optimisticDeletedByModelId]);
	const derivedState = (0, import_react.useMemo)(() => calculateModelListDerivedState({
		models: optimisticModels,
		searchText,
		selectedCapabilityFilter: selectedTypeFilter,
		modelStatuses: []
	}), [
		optimisticModels,
		searchText,
		selectedTypeFilter
	]);
	(0, import_react.useEffect)(() => {
		const validModelIds = new Set(models.map((model) => model.id));
		setPendingModelIdMap((current) => withPrunedModelIds(current, validModelIds));
		setOptimisticDeletedByModelId((current) => withPrunedModelIds(current, validModelIds));
	}, [models]);
	const displayState = (0, import_react.useMemo)(() => {
		const preserveGroupOrder = Boolean(searchText.trim());
		const groups = groupModels(derivedState.filteredModels, preserveGroupOrder, { preferModelGroup: true });
		return {
			groups,
			displayEnabledModelCount: countModelsInGroups(groups)
		};
	}, [derivedState.filteredModels, searchText]);
	const openEditModelDrawer = (0, import_react.useCallback)((model) => {
		setEditingModel(model);
	}, []);
	const closeEditModelDrawer = (0, import_react.useCallback)(() => {
		setEditingModel(null);
	}, []);
	const onDeleteModel = (0, import_react.useCallback)(async (model) => {
		if (defaultModelIds.has(model.id)) return;
		const { modelId } = parseUniqueModelId(model.id);
		setOptimisticDeletedByModelId((current) => ({
			...current,
			[model.id]: true
		}));
		setPendingModelIdMap((current) => ({
			...current,
			[model.id]: true
		}));
		try {
			await deleteModel(model.providerId, modelId);
		} catch (error) {
			setOptimisticDeletedByModelId((current) => {
				const next = { ...current };
				delete next[model.id];
				return next;
			});
			throw error;
		} finally {
			setPendingModelIdMap((current) => {
				const next = { ...current };
				delete next[model.id];
				return next;
			});
		}
	}, [defaultModelIds, deleteModel]);
	const onDeleteModels = (0, import_react.useCallback)(async (modelsToDelete) => {
		const deletableModels = modelsToDelete.filter((model) => !defaultModelIds.has(model.id));
		if (deletableModels.length === 0) return;
		setOptimisticDeletedByModelId((current) => {
			const next = { ...current };
			for (const model of deletableModels) next[model.id] = true;
			return next;
		});
		setPendingModelIdMap((current) => {
			const next = { ...current };
			for (const model of deletableModels) next[model.id] = true;
			return next;
		});
		try {
			await deleteModels(deletableModels.map((model) => model.id));
		} catch (error) {
			setOptimisticDeletedByModelId((current) => {
				const next = { ...current };
				for (const model of deletableModels) delete next[model.id];
				return next;
			});
			throw error;
		} finally {
			setPendingModelIdMap((current) => {
				const next = { ...current };
				for (const model of deletableModels) delete next[model.id];
				return next;
			});
		}
	}, [defaultModelIds, deleteModels]);
	const enabledSections = (0, import_react.useMemo)(() => toGroupSections(displayState.groups), [displayState.groups]);
	const pendingModelIds = (0, import_react.useMemo)(() => new Set(Object.keys(pendingModelIdMap)), [pendingModelIdMap]);
	return {
		header: {
			modelCount: derivedState.modelCount,
			hasVisibleModels: derivedState.hasVisibleModels,
			hasNoModels: derivedState.hasNoModels,
			searchText: searchInputText,
			setSearchText: setSearchInputText,
			selectedTypeFilter,
			setSelectedTypeFilter,
			typeCounts: derivedState.capabilityModelCounts
		},
		sections: {
			isLoading: isModelsLoading && models.length === 0,
			hasNoModels: derivedState.hasNoModels,
			hasVisibleModels: derivedState.hasVisibleModels,
			displayEnabledModelCount: displayState.displayEnabledModelCount,
			enabledSections,
			disabled,
			pendingModelIds,
			defaultModelIds,
			onEditModel: openEditModelDrawer,
			onDeleteModel,
			onDeleteModels
		},
		editDrawer: {
			open: editingModel !== null,
			model: editingModel,
			onClose: closeEditModelDrawer
		}
	};
}
var ProviderModelList = ({ providerId, disabled, actions }) => {
	const [groupExpansionCommand, setGroupExpansionCommand] = (0, import_react.useState)({
		expanded: true,
		version: 0
	});
	const modelList = useProviderModelList({
		providerId,
		disabled
	});
	const providerMeta = useProviderMeta(providerId);
	const toolbarDisabled = disabled;
	const toggleGroupsExpanded = (0, import_react.useCallback)(() => {
		setGroupExpansionCommand((current) => ({
			expanded: !current.expanded,
			version: current.version + 1
		}));
	}, []);
	(0, import_react.useEffect)(() => {
		if (!modelList.header.searchText.trim()) return;
		setGroupExpansionCommand((current) => {
			if (current.expanded) return current;
			return {
				expanded: true,
				version: current.version + 1
			};
		});
	}, [modelList.header.searchText]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-model-list",
		className: modelListClasses.headerBlock,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelListHeader_default, {
			isBusy: toolbarDisabled,
			hasNoModels: modelList.header.hasNoModels,
			searchText: modelList.header.searchText,
			setSearchText: modelList.header.setSearchText,
			selectedTypeFilter: modelList.header.selectedTypeFilter,
			setSelectedTypeFilter: modelList.header.setSelectedTypeFilter,
			typeCounts: modelList.header.typeCounts,
			groupsExpanded: groupExpansionCommand.expanded,
			onToggleGroupsExpanded: toggleGroupsExpanded,
			docsWebsite: providerMeta.docsWebsite,
			modelsWebsite: providerMeta.modelsWebsite,
			actions: actions?.({
				disabled: toolbarDisabled,
				hasVisibleModels: modelList.header.hasVisibleModels
			})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelListSections_default, {
			provider: providerMeta.provider,
			isLoading: modelList.sections.isLoading,
			hasNoModels: modelList.sections.hasNoModels,
			hasVisibleModels: modelList.sections.hasVisibleModels,
			enabledSections: modelList.sections.enabledSections,
			disabled: modelList.sections.disabled,
			pendingModelIds: modelList.sections.pendingModelIds,
			defaultModelIds: modelList.sections.defaultModelIds,
			onEditModel: modelList.sections.onEditModel,
			onDeleteModel: modelList.sections.onDeleteModel,
			onDeleteModels: modelList.sections.onDeleteModels,
			bulkActionDisabled: toolbarDisabled,
			expansionCommand: groupExpansionCommand
		})]
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EditModelDrawer, {
		providerId,
		open: modelList.editDrawer.open,
		model: modelList.editDrawer.model,
		onClose: modelList.editDrawer.onClose
	})] });
};
var ProviderModelList_default = ProviderModelList;
function modelIdLine(model) {
	return model.apiModelId ?? parseUniqueModelId(model.id).modelId;
}
var ModelGlyph = (0, import_react.memo)(function ModelGlyph$1({ model }) {
	const Icon = useIcon(getModelLogoRef(model));
	if (Icon) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		"data-ui": "settings.model-sync-preview-panel",
		className: modelListClasses.rowAvatar,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon.Avatar, {
			size: 26,
			shape: "circle"
		})
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Avatar, {
		className: modelListClasses.rowAvatar,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AvatarFallback, {
			className: "rounded-[inherit]",
			children: model.name?.slice(0, 1).toUpperCase()
		})
	});
});
var ManageModelRow = (0, import_react.memo)(function ManageModelRow$1({ model, provider, isAdded, isRemovable, isDefaultModel, isStale, isApplying, onAddModels, onRemoveModels }) {
	const { t } = useTranslation();
	const actionTooltip = isAdded ? isDefaultModel ? t("settings.models.manage.default_model_cannot_remove") : t("settings.models.manage.remove_model") : t("button.add");
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.model-sync-preview-panel",
		className: modelSyncClasses.manageRow,
		"data-added": isAdded,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelGlyph, { model }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "min-w-0 flex-1",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: modelSyncClasses.manageRowTitleLine,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
							className: modelSyncClasses.manageRowTitle,
							children: modelIdLine(model)
						}),
						model.description ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
							content: model.description,
							placement: "top",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								tabIndex: 0,
								"aria-label": model.description,
								className: modelSyncClasses.manageRowDescriptionHelp,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleQuestionMark, {
									"aria-hidden": true,
									className: "size-3"
								})
							})
						}) : null,
						isStale ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
							variant: "secondary",
							className: modelSyncClasses.manageStaleBadge,
							children: t("settings.models.manage.stale_badge")
						}) : null
					]
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: modelSyncClasses.fetchCapabilityStrip,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelTagsWithLabel_default, {
					model,
					provider,
					size: 12,
					style: { flexWrap: "nowrap" }
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
				content: actionTooltip,
				placement: "top",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					type: "button",
					variant: "ghost",
					size: "icon-sm",
					"aria-label": isAdded ? t("settings.models.manage.remove_model") : t("button.add"),
					disabled: isApplying || isAdded && !isRemovable,
					className: modelSyncClasses.manageRowAction,
					onClick: () => {
						if (isAdded) {
							if (isRemovable) onRemoveModels([model.id]);
							return;
						}
						onAddModels([model]);
					},
					children: isAdded ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Minus, { className: "size-4" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, { className: "size-4" })
				})
			})
		]
	});
});
function ModelSyncPreviewPanel({ provider, modelGroups, localModelIds, removableModelIds, defaultModelIds, staleModelIds, isLoading, isApplying, searchActive = false, onAddModels, onRemoveModels }) {
	const { t } = useTranslation();
	const [collapsedGroups, setCollapsedGroups] = (0, import_react.useState)(/* @__PURE__ */ new Set());
	const entries = (0, import_react.useMemo)(() => Object.entries(modelGroups).filter(([, models]) => models.length > 0), [modelGroups]);
	const virtualRows = (0, import_react.useMemo)(() => {
		return entries.flatMap(([groupName, models]) => {
			const collapsed = !searchActive && collapsedGroups.has(groupName);
			const groupLabel = getModelGroupLabel(groupName, t);
			const groupRow = {
				key: `group:${groupName}`,
				type: "group",
				groupName,
				groupLabel,
				models,
				collapsed
			};
			if (collapsed) return [groupRow];
			return [groupRow, ...models.map((model) => ({
				key: `model:${model.id}`,
				type: "model",
				model
			}))];
		});
	}, [
		collapsedGroups,
		entries,
		searchActive,
		t
	]);
	const toggleGroup = (0, import_react.useCallback)((groupName) => {
		setCollapsedGroups((current) => {
			const next = new Set(current);
			if (next.has(groupName)) next.delete(groupName);
			else next.add(groupName);
			return next;
		});
	}, []);
	const renderGroupAction = (0, import_react.useCallback)((models) => {
		const isAllInProvider = models.every((model) => localModelIds.has(model.id));
		const removableGroupModelIds = models.filter((model) => localModelIds.has(model.id) && removableModelIds.has(model.id)).map((model) => model.id);
		const title = isAllInProvider ? t("settings.models.manage.remove_whole_group") : t("settings.models.manage.add_whole_group");
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
			content: title,
			placement: "top",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				type: "button",
				variant: "ghost",
				size: "icon-sm",
				"aria-label": title,
				disabled: isApplying || isAllInProvider && removableGroupModelIds.length === 0,
				className: modelSyncClasses.manageRowAction,
				onClick: (event) => {
					event.stopPropagation();
					if (isAllInProvider) {
						onRemoveModels(removableGroupModelIds);
						return;
					}
					onAddModels(models.filter((model) => !localModelIds.has(model.id)));
				},
				children: isAllInProvider ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Minus, { className: "size-4" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, { className: "size-4" })
			})
		});
	}, [
		isApplying,
		localModelIds,
		onAddModels,
		onRemoveModels,
		removableModelIds,
		t
	]);
	if (isLoading) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.model-sync-preview-panel",
		className: modelSyncClasses.manageLoading,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Spinner, { text: t("common.loading") })
	});
	if (entries.length === 0) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EmptyState, {
		compact: true,
		preset: searchActive ? "no-result" : "no-model",
		title: searchActive ? t("common.no_results") : t("settings.models.empty"),
		className: "min-h-52"
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DynamicVirtualList_default, {
		list: virtualRows,
		className: modelSyncClasses.manageScrollArea,
		role: "list",
		estimateSize: () => 44,
		itemContainerStyle: { height: 44 },
		overscan: 5,
		isSticky: (index) => virtualRows[index]?.type === "group",
		getItemKey: (index) => virtualRows[index]?.key ?? index,
		children: (row) => {
			if (row.type === "group") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: modelSyncClasses.manageVirtualGroupRow,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: cn$1(modelSyncClasses.manageGroupHeaderSurface, row.collapsed && modelSyncClasses.manageGroupHeaderSurfaceCollapsed),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
						type: "button",
						className: modelSyncClasses.manageGroupToggle,
						"aria-expanded": !row.collapsed,
						onClick: () => toggleGroup(row.groupName),
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRight, {
								className: cn$1(modelSyncClasses.manageGroupChevron, !row.collapsed && "rotate-90"),
								"aria-hidden": true
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: modelSyncClasses.manageGroupTitle,
								children: row.groupLabel
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
								variant: "secondary",
								className: modelSyncClasses.manageGroupBadge,
								children: row.models.length
							})
						]
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "ml-auto pr-2",
						children: renderGroupAction(row.models)
					})]
				})
			});
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: modelSyncClasses.manageVirtualModelRow,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ManageModelRow, {
					provider,
					model: row.model,
					isAdded: localModelIds.has(row.model.id),
					isRemovable: removableModelIds.has(row.model.id),
					isDefaultModel: defaultModelIds.has(row.model.id),
					isStale: staleModelIds.has(row.model.id),
					isApplying,
					onAddModels,
					onRemoveModels
				})
			});
		}
	});
}
function ModelListSyncDrawer({ open, provider, allModels = [], localModels = [], removableModelIds = [], defaultModelIds = [], isLoading, isApplying, loadErrorMessage, staleModelCount = 0, staleModelIds = [], onRetryLoadModels, onAddModels, onRemoveModels, onCleanStaleModels, onClose }) {
	const { t } = useTranslation();
	const [searchText, setSearchText] = (0, import_react.useState)("");
	const [actualFilter, setActualFilter] = (0, import_react.useState)("all");
	(0, import_react.useEffect)(() => {
		setSearchText("");
		setActualFilter("all");
	}, [open]);
	const localModelIds = (0, import_react.useMemo)(() => new Set(localModels.map((model) => model.id)), [localModels]);
	const removableModelIdSet = (0, import_react.useMemo)(() => new Set(removableModelIds), [removableModelIds]);
	const defaultModelIdSet = (0, import_react.useMemo)(() => new Set(defaultModelIds), [defaultModelIds]);
	const staleModelIdSet = (0, import_react.useMemo)(() => new Set(staleModelIds), [staleModelIds]);
	const filteredModels = (0, import_react.useMemo)(() => {
		if (actualFilter === "stale") return applyModelFilters(allModels, searchText, "all").filter((model) => staleModelIdSet.has(model.id));
		return applyModelFilters(allModels, searchText, actualFilter);
	}, [
		actualFilter,
		allModels,
		searchText,
		staleModelIdSet
	]);
	const filteredGroups = (0, import_react.useMemo)(() => groupModels(filteredModels, Boolean(searchText.trim())), [filteredModels, searchText]);
	const typeCounts = (0, import_react.useMemo)(() => getCapabilityModelCounts(applyModelFilters(allModels, searchText, "all")), [allModels, searchText]);
	const isAllFilteredInProvider = filteredModels.length > 0 && filteredModels.every((model) => localModelIds.has(model.id));
	const removableFilteredModelIds = (0, import_react.useMemo)(() => filteredModels.filter((model) => localModelIds.has(model.id) && removableModelIdSet.has(model.id)).map((model) => model.id), [
		filteredModels,
		localModelIds,
		removableModelIdSet
	]);
	const busy = isLoading || isApplying;
	const hasLoadError = Boolean(loadErrorMessage);
	const drawerTitle = provider?.name ? `${provider.name} ${t("common.models")}` : t("settings.models.manage.drawer_title");
	const bulkActionLabel = isAllFilteredInProvider ? t("settings.models.manage.remove_listed") : t("settings.models.manage.add_listed.label");
	const cleanStaleLabel = t("settings.models.manage.clean_stale_models");
	(0, import_react.useEffect)(() => {
		if (staleModelCount === 0 && actualFilter === "stale") setActualFilter("all");
	}, [actualFilter, staleModelCount]);
	const handleBulkAction = (0, import_react.useCallback)(() => {
		if (isAllFilteredInProvider) {
			onRemoveModels(removableFilteredModelIds);
			return;
		}
		onAddModels(filteredModels.filter((model) => !localModelIds.has(model.id)));
	}, [
		filteredModels,
		isAllFilteredInProvider,
		localModelIds,
		onAddModels,
		onRemoveModels,
		removableFilteredModelIds
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ProviderSettingsDrawer, {
		open,
		onClose,
		title: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
			className: modelSyncClasses.manageTitle,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: modelSyncClasses.manageTitleText,
				children: drawerTitle
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
				variant: "secondary",
				className: modelSyncClasses.manageTitleCountBadge,
				children: allModels.length
			})]
		}),
		titleActions: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
			className: "flex items-center gap-1",
			children: [
				hasLoadError ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: loadErrorMessage,
					placement: "top",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						type: "button",
						variant: "ghost",
						size: "icon-sm",
						"aria-label": loadErrorMessage ?? t("common.refresh"),
						disabled: isLoading,
						loading: isLoading,
						className: modelSyncClasses.manageTitleErrorRetryButton,
						onClick: () => void onRetryLoadModels?.(),
						children: [isLoading ? null : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RefreshCw, { className: "size-3.5" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: modelSyncClasses.manageTitleErrorDot })]
					})
				}) : null,
				staleModelCount > 0 && onCleanStaleModels ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: cleanStaleLabel,
					placement: "top",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						type: "button",
						variant: "ghost",
						size: "sm",
						"aria-label": cleanStaleLabel,
						disabled: busy,
						className: modelSyncClasses.manageTitleActionButton,
						onClick: () => void onCleanStaleModels(),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trash2, { className: "size-4" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: cleanStaleLabel })]
					})
				}) : null,
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: bulkActionLabel,
					placement: "top",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
						type: "button",
						variant: "ghost",
						size: "sm",
						"aria-label": bulkActionLabel,
						disabled: busy || filteredModels.length === 0 || isAllFilteredInProvider && removableFilteredModelIds.length === 0,
						className: modelSyncClasses.manageTitleActionButton,
						onClick: handleBulkAction,
						children: [isAllFilteredInProvider ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ListMinus, { className: "size-4" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ListPlus, { className: "size-4" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: bulkActionLabel })]
					})
				})
			]
		}),
		bodyClassName: "flex flex-col space-y-0 overflow-hidden pt-0",
		contentClassName: "w-[min(calc(100vw-24px),620px)]",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: modelSyncClasses.manageStickyHeader,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: modelSyncClasses.manageToolbar,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "relative min-w-0 flex-1",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Search, { className: modelSyncClasses.manageSearchIcon }),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
							type: "text",
							value: searchText,
							placeholder: t("settings.models.manage.search_models_placeholder"),
							disabled: isLoading,
							onChange: (event) => setSearchText(event.target.value),
							className: modelSyncClasses.manageSearchInput
						}),
						searchText ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
							type: "button",
							onClick: () => setSearchText(""),
							className: modelSyncClasses.manageSearchClear,
							"aria-label": t("common.clear"),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { size: 9 })
						}) : null
					]
				})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelTypeFilterTabs, {
				value: actualFilter,
				onValueChange: (next) => setActualFilter(next),
				counts: typeCounts,
				extraTabs: staleModelCount > 0 ? [{
					value: "stale",
					label: t("settings.models.manage.stale_filter"),
					count: staleModelCount,
					destructive: true
				}] : []
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelSyncPreviewPanel, {
			provider,
			modelGroups: filteredGroups,
			localModelIds,
			removableModelIds: removableModelIdSet,
			defaultModelIds: defaultModelIdSet,
			staleModelIds: staleModelIdSet,
			isLoading,
			isApplying,
			searchActive: Boolean(searchText.trim()),
			onAddModels,
			onRemoveModels
		})]
	});
}
var logger = loggerService.withContext("ProviderModelManageDrawer");
function uniqueById(models) {
	const result = /* @__PURE__ */ new Map();
	for (const model of models) if (!result.has(model.id)) result.set(model.id, model);
	return Array.from(result.values());
}
async function deleteModelsSkippingDefaults(uniqueIds, deleteModels) {
	let remainingIds = uniqueIds;
	const skippedIds = /* @__PURE__ */ new Set();
	while (remainingIds.length > 0) try {
		await deleteModels(remainingIds);
		return skippedIds;
	} catch (error) {
		const blockedId = getModelInUseAsDefaultUniqueModelId(error);
		if (!blockedId || !remainingIds.includes(blockedId)) throw error;
		skippedIds.add(blockedId);
		remainingIds = remainingIds.filter((id) => id !== blockedId);
	}
	return skippedIds;
}
function useProviderModelPullReconcile(providerId) {
	const { t } = useTranslation();
	const [pullReconcileDrawerOpen, setPullReconcileDrawerOpen] = (0, import_react.useState)(false);
	const [catalogModels, setCatalogModels] = (0, import_react.useState)([]);
	const [fetchedModels, setFetchedModels] = (0, import_react.useState)([]);
	const [isLoadingModels, setIsLoadingModels] = (0, import_react.useState)(false);
	const [hasLoadedCompleteRemoteModels, setHasLoadedCompleteRemoteModels] = (0, import_react.useState)(false);
	const [loadErrorMessage, setLoadErrorMessage] = (0, import_react.useState)(null);
	const loadModelsSequenceRef = (0, import_react.useRef)(0);
	const [defaultModelId] = usePreference("chat.default_model_id");
	const [quickAssistantModelId] = usePreference("feature.quick_assistant.model_id");
	const [translateModelId] = usePreference("feature.translate.model_id");
	const { provider, enableProvider } = useProvider(providerId);
	const { models } = useModels({ providerId });
	const { createModels, deleteModels, isCreating, isDeleting, isBulkDeleting } = useModelMutations();
	const { trigger: reconcileModels, isLoading: isReconciling } = useMutation("POST", "/providers/:providerId/models:reconcile", { refresh: ["/models"] });
	const allModels = (0, import_react.useMemo)(() => uniqueById([
		...fetchedModels,
		...catalogModels,
		...models
	]), [
		catalogModels,
		fetchedModels,
		models
	]);
	const remoteModelIds = (0, import_react.useMemo)(() => new Set([...catalogModels, ...fetchedModels].map((model) => model.id)), [catalogModels, fetchedModels]);
	const defaultModelIds = (0, import_react.useMemo)(() => new Set([
		defaultModelId,
		quickAssistantModelId,
		translateModelId
	].filter((modelId) => modelId != null)), [
		defaultModelId,
		quickAssistantModelId,
		translateModelId
	]);
	const removableModelIds = (0, import_react.useMemo)(() => models.filter((model) => !defaultModelIds.has(model.id) && (remoteModelIds.has(model.id) || model.presetModelId != null && model.presetModelId !== "")).map((model) => model.id), [
		defaultModelIds,
		models,
		remoteModelIds
	]);
	const staleModels = (0, import_react.useMemo)(() => {
		if (!hasLoadedCompleteRemoteModels) return [];
		return models.filter((model) => !remoteModelIds.has(model.id) && model.presetModelId != null && model.presetModelId !== "");
	}, [
		hasLoadedCompleteRemoteModels,
		models,
		remoteModelIds
	]);
	const loadModels = (0, import_react.useCallback)(async () => {
		const sequence = ++loadModelsSequenceRef.current;
		const isLatestLoad = () => sequence === loadModelsSequenceRef.current;
		setIsLoadingModels(true);
		setHasLoadedCompleteRemoteModels(false);
		setLoadErrorMessage(null);
		try {
			const [catalogResult, fetchedResult] = await Promise.allSettled([fetchProviderCatalogModels(providerId), fetchResolvedProviderModels(providerId)]);
			if (!isLatestLoad()) return;
			const catalog = catalogResult.status === "fulfilled" ? catalogResult.value : [];
			const fetched = fetchedResult.status === "fulfilled" ? fetchedResult.value : [];
			const hasLoadedAllModels = catalogResult.status === "fulfilled" && fetchedResult.status === "fulfilled";
			setCatalogModels(catalog.filter((model) => model.name?.trim()));
			setFetchedModels(fetched.filter((model) => model.name?.trim()));
			setHasLoadedCompleteRemoteModels(hasLoadedAllModels);
			if (!hasLoadedAllModels) {
				logger.error("Failed to load provider models for manage drawer", {
					providerId,
					catalogError: catalogResult.status === "rejected" ? catalogResult.reason : void 0,
					upstreamError: fetchedResult.status === "rejected" ? fetchedResult.reason : void 0
				});
				setLoadErrorMessage(t("settings.models.manage.sync_pull_failed"));
			}
		} finally {
			if (isLatestLoad()) setIsLoadingModels(false);
		}
	}, [providerId, t]);
	const openPullReconcile = (0, import_react.useCallback)(() => {
		setPullReconcileDrawerOpen(true);
		loadModels();
	}, [loadModels]);
	const closePullReconcile = (0, import_react.useCallback)(() => {
		setPullReconcileDrawerOpen(false);
	}, []);
	const addModels = (0, import_react.useCallback)(async (nextModels) => {
		const currentIds = new Set(models.map((model) => model.id));
		const toAdd = uniqueById(nextModels).filter((model) => !currentIds.has(model.id));
		if (toAdd.length === 0) return;
		try {
			const chunks = chunkArray(toAdd.map((model) => toCreateModelDto(providerId, model, resolveCreateModelEndpointTypes(provider, model))), 500);
			for (const chunk of chunks) await createModels(chunk);
		} catch (error) {
			logger.error("Failed to add provider models from manage drawer", {
				providerId,
				count: toAdd.length,
				error
			});
			toast.error(t("settings.models.manage.operation_failed"));
			return;
		}
		try {
			await enableProviderWhenModelsAvailable(provider, enableProvider, models.length + toAdd.length, "model_manage_add");
		} catch (error) {
			logger.error("Models were added but provider enablement failed", {
				providerId,
				count: toAdd.length,
				error
			});
			toast.warning(t("settings.models.manage.add_success_enable_failed"));
		}
	}, [
		createModels,
		enableProvider,
		models,
		provider,
		providerId,
		t
	]);
	const removeModels = (0, import_react.useCallback)(async (uniqueModelIds) => {
		const uniqueIds = Array.from(new Set(uniqueModelIds));
		if (uniqueIds.length === 0) return;
		try {
			const skippedIds = await deleteModelsSkippingDefaults(uniqueIds, deleteModels);
			if (skippedIds.size > 0) toast.warning(t("settings.models.manage.remove_skipped_default_in_use", { count: skippedIds.size }));
		} catch (error) {
			logger.error("Failed to remove provider models from manage drawer", {
				providerId,
				count: uniqueIds.length,
				error
			});
			toast.error(t("settings.models.manage.operation_failed"));
		}
	}, [
		deleteModels,
		providerId,
		t
	]);
	const cleanStaleModels = (0, import_react.useCallback)(async () => {
		const staleIds = staleModels.map((model) => model.id);
		if (staleIds.length === 0) return;
		try {
			const reconciledModels = await reconcileModels({
				params: { providerId },
				body: {
					toAdd: [],
					toRemove: staleIds
				}
			});
			const reconciledIds = new Set(reconciledModels.map((model) => model.id));
			const skippedCount = staleIds.filter((id) => reconciledIds.has(id)).length;
			if (skippedCount > 0) toast.warning(t("settings.models.manage.remove_skipped_default_in_use", { count: skippedCount }));
			else toast.success(t("settings.models.manage.clean_stale_success", { count: staleIds.length }));
		} catch (error) {
			logger.error("Failed to clean stale provider models from manage drawer", {
				providerId,
				count: staleIds.length,
				error
			});
			toast.error(t("settings.models.manage.operation_failed"));
		}
	}, [
		providerId,
		reconcileModels,
		staleModels,
		t
	]);
	return {
		allModels,
		provider,
		localModels: models,
		removableModelIds,
		defaultModelIds: [...defaultModelIds],
		staleModelCount: staleModels.length,
		staleModelIds: staleModels.map((model) => model.id),
		openPullReconcile,
		closePullReconcile,
		reloadModels: loadModels,
		pullReconcileDrawerOpen,
		addModels,
		removeModels,
		cleanStaleModels,
		isLoadingModels,
		loadErrorMessage,
		isApplyingPullReconcile: isCreating || isDeleting || isBulkDeleting || isReconciling,
		isBusy: isLoadingModels || isCreating || isDeleting || isBulkDeleting || isReconciling
	};
}
var ProviderModelPullReconcile = ({ providerId, disabled, guideVersion = 0 }) => {
	const { t } = useTranslation();
	const pullReconcile = useProviderModelPullReconcile(providerId);
	const [showPullGuide, setShowPullGuide] = (0, import_react.useState)(false);
	const pullGuideTimerRef = (0, import_react.useRef)(null);
	const showPullGuideBriefly = (0, import_react.useCallback)(() => {
		setShowPullGuide(true);
		if (pullGuideTimerRef.current) clearTimeout(pullGuideTimerRef.current);
		pullGuideTimerRef.current = setTimeout(() => {
			setShowPullGuide(false);
			pullGuideTimerRef.current = null;
		}, 1200);
	}, []);
	const openPullReconcile = (0, import_react.useCallback)(() => {
		setShowPullGuide(false);
		pullReconcile.openPullReconcile();
	}, [pullReconcile]);
	(0, import_react.useEffect)(() => {
		return () => {
			if (pullGuideTimerRef.current) clearTimeout(pullGuideTimerRef.current);
		};
	}, []);
	(0, import_react.useEffect)(() => {
		if (guideVersion > 0) showPullGuideBriefly();
	}, [guideVersion, showPullGuideBriefly]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ButtonGroupItem, {
		className: modelListClasses.fetchGuideWrap,
		children: [showPullGuide ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			"data-ui": "settings.provider-model-pull-reconcile.model-pull-guide-arrow",
			className: modelListClasses.fetchGuideArrow,
			"aria-hidden": "true",
			"data-testid": "model-pull-guide-arrow",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrowRight, {
				className: "size-4",
				strokeWidth: 2.5
			})
		}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
			type: "button",
			variant: "outline",
			size: "sm",
			className: modelListClasses.fetchActionButton,
			disabled: disabled || pullReconcile.isBusy,
			loading: pullReconcile.isBusy,
			onClick: openPullReconcile,
			children: [pullReconcile.isBusy ? null : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RefreshCw, { className: modelListClasses.toolbarDesignIcon }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: t("settings.models.toolbar.pull_short") })]
		})]
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelListSyncDrawer, {
		open: pullReconcile.pullReconcileDrawerOpen,
		provider: pullReconcile.provider,
		allModels: [...pullReconcile.allModels],
		localModels: [...pullReconcile.localModels],
		removableModelIds: pullReconcile.removableModelIds,
		defaultModelIds: pullReconcile.defaultModelIds,
		isLoading: pullReconcile.isLoadingModels,
		isApplying: pullReconcile.isApplyingPullReconcile,
		loadErrorMessage: pullReconcile.loadErrorMessage,
		staleModelCount: pullReconcile.staleModelCount,
		staleModelIds: pullReconcile.staleModelIds,
		onRetryLoadModels: pullReconcile.reloadModels,
		onAddModels: pullReconcile.addModels,
		onRemoveModels: pullReconcile.removeModels,
		onCleanStaleModels: pullReconcile.cleanStaleModels,
		onClose: pullReconcile.closePullReconcile
	})] });
};
var ProviderModelPullReconcile_default = ProviderModelPullReconcile;
function ModelListContent({ providerId, modelPullGuideVersion = 0 }) {
	const { isHealthChecking } = useModelListHealthRun();
	const disabled = isHealthChecking;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderModelList_default, {
		providerId,
		disabled,
		actions: ({ disabled: toolbarDisabled }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ButtonGroup, {
			className: modelListClasses.toolbarButtonGroup,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderModelPullReconcile_default, {
				providerId,
				disabled: toolbarDisabled,
				guideVersion: modelPullGuideVersion
			}), providerId === "ovms" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderModelDownload_default, {
				providerId,
				disabled: toolbarDisabled
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderModelAdd_default, {
				providerId,
				disabled: toolbarDisabled
			})]
		})
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderModelHealthCheck_default, {
		disabled,
		hasVisibleModels: false,
		renderTrigger: false
	})] });
}
var ModelList = ({ providerId, modelPullGuideVersion = 0 }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "settings.model-list",
		className: modelListClasses.cqRoot,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
			"data-ui": "settings.model-list.provider-model-list",
			"data-testid": "provider-model-list",
			className: modelListClasses.section,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelListContent, {
				providerId,
				modelPullGuideVersion
			})
		})
	});
};
var ModelList_default = (0, import_react.memo)(ModelList);
function ProviderSettingSections({ providerId, isLoginBased }) {
	const health = useModelListHealth();
	const [modelPullGuideVersion, setModelPullGuideVersion] = (0, import_react.useState)(0);
	const requestModelPullGuide = (0, import_react.useCallback)(() => {
		setModelPullGuideVersion((version) => version + 1);
	}, []);
	const authenticationSection = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AuthenticationSection, {
		providerId,
		onOpenModelHealthCheck: health.openHealthCheck,
		onRequestModelPullGuide: requestModelPullGuide
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Scrollbar_default, {
		className: providerDetailColumnClasses.scrollStrip,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: cn(providerDetailColumnClasses.sectionStack, isLoginBased && "gap-3"),
			children: [authenticationSection, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex min-h-0 flex-1 flex-col",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelList_default, {
					providerId,
					modelPullGuideVersion
				})
			})]
		})
	});
}
function ProviderSetting({ providerId, isOnboarding = false }) {
	const { provider } = useProvider(providerId);
	const { theme } = useTheme();
	useProviderOnboardingAutoEnable({
		providerId,
		isOnboarding
	});
	if (!provider) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingsContainer, {
		theme,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "flex h-full min-h-0 w-full flex-col",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				"data-ui": "settings.provider-setting.provider-detail-shell",
				"data-testid": "provider-detail-shell",
				className: "flex min-h-0 flex-1 flex-col overflow-hidden",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: providerDetailColumnClasses.headerPad,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: providerDetailColumnClasses.headerContentMaxWidth,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderHeader, { providerId })
					})
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelListHealthProvider, {
					providerId,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSettingSections, {
						providerId,
						isLoginBased: isLoginBasedProvider(provider)
					})
				})]
			})
		})
	});
}
function ProviderSettingsPage({ isOnboarding = false }) {
	const search = useSearch({ strict: false });
	const navigate = useNavigate();
	const { providers: rawProviders } = useProviders();
	const [lastSelectedProviderId, setLastSelectedProviderId] = usePersistCache("settings.provider.last_selected_provider_id");
	const [selectedProviderId, setSelectedProviderIdState] = (0, import_react.useState)(() => lastSelectedProviderId ?? void 0);
	const setLastSelectedProviderIdRef = (0, import_react.useRef)(setLastSelectedProviderId);
	const providers = (0, import_react.useMemo)(() => Array.isArray(rawProviders) ? rawProviders : [], [rawProviders]);
	const visibleProviders = (0, import_react.useMemo)(() => providers.filter(isProviderSettingsListVisibleProvider), [providers]);
	const filterModeHint = search.filter === "agent" ? "agent" : void 0;
	(0, import_react.useEffect)(() => {
		setLastSelectedProviderIdRef.current = setLastSelectedProviderId;
	}, [setLastSelectedProviderId]);
	(0, import_react.useEffect)(() => {
		const persistedProviderId = lastSelectedProviderId ?? void 0;
		setSelectedProviderIdState((currentProviderId) => currentProviderId === persistedProviderId ? currentProviderId : persistedProviderId);
	}, [lastSelectedProviderId]);
	const setSelectedProviderId = (0, import_react.useCallback)((providerId) => {
		setLastSelectedProviderIdRef.current(providerId ?? null);
		(0, import_react.startTransition)(() => setSelectedProviderIdState(providerId));
	}, []);
	useProviderDeepLinkImport(search.addProviderData, setSelectedProviderId);
	(0, import_react.useEffect)(() => {
		let shouldConsume = false;
		if (search.filter === "agent") shouldConsume = true;
		if (search.id) {
			setSelectedProviderId(visibleProviders.find((item) => item.id === search.id)?.id ?? visibleProviders[0]?.id);
			shouldConsume = true;
		}
		if (shouldConsume) navigate({
			to: "/settings/provider",
			search: omit(search, ["filter", "id"]),
			replace: true
		});
	}, [
		navigate,
		search,
		setSelectedProviderId,
		visibleProviders
	]);
	(0, import_react.useEffect)(() => {
		if (!selectedProviderId && visibleProviders[0]) {
			setSelectedProviderId(visibleProviders[0].id);
			return;
		}
		if (selectedProviderId && !visibleProviders.some((provider) => provider.id === selectedProviderId)) setSelectedProviderId(visibleProviders[0]?.id);
	}, [
		selectedProviderId,
		setSelectedProviderId,
		visibleProviders
	]);
	const selectedProvider = (0, import_react.useMemo)(() => visibleProviders.find((provider) => provider.id === selectedProviderId), [selectedProviderId, visibleProviders]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "settings.provider-settings-page",
		className: "relative flex h-full min-h-0 w-full min-w-0 overflow-hidden",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderList, {
			selectedProviderId,
			filterModeHint,
			onSelectProvider: setSelectedProviderId
		}), selectedProvider && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProviderSetting, {
			providerId: selectedProvider.id,
			isOnboarding
		}, selectedProvider.id)]
	});
}
export { oauthWithCherryIn as n, useProviderModelSync as r, ProviderSettingsPage as t };
