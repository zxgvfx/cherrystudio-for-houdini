{
  "id": "ai-pipeline-bridge",
  "name": "AI Pipeline",
  "version": "0.5.0",
  "description": "ai-pipeline 的 Cherry 侧通用 workflow 启动器 + 配置与兼容 API 透传。打开后自动列出 server 上已注册的 workflow；开发者不需要再为每个 workflow 新增 CherryStudio 插件。配置：PIPELINE_API_BASE 或 ~/.cherrystudio/ai-pipeline-bridge.json",
  "icon": "cube",
  "launcher": {
    "enabled": true,
    "type": "webview",
    "label": "AI Pipeline",
    "category": "ai",
    "pinnable": true,
    "entry": {
      "action": "route",
      "url": "/plugins/ai-pipeline-bridge"
    }
  },
  "backend_routes": "backend/routes.py",
  "output_types": ["model_3d", "image", "motion", "animation"],
  "dependencies": {
    "servers": ["ai_pipeline_api:9331"]
  }
}
