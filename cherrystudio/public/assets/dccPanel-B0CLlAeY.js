import { c as __toESM } from "./rolldown-runtime-D8OvLAZx.js";
import "./abortSignalPolyfill-CroYRteo.js";
/* empty css                */
import { t as require_client } from "./client-Dri0ImSL.js";
import "./LoggerService-ChVOAPl8.js";
import "./error-CskkREu_.js";
import "./PreferenceService-CvpJqJd7.js";
import "./platform-fGkkNTU9.js";
import "./dataApiDevtools-Bm4JBknU.js";
import "./dayjs.min-BBb2vAs7.js";
import { r as resolver_default } from "./resolver-Bn-i1elC.js";
import "./i18next-CqNcSVOM.js";
import { t as require_react } from "./react-C1DTAr29.js";
import { a as prepareWindow, i as ThemeProvider, n as useCustomCss, r as WindowFatalFallback, t as useLanguageSync } from "./useLanguageSync-C9ExgPoX.js";
import "./react-dom-D5lMhlFn.js";
import "./usePreference-DRNUEk4I.js";
import { r as mergeUiProps } from "./utils-Bnoyl1me.js";
import { t as require_jsx_runtime } from "./jsx-runtime-Cc0-uZGc.js";
import "./shim-2TzZzGeK.js";
import "./react-error-boundary-CnvE_Ln4.js";
import "./es2015-wfS3L7va.js";
import "./w3c-keyname-DKIRohbk.js";
import "./diff-KPuWBc9d.js";
import "./with-selector-DlsRhNV6.js";
import "./chunk-BO2N2NFS-fKJ-DR8B.js";
import "./extend-BDo4rIBl.js";
import "./marked.esm-5kn1DqGn.js";
import "./dist-Bsd0pttl.js";
import "./dist-oTn6Mzbo.js";
import "./useCodeStyle-hWHmowmT.js";
import "./useTheme-mM6gKAcD.js";
import "./shiki-g8Tr0-KC.js";
import { n as CodeStyleProvider, t as PopupHost } from "./PopupHost-ORVBzrBR.js";
import "./command-CyHQabGE.js";
import { o as CommandContextKeyProvider, t as CommandProvider } from "./command-B9oiyMDG.js";
import "./useCloseBeforeAction-DvYaYpLC.js";
import "./ipc-DpcwPFwy.js";
import { t as ErrorBoundaryCustomized } from "./ErrorBoundary-CrY8yFbh.js";
import "./tab-CVOgL8bf.js";
import "./useWindowInitData-BzKs7Qq9.js";
import "./mainWindowNavigation-Dpzd_Ttr.js";
import { n as cn } from "./style-BQVh98fR.js";
import "./CacheService-IyXh62g9.js";
import "./useCache-lF6Sw_ck.js";
import "./file-OKCzlHoD.js";
import { r as toFileUrl } from "./file-CkrjUGO_.js";
import "./model-BOGgSmTN.js";
import "./aiSdk-C-csCQz7.js";
import "./DataApiService-Csr1w5Kb.js";
import "./useDataApi-H7ZhyZ_J.js";
import "./useReorder-Cmki1Ln4.js";
import { t as toast } from "./toast-D2efAzAF.js";
import "./popup-fJcKiA2S.js";
import "./mcp-BGt1L-d_.js";
import "./label-CSb71add.js";
import "./systemProviderId-B4QvwwvR.js";
import "./provider-0IejLFjL.js";
import "./naming-BVJSQooI.js";
import "./image-DyuN5EBc.js";
import { i as useSessions } from "./useSession-CwZkOUrY.js";
import "./conversationEntry-CTSSFR4L.js";
import "./agentRuntimeCapabilities-C7UvdsI7.js";
import "./provider-bQl8RVRp.js";
import { o as isToolUIPart, r as getToolName } from "./dist-CK6lZPpu.js";
import "./toolOutput-DFxKz6Jf.js";
import "./uiParts-ClY38h-2.js";
import "./with-selector-DFVq3c2Y.js";
import "./filePreview-CGUDTTTP.js";
import "./customCssMigration-CzAWBFG-.js";
import { t as ToastHost } from "./ToastHost-IGq0t1_z.js";
import "./filePath-SVkx1Fle.js";
import "./constants-BF9E3O0o.js";
import "./useExecutionOverlay-Cx6lCsHu.js";
import { i as useModels } from "./useModel-kU1hKaYa.js";
import "./useProvider-DvntuFRN.js";
import "./capabilities-DNV_QUgI.js";
import "./useTopicStreamStatus-Be-jC8fI.js";
import "./react-hotkeys-hook.esm-Bq09MseZ.js";
import "./useTimer-CtD1TRRj.js";
import { a as readCocoMode } from "./cocoAgent-Dkf7ljK5.js";
import { n as useAgents, r as useUpdateAgent } from "./useAgent-9Zqveu03.js";
import { t as AiPipelinePage } from "./AiPipelinePage-BdD5AGyH.js";
import { S as pipelineSlashQuickPanelFields, s as usePipelineNodeCatalog } from "./tokenView-yvehtvx6.js";
import "./file-B-bjOfog.js";
import { l as readCocoSessionAssets, o as cocoSessionAssetPromptText } from "./composerTokens-DYa0FU5N.js";
import "./composerTokenPolicy-BR1gNZTA.js";
import "./BracesVariableIcon-zDcWRqre.js";
import "./FallbackFavicon-Cm9QCLm8.js";
import "./ImagePreviewService-B71uVdEp.js";
import "./__vite-browser-external-DlQ9XxoO.js";
import "./composerDraft-CEGp3JAk.js";
import { r as sessionAssetToComposerAttachment, t as useAgentChatRuntimeState } from "./useAgentChatRuntimeState-mC0wdYXb.js";
import { t as useAvailableSkills } from "./useSkills-Be92aMP5.js";
import { t as buildFilePartsForAttachments } from "./buildFileParts-69zcS5WM.js";
import "./group-Cqj37oRb.js";
import "./knowledge-8Me8AnnG.js";
import { et as APPROVAL_REQUESTED, tt as APPROVAL_RESPONDED } from "./agent-e68nHjCJ.js";
import "./ToolBlockGroup-DmvbWNSS.js";
import "./MessagePartsContext-Be-3g0sl.js";
import "./ScrollOwnershipContext-Cz9Tjpos.js";
import "./iconify-C6gOqm1j.js";
import "./SvgIcon-DI_kUaAV.js";
import "./CodeViewer-DvhZTSjd.js";
import "./CopyIcon-XfJhVEu-.js";
import "./useTemporaryValue-CJVn11gD.js";
import "./ImageViewer-B8XAqqwe.js";
import "./useMcpServer-BA8oDtqe.js";
import "./Link-0PkeiIUY.js";
import "./MarqueeText-DgzQ8dVh.js";
import "./Scrollbar-CjwT5bYS.js";
import "./useAgentSessionParts-CXWIKKtJ.js";
import "./useConversationHistoryQuery-C8JSfBsH.js";
import { t as toComposerAttachment } from "./composerAttachment-BB5cm1a7.js";
var import_client = require_client();
var import_react = /* @__PURE__ */ __toESM(require_react());
var TRIGGER_RE = /(^|[\s\n])([/@])([^\s]*)$/;
function readLiteMentionTrigger(text, caret) {
	const before = text.slice(0, Math.max(0, Math.min(caret, text.length)));
	const match = before.match(TRIGGER_RE);
	if (!match || match.index === void 0) return null;
	const prefix = match[1];
	return {
		char: match[2],
		query: match[3] ?? "",
		start: match.index + prefix.length,
		end: before.length
	};
}
function filterLiteMentions(items, query) {
	const needle = query.trim().toLowerCase().replace(/^[/@]+/, "");
	if (!needle) return [...items];
	return items.filter((item) => {
		return `${item.label} ${item.insert} ${item.description ?? ""} ${item.search ?? ""} ${item.group}`.toLowerCase().includes(needle);
	});
}
function applyLiteMention(text, trigger, insert) {
	const token = insert.endsWith(" ") || insert.length === 0 ? insert : `${insert} `;
	return {
		text: `${text.slice(0, trigger.start)}${token}${text.slice(trigger.end)}`,
		caret: trigger.start + token.length
	};
}
function removeLiteMentionTrigger(text, trigger) {
	return {
		text: `${text.slice(0, trigger.start)}${text.slice(trigger.end)}`,
		caret: trigger.start
	};
}
var import_jsx_runtime = /* @__PURE__ */ __toESM(require_jsx_runtime());
function Icon(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
		"data-ui": "dcc-panel.icon",
		viewBox: "0 0 24 24",
		width: "16",
		height: "16",
		fill: "none",
		stroke: "currentColor",
		strokeWidth: "1.8",
		...mergeUiProps(props, "dcc-panel.icon")
	});
}
function ClockIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Icon, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", {
		cx: "12",
		cy: "12",
		r: "8"
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M12 8v5l3 2" })] });
}
function SparkIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
		"data-ui": "dcc-panel.spark-icon",
		viewBox: "0 0 24 24",
		width: "14",
		height: "14",
		fill: "currentColor",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M12 2.5 13.8 8.4 20 10.2 13.8 12 12 17.9 10.2 12 4 10.2 10.2 8.4 12 2.5Z" })
	});
}
function ModelMark() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Icon, {
		width: "14",
		height: "14",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", {
			cx: "12",
			cy: "12",
			r: "3.5"
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M12 5v2M12 17v2M5 12h2M17 12h2" })]
	});
}
function ChevronIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		width: "14",
		height: "14",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M6 9l6 6 6-6" })
	});
}
function McpMark() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
		"data-ui": "dcc-panel.mcp-mark",
		viewBox: "0 0 24 24",
		width: "18",
		height: "18",
		fill: "none",
		stroke: "currentColor",
		strokeWidth: "1.7",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M7 7c4-4 6 4 10 0" }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M7 12c4-4 6 4 10 0" }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M7 17c4-4 6 4 10 0" })
		]
	});
}
function CopyIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Icon, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", {
		x: "8",
		y: "8",
		width: "10",
		height: "12",
		rx: "2"
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M6 16V6a2 2 0 0 1 2-2h8" })] });
}
function UpIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Icon, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M7 11v8h3v-8z" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M10 11 12.5 5a2 2 0 0 1 3.8 1.2L15.5 11H19a2 2 0 0 1 1.9 2.6l-1.4 4A3 3 0 0 1 16.6 20H10" })] });
}
function DownIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", {
		transform: "rotate(180 12 12)",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M7 11v8h3v-8z" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M10 11 12.5 5a2 2 0 0 1 3.8 1.2L15.5 11H19a2 2 0 0 1 1.9 2.6l-1.4 4A3 3 0 0 1 16.6 20H10" })]
	}) });
}
function InfoIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Icon, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", {
		cx: "12",
		cy: "12",
		r: "8"
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M12 11v5M12 8h.01" })] });
}
function GlobeIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Icon, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", {
		cx: "12",
		cy: "12",
		r: "8"
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M4 12h16M12 4c2.5 3 2.5 13 0 16M12 4c-2.5 3-2.5 13 0 16" })] });
}
function ClipIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M8 12.5V8a4 4 0 0 1 8 0v8a3 3 0 0 1-6 0V9" }) });
}
function SendIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M4 12 20 5l-6 14-2.5-6.5L4 12Z" }) });
}
function StopIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", {
		x: "7",
		y: "7",
		width: "10",
		height: "10",
		rx: "1.5",
		fill: "currentColor",
		stroke: "none"
	}) });
}
function CloseIcon() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {
		width: "12",
		height: "12",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M6 6l12 12M18 6 6 18" })
	});
}
function IconBtn({ children, label, onClick, pressed }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		"data-ui": "dcc-panel.icon-btn",
		type: "button",
		className: pressed ? "icon-btn is-on" : "icon-btn",
		"aria-label": label,
		"aria-pressed": pressed,
		onClick,
		children
	});
}
function approvalLabel(status) {
	switch (status) {
		case "pending": return "";
		case "allowed":
		case "running": return "已允许";
		case "done": return "已完成";
		case "denied": return "已拒绝该工具";
		case "error": return "工具执行失败";
		default: return status;
	}
}
function isImageMedia(mediaType, name) {
	if (mediaType?.startsWith("image/")) return true;
	return !!name && /\.(png|jpe?g|webp|gif|bmp|tif|tiff|exr)$/i.test(name);
}
function FileChips({ files }) {
	if (files.length === 0) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "dcc-panel.file-chips",
		className: "file-chips",
		children: files.map((file) => isImageMedia(file.mediaType, file.name) && file.previewUrl ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
			className: "file-thumb",
			src: file.previewUrl,
			alt: file.name
		}, file.name) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: "file-chip",
			children: file.name
		}, file.name))
	});
}
function AssetCard({ asset, importLabel, importing, onImport }) {
	const preview = asset.previewUrl;
	const showImage = preview && isImageMedia(asset.assetType, asset.name);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "dcc-panel.asset-card",
		className: "asset-card",
		children: [
			showImage ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
				className: "asset-preview",
				src: preview,
				alt: asset.name
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "asset-preview is-model",
				children: asset.assetType || "asset"
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "asset-meta",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: asset.name }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("small", { children: asset.assetType || "pipeline" })]
			}),
			importLabel && onImport ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				className: "btn btn-allow asset-import",
				disabled: importing,
				onClick: () => onImport(asset),
				children: importing ? "导入中…" : `导入到 ${importLabel}`
			}) : null
		]
	});
}
var FILE_ACCEPT = "image/*,video/*,.png,.jpg,.jpeg,.webp,.exr,.hdr,.pdf,.glb,.gltf,.fbx,.obj,.abc,.usd,.usda,.usdc,.hip,.hiplc";
function AgentChatLiteView({ mode, title = "智能助手", subtitle = "有任何关于场景、节点或流水线的问题，直接用中文问我。", agentName, items, sessions, activeSessionId, draft, eventsOpen, reviewOpenId, historyOpen, isPending, isLoading, errorText, emptyText = "开始提问，Agent 会在需要时弹出工具确认。", fineprint, canCreateSession, attachments = [], modes = [], modeId, models = [], modelId, webSearchOn = false, importLabel = "", toolsHint = "DCC 工具已随当前场景自动挂载，无需手动添加。", slashItems = [], fileItems = [], onDraftChange, onSend, onSelectMention, onStop, onCopy, onAllow, onDeny, onSelectSession, onCreateSession, onToggleEvents, onToggleHistory, onToggleReview, onPickFiles, onRemoveAttachment, onToggleWebSearch, onSelectMode, onSelectModel, onImportAsset }) {
	const threadRef = (0, import_react.useRef)(null);
	const fileInputRef = (0, import_react.useRef)(null);
	const textareaRef = (0, import_react.useRef)(null);
	const pendingCaretRef = (0, import_react.useRef)(null);
	const [caret, setCaret] = (0, import_react.useState)(0);
	const [mentionIndex, setMentionIndex] = (0, import_react.useState)(0);
	const [dismissedStart, setDismissedStart] = (0, import_react.useState)(null);
	const [busyApprovalId, setBusyApprovalId] = (0, import_react.useState)(null);
	const [importingId, setImportingId] = (0, import_react.useState)(null);
	const [feedback, setFeedback] = (0, import_react.useState)(null);
	const [picker, setPicker] = (0, import_react.useState)(null);
	const [toolsOpen, setToolsOpen] = (0, import_react.useState)(false);
	const [dragging, setDragging] = (0, import_react.useState)(false);
	const canSend = (draft.trim().length > 0 || attachments.length > 0) && !isPending;
	const eventSummary = items.filter((item) => item.kind === "approval").map((item) => item.toolName).join(" → ") || "等待回复";
	const selectedMode = modes.find((item) => item.id === modeId) ?? modes[0];
	const selectedModel = models.find((item) => item.id === modelId);
	const modelLabel = selectedModel?.label || selectedModel?.id || "模型";
	(0, import_react.useEffect)(() => {
		const node = threadRef.current;
		if (node) node.scrollTop = node.scrollHeight;
	}, [
		items,
		eventsOpen,
		reviewOpenId,
		isPending,
		attachments.length
	]);
	(0, import_react.useEffect)(() => {
		setBusyApprovalId(null);
	}, [items]);
	const respond = (item, approved) => {
		setBusyApprovalId(item.id);
		if (approved) onAllow?.(item);
		else onDeny?.(item);
	};
	const takeFiles = (list) => {
		if (!list || !onPickFiles) return;
		const files = Array.from(list).filter((file) => file.size > 0);
		if (files.length) onPickFiles(files);
	};
	const onDrop = (event) => {
		event.preventDefault();
		setDragging(false);
		takeFiles(event.dataTransfer.files);
	};
	const trigger = (0, import_react.useMemo)(() => readLiteMentionTrigger(draft, caret), [caret, draft]);
	const mentionSource = trigger?.char === "/" ? slashItems : trigger?.char === "@" ? fileItems : [];
	const mentionItems = (0, import_react.useMemo)(() => trigger ? filterLiteMentions(mentionSource, trigger.query).slice(0, 16) : [], [mentionSource, trigger]);
	const mentionOpen = Boolean(trigger && trigger.start !== dismissedStart && (mentionSource.length > 0 || trigger.query.length > 0));
	(0, import_react.useEffect)(() => {
		setMentionIndex(0);
	}, [
		trigger?.char,
		trigger?.query,
		trigger?.start
	]);
	(0, import_react.useEffect)(() => {
		const pos = pendingCaretRef.current;
		if (pos == null) return;
		pendingCaretRef.current = null;
		const node = textareaRef.current;
		if (!node) return;
		node.focus();
		node.setSelectionRange(pos, pos);
		setCaret(pos);
	}, [draft]);
	const syncCaret = () => {
		const node = textareaRef.current;
		if (node) setCaret(node.selectionStart);
	};
	const pickMention = (item) => {
		if (!trigger) return;
		if (item.action === "pick-file") {
			const next$1 = removeLiteMentionTrigger(draft, trigger);
			pendingCaretRef.current = next$1.caret;
			onDraftChange(next$1.text);
			fileInputRef.current?.click();
			return;
		}
		const next = applyLiteMention(draft, trigger, item.insert);
		pendingCaretRef.current = next.caret;
		onDraftChange(next.text);
		onSelectMention?.(item);
	};
	const onComposerKeyDown = (event) => {
		if (mentionOpen) {
			if (event.key === "ArrowDown") {
				event.preventDefault();
				setMentionIndex((index) => mentionItems.length === 0 ? 0 : (index + 1) % mentionItems.length);
				return;
			}
			if (event.key === "ArrowUp") {
				event.preventDefault();
				setMentionIndex((index) => mentionItems.length === 0 ? 0 : (index - 1 + mentionItems.length) % mentionItems.length);
				return;
			}
			if (event.key === "Escape") {
				event.preventDefault();
				setDismissedStart(trigger?.start ?? null);
				return;
			}
			if ((event.key === "Enter" || event.key === "Tab") && mentionItems[mentionIndex]) {
				event.preventDefault();
				pickMention(mentionItems[mentionIndex]);
				return;
			}
			if (event.key === "Enter" && !event.shiftKey) {
				event.preventDefault();
				return;
			}
		}
		if (event.key === "Enter" && !event.shiftKey) {
			event.preventDefault();
			if (canSend) onSend();
		}
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "dcc-panel.agent-chat-lite-view",
		className: `dcc-agent-lite ${mode === "page" ? "is-page" : "is-embed"}${dragging ? " is-drop" : ""}`,
		onDragOver: (event) => {
			if (!onPickFiles) return;
			event.preventDefault();
			setDragging(true);
		},
		onDragLeave: () => setDragging(false),
		onDrop,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
				ref: fileInputRef,
				type: "file",
				hidden: true,
				multiple: true,
				accept: FILE_ACCEPT,
				onChange: (event) => {
					takeFiles(event.target.files);
					event.target.value = "";
				}
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: "panel",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("header", {
					className: "hero",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "hero-title",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "live" }), title]
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: subtitle })]
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
					className: "card",
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "card-top",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconBtn, {
								label: "历史会话",
								onClick: () => onToggleHistory(true),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ClockIcon, {})
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "thread",
							ref: threadRef,
							children: [
								errorText ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
									className: "error-hint",
									children: errorText
								}) : null,
								!errorText && isLoading && items.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
									className: "empty-hint",
									children: "正在连接对话服务…"
								}) : null,
								!errorText && !isLoading && items.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
									className: "empty-hint",
									children: emptyText
								}) : null,
								items.map((item, index) => {
									const meta = item.kind !== "user" && (index === 0 || items[index - 1]?.kind === "user") ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: "agent-meta",
										children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SparkIcon, {}), agentName] }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
											"data-ui": "dcc-panel.agent-chat-lite-view.action.toggle",
											type: "button",
											className: "ghost",
											onClick: onToggleEvents,
											children: ["响应事件", /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronIcon, {})]
										})]
									}), eventsOpen ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
										className: "agent-copy",
										children: eventSummary
									}) : null] }) : null;
									if (item.kind === "user") return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: "user-bubble",
										children: [item.text ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: item.text }) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FileChips, { files: item.files || [] })]
									}, item.id);
									if (item.kind === "agent") return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [meta, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
										className: "agent-copy",
										children: item.text
									})] }, item.id);
									if (item.kind === "asset") return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [meta, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AssetCard, {
										asset: item.asset,
										importLabel,
										importing: importingId === item.asset.assetId,
										onImport: onImportAsset ? (asset) => {
											setImportingId(asset.assetId);
											onImportAsset(asset);
											window.setTimeout(() => setImportingId(null), 800);
										} : void 0
									})] }, item.id);
									const busy = busyApprovalId === item.id;
									const pillClass = item.status === "denied" || item.status === "error" ? "no" : item.status === "pending" ? "run" : "ok";
									return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [meta, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: "mcp-card",
										children: [
											/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
												className: "mcp-mark",
												children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(McpMark, {})
											}),
											/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h3", { children: "工具确认" }), item.status === "pending" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: item.description }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
												className: `status-pill ${pillClass}`,
												children: [
													approvalLabel(item.status),
													" ",
													item.toolName
												]
											})] }),
											item.status === "pending" ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
												className: "mcp-actions",
												children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
													type: "button",
													className: "btn btn-deny",
													disabled: busy,
													onClick: () => respond(item, false),
													children: "拒绝"
												}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
													type: "button",
													className: "btn btn-allow",
													disabled: busy,
													onClick: () => respond(item, true),
													children: "允许"
												})]
											}) : null,
											/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
												type: "button",
												className: "ghost review",
												onClick: () => onToggleReview(reviewOpenId === item.id ? null : item.id),
												children: ["查看参数", /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronIcon, {})]
											}),
											reviewOpenId === item.id ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
												className: "review-body",
												children: [
													"工具 ",
													/* @__PURE__ */ (0, import_jsx_runtime.jsx)("code", { children: item.toolName }),
													item.inputText ? `\n${item.inputText}` : ""
												]
											}) : null
										]
									})] }, item.id);
								}),
								isPending ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
									className: "agent-copy",
									children: "正在思考…"
								}) : null,
								items.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
									className: "feedback",
									children: [
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconBtn, {
											label: "复制",
											onClick: onCopy,
											children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyIcon, {})
										}),
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconBtn, {
											label: "有用",
											pressed: feedback === "up",
											onClick: () => setFeedback((value) => value === "up" ? null : "up"),
											children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UpIcon, {})
										}),
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconBtn, {
											label: "没用",
											pressed: feedback === "down",
											onClick: () => setFeedback((value) => value === "down" ? null : "down"),
											children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DownIcon, {})
										})
									]
								}) : null
							]
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: "composer-wrap",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
									className: "composer-head",
									children: [agentName, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconBtn, {
										label: "工具说明",
										pressed: toolsOpen,
										onClick: () => setToolsOpen((open) => !open),
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InfoIcon, {})
									})]
								}),
								toolsOpen ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
									className: "tools-hint",
									children: toolsHint
								}) : null,
								attachments.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: "attach-row",
									children: attachments.map((file) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
										className: "attach-chip",
										children: [
											isImageMedia(file.mediaType, file.name) && file.previewUrl ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
												src: file.previewUrl,
												alt: ""
											}) : null,
											/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: file.name }),
											/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
												type: "button",
												"aria-label": `移除 ${file.name}`,
												onClick: () => onRemoveAttachment?.(file.id),
												children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CloseIcon, {})
											})
										]
									}, file.id))
								}) : null,
								/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
									className: "composer",
									children: [
										mentionOpen ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
											"data-ui": "dcc-panel.agent-chat-lite-view.listbox",
											className: "mention-menu",
											role: "listbox",
											"aria-label": trigger?.char === "@" ? "标记文件" : "插入节点 Skill 或 MCP",
											children: mentionItems.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
												className: "mention-empty",
												children: "没有匹配项"
											}) : mentionItems.map((item, index) => {
												return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [index === 0 || item.group !== mentionItems[index - 1]?.group ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
													className: "mention-group",
													children: item.group
												}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
													type: "button",
													className: index === mentionIndex ? "active" : "",
													onMouseDown: (event) => event.preventDefault(),
													onMouseEnter: () => setMentionIndex(index),
													onClick: () => pickMention(item),
													children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: item.label }), item.description ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("small", { children: item.description }) : null]
												})] }, item.id);
											})
										}) : null,
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)("textarea", {
											ref: textareaRef,
											rows: 2,
											value: draft,
											placeholder: "输入 / 插入画布节点、Skill、MCP，@ 标记文件",
											onChange: (event) => {
												onDraftChange(event.target.value);
												setCaret(event.target.selectionStart);
												setDismissedStart(null);
											},
											onClick: syncCaret,
											onKeyUp: syncCaret,
											onSelect: syncCaret,
											onPaste: (event) => {
												const files = event.clipboardData?.files;
												if (files && files.length > 0 && onPickFiles) {
													event.preventDefault();
													takeFiles(files);
												}
											},
											onKeyDown: onComposerKeyDown
										}),
										/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
											className: "composer-bar",
											children: [
												/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
													className: "chip-row",
													children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
														className: "chip-wrap",
														children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
															type: "button",
															className: "chip",
															"aria-label": "Agent 模式",
															disabled: !onSelectMode || modes.length === 0,
															onClick: () => setPicker((open) => open === "mode" ? null : "mode"),
															children: [
																/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SparkIcon, {}),
																/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
																	className: "chip-text",
																	children: selectedMode?.label || "代理"
																}),
																onSelectMode && modes.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronIcon, {}) : null
															]
														}), picker === "mode" && modes.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
															"data-ui": "dcc-panel.agent-chat-lite-view.listbox",
															className: "model-menu is-mode",
															role: "listbox",
															"aria-label": "Agent 模式",
															children: modes.map((item) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
																type: "button",
																className: item.id === (modeId || selectedMode?.id) ? "active" : "",
																onClick: () => {
																	onSelectMode?.(item.id);
																	setPicker(null);
																},
																children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: item.label }), item.description ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("small", { children: item.description }) : null]
															}, item.id))
														}) : null]
													}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
														className: "chip-wrap",
														children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
															type: "button",
															className: "chip chip-model",
															"aria-label": "模型",
															disabled: !onSelectModel || models.length === 0,
															onClick: () => setPicker((open) => open === "model" ? null : "model"),
															children: [
																/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelMark, {}),
																/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
																	className: "chip-text",
																	children: modelLabel
																}),
																onSelectModel && models.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronIcon, {}) : null
															]
														}), picker === "model" && models.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
															"data-ui": "dcc-panel.agent-chat-lite-view.listbox",
															className: "model-menu",
															role: "listbox",
															"aria-label": "模型",
															children: models.map((item) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
																type: "button",
																className: item.id === modelId ? "active" : "",
																onClick: () => {
																	onSelectModel?.(item.id);
																	setPicker(null);
																},
																children: item.label
															}, item.id))
														}) : null]
													})]
												}),
												/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
													className: "bar-icons",
													children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconBtn, {
														label: "联网搜索",
														pressed: webSearchOn,
														onClick: onToggleWebSearch,
														children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobeIcon, {})
													}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconBtn, {
														label: "附件",
														onClick: () => fileInputRef.current?.click(),
														children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ClipIcon, {})
													})]
												}),
												isPending ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
													"data-ui": "dcc-panel.agent-chat-lite-view.action.stop",
													type: "button",
													className: "send stop",
													"aria-label": "停止",
													onClick: onStop,
													children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StopIcon, {})
												}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
													"data-ui": "dcc-panel.agent-chat-lite-view.action.send",
													type: "button",
													className: canSend ? "send ready" : "send",
													"aria-label": "发送",
													disabled: !canSend,
													onClick: onSend,
													children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SendIcon, {})
												})
											]
										})
									]
								})
							]
						}),
						fineprint ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "fineprint",
							children: fineprint
						}) : null
					]
				})]
			}),
			dragging ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "drop-hint",
				children: "放到这里，作为本轮附件发给 Agent"
			}) : null,
			historyOpen ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "drawer-mask",
				onClick: () => onToggleHistory(false),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "drawer",
					onClick: (event) => {
						event.stopPropagation();
					},
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", { children: "会话" }),
						sessions.map((session) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
							type: "button",
							className: session.id === activeSessionId ? "session active" : "session",
							onClick: () => {
								onSelectSession(session.id);
								onToggleHistory(false);
							},
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("b", { children: session.title }), session.time ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("small", { children: session.time }) : null]
						}, session.id)),
						canCreateSession ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
							"data-ui": "dcc-panel.agent-chat-lite-view.action.create",
							type: "button",
							className: "new-session",
							onClick: onCreateSession,
							children: "新建会话"
						}) : null
					]
				})
			}) : null
		]
	});
}
const DCC_LITE_MODES = [
	{
		id: "agent",
		label: "代理",
		description: "改场景、跑节点、导入产物"
	},
	{
		id: "plan",
		label: "规划",
		description: "只规划方案，不改场景"
	},
	{
		id: "ask",
		label: "问答",
		description: "只读提问，不调用写工具"
	},
	{
		id: "debug",
		label: "调试",
		description: "排查失败节点和参数"
	},
	{
		id: "multitask",
		label: "多任务",
		description: "并行协调多个画布任务"
	}
];
const LITE_PICK_FILE_ITEM = {
	id: "pick-file",
	kind: "pick",
	label: "选择本地文件…",
	insert: "",
	description: "从磁盘标记为附件",
	group: "文件",
	action: "pick-file"
};
function buildDccLiteSlashItems(options) {
	const nodes = (options.nodes ?? []).map((node) => {
		const fields = pipelineSlashQuickPanelFields(node);
		return {
			id: `node:${node.node_id}`,
			kind: "node",
			label: fields.label,
			insert: fields.slashId,
			description: fields.slashId,
			group: "画布节点",
			search: `${node.node_id} ${node.tags.join(" ")} ${node.description}`
		};
	});
	const skills = (options.skills ?? []).map((skill) => ({
		id: `skill:${skill.filename}`,
		kind: "skill",
		label: skill.name,
		insert: `Use the ${skill.name} skill.`,
		description: skill.description || skill.filename,
		group: "Skill",
		search: skill.filename
	}));
	const tools = (options.mcpTools ?? []).map((tool) => ({
		id: `mcp:${tool.name}`,
		kind: "mcp",
		label: tool.name,
		insert: `/mcp:${tool.name}`,
		description: tool.description || "MCP 工具",
		group: "MCP 工具"
	}));
	const servers = tools.length > 0 ? [] : (options.mcpServers ?? []).map((server) => ({
		id: `mcp-server:${server}`,
		kind: "mcp",
		label: server,
		insert: `/mcp:${server}`,
		description: "MCP 服务",
		group: "MCP 工具"
	}));
	return [
		...nodes,
		...skills,
		...tools,
		...servers
	];
}
function buildDccLiteFileItems(assets = []) {
	return [LITE_PICK_FILE_ITEM, ...assets.map((asset) => ({
		id: `file:${asset.assetId}`,
		kind: "file",
		label: asset.name,
		insert: cocoSessionAssetPromptText(asset),
		description: asset.caption || "本会话资产",
		group: "文件",
		search: `${asset.assetId} ${asset.origin ?? ""}`,
		payload: { assetId: asset.assetId }
	}))];
}
function textFromParts(parts) {
	return parts.flatMap((part) => part.type === "text" && "text" in part && part.text ? [part.text] : []).join("").trim();
}
function formatInput(input) {
	if (input == null) return "";
	if (typeof input === "string") return input;
	try {
		return JSON.stringify(input, null, 2);
	} catch {
		return String(input);
	}
}
function statusFromToolState(state) {
	switch (state) {
		case APPROVAL_REQUESTED: return "pending";
		case "output-denied":
		case "cancelled": return "denied";
		case APPROVAL_RESPONDED:
		case "input-available":
		case "input-streaming": return "running";
		case "output-error": return "error";
		case "output-available": return "done";
		default: return "allowed";
	}
}
function isFilePart(part) {
	return part.type === "file";
}
function isPipelineAssetPart(part) {
	return part.type === "data-pipeline-asset" && "data" in part && !!part.data;
}
function fileChip(part) {
	return {
		name: part.filename || "附件",
		mediaType: part.mediaType,
		previewUrl: part.url
	};
}
function projectAgentLiteItems(messages, partsByMessageId) {
	const items = [];
	for (const message of messages) {
		const parts = partsByMessageId[message.id] ?? message.parts ?? [];
		if (message.role === "user") {
			const text$1 = textFromParts(parts);
			const files = parts.filter(isFilePart).map(fileChip);
			if (text$1 || files.length > 0) items.push(files.length > 0 ? {
				kind: "user",
				id: message.id,
				text: text$1,
				files
			} : {
				kind: "user",
				id: message.id,
				text: text$1
			});
			continue;
		}
		if (message.role !== "assistant") continue;
		const text = textFromParts(parts);
		if (text) items.push({
			kind: "agent",
			id: `${message.id}-text`,
			text
		});
		for (const [index, part] of parts.entries()) {
			if (isFilePart(part)) {
				items.push({
					kind: "asset",
					id: `${message.id}-file-${index}`,
					asset: {
						assetId: `${message.id}-file-${index}`,
						name: part.filename || "生成文件",
						assetType: part.mediaType || "file",
						previewUrl: part.url,
						downloadUrl: part.url
					}
				});
				continue;
			}
			if (isPipelineAssetPart(part)) {
				items.push({
					kind: "asset",
					id: `${message.id}-asset-${part.data.assetId || index}`,
					asset: {
						assetId: part.data.assetId,
						name: part.data.name,
						assetType: part.data.assetType,
						previewUrl: part.data.previewUrl,
						downloadUrl: part.data.downloadUrl,
						localPath: part.data.localPath
					}
				});
				continue;
			}
			if (!isToolUIPart(part)) continue;
			const toolName = getToolName(part) || "tool";
			const approvalId = part.approval?.id;
			const status = statusFromToolState(part.state);
			items.push({
				kind: "approval",
				id: `${message.id}-tool-${part.toolCallId || index}`,
				toolName,
				description: `允许 Agent 执行 ${toolName}`,
				inputText: formatInput(part.input),
				status,
				match: approvalId ? {
					part,
					state: part.state ?? "",
					toolCallId: part.toolCallId,
					messageId: message.id,
					approvalId,
					input: part.input
				} : null
			});
		}
	}
	return items;
}
function lastAgentText(items) {
	for (let index = items.length - 1; index >= 0; index -= 1) {
		const item = items[index];
		if (item.kind === "agent") return item.text;
	}
	return "";
}
var EMPTY_MESSAGES = [];
function formatQueryError(error) {
	if (!error) return "";
	if (error instanceof Error) return error.message;
	return String(error);
}
function idleLiteView(errorText, onRetry) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AgentChatLiteView, {
		mode: "embed",
		agentName: "Agent",
		items: [],
		sessions: [],
		activeSessionId: null,
		draft: "",
		eventsOpen: false,
		reviewOpenId: null,
		historyOpen: false,
		isPending: false,
		errorText,
		emptyText: "开始提问，Agent 会在需要时弹出工具确认。",
		onDraftChange: () => void 0,
		onSend: () => onRetry?.(),
		onSelectSession: () => void 0,
		onToggleEvents: () => void 0,
		onToggleHistory: () => void 0,
		onToggleReview: () => void 0
	});
}
function DccAgentChatFallback({ error, resetErrorBoundary }) {
	return idleLiteView(formatQueryError(error) || "对话服务暂时不可用", resetErrorBoundary);
}
function dccImportLabel() {
	const dcc = window.__CHERRY_DCC_TYPE || "";
	if (!dcc || dcc === "standalone" || dcc === "dcc") return "";
	if (dcc === "houdini") return "Houdini";
	if (dcc === "maya") return "Maya";
	return dcc;
}
function shortModelLabel(modelId) {
	if (!modelId) return "";
	const parts = modelId.split(/[/:]/);
	return parts[parts.length - 1] || modelId;
}
function attachmentPreview(file) {
	if (file.previewUrl) return file.previewUrl;
	if (file.path && file.type === "image") return toFileUrl(file.path);
}
async function ingestBrowserFiles(files) {
	const api = window.api?.file;
	if (!api?.createTempFile || !api.write || !api.get) throw new Error("当前窗口还不能落盘附件，请稍后重试");
	const next = [];
	for (const file of files) {
		const existingPath = api.getPathForFile?.(file);
		if (existingPath) {
			const selected$1 = await api.get(existingPath);
			if (selected$1) next.push(toComposerAttachment(selected$1));
			continue;
		}
		const tempPath = await api.createTempFile(file.name);
		await api.write(tempPath, new Uint8Array(await file.arrayBuffer()));
		const selected = await api.get(tempPath);
		if (selected) next.push(toComposerAttachment(selected));
	}
	return next;
}
async function importAssetToDcc(asset) {
	const win = window;
	const origin = win.__CHERRY_BACKEND_URL?.replace(/\/$/, "") || "";
	const sessionId = win.__CHERRY_SESSION_ID || "";
	if (!origin || !sessionId) throw new Error("当前窗口没有绑定 DCC 会话");
	let localPath = asset.localPath || "";
	if (!localPath && asset.downloadUrl) {
		const response = await fetch(asset.downloadUrl);
		if (!response.ok) throw new Error(`下载产物失败 ${response.status}`);
		const bytes = new Uint8Array(await response.arrayBuffer());
		const tempPath = await window.api.file.createTempFile(asset.name);
		await window.api.file.write(tempPath, bytes);
		localPath = tempPath;
	}
	if (!localPath) throw new Error("产物还没有本地路径");
	const data = await (await fetch(`${origin}/api/v1/dcc/import`, {
		method: "POST",
		headers: {
			"Content-Type": "application/json",
			"X-Session-Id": sessionId
		},
		body: JSON.stringify({
			sessionId,
			localPath
		})
	})).json();
	if (data?.error) throw new Error(data.error);
}
function formatSessionTime(value) {
	const then = Date.parse(value);
	if (!Number.isFinite(then)) return "";
	const delta = Date.now() - then;
	if (delta < 6e4) return "刚刚";
	if (delta < 36e5) return `${Math.max(1, Math.floor(delta / 6e4))} 分钟前`;
	if (delta < 864e5) return `${Math.max(1, Math.floor(delta / 36e5))} 小时前`;
	return new Date(then).toLocaleDateString();
}
function useDccMcpTools() {
	const [tools, setTools] = (0, import_react.useState)([]);
	(0, import_react.useEffect)(() => {
		const win = window;
		const origin = win.__CHERRY_BACKEND_URL?.replace(/\/$/, "") || "";
		const sessionId = win.__CHERRY_SESSION_ID || "";
		if (!origin || !sessionId) return;
		let cancelled = false;
		fetch(`${origin}/api/v1/mcp/list-dcc-tools?sessionId=${encodeURIComponent(sessionId)}`, { headers: { "X-Session-Id": sessionId } }).then(async (response) => await response.json()).then((data) => {
			if (cancelled) return;
			setTools((data.tools ?? []).filter((tool) => typeof tool.name === "string" && tool.name.trim()).map((tool) => ({
				name: tool.name.trim(),
				description: tool.description
			})));
		}).catch(() => {
			if (!cancelled) setTools([]);
		});
		return () => {
			cancelled = true;
		};
	}, []);
	return tools;
}
function DccAgentChatSession({ sessionId, agentId, agentName, modeId, models, modelId, onSelectMode, onSelectModel, sessions, onSelectSession, onCreateSession, creating, configuration, mcps }) {
	const runtime = useAgentChatRuntimeState({
		sessionId,
		sessionMessagesEnabled: true,
		reservedMessages: EMPTY_MESSAGES
	});
	const [draft, setDraft] = (0, import_react.useState)("");
	const [eventsOpen, setEventsOpen] = (0, import_react.useState)(false);
	const [reviewOpenId, setReviewOpenId] = (0, import_react.useState)(null);
	const [historyOpen, setHistoryOpen] = (0, import_react.useState)(false);
	const [sendError, setSendError] = (0, import_react.useState)("");
	const [files, setFiles] = (0, import_react.useState)([]);
	const [webSearchOn, setWebSearchOn] = (0, import_react.useState)(false);
	const { nodes } = usePipelineNodeCatalog(true);
	const { skills } = useAvailableSkills(agentId, void 0, { enabled: true });
	const mcpTools = useDccMcpTools();
	const sessionAssets = (0, import_react.useMemo)(() => readCocoSessionAssets(configuration, sessionId), [configuration, sessionId]);
	const slashItems = (0, import_react.useMemo)(() => buildDccLiteSlashItems({
		nodes,
		skills,
		mcpTools,
		mcpServers: mcps
	}), [
		mcpTools,
		mcps,
		nodes,
		skills
	]);
	const fileItems = (0, import_react.useMemo)(() => buildDccLiteFileItems(sessionAssets), [sessionAssets]);
	const items = (0, import_react.useMemo)(() => projectAgentLiteItems(runtime.uiMessages, runtime.partsByMessageId), [runtime.partsByMessageId, runtime.uiMessages]);
	const liteAttachments = (0, import_react.useMemo)(() => files.map((file) => ({
		id: file.fileTokenSourceId,
		name: file.origin_name || file.name,
		mediaType: file.type === "image" ? "image/png" : void 0,
		previewUrl: attachmentPreview(file)
	})), [files]);
	const send = (0, import_react.useCallback)(async () => {
		const text = draft.trim();
		if (!text && files.length === 0 || runtime.isPending) return;
		const outgoingText = webSearchOn && text ? `${text}\n\n请在需要时使用联网搜索。` : text;
		setDraft("");
		setSendError("");
		try {
			const fileParts = files.length > 0 ? await buildFilePartsForAttachments(files) : [];
			setFiles([]);
			const parts = [...outgoingText ? [{
				type: "text",
				text: outgoingText
			}] : [], ...fileParts];
			await runtime.sendMessage({ text: outgoingText }, { body: {
				agentId,
				sessionId,
				userMessageParts: parts
			} });
		} catch (error) {
			setDraft(text);
			setSendError(error instanceof Error ? error.message : String(error));
		}
	}, [
		agentId,
		draft,
		files,
		runtime,
		sessionId,
		webSearchOn
	]);
	const respond = (0, import_react.useCallback)(async (item, approved) => {
		if (!item.match) return;
		setSendError("");
		try {
			await runtime.respondToolApproval({
				match: item.match,
				approved,
				reason: approved ? void 0 : "User denied tool execution"
			});
		} catch (error) {
			setSendError(error instanceof Error ? error.message : String(error));
		}
	}, [runtime]);
	const copy = (0, import_react.useCallback)(() => {
		const text = lastAgentText(items);
		if (!text) return;
		navigator.clipboard?.writeText(text);
	}, [items]);
	const selectMention = (0, import_react.useCallback)((item) => {
		const assetId = item.payload?.assetId;
		if (item.kind !== "file" || !assetId) return;
		const asset = sessionAssets.find((entry) => entry.assetId === assetId);
		if (!asset) return;
		const attachment = sessionAssetToComposerAttachment(asset);
		setFiles((current) => current.some((file) => file.pipelineAssetId === asset.assetId) ? current : [...current, attachment]);
	}, [sessionAssets]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AgentChatLiteView, {
		mode: "embed",
		agentName: agentName.toUpperCase(),
		items,
		sessions,
		activeSessionId: sessionId,
		draft,
		eventsOpen,
		reviewOpenId,
		historyOpen,
		isPending: runtime.isPending,
		isLoading: runtime.isLoading,
		errorText: sendError,
		canCreateSession: !creating,
		onDraftChange: setDraft,
		onSend: () => void send(),
		onStop: () => void runtime.stop(),
		onCopy: copy,
		onAllow: (item) => void respond(item, true),
		onDeny: (item) => void respond(item, false),
		onSelectSession,
		onCreateSession,
		onToggleEvents: () => setEventsOpen((open) => !open),
		onToggleHistory: setHistoryOpen,
		onToggleReview: setReviewOpenId,
		attachments: liteAttachments,
		modes: DCC_LITE_MODES,
		modeId,
		models,
		modelId,
		webSearchOn,
		importLabel: dccImportLabel(),
		slashItems,
		fileItems,
		onPickFiles: (picked) => {
			ingestBrowserFiles(picked).then((next) => setFiles((current) => [...current, ...next])).catch((error) => setSendError(error instanceof Error ? error.message : String(error)));
		},
		onRemoveAttachment: (id) => setFiles((current) => current.filter((file) => file.fileTokenSourceId !== id)),
		onToggleWebSearch: () => setWebSearchOn((value) => !value),
		onSelectMode,
		onSelectModel,
		onSelectMention: selectMention,
		onImportAsset: (asset) => {
			importAssetToDcc(asset).then(() => toast.success(`已导入到 ${dccImportLabel() || "DCC"}`)).catch((error) => toast.error(error instanceof Error ? error.message : String(error)));
		}
	});
}
function DccAgentChatBody() {
	const { agents, isLoading: agentsLoading, error: agentsError } = useAgents();
	const { updateAgent, updateModel } = useUpdateAgent();
	const { models } = useModels({ enabled: true });
	const agent = (0, import_react.useMemo)(() => {
		const cocoAgents = agents.filter((item) => item.type === "coco");
		return (cocoAgents.length > 0 ? cocoAgents : agents)[0];
	}, [agents]);
	const { sessions, createSession, isLoading: sessionsLoading, error: sessionsError } = useSessions(agent?.id, {
		enabled: !!agent?.id,
		pageSize: 20
	});
	const [activeSessionId, setActiveSessionId] = (0, import_react.useState)(null);
	const [errorText, setErrorText] = (0, import_react.useState)("");
	const creatingRef = (0, import_react.useRef)(false);
	const [creating, setCreating] = (0, import_react.useState)(false);
	const modeId = readCocoMode(agent?.configuration);
	const modelId = agent?.model || "";
	const liteModels = (0, import_react.useMemo)(() => {
		const options = models.map((item) => ({
			id: item.id,
			label: item.name?.trim() || shortModelLabel(item.id)
		}));
		if (modelId && !options.some((item) => item.id === modelId)) options.unshift({
			id: modelId,
			label: shortModelLabel(modelId)
		});
		return options;
	}, [modelId, models]);
	const handleSelectMode = (0, import_react.useCallback)((id) => {
		if (!agent?.id || id === modeId) return;
		updateAgent({
			id: agent.id,
			configuration: { coco_mode: id }
		}, { showSuccessToast: false });
	}, [
		agent?.id,
		modeId,
		updateAgent
	]);
	const handleSelectModel = (0, import_react.useCallback)((id) => {
		if (!agent?.id || id === modelId) return;
		updateModel({
			agentId: agent.id,
			modelId: id
		}, { showSuccessToast: false });
	}, [
		agent?.id,
		modelId,
		updateModel
	]);
	const liteSessions = (0, import_react.useMemo)(() => sessions.map((session) => ({
		id: session.id,
		title: session.name.trim() || "新对话",
		time: formatSessionTime(session.lastActivityAt || session.updatedAt)
	})), [sessions]);
	(0, import_react.useEffect)(() => {
		if (activeSessionId && sessions.some((session) => session.id === activeSessionId)) return;
		if (sessions[0]) setActiveSessionId(sessions[0].id);
	}, [activeSessionId, sessions]);
	const handleCreateSession = (0, import_react.useCallback)(async () => {
		if (!agent?.id || creatingRef.current) return;
		creatingRef.current = true;
		setCreating(true);
		setErrorText("");
		try {
			const created = await createSession({
				name: "",
				workspace: { type: "system" }
			});
			if (created) setActiveSessionId(created.id);
			else setErrorText("创建会话失败");
		} finally {
			creatingRef.current = false;
			setCreating(false);
		}
	}, [agent?.id, createSession]);
	(0, import_react.useEffect)(() => {
		if (agentsLoading || sessionsLoading || !agent?.id || sessionsError) return;
		if (sessions.length === 0 && !creatingRef.current) handleCreateSession();
	}, [
		agent?.id,
		agentsLoading,
		handleCreateSession,
		sessions.length,
		sessionsError,
		sessionsLoading
	]);
	const queryError = errorText || formatQueryError(agentsError) || formatQueryError(sessionsError);
	if (!agentsLoading && !agent) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AgentChatLiteView, {
		mode: "embed",
		agentName: "Agent",
		items: [],
		sessions: [],
		activeSessionId: null,
		draft: "",
		eventsOpen: false,
		reviewOpenId: null,
		historyOpen: false,
		isPending: false,
		errorText: queryError || "未找到 Agent。请先在 Cherry Studio 创建一个 Coco Agent。",
		onDraftChange: () => void 0,
		onSend: () => void 0,
		onSelectSession: () => void 0,
		onToggleEvents: () => void 0,
		onToggleHistory: () => void 0,
		onToggleReview: () => void 0
	});
	if (!activeSessionId) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AgentChatLiteView, {
		mode: "embed",
		agentName: agent?.name || "Agent",
		items: [],
		sessions: liteSessions,
		activeSessionId: null,
		draft: "",
		eventsOpen: false,
		reviewOpenId: null,
		historyOpen: false,
		isPending: false,
		isLoading: agentsLoading || sessionsLoading || creating,
		errorText: queryError,
		emptyText: "正在连接对话服务…",
		canCreateSession: !!agent?.id && !creating,
		modes: DCC_LITE_MODES,
		modeId,
		models: liteModels,
		modelId,
		onDraftChange: () => void 0,
		onSend: () => void 0,
		onSelectSession: setActiveSessionId,
		onCreateSession: () => void handleCreateSession(),
		onToggleEvents: () => void 0,
		onToggleHistory: () => void 0,
		onToggleReview: () => void 0,
		onSelectMode: handleSelectMode,
		onSelectModel: handleSelectModel
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DccAgentChatSession, {
		sessionId: activeSessionId,
		agentId: agent?.id || "",
		agentName: agent?.name || "Agent",
		modeId,
		models: liteModels,
		modelId,
		onSelectMode: handleSelectMode,
		onSelectModel: handleSelectModel,
		sessions: liteSessions,
		creating,
		onSelectSession: setActiveSessionId,
		onCreateSession: () => void handleCreateSession(),
		configuration: agent?.configuration,
		mcps: agent?.mcps
	});
}
function DccAgentChat() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, {
		fallbackComponent: DccAgentChatFallback,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DccAgentChatBody, {})
	});
}
function runtimeInfo() {
	const win = window;
	return {
		backendUrl: (win.__CHERRY_BACKEND_URL || "").replace(/\/$/, ""),
		sessionId: win.__CHERRY_SESSION_ID || "",
		dccType: win.__CHERRY_DCC_TYPE || "dcc"
	};
}
function selectedLabel(node) {
	if (typeof node === "string") return node;
	return node.path || node.name || node.type || "?";
}
function readBoundDccSession(agent) {
	return agent.configuration?.coco_dcc?.sessionId || "";
}
function useBindDccAgentSession(dccSessionId, dccType) {
	const { agents, isLoading } = useAgents();
	const { updateAgent } = useUpdateAgent();
	const inflightRef = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!dccSessionId || isLoading) return;
		const cocoAgents = agents.filter((agent) => agent.type === "coco");
		const next = (cocoAgents.length > 0 ? cocoAgents : agents).find((agent) => readBoundDccSession(agent) !== dccSessionId);
		if (!next || inflightRef.current === next.id) return;
		inflightRef.current = next.id;
		updateAgent({
			id: next.id,
			configuration: { coco_dcc: {
				sessionId: dccSessionId,
				dccType
			} }
		}, { showSuccessToast: false }).finally(() => {
			if (inflightRef.current === next.id) inflightRef.current = null;
		});
	}, [
		agents,
		dccSessionId,
		dccType,
		isLoading,
		updateAgent
	]);
}
function DccPanel() {
	const { backendUrl, sessionId: dccSessionId, dccType } = runtimeInfo();
	const [tab, setTab] = (0, import_react.useState)("chat");
	const [ctx, setCtx] = (0, import_react.useState)(null);
	useBindDccAgentSession(dccSessionId, dccType);
	(0, import_react.useEffect)(() => {
		if (!backendUrl || !dccSessionId) return;
		let cancelled = false;
		const pull = async () => {
			try {
				const data = await (await fetch(`${backendUrl}/api/v1/dcc/context?sessionId=${encodeURIComponent(dccSessionId)}`)).json();
				if (!cancelled) setCtx(data);
			} catch (error) {
				if (!cancelled) setCtx({
					available: false,
					error: String(error),
					dccType
				});
			}
		};
		pull();
		const timer = window.setInterval(() => void pull(), 2e3);
		return () => {
			cancelled = true;
			window.clearInterval(timer);
		};
	}, [
		backendUrl,
		dccSessionId,
		dccType
	]);
	const fileLabel = ctx?.hipFile || ctx?.scenePath || "未打开文件";
	const selected = (ctx?.selectedNodes || []).slice(0, 4).map(selectedLabel);
	const displayDcc = ctx?.dccType || ctx?.dcc || dccType;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "dcc-panel",
		className: "flex h-full min-h-0 w-full flex-col bg-background text-foreground",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("header", {
			className: "flex h-9 shrink-0 items-center gap-2 border-b border-border px-2",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("nav", {
					className: "flex shrink-0 gap-0.5",
					children: [["chat", "对话"], ["canvas", "画布"]].map(([id, label]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						type: "button",
						className: cn("rounded-md px-2.5 py-1 text-sm", tab === id ? "bg-muted font-medium" : "text-muted-foreground hover:bg-muted/60"),
						onClick: () => setTab(id),
						children: label
					}, id))
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "min-w-0 flex-1 truncate text-xs text-muted-foreground",
					children: [
						displayDcc,
						ctx?.dccVersion ? ` ${ctx.dccVersion}` : "",
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "px-1",
							children: "·"
						}),
						fileLabel
					]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex max-w-[40%] flex-wrap justify-end gap-1",
					children: [selected.map((label) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "rounded-full bg-muted px-2 py-0.5 text-[11px]",
						children: label
					}, label)), ctx?.available === false ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "text-[11px] text-destructive",
						children: ctx.error || "DCC 未连接"
					}) : null]
				})
			]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: "relative flex min-h-0 flex-1 overflow-hidden",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: cn("flex h-full min-h-0 min-w-0 flex-1", tab !== "chat" && "hidden"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DccAgentChat, {})
			}), tab === "canvas" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AiPipelinePage, {}) : null]
		})]
	});
}
function DccPanelRuntime() {
	useLanguageSync();
	useCustomCss();
	(0, import_react.useEffect)(() => {
		document.getElementById("spinner")?.remove();
	}, []);
	return null;
}
function DccPanelApp() {
	if (!resolver_default.isInitialized) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundaryCustomized, {
		fallbackComponent: WindowFatalFallback,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ThemeProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CodeStyleProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandContextKeyProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DccPanelRuntime, {}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DccPanel, {}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopupHost, {}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToastHost, {})
		] }) }) }) }) })
	});
}
var DccPanelApp_default = DccPanelApp;
await prepareWindow({
	preference: [
		"app.language",
		"ui.custom_css",
		"ui.theme_mode",
		"ui.theme_user.color_primary",
		"ui.window_style"
	],
	preferenceTimeoutMs: 2500
});
(0, import_client.createRoot)(document.getElementById("root")).render(/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DccPanelApp_default, {}));
