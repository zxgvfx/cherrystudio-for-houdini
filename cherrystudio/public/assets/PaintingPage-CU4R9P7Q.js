import { s as __toESM } from "./chunk-DiqNceaa.js";
import { t as loggerService } from "./LoggerService-oVV4iwe6.js";
import { m as aiErrorDetail } from "./error-3V5V4Mev.js";
import { o as cacheService, t as useCache } from "./useCache-SsOQx-L2.js";
import { n as omitBy, t as isUndefined } from "./isUndefined-CZjb9k0E.js";
import { t as head } from "./head-D3xX9ybW.js";
import { c as withoutTrailingSlash } from "./format-BU2M4Rvo.js";
import { r as resolver_default } from "./resolver-eUld2ti5.js";
import { t as instance } from "./i18next-DBAWv9hQ.js";
import { t as require_react } from "./react-BgPOU4At.js";
import { t as useTranslation } from "./useTranslation-DnuRkr5k.js";
import { n as usePreference } from "./usePreference-yBX61WcV.js";
import { i as UiDataSlot, r as cn } from "./dist-BGahvVh8.js";
import { t as require_jsx_runtime } from "./jsx-runtime-DCB_IiL2.js";
import { n as AvatarFallback, t as Avatar } from "./avatar-BMK8th-L.js";
import { a as SelectLabel, c as SelectValue, i as SelectItem, n as SelectContent, r as SelectGroup, s as SelectTrigger, t as Select } from "./select-BPh2DI-f.js";
import { t as Button } from "./button-Db6_VSWw.js";
import { n as Tooltip, t as NormalTooltip } from "./tooltip-ZuayyV11.js";
import { n as motion } from "./react-DY3ULJV9.js";
import { A as useComposerToolDispatch, D as ComposerToolRuntimeHost, F as ComposerToolDerivedStateProvider, J as QuickPanelProvider, N as useComposerToolLauncherVersion, O as ComposerToolRuntimeProvider, P as useComposerToolState, R as fileToComposerToken, T as ComposerToolbarControls, Y as useReducedMotion, b as COMPOSER_SELECTOR_BUTTON_CLASS, j as useComposerToolLauncherActions, k as useComposerTokenReconcile, n as getComposerToolConfig, r as ComposerSurface, t as HorizontalScrollContainer_default } from "./HorizontalScrollContainer-Duvqb1_x.js";
import { n as Switch } from "./switch-DEX3m-Zc.js";
import { t as ConfirmDialog } from "./confirm-dialog-B5-JWpzh.js";
import { n as RadioGroupItem, t as RadioGroup } from "./radio-group-BLEiejOL.js";
import { t as Input } from "./input-dvr72LyA.js";
import { i as PopoverTrigger, r as PopoverContent, t as Popover } from "./popover-n5iWPIgn.js";
import { a as RowFlex } from "./flex-BmIgOnUt.js";
import { t as Input$1 } from "./textarea-Djv1FqZD.js";
import { t as InfoTooltip } from "./info-tooltip-B4gcegN9.js";
import { t as Slider } from "./slider-_Xxahv5d.js";
import { n as ipcApi } from "./ipc-BDTAufGC.js";
import { r as openSettingsTab } from "./mainWindowNavigation-m8-JFOdz.js";
import { n as cn$1 } from "./style-qqUWb85F.js";
import { t as AbsoluteFilePathSchema } from "./file-KsLXrn8b.js";
import { i as toSafeFileUrl, m as imageExts, r as toFileUrl, u as getFileTypeByExt } from "./file-DzPAqnYr.js";
import { _ as ENDPOINT_TYPE, a as isUniqueModelId, h as buildParamsSchema, i as createUniqueModelId, o as parseUniqueModelId, v as MODALITY, y as MODEL_CAPABILITY } from "./model-CfoN7z8F.js";
import { t as dataApiService } from "./DataApiService-DP44jQXR.js";
import { c as useQuery, i as useInfiniteQuery, o as useMutation, r as useInfiniteFlatItems, t as prefetch } from "./useDataApi-DhDD9bgI.js";
import { t as useReorder } from "./useReorder-CHjMnht2.js";
import { t as toast } from "./toast-DsSiWKrR.js";
import { t as ChevronDown } from "./chevron-down-BVtofl6V.js";
import { t as ImageDown } from "./image-down-WSuFTQWq.js";
import { t as ImageUp } from "./image-up-B8KfVGoI.js";
import { t as LoaderCircle } from "./loader-circle-CyzyZXyF.js";
import { t as Palette } from "./palette-CWHPG6GL.js";
import { t as Plus } from "./plus-DvXnghWX.js";
import { t as RefreshCcw } from "./refresh-ccw-n8_3N9Vc.js";
import { t as RotateCcwSquare } from "./rotate-ccw-square-DpJg2I8A.js";
import { t as RotateCcw } from "./rotate-ccw-BS5J7gIn.js";
import { t as RotateCwSquare } from "./rotate-cw-square-Bz8eV6cA.js";
import { t as Settings2 } from "./settings-2-BTluMuWp.js";
import { t as Trash2 } from "./trash-2-_PaXFSXZ.js";
import { t as X } from "./x-CZLr1OkN.js";
import { t as ZoomIn } from "./zoom-in-CyXlkrIh.js";
import { t as ZoomOut } from "./zoom-out-q-aeGunZ.js";
import { t as popup } from "./popup-C0FVl5N5.js";
import { t as useIcon } from "./use-icon-BeENEiSO.js";
import { u as getModelLogoRef } from "./model-3irbiX6r.js";
import { u as getImageBlobFromSource } from "./image-Bb803VSe.js";
import { t as uuid } from "./uuid-Z_-TZRDQ.js";
import { i as useModels } from "./useModel-wPmUe3o8.js";
import { c as useProviderPreset, i as useProviderApiKeys, l as useProviders, n as useProvider, t as getProviderDisplayName } from "./useProvider-BIst-GY7.js";
import { a as isEditImageModel } from "./model-Z1svQByC.js";
import { s as getProviderDisplayName$1, t as ModelSelector } from "./ModelSelector-Dwt-BrMn.js";
import { t as joinPath } from "./path-CxRPiBbQ.js";
import { t as CopyButton_default } from "./CopyButton-cq35cwKK.js";
import { d as FILE_TYPE } from "./file-AcqqP-OX.js";
import { l as createComposerFileTokenSourceId, s as ImageViewer_default } from "./tokenView-DU2XPw-v.js";
import { t as useBundledCatalog } from "./useBundledCatalog-B1h8EHfw.js";
import { u as toComposerAttachments } from "./composerClipboard-q_YG-zvr.js";
import { t as DEFAULT_API_FEATURES } from "./provider-CCv2sQOu.js";
var KEY_LABELS = {
	size: { title: "paintings.image.size" },
	numImages: {
		title: "paintings.number_images",
		tooltip: "paintings.number_images_tip"
	},
	aspectRatio: { title: "paintings.aspect_ratio" },
	imageResolution: { title: "paintings.image.size" },
	customSize: { title: "paintings.custom_size" },
	negativePrompt: {
		title: "paintings.negative_prompt",
		tooltip: "paintings.negative_prompt_tip"
	},
	seed: {
		title: "paintings.seed",
		tooltip: "paintings.seed_tip"
	},
	promptEnhancement: {
		title: "paintings.prompt_enhancement",
		tooltip: "paintings.prompt_enhancement_tip"
	},
	promptExtend: {
		title: "paintings.prompt_enhancement",
		tooltip: "paintings.prompt_enhancement_tip"
	},
	thinkingMode: {
		title: "paintings.thinking_mode",
		tooltip: "paintings.thinking_mode_tip"
	},
	magicPromptOption: { title: "paintings.magic_prompt_option" },
	addWatermark: { title: "paintings.watermark" },
	outputFormat: { title: "paintings.ppio.output_format" },
	outputCompression: { title: "paintings.output_compression" },
	quality: { title: "paintings.quality" },
	resolution: { title: "paintings.image.size" },
	moderation: { title: "paintings.moderation" },
	background: { title: "paintings.background" },
	styleType: {
		title: "paintings.style_type",
		tooltip: "paintings.style_type_tip"
	},
	style: {
		title: "paintings.style_type",
		tooltip: "paintings.style_type_tip"
	},
	renderingSpeed: { title: "paintings.rendering_speed" },
	personGeneration: {
		title: "paintings.person_generation",
		tooltip: "paintings.person_generation_tip"
	},
	numInferenceSteps: {
		title: "paintings.inference_steps",
		tooltip: "paintings.inference_steps_tip"
	},
	guidanceScale: {
		title: "paintings.guidance_scale",
		tooltip: "paintings.guidance_scale_tip"
	},
	cfg: {
		title: "paintings.guidance_scale",
		tooltip: "paintings.guidance_scale_tip"
	},
	safetyTolerance: {
		title: "paintings.safety_tolerance",
		tooltip: "paintings.safety_tolerance_tip"
	},
	imageWeight: { title: "paintings.image_weight" },
	resemblance: { title: "paintings.upscale.resemblance" },
	detail: { title: "paintings.upscale.detail" },
	refStrength: { title: "paintings.dashscope.ref_strength" },
	refMode: { title: "paintings.dashscope.ref_mode" },
	enableInterleave: {
		title: "paintings.dashscope.enable_interleave",
		tooltip: "paintings.dashscope.enable_interleave_tip"
	},
	sourceLang: { title: "paintings.dashscope.source_lang" },
	targetLang: { title: "paintings.dashscope.target_lang" },
	function: { title: "paintings.dashscope.function" },
	strength: { title: "paintings.dashscope.strength" },
	upscaleFactor: { title: "paintings.dashscope.upscale_factor" },
	topScale: { title: "paintings.dashscope.top_scale" },
	bottomScale: { title: "paintings.dashscope.bottom_scale" },
	leftScale: { title: "paintings.dashscope.left_scale" },
	rightScale: { title: "paintings.dashscope.right_scale" },
	isSketch: { title: "paintings.dashscope.is_sketch" },
	sequentialImageGeneration: { title: "paintings.dmxapi.sequential_image_generation" },
	maxImages: { title: "paintings.dmxapi.max_images" }
};
var SIZE_AUTO_OPTION = { auto: "paintings.image_size_options.auto" };
var OPTION_LABELS = {
	size: SIZE_AUTO_OPTION,
	aspectRatio: SIZE_AUTO_OPTION,
	imageResolution: SIZE_AUTO_OPTION,
	quality: {
		auto: "paintings.quality_options.auto",
		low: "paintings.quality_options.low",
		medium: "paintings.quality_options.medium",
		high: "paintings.quality_options.high",
		standard: "paintings.quality_options.standard",
		hd: "paintings.quality_options.hd"
	},
	renderingSpeed: {
		DEFAULT: "paintings.rendering_speeds.default",
		TURBO: "paintings.rendering_speeds.turbo",
		QUALITY: "paintings.rendering_speeds.quality"
	},
	personGeneration: {
		DONT_ALLOW: "paintings.person_generation_options.allow_none",
		ALLOW_ADULT: "paintings.person_generation_options.allow_adult",
		ALLOW_ALL: "paintings.person_generation_options.allow_all"
	},
	background: {
		auto: "paintings.background_options.auto",
		transparent: "paintings.background_options.transparent",
		opaque: "paintings.background_options.opaque"
	},
	moderation: {
		auto: "paintings.moderation_options.auto",
		low: "paintings.moderation_options.low"
	},
	styleType: {
		AUTO: "paintings.style_type_options.auto",
		GENERAL: "paintings.style_type_options.general",
		REALISTIC: "paintings.style_type_options.realistic",
		DESIGN: "paintings.style_type_options.design",
		RENDER_3D: "paintings.style_type_options.render_3d",
		ANIME: "paintings.style_type_options.anime"
	},
	sequentialImageGeneration: {
		auto: "paintings.dmxapi.sequential_image_generation_options.auto",
		disabled: "paintings.dmxapi.sequential_image_generation_options.disabled"
	},
	refMode: {
		repaint: "paintings.dashscope.ref_mode_options.repaint",
		refonly: "paintings.dashscope.ref_mode_options.refonly"
	},
	style: {
		vivid: "paintings.style_options.vivid",
		natural: "paintings.style_options.natural",
		"<auto>": "paintings.style_options.auto",
		"<photography>": "paintings.style_options.photography",
		"<portrait>": "paintings.style_options.portrait",
		"<3d cartoon>": "paintings.style_options.cartoon_3d",
		"<anime>": "paintings.style_options.anime",
		"<oil painting>": "paintings.style_options.oil_painting",
		"<watercolor>": "paintings.style_options.watercolor",
		"<sketch>": "paintings.style_options.sketch",
		"<chinese painting>": "paintings.style_options.chinese_painting",
		"<flat illustration>": "paintings.style_options.flat_illustration"
	},
	function: {
		stylization_all: "paintings.dashscope.function_options.stylization_all",
		stylization_local: "paintings.dashscope.function_options.stylization_local",
		description_edit: "paintings.dashscope.function_options.description_edit",
		description_edit_with_mask: "paintings.dashscope.function_options.description_edit_with_mask",
		remove_watermark: "paintings.dashscope.function_options.remove_watermark",
		expand: "paintings.dashscope.function_options.expand",
		super_resolution: "paintings.dashscope.function_options.super_resolution",
		colorization: "paintings.dashscope.function_options.colorization",
		doodle: "paintings.dashscope.function_options.doodle",
		control_cartoon_feature: "paintings.dashscope.function_options.control_cartoon_feature"
	}
};
function toOptions(key, values) {
	const labelMap = OPTION_LABELS[key];
	return values.map((v) => {
		const labelKey = labelMap?.[v];
		return labelKey ? {
			labelKey,
			value: v
		} : {
			label: v,
			value: v
		};
	});
}
function specToField(key, spec, allSupports) {
	const labels = KEY_LABELS[key] ?? { title: key };
	switch (spec.type) {
		case "switch": return {
			type: "switch",
			key,
			...labels,
			initialValue: spec.default ?? false
		};
		case "text": return spec.multiline ? {
			type: "textarea",
			key,
			...labels
		} : {
			type: "input",
			key,
			...labels
		};
		case "range": {
			const item = {
				type: "slider",
				key,
				...labels,
				min: spec.min,
				max: spec.max,
				initialValue: spec.default ?? spec.min
			};
			if (spec.step !== void 0) item.step = spec.step;
			return item;
		}
		case "enum": {
			const renderAsChips = spec.render === "chips";
			const customSizeSpec = allSupports.customSize;
			const pairedSize = (customSizeSpec?.type === "size" ? customSizeSpec.pairedEnumKey ?? "size" : void 0) === key;
			const options = toOptions(key, spec.options);
			if (pairedSize) {
				const customOption = {
					labelKey: "paintings.custom_size",
					value: "custom"
				};
				const customIndex = options.findIndex((option) => option.value === "custom");
				if (customIndex >= 0) options[customIndex] = customOption;
				else options.push(customOption);
			}
			if (renderAsChips) return {
				type: "sizeChips",
				key,
				...labels,
				options,
				initialValue: spec.default,
				columns: spec.columns ?? 3
			};
			return {
				type: "select",
				key,
				...labels,
				options,
				initialValue: spec.default
			};
		}
		case "size": {
			const pairedKey = spec.pairedEnumKey;
			return {
				type: "customSize",
				key,
				widthKey: `${key}_width`,
				heightKey: `${key}_height`,
				sizeKey: pairedKey ?? "size",
				validation: {
					minWidth: spec.minSide,
					maxWidth: spec.maxSide,
					minHeight: spec.minSide,
					maxHeight: spec.maxSide
				},
				condition: pairedKey ? (painting) => painting[pairedKey] === "custom" : void 0
			};
		}
		default: return spec;
	}
}
function imageGenerationToFields(support, opts) {
	const allModes = support?.modes;
	if (!allModes) return [];
	const requested = opts?.mode ?? "generate";
	const fallbackKey = Object.keys(allModes)[0];
	const supports = allModes[requested]?.supports ?? (fallbackKey ? allModes[fallbackKey]?.supports : void 0);
	if (!supports) return [];
	const items = [];
	for (const [key, spec] of Object.entries(supports)) {
		const item = specToField(key, spec, supports);
		if (item) items.push(item);
	}
	return items;
}
function resolveOptions(item, painting, translate) {
	return (typeof item.options === "function" ? item.options(item, painting) : item.options ?? []).map((option) => ({
		...option,
		label: option.labelKey ? translate(option.labelKey) : option.label
	}));
}
function parseRatio(value) {
	const dims = value.match(/^(\d+)[x×](\d+)$/);
	if (dims) return {
		w: Number(dims[1]),
		h: Number(dims[2])
	};
	const aspect = value.match(/^(?:ASPECT_)?(\d+)[_:](\d+)$/i);
	if (aspect) return {
		w: Number(aspect[1]),
		h: Number(aspect[2])
	};
	return null;
}
function parseDims(s) {
	const m = s.match(/^(\d+)\s*[x×]\s*(\d+)$/);
	return m ? {
		w: Number(m[1]),
		h: Number(m[2])
	} : null;
}
function formatDims({ w, h }) {
	return `${w}×${h}`;
}
function splitParens(label) {
	const m = label.match(/^(.*?)\s*[(（]([^)）]+)[)）]\s*$/);
	return m ? {
		head: m[1].trim(),
		inner: m[2].trim()
	} : {
		head: label.trim(),
		inner: ""
	};
}
function deriveChipLabel(label, value) {
	const aspectMatch = value.match(/^(?:ASPECT_)?(\d+)[_:](\d+)$/i);
	if (aspectMatch) return `${Number(aspectMatch[1])}:${Number(aspectMatch[2])}`;
	const { head: head$1, inner } = splitParens(label);
	if (parseDims(inner)) return head$1;
	const dims = parseDims(value);
	if (dims) return formatDims(dims);
	return label;
}
function sizeOptionLabel(item, value, params, translate) {
	return deriveChipLabel(resolveOptions(item, params ?? {}, translate).find((option) => String(option.value) === value)?.label ?? value, value);
}
const SIZE_PREVIEW_KEYS = [
	"size",
	"imageResolution",
	"aspectRatio"
];
function resolveRatio(params, items) {
	let sawAuto = false;
	for (const item of items) {
		if (!item.key || !SIZE_PREVIEW_KEYS.includes(item.key)) continue;
		if (item.condition && !item.condition(params ?? {})) continue;
		const value = params?.[item.key] ?? item.initialValue;
		if (value === "custom") {
			const customWidth = Number(params?.customSize_width);
			const customHeight = Number(params?.customSize_height);
			if (customWidth > 0 && customHeight > 0) return customWidth / customHeight;
			continue;
		}
		if (typeof value !== "string") continue;
		if (value === "auto") {
			sawAuto = true;
			continue;
		}
		const dim = parseRatio(value);
		if (dim && dim.w > 0 && dim.h > 0) return dim.w / dim.h;
	}
	return sawAuto ? 1 : null;
}
function resolveSizeLabel(params, items, translate) {
	for (const item of items) {
		if (!item.key || !SIZE_PREVIEW_KEYS.includes(item.key)) continue;
		if (item.condition && !item.condition(params ?? {})) continue;
		const value = params?.[item.key] ?? item.initialValue;
		if (value === "custom") {
			const customWidth = Number(params?.customSize_width);
			const customHeight = Number(params?.customSize_height);
			return customWidth > 0 && customHeight > 0 ? `${customWidth}×${customHeight}` : void 0;
		}
		if (typeof value !== "string" || value === "") continue;
		return sizeOptionLabel(item, value, params, translate);
	}
}
function tabToImageGenerationMode(dbMode) {
	if (dbMode === "generate" || dbMode === "draw") return "generate";
	if (dbMode === "edit") return "edit";
	if (dbMode === "remix") return "remix";
	if (dbMode === "upscale") return "upscale";
	if (dbMode === "merge") return "merge";
}
function useImageGenerationSupport(providerId, modelId) {
	const { data } = useQuery("/providers/:providerId/models/:modelId*/image-generation-support", {
		params: {
			providerId: providerId ?? "__none__",
			modelId: modelId ?? "__none__"
		},
		enabled: Boolean(providerId && modelId)
	});
	return data ?? void 0;
}
var import_react = /* @__PURE__ */ __toESM(require_react());
function usePaintingSizeInfo(painting) {
	const { t } = useTranslation();
	const registrySupport = useImageGenerationSupport(painting.providerId, painting.model);
	const configItems = (0, import_react.useMemo)(() => imageGenerationToFields(registrySupport, { mode: tabToImageGenerationMode(painting.mode) }), [registrySupport, painting.mode]);
	return {
		ratio: (0, import_react.useMemo)(() => resolveRatio(painting.params, configItems), [painting.params, configItems]),
		sizeLabel: (0, import_react.useMemo)(() => resolveSizeLabel(painting.params, configItems, t), [
			painting.params,
			configItems,
			t
		])
	};
}
const paintingClasses = {
	page: "flex h-full flex-1 flex-col",
	content: "flex min-h-0 flex-1 flex-col overflow-hidden bg-background",
	tabsWrap: "shrink-0 flex justify-center px-6 pt-3 pb-2",
	tabsList: "rounded-full border border-border-subtle bg-secondary p-1 shadow-sm backdrop-blur-sm",
	tabsTrigger: "rounded-full px-4 py-1.5 text-xs font-medium text-muted-foreground transition data-[state=active]:bg-background data-[state=active]:text-foreground data-[state=active]:shadow-sm",
	frame: "relative flex min-h-0 flex-1 overflow-hidden px-2 pt-2 pb-1",
	surface: "relative isolate flex min-w-0 flex-1 overflow-hidden rounded-none bg-background",
	centerPane: "relative flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden",
	centerStage: "flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden",
	promptDock: "relative z-20 shrink-0 bg-background px-2 pt-2 pb-2",
	historyStrip: "flex h-full w-[68px] shrink-0 flex-col gap-2 overflow-y-auto border-border-subtle border-r bg-background px-2 py-2 [scrollbar-width:none] [-ms-overflow-style:none] [&::-webkit-scrollbar]:hidden",
	historyAddButton: "sticky top-0 z-10 mb-1 flex h-11 w-11 shrink-0 items-center justify-center rounded-[12px] border border-dashed border-border-subtle bg-background text-muted-foreground hover:bg-secondary-hover hover:text-foreground",
	historyItem: "group relative flex h-11 w-11 shrink-0 items-center justify-center overflow-visible rounded-[12px] bg-secondary p-0 leading-none transition hover:bg-secondary-hover",
	historyItemActive: "bg-background",
	historyDelete: "absolute -top-1 -right-1 z-20 flex size-5 cursor-pointer items-center justify-center rounded-full border border-border-subtle bg-background/95 text-destructive opacity-0 shadow-sm transition group-hover:opacity-100",
	promptModeTabsList: "h-8 rounded-full border border-border-subtle bg-background-subtle p-0.5 shadow-none",
	promptModeTabsTrigger: "h-7 rounded-full px-2.5 text-xs text-muted-foreground data-[state=active]:bg-background data-[state=active]:text-foreground data-[state=active]:shadow-sm",
	promptWrap: "shrink-0 px-2 pb-4 pt-2",
	toolbarWrap: "absolute top-1/2 left-4 z-20 -translate-y-1/2",
	toolbarRail: "flex flex-col items-center gap-1 rounded-full border border-border-subtle bg-background/90 p-1 shadow-md backdrop-blur-xl",
	toolbarButton: "rounded-full text-muted-foreground hover:bg-muted/55 hover:text-foreground",
	toolbarButtonActive: "bg-muted text-foreground"
};
var logger$13 = loggerService.withContext("paintings/computeImageNaturalSize");
async function computeImageNaturalSize(src) {
	let bitmap = null;
	try {
		const blob = await getImageBlobFromSource(src);
		bitmap = await createImageBitmap(blob);
		return {
			naturalWidth: bitmap.width,
			naturalHeight: bitmap.height
		};
	} catch (error) {
		logger$13.warn("Failed to compute natural size for painting image", { error });
		return null;
	} finally {
		bitmap?.close();
	}
}
var logger$12 = loggerService.withContext("paintingFileUrl");
var warnedPaintingPaths = /* @__PURE__ */ new Set();
function getPaintingFileUrl(file) {
	if (!file.path) return void 0;
	const parsedPath = AbsoluteFilePathSchema.safeParse(file.path);
	if (!parsedPath.success) {
		if (!warnedPaintingPaths.has(file.path)) {
			warnedPaintingPaths.add(file.path);
			logger$12.warn("getPaintingFileUrl: non-canonical/invalid painting path", { path: file.path });
		}
		return;
	}
	const runtimeWindow = window;
	if (runtimeWindow.__CHERRY_BACKEND_URL) return `${runtimeWindow.__CHERRY_BACKEND_URL.replace(/\/$/, "")}/api/v1/files/raw-image?path=${encodeURIComponent(parsedPath.data)}`;
	return toSafeFileUrl(parsedPath.data, file.ext || null);
}
var import_jsx_runtime = /* @__PURE__ */ __toESM(require_jsx_runtime());
var logger$11 = loggerService.withContext("paintings/PaintingSkeletonSurface");
var CELL_SIZE = 14;
var CELL_GAP = 14;
var CELL_PITCH = CELL_SIZE + CELL_GAP;
var CELL_RADIUS = 3;
var BLINK_DURATION_MS = 2e3;
var CELL_FILL_DURATION = .5;
var CELL_FADE_DURATION_MS = 600;
var CELL_FADE_DELAY_MAX_MS = 300;
var IMAGE_REVEAL_DURATION = .9;
function cellNoise(index, salt) {
	const value = Math.sin(index * 12.9898 + salt * 78.233) * 43758.5453;
	return value - Math.floor(value);
}
async function downsampleCellColors(src, grid) {
	let bitmap = null;
	try {
		const blob = await getImageBlobFromSource(src);
		bitmap = await createImageBitmap(blob);
		const canvas = document.createElement("canvas");
		canvas.width = grid.cols;
		canvas.height = grid.rows;
		const context = canvas.getContext("2d", { willReadFrequently: true });
		if (!context) {
			logger$11.warn("Failed to acquire 2d canvas context for painting loader");
			return null;
		}
		context.drawImage(bitmap, 0, 0, grid.cols, grid.rows);
		const { data } = context.getImageData(0, 0, grid.cols, grid.rows);
		return Array.from({ length: grid.cols * grid.rows }, (_, index) => {
			const offset = index * 4;
			return `rgb(${data[offset]}, ${data[offset + 1]}, ${data[offset + 2]})`;
		});
	} catch (error) {
		logger$11.warn("Failed to sample image colors for painting loader", { error });
		return null;
	} finally {
		bitmap?.close();
	}
}
var Cell = (0, import_react.memo)(({ cell, color, fading, ready, reduceMotion }) => {
	const style = ready ? {
		animationName: "none",
		backgroundColor: color ?? "var(--muted-foreground)",
		borderRadius: CELL_RADIUS,
		height: CELL_PITCH,
		left: cell.x - CELL_GAP / 2,
		opacity: fading ? 0 : 1,
		position: "absolute",
		top: cell.y - CELL_GAP / 2,
		transition: fading ? `opacity ${CELL_FADE_DURATION_MS}ms ease` : [
			`background-color ${CELL_FILL_DURATION}s ease`,
			`height ${CELL_FILL_DURATION}s ease`,
			`left ${CELL_FILL_DURATION}s ease`,
			`opacity ${CELL_FILL_DURATION}s ease`,
			`top ${CELL_FILL_DURATION}s ease`,
			`width ${CELL_FILL_DURATION}s ease`
		].join(", "),
		transitionDelay: fading ? `${cell.fadeDelayMs}ms` : "0ms",
		width: CELL_PITCH
	} : {
		animationDelay: `${cell.blinkDelayMs}ms`,
		animationDirection: "normal",
		animationDuration: `${BLINK_DURATION_MS}ms`,
		animationFillMode: "both",
		animationIterationCount: "infinite",
		animationName: reduceMotion ? "none" : "painting-skeleton-cell-blink",
		animationTimingFunction: "ease-in-out",
		backgroundColor: "var(--muted-foreground)",
		borderRadius: CELL_RADIUS,
		height: CELL_SIZE,
		left: cell.x,
		opacity: reduceMotion ? .24 : cell.initialOpacity,
		position: "absolute",
		top: cell.y,
		width: CELL_SIZE
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "paintings.painting-skeleton-surface.painting-skeleton-grid-cell part:painting-skeleton-grid-cell",
		"data-phase": ready ? fading ? "fading" : "coloring" : "loading",
		"data-slot": "painting-skeleton-grid-cell",
		style
	});
});
var PaintingSkeletonSurface = ({ imageUrl, onRevealReady }) => {
	const rootRef = (0, import_react.useRef)(null);
	const didHandOffRef = (0, import_react.useRef)(false);
	const reduceMotion = Boolean(useReducedMotion());
	const [grid, setGrid] = (0, import_react.useState)(null);
	const [sampledColors, setSampledColors] = (0, import_react.useState)(null);
	const [fadingKey, setFadingKey] = (0, import_react.useState)(null);
	(0, import_react.useLayoutEffect)(() => {
		const root = rootRef.current;
		if (!root) return;
		const measure = () => {
			const { height, width } = root.getBoundingClientRect();
			if (height <= 0 || width <= 0) return;
			const next = {
				cols: Math.ceil(width / CELL_PITCH) + 2,
				rows: Math.ceil(height / CELL_PITCH) + 2
			};
			setGrid((current) => current?.cols === next.cols && current.rows === next.rows ? current : next);
		};
		measure();
		const observer = new ResizeObserver(measure);
		observer.observe(root);
		return () => observer.disconnect();
	}, []);
	const cells = (0, import_react.useMemo)(() => {
		if (!grid) return [];
		return Array.from({ length: grid.cols * grid.rows }, (_, index) => {
			const column = index % grid.cols;
			const row = Math.floor(index / grid.cols);
			return {
				blinkDelayMs: Math.round(cellNoise(index, 1) * BLINK_DURATION_MS),
				fadeDelayMs: Math.round(cellNoise(index, 2) * CELL_FADE_DELAY_MAX_MS),
				id: `${row}-${column}`,
				initialOpacity: .16 + cellNoise(index, 3) * .36,
				x: (column - 1) * CELL_PITCH,
				y: (row - 1) * CELL_PITCH
			};
		});
	}, [grid]);
	const sampleKey = imageUrl && grid ? `${imageUrl}\u0000${grid.cols}x${grid.rows}` : null;
	const colorsReady = Boolean(sampleKey && sampledColors?.key === sampleKey);
	const fading = Boolean(sampleKey && fadingKey === sampleKey);
	(0, import_react.useEffect)(() => {
		if (!imageUrl || !grid || !sampleKey || reduceMotion) return;
		let active = true;
		downsampleCellColors(imageUrl, grid).then((colors) => {
			if (active) setSampledColors({
				colors: colors ?? [],
				key: sampleKey
			});
		});
		return () => {
			active = false;
		};
	}, [
		grid,
		imageUrl,
		reduceMotion,
		sampleKey
	]);
	(0, import_react.useEffect)(() => {
		if (!colorsReady || !sampleKey || reduceMotion) return;
		const timer = setTimeout(() => setFadingKey(sampleKey), CELL_FILL_DURATION * 1e3);
		return () => clearTimeout(timer);
	}, [
		colorsReady,
		reduceMotion,
		sampleKey
	]);
	(0, import_react.useEffect)(() => {
		didHandOffRef.current = false;
	}, [imageUrl]);
	const handOffReveal = (0, import_react.useCallback)(() => {
		if (didHandOffRef.current || !onRevealReady) return;
		didHandOffRef.current = true;
		onRevealReady();
	}, [onRevealReady]);
	(0, import_react.useEffect)(() => {
		if (imageUrl && reduceMotion) handOffReveal();
	}, [
		handOffReveal,
		imageUrl,
		reduceMotion
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "paintings.painting-skeleton-surface part:painting-skeleton-surface",
		ref: rootRef,
		"aria-hidden": "true",
		className: "relative h-full w-full overflow-hidden bg-background",
		"data-slot": "painting-skeleton-surface",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("style", { children: `
        @keyframes painting-skeleton-cell-blink {
          0%, 100% { opacity: 0.16; }
          50% { opacity: 0.62; }
        }
      ` }),
			grid && !(imageUrl && reduceMotion) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-ui": "paintings.painting-skeleton-surface.painting-skeleton-grid part:painting-skeleton-grid",
				className: "pointer-events-none absolute inset-0 z-10",
				"data-slot": "painting-skeleton-grid",
				children: cells.map((cell, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Cell, {
					cell,
					color: colorsReady ? sampledColors?.colors[index] : void 0,
					fading,
					ready: colorsReady,
					reduceMotion
				}, cell.id))
			}, `${grid.cols}x${grid.rows}`),
			imageUrl && (reduceMotion ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
				"data-ui": "paintings.painting-skeleton-surface.painting-skeleton-reveal part:painting-skeleton-reveal",
				src: imageUrl,
				alt: "",
				className: "pointer-events-none absolute inset-0 size-full object-cover",
				"data-slot": "painting-skeleton-reveal",
				draggable: false
			}) : colorsReady && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(motion.img, {
				"data-ui": "paintings.painting-skeleton-surface.painting-skeleton-reveal part:painting-skeleton-reveal",
				src: imageUrl,
				alt: "",
				className: "pointer-events-none absolute inset-0 size-full object-cover",
				"data-slot": "painting-skeleton-reveal",
				draggable: false,
				initial: { opacity: 0 },
				animate: { opacity: 1 },
				transition: {
					delay: CELL_FILL_DURATION,
					duration: IMAGE_REVEAL_DURATION,
					ease: "easeOut"
				},
				onAnimationComplete: handOffReveal
			}, sampleKey))
		]
	});
};
var PaintingSkeletonSurface_default = PaintingSkeletonSurface;
var SKELETON_MAX_SIZE = "100%";
var PaintingImageSkeleton = ({ imageUrl, naturalWidth, naturalHeight, onRevealReady, painting, topBar }) => {
	const { t } = useTranslation();
	const { ratio } = usePaintingSizeInfo(painting);
	const wrapperRef = (0, import_react.useRef)(null);
	const [container, setContainer] = (0, import_react.useState)(null);
	const topBarObserverRef = (0, import_react.useRef)(null);
	const [topBarHeight, setTopBarHeight] = (0, import_react.useState)(0);
	(0, import_react.useLayoutEffect)(() => {
		const el = wrapperRef.current;
		if (!el) return;
		const measure = () => setContainer({
			width: el.clientWidth,
			height: el.clientHeight
		});
		measure();
		const observer = new ResizeObserver(measure);
		observer.observe(el);
		return () => observer.disconnect();
	}, []);
	const setTopBarRef = (0, import_react.useCallback)((el) => {
		topBarObserverRef.current?.disconnect();
		topBarObserverRef.current = null;
		if (!el) {
			setTopBarHeight(0);
			return;
		}
		const measure = () => setTopBarHeight(el.clientHeight);
		measure();
		const observer = new ResizeObserver(measure);
		observer.observe(el);
		topBarObserverRef.current = observer;
	}, []);
	const availableHeight = container ? Math.max(0, container.height - topBarHeight) : null;
	const hasMeasuredWidth = container != null && container.width > 0;
	const containerRatio = hasMeasuredWidth && availableHeight != null && availableHeight > 0 && container && availableHeight ? container.width / availableHeight : null;
	let lockedSize = null;
	if (naturalWidth && naturalHeight && naturalWidth > 0 && naturalHeight > 0 && container && availableHeight != null) {
		const scale = Math.min(1, container.width / naturalWidth, availableHeight / naturalHeight);
		lockedSize = {
			width: naturalWidth * scale,
			height: naturalHeight * scale
		};
	}
	let boxSize = lockedSize;
	if (!boxSize && ratio != null && container) {
		if (containerRatio != null && ratio < containerRatio && availableHeight != null) boxSize = {
			width: availableHeight * ratio,
			height: availableHeight
		};
		else if (hasMeasuredWidth) boxSize = {
			width: container.width,
			height: container.width / ratio
		};
	}
	const boxStyle = boxSize ? {
		width: boxSize.width,
		height: boxSize.height
	} : ratio == null ? void 0 : {
		width: SKELETON_MAX_SIZE,
		height: "auto",
		aspectRatio: String(ratio)
	};
	const hasKnownSize = boxStyle != null;
	const columnWidth = boxSize?.width;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "paintings.painting-image-skeleton.status",
		ref: wrapperRef,
		className: "relative flex min-h-0 w-full flex-1 items-center justify-center overflow-hidden",
		role: "status",
		"aria-live": "polite",
		"aria-label": t(imageUrl ? "paintings.revealing" : "paintings.generating"),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: cn("flex flex-col items-stretch", hasKnownSize ? "max-h-full max-w-full" : "h-full w-full"),
			style: columnWidth != null ? { width: columnWidth } : void 0,
			children: [topBar && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-ui": "paintings.painting-image-skeleton.painting-skeleton-top-bar",
				ref: setTopBarRef,
				className: "min-w-0",
				"data-testid": "painting-skeleton-top-bar-measure",
				children: topBar
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: cn("overflow-hidden rounded-md bg-background", !hasKnownSize && "min-h-0 flex-1"),
				style: boxStyle,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingSkeletonSurface_default, {
					imageUrl,
					onRevealReady
				})
			})]
		})
	});
};
var PaintingImageSkeleton_default = PaintingImageSkeleton;
var logger$10 = loggerService.withContext("paintings/Artboard");
var DEFAULT_IMAGE_SCALE = 1;
var MIN_IMAGE_SCALE = .25;
var MAX_IMAGE_SCALE = 4;
var IMAGE_SCALE_STEP = .25;
var DEFAULT_IMAGE_OFFSET = {
	x: 0,
	y: 0
};
var PROMPT_POPOVER_CLOSE_DELAY = 150;
var ArtboardPromptBar = ({ prompt, sizeLabel }) => {
	const { t } = useTranslation();
	const [isPromptPopoverOpen, setPromptPopoverOpen] = (0, import_react.useState)(false);
	const promptPopoverCloseTimerRef = (0, import_react.useRef)(null);
	const promptPopoverTriggerRef = (0, import_react.useRef)(null);
	const promptPopoverContentRef = (0, import_react.useRef)(null);
	const promptPopoverOpenReasonRef = (0, import_react.useRef)("pointer");
	const cancelPromptPopoverClose = (0, import_react.useCallback)(() => {
		if (promptPopoverCloseTimerRef.current === null) return;
		window.clearTimeout(promptPopoverCloseTimerRef.current);
		promptPopoverCloseTimerRef.current = null;
	}, []);
	const openPromptPopover = (0, import_react.useCallback)((reason) => {
		cancelPromptPopoverClose();
		promptPopoverOpenReasonRef.current = reason;
		setPromptPopoverOpen(true);
	}, [cancelPromptPopoverClose]);
	const openPromptPopoverFromPointer = (0, import_react.useCallback)(() => {
		cancelPromptPopoverClose();
		if (isPromptPopoverOpen) return;
		promptPopoverOpenReasonRef.current = "pointer";
		setPromptPopoverOpen(true);
	}, [cancelPromptPopoverClose, isPromptPopoverOpen]);
	const schedulePromptPopoverClose = (0, import_react.useCallback)(() => {
		cancelPromptPopoverClose();
		promptPopoverCloseTimerRef.current = window.setTimeout(() => {
			promptPopoverCloseTimerRef.current = null;
			const focusedElement = document.activeElement;
			if (promptPopoverTriggerRef.current?.contains(focusedElement) || promptPopoverContentRef.current?.contains(focusedElement)) return;
			setPromptPopoverOpen(false);
		}, PROMPT_POPOVER_CLOSE_DELAY);
	}, [cancelPromptPopoverClose]);
	(0, import_react.useEffect)(() => cancelPromptPopoverClose, [cancelPromptPopoverClose]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "paintings.artboard-prompt-bar",
		className: "mb-2 flex w-full min-w-0 items-center justify-between gap-2 text-muted-foreground text-xs",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "min-w-0 max-w-xs flex-1 overflow-hidden",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, {
				open: isPromptPopoverOpen,
				onOpenChange: (open) => {
					cancelPromptPopoverClose();
					if (open && promptPopoverOpenReasonRef.current !== "keyboard") promptPopoverOpenReasonRef.current = "pointer";
					setPromptPopoverOpen(open);
				},
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverTrigger, {
					asChild: true,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
						"data-ui": "paintings.artboard-prompt-bar.action.close",
						ref: promptPopoverTriggerRef,
						type: "button",
						onPointerEnter: (event) => {
							if (event.pointerType !== "touch") openPromptPopoverFromPointer();
						},
						onPointerLeave: (event) => {
							if (event.pointerType !== "touch") schedulePromptPopoverClose();
						},
						onBlur: schedulePromptPopoverClose,
						onKeyDown: (event) => {
							if (event.key !== "Enter" && event.key !== " ") return;
							event.preventDefault();
							event.stopPropagation();
							openPromptPopover("keyboard");
						},
						className: "flex w-full min-w-0 items-center gap-1.5 overflow-hidden rounded-sm text-left text-inherit outline-none focus-visible:bg-accent focus-visible:text-foreground",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Palette, {
							className: "size-3.5 shrink-0",
							"aria-hidden": true
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "truncate",
							children: prompt
						})]
					}) })
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(PopoverContent, {
					ref: promptPopoverContentRef,
					side: "bottom",
					sideOffset: 2,
					onPointerEnter: (event) => {
						if (event.pointerType !== "touch") openPromptPopoverFromPointer();
					},
					onPointerLeave: (event) => {
						if (event.pointerType !== "touch") schedulePromptPopoverClose();
					},
					onFocusCapture: cancelPromptPopoverClose,
					onBlurCapture: schedulePromptPopoverClose,
					onOpenAutoFocus: (event) => {
						if (promptPopoverOpenReasonRef.current !== "keyboard") event.preventDefault();
					},
					onCloseAutoFocus: (event) => {
						if (promptPopoverOpenReasonRef.current !== "keyboard") event.preventDefault();
					},
					role: "dialog",
					"aria-label": t("common.prompt"),
					className: "max-h-80 w-fit max-w-md overflow-y-auto rounded-md border-0 bg-neutral-900 p-2 text-neutral-50 text-xs leading-relaxed shadow-md",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyButton_default, {
						textToCopy: prompt,
						"aria-label": t("common.copy"),
						successFeedback: "icon",
						size: 14,
						color: "inherit",
						hoverColor: "inherit",
						className: "float-right ml-0.5 size-5 justify-center rounded-md text-neutral-50 hover:bg-neutral-50/10 hover:text-neutral-50 focus-visible:bg-neutral-50/10 focus-visible:text-neutral-50 focus-visible:outline-none [&_svg]:stroke-neutral-50! [&_svg]:text-neutral-50!"
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "select-text whitespace-pre-wrap break-words",
						children: prompt
					})]
				})]
			})
		}), sizeLabel && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: "shrink-0",
			children: sizeLabel
		})]
	});
};
var ArtboardToolButton = ({ children, disabled, label, onClick }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
		content: label,
		placement: "right",
		delay: 800,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			type: "button",
			variant: "ghost",
			size: "icon-sm",
			disabled,
			"aria-label": label,
			onClick,
			className: paintingClasses.toolbarButton,
			children
		})
	});
};
var Artboard = ({ painting, isLoading, imageCover }) => {
	const { t } = useTranslation();
	const [currentImageIndex, setCurrentImageIndex] = (0, import_react.useState)(0);
	const [imageScale, setImageScale] = (0, import_react.useState)(DEFAULT_IMAGE_SCALE);
	const [imageRotation, setImageRotation] = (0, import_react.useState)(0);
	const [imageOffset, setImageOffset] = (0, import_react.useState)(DEFAULT_IMAGE_OFFSET);
	const [isDraggingImage, setIsDraggingImage] = (0, import_react.useState)(false);
	const [revealState, setRevealState] = (0, import_react.useState)(null);
	const [viewerContainer, setViewerContainer] = (0, import_react.useState)(null);
	const [displayedNaturalSize, setDisplayedNaturalSize] = (0, import_react.useState)(null);
	const [promptBarHeight, setPromptBarHeight] = (0, import_react.useState)(0);
	const imageDragRef = (0, import_react.useRef)(null);
	const awaitingRevealRef = (0, import_react.useRef)(false);
	const previousLoadingRef = (0, import_react.useRef)(isLoading);
	const paintingIdRef = (0, import_react.useRef)(painting.id);
	const viewerResizeObserverRef = (0, import_react.useRef)(null);
	const viewerWheelCleanupRef = (0, import_react.useRef)(null);
	const promptBarResizeObserverRef = (0, import_react.useRef)(null);
	const displayedImageIndex = painting.files.length > 0 ? Math.min(currentImageIndex, painting.files.length - 1) : 0;
	const currentFile = painting.files[displayedImageIndex];
	const { sizeLabel } = usePaintingSizeInfo(painting);
	const currentImageUrl = currentFile ? getPaintingFileUrl(currentFile) : void 0;
	const onPrevImage = (0, import_react.useCallback)(() => {
		setCurrentImageIndex((index) => index > 0 ? index - 1 : Math.max(0, painting.files.length - 1));
	}, [painting.files.length]);
	const onNextImage = (0, import_react.useCallback)(() => {
		setCurrentImageIndex((index) => painting.files.length > 0 ? (index + 1) % painting.files.length : 0);
	}, [painting.files.length]);
	const zoomIn = (0, import_react.useCallback)(() => {
		setImageScale((scale) => Math.min(MAX_IMAGE_SCALE, scale + IMAGE_SCALE_STEP));
	}, []);
	const zoomOut = (0, import_react.useCallback)(() => {
		setImageScale((scale) => Math.max(MIN_IMAGE_SCALE, scale - IMAGE_SCALE_STEP));
	}, []);
	const zoomImageFromWheel = (0, import_react.useCallback)((deltaY) => {
		if (deltaY === 0) return;
		setImageScale((scale) => {
			const next = scale + (deltaY < 0 ? IMAGE_SCALE_STEP : -IMAGE_SCALE_STEP);
			return Math.min(MAX_IMAGE_SCALE, Math.max(MIN_IMAGE_SCALE, next));
		});
	}, []);
	const rotateImageRight = (0, import_react.useCallback)(() => {
		setImageRotation((rotation) => rotation + 90);
	}, []);
	const rotateImageLeft = (0, import_react.useCallback)(() => {
		setImageRotation((rotation) => rotation - 90);
	}, []);
	const resetImageTransform = (0, import_react.useCallback)(() => {
		imageDragRef.current = null;
		setIsDraggingImage(false);
		setImageScale(DEFAULT_IMAGE_SCALE);
		setImageRotation(0);
		setImageOffset(DEFAULT_IMAGE_OFFSET);
	}, []);
	const onImagePointerDown = (0, import_react.useCallback)((event) => {
		if (event.button !== 0) return;
		event.preventDefault();
		event.currentTarget.setPointerCapture(event.pointerId);
		imageDragRef.current = {
			pointerId: event.pointerId,
			x: event.clientX,
			y: event.clientY
		};
		setIsDraggingImage(true);
	}, []);
	const onImagePointerMove = (0, import_react.useCallback)((event) => {
		const dragState = imageDragRef.current;
		if (!dragState || dragState.pointerId !== event.pointerId) return;
		event.preventDefault();
		const deltaX = event.clientX - dragState.x;
		const deltaY = event.clientY - dragState.y;
		dragState.x = event.clientX;
		dragState.y = event.clientY;
		setImageOffset((offset) => ({
			x: offset.x + deltaX,
			y: offset.y + deltaY
		}));
	}, []);
	const stopImageDrag = (0, import_react.useCallback)((event) => {
		if (event.currentTarget.hasPointerCapture(event.pointerId)) event.currentTarget.releasePointerCapture(event.pointerId);
		if (imageDragRef.current?.pointerId === event.pointerId) {
			imageDragRef.current = null;
			setIsDraggingImage(false);
		}
	}, []);
	const onDisplayedImageLoad = (0, import_react.useCallback)((event) => {
		const { naturalWidth, naturalHeight } = event.currentTarget;
		if (naturalWidth > 0 && naturalHeight > 0) setDisplayedNaturalSize({
			width: naturalWidth,
			height: naturalHeight
		});
	}, []);
	const setViewerContainerRef = (0, import_react.useCallback)((el) => {
		viewerResizeObserverRef.current?.disconnect();
		viewerResizeObserverRef.current = null;
		viewerWheelCleanupRef.current?.();
		viewerWheelCleanupRef.current = null;
		if (!el) return;
		const measure = () => setViewerContainer({
			width: el.clientWidth,
			height: el.clientHeight
		});
		measure();
		const observer = new ResizeObserver(measure);
		observer.observe(el);
		viewerResizeObserverRef.current = observer;
		const handleWheel = (event) => {
			const target = event.target;
			if (!(target instanceof Element) || !target.closest("[data-testid=\"artboard-image-layout\"]")) return;
			event.preventDefault();
			event.stopPropagation();
			zoomImageFromWheel(event.deltaY);
		};
		el.addEventListener("wheel", handleWheel, {
			capture: true,
			passive: false
		});
		viewerWheelCleanupRef.current = () => el.removeEventListener("wheel", handleWheel, { capture: true });
	}, [zoomImageFromWheel]);
	const setPromptBarRef = (0, import_react.useCallback)((el) => {
		promptBarResizeObserverRef.current?.disconnect();
		promptBarResizeObserverRef.current = null;
		if (!el) {
			setPromptBarHeight(0);
			return;
		}
		const measure = () => setPromptBarHeight(el.clientHeight);
		measure();
		const observer = new ResizeObserver(measure);
		observer.observe(el);
		promptBarResizeObserverRef.current = observer;
	}, []);
	(0, import_react.useEffect)(() => {
		setDisplayedNaturalSize(null);
	}, [currentFile?.id]);
	const displayedImageBoxSize = (() => {
		if (!displayedNaturalSize || !viewerContainer || viewerContainer.width <= 0) return null;
		const availableHeight = Math.max(0, viewerContainer.height - promptBarHeight);
		if (availableHeight <= 0) return null;
		const scale = Math.min(1, viewerContainer.width / displayedNaturalSize.width, availableHeight / displayedNaturalSize.height);
		return {
			width: displayedNaturalSize.width * scale,
			height: displayedNaturalSize.height * scale
		};
	})();
	(0, import_react.useEffect)(() => {
		setCurrentImageIndex(0);
		resetImageTransform();
	}, [painting.id, resetImageTransform]);
	(0, import_react.useLayoutEffect)(() => {
		resetImageTransform();
	}, [currentFile?.id, resetImageTransform]);
	(0, import_react.useLayoutEffect)(() => {
		const paintingChanged = paintingIdRef.current !== painting.id;
		paintingIdRef.current = painting.id;
		if (paintingChanged) {
			awaitingRevealRef.current = false;
			setRevealState(null);
		}
		const wasLoading = paintingChanged ? isLoading : previousLoadingRef.current;
		previousLoadingRef.current = isLoading;
		if (isLoading) {
			awaitingRevealRef.current = false;
			setRevealState(null);
			return;
		}
		if (!(wasLoading || awaitingRevealRef.current)) {
			setRevealState((state) => state && state.status !== "awaiting" && (state.fileId !== currentFile?.id || state.imageUrl !== currentImageUrl) ? null : state);
			return;
		}
		if (!currentFile || !currentImageUrl) {
			if (painting.generationStatus === "canceled" || painting.generationStatus === "failed") {
				awaitingRevealRef.current = false;
				setRevealState(null);
				return;
			}
			awaitingRevealRef.current = true;
			setRevealState((state) => state?.status === "awaiting" ? state : { status: "awaiting" });
			return;
		}
		let active = true;
		const target = {
			fileId: currentFile.id,
			imageUrl: currentImageUrl
		};
		awaitingRevealRef.current = false;
		setRevealState({
			...target,
			status: "pending"
		});
		computeImageNaturalSize(currentImageUrl).then((result) => {
			if (!active) return;
			if (!result) {
				setRevealState(null);
				return;
			}
			setRevealState({
				...target,
				naturalWidth: result.naturalWidth,
				naturalHeight: result.naturalHeight,
				status: "ready"
			});
		}).catch((error) => {
			logger$10.warn("Failed to prepare painting image reveal", { error });
			if (active) setRevealState(null);
		});
		return () => {
			active = false;
		};
	}, [
		painting.id,
		currentFile,
		currentImageUrl,
		isLoading,
		painting.generationStatus
	]);
	const activeReveal = (() => {
		if (isLoading || !revealState) return null;
		if (revealState.status === "awaiting") return revealState;
		return currentFile?.id === revealState.fileId && currentImageUrl === revealState.imageUrl ? revealState : null;
	})();
	const finishReveal = (0, import_react.useCallback)(() => {
		setRevealState(null);
	}, []);
	const promptBar = painting.prompt ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArtboardPromptBar, {
		prompt: painting.prompt,
		sizeLabel
	}) : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "paintings.artboard",
		className: "flex min-h-0 w-full flex-1 flex-col p-2",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "relative flex min-h-0 flex-1 flex-col items-center justify-center [container-type:size]",
			children: isLoading || activeReveal ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingImageSkeleton_default, {
				imageUrl: activeReveal?.status === "ready" ? activeReveal.imageUrl : void 0,
				naturalWidth: activeReveal?.status === "ready" ? activeReveal.naturalWidth : void 0,
				naturalHeight: activeReveal?.status === "ready" ? activeReveal.naturalHeight : void 0,
				onRevealReady: activeReveal?.status === "ready" ? finishReveal : void 0,
				painting,
				topBar: promptBar
			}) : painting.files.length > 0 && currentImageUrl ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				ref: setViewerContainerRef,
				className: "relative flex min-h-0 w-full flex-1 items-center justify-center overflow-hidden",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						"data-ui": "paintings.artboard.artboard-image-layout",
						"data-testid": "artboard-image-layout",
						className: "flex max-h-full max-w-full flex-col items-stretch",
						style: { ...displayedImageBoxSize ? { width: displayedImageBoxSize.width } : void 0 },
						children: [promptBar && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							"data-ui": "paintings.artboard.artboard-prompt-bar-measure",
							ref: setPromptBarRef,
							"data-testid": "artboard-prompt-bar-measure",
							children: promptBar
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ImageViewer_default, {
							alt: "",
							"data-testid": "artboard-image-transform",
							className: `max-h-full min-h-0 max-w-full select-none rounded-md object-contain ${isDraggingImage ? "cursor-grabbing transition-none will-change-transform" : "cursor-grab transition-transform duration-150"}`,
							draggable: false,
							onLoad: onDisplayedImageLoad,
							onPointerCancel: stopImageDrag,
							onPointerDown: onImagePointerDown,
							onPointerMove: onImagePointerMove,
							onPointerUp: stopImageDrag,
							preview: false,
							src: currentImageUrl,
							style: {
								touchAction: "none",
								transform: `translate(${imageOffset.x}px, ${imageOffset.y}px) scale(${imageScale}) rotate(${imageRotation}deg)`,
								...displayedImageBoxSize ? { height: displayedImageBoxSize.height } : void 0
							}
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						"data-ui": "paintings.artboard.toolbar",
						className: `${paintingClasses.toolbarWrap} ${paintingClasses.toolbarRail}`,
						role: "toolbar",
						"aria-label": t("preview.label"),
						children: [
							painting.files.length > 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArtboardToolButton, {
									label: t("preview.previous"),
									onClick: onPrevImage,
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ImageUp, { className: "size-[18px]" })
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArtboardToolButton, {
									label: t("preview.next"),
									onClick: onNextImage,
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ImageDown, { className: "size-[18px]" })
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: "my-0.5 h-px w-4 bg-border-subtle",
									"aria-hidden": true
								})
							] }),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArtboardToolButton, {
								label: t("preview.zoom_out"),
								disabled: imageScale <= MIN_IMAGE_SCALE,
								onClick: zoomOut,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ZoomOut, { className: "size-4" })
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArtboardToolButton, {
								label: t("preview.zoom_in"),
								disabled: imageScale >= MAX_IMAGE_SCALE,
								onClick: zoomIn,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ZoomIn, { className: "size-4" })
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArtboardToolButton, {
								label: t("preview.rotate_left"),
								onClick: rotateImageLeft,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RotateCcwSquare, { className: "size-4" })
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArtboardToolButton, {
								label: t("preview.rotate_right"),
								onClick: rotateImageRight,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RotateCwSquare, { className: "size-4" })
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArtboardToolButton, {
								label: t("preview.reset"),
								onClick: resetImageTransform,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RefreshCcw, { className: "size-4" })
							})
						]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: "-translate-x-1/2 absolute bottom-2.5 left-1/2 rounded-full bg-foreground/60 px-2 py-1 text-background text-xs",
						children: [
							displayedImageIndex + 1,
							" / ",
							painting.files.length
						]
					})
				]
			}) : imageCover ? imageCover : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-ui": "paintings.artboard.img",
				role: "img",
				"aria-label": t("paintings.image_placeholder"),
				className: "relative size-[min(72cqh,96cqw)] rounded-3xl border border-border-subtle bg-[linear-gradient(145deg,var(--muted),transparent_58%)]",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					"aria-hidden": true,
					className: "absolute inset-2 rounded-2xl border border-border-subtle bg-background-subtle"
				})
			})
		})
	});
};
var Artboard_default = Artboard;
var logger$9 = loggerService.withContext("usePaintingComposerInputFiles");
var withDot = (ext) => {
	if (!ext) return "";
	return ext.startsWith(".") ? ext : `.${ext}`;
};
async function entryStillExists(id) {
	try {
		await window.api.file.getPhysicalPath({ id });
		return true;
	} catch {
		return false;
	}
}
function usePaintingComposerInputFiles({ paintingId, inputFiles, files, setFiles, inputCapability, providerId: _providerId }) {
	const { t } = useTranslation();
	const entryCacheRef = (0, import_react.useRef)(/* @__PURE__ */ new Map());
	const unseededEntriesRef = (0, import_react.useRef)([]);
	const seededPaintingIdRef = (0, import_react.useRef)(null);
	const draftEpochRef = (0, import_react.useRef)(0);
	const inputFilesRef = (0, import_react.useRef)(inputFiles);
	inputFilesRef.current = inputFiles;
	const filesRef = (0, import_react.useRef)(files);
	filesRef.current = files;
	const lastCapabilityRef = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (seededPaintingIdRef.current === paintingId) return;
		seededPaintingIdRef.current = paintingId;
		unseededEntriesRef.current = [];
		const entries = inputFilesRef.current;
		if (entries.length === 0) {
			entryCacheRef.current = /* @__PURE__ */ new Map();
			setFiles([]);
			return;
		}
		const epoch = draftEpochRef.current;
		(async () => {
			const cache = /* @__PURE__ */ new Map();
			const attachments = [];
			const unseeded = [];
			for (const entry of entries) try {
				const path = await window.api.file.getPhysicalPath({ id: entry.id });
				const sourceId = createComposerFileTokenSourceId();
				cache.set(sourceId, entry);
				attachments.push({
					fileTokenSourceId: sourceId,
					path,
					name: entry.name,
					origin_name: entry.name,
					ext: withDot(entry.ext),
					size: "size" in entry ? entry.size ?? 0 : 0,
					type: getFileTypeByExt(entry.ext ?? "")
				});
			} catch (error) {
				logger$9.error("failed to seed composer attachment from input file", error);
				unseeded.push(entry);
			}
			if (draftEpochRef.current !== epoch) return;
			entryCacheRef.current = cache;
			unseededEntriesRef.current = unseeded;
			setFiles(attachments);
		})();
		return () => {
			draftEpochRef.current++;
		};
	}, [paintingId, setFiles]);
	(0, import_react.useEffect)(() => {
		const droppedImageSupport = lastCapabilityRef.current === "accept" && inputCapability === "reject";
		if (inputCapability !== "unknown") lastCapabilityRef.current = inputCapability;
		if (!droppedImageSupport) return;
		draftEpochRef.current++;
		entryCacheRef.current = /* @__PURE__ */ new Map();
		unseededEntriesRef.current = [];
		setFiles([]);
	}, [inputCapability, setFiles]);
	return { materializeInputs: (0, import_react.useCallback)(async () => {
		const cache = entryCacheRef.current;
		const entries = [];
		const failedSourceIds = [];
		for (const file of filesRef.current) {
			const cached = cache.get(file.fileTokenSourceId);
			if (cached && await entryStillExists(cached.id)) {
				entries.push(cached);
				continue;
			}
			if (cached) {
				cache.delete(file.fileTokenSourceId);
				logger$9.info("input entry was reclaimed since it was cached; re-importing", { entryId: cached.id });
			}
			try {
				const entry = await window.api.file.createInternalEntry({
					source: "path",
					path: AbsoluteFilePathSchema.parse(file.path),
					cleanupPolicy: "delete_when_unreferenced"
				});
				cache.set(file.fileTokenSourceId, entry);
				entries.push(entry);
			} catch (error) {
				logger$9.error("failed to create input file entry from composer attachment", error);
				failedSourceIds.push(file.fileTokenSourceId);
			}
		}
		if (failedSourceIds.length > 0) {
			const failed = new Set(failedSourceIds);
			setFiles((prev) => prev.filter((file) => !failed.has(file.fileTokenSourceId)));
			toast.error(t("paintings.image_file_retry"));
		}
		const preserved = unseededEntriesRef.current;
		return {
			entries: preserved.length ? [...entries, ...preserved] : entries,
			complete: failedSourceIds.length === 0
		};
	}, [setFiles, t]) };
}
function imagePreviewUrl(path, ext) {
	return getPaintingFileUrl({
		path,
		ext: ext.replace(/^\./, "").toLowerCase() || null
	}) ?? "";
}
var stop = (event) => event.stopPropagation();
const PaintingImageAddButton = () => {
	const { t } = useTranslation();
	const { extensions } = useComposerToolState();
	const { setFiles } = useComposerToolDispatch();
	const [selecting, setSelecting] = (0, import_react.useState)(false);
	const pickImages = (0, import_react.useCallback)(async () => {
		if (selecting) return;
		setSelecting(true);
		try {
			const picked = await window.api.file.select({
				properties: ["openFile", "multiSelections"],
				filters: [{
					name: "Images",
					extensions: extensions.map((ext) => ext.replace(/^\./, ""))
				}]
			});
			if (picked?.length) setFiles((current) => [...current, ...toComposerAttachments(picked)]);
		} finally {
			setSelecting(false);
		}
	}, [
		extensions,
		selecting,
		setFiles
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		"data-ui": "paintings.painting-image-add-button",
		type: "button",
		"aria-label": t("paintings.add_image"),
		title: t("paintings.add_image"),
		disabled: selecting,
		onMouseDown: stop,
		onClick: (event) => {
			stop(event);
			pickImages();
		},
		className: "flex size-9 shrink-0 items-center justify-center rounded-full border border-border-subtle text-muted-foreground transition-colors hover:border-border-strong hover:bg-accent hover:text-foreground",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, {
			className: "size-5",
			"aria-hidden": true
		})
	});
};
const PaintingImageGallery = () => {
	const { t } = useTranslation();
	const { files } = useComposerToolState();
	const { setFiles } = useComposerToolDispatch();
	const previewItems = (0, import_react.useMemo)(() => files.flatMap((file) => file.type === FILE_TYPE.IMAGE && file.path ? [{
		id: file.fileTokenSourceId,
		src: imagePreviewUrl(file.path, file.ext),
		alt: file.origin_name
	}] : []), [files]);
	const removeImage = (0, import_react.useCallback)((sourceId) => {
		setFiles((current) => current.filter((file) => file.fileTokenSourceId !== sourceId));
	}, [setFiles]);
	if (previewItems.length === 0) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "paintings.painting-image-gallery",
		className: "px-3.5 pt-2.5",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HorizontalScrollContainer_default, {
			dependencies: [previewItems.length],
			gap: "6px",
			children: previewItems.map((item) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
				className: "group/tile relative inline-flex size-14 shrink-0 overflow-hidden rounded-lg border border-border-subtle",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ImageViewer_default, {
					src: item.src,
					alt: item.alt,
					draggable: false,
					className: "size-full cursor-pointer object-cover",
					preview: { items: previewItems }
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
					"data-ui": "paintings.painting-image-gallery.action.stop",
					type: "button",
					"aria-label": t("common.delete"),
					title: t("common.delete"),
					onMouseDown: stop,
					onClick: (event) => {
						stop(event);
						removeImage(item.id);
					},
					className: "absolute top-0.5 right-0.5 z-1 inline-flex size-4 items-center justify-center rounded-full bg-black/55 text-white opacity-0 transition-opacity group-focus-within/tile:opacity-100 group-hover/tile:opacity-100",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, {
						className: "size-3",
						"aria-hidden": true
					})
				})]
			}, item.id))
		})
	});
};
function createModelOptionFromModel(model) {
	return {
		label: model.name || model.apiModelId || parseUniqueModelId(model.id).modelId,
		value: model.apiModelId || parseUniqueModelId(model.id).modelId,
		group: model.group,
		isEnabled: model.isEnabled,
		raw: model
	};
}
function canOutputImage(model) {
	return !model.outputModalities?.length || model.outputModalities.includes(MODALITY.IMAGE);
}
function supportsImageGenerationEndpoint(model) {
	return (model.endpointTypes?.some((e) => e === ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION || e === ENDPOINT_TYPE.OPENAI_IMAGE_EDIT) ?? false) || model.capabilities.includes(MODEL_CAPABILITY.IMAGE_GENERATION) && canOutputImage(model);
}
function getPaintingModelOptions(providerId, models) {
	return models.filter((model) => model.providerId === providerId && !model.isHidden && supportsImageGenerationEndpoint(model)).map(createModelOptionFromModel);
}
async function loadPaintingModelOptions(providerId) {
	return getPaintingModelOptions(providerId, await dataApiService.get("/models", { query: { providerId } }));
}
var PaintingSectionTitle = ({ children, className }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
	"data-ui": "paintings.painting-section-title",
	className: cn$1("mb-1.5 flex select-none items-center justify-start gap-1", "text-muted-foreground text-xs uppercase tracking-wider", className),
	children
});
var PaintingSectionTitle_default = PaintingSectionTitle;
var PaintingModelSelector = ({ className, painting, onSelect, hideTitle }) => {
	const { t } = useTranslation();
	const [open, setOpen] = (0, import_react.useState)(false);
	const { models } = useModels();
	const { providers } = useProviders({ enabled: true });
	const selectedModelId = (0, import_react.useMemo)(() => painting.providerId && painting.model ? createUniqueModelId(painting.providerId, painting.model) : void 0, [painting.providerId, painting.model]);
	const selectedModel = (0, import_react.useMemo)(() => painting.model ? models.find((model) => model.providerId === painting.providerId && model.apiModelId === painting.model) : void 0, [
		models,
		painting.providerId,
		painting.model
	]);
	const selectedProvider = (0, import_react.useMemo)(() => painting.providerId ? providers.find((provider) => provider.id === painting.providerId) : void 0, [providers, painting.providerId]);
	const selectedName = selectedModel?.name ?? painting.model;
	const selectedIdentifier = selectedModel?.apiModelId && selectedModel.apiModelId !== selectedName ? selectedModel.apiModelId : void 0;
	const selectedProviderName = selectedProvider ? getProviderDisplayName$1(selectedProvider) : void 0;
	const selectedIcon = useIcon((0, import_react.useMemo)(() => painting.providerId ? getModelLogoRef(selectedModel ?? {
		id: painting.model ?? "",
		name: painting.model ?? ""
	}, painting.providerId) : void 0, [
		painting.providerId,
		painting.model,
		selectedModel
	]));
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "paintings.painting-model-selector",
		className: hideTitle ? "contents" : void 0,
		children: [!hideTitle && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingSectionTitle_default, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			className: "min-w-0 truncate",
			children: t("paintings.model")
		}) }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ModelSelector, {
			open,
			onOpenChange: setOpen,
			multiple: false,
			selectionType: "id",
			value: selectedModelId,
			onSelect: (uniqueModelId) => {
				if (!uniqueModelId) return;
				const { providerId, modelId } = parseUniqueModelId(uniqueModelId);
				onSelect({
					providerId,
					modelId
				});
			},
			filter: supportsImageGenerationEndpoint,
			showTagFilter: false,
			showPinnedModels: false,
			showPinActions: false,
			prioritizedProviderIds: painting.providerId ? [painting.providerId] : void 0,
			contentClassName: "w-[min(420px,calc(100vw-2rem))] rounded-[8px]",
			trigger: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
				variant: "secondary",
				size: "sm",
				className: cn("h-auto w-full max-w-none justify-between gap-2 rounded-[8px] border border-border-subtle px-2.5 py-1.5 text-muted-foreground text-xs shadow-none hover:text-foreground", className),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: "flex min-w-0 flex-1 items-center gap-2 overflow-hidden",
					children: [selectedName ? selectedIcon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(selectedIcon.Avatar, {
						size: 18,
						className: "shrink-0"
					}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Avatar, {
						className: "size-[18px] shrink-0 items-center justify-center rounded-lg",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AvatarFallback, {
							className: "rounded-lg text-[10px]",
							children: head(selectedName) || "M"
						})
					}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "min-w-0 truncate text-foreground",
						title: [
							selectedName,
							selectedIdentifier,
							selectedProviderName
						].filter(Boolean).join(" · "),
						children: selectedName ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
							selectedName,
							selectedIdentifier && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
								className: "font-mono text-muted-foreground",
								children: [" ", selectedIdentifier]
							}),
							selectedProviderName && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
								className: "text-muted-foreground",
								children: [" | ", selectedProviderName]
							})
						] }) : t("paintings.select_model")
					})]
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown, { className: "size-3 shrink-0 text-muted-foreground" })]
			})
		})]
	});
};
var PaintingModelSelector_default = PaintingModelSelector;
function SelectField({ item, fieldKey, painting, translate, onChange, currentValue, disabled }) {
	const options = resolveOptions(item, painting, translate);
	const grouped = options.some((option) => Array.isArray(option.options) && option.options.length > 0);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Select, {
		disabled,
		value: currentValue !== void 0 && currentValue !== null ? String(currentValue) : "",
		onValueChange: (nextValue) => onChange({ [fieldKey]: nextValue }),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectTrigger, {
			"aria-label": item.title ? translate(item.title) : fieldKey,
			className: "h-auto w-full justify-between gap-2 rounded-[8px] bg-secondary px-2.5 py-1.5 text-xs hover:bg-secondary-hover",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectValue, { placeholder: item.title ? translate(item.title) : fieldKey })
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectContent, { children: grouped ? options.map((group) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(SelectGroup, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectLabel, { children: group.label || group.title }), group.options?.map((option) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
			value: String(option.value),
			children: option.label
		}, `${fieldKey}-${option.value}`))] }, group.title || group.label)) : options.map((option) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItem, {
			value: String(option.value),
			children: option.label
		}, `${fieldKey}-${option.value}`)) })]
	});
}
var MAX_THUMB = 14;
var MIN_THUMB = 6;
var DEFAULT_COLUMNS = 3;
var chipClass = {
	base: "flex min-h-10 min-w-0 cursor-pointer flex-col items-center justify-center gap-0.5 rounded-[10px] px-1 py-1 text-[11px] leading-tight transition-all",
	active: "bg-secondary-active text-foreground ring-1 ring-[color:color-mix(in_oklch,var(--foreground)_33.3333%,transparent)]",
	inactive: "bg-muted text-foreground-tertiary hover:bg-secondary-hover hover:text-foreground",
	disabled: "cursor-not-allowed opacity-50"
};
function RatioShape({ ratio, selected }) {
	const scale = MAX_THUMB / Math.max(ratio.w, ratio.h);
	const w = Math.max(MIN_THUMB, Math.round(ratio.w * scale));
	const h = Math.max(MIN_THUMB, Math.round(ratio.h * scale));
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		"data-ui": "paintings.ratio-shape",
		className: cn("inline-block rounded-[2px] border border-current transition-all", !selected && "opacity-40"),
		style: {
			width: w,
			height: h
		}
	});
}
function RatioThumb({ value, selected }) {
	const ratio = parseRatio(value);
	if (!ratio) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		"data-ui": "paintings.ratio-thumb",
		className: "flex shrink-0 items-center justify-center",
		style: {
			width: MAX_THUMB,
			height: MAX_THUMB
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RatioShape, {
			ratio,
			selected
		})
	});
}
function autoColumns(options, explicit) {
	if (explicit) return explicit;
	let longest = 0;
	for (const option of options) {
		const label = deriveChipLabel(String(option.label ?? option.value), String(option.value));
		if (label.length > longest) longest = label.length;
	}
	return longest >= 8 ? 2 : DEFAULT_COLUMNS;
}
function SizeChipsField({ item, fieldKey, painting, translate, onChange, currentValue, disabled }) {
	const options = resolveOptions(item, painting, translate);
	const value = currentValue == null ? "" : String(currentValue);
	const columns = autoColumns(options, item.columns);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "paintings.size-chips-field",
		className: "grid gap-2",
		style: { gridTemplateColumns: `repeat(${columns}, minmax(0, 1fr))` },
		children: options.map((option) => {
			const optionValue = String(option.value);
			const label = option.label || optionValue;
			const isSelected = value === optionValue;
			const chipLabel = deriveChipLabel(label, optionValue);
			return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
				type: "button",
				disabled,
				title: label,
				className: cn(chipClass.base, isSelected ? chipClass.active : chipClass.inactive, disabled && chipClass.disabled),
				onClick: () => onChange({ [fieldKey]: optionValue }),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RatioThumb, {
					value: optionValue,
					selected: isSelected
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "block max-w-full truncate font-medium tracking-tight",
					children: chipLabel
				})]
			}, optionValue);
		})
	});
}
function SizeField({ item, painting, onChange }) {
	const { t } = useTranslation();
	const { widthKey = "width", heightKey = "height", validation = {} } = item;
	const widthValue = painting[widthKey] ?? "";
	const heightValue = painting[heightKey] ?? "";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "paintings.size-field",
		className: "flex flex-col gap-2",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RowFlex, {
			className: "items-center gap-2",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
					"aria-label": t("paintings.generate.width"),
					placeholder: t("paintings.generate.width"),
					type: "number",
					value: widthValue === void 0 || widthValue === null ? "" : String(widthValue),
					onChange: (event) => {
						const value = event.target.value === "" ? "" : Number(event.target.value);
						onChange({ [widthKey]: value });
					},
					min: validation.minWidth,
					max: validation.maxWidth,
					className: "flex-1"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "text-muted-foreground text-xs",
					children: "x"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
					"aria-label": t("paintings.generate.height"),
					placeholder: t("paintings.generate.height"),
					type: "number",
					value: heightValue === void 0 || heightValue === null ? "" : String(heightValue),
					onChange: (event) => {
						const value = event.target.value === "" ? "" : Number(event.target.value);
						onChange({ [heightKey]: value });
					},
					min: validation.minHeight,
					max: validation.maxHeight,
					className: "flex-1"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "text-muted-foreground text-xs",
					children: "px"
				})
			]
		})
	});
}
const fieldRegistry = {
	select: SelectField,
	sizeChips: SizeChipsField,
	customSize: SizeField
};
function PaintingFieldRenderer({ item, painting, onChange, onGenerateRandomSeed }) {
	const { t } = useTranslation();
	const fieldKey = item.key;
	if (!fieldKey) return null;
	const disabled = typeof item.disabled === "function" ? item.disabled(item, painting) : item.disabled;
	const currentValue = painting[fieldKey] ?? item.initialValue;
	const RegisteredField = fieldRegistry[item.type];
	if (RegisteredField) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RegisteredField, {
		item,
		fieldKey,
		painting,
		translate: t,
		onChange,
		onGenerateRandomSeed,
		currentValue,
		disabled
	});
	switch (item.type) {
		case "radio": {
			const options = resolveOptions(item, painting, t);
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroup, {
				value: currentValue !== void 0 && currentValue !== null ? String(currentValue) : "",
				className: "flex flex-wrap gap-3",
				onValueChange: (nextValue) => onChange({ [fieldKey]: nextValue }),
				children: options.map((option) => {
					const optionValue = String(option.value);
					const inputId = `${fieldKey}-${optionValue}`;
					return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", {
						htmlFor: inputId,
						className: "flex cursor-pointer items-center gap-2 text-sm",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroupItem, {
							id: inputId,
							value: optionValue
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: option.label })]
					}, optionValue);
				})
			});
		}
		case "slider": {
			const numericValue = Number(currentValue ?? item.min ?? 0);
			const min = item.min ?? 0;
			const max = item.max ?? 100;
			if (min === max) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				className: "w-20",
				type: "number",
				value: String(numericValue),
				readOnly: true,
				disabled: true
			});
			return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				"data-ui": "paintings.painting-field",
				className: "flex items-center gap-3",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Slider, {
					className: "flex-1",
					min,
					max,
					step: item.step ?? 1,
					value: [numericValue],
					onValueChange: (values) => onChange({ [fieldKey]: values[0] })
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
					className: "w-20",
					type: "number",
					min,
					max,
					step: item.step,
					value: String(numericValue),
					onChange: (event) => {
						const raw = event.target.value;
						if (raw === "") return;
						const parsed = Number(raw);
						if (Number.isNaN(parsed)) return;
						onChange({ [fieldKey]: Math.min(max, Math.max(min, parsed)) });
					}
				})]
			});
		}
		case "input": return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			"data-ui": "paintings.painting-field",
			className: "flex items-center gap-2",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input, {
				disabled,
				className: "flex-1",
				value: currentValue === void 0 || currentValue === null ? "" : String(currentValue),
				onChange: (event) => onChange({ [fieldKey]: event.target.value })
			}), fieldKey.toLowerCase().includes("seed") && onGenerateRandomSeed ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				type: "button",
				size: "icon-sm",
				variant: "outline",
				onClick: () => onGenerateRandomSeed(fieldKey),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RotateCcw, { size: 14 })
			}) : null]
		});
		case "textarea": return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Input$1, {
			value: currentValue === void 0 || currentValue === null ? "" : String(currentValue),
			rows: 4,
			onValueChange: (nextValue) => onChange({ [fieldKey]: nextValue })
		});
		case "switch": return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "paintings.painting-field",
			className: "flex items-center",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Switch, {
				checked: Boolean(currentValue),
				onCheckedChange: (checked) => onChange({ [fieldKey]: checked })
			})
		});
		case "iconRadio": {
			const options = resolveOptions(item, painting, t);
			const value = currentValue !== void 0 && currentValue !== null ? String(currentValue) : "";
			const columns = item.columns || 3;
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroup, {
				value,
				"aria-label": item.title ? t(item.title) : fieldKey,
				className: "grid gap-2",
				style: { gridTemplateColumns: `repeat(${columns}, minmax(0, 1fr))` },
				onValueChange: (nextValue) => onChange({ [fieldKey]: nextValue }),
				children: options.map((option) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", {
					htmlFor: `${fieldKey}-${option.value}`,
					className: `flex cursor-pointer flex-col items-center justify-center gap-1 rounded-[10px] px-2 py-1.5 text-[11px] transition-all ${value === String(option.value) ? "bg-secondary-active text-foreground ring-1 ring-[color:color-mix(in_oklch,var(--foreground)_33.3333%,transparent)]" : "bg-muted text-foreground-tertiary hover:bg-secondary-hover hover:text-foreground"}`,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroupItem, {
							value: String(option.value),
							id: `${fieldKey}-${option.value}`,
							className: "sr-only"
						}),
						option.icon && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: "flex items-center justify-center bg-transparent",
							"aria-hidden": true,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								className: `h-3 w-3 bg-current transition-opacity ${value === String(option.value) ? "opacity-100" : "opacity-60"}`,
								style: {
									mask: `url(${option.icon}) center / contain no-repeat`,
									WebkitMask: `url(${option.icon}) center / contain no-repeat`
								}
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: "font-medium tracking-tight",
							children: option.label
						})
					]
				}, String(option.value)))
			});
		}
		case "styleToggle": {
			const options = resolveOptions(item, painting, t);
			const { toggleMode = "single" } = item;
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				"data-ui": "paintings.painting-field",
				className: "flex flex-wrap items-start gap-2",
				children: options.map((option) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
					type: "button",
					className: `rounded-[6px] border px-[6px] py-[2px] transition-all ${currentValue === String(option.value) ? "border-primary bg-primary text-primary-foreground" : "border-border bg-background hover:bg-accent"}`,
					onClick: () => {
						if (toggleMode === "single" && currentValue === String(option.value)) onChange({ [fieldKey]: "" });
						else onChange({ [fieldKey]: String(option.value) });
					},
					children: option.label
				}, String(option.value)))
			});
		}
		default: return null;
	}
}
function resolveItemOptions(item, painting) {
	return typeof item.options === "function" ? item.options(item, painting) : item.options ?? [];
}
function shouldRenderConfigItem(item, painting) {
	if (item.condition && !item.condition(painting)) return false;
	if (item.type === "sizeChips" && resolveItemOptions(item, painting).length === 0) return false;
	return true;
}
var PaintingSettings = ({ painting, onConfigChange, onGenerateRandomSeed }) => {
	const { t } = useTranslation();
	const paintingParams = painting.params ?? {};
	const registrySupport = useImageGenerationSupport(painting.providerId, painting.model);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: (0, import_react.useMemo)(() => imageGenerationToFields(registrySupport, { mode: tabToImageGenerationMode(painting.mode) }), [registrySupport, painting.mode]).filter((item) => shouldRenderConfigItem(item, paintingParams)).map((item) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "paintings.painting-settings",
		children: [item.title && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(PaintingSectionTitle_default, { children: [t(item.title), item.tooltip && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InfoTooltip, { content: t(item.tooltip, {
			min: item.min,
			max: item.max
		}) })] }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingFieldRenderer, {
			item,
			painting: paintingParams,
			onChange: (updates) => onConfigChange({ params: {
				...paintingParams,
				...updates
			} }),
			onGenerateRandomSeed
		})]
	}, item.key ?? `${item.type}-${item.title ?? ""}`)) });
};
var PaintingSettings_default = PaintingSettings;
var PAINTING_MANAGED_TOKEN_KINDS = ["file"];
var PAINTING_NO_MANAGED_TOKEN_KINDS = [];
var EMPTY_TOKENS = [];
var PAINTING_IMAGE_EXTS = imageExts.map((ext) => ext.startsWith(".") ? ext : `.${ext}`);
var PAINTING_SCOPE = "painting";
var SUMMARY_TYPES = new Set([
	"select",
	"sizeChips",
	"slider",
	"radio",
	"iconRadio",
	"styleToggle"
]);
function formatSummaryValue(item, value, params, translate) {
	if (SIZE_PREVIEW_KEYS.includes(item.key ?? "")) {
		if (value === "custom") {
			const w = params?.customSize_width;
			const h = params?.customSize_height;
			return w && h ? `${String(w)}×${String(h)}` : void 0;
		}
		return sizeOptionLabel(item, String(value), params, translate);
	}
	if (item.type === "slider") return String(value);
	return resolveOptions(item, params ?? {}, translate).find((opt) => String(opt.value) === String(value))?.label ?? String(value);
}
function paramsSummary(params, items, translate) {
	const parts = [];
	for (const item of items) {
		if (!item.key || !SUMMARY_TYPES.has(item.type)) continue;
		if (item.condition && !item.condition(params ?? {})) continue;
		const value = params?.[item.key] ?? item.initialValue;
		if (value === void 0 || value === null || value === "") continue;
		const formatted = formatSummaryValue(item, value, params, translate);
		if (formatted) parts.push(formatted);
	}
	return parts.join(" · ");
}
var PaintingParamsButton = ({ painting, onConfigChange, onGenerateRandomSeed }) => {
	const { t } = useTranslation();
	const registrySupport = useImageGenerationSupport(painting.providerId, painting.model);
	const configItems = (0, import_react.useMemo)(() => imageGenerationToFields(registrySupport, { mode: tabToImageGenerationMode(painting.mode) }), [registrySupport, painting.mode]);
	const summary = (0, import_react.useMemo)(() => paramsSummary(painting.params, configItems, t), [
		painting.params,
		configItems,
		t
	]);
	if (configItems.length === 0) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverTrigger, {
		asChild: true,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UiDataSlot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button, {
			type: "button",
			variant: "ghost",
			size: "sm",
			className: cn(COMPOSER_SELECTOR_BUTTON_CLASS, "text-muted-foreground"),
			"aria-label": summary ? `${t("common.settings")}: ${summary}` : t("common.settings"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Settings2, { className: "size-4" }), summary && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "max-w-55 truncate",
				title: summary,
				children: summary
			})]
		}) })
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContent, {
		align: "start",
		side: "top",
		className: "w-[min(340px,calc(100vw-2rem))] rounded-[8px] p-3",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "flex max-h-[60vh] flex-col gap-4 overflow-y-auto pr-1",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingSettings_default, {
				painting,
				onConfigChange,
				onGenerateRandomSeed
			})
		})
	})] });
};
var PaintingComposerInner = ({ painting, generating, submitting, onPromptChange, onGenerate, onCancel, onModelSelect, onConfigChange, onGenerateRandomSeed, model, couldAddImageFile }) => {
	const { t } = useTranslation();
	const { files, isExpanded } = useComposerToolState();
	const { setFiles, setIsExpanded } = useComposerToolDispatch();
	const { getLaunchers, dispatchLauncher } = useComposerToolLauncherActions();
	const toolLaunchersVersion = useComposerToolLauncherVersion();
	const text = painting.prompt ?? "";
	const [enableSpellCheck] = usePreference("app.spell_check.enabled");
	const [fontSize] = usePreference("chat.message.font_size");
	const config = getComposerToolConfig(PAINTING_SCOPE);
	const support = useImageGenerationSupport(painting.providerId, painting.model);
	const imageRequired = couldAddImageFile && !!support?.modes && !support.modes.generate && Object.keys(support.modes).length > 0;
	const draftImageCount = files.filter((file) => file.type === FILE_TYPE.IMAGE).length;
	const missingRequiredImage = imageRequired && draftImageCount === 0;
	const placeholder = !couldAddImageFile ? t("paintings.prompt_placeholder") : imageRequired ? t("paintings.prompt_placeholder_upload_required") : t("paintings.prompt_placeholder_upload");
	const inputCapability = !model ? "unknown" : couldAddImageFile ? "accept" : "reject";
	const { materializeInputs } = usePaintingComposerInputFiles({
		paintingId: painting.id,
		inputFiles: painting.inputFiles ?? [],
		files,
		setFiles,
		inputCapability,
		providerId: painting.providerId
	});
	const tokens = (0, import_react.useMemo)(() => couldAddImageFile ? EMPTY_TOKENS : files.map(fileToComposerToken), [couldAddImageFile, files]);
	const handleTokensChange = useComposerTokenReconcile({
		scope: PAINTING_SCOPE,
		model
	});
	const handleTextChange = (0, import_react.useCallback)((value) => onPromptChange(value), [onPromptChange]);
	const handleSendDraft = (0, import_react.useCallback)(() => onGenerate(materializeInputs), [materializeInputs, onGenerate]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ComposerToolDerivedStateProvider, {
		couldAddImageFile,
		extensions: PAINTING_IMAGE_EXTS,
		children: [model && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComposerToolRuntimeHost, {
			scope: PAINTING_SCOPE,
			model
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComposerSurface, {
			text,
			onTextChange: handleTextChange,
			tokens,
			managedTokenKinds: couldAddImageFile ? PAINTING_NO_MANAGED_TOKEN_KINDS : PAINTING_MANAGED_TOKEN_KINDS,
			onTokensChange: handleTokensChange,
			topContent: couldAddImageFile ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingImageGallery, {}) : void 0,
			leadingContent: couldAddImageFile ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingImageAddButton, {}) : void 0,
			placeholder,
			sendDisabled: generating || submitting || !model || text.trim().length === 0 && files.length === 0 || missingRequiredImage,
			sendBlockedReason: missingRequiredImage ? t("paintings.edit.image_required") : void 0,
			isLoading: generating,
			onSendDraft: handleSendDraft,
			onPause: onCancel,
			supportedExts: PAINTING_IMAGE_EXTS,
			setFiles,
			filesCount: files.length,
			isExpanded,
			onExpandedChange: setIsExpanded,
			quickPanelEnabled: config.enableQuickPanel ?? false,
			enableDragDrop: config.enableDragDrop ?? true,
			enableSpellCheck,
			fontSize,
			narrowMode: true,
			getToolLaunchers: () => getLaunchers(),
			toolLaunchersVersion,
			onToolLauncherSelect: (launcher, options) => dispatchLauncher(launcher, options),
			renderLeftControls: (inputAdapter, unifiedPanelControl) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComposerToolbarControls, {
				inputAdapter,
				unifiedPanelControl,
				renderContextControls: () => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingModelSelector_default, {
					hideTitle: true,
					painting,
					onSelect: onModelSelect,
					className: cn(COMPOSER_SELECTOR_BUTTON_CLASS, "w-auto max-w-[280px] border border-border-subtle")
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingParamsButton, {
					painting,
					onConfigChange,
					onGenerateRandomSeed
				})] })
			})
		})]
	});
};
var PaintingComposer = (props) => {
	const { painting } = props;
	const { models } = useModels(painting.providerId ? { providerId: painting.providerId } : void 0);
	const model = (0, import_react.useMemo)(() => painting.model ? models.find((entry) => entry.providerId === painting.providerId && entry.apiModelId === painting.model) : void 0, [
		models,
		painting.providerId,
		painting.model
	]);
	const couldAddImageFile = model ? isEditImageModel(model) : false;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComposerToolRuntimeProvider, {
		initialState: {
			files: [],
			couldAddImageFile,
			extensions: PAINTING_IMAGE_EXTS
		},
		actions: {
			addNewTopic: () => {},
			onTextChange: () => {}
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingComposerInner, {
			...props,
			model,
			couldAddImageFile
		})
	}, painting.id);
};
var PaintingComposer_default = PaintingComposer;
var PaintingStripItem = ({ painting, selected, loading, onDelete, onSelect, selectLabel, deleteLabel }) => {
	const previewFile = painting.files?.[0];
	const previewUrl = previewFile ? getPaintingFileUrl(previewFile) : void 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		"data-ui": "paintings.painting-strip-item",
		className: cn(paintingClasses.historyItem, selected && paintingClasses.historyItemActive),
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				className: "absolute inset-0 z-0",
				"aria-label": selectLabel,
				onClick: () => onSelect(painting),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: "pointer-events-none absolute inset-0 overflow-hidden rounded-[12px]",
					children: previewUrl ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
						src: previewUrl,
						alt: "",
						className: "h-full w-full object-cover"
					}) : loading ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: "size-full bg-background",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingSkeletonSurface_default, {})
					}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "block size-full bg-muted/60",
						"aria-hidden": true
					})
				})
			}),
			selected && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				"aria-hidden": true,
				className: "pointer-events-none absolute inset-0 z-10 rounded-[12px] ring-1 ring-muted-foreground/55 ring-inset"
			}),
			loading && previewFile && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: "pointer-events-none absolute inset-x-1 bottom-1 z-10 h-1 overflow-hidden rounded-full bg-black/10",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "block h-full w-5 animate-[painting-history-loading_1.2s_ease-in-out_infinite] rounded-full bg-foreground/70" })
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				"aria-label": deleteLabel,
				className: paintingClasses.historyDelete,
				onClick: (event) => {
					event.stopPropagation();
					onDelete(painting);
				},
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trash2, { className: "size-3" })
			})
		]
	});
};
var PaintingStrip = ({ selectedPaintingId, runningPaintingId, items, hasMore, loadMore, onDeletePainting, onSelectPainting, onAddPainting }) => {
	const { t } = useTranslation();
	const [pendingDelete, setPendingDelete] = (0, import_react.useState)(null);
	const stripRef = (0, import_react.useRef)(null);
	const handleScroll = (event) => {
		const target = event.currentTarget;
		if (target.scrollTop + target.clientHeight >= target.scrollHeight - 120) loadMore();
	};
	(0, import_react.useEffect)(() => {
		const strip = stripRef.current;
		if (hasMore && strip && strip.scrollHeight <= strip.clientHeight) loadMore();
	}, [
		hasMore,
		items.length,
		loadMore
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			"data-ui": "paintings.painting-strip",
			ref: stripRef,
			className: paintingClasses.historyStrip,
			onScroll: handleScroll,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip, {
					content: t("paintings.button.new.image"),
					placement: "right",
					delay: 500,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						type: "button",
						variant: "ghost",
						size: "icon-sm",
						className: paintingClasses.historyAddButton,
						"aria-label": t("paintings.button.new.image"),
						onClick: onAddPainting,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, { className: "size-4" })
					})
				}),
				items.map((painting) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingStripItem, {
					painting,
					selected: painting.id === selectedPaintingId,
					loading: painting.id === runningPaintingId,
					onDelete: setPendingDelete,
					onSelect: onSelectPainting,
					selectLabel: t("paintings.button.select.image"),
					deleteLabel: t("paintings.button.delete.image.label")
				}, painting.id)),
				hasMore && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoaderCircle, {
					className: "mx-auto size-4 shrink-0 animate-spin text-foreground-tertiary",
					"aria-hidden": true
				})
			]
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("style", { children: `
        @keyframes painting-history-loading {
          0% { transform: translateX(-150%); }
          100% { transform: translateX(260%); }
        }
      ` }),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfirmDialog, {
			open: Boolean(pendingDelete),
			onOpenChange: (open) => {
				if (!open) setPendingDelete(null);
			},
			title: t("paintings.button.delete.image.confirm"),
			confirmText: t("common.delete"),
			cancelText: t("common.cancel"),
			destructive: true,
			onConfirm: () => {
				if (pendingDelete) onDeletePainting(pendingDelete);
				setPendingDelete(null);
			}
		})
	] });
};
var PaintingStrip_default = PaintingStrip;
var carouselPositions = [
	{
		x: "calc(-50% - 23cqw)",
		y: "calc(-50% + 8px)",
		rotation: "-7deg",
		scale: .82,
		className: "z-10 opacity-70"
	},
	{
		x: "calc(-50% - 11.5cqw)",
		y: "calc(-50% + 3px)",
		rotation: "-3deg",
		scale: .92,
		className: "z-20 opacity-90"
	},
	{
		x: "-50%",
		y: "calc(-50% - 2px)",
		rotation: "0deg",
		scale: 1.12,
		className: "z-40 opacity-100"
	},
	{
		x: "calc(-50% + 11.5cqw)",
		y: "calc(-50% + 3px)",
		rotation: "3deg",
		scale: .92,
		className: "z-20 opacity-90"
	},
	{
		x: "calc(-50% + 23cqw)",
		y: "calc(-50% + 8px)",
		rotation: "7deg",
		scale: .82,
		className: "z-10 opacity-70"
	}
];
var PaintingTemplateShowcase = ({ paintingId, prompt, templates, onSelect }) => {
	const { t } = useTranslation();
	const [styleSelection, setStyleSelection] = (0, import_react.useState)();
	const promptedStyleIndex = templates.findIndex((preset) => preset.prompt === prompt);
	const rememberedStyleIndex = styleSelection?.paintingId === paintingId ? templates.findIndex((preset) => preset.id === styleSelection.presetId) : -1;
	const selectedStyleIndex = rememberedStyleIndex >= 0 ? rememberedStyleIndex : promptedStyleIndex;
	const activeStyleIndex = selectedStyleIndex >= 0 ? selectedStyleIndex : 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "paintings.painting-template-showcase.group",
		className: "relative flex max-h-[220px] min-h-0 w-full flex-1 items-center justify-center overflow-hidden [container-type:size]",
		role: "group",
		"aria-label": t("paintings.showcase.styles_label"),
		children: templates.map((preset, index) => {
			const isSelected = index === selectedStyleIndex;
			const middlePosition = Math.floor(templates.length / 2);
			const relativePosition = (index - activeStyleIndex + middlePosition + templates.length) % templates.length - middlePosition;
			const isHidden = Math.abs(relativePosition) > 2;
			const carouselPosition = carouselPositions[Math.max(-2, Math.min(2, relativePosition)) + 2] ?? carouselPositions[2];
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NormalTooltip, {
				content: preset.label,
				side: "top",
				sideOffset: 8,
				delayDuration: 150,
				contentProps: { className: "max-w-none whitespace-nowrap px-2 py-1 font-medium" },
				...isSelected ? { open: true } : void 0,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					type: "button",
					variant: "ghost",
					"aria-pressed": isSelected,
					"aria-hidden": isHidden || void 0,
					"aria-label": preset.label,
					tabIndex: isHidden ? -1 : 0,
					className: cn("group absolute top-1/2 left-1/2 h-[clamp(44px,min(18cqw,66cqh),148px)] w-[clamp(36px,min(14cqw,53cqh),118px)] overflow-visible rounded-xl p-0 opacity-100 transition-[transform,opacity,box-shadow] duration-500 ease-[cubic-bezier(0.22,1,0.36,1)] will-change-transform hover:z-50 hover:bg-transparent hover:shadow-lg focus-visible:z-50 focus-visible:ring-2 focus-visible:ring-muted-foreground focus-visible:ring-inset focus-visible:ring-offset-0", carouselPosition.className, isHidden && "pointer-events-none opacity-0", isSelected && "shadow-md"),
					style: { transform: `translate(${carouselPosition.x}, ${carouselPosition.y}) rotate(${carouselPosition.rotation}) scale(${carouselPosition.scale})` },
					onClick: () => {
						setStyleSelection({
							paintingId,
							presetId: preset.id
						});
						onSelect(preset.prompt);
					},
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: "pointer-events-none relative size-full overflow-hidden rounded-xl bg-card",
						children: preset.imageUrl ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
							src: preset.imageUrl,
							alt: "",
							draggable: false,
							className: "size-full object-cover"
						}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							"aria-hidden": true,
							className: "absolute inset-0 bg-[linear-gradient(145deg,var(--muted),transparent_58%)]"
						})
					})
				})
			}, preset.id);
		})
	});
};
var PaintingTemplateShowcase_default = PaintingTemplateShowcase;
var PaintingGenerateError = class extends Error {
	code;
	presentation;
	severity;
	constructor(code, options = {}) {
		super(options.message || code);
		this.name = "PaintingGenerateError";
		this.code = code;
		this.presentation = options.presentation || "modal";
		this.severity = options.severity || "error";
	}
};
function createPaintingGenerateError(code, options) {
	return new PaintingGenerateError(code, options);
}
function normalizePaintingGenerateError(error) {
	if (error instanceof PaintingGenerateError) return error;
	if (error instanceof Error) return createPaintingGenerateError("REMOTE_ERROR", { message: error.message });
	return createPaintingGenerateError("GENERATE_FAILED");
}
function translatePaintingGenerateError(error) {
	if (!(error instanceof PaintingGenerateError)) return error.message;
	if (error.code === "REMOTE_ERROR") return error.message || resolver_default.t("paintings.generate_failed");
	return resolver_default.t({
		PROVIDER_DISABLED: "error.provider_disabled",
		PROMPT_REQUIRED: "paintings.prompt_required",
		TEXT_DESC_REQUIRED: "paintings.text_desc_required",
		IMAGE_REQUIRED: "paintings.image_file_required",
		IMAGE_RETRY_REQUIRED: "paintings.image_file_retry",
		EDIT_IMAGE_REQUIRED: "paintings.edit.image_required",
		INPUT_IMAGE_LIMIT_EXCEEDED: "paintings.input_image_limit_exceeded",
		MISSING_REQUIRED_FIELDS: "error.missing_required_fields",
		IMAGE_HANDLE_REQUIRED: "paintings.image_handle_required",
		REQ_ERROR_TOKEN: "paintings.req_error_token",
		REQ_ERROR_NO_BALANCE: "paintings.req_error_no_balance",
		OPERATION_FAILED: "paintings.operation_failed",
		GENERATE_FAILED: "paintings.generate_failed",
		IMAGE_MIX_FAILED: "paintings.image_mix_failed",
		CUSTOM_SIZE_REQUIRED: "paintings.zhipu.custom_size_required",
		CUSTOM_SIZE_RANGE: "paintings.zhipu.custom_size_range",
		CUSTOM_SIZE_DIVISIBLE: "paintings.zhipu.custom_size_divisible",
		CUSTOM_SIZE_PIXELS: "paintings.zhipu.custom_size_pixels"
	}[error.code]);
}
function presentPaintingGenerateError(error) {
	const normalized = normalizePaintingGenerateError(error);
	const message = translatePaintingGenerateError(normalized);
	if (normalized instanceof PaintingGenerateError && normalized.presentation === "toast") {
		if (normalized.severity === "warning") toast.warning(message);
		else toast.error(message);
		return;
	}
	popup.error({
		content: message,
		centered: true
	});
}
function openProviderSettings(providerId) {
	openSettingsTab(`/settings/provider?id=${encodeURIComponent(providerId)}`);
}
async function presentPaintingGenerationGuardFeedback(reason, error, providerId) {
	if (reason === "provider_disabled") {
		if (providerId) {
			if (await popup.warning({
				content: resolver_default.t("error.provider_disabled"),
				centered: true,
				closable: true,
				okText: resolver_default.t("common.go_to_settings")
			})) openProviderSettings(providerId);
			return;
		}
		presentPaintingGenerateError(createPaintingGenerateError("PROVIDER_DISABLED"));
		return;
	}
	if (reason === "catalog_error") {
		toast.error(error?.message || resolver_default.t("paintings.req_error_model"));
		return;
	}
	if (reason === "model_unavailable") {
		toast.error(resolver_default.t("paintings.req_error_model"));
		return;
	}
	toast.error(resolver_default.t("paintings.select_model"));
}
function usePaintings(query) {
	const filtered = query ? omitBy(query, isUndefined) : void 0;
	const { data, isLoading, refetch } = useQuery("/paintings", filtered && Object.keys(filtered).length > 0 ? { query: filtered } : void 0);
	const { trigger: createTrigger } = useMutation("POST", "/paintings", { refresh: ["/paintings"] });
	const { trigger: updateTrigger } = useMutation("PATCH", "/paintings/:id", { refresh: ["/paintings"] });
	const { trigger: deleteTrigger } = useMutation("DELETE", "/paintings/:id", { refresh: ["/paintings"] });
	const { applyReorderedList } = useReorder("/paintings");
	const createPainting = (0, import_react.useCallback)((painting) => {
		return createTrigger({ body: painting });
	}, [createTrigger]);
	const updatePainting = (0, import_react.useCallback)((id, updates) => {
		return updateTrigger({
			params: { id },
			body: updates
		});
	}, [updateTrigger]);
	const deletePainting = (0, import_react.useCallback)((id) => {
		return deleteTrigger({ params: { id } });
	}, [deleteTrigger]);
	const reorderPaintings = (0, import_react.useCallback)((paintings) => {
		return applyReorderedList(paintings);
	}, [applyReorderedList]);
	return {
		records: data?.items ?? [],
		total: data?.total ?? 0,
		isLoading,
		refresh: refetch,
		createPainting,
		updatePainting,
		deletePainting,
		reorderPaintings
	};
}
function getTopLevelFileIds(files) {
	if (!Array.isArray(files)) return [];
	return files.flatMap((file) => {
		if (file && typeof file === "object" && "id" in file && typeof file.id === "string") return [file.id];
		return [];
	});
}
function paintingFileIdsForPersistence(files) {
	return getTopLevelFileIds(files);
}
function paintingDataToCreateDto(painting) {
	return {
		id: painting.id,
		providerId: painting.providerId,
		modelId: typeof painting.model === "string" && painting.model.trim() ? painting.model : void 0,
		prompt: painting.prompt,
		files: {
			output: getTopLevelFileIds(painting.files),
			input: getTopLevelFileIds(painting.inputFiles)
		}
	};
}
function paintingDataToUpdateDto(painting) {
	return {
		providerId: painting.providerId,
		modelId: typeof painting.model === "string" && painting.model.trim() ? painting.model : void 0,
		prompt: painting.prompt,
		files: {
			output: paintingFileIdsForPersistence(painting.files),
			input: paintingFileIdsForPersistence(painting.inputFiles)
		}
	};
}
var abortControllers = /* @__PURE__ */ new Map();
function registerPaintingAbortController(paintingId, controller) {
	abortControllers.get(paintingId)?.abort();
	abortControllers.set(paintingId, controller);
}
function clearPaintingAbortController(paintingId, controller) {
	if (!controller || abortControllers.get(paintingId) === controller) abortControllers.delete(paintingId);
}
function abortPaintingGeneration(paintingId) {
	abortControllers.get(paintingId)?.abort();
}
function navigateToProviderSettings(providerId) {
	openSettingsTab(`/settings/provider?id=${encodeURIComponent(providerId)}`);
}
async function checkProviderEnabled(provider) {
	if (!provider.isEnabled) {
		if (await popup.warning({
			content: instance.t("error.provider_disabled"),
			centered: true,
			closable: true,
			okText: instance.t("common.go_to_settings")
		})) navigateToProviderSettings(provider.id);
		throw "Provider disabled";
	}
	return provider.getApiKey();
}
async function fileEntryToMetadata(entry) {
	const path = await window.api.file.getPhysicalPath({ id: entry.id });
	const dottedExt = entry.ext ? `.${entry.ext}` : "";
	const onDiskName = `${entry.id}${dottedExt}`;
	const displayName = `${entry.name}${dottedExt}`;
	const size = entry.origin === "internal" ? entry.size : 0;
	return {
		id: entry.id,
		name: onDiskName,
		origin_name: displayName,
		path,
		size,
		ext: dottedExt,
		type: "image",
		created_at: new Date(entry.createdAt).toISOString(),
		count: 1
	};
}
var logger$8 = loggerService.withContext("paintings/downloadImages");
async function downloadImages(urls, options) {
	const { allowBase64DataUrls = false, showProxyWarning = false } = options ?? {};
	return (await Promise.all(urls.map(async (url) => {
		try {
			if (!url?.trim()) {
				logger$8.error("Image URL is empty, possibly due to prohibited prompt");
				toast.warning(instance.t("message.empty_url"));
				return null;
			}
			return await fileEntryToMetadata(allowBase64DataUrls && url.startsWith("data:image") ? await window.api.file.createInternalEntry({
				source: "base64",
				data: url,
				cleanupPolicy: "delete_when_unreferenced"
			}) : await window.api.file.createInternalEntry({
				source: "url",
				url,
				cleanupPolicy: "delete_when_unreferenced"
			}));
		} catch (error) {
			logger$8.error(`Failed to download image: ${error}`);
			if (error instanceof Error && (error.message.includes("Failed to parse URL") || error.message.includes("Invalid URL"))) toast.warning(instance.t("message.empty_url"));
			else if (showProxyWarning) toast.warning(instance.t("paintings.proxy_required"));
			return null;
		}
	}))).filter((file) => file !== null);
}
var logger$7 = loggerService.withContext("paintings/generation");
function aiDetailMessage(detail) {
	if (detail.message) return detail.message;
	return [typeof detail.statusCode === "number" ? `HTTP ${detail.statusCode}` : "", typeof detail.responseBody === "string" ? detail.responseBody.slice(0, 300) : ""].filter(Boolean).join(" ") || detail.name || "Image generation failed";
}
async function resolvePaintingFiles(result) {
	let files = [];
	if ("files" in result) files = result.files;
	else if ("base64s" in result) {
		const entries = await Promise.all(result.base64s.map((b64) => window.api.file.createInternalEntry({
			source: "base64",
			data: `data:image/png;base64,${b64}`,
			cleanupPolicy: "delete_when_unreferenced"
		})));
		files = await Promise.all(entries.map(fileEntryToMetadata));
	} else if ("urls" in result && result.urls.length > 0) files = await downloadImages(result.urls, result.downloadOptions);
	if (files.length === 0) throw createPaintingGenerateError("GENERATE_FAILED");
	return files;
}
async function runPainting(generate) {
	try {
		const result = await generate();
		if (!result) throw createPaintingGenerateError("GENERATE_FAILED");
		if (Array.isArray(result)) {
			if (result.length === 0) throw createPaintingGenerateError("GENERATE_FAILED");
			return result;
		}
		return resolvePaintingFiles(result);
	} catch (error) {
		if (error instanceof Error && error.name !== "AbortError") {
			const detail = aiErrorDetail(error);
			const logPayload = detail ? {
				name: detail.name,
				message: detail.message,
				statusCode: detail.statusCode,
				responseBody: typeof detail.responseBody === "string" ? detail.responseBody.slice(0, 500) : void 0
			} : error instanceof Error ? {
				name: error.name,
				message: error.message
			} : { message: String(error) };
			logger$7.error("Image generation failed:", logPayload);
			if (detail) throw createPaintingGenerateError("REMOTE_ERROR", { message: aiDetailMessage(detail) });
			throw normalizePaintingGenerateError(error);
		}
		throw error;
	}
}
function generatePainting(opts) {
	return runPainting(async () => {
		const requestId = crypto.randomUUID();
		const onAbort = () => void ipcApi.request("ai.image.abort", { requestId });
		opts.signal.addEventListener("abort", onAbort, { once: true });
		const result = await ipcApi.request("ai.image.generate", {
			requestId,
			payload: {
				uniqueModelId: `${opts.provider.id}::${opts.modelId}`,
				prompt: opts.prompt,
				...opts.mode && { mode: opts.mode },
				paramValues: opts.paramValues,
				cleanupPolicy: "delete_when_unreferenced",
				...opts.inputImages && opts.inputImages.length > 0 && { inputImages: opts.inputImages }
			}
		}).catch((error) => {
			if (opts.signal.aborted) throw new DOMException("Image generation aborted", "AbortError");
			throw error;
		}).finally(() => opts.signal.removeEventListener("abort", onAbort));
		if (opts.signal.aborted) throw new DOMException("Image generation aborted", "AbortError");
		if (result.files.length === 0) return;
		const files = await Promise.all(result.files.map(fileEntryToMetadata));
		return files.length > 0 ? { files } : void 0;
	});
}
function bytesToDataUrl(bytes, mime) {
	let binary = "";
	const chunkSize = 32768;
	for (let offset = 0; offset < bytes.length; offset += chunkSize) binary += String.fromCharCode(...bytes.subarray(offset, offset + chunkSize));
	return `data:${mime || "image/png"};base64,${btoa(binary)}`;
}
async function blobToDataUrl(blob) {
	const buffer = await blob.arrayBuffer();
	return bytesToDataUrl(new Uint8Array(buffer), blob.type || "image/png");
}
async function loadInputImageDataUrl(entry) {
	const backendUrl = window.__CHERRY_BACKEND_URL?.replace(/\/$/, "");
	if (backendUrl) {
		const physicalPath = await window.api.file.getPhysicalPath({ id: entry.id });
		const response = await fetch(`${backendUrl}/api/v1/files/raw-image?path=${encodeURIComponent(String(physicalPath))}`);
		if (!response.ok) throw createPaintingGenerateError("IMAGE_RETRY_REQUIRED");
		return blobToDataUrl(await response.blob());
	}
	const onDiskName = `${entry.id}${entry.ext ? `.${entry.ext}` : ""}`;
	const result = await window.api.file.binaryImage(onDiskName);
	if (!result) throw createPaintingGenerateError("IMAGE_RETRY_REQUIRED");
	const mime = typeof result.mime === "string" && result.mime ? result.mime : "image/png";
	const raw = result.data;
	if (typeof raw === "string") {
		if (raw.startsWith("data:")) return raw;
		return `data:${mime};base64,${raw}`;
	}
	if (raw instanceof ArrayBuffer) return bytesToDataUrl(new Uint8Array(raw), mime);
	if (ArrayBuffer.isView(raw)) return bytesToDataUrl(new Uint8Array(raw.buffer, raw.byteOffset, raw.byteLength), mime);
	if (Array.isArray(raw)) return bytesToDataUrl(Uint8Array.from(raw), mime);
	throw createPaintingGenerateError("IMAGE_RETRY_REQUIRED");
}
async function canonicalGenerate(input, options = {}) {
	const { painting, provider, abortController } = input;
	options.preValidate?.(painting);
	const inputImageFiles = (painting.inputFiles ?? []).filter((entry) => getFileTypeByExt(entry.ext ?? "") === FILE_TYPE.IMAGE);
	const mode = options.mode ?? (inputImageFiles.length > 0 ? "edit" : "generate");
	const maxInputImages = options.support?.modes?.[mode]?.maxInputImages;
	if (maxInputImages !== void 0 && inputImageFiles.length > maxInputImages) throw createPaintingGenerateError("INPUT_IMAGE_LIMIT_EXCEEDED");
	if (mode !== "generate" && inputImageFiles.length === 0) throw createPaintingGenerateError("EDIT_IMAGE_REQUIRED");
	await checkProviderEnabled(provider);
	const modelId = painting.model;
	if (!modelId) throw createPaintingGenerateError("MISSING_REQUIRED_FIELDS");
	const prompt = (painting.prompt ?? "").trim();
	if ((typeof options.requirePrompt === "function" ? options.requirePrompt(painting) : options.requirePrompt ?? true) && !prompt) throw createPaintingGenerateError("PROMPT_REQUIRED");
	const rawParams = painting.params ?? {};
	const validated = buildParamsSchema(options.support, options.mode).safeParse(rawParams);
	const source = validated.success ? validated.data : rawParams;
	const paramValues = {};
	for (const [key, value] of Object.entries(source)) {
		if (key === "customSize_width" || key === "customSize_height") continue;
		if (value === void 0 || value === "" || value === null) continue;
		paramValues[key] = value;
	}
	if (paramValues.size === "custom") {
		const width = source.customSize_width;
		const height = source.customSize_height;
		if (typeof width === "number" && typeof height === "number") paramValues.size = `${width}x${height}`;
		else delete paramValues.size;
	}
	const inputImages = inputImageFiles.length > 0 ? await Promise.all(inputImageFiles.map((entry) => loadInputImageDataUrl(entry))) : void 0;
	return generatePainting({
		provider,
		signal: abortController.signal,
		modelId,
		prompt,
		...options.mode && { mode: options.mode },
		paramValues,
		...inputImages && { inputImages }
	});
}
var logger$6 = loggerService.withContext("paintings/paintingPipeline");
function createDefaultPainting(providerId) {
	return {
		id: uuid(),
		providerId,
		mode: "generate",
		prompt: "",
		files: [],
		params: {}
	};
}
async function paintingGenerate(input) {
	const modelId = input.painting.model;
	const canonicalMode = tabToImageGenerationMode(input.painting.mode);
	let requirePrompt;
	let support;
	let effectiveMode;
	if (modelId) try {
		support = await prefetch("/providers/:providerId/models/:modelId*/image-generation-support", { params: {
			providerId: input.provider.id,
			modelId
		} }) ?? void 0;
		const modes = support?.modes;
		effectiveMode = canonicalMode && modes?.[canonicalMode] ? canonicalMode : modes ? Object.keys(modes)[0] : void 0;
		requirePrompt = effectiveMode && modes ? modes[effectiveMode]?.requirePrompt : void 0;
	} catch (error) {
		logger$6.warn("Failed to prefetch image-generation support", {
			providerId: input.provider.id,
			modelId,
			mode: canonicalMode,
			error
		});
	}
	const options = {
		...requirePrompt !== void 0 && { requirePrompt },
		...support !== void 0 && { support },
		...effectiveMode !== void 0 && { mode: effectiveMode }
	};
	return canonicalGenerate(input, Object.keys(options).length > 0 ? options : void 0);
}
function paintingGenerationStateToCache(state) {
	if (!state.generationStatus) return null;
	return {
		status: state.generationStatus,
		taskId: state.generationTaskId ?? null,
		error: state.generationError ?? null,
		progress: state.generationProgress ?? null
	};
}
function cacheToPaintingGenerationState(cached) {
	if (!cached) return {
		generationStatus: null,
		generationTaskId: null,
		generationError: null,
		generationProgress: null
	};
	return {
		generationStatus: cached.status,
		generationTaskId: cached.taskId,
		generationError: cached.error,
		generationProgress: cached.progress
	};
}
var OPENAI_COMPAT_IMAGE_PROVIDER_IDS = new Set([
	"new-api",
	"cherryin",
	"aionly"
]);
function isPaintingNewApiProvider(provider) {
	return OPENAI_COMPAT_IMAGE_PROVIDER_IDS.has(provider.id) || provider.presetProviderId != null && OPENAI_COMPAT_IMAGE_PROVIDER_IDS.has(provider.presetProviderId);
}
function baseUrlFromEndpointConfigs(endpointConfigs, preferred) {
	if (!endpointConfigs) return "";
	const raw = endpointConfigs[ENDPOINT_TYPE.OPENAI_IMAGE_GENERATION]?.baseUrl || (preferred ? endpointConfigs[preferred]?.baseUrl : void 0) || endpointConfigs[ENDPOINT_TYPE.OPENAI_CHAT_COMPLETIONS]?.baseUrl || "";
	return raw ? withoutTrailingSlash(raw) : "";
}
function resolvePaintingApiHost(provider, presetEndpointConfigs) {
	if (!provider) return "";
	const configured = baseUrlFromEndpointConfigs(provider.endpointConfigs, provider.defaultChatEndpoint);
	if (configured) return configured;
	if (!isPaintingNewApiProvider(provider)) return "";
	return baseUrlFromEndpointConfigs(presetEndpointConfigs ?? void 0, provider.defaultChatEndpoint);
}
function pickFirstEnabledApiKey(apiKeys) {
	if (!apiKeys) return "";
	for (const entry of apiKeys) {
		if (!entry.isEnabled) continue;
		const trimmed = entry.key.trim();
		if (trimmed) return trimmed;
	}
	return "";
}
function createPaintingProviderRuntime(provider, providerId, apiKey, presetEndpointConfigs) {
	return {
		id: provider?.id || providerId,
		name: provider?.name || providerId,
		presetProviderId: provider?.presetProviderId,
		defaultChatEndpoint: provider?.defaultChatEndpoint,
		isEnabled: provider?.isEnabled ?? false,
		apiHost: resolvePaintingApiHost(provider, presetEndpointConfigs),
		getApiKey: async () => apiKey
	};
}
var ENDPOINT_CONFIG_PRESET_FIELDS = ["endpointConfigs"];
function usePaintingProviderRuntime(providerId) {
	const { provider, isLoading, error } = useProvider(providerId);
	const { data: apiKeysData } = useProviderApiKeys(providerId);
	const { data: preset, isLoading: isPresetLoading, error: presetError } = useProviderPreset(providerId, ENDPOINT_CONFIG_PRESET_FIELDS);
	const apiKey = (0, import_react.useMemo)(() => pickFirstEnabledApiKey(apiKeysData?.keys), [apiKeysData]);
	return {
		provider: (0, import_react.useMemo)(() => createPaintingProviderRuntime(provider, providerId, apiKey, preset?.endpointConfigs), [
			provider,
			providerId,
			apiKey,
			preset?.endpointConfigs
		]),
		isLoading: isLoading || isPresetLoading,
		error: error ?? presetError
	};
}
function hasOutput(painting) {
	return (painting.files?.length ?? 0) > 0;
}
function usePaintingGeneration({ painting, onPaintingChange }) {
	const { createPainting, updatePainting, refresh } = usePaintings();
	const currentProviderId = painting.providerId;
	const { provider } = usePaintingProviderRuntime(currentProviderId);
	const visibleIdRef = (0, import_react.useRef)(painting.id);
	(0, import_react.useEffect)(() => {
		visibleIdRef.current = painting.id;
	}, [painting.id]);
	const isGenerating = (0, import_react.useCallback)((p) => {
		return p.generationStatus === "running";
	}, []);
	const applyIfVisible = (0, import_react.useCallback)((next) => {
		if (visibleIdRef.current === next.id) onPaintingChange(next);
	}, [onPaintingChange]);
	return {
		generate: (0, import_react.useCallback)(async (inputFiles) => {
			const base = {
				...painting,
				inputFiles
			};
			const shouldCreate = hasOutput(base) || !base.persistedAt;
			const targetPainting = shouldCreate ? {
				...base,
				id: uuid(),
				files: hasOutput(base) ? [] : base.files
			} : { ...base };
			try {
				targetPainting.persistedAt = (shouldCreate ? await createPainting(paintingDataToCreateDto(targetPainting)) : await updatePainting(targetPainting.id, paintingDataToUpdateDto(targetPainting))).createdAt;
			} catch (error) {
				presentPaintingGenerateError(error);
				return;
			}
			const generationState = {
				generationStatus: "running",
				generationTaskId: null,
				generationError: null,
				generationProgress: 0
			};
			const controller = new AbortController();
			const cacheKey = `painting.generation.${targetPainting.id}`;
			const pushGenerationState = (updates) => {
				Object.assign(generationState, updates, { generationStatus: "running" });
				cacheService.set(cacheKey, paintingGenerationStateToCache(generationState));
				applyIfVisible({
					...targetPainting,
					...generationState
				});
			};
			visibleIdRef.current = targetPainting.id;
			onPaintingChange({
				...targetPainting,
				...generationState
			});
			registerPaintingAbortController(targetPainting.id, controller);
			pushGenerationState(generationState);
			try {
				const generatedFiles = await paintingGenerate({
					painting: targetPainting,
					provider,
					tab: "default",
					abortController: controller
				});
				await updatePainting(targetPainting.id, { files: {
					output: generatedFiles.map((file) => file.id),
					input: targetPainting.inputFiles?.map((entry) => entry.id) ?? []
				} });
				cacheService.set(cacheKey, null);
				applyIfVisible({
					...targetPainting,
					files: generatedFiles
				});
				await refresh();
			} catch (error) {
				const isCanceled = controller.signal.aborted || error instanceof Error && error.name === "AbortError";
				const failedState = {
					...generationState,
					generationStatus: isCanceled ? "canceled" : "failed",
					generationError: isCanceled ? null : error instanceof Error ? error.message : String(error)
				};
				cacheService.set(cacheKey, paintingGenerationStateToCache(failedState));
				applyIfVisible({
					...targetPainting,
					...failedState
				});
				if (!isCanceled) presentPaintingGenerateError(error);
			} finally {
				clearPaintingAbortController(targetPainting.id, controller);
			}
		}, [
			applyIfVisible,
			createPainting,
			painting,
			provider,
			refresh,
			onPaintingChange,
			updatePainting
		]),
		cancel: (0, import_react.useCallback)((paintingId) => {
			abortPaintingGeneration(paintingId);
		}, []),
		generating: isGenerating(painting)
	};
}
function usePaintingGenerationGuard({ painting, ensureCurrentCatalog }) {
	const providerId = painting.providerId;
	const modelId = painting.model;
	const { provider } = usePaintingProviderRuntime(providerId);
	return { validateBeforeGenerate: (0, import_react.useCallback)(async () => {
		if (!provider.isEnabled) return {
			ok: false,
			reason: "provider_disabled"
		};
		if (!modelId) return {
			ok: false,
			reason: "model_missing"
		};
		let ensuredOptions;
		try {
			ensuredOptions = await ensureCurrentCatalog();
		} catch (error) {
			return {
				ok: false,
				reason: "catalog_error",
				error: error instanceof Error ? error : /* @__PURE__ */ new Error("Failed to load painting models")
			};
		}
		const ensuredOption = ensuredOptions.find((option) => option.value === modelId);
		if (!ensuredOption || ensuredOption.isEnabled === false) return {
			ok: false,
			reason: "model_unavailable"
		};
		return { ok: true };
	}, [
		ensureCurrentCatalog,
		modelId,
		provider
	]) };
}
function usePaintingGenerationSubmit({ painting, onPaintingChange, ensureCurrentCatalog }) {
	const { validateBeforeGenerate } = usePaintingGenerationGuard({
		painting,
		ensureCurrentCatalog
	});
	const { generate, cancel, generating } = usePaintingGeneration({
		painting,
		onPaintingChange
	});
	const submittingRef = (0, import_react.useRef)(false);
	const [submitting, setSubmitting] = (0, import_react.useState)(false);
	return {
		generating,
		submitting,
		submit: (0, import_react.useCallback)(async (materialize) => {
			if (generating || submittingRef.current) return;
			submittingRef.current = true;
			setSubmitting(true);
			try {
				const guardResult = await validateBeforeGenerate();
				if (!guardResult.ok) {
					presentPaintingGenerationGuardFeedback(guardResult.reason, guardResult.error, painting.providerId);
					return;
				}
				const { entries, complete } = await materialize();
				if (!complete) return;
				await generate(entries);
			} finally {
				submittingRef.current = false;
				setSubmitting(false);
			}
		}, [
			generate,
			generating,
			painting.providerId,
			validateBeforeGenerate
		]),
		cancel
	};
}
var logger$5 = loggerService.withContext("paintings/recordToPaintingData");
function normalizeStoredPaintingModel(value) {
	if (value == null) return void 0;
	if (typeof value !== "string") return void 0;
	const trimmed = value.trim();
	if (!trimmed) return void 0;
	if (isUniqueModelId(trimmed)) try {
		return parseUniqueModelId(trimmed).modelId;
	} catch {
		return trimmed;
	}
	return trimmed;
}
async function resolveEntries(ids) {
	if (ids.length === 0) return [];
	return (await Promise.all(ids.map(async (id) => {
		try {
			return await dataApiService.get(`/files/entries/${id}`);
		} catch (error) {
			logger$5.warn("Skipping unresolved file_entry for painting", {
				id,
				error
			});
			return null;
		}
	}))).filter((e) => e !== null);
}
async function recordToPaintingData(record) {
	const outputEntries = await resolveEntries(record.files.output);
	const inputFiles = await resolveEntries(record.files.input);
	const files = await Promise.all(outputEntries.map(fileEntryToMetadata));
	const model = normalizeStoredPaintingModel(record.modelId);
	return {
		id: record.id,
		providerId: record.providerId,
		mode: "generate",
		prompt: record.prompt,
		files,
		inputFiles,
		persistedAt: record.createdAt,
		model
	};
}
function recordsToPaintingDataList(records) {
	return Promise.all(records.map(recordToPaintingData));
}
var PAGE_SIZE = 30;
var logger$4 = loggerService.withContext("usePaintingHistory");
function usePaintingHistory() {
	const { pages, isLoading: isQueryLoading, isRefreshing, hasNext, loadNext } = useInfiniteQuery("/paintings", { limit: PAGE_SIZE });
	const records = useInfiniteFlatItems(pages);
	const [hydration, setHydration] = (0, import_react.useState)(null);
	(0, import_react.useEffect)(() => {
		let cancelled = false;
		recordsToPaintingDataList(records).then((mapped) => {
			if (!cancelled) setHydration({
				records,
				items: mapped
			});
		}).catch((error) => {
			if (cancelled) return;
			logger$4.error("Failed to hydrate painting history", error);
			setHydration({
				records,
				items: []
			});
		});
		return () => {
			cancelled = true;
		};
	}, [records]);
	const currentHydration = hydration?.records === records ? hydration : null;
	return {
		items: hydration?.items ?? [],
		isLoading: isQueryLoading || isRefreshing || !currentHydration,
		hasMore: hasNext,
		loadMore: loadNext
	};
}
function isUntouchedDraft(painting) {
	return !painting.persistedAt && !painting.model && !painting.prompt && painting.files.length === 0 && (painting.inputFiles?.length ?? 0) === 0 && Object.keys(painting.params ?? {}).length === 0 && !painting.generationStatus;
}
function usePaintingInitialDraft({ currentPainting, initialProviderId, setCurrentPainting }) {
	const bootstrapDraftIdRef = (0, import_react.useRef)(currentPainting.id);
	(0, import_react.useEffect)(() => {
		if (currentPainting.id !== bootstrapDraftIdRef.current) return;
		if (currentPainting.persistedAt || !isUntouchedDraft(currentPainting)) return;
		if (initialProviderId && currentPainting.providerId !== initialProviderId) {
			const nextPainting = createDefaultPainting(initialProviderId);
			bootstrapDraftIdRef.current = nextPainting.id;
			setCurrentPainting(nextPainting);
		}
	}, [
		currentPainting,
		initialProviderId,
		setCurrentPainting
	]);
}
function getValidPaintingOptions(options, isOvmsSupported, ovmsStatus) {
	return options.filter((option) => {
		if (option === "ovms") return isOvmsSupported && ovmsStatus === "running";
		return true;
	});
}
function resolvePaintingProvider(requestedProvider, defaultProvider, validOptions) {
	if (requestedProvider && validOptions.includes(requestedProvider)) return requestedProvider;
	if (defaultProvider && validOptions.includes(defaultProvider)) return defaultProvider;
	return validOptions[0];
}
var FALLBACK_PROVIDER = "zhipu";
function usePaintingInitialProvider(providerOptions) {
	const [defaultPaintingProvider, setDefaultPaintingProvider] = usePreference("feature.paintings.default_provider");
	const initialProviderId = (0, import_react.useMemo)(() => resolvePaintingProvider(void 0, defaultPaintingProvider ?? void 0, providerOptions) ?? FALLBACK_PROVIDER, [defaultPaintingProvider, providerOptions]);
	(0, import_react.useEffect)(() => {
		if (!defaultPaintingProvider && providerOptions.length > 0) setDefaultPaintingProvider(initialProviderId);
	}, [
		defaultPaintingProvider,
		initialProviderId,
		providerOptions,
		setDefaultPaintingProvider
	]);
	return { initialProviderId };
}
var logger$3 = loggerService.withContext("paintings/usePaintingList");
function usePaintingList({ painting, setCurrentPainting, currentProviderId, modelOptions, historyItems, cancelGeneration }) {
	const { updatePainting, deletePainting, refresh } = usePaintings();
	const modelOptionsRef = (0, import_react.useRef)([]);
	const historyItemsRef = (0, import_react.useRef)([]);
	const paintingRef = (0, import_react.useRef)(painting);
	modelOptionsRef.current = modelOptions;
	historyItemsRef.current = historyItems;
	paintingRef.current = painting;
	const saveCurrent = (0, import_react.useCallback)(async () => {
		const current = paintingRef.current;
		if (!current.persistedAt) return true;
		try {
			await updatePainting(current.id, paintingDataToUpdateDto(current));
			return true;
		} catch (error) {
			presentPaintingGenerateError(error);
			return false;
		}
	}, [updatePainting]);
	const select = (0, import_react.useCallback)(async (target) => {
		const current = paintingRef.current;
		if (target.id === current.id) return;
		if (!await saveCurrent()) return;
		setCurrentPainting(target);
	}, [saveCurrent, setCurrentPainting]);
	const add = (0, import_react.useCallback)(() => {
		setCurrentPainting(createDefaultPainting(currentProviderId));
	}, [currentProviderId, setCurrentPainting]);
	const selectNextAfterDelete = (0, import_react.useCallback)(async (deletedId) => {
		const currentItems = historyItemsRef.current;
		const deletedIndex = currentItems.findIndex((item) => item.id === deletedId);
		const nextPainting = deletedIndex >= 0 ? currentItems[deletedIndex + 1] ?? currentItems[deletedIndex - 1] : currentItems.find((item) => item.id !== deletedId);
		await refresh();
		if (nextPainting) {
			setCurrentPainting(nextPainting);
			return;
		}
		add();
	}, [
		add,
		refresh,
		setCurrentPainting
	]);
	return {
		add,
		remove: (0, import_react.useCallback)(async (target) => {
			cancelGeneration(target.id);
			try {
				await deletePainting(target.id);
			} catch (error) {
				logger$3.error("Failed to delete painting", error);
				presentPaintingGenerateError(error);
				return;
			}
			if (target.id === painting.id) await selectNextAfterDelete(target.id);
			else await refresh();
		}, [
			cancelGeneration,
			deletePainting,
			painting.id,
			refresh,
			selectNextAfterDelete
		]),
		select,
		saveCurrent
	};
}
function createSelectorProvider(providerId, provider) {
	return {
		id: providerId,
		presetProviderId: provider?.presetProviderId,
		name: getProviderDisplayName(provider) || providerId,
		apiKeys: [],
		authType: "api-key",
		apiFeatures: DEFAULT_API_FEATURES,
		settings: {},
		isEnabled: provider?.isEnabled ?? false
	};
}
function usePaintingModelCatalog({ providerOptions, painting }) {
	const currentProviderId = painting.providerId;
	const normalizedApiModelId = painting.model?.trim() ?? "";
	const { providers: dataProviders } = useProviders();
	const { models: dataModels, isLoading } = useModels();
	const providerMap = (0, import_react.useMemo)(() => new Map(dataProviders.map((provider) => [provider.id, provider])), [dataProviders]);
	const optionsByProvider = (0, import_react.useMemo)(() => {
		const map = /* @__PURE__ */ new Map();
		for (const providerId of providerOptions) map.set(providerId, getPaintingModelOptions(providerId, dataModels));
		return map;
	}, [dataModels, providerOptions]);
	const { selectorData, modelOptionMap } = (0, import_react.useMemo)(() => {
		const providers = [];
		const models = [];
		const seenProviderIds = /* @__PURE__ */ new Set();
		const seenModelIds = /* @__PURE__ */ new Set();
		const optionMap = /* @__PURE__ */ new Map();
		for (const providerId of providerOptions) {
			const providerModelOptions = optionsByProvider.get(providerId) ?? [];
			if (providerModelOptions.length === 0) continue;
			const provider = providerMap.get(providerId);
			if (!seenProviderIds.has(providerId)) {
				seenProviderIds.add(providerId);
				providers.push(createSelectorProvider(providerId, provider));
			}
			for (const modelOption of providerModelOptions) {
				const modelId = String(modelOption.value || "").trim();
				if (!modelId) continue;
				const uniqueModelId = createUniqueModelId(providerId, modelId);
				if (seenModelIds.has(uniqueModelId)) continue;
				seenModelIds.add(uniqueModelId);
				optionMap.set(uniqueModelId, modelOption);
				models.push({
					id: uniqueModelId,
					providerId,
					apiModelId: modelId,
					name: modelOption.label || modelId,
					group: modelOption.group,
					capabilities: [MODEL_CAPABILITY.IMAGE_GENERATION],
					supportsStreaming: false,
					isEnabled: modelOption.isEnabled ?? true,
					isHidden: false
				});
			}
		}
		let selectedModelId;
		if (normalizedApiModelId.length > 0) {
			const uniqueModelId = createUniqueModelId(currentProviderId, normalizedApiModelId);
			selectedModelId = uniqueModelId;
			if (!seenModelIds.has(uniqueModelId)) {
				const currentProvider = providerMap.get(currentProviderId);
				if (!seenProviderIds.has(currentProviderId)) providers.unshift(createSelectorProvider(currentProviderId, currentProvider));
				models.unshift({
					id: uniqueModelId,
					providerId: currentProviderId,
					apiModelId: normalizedApiModelId,
					name: normalizedApiModelId,
					capabilities: [MODEL_CAPABILITY.IMAGE_GENERATION],
					supportsStreaming: false,
					isEnabled: false,
					isHidden: false
				});
			}
		}
		const selectedModel = selectedModelId ? models.find((model) => model.id === selectedModelId) : void 0;
		const selectedProvider = selectedModel ? providers.find((provider) => provider.id === selectedModel.providerId) : void 0;
		const fallbackLabel = normalizedApiModelId.length > 0 ? optionMap.get(createUniqueModelId(currentProviderId, normalizedApiModelId))?.label ?? normalizedApiModelId : void 0;
		return {
			selectorData: {
				providers,
				models,
				selectedModelId,
				selectedModelName: selectedModel?.name ?? fallbackLabel,
				selectedProviderName: selectedProvider?.name
			},
			modelOptionMap: optionMap
		};
	}, [
		optionsByProvider,
		providerOptions,
		providerMap,
		currentProviderId,
		normalizedApiModelId
	]);
	const getModelOption = (0, import_react.useCallback)((providerId, modelId) => modelOptionMap.get(createUniqueModelId(providerId, modelId)), [modelOptionMap]);
	const ensureProviderCatalog = (0, import_react.useCallback)(async (providerId) => {
		const cached = optionsByProvider.get(providerId);
		if (cached && cached.length > 0) return cached;
		return loadPaintingModelOptions(providerId);
	}, [optionsByProvider]);
	const ensureCurrentCatalog = (0, import_react.useCallback)(() => ensureProviderCatalog(currentProviderId), [currentProviderId, ensureProviderCatalog]);
	return {
		selectorData,
		currentModelOptions: (0, import_react.useMemo)(() => optionsByProvider.get(currentProviderId) ?? [], [optionsByProvider, currentProviderId]),
		selectedModelOption: (0, import_react.useMemo)(() => normalizedApiModelId.length ? getModelOption(currentProviderId, normalizedApiModelId) : void 0, [
			normalizedApiModelId,
			currentProviderId,
			getModelOption
		]),
		isLoading,
		getModelOption,
		ensureProviderCatalog,
		ensureCurrentCatalog
	};
}
var logger$2 = loggerService.withContext("paintings/modelFieldReset");
async function computeModelFieldReset(input) {
	const { providerId, oldModelId, newModelId, mode, currentValues = {} } = input;
	if (oldModelId && oldModelId === newModelId) return {};
	const fetchSupport = async (modelId) => {
		try {
			return await prefetch("/providers/:providerId/models/:modelId*/image-generation-support", { params: {
				providerId,
				modelId
			} }) ?? void 0;
		} catch (error) {
			logger$2.warn("Failed to prefetch image-generation-support", {
				providerId,
				modelId,
				error
			});
			return;
		}
	};
	const [oldSupport, newSupport] = await Promise.all([oldModelId ? fetchSupport(oldModelId) : Promise.resolve(void 0), fetchSupport(newModelId)]);
	const oldItems = oldSupport ? imageGenerationToFields(oldSupport, { mode }) : [];
	const newItems = newSupport ? imageGenerationToFields(newSupport, { mode }) : [];
	if (newItems.length === 0) return {};
	const collectKeys = (items) => {
		const keys = /* @__PURE__ */ new Set();
		for (const item of items) {
			if (item.key) keys.add(item.key);
			const widget = item;
			if (widget.widthKey) keys.add(widget.widthKey);
			if (widget.heightKey) keys.add(widget.heightKey);
			if (widget.sizeKey) keys.add(widget.sizeKey);
		}
		return keys;
	};
	const oldKeys = collectKeys(oldItems);
	const newKeys = collectKeys(newItems);
	const patch = {};
	for (const key of oldKeys) if (!newKeys.has(key)) patch[key] = void 0;
	for (const item of newItems) {
		if (!item.key) continue;
		if (Object.prototype.hasOwnProperty.call(patch, item.key)) continue;
		const currentValue = currentValues[item.key];
		if (currentValue === void 0 || currentValue === null || currentValue === "") {
			if (item.initialValue !== void 0) patch[item.key] = item.initialValue;
			continue;
		}
		const options = typeof item.options === "function" ? item.options(item, currentValues) : item.options ?? [];
		if (options.length > 0) {
			if (!new Set(options.map((option) => String(option.value))).has(String(currentValue))) patch[item.key] = item.initialValue;
			continue;
		}
		if (item.type === "slider") {
			const numeric = typeof currentValue === "number" ? currentValue : Number(currentValue);
			if (Number.isNaN(numeric) || typeof item.min === "number" && numeric < item.min || typeof item.max === "number" && numeric > item.max) patch[item.key] = item.initialValue;
		}
	}
	return patch;
}
var logger$1 = loggerService.withContext("paintings/usePaintingModelSwitch");
function usePaintingModelSwitch({ painting, onPaintingChange, ensureProviderCatalog }) {
	const currentProviderId = painting.providerId;
	const { models } = useModels(currentProviderId ? { providerId: currentProviderId } : void 0);
	return (0, import_react.useCallback)(async ({ providerId, modelId }) => {
		if (providerId === currentProviderId) {
			const resetPatch = await computeModelFieldReset({
				providerId: currentProviderId,
				oldModelId: painting.model,
				newModelId: modelId,
				mode: tabToImageGenerationMode(painting.mode),
				currentValues: painting.params ?? {}
			});
			const nextModel$1 = models.find((model) => model.apiModelId === modelId);
			const keepInputFiles$1 = !nextModel$1 || isEditImageModel(nextModel$1);
			onPaintingChange({
				params: {
					...painting.params,
					...resetPatch
				},
				model: modelId,
				...keepInputFiles$1 ? {} : { inputFiles: [] }
			});
			return;
		}
		onPaintingChange({
			providerId,
			model: modelId
		});
		let catalog;
		try {
			catalog = await ensureProviderCatalog(providerId);
		} catch (error) {
			logger$1.error("Failed to load provider catalog on model switch", error);
			presentPaintingGenerateError(error);
			return;
		}
		const targetPainting = createDefaultPainting(providerId);
		const nextModel = catalog.find((option) => String(option.value || "").trim() === modelId)?.raw;
		const keepInputFiles = nextModel ? isEditImageModel(nextModel) : false;
		onPaintingChange({
			...targetPainting,
			id: painting.id,
			files: painting.files,
			prompt: painting.prompt,
			providerId,
			mode: "generate",
			model: modelId,
			inputFiles: keepInputFiles ? painting.inputFiles ?? [] : []
		});
	}, [
		currentProviderId,
		ensureProviderCatalog,
		models,
		onPaintingChange,
		painting
	]);
}
var DEFAULT_OVMS_STATE = {
	supported: false,
	status: "not-running"
};
var logger = loggerService.withContext("usePaintingProviderOptions");
var cachedOvmsState;
var inflightOvmsPromise;
async function loadOvmsState() {
	if (cachedOvmsState) return cachedOvmsState;
	if (!inflightOvmsPromise) inflightOvmsPromise = (async () => {
		try {
			const supported = await ipcApi.request("ovms.is_supported");
			cachedOvmsState = {
				supported,
				status: supported ? await ipcApi.request("ovms.get_status") : "not-running"
			};
			return cachedOvmsState;
		} finally {
			inflightOvmsPromise = void 0;
		}
	})();
	return inflightOvmsPromise;
}
function buildPaintingProviderOptions(input) {
	const capabilityProviderIds = /* @__PURE__ */ new Set();
	for (const model of input.models) if (supportsImageGenerationEndpoint(model)) capabilityProviderIds.add(model.providerId);
	return getValidPaintingOptions([...new Set([...[...capabilityProviderIds].sort(), ...input.newApiProviderIds])], input.ovmsSupported, input.ovmsStatus);
}
function usePaintingProviderOptions() {
	const { providers: allProviders } = useProviders();
	const { models } = useModels();
	const [ovmsState, setOvmsState] = (0, import_react.useState)(() => cachedOvmsState ?? DEFAULT_OVMS_STATE);
	(0, import_react.useEffect)(() => {
		if (cachedOvmsState) return;
		let cancelled = false;
		loadOvmsState().then((state) => {
			if (!cancelled) setOvmsState(state);
		}).catch((error) => {
			logger.warn("Failed to load OVMS state", error);
			if (!cancelled) setOvmsState(DEFAULT_OVMS_STATE);
		});
		return () => {
			cancelled = true;
		};
	}, []);
	return (0, import_react.useMemo)(() => {
		return buildPaintingProviderOptions({
			models,
			newApiProviderIds: allProviders.filter(isPaintingNewApiProvider).map((provider) => provider.id),
			ovmsSupported: ovmsState.supported,
			ovmsStatus: ovmsState.status
		});
	}, [
		allProviders,
		models,
		ovmsState
	]);
}
function usePaintingResultSync({ currentPainting, historyItems, setCurrentPainting }) {
	const currentId = currentPainting.id;
	const localFileCount = currentPainting.files.length;
	const historyFiles = historyItems.find((item) => item.id === currentId)?.files;
	(0, import_react.useEffect)(() => {
		if (!historyFiles || historyFiles.length <= localFileCount) return;
		setCurrentPainting((prev) => {
			if (prev.id !== currentId || prev.files.length >= historyFiles.length) return prev;
			return {
				...prev,
				files: historyFiles
			};
		});
	}, [
		currentId,
		localFileCount,
		historyFiles,
		setCurrentPainting
	]);
}
var PAINTING_TEMPLATE_RESOURCE_DIRECTORY = "data/painting-templates";
var PAINTING_TEMPLATE_ID_PATTERN = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;
function normalizeManifest(value) {
	if (!Array.isArray(value)) return [];
	return value.filter((item) => typeof item === "string" && PAINTING_TEMPLATE_ID_PATTERN.test(item));
}
function normalizeTranslations(value) {
	if (!value || typeof value !== "object" || Array.isArray(value)) return {};
	return Object.fromEntries(Object.entries(value).filter((entry) => {
		const translation = entry[1];
		return Boolean(translation && typeof translation === "object" && typeof translation.label === "string" && typeof translation.prompt === "string");
	}));
}
function getLocaleFileName(language) {
	return language.toLowerCase() === "zh-cn" ? "zh-cn.json" : "en-us.json";
}
function shuffleTemplates(templates) {
	const shuffled = [...templates];
	for (let index = shuffled.length - 1; index > 0; index -= 1) {
		const randomIndex = Math.floor(Math.random() * (index + 1));
		const current = shuffled[index];
		const replacement = shuffled[randomIndex];
		if (!current || !replacement) continue;
		shuffled[index] = replacement;
		shuffled[randomIndex] = current;
	}
	return shuffled;
}
async function loadPaintingTemplateCatalog(resourcesPath, language) {
	const resourceRoot = joinPath(resourcesPath, PAINTING_TEMPLATE_RESOURCE_DIRECTORY);
	const [manifestContent, translationContent] = await Promise.all([window.api.fs.read(joinPath(resourceRoot, "catalog.json"), "utf-8"), window.api.fs.read(joinPath(resourceRoot, `locales/${getLocaleFileName(language)}`), "utf-8")]);
	const manifest = normalizeManifest(JSON.parse(manifestContent));
	const translations = normalizeTranslations(JSON.parse(translationContent));
	return shuffleTemplates(manifest.map((id) => {
		const translation = translations[id];
		if (!translation) throw new Error(`Missing painting template translation: ${id}`);
		const previewPath = AbsoluteFilePathSchema.parse(joinPath(resourceRoot, `images/${id}.webp`));
		const runtimeWindow = window;
		return {
			id,
			imageUrl: runtimeWindow.__CHERRY_BACKEND_URL ? `${runtimeWindow.__CHERRY_BACKEND_URL.replace(/\/$/, "")}/api/v1/files/raw-image?path=${encodeURIComponent(previewPath)}` : toFileUrl(previewPath),
			...translation
		};
	}));
}
function usePaintingTemplateCatalog() {
	const { items: templates } = useBundledCatalog({
		catalog: "painting templates",
		load: loadPaintingTemplateCatalog
	});
	return { templates };
}
var PaintingPage = () => {
	const { t } = useTranslation();
	const { templates: promptPresets } = usePaintingTemplateCatalog();
	const providerOptions = usePaintingProviderOptions();
	const { initialProviderId } = usePaintingInitialProvider(providerOptions);
	const [currentPainting, setCurrentPainting] = (0, import_react.useState)(() => createDefaultPainting(initialProviderId));
	const patchPainting = (0, import_react.useCallback)((updates) => {
		setCurrentPainting((current) => ({
			...current,
			...updates
		}));
	}, []);
	const history = usePaintingHistory();
	usePaintingInitialDraft({
		currentPainting,
		initialProviderId,
		setCurrentPainting
	});
	usePaintingResultSync({
		currentPainting,
		historyItems: history.items,
		setCurrentPainting
	});
	const [cachedGeneration] = useCache(`painting.generation.${currentPainting.id}`);
	const liveGenerationState = (0, import_react.useMemo)(() => cacheToPaintingGenerationState(cachedGeneration), [cachedGeneration]);
	const currentProviderId = currentPainting.providerId || initialProviderId;
	const modelCatalog = usePaintingModelCatalog({
		providerOptions,
		painting: currentPainting
	});
	const composerPainting = (0, import_react.useMemo)(() => {
		if (currentPainting.model) return currentPainting;
		const fallback = modelCatalog.currentModelOptions[0]?.value;
		return fallback ? {
			...currentPainting,
			model: String(fallback)
		} : currentPainting;
	}, [currentPainting, modelCatalog.currentModelOptions]);
	const { generating: liveGenerating, submitting, submit, cancel: cancelGeneration } = usePaintingGenerationSubmit({
		painting: composerPainting,
		onPaintingChange: setCurrentPainting,
		ensureCurrentCatalog: modelCatalog.ensureCurrentCatalog
	});
	const generating = liveGenerating || liveGenerationState.generationStatus === "running";
	const showTemplateShowcase = !currentPainting.persistedAt && currentPainting.files.length === 0 && !submitting && !generating && !currentPainting.generationStatus && !liveGenerationState.generationStatus;
	const switchModel = usePaintingModelSwitch({
		painting: currentPainting,
		onPaintingChange: patchPainting,
		ensureProviderCatalog: modelCatalog.ensureProviderCatalog
	});
	const list = usePaintingList({
		painting: currentPainting,
		setCurrentPainting,
		currentProviderId,
		modelOptions: modelCatalog.currentModelOptions,
		historyItems: history.items,
		cancelGeneration
	});
	const onCancel = (0, import_react.useCallback)(() => cancelGeneration(currentPainting.id), [cancelGeneration, currentPainting.id]);
	const saveCurrentRef = (0, import_react.useRef)(list.saveCurrent);
	saveCurrentRef.current = list.saveCurrent;
	(0, import_react.useEffect)(() => {
		return () => {
			saveCurrentRef.current();
		};
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-ui": "paintings.view",
		className: paintingClasses.page,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			"data-ui": "paintings.painting-page.content-container",
			id: "content-container",
			className: paintingClasses.content,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: "flex h-full flex-1 flex-col",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: paintingClasses.frame,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: paintingClasses.surface,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingStrip_default, {
							selectedPaintingId: currentPainting.id,
							runningPaintingId: generating ? currentPainting.id : void 0,
							items: history.items,
							hasMore: history.hasMore,
							loadMore: history.loadMore,
							onDeletePainting: list.remove,
							onSelectPainting: list.select,
							onAddPainting: list.add
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: paintingClasses.centerPane,
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: paintingClasses.centerStage,
									children: !showTemplateShowcase && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Artboard_default, {
										painting: composerPainting,
										isLoading: generating
									})
								}),
								showTemplateShowcase && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
									"data-ui": "paintings.painting-page.painting-template-stage",
									"data-testid": "painting-template-stage",
									className: "absolute inset-0 z-0 mx-auto flex min-h-0 w-full max-w-5xl items-center justify-center overflow-hidden px-3 pt-3 pb-36 [container-type:size]",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: "flex h-full max-h-80 min-h-0 w-full flex-col items-center",
										children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h1", {
											className: "max-w-xl shrink-0 text-center font-bold tracking-tight [font-size:clamp(var(--font-size-heading-sm),4cqw,var(--font-size-heading-md))] [line-height:1.1]",
											children: t("paintings.showcase.title")
										}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
											className: "mt-[clamp(8px,5cqh,30px)] flex min-h-0 w-full flex-1 flex-col items-center",
											children: [promptPresets.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingTemplateShowcase_default, {
												paintingId: composerPainting.id,
												prompt: composerPainting.prompt,
												templates: promptPresets,
												onSelect: (prompt) => patchPainting({ prompt })
											}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Artboard_default, {
												painting: composerPainting,
												isLoading: false
											}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
												className: "mt-[clamp(4px,2cqh,10px)] max-w-lg shrink-0 px-4 pb-1 text-center text-muted-foreground text-xs leading-5",
												children: t("paintings.showcase.caption")
											})]
										})]
									})
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: paintingClasses.promptDock,
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
										className: "mx-auto w-full max-w-5xl",
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(QuickPanelProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PaintingComposer_default, {
											painting: composerPainting,
											generating,
											submitting,
											onPromptChange: (prompt) => patchPainting({ prompt }),
											onGenerate: submit,
											onCancel,
											onModelSelect: switchModel,
											onConfigChange: patchPainting,
											onGenerateRandomSeed: (key) => patchPainting({ params: {
												...currentPainting.params,
												[key]: String(Math.floor(Math.random() * 1e6))
											} })
										}) })
									})
								})
							]
						})]
					})
				})
			})
		})
	});
};
var PaintingPage_default = PaintingPage;
export { PaintingPage_default as t };
